/**
 * @header {
 *   "module": "Old",
 *   "layer": "util",
 *   "domain": "demo",
 *   "description": "config.exclude 전용 제외 디렉토리(svc/thirdparty) 하위 파일 — 인라인 헤더로 자기완결, files_key_removed 대칭성 전용 검증(태스크 077, RED-EVIDENCE-5, req2)",
 *   "exports": ["Old"]
 * }
 */
package svc.thirdparty;
public class Old {}
