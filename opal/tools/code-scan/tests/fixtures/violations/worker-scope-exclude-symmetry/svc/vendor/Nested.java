/**
 * @header {
 *   "module": "Nested",
 *   "layer": "util",
 *   "domain": "demo",
 *   "description": "index.exclude 전용 제외 디렉토리(svc/vendor) 하위 파일 — 인라인 헤더로 자기완결(uncovered 무관), files_key_removed 대칭성 전용 검증(태스크 077, RED-EVIDENCE-5, req3)",
 *   "exports": ["Nested"]
 * }
 */
package svc.vendor;
public class Nested {}
