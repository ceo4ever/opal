package svc.mod;
// fixture: uncovered:incomplete — manifest entry exists but "description" field is missing
// (required 5-field completeness check must fail: module/layer/domain present via rule/domain
// tier + stem, but description is absent).
public class Partial {}
