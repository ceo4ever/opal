package svc.shared;

// fixture: mixed-scope-ambiguous member — matches both order-svc and ship-svc include (*.java) → scope_ambiguous
// (mirrors mixed-scope/svc/shared/VendorLegacy.java for structural parity; unregistered in either manifest)

public class VendorLegacy {
    public void legacyOp() {
    }
}
