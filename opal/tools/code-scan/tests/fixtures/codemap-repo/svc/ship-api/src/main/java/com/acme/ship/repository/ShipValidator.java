package com.acme.ship.repository;

// fixture: second file in same package-tier directory — used to prove package.depends inheritance
// propagates to 2+ files (H-2 / S-20 depends snapshot).
public class ShipValidator {
}
