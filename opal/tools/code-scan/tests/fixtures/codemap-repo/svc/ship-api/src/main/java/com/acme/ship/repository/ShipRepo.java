package com.acme.ship.repository;

// fixture: no inline @-header (no real header block), no file-tier manifest fields — inherits entirely from package tier
// (package.depends = ["ship-common"]) → resolveHeader _source must be "package" alone.
public class ShipRepo {
}
