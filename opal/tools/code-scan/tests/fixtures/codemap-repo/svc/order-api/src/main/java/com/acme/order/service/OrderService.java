package com.acme.order.service;

// fixture: no inline @-header (no real header block) — covered by code-map file tier (manifest entry has full fields)
public class OrderService {
    public void createOrder() {
        // no-op fixture body
    }
}
