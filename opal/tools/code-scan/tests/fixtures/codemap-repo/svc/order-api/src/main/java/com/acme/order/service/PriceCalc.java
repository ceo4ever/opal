package com.acme.order.service;

// fixture: no inline @-header (no real header block) — file tier entry fully filled too (both files clean in this dir)
public class PriceCalc {
    public double calculate() {
        return 0.0;
    }
}
