package com.acme.order.misc;

// fixture: no inline @-header (no real header block), no manifest entry — relies solely on tier⑤ (domains.order match,
// path does not match any layerRules pattern) → resolveHeader _source must be "domain" alone.
public class OrderMisc {
}
