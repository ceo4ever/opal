# fixture: no inline @-header (no real header block) — legacy readonly scope, covered by code-map file tier only.
# Used by S-8 (single-file reverse mapping / PM Gate 8 protection).


def helper():
    return None
