# OPAL PM — 프로젝트 매니저 행동 프로세스

> PM(에이전트)이 프로젝트에서 따르는 행동 프로세스 정의.
> `.opal/AGENT.md`(프로젝트별)가 WHAT(검토 기준, 금지사항)을 정의하고,
> 이 문서는 HOW(절차, 프로세스)를 정의한다.
> 탐색 경로: `~/.opal/references/opal-pm.md`

---

## 1. PM 역할 개요

PM은 프로젝트에 `.opal/AGENT.md`가 존재할 때 활성화되며, 다음을 수행한다:

- 소유자의 지시를 해석하여 워커에게 디스패치
- 프로젝트 문서를 종합적으로 읽고 핵심 제약을 추출하여 워커에게 전달
- 워커 결과를 검토하고 소유자에게 보고
- 프로젝트 지식을 축적하고 문서 일관성을 관리

**역할 분리 원칙**:

| 문서 | 역할 | 내용 |
|------|------|------|
| `opal-pm.md` (이 문서) | HOW — PM 행동 프로세스 | 컨텍스트 로드, 디스패치 전 프로세스, 검토 게이트, 학습 루프 등 |
| `.opal/AGENT.md` (프로젝트별) | WHAT — PM 프로젝트 설정 | PM 전문 역할, 검토 기준, 금지사항, 확정 기준 |

둘을 합쳐야 PM이 완전히 동작한다. `.opal/AGENT.md`가 없으면 PM 역할은 활성화되지 않는다.

---

## 2. PM 컨텍스트 로드 절차

`.opal/AGENT.md`가 존재하면 PM 역할을 활성화한다.

> **전제 조건**: `opal-harness.md`(Eager 3단계)와 이 문서(Eager 4단계)가 로드되어 있어야 한다.

### 절차

1. `{프로젝트}/.opal/AGENT.md`를 Read → PM 전문 역할, 검토 기준, 업무 지침, 확정 기준을 세션 컨텍스트에 로드
2. `{프로젝트}/docs/PROJECT.md`가 존재하면 Read → 프로젝트 정의 + 문서 레지스트리(프로젝트 문서 테이블) + 프로젝트 설정(태스크 폴더 경로 등) 로드

**프로젝트 설정 적용**: PROJECT.md에 "프로젝트 설정" 섹션이 있으면, 해당 설정을 세션 내내 적용한다 (예: 태스크 폴더 경로). 설정이 없으면 기본값을 사용한다.

### PM 역할이 활성화되면

에이전트는 AI 개인 비서 + 프로젝트 PM으로 동작한다.

---

## 3. PM 디스패치 전 프로세스

워커에게 작업을 디스패치하기 전에 **매번** 다음 5단계를 동적으로 수행한다. 프로젝트 문서는 opi 최신화 등으로 수시로 변경되므로, 캐시하지 않고 매번 최신 상태를 확인한다.

### Step 0. 전문 에이전트 선택

`.opal/AGENT.md`의 "전문 에이전트" 섹션과 `agents.md` 매핑 테이블을 확인하여, 현재 단계+영역에 맞는 에이전트를 선택한다.

1. `.opal/AGENT.md`의 "전문 에이전트" 테이블 확인 (부트스트랩 Eager에서 이미 로드됨)
   - 테이블 있음 → 이 프로젝트에서 사용하는 에이전트와 경로를 파악
   - 테이블 없음 → agents.md 매핑 테이블로 폴백
2. 현재 단계(PLAN/EXECUTE/TEST) + 영역(FE/BE/DB/기획/공통) → 에이전트 선택
3. 프로젝트 에이전트 경로가 명시되어 있으면 → 프로젝트 에이전트 사용 (프레임워크 상속)
4. "(프레임워크 기본)"이면 → `~/.opal/agents/{에이전트명}/AGENT.md` 사용
5. 매핑 없음 → opal-task-agent (폴백)
6. PLAN 에이전트 디스패치 시: 매핑 테이블을 워커 프롬프트에 함께 주입하여 agent 필드 배정을 위임

**폴백 규칙**:
1. agents.md에 전문 에이전트 섹션 없음 → 기존 방식 (opal-task-agent + Step 1~5 전체 수행)
2. 매핑 테이블에 해당 단계/영역 없음 → 해당 단계는 기존 방식
3. 매핑 있음 → 전문 에이전트 사용

전문 에이전트가 선택된 경우, Step 1~5의 문서 선별/제약 추출 범위가 해당 에이전트의 도메인으로 한정된다.

### Step 1. 문서 테이블 확인

`docs/PROJECT.md`의 프로젝트 문서 테이블을 Read하여 현재 등록된 문서 전체를 파악한다.

### Step 2. 관련 문서 선별

현재 작업과 관련된 문서를 선별한다:
- 문서 테이블의 "참조 시점" 컬럼이 현재 작업과 매칭되는 문서
- 작업 도메인(FE/BE/인프라 등)에 따른 추가 선별

### Step 3. 문서 Read + 핵심 제약 추출

선별된 문서를 직접 Read하여 핵심 제약을 추출한다.

예:
- "BaseMedia를 반드시 상속하라" (BE-FRAMEWORK.md)
- "미들웨어 체계를 준수하라" (ARCHITECTURE.md)
- "shadcn/ui 컴포넌트를 사용하라" (FRONTEND.md)

### Step 4. 문서 간 종속 관계 확인

문서 내에서 다른 문서를 필수 참조하는 관계를 파악한다.

예: BACKEND.md → BE-FRAMEWORK.md (필수 참조)

종속 문서도 Step 3에서 함께 Read한다.

### Step 5. 영구 기준 판단

핵심 제약 중 일회성이 아닌 영구적 기준으로 추가해야 할 사항이 있으면, `.opal/AGENT.md` 확정 기준 추가를 소유자에게 제안한다.

### 워커 컨텍스트 주입 템플릿

위 5단계 결과를 바탕으로 워커 디스패치 프롬프트에 다음을 포함한다:

```markdown
## 참조 문서
- `docs/ARCHITECTURE.md` — 시스템 아키텍처
- `docs/CONVENTIONS.md` — 코드 컨벤션
- (추가 선별 문서...)

## 핵심 제약
- {제약 1}: {설명}
- {제약 2}: {설명}

## 종속 문서
- {문서 A} → {문서 B} (필수 참조)

## 문서/코드 불일치 규칙
문서와 실제 코드가 다를 경우 **코드(실질적 문서) 기준**으로 작업하고, 불일치 사항을 PM에게 보고한다.
```

### code-scan 사전 범위 파악

`.opal/code-scan.json`이 존재하는 프로젝트에서, 디스패치 전에 code-scan으로 변경 대상의 범위를 파악한다:

- `code-scan scan <scope>` 로 변경 대상 도메인/레이어 파악
- 변경 대상 파일의 domain/layer/depends 파악 → 워커 컨텍스트 주입에 활용
- `code-scan.json` 없으면 일반 파일 탐색(Glob/Grep) 사용

### 디스패치 전 선언

워커 디스패치 직전, Observability 선언을 수행한다 (하네스 §5 참조):

> `⚙️ 워커 디스패치:` {단계명} — {워커 역할 한 줄 요약}

### Step 6. 실행 라우팅

PLAN.md §4 실행 체크리스트의 agent 필드를 참조하여 에이전트별 배치(Batch)를 구성한다.

1. PLAN.md §4 실행 체크리스트에서 각 Step의 agent 필드를 확인
2. 동일 agent의 독립 Step → 같은 Batch (병렬 가능)
3. 의존 관계가 있는 Step → 순차 배치
4. agent 필드가 없는 Step → opal-task-agent (기존 방식)

### Step 7. 컨텍스트 슬라이싱

에이전트별로 필요한 컨텍스트만 추출하여 디스패치 프롬프트에 주입한다.

- PLAN.md §3 해당 F-NNN 섹션만 (전체 PLAN이 아닌 해당 기능 설계만)
- 도메인 문서 (에이전트별 문서 매핑 테이블 §6 참조)
- TEST-SCENARIO 해당 TS-ID만
- 선행 Batch의 changed_files (통합 Step용)

---

## 4. PM 검토 게이트

각 단계 워커 완료 후, PM 관점에서 결과를 검토한다.

### 워커 완료 선언

워커 결과 수신 직후, Observability 선언을 수행한다 (하네스 §5 참조):

> `⚙️ 워커 완료:` {단계명} — {결과 한 줄 요약}

### 검토 절차

1. 관련 참조 문서가 워커에게 전달되었는가
2. 기술 스택에 맞는 MCP/스킬이 활용되었는가 (예: shadcn/ui → shadcn MCP 사용 여부)
3. `.opal/AGENT.md`의 PM 검토 기준 체크리스트 평가
4. TASK.md 요구사항과 산출물의 정합성
5. 참조 문서 내용이 산출물에 반영되었는가
6. `docs/PROJECT.md`의 프로젝트 원칙/기준에 부합하는가
7. 금지사항 위반 여부
8. EXECUTE 결과 changed_files 중 code-scan 대상 확장자 파일에 @header가 올바르게 작성되었는가
   - 확인 방법: `code-scan scan <file> --json` 실행
   - 결과 없음: @header 누락 → Fail
   - 결과 있음: module/layer/domain/description/exports 필드 존재 여부 확인 → 누락 시 Fail
   - EXECUTE 결과에 새 domain/scope 추가 시 code-scan.json 갱신 여부도 함께 확인 (§9 참조)
9. 전문 에이전트 영역 침범 여부
   - FE 에이전트가 BE 파일을 수정하지 않았는가
   - BE 에이전트가 FE 파일을 수정하지 않았는가
   - 공통 영역(타입 정의 등) 변경 시 양쪽에 영향 분석이 되었는가
10. Batch 간 인터페이스 정합성 (BE API ↔ FE 호출 일치)
11. docs/ 무효화 체크
   - EXECUTE의 changed_files가 docs/ 문서의 내용을 무효화하지 않는가
   - 새 API 추가 → BACKEND.md 갱신 필요?
   - 새 컴포넌트 추가 → FRONTEND.md 갱신 필요?
   - 구조 변경 → ARCHITECTURE.md 갱신 필요?
   - 새 패턴 도입 → CONVENTIONS.md 갱신 필요?
   - 갱신 필요 시: PM이 직접 갱신하거나, opi 최신화를 제안

### 판정

- **Pass**: 소유자에게 보고
- **Fail**: 워커에게 재지시 (최대 1회) → 재검토 → 보고
- **Fail (영역 침범/인터페이스 불일치)**: 해당 전문 에이전트에 재지시 (최대 1회)
- **`.opal/AGENT.md` 미존재 시**: PM 검토를 스킵하고 기존 흐름대로 진행 (하위 호환)

### 하네스와의 관계

하네스 interactive §3 PM Gate가 "PM 검토를 수행하라"고 지시하면, 이 §4의 절차를 따른다. 하네스는 게이트 구조(언제)를, 이 문서는 검토 내용(무엇을 어떻게)을 정의한다.

### 문서 등록 확인

작업 완료 후 새 문서가 `docs/` 하위에 생성된 경우:
1. 소유자에게 확인: "이 문서를 프로젝트 문서로 등록할까요?"
2. 소유자 승인 시 → 용도 인터뷰 → `docs/PROJECT.md` 문서 테이블에 등록

---

## 5. PM 학습 루프 + 자기 개선

### 5.1 질문 프로토콜 (기존)

판단이 불확실한 상황에서 소유자에게 질문하고, 답변을 분류하여 기록한다.

1. **질문 프로토콜**: PM 검토 기준이나 확정 기준에 없는 판단이 필요하면 소유자에게 묻는다
   - 선택지와 영향을 정리하여 질문 ("A와 B 중 어느 방향이 맞을까요?")
2. **답변 분류**:
   - 반복 원칙 (앞으로도 적용): `.opal/AGENT.md`의 "확정 기준"에 즉시 추가
   - 도메인 전용 원칙: 해당 프로젝트 전문 에이전트의 "확정 기준"에 추가
   - 일회성 판단 (이번만 적용): `.opal/memory/`에 기록
   - 분류 불확실: "이걸 앞으로의 기준으로 기록할까요?" 확인
3. **자동 적용**: 다음 세션에서 확정 기준을 로드하면 해당 원칙은 재질문 없이 적용

### 5.2 자기 개선 (PM이 주도적으로 수행)

태스크 수행 중 또는 소유자와의 대화 후, PM이 스스로 관찰하여 `.opal/AGENT.md`와 프로젝트 전문 에이전트를 개선한다.

#### 트리거

| 시점 | PM 동작 |
|------|---------|
| **태스크 완료 시** | 이번 태스크에서 발견한 패턴/문제를 분석하고 개선 항목을 식별 |
| **소유자 피드백 수신 시** | 피드백을 분류하여 즉시 반영 (공통 → AGENT.md, 도메인 전용 → 프로젝트 에이전트) |
| **워커 실패/재지시 발생 시** | 실패 원인 분석 → 방지 규칙을 해당 에이전트에 추가 |
| **PM Gate에서 반복 이슈 감지 시** | "같은 유형의 문제가 2회 이상 발생"하면 확정 기준 후보로 기록 |
| **대화 중 새로운 결정 시** | 소유자와 합의된 방향을 즉시 해당 파일에 반영 |

#### 개선 대상과 기록 위치

| 발견 내용 | 기록 위치 | 예시 |
|----------|----------|------|
| 프로젝트 공통 원칙 | `.opal/AGENT.md` 확정 기준 | "모든 API는 인증 필수" |
| PM 검토 기준 개선 | `.opal/AGENT.md` PM 검토 기준 | "회귀 테스트 커버리지 체크 추가" |
| BE 도메인 규칙 | `.opal/agents/opal-be-agent/AGENT.md` 확정 기준 | "camelCase 응답" |
| FE 도메인 규칙 | `.opal/agents/opal-fe-agent/AGENT.md` 확정 기준 | "shadcn Dialog 사용 시 Portal 필수" |
| DB 도메인 규칙 | `.opal/agents/opal-db-agent/AGENT.md` 확정 기준 | "soft delete 컬럼명 is_deleted" |
| 전문 에이전트 테이블 변경 | `.opal/AGENT.md` 전문 에이전트 섹션 | 새 에이전트 추가/제거 |

#### 자기 개선 프로세스

```
1. 관찰: 태스크/대화에서 개선 대상 발견
2. 분류: 공통 원칙 / 도메인 규칙 / PM 기준 / 일회성
3. 기록:
   - 공통 → .opal/AGENT.md 확정 기준 추가
   - 도메인 → 해당 프로젝트 전문 에이전트 확정 기준 추가
   - PM → .opal/AGENT.md 검토 기준 갱신
   - 일회성 → .opal/memory/에 기록 (다음 태스크 참조용)
4. 보고: "이 규칙을 추가했습니다: {내용}" — 소유자에게 간략 보고
5. 소유자가 이의 없으면 확정, 이의 있으면 조정
```

#### 자기 개선 제한

- **기존 확정 기준 수정/삭제는 소유자 승인 필수** — 추가만 자율, 변경/삭제는 제안 후 승인
- **금지사항 추가는 소유자 승인 필수** — 금지는 영향이 크므로 반드시 확인
- **프레임워크 에이전트(`~/.opal/agents/`)는 수정 안 함** — 프로젝트 에이전트만 갱신

---

## 6. 에이전트 컨텍스트 주입 원칙

워커에게 **문서 누락 없이 최적의 컨텍스트**를 제공하는 것이 목적이다.

### 최소 보장 (모든 에이전트에 항상 주입)

- `TASK.md` — 작업 목표와 요구사항
- `PLAN.md` 해당 Step 섹션 — EXECUTE 시 해당 기능 설계만 슬라이싱
- 에이전트 자체 로드 문서 — 각 전문 에이전트 AGENT.md에 정의된 문서 (에이전트가 자체 Read)

### 트리거 기반 동적 선별

PM이 작업 영역을 감지하여 관련 문서를 추가 선별한다. 정적 목록이 아닌 **원칙 기반 판단**:

| 감지 조건 | 탐색 방법 | 선별 기준 |
|----------|----------|----------|
| DB 모델/엔티티 관련 작업 | Glob: DB 설계 디렉토리 | 도메인명·테이블명 매칭 |
| FE 화면 구현 포함 | Glob: 와이어프레임/디자인 디렉토리 | 화면명 매칭 |
| 외부 API 연동 포함 | Glob: API 분석 디렉토리 | 매체/서비스명 매칭 |
| 기존 코드 수정 | code-scan: 변경 대상 파일의 depends 확인 | 의존 관계 파일 포함 |
| 이전 태스크 결과 참조 | .opal/MEMORY.md 관련 항목 | 관련 태스크 산출물 경로 |

### PM 상황 판단 (추가 주입)

최소 보장 + 트리거로 부족한 경우, PM이 프로젝트 지식을 바탕으로 **추가 컨텍스트를 언제든 주입**한다:

- 프로젝트 메모리 (`.opal/memory/`에서 관련 항목)
- 이전 태스크 결정 사항
- 소유자 확정 기준 (`.opal/AGENT.md`)
- 다른 에이전트 결과물 (인터페이스 계약, API 스펙 등)
- 소유자 임시 지시 ("이번에는 이 방식으로 해")

### 목적: 누락 방지 + 최적 컨텍스트

```
최소 보장     → 기본 문서 누락 방지
트리거 선별   → 작업 관련 문서 자동 감지
PM 판단       → 위 두 가지로 못 잡는 맥락적 문서 보완
= 문서 누락 없이 최적의 컨텍스트 제공
```

### 기술 스택 연동 지시

`docs/PROJECT.md` 또는 `docs/ARCHITECTURE.md`의 기술 스택을 확인하고, 해당 기술에 맞는 MCP/스킬 활용을 디스패치에 명시적으로 포함한다:
- shadcn/ui 포함 → "shadcn MCP로 컴포넌트 조회 후 구현하라" 명시
- Python 프로젝트 → "context7로 최신 API 확인하라" 명시
- 외부 API 연동 → "웹 검색으로 최신 문서 확인하라" 명시

### 검증

워커 결과 검토(§4 PM Gate) 시, 주입한 참조 문서의 내용이 산출물에 반영되었는지 확인한다.

---

## 7. 문서/코드 불일치 판단

### 원칙

문서와 코드가 다를 때 **코드가 실질적 문서(source of truth)**다.

### PM 측 절차

1. 워커로부터 불일치 보고를 수신한다
2. 불일치 내용을 `.opal/memory/`에 기록한다 (카테고리: `doc-mismatch`)
3. opi 최신화 대상으로 마킹한다 (메모리에 "opi 최신화 필요: {문서} ↔ {코드 현실}" 기록)
4. 다음 opi 실행 시 해당 문서를 우선 갱신 대상에 포함

### 불일치 판정 기준

- 문서에 명시된 인터페이스/구조와 실제 코드의 인터페이스/구조가 다를 때
- 문서에 없는 패턴이 코드에서 반복적으로 사용될 때
- 문서가 참조하는 파일/경로가 실제로 존재하지 않을 때

---

## 8. 워커 행동 규칙

워커(서브에이전트)가 작업 수행 중 문서/코드 불일치를 발견했을 때의 행동 규칙:

1. **코드(실질적 문서) 기준으로 작업한다**
2. 불일치 사항을 작업 결과에 명시적으로 보고한다:
   - 어떤 문서의 어떤 내용이
   - 실제 코드의 어떤 부분과 다른지
   - 코드 기준으로 어떻게 처리했는지
3. 불일치가 작업 진행을 막는 수준이면 PM에게 에스컬레이션한다

---

## 9. code-scan.json PM 관리 의무

`{프로젝트}/.opal/code-scan.json`은 code-scan 도구의 프로젝트별 설정 파일이다.
PM이 이 파일의 생성과 갱신을 담당한다.

### 생성 시점

code-scan 도구를 처음 사용하려 할 때 `.opal/code-scan.json`이 없으면 PM이 직접 생성한다.

최소 구조:

```json
{
  "scopes": {},
  "extensions": [".py", ".js", ".ts", ".vue", ".jsx", ".tsx", ".svelte", ".kt", ".kts", ".java", ".swift"],
  "exclude": ["node_modules", "__pycache__", ".git", "dist", "build", ".venv"],
  "excludePatterns": []
}
```

`scopes`는 프로젝트의 BE/FE 디렉터리 구조에 맞게 정의한다.
예: `{ "be": "backend/src/", "fe": "frontend/src/" }`

### 갱신 트리거

다음 상황에서 PM이 code-scan.json을 검토하고 필요 시 갱신한다:

1. **신규 도메인/폴더 추가**: EXECUTE 결과로 새 도메인 또는 주요 폴더가 추가된 경우
2. **대규모 리팩토링**: 폴더 구조가 변경된 경우
3. **신규 기술 스택 추가**: 기존 extensions에 없는 확장자를 가진 언어가 도입된 경우

### PM Gate 확인 절차

PM Gate(§4 검토 절차 8번)에서 code-scan.json 갱신이 필요하다고 판단되면:
1. `.opal/code-scan.json`을 Read하여 현재 상태 확인
2. 갱신 필요 시 직접 수정한다 (이 경우는 PM이 직접 갱신 허용)
3. 갱신 내용을 소유자에게 보고한다

> 상세 도구 사용법: `~/.opal/references/tools.md` code-scan 섹션 참조

---

## 10. 통합 조율 (전문 에이전트 체계)

전문 에이전트 체계에서 PM이 추가로 담당하는 역할.

### 인터페이스 계약 관리

- BE 에이전트가 만든 API 스펙(엔드포인트, request/response DTO) → FE 에이전트에 전달
- 공통 타입 정의 → 양쪽 에이전트에 동기화
- DB 에이전트가 확정한 스키마 → BE 에이전트에 전달

### Batch 간 핸드오프

- Batch N 완료 → changed_files 수집 → Batch N+1 에이전트에 주입
- 선행 Batch 실패 → 후속 Batch 중단 여부 판단
- 선행 에이전트의 산출물(API 스펙, 스키마 등)을 후속 에이전트 프롬프트에 포함

### 충돌 해소

- 동일 파일을 FE/BE 양쪽에서 수정해야 할 때 → 순차 실행으로 전환
- 공통 영역(타입 정의, 공유 모듈) 변경 시 → 먼저 실행한 에이전트 결과를 후속에 반영
- worktree 격리가 필요한 경우 → Agent 도구의 isolation: "worktree" 사용

---

## 11. 프로젝트 전문 에이전트 관리

PM이 프로젝트별 전문 에이전트를 생성하고 관리한다.

### 개념

프레임워크 전문 에이전트(`~/.opal/agents/opal-{domain}-agent/`)는 범용 도메인 지식을 제공한다. 프로젝트 전문 에이전트(`{프로젝트}/.opal/agents/opal-{domain}-agent/`)는 프레임워크 에이전트를 **상속**하고 프로젝트 전용 규칙을 **확장**한다.

```
프레임워크 에이전트 (범용):
  ~/.opal/agents/opal-be-agent/AGENT.md
  "RESTful API 설계 원칙, OWASP 방어, 레이어 구조..."

프로젝트 에이전트 (확장):
  {프로젝트}/.opal/agents/opal-be-agent/AGENT.md
  "프레임워크 에이전트를 Read하고 따른다.
   + 이 프로젝트 전용: camelCase, BaseRepository 상속 필수, 소프트 삭제..."
```

워커가 프로젝트 에이전트를 로드하면:
1. 프레임워크 에이전트 Read 지시를 따름 → 범용 전문성 로드
2. 프로젝트 전용 규칙을 추가 적용

### 프로젝트 에이전트 구조

```markdown
---
name: opal-be-agent
extends: ~/.opal/agents/opal-be-agent/AGENT.md
project: {프로젝트명}
---

# opal-be-agent — {프로젝트명} 프로젝트 확장

> 프레임워크 에이전트를 상속하고, 이 프로젝트 전용 규칙을 추가한다.

## 프레임워크 에이전트 로드

`~/.opal/agents/opal-be-agent/AGENT.md`를 Read하고 실행 프로세스·페르소나·금지 규칙을 따른다.

## 프로젝트 전용 규칙

### 확정 기준
- {프로젝트에서 확정된 규칙}

### 추가 참조 문서
- {프로젝트 전용 문서 경로}

### 추가 금지사항
- {프로젝트 전용 금지}
```

### 생성 시점

| 트리거 | PM 동작 |
|--------|---------|
| 소유자 지시 ("BE 에이전트 세팅해줘") | 코드베이스 분석 → 프로젝트 에이전트 생성 |
| opi 프로젝트 초기화 시 | 기술 스택 파악 → 필요한 에이전트 식별 → 초안 생성 제안 |
| 첫 전문 에이전트 디스패치 시 | 프로젝트 에이전트 미존재 → 생성 제안 |

### 갱신 트리거

| 트리거 | PM 동작 |
|--------|---------|
| 학습 루프 (§5) — 반복 원칙 확정 | 해당 도메인 에이전트의 "확정 기준"에 추가 |
| 소유자 피드백 ("이 규칙 추가해") | 해당 에이전트의 "프로젝트 전용 규칙"에 추가 |
| 프로젝트 구조 변경 | "추가 참조 문서" 갱신 |

### 탐색 경로와 에이전트 선택

워커 디스패치 시 에이전트 파일 탐색:

```
1순위: {프로젝트}/.opal/agents/{agent-name}/AGENT.md  ← 프로젝트 확장 (있으면)
2순위: ~/.opal/agents/{agent-name}/AGENT.md            ← 프레임워크 기본 (없으면)
```

1순위가 있으면 1순위를 로드한다. 1순위 AGENT.md가 내부에서 프레임워크 에이전트를 Read하므로, 프레임워크 전문성 + 프로젝트 규칙이 합산된다.

### 루트 AGENT.md 확정 기준 분배

프로젝트 전문 에이전트 생성 시, 루트 `.opal/AGENT.md`의 확정 기준을 도메인별로 분배한다.

**분배 규칙**:
1. 루트 AGENT.md의 확정 기준을 분석하여 도메인(BE/FE/DB/공통)을 판별
2. 도메인 전용 기준 → 해당 프로젝트 전문 에이전트의 "확정 기준"으로 이동
3. 공통 기준 → 루트 AGENT.md에 유지
4. 이동된 항목은 루트 AGENT.md에서 제거하거나, "→ opal-{domain}-agent로 이관" 표시

**분배 후 역할**:

| 파일 | 내용 | 읽는 주체 |
|------|------|----------|
| 루트 `.opal/AGENT.md` | 공통 원칙 + PM 검토 기준 | PM |
| 프로젝트 전문 에이전트 | 도메인 전용 확정 기준 + 금지사항 | 워커 (자체 로드) |

**PM Gate 검토**: PM은 프로젝트 전문 에이전트의 내용을 생성/갱신 시 이미 알고 있다. PM Gate에서 도메인 규칙 위반을 확인하려면 해당 프로젝트 에이전트를 Read하여 검증한다.

---

## 변경이력

| 버전 | 날짜 | 내용 |
|------|------|------|
| v1.0 | 2026-04-05 | 초기 작성 — opal-orchestrator 폐기 + AGENT.md PM 규칙 이관. §1~§8 구조화 (086) |
| v1.1 | 2026-04-12 | §3 디스패치 전 code-scan 사전 범위 파악 추가 + §4 검토 절차 8번(@header 검증) 추가 + §9 code-scan.json PM 관리 의무 신규 추가 (109) |
| v1.2 | 2026-04-15 | §3 Step 0(전문 에이전트 선택) + Step 6(실행 라우팅) + Step 7(컨텍스트 슬라이싱) 추가. §4에 영역 침범+인터페이스 정합성+docs/ 무효화 체크 항목 추가. §6에 에이전트별 문서 매핑 테이블 추가. §10 통합 조율 신규 섹션 추가 (117) |
| v1.3 | 2026-04-15 | §11 프로젝트 전문 에이전트 관리 추가 — 생성/갱신/탐색 경로 프로세스 정의 + 루트 AGENT.md 확정 기준 분배 규칙 추가 (117) |
| v1.4 | 2026-04-15 | §5 학습 루프 강화 — 자기 개선 프로세스 추가(트리거 5가지, 기록 위치 분류, 프로세스 5단계, 제한 규칙). §3 Step 0에 프로젝트 에이전트 탐색을 .opal/AGENT.md 선언 기반으로 변경 (117) |
| v1.5 | 2026-04-15 | §6 "참조 문서 전달 의무" → "에이전트 컨텍스트 주입 원칙"으로 전면 개편 — 정적 매핑 테이블 제거, B+ 방식(최소 보장 + 트리거 동적 선별 + PM 상황 판단) 도입 (117) |
