.DS_Store
.opal/*
!.opal/brain/
!.opal/brain/**
!.opal/code-map/
!.opal/code-map/**
!.opal/code-scan.json
!.opal/memory/
!.opal/memory/**
!.opal/worktree.json

# machine-local 산출물 — `.local` 접미사 규약(setting.local.json 계열).
# `.opal/memory/**` 예외보다 뒤에 두어야 재-무시가 적용된다(마지막 매칭 우선).
*.local.md

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
.tox/
build/
dist/
venv/
.venv/
env/
.env

# Node.js
node_modules/

# MCP / tooling temp
.playwright-mcp/

# oppl 루프 액션 에이전트 실행 캡처 (전송 산출물 — AGENT.md §결과 파일 규약)
.oppl-run/
.opal-worktrees/

# 태스크 실행 로그 — 추적 경계 (123 T02 / AC-14 · MV-18 · TRD §8.1)
# 원본 캡처·파생 색인·배타 락은 영구 제외한다.
tasks/**/run/raw/
tasks/**/run/.runtime/
.opal-task.lock
# [한시 제외 — T06 마스킹 구현 완료 시 이 줄을 제거한다]
# AC-14는 "마스킹된 segment는 추적된다"를 요구하므로 아래 줄은 영구 규칙이 아니다.
# 다만 현재 run_log_core.redact()가 pass-through이므로 지금 조각을 추적하면
# 마스킹되지 않은 내용이 git 히스토리에 들어가고, 히스토리 진입 후에는 회수가
# 불가능하다 — TRD D-9가 사후 스캔(b안)을 기각한 바로 그 이유다.
# 복귀 조건: T06(마스킹·원본 상한·추적 경계)이 redact() 본문을 구현하면 제거.
tasks/**/run/*.jsonl
