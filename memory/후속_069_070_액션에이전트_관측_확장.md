# 후속 069·070 — oppd·opsdd 액션 에이전트 관측 확장 (opal-action-monitor 공용화)

> 등록: 2026-07-17 | 유형: task | 상태: active | 출처: 067 추가작업 시 캡틴 지시("나중에 oppd, opsdd에서도 참고해서 수정")

## 목표

`opal-task-action-agent`(oppd)·`opal-sdd-action-agent`(opsdd)의 내부 디스패치를 opal-agent 채널로 전환하고 관측 규약을 채택하여, **`opal-action-monitor` 단일 도구 + `//opm` 단일 스킬**로 3개 액션 에이전트 전부를 모니터링한다 (에이전트별 도구 신설 금지 — 표준화 원칙).

## 참고 템플릿 (그대로 준용)

| 참고 | 위치 | 내용 |
|------|------|------|
| 채널 전환 템플릿 | task 066 (`tasks/066-260717-opd-루프액션-opal-agent-채널/`) | 단계×축×호출모드 매트릭스·동기/비동기 명령 형태·allowedTools·timeout 배분·플랫폼 가용성 — oppd/opsdd 단계 어휘로 치환만 |
| 관측 규약 SSOT | `opal/agents/opal-loop-action-agent/AGENT.md` §결과 파일 규약(v2)·§운행 일지 | `.oppl-run/` 레이아웃(events.jsonl/err.log/exitcode/prompt.txt)·완료 마커=exitcode·journal 4컬럼 append-only — **디렉토리명 `.oppl-run/`은 공용 규약명으로 유지**(파이프라인별 변경 금지) |
| stream 채널 | `opal/tools/opal-agent/` `--stream` (067) | 비동기 축 events.jsonl 리다이렉트 패턴 |
| 뷰어 | `opal/tools/opal-action-monitor/` (067에서 oppl-monitor로 만들고 리네임) | 폴더 규약 파서 — 파이프라인 무관 |
| brain | `.opal/brain/pages/concept/oppl-internal-channel-opal-agent.md` | 066 설계 결정 |

## 전환 시 필수 수정 1건 — monitor phase 동적 발견

`opal_action_monitor.py`가 phase 목록을 `[t1,t2,g,t3,t4a,t4b]`(루프 액션 어휘)로 **하드코딩**하고 있음 — oppd(PLAN→QA→EXECUTE…)·opsdd(ACT 단계)는 어휘가 다르므로, 069에서 `*.exitcode`/`*.events.jsonl` 파일명 스캔 기반 **동적 phase 발견**으로 개선해야 함(실데이터 생기는 시점에 검증까지 한 번에). 리네임 시점(067)에는 로직 무변경으로 둠.

## 완료 시 판단 항목

- 3/3 커버 확정 후: 도구·규약 문서의 oppl 잔향 표현 정리 여부 재판단.
- 구독 rate limit 실측(066 R-4, 다태스크 연속 실행 시) 병행.
