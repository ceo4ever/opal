# AGENTIC-LOG: 태스크 루트 해석 수렴 + OPAL_TASKS_ROOT 계약 신설

> 모드: agentic | 시작: 2026-09-06 23:22 | 스킬: //opd (opds → opd 승격) | 워크스페이스: --wt

## [GATE] TASK 단계
- 판정: Pass — `verify --clarification-check` = pass
- 태스크 번호 109(`memory-tool task-number --bump`), 워크트리 `feat/OP-TASK-109` 생성(`ok:true`)
- Git 사전 점검: 워킹트리 클린(`3fec20c` 이후 0건)
- 발원: 태스크 107 DONE.md §7 하네스 발견 2번 + 완료 태스크 20건 backup 이관의 파급

## [DECISION] 범위를 (A)+(B) 둘 다로 확정 (캡틴)
- (A) **위치가 바뀌면 깨진다** — backup 이관으로 **허브에서** state-tool 1건 + console BE 7건 신규 실패(이관 전 0·2)
- (B) **존재하지 않으면 깨진다** — 워크트리에 `tasks/` 부재로 console BE 24건 추가 실패
- (B)만 닫으면 「워크트리는 허브와 같아졌는데 허브가 깨져 있다」가 되어 목표 진술이 성립하지 않는다. 태스크 107에서 세 번 겪은 「부분 범위가 목표를 못 채운다」와 같은 구조다
- **성질 진단**: (A)(B)는 「태스크 폴더를 **데이터**로 쓰면서 위치·존재를 **불변으로 가정**한다」는 하나의 문제이며, 107이 brain에 등재한 `regression-pin-of-task-time-fact`의 또 다른 발현이다

## [DECISION] 심볼릭 링크 미채택 — 워크트리에서 직접 실험한 결과
- 캡틴 제안(허브 `tasks/`를 워크트리에서 읽기만 가능하게)을 **실제로 걸어 봤다**
- `ln -s ../../tasks tasks` → 읽기는 성공하나 **`git status`에 1,022건 `D`**. sparse-checkout이 `skip-worktree`로 감추던 tracked 파일이 그 경로를 링크가 차지하자 **삭제로 인식**된다 — 워커가 `git add -A` 한 번 하면 브랜치에서 `tasks/`가 통째로 지워진다
- 이름을 바꾸면(`.hub-tasks`) git은 깨끗하나(`?? 1건`), 코드가 `tasks/`를 문자 그대로 찾으므로 **경로 해석 한 겹이 어차피 필요**하다 → 링크는 잉여
- 캡틴이 「링크 없이 환경변수만」으로 정정했고 그게 맞다. 파일시스템 흔적 0, `.gitignore` 불요, 워크트리 재생성과 무관
- 실험 후 원복 확인(`git status` 0건)

## [DECISION] opds → opd 승격 (캡틴 `//opd --agentic --wt`)
- 사유: 경로 조립 8곳 수렴 + 테스트 하드코딩 4곳 + 하네스 문서 3건 + worktree-tool 응답 확장이 Short Task 규모를 넘는다
- 승격 이득: **ANALYSIS**가 붙어 8곳 수렴 설계 전에 구조를 먼저 본다. **TEST-SCENARIO**가 붙어 R-3 AC(b)「단언 강도 무변경」을 시나리오로 고정할 수 있다 — opds면 둘 다 PLAN에 뭉개진다
- 폴더명의 `opds` 접미사는 **유지**했다. rename하면 TASK.md 인용 경로와 `state.json` 경로가 전부 깨진다. 스킬 SSOT는 `state.json`의 `skill` 필드다
- **[ERROR] 도구 우회 재발**: state-tool에 「재개 시 스킬·모드 변경」 명령이 없어 `init --force`로 재구성했다. fw-inbox에 「state-tool 재개 시 모드 변경 override 명령 부재」가 이미 있고 그 결함을 다시 밟았다. TASK 행 1개만 잃고 재마킹으로 복구

## [GATE] ANALYSIS 진입
- 판정: Pass — `advance analysis.analysis_md` 시 도구가 row 2(TASK 사용자 확인)를 `auto_approved`
- brain 선조회 4페이지: `worktree-tasks-fixture-structural-limit`(score 10) · `worktree-slot-existence-to-occupancy-judgment` · `worktree-workspace-isolation-axis` · `regression-pin-of-task-time-fact`
- 기준선 실측(RED 증거): 허브 state-tool 1 failed/396 · console BE **9 failed**/348 / 워크트리 state-tool 1/396 · console BE **33 failed**/322

## [ERROR] PM이 근거 없이 성질을 추정해 3회 잘못 제안했다 (PM(대화) 구간)
| # | PM 주장 | 실제 | 정정 계기 |
|---|---|---|---|
| 1 | `OPAL_TASKS_ROOT` **신설** 필요 | 허브 경로는 `.opal-worktrees/.meta/task_{NNN}.json` `entries[].repo`에 **이미 기록**돼 있고 경로 규약으로도 유도된다 | 캡틴 되물음 |
| 2 | 「환경변수 vs 워크트리로 옮기고 머지」 **아키텍처 선택** 사안 | 계약은 옳고 **참조 구현이 없을 뿐**이다 — 저울질 대상이 아니다 | 캡틴 되물음 |
| 3 | `code-scan.json`은 **복사**해야 한다(허브 설정 읽으면 스캔 대상 모호) | 스코프가 전부 **상대경로**(`"framework": "opal/"`)라 모호하지 않다. 복사하면 설정 드리프트만 생긴다 | 캡틴 되물음 |
- 공통 원인: **읽기 도구를 쓰지 않고 성질을 추정**했다. 3건 모두 파일 하나 열면 끝나는 확인이었고, `.meta`는 PM이 이미 출력까지 본 파일이었다
- **규범은 존재한다** — `PRINCIPLES.md:19`「If you're unsure, say so — don't answer confidently and wrong」 · `AGENT.md:174`「도구 선제 활용: 직접 추론으로 때우기 전에 도구를 먼저 집어든다(읽기 계열은 **선제**)」
- **집행 장치가 없고, 그건 명시적이다** — `opal-pm.md:143`이 PM(대화)를 **하네스 미적용**으로 규정한다. PM(태스크)에는 citation-rules·PM Gate·목표-커버 게이트가 걸려 실제로 PM 오류를 잡았으나(107: 게이트 1.33 반려, 컨벤션 진단 High 2회), 대화 구간에는 아무 게이트가 없다
- `PRINCIPLES.md:15`가 이 상황을 예언한다 —「Enforce, don't just advise: if a rule must always hold, **a tool gates it — not prose**」. 근거 의무가 대화 구간에서는 산문으로만 존재한다
- **107 PM 귀책 5건 중 3건과 같은 축**이다(워커에겐 [MUST]로 주입하고 PM 자신은 미준수). 규율이 워커 디스패치 경로에만 실려 있다
- 조치: 캡틴 지시로 이번 태스크를 먼저 진행한다. PM(대화) 근거 의무 명문화는 별건 후보로 남긴다

## [DECISION] TASK.md 개정 — 문제 정의를 「계약 구현 부재」로 축소
- 목표 재정의: 「환경변수 계약 신설」 → **「허브 루트 해석 규칙 수립 + 8곳 수렴」**
- 철회 3건을 `[결정]`으로 명시 기재(환경변수 신설·`code-scan.json` 복사·심볼릭 링크) — 왜 안 하는지를 남겨야 다음 사람이 같은 안을 다시 낸다
- 신규 `[결정]` 2건: 해석 규칙(세그먼트 기준, 레이아웃 무관) / **런타임별 2구현·계약 1개**(두 세계에 공용 모듈 자리가 없음 — `dashboard/backend`는 표준 라이브러리만 import, `opal/tools/`는 자기 디렉터리만 `sys.path`에 넣는다)
- §배경 분석 (5) 신설: 워크트리에 `.opal/`이 아예 없다 + task_107 sparse 무단 변경(생성 11:52 → 수정 13:52) + 정규 워크트리에서 code-scan `header_source_unset` 실패
- 완료기준에 **「정규 워크트리에서 `code-scan validate` exit 0」** 추가 — 이 태스크가 자기 검증을 수행할 수 있어야 한다
- `verify --clarification-check` = pass 재확인

## [GATE] ANALYSIS PM Gate — Pass (재지시 1회)
- 산출물 22,102 → **37,948 bytes**. 정정 9개 절(제목·확정입력판정·§1.1·§1.3·§3.3·§4·§5·§7·§8)
- **PM 독립 재현으로 워커 결론 2건 확인**:
  - **Q9 — 워커가 PM 우려를 뒤집었고 그게 맞다.** PM은 「허브 코드가 워크트리 cwd에서 실행되면 `__file__`과 `cwd`가 갈린다」를 설계 쟁점으로 봤으나, 실측상 **8곳 어디도 `__file__`을 쓰지 않는다**(인자 또는 `cwd()`뿐). 실제 버그는 반대 방향 — `code-scan.js:331` `findProjectRoot()`가 `fs.existsSync(dir/'.git')`로 멈추고, **git 워크트리의 `.git`은 78바이트 파일**(`gitdir: …`)이라 워크트리 루트에서 즉시 반환된다(`.opal`·`CLAUDE.md` 조건에 도달조차 안 함). 따라서 세그먼트 규칙은 **제3의 입력이 아니라 기존 값 위의 후처리 정규화**여야 한다 — PM이 「입력을 무엇으로 할지」를 쟁점으로 본 것이 틀렸다
  - **Q11 — 비대칭 확인.** `tasks.py:464-465`에 `if entry.name == "backup": continue`와 `:474` archive 컬럼 처리가 있는데 `scanner.py`·`dashboard.py`는 `backup` 언급 **0건**. 같은 `tasks/`를 세면서 한쪽은 backup을 빼고 한쪽은 넣는다 — 집계 항등 파손의 직접 원인이며 단순 재귀 전환은 `tasks.py`와 새 불일치를 만든다
- Q10 판정 타당: 설정/스캔 루트 분리는 6개 이상 함수 이중 인자화 필요 → 과도. 정규화 한 겹으로 대체
- Q12 판정 타당: 런타임이 갈려 단일 프로세스 동치 테스트 불가 → **공유 골든 케이스 표 파일**을 양쪽 스위트가 읽는 방식 권고
- 워커가 **미확인 2건을 명시**했다(`docs/PROJECT.md` 읽기 기준 전체 파급 · code-scan 외 CLI 서브커맨드 유무) — 「성질을 추정하지 마라, 미확인이면 밝혀라」 지시가 작동했다
- 행 처리: `analysis.analysis_md` done(`--worker-duration-minutes 12` — 워커 2회 합산 455962+272528ms ≈ 12분) / `analysis.pm_gate` done

## [GATE] PLAN PM Gate — Pass (93,284 bytes / F-001~F-006 / 15 Step / 5 Phase)
- **PLAN이 ANALYSIS 권고를 사실오류로 뒤집었고 PM이 실측 확인했다** — ANALYSIS §8은 console BE 집계 5건 처방으로 「`tasks.py`의 `backup` 스킵 이식」을 권고했으나, `test_routers.py` 동결 코호트 **21건 중 19건이 `tasks/backup/` 아래**다(`080~099`; `tasks/` 직속은 `100`·`101`뿐). `test_routers.py:1435`가 `set(cohort) <= observed_prefixes`를 단정하므로 스킵 이식은 실패를 고착시킨다
  - **PM 자기 검토 누락**: PM은 ANALYSIS Q11 검증 시 「`tasks.py`만 backup을 안다」는 **비대칭은 맞게 확인했으나 맞추는 방향을 확인하지 않았다.** 비대칭 존재 ≠ 어느 쪽으로 통일할지. 채택은 「1-depth + backup 1-depth **2단 열거** + 아카이브 플래그」 단일 함수
- **조립 지점 8 → 9곳** — `brain_tool.py:239` `resolve_brain_path`가 `p / ".opal" / "brain"`을 반환해 워크트리에서 **brain-tool 전 명령이 먼저 차단**된다. ANALYSIS 목록에 없던 지점(PM 실측 확인)
- **헬퍼 2 → 3개** — Node 런타임(`code-scan.js`)이 별도. `hub_root(path)->str` / `hubRootFromPath(p)->string`, **순수 문자열 함수**(FS·env·cwd 미접근), 세그먼트 첫 출현 기준, 항등 반환
- **[ERROR] PM이 쓴 완료기준이 달성 불가였다** — 「정규 워크트리 `code-scan validate` exit 0」인데 **허브 자체가 exit 2**다(차단 3건 = FE 테스트 3파일 `uncovered:incomplete`, PM 실측 확인). 세그먼트 수정과 무관하므로 워크트리를 허브와 같게 만들어도 exit 0이 안 된다. 판정 축을 「허브와 동일한 exit·차단 위반 집합」으로 옮기고 3파일 정비를 **R-7**로 편입
- `doctor.py` 제외 채택 — `doctor.py:83-90`이 「인자 경로의 **리터럴** `tasks/` 상태」를 보고하므로 허브 정규화 시 워크트리 진단이 영구 통과가 되어 **자기 관측이 소멸**한다(진단 도구가 진단 대상 해석에 의존하는 순환). 주석·테스트(TS-044)·문서 3중 고정
- `findProjectRoot()` 개정은 **최상위 진입점 정규화**(첫 줄 1곳) 채택 — 삽입점 1곳, 세그먼트 부재 시 본문이 현행 경로를 타 **바이트 동일이 구조적 보장**
- RED-first **ON(강제)** — `red-first.md` §1.5 「버그 수정(회귀 방지)」. 작성자≠구현자 Step 배정(테스트 1·2·5·6·7 = test-agent / 구현 4·8~12 = be·task·fe-agent), Step 3이 RED 증거 게이트. §1.6 선작성 트랙 미적용(파괴 관점과 검증 관점 일치)
- 체크박스 **15개 = Step 15개** — 107의 공용 체크박스 결함 재발 없음
- 행 처리: `plan.plan_md` done(`--worker-duration-minutes 27` — 1604870ms) / `plan.pm_gate` done

## [DECISION] TASK.md 갱신 — PLAN 요청 3건 반영 (캡틴 승인)
- **R-6 신설**(console BE 집계 2단 열거) · **R-7 신설**(FE 테스트 3파일 `@header` 정비)
- 조립 지점 8 → 9곳, 헬퍼 2 → 3개, 범위에 R-6·R-7 편입
- 완료기준 보정 — 「워크트리 exit 0」 → **「허브와 동일한 exit·차단 위반 집합」**(R-7로 허브 차단도 해소하므로 결과적으로 양쪽 exit 0)
- 허브 `0 failed` 검증 시점을 **머지 후 CLOSE 게이트**로 명시(코드 변경이 워크트리 한정)
- §배경 분석 (6)(7) 신설 — 완료기준 오류와 ANALYSIS 권고 오류를 **왜 틀렸는지까지** 기재. 「안 한다」만 쓰면 다음 사람이 같은 안을 다시 낸다
- `verify --clarification-check` = pass 재확인

---

## TEST-SCENARIO 단계 — 2026-09-07 14:08~14:16

### TEST-SCENARIO.md 작성 (PM+캡틴 페어, 워커 미투입)

`opd` STEP 3.5 규율에 따라 워커를 쓰지 않고 PM이 직접 작성했다. 18,601 B.

- §1 리스크 가설 표 — PLAN의 H-1~H-11 **전건 전재** (P0 8건 / P1 3건, H-5는 P0 보안)
- §2 테스트 데이터 설계 + **§2.3 회귀 기준선** — 절대 수치 대신 이 표 대비 판정. 컨텍스트에서 3회 반복된 오답(console BE를 `dashboard/backend`에서 실행 → `ModuleNotFoundError` 134 errors / brain-tool을 시스템 `python3`로 실행 → PyYAML 부재)을 `[MUST]`로 못박았다
- §3 시나리오 **38건** (L1 27 / L2 10 / L3 1)
- §4 AC↔가설↔계층↔시나리오 4열 매핑 — R-1~R-7 × F-001~F-006 × H-1~H-11 전건 커버
- 선작성 트랙(§1.6) **미적용** — PLAN 판정 승계(버그 수정이라 파괴 관점과 검증 관점이 일치)

### PM 보강 2건 — PLAN TS 집합의 구조적 빈틈

PLAN이 설계한 TS-001~TS-053(36건)은 촘촘했다. 두 지점만 비어 있었다.

| TS | 빈틈 | 왜 빈틈인가 |
|----|------|------------|
| **TS-060** | 골든 케이스 표의 **단일성** | TS-010은 「구현이 1개씩」을, TS-011은 「**오늘** 답이 같음」을 본다. **표가 사본으로 갈라지는 경로는 어느 시나리오도 막지 않는다** — 사본이 생기면 TS-011은 계속 통과하면서 드리프트가 시작된다. 「오늘 같다」와 「내일도 같다」는 다른 명제다 (107 brain `prohibit-by-property-not-name` 계열) |
| **TS-061** | H-9의 **사람 판정** | TS-026은 「문서에 적혔는가」를 본다. H-9의 위험은 **사람이 오해하는 것**이고, 문서 존재는 오해 방지를 증명하지 않는다. 오해가 남으면 고칠 곳은 문서가 아니라 **출력**이다. 문자열 단언으로 판정 불가하므로 L3 M3 [SUPERVISOR] |

TS-061은 PLAN TS 목록에 L3가 전무했던 것을 메운다. `opd` STEP 5-0에 따라 TEST 단계에서 test-agent는 L3 제외 모드로 돌리고, PM 표준 요청 양식으로 캡틴에게 발신한다(양식 본문에 기재).

### 목표-커버 게이트 iteration 1

- **Step 2 정규화**: `.scenario-coverage-input.json` 빌드 — requirements 7 / features 6 / hypotheses 11 / scenarios 38. 플래그 산정: goal 8 · adoption 12 · boundary 13
- **Step 3 결정론 게이트 (②③④)**: `test-tool scenario-coverage-check` → **exit 0**, `all_covered: true`, 누락 0
- **Step 4 판단 게이트 (①⑤⑥)**: `opal-evaluator-agent` `phase: scenario-rubric` 디스패치 — Producer≠Evaluator 유지(PM이 작성자이므로 자기 채점 불가). 진행 중

디스패치 프롬프트에 **플래그 불신 지시**를 넣었다 — `.scenario-coverage-input.json`의 3플래그는 내가 산정한 값이므로, 평가자가 시나리오 본문을 직접 읽고 과대 산정을 gap으로 보고하도록 요구했다. 자기 산정값을 채점 입력으로 넘기면 게이트가 self-confirming으로 무너진다.

### 게이트 iteration 1 결과 — pass인데 고쳤다

평가자 판정: **`verdict: pass`** (① 목표 2 / ⑤ 채택 **1** / ⑥ 경계 2, 평균 1.67). §5-1 수렴 조건(각 ≥1 AND 평균 ≥1.5) 충족.

그런데 **⑤가 1점인 사유가 gap G-1**이었다. 임계를 넘었다는 것과 결함이 없다는 것은 다르다 — pass를 근거로 그냥 mark하면 게이트를 통과 도장으로 쓰는 것이다. 4건 전건 반영 후 iteration 2 재검으로 갔다.

**워커 보고를 그대로 받지 않고 먼저 검증했다** (G-1·G-4는 사실 주장이므로 대조 가능):

```
$ sed -n '132p' TASK.md   # R-6 AC 원문
(a) 열거 함수가 1개이고 3지점이 그것만 호출한다
(b) `grep`으로 잔여 직접 `scandir`/`iterdir` 열거 0건
(c) 동결 코호트 21건이 관측 집합에 전건 포함된다
(d) 코호트 필터 중앙값이 재현된다
(e) 아카이브 구분이 필요한 지점(`tasks.py` archive 컬럼)의 기존 동작 무변경

$ grep -nE "scandir|iterdir|열거 함수|그것만 호출" TEST-SCENARIO.md
→ §1 가설 표 1행뿐, 단정하는 TS 0건        # G-1 확인
```

내 §4 매핑은 AC(a)→TS-040(열거 항등), AC(b)→TS-041/042(코호트), AC(c)→TS-043(아카이브), AC(d)→TS-044(doctor), AC(e)→**미매핑**. 원문과 **한 칸씩 어긋나** 있었다 — PLAN 시점 문자를 TASK 개정 후에도 승계한 것이다. **G-4 확인.**

| gap | 반영 |
|-----|------|
| **G-1** | **TS-047 신설** — 「함수 1개」+「3지점이 그것만 호출」+「잔여 직접 열거 0건」 3단 단정 |
| **G-2** | TS-023을 워크트리로 한정, 허브 exit 0은 TS-071로 **이연**(삭제 아님) |
| **G-3** | **TS-071 신설** — 허브 목표 도달을 CLOSE 게이트 시점의 독립 TS로 |
| **G-4** | §4 R-6 AC(a)~(e) 전건 재정렬 + AC(e)→TS-043 기대 결과에 `archive` 컬럼 추가 |

38 → **40 시나리오**. 결정론 게이트 재실행 exit 0 유지(`all_covered: true`, scenarios 40).

**G-4가 G-1을 가렸다** — 이게 이번 반복의 핵심 교훈이다. 매핑 표의 AC 문자를 원문과 대조하지 않으면 「모든 AC에 시나리오가 붙어 있다」는 **외형이 유지되면서 실제로는 다른 AC를 검증**한다. 결정론 게이트(②③④)는 R-ID 단위 커버만 보므로 이 어긋남을 구조적으로 잡지 못한다. AC 문자 단위 정합은 판단축이 잡았다 — 6축 분리 설계의 근거 사례다.

또 하나: **G-1의 정체는 내가 TS-060에서 쓴 논거를 내가 적용하지 않은 것**이다. 「오늘 답이 같은 것과 내일도 같을 것은 다른 명제」를 골든 표에는 적용했는데, 열거 함수에는 적용하지 않았다(TS-040은 출력 항등만). 조립 축엔 TS-020(grep 잔존 0)이 있는데 열거 축엔 등가물이 없던 비대칭이다.

### 게이트 iteration 2 디스패치

문서를 고친 당사자가 「G-1이 닫혔다」고 선언하면 Producer≠Evaluator가 무너진다. iteration 2를 별도 세션으로 디스패치했다. 프롬프트에 넣은 지시:
- G-1~G-4 각각 **닫힘/열림을 근거와 함께** 판정 — TS-047이 3단 단정을 모두 하는지, 느슨하면 ⑤를 올려주지 말 것
- §4 AC 문자를 **TASK.md:132 원문과 직접 대조** — 재정렬이 또 어긋났을 수 있음
- **이연과 삭제 구별** — G-2 반영이 검증을 삭제한 것은 아닌지
- **iteration 1 점수에 anchor 금지** — 반영이 부실하면 ⑤는 1점에 머물러야 하고, 새 결함이 생겼으면 다른 축이 내려갈 수도 있음

### 게이트 iteration 2 — 수렴

평가자 판정: **`verdict: pass`** — ① 목표 **2** / ⑤ 채택 **2**(1→2) / ⑥ 경계 **2**, 평균 **2.0**. **G-1~G-4 전건 닫힘, still_open 0.**

⑤ 상향 근거로 평가자가 명시한 것: 「직전 평가자 동조가 아니라 TS-047이라는 문면 변화에 근거한다 — 없었다면 1에 머물렀다.」 anchor 금지 지시가 작동했다. G-2도 「이연이지 삭제가 아니다」로 별도 확인됐고(허브 exit 0 인수가 TS-071에서 오히려 강해짐), ID 유니크 40 / 페이로드 40 양방향 차집합 0으로 신결함 0건.

감점 아닌 권고 3건이 남았고 **전건 반영했다** — 사실 주장이므로 먼저 대조했다.

| 권고 | 대조 결과 | 반영 |
|------|----------|------|
| **G-5** | `PLAN.md:556-560`에 구버전 문자 잔존 확인(TS-041→AC(b), TS-042→AC(b), TS-043→AC(c), TS-044→AC(d)) + `PLAN.md:438`이 「워크트리·허브 양쪽 exit 0」 | PLAN AC 문자 재정렬 + TS-047·TS-071 행 신설 + TS-023 시점 분리 + 개정 근거 기재 |
| **G-6** | TS-047의 grep 절이 `scandir`/`iterdir` 두 이름만 열거 — `listdir`/`glob`/`walk` 우회 가능 | 판정을 **성질 기준**(「이 지점이 `tasks/`를 스스로 순회하는가」)으로 전환 + 패턴 6종 확장 |
| **O-1** | §1 가설 표 시나리오 셀이 페이로드 대비 누락 5행(H-1 3건·H-2 2건·H-4·H-6·H-8 각 1건) — 스크립트 대조로 확인 | §1 셀을 페이로드와 **양방향 일치**로 재생성 → 불일치 가설 **0** |

**G-5가 가장 중요했다.** 나는 G-4를 「하류(TEST-SCENARIO)만」 고쳤다. AC 문자는 `TASK.md`가 SSOT이고 PLAN·TEST-SCENARIO는 그 파생이므로, **파생 하나만 고치는 것은 수정이 아니라 분기다.** G-4를 만든 기전(PLAN 문자를 TASK 개정 후에도 승계)이 그대로 남아 있었으니, 다음 태스크가 PLAN을 읽으면 같은 어긋남을 다시 상속한다.

**G-6도 자기 논거의 비적용이었다.** TS-060에서 「이름이 아니라 성질」을 근거로 삼았는데 TS-047 자신은 API 이름 2개를 열거했다. G-1(TS-060 논거를 열거 함수에 미적용)과 **같은 유형의 실수가 한 반복 안에서 두 번** 나왔다 — 내가 세운 원칙을 내가 쓰는 곳에 적용하지 않는 패턴이다. brain 누적 대상.

### 게이트 종료 — §5-1 수렴

두 증거 확보:
1. **결정론(②③④)**: `test-tool scenario-coverage-check` **exit 0** — `all_covered: true`, requirements 7 / features 6 / hypotheses 11 / scenarios **40**
2. **판단(①⑤⑥)**: `opal-evaluator-agent` `scenario-rubric` iteration 2 **`verdict: pass`** — 2/2/2, 평균 2.0

`scenario-gate.md` §6 tool-gated 요건 충족. 산출: `SCENARIO-GATE-1.md`(12,170 B) · `SCENARIO-GATE-2.md` · `.scenario-gate-history.json`(iteration 1·2). `test_scenario.scenario_gate` 행 mark로 진행.

## R-ID 충돌 정정 — R-8 신설 (2026-09-07 14:55)

소유자 결정 3건을 반영하면서 R-4 AC 문자를 원문과 대조하니 **AC(d)가 §4 매핑 표에 없었다**(G-4와 같은 유형). 그래서 R-1~R-7 **전건**을 스크립트로 감사했고, 그 감사가 훨씬 큰 것을 잡았다.

### 무엇이 문제였나

| | `TASK.md` | `PLAN.md` |
|---|---|---|
| **R-7** | FE 테스트 3파일 `@header` 정비 (`:133`, AC a~d) | **이동값 단언 규약 정렬** (`:14` 「R-7로 신설」, F-006) |

**같은 ID가 서로 다른 두 요구사항을 가리키고 있었다.** 결과로 두 가지가 동시에 발생했다:

1. **TASK.md R-7(FE `@header`)의 AC(a)(b)(d)에 검증 시나리오가 없었다** — 내 §4는 R-7 슬롯을 PLAN의 의미(이동값)로 채웠으므로, TASK가 요구한 「차단 판정 통과」·「107 규정 준수」·「`counts` 회귀 0」이 **아무 시나리오에도 대응되지 않았다.** AC(c)만 TS-023으로 우연히 걸려 있었다.
2. **이동값 단언 규약 정렬에 TASK 수준 R-ID가 없었다** — PLAN이 자기완결로 들고 있었을 뿐이다.

`PLAN.md:93`과 리스크 R-11이 「TASK.md 요구사항 표 갱신이 필요하다 — 소유자 보고 대상」이라고 **예고까지 했다.** 그런데 R-6은 TASK.md에 반영됐고 두 번째 신설은 이미 점유된 번호를 가져갔다. 예고가 있어도 번호 점유 여부는 아무도 확인하지 않았다.

### 정정

| 항목 | 조치 |
|------|------|
| 이동값 단언 규약 정렬 | **R-8로 재배정** — TASK.md에 정식 요구사항 행 신설(AC (a)(b)(c)) + PLAN F-006 요구 열·소제목·TS 매핑 전건 갱신 |
| R-7 | **FE `@header` 정비로 확정** — PLAN F-003에 편입(Step 12가 담당) |
| R-7 AC(a)(b)(d) | **TS-027·TS-028·TS-029 신설** |
| R-4 AC(d) | TS-041·TS-042 매핑(집계 의존 5건 → 처방은 R-6 2단 열거) |
| PLAN 리스크 R-11 | 해소 기재 |

TS-027은 차단 위반을 **집합으로** 대조한다 — 건수만 보면 3건이 사라지고 다른 3건이 생겨도 통과한다. TS-028은 이 태스크가 **107 규정을 위반하며 107의 성과를 되돌리지 않는지**를 본다(FE `@header`를 만지는 작업이므로 실재하는 위험이다).

### 왜 세 번이나 같은 유형이 나왔나

G-4(R-6 AC 어긋남) → R-4 AC(d) 미매핑 → R-7 ID 충돌. **전부 「AC/ID 수준의 정합을 사람이 눈으로 승계한」 결과다.** 결정론 게이트(②③④)는 **R-ID 단위** 커버만 보므로 이 층을 구조적으로 보지 못한다 — R-7이 커버돼 있으니 `all_covered: true`였고, 그 R-7이 어느 요구사항인지는 묻지 않는다.

한 곳씩 고치는 방식으로는 못 잡는다. **스크립트 감사(요구사항별 AC 문자 집합 대조)를 돌린 뒤에야 전건이 드러났다.** 이건 `scenario-coverage-check`가 AC 단위로 내려가야 한다는 프레임워크 개선 신호다 — 별건으로 기록한다.

### 게이트 iteration 3 — 수렴 + 충돌 정정 sound

평가자 판정: **`verdict: pass`** — 2/2/2, 평균 2.0. **`collision_fix_verdict: sound`**, 감점 0.

핵심 확인 사항:
- **R-8 AC(a)(b)(c)가 PLAN F-006의 네 보장을 무손실 승계** — 동결값 425/799 → AC(a), 값 단정 삭제 0건 → AC(b) 전단, additive 불변식·`quiet_hours` 무조건 보존 → AC(b) 후단, 역검증 → AC(c). AC(c)는 PLAN 자기완결 TS에서 **TASK 수준 요구로 격상**됐다. 단언 약화 0건.
- **TS-027이 AC(a)보다 강하다** — 차단 위반을 집합으로 대조해 「3건 사라지고 다른 3건 생김」을 배제한다.
- **PLAN 산문에 「R-7=이동값」 잔존 0건** (9개 지점 전건 정렬) — G-5 기전 재발 없음. 단 페이로드에 구버전 링크 1건 잔존(G-10).
- §1 가설 표 ↔ 페이로드 **H-1~H-11 전건 일치**(iteration 2 O-1 닫힘), §4 ↔ TASK.md **전 AC 문자 매핑 존재, 미매핑 0건**.

권고 6건 **전건 반영**. 사실 주장이므로 먼저 대조했다.

| 권고 | 대조 결과 | 반영 |
|------|----------|------|
| **G-7** (강권) | `PLAN.md:802`(Step 13 완료 기준)·`:854`(§5.2 목표 열)에 「skip 수치·사유 동일」 구문면 잔존 확인 — **`1 == 1`로도 충족되어 R-4 AC(e) 미달로 Step 13 통과 가능** | 양쪽 `0 skipped` + 수집 358 불변으로 교체 |
| **G-8** (강권) | TS-035에 수집 수 단정 부재 확인 — **테스트 삭제·skip 조건 무력화로 `0 skipped` 달성 가능** | TS-035를 **3중 단정**으로(0 skipped + 수집 358 불변 + 신규 실패 0건). TS-071 기대 결과에도 수집 불변 추가 |
| **G-9** | TS-035에 G-2의 시점 처방 미적용 | 워크트리 한정 + 허브는 TS-071로 이연 |
| **G-10** | 페이로드 미전파 4건 확인 | TS-035 +R-4 / TS-041·042 +R-4 / TS-071 +R-7 / TS-023 `covers_features` F-006 → F-003 |
| **G-11** | `TASK.md` R-8 「어디에」가 `test_stats.py` — **실측: `test_routers.py:1806`·`:1844`**. `test_stats.py:59`는 §범위 ④에서 사실오류로 제외된 파일이라 그대로 두면 EXECUTE가 대상을 못 찾는다 | 정정 + 오기 사실 명기 |
| **G-12** | `TASK.md` R-4 「4곳」 vs PLAN 실측 「폴더명 3곳 + 루트 파생 3곳」 | 수량을 지우고 PLAN 실측 참조로 교체 — **틀린 수치를 다른 수치로 바꾸는 대신 근거 포인터를 둔다** |

**G-7·G-8은 같은 성질이다** — 둘 다 「통과 가능하지만 목표 미달인 경로」를 열어두고 있었다. G-7은 상류 문면이 하류보다 느슨했고(G-5의 재발 지점), G-8은 목표를 **결과값으로만** 단정해 그 결과에 도달하는 부정한 경로를 막지 않았다. `0 skipped`는 테스트를 지워도 달성된다. **결과를 단정할 때는 그 결과에 이르는 나쁜 경로를 함께 막아야 한다.**

### 게이트 종료 — 3회 사용, 수렴

| 회차 | 결정론 | 판단 | 조치 |
|------|--------|------|------|
| 1 | exit 0 (S 38) | pass (2/**1**/2, 1.67) | ⑤ 감점 사유 G-1 포함 4건 반영 |
| 2 | exit 0 (S 40) | pass (2/2/2, 2.0) | 권고 G-5·G-6·O-1 반영 |
| 3 | exit 0 (**R 8** / S **43**) | pass (2/2/2, 2.0) + sound | 권고 6건 반영 |

**세 회차 모두 pass였고 세 회차 모두 고쳤다.** 임계 통과와 결함 부재는 다르다 — 게이트를 통과 도장으로 쓰지 않은 결과, 요구사항 ID 충돌·상류 문면 이완·리워드해킹 경로까지 EXECUTE 전에 잡았다.

반복 상한 3회를 전부 소진했다. **이후 TEST-SCENARIO 재작성이 필요해지면 자율 재시도가 아니라 캡틴 에스컬레이션이다**(`opal-harness.md:57`).

---

## EXECUTE 단계 — 2026-09-07 15:20~

### Step 1·2 병렬 디스패치 [RED]

`opd`가 EXECUTE에서 워커를 쓰므로 Step 1(Python 2스위트 동치)·Step 2(Node 동치 + CLI 블랙박스)를 동시에 보냈다. PLAN이 「파일 교집합 0」으로 병렬 판정한 조합이다.

두 프롬프트에 **이 세션에서 실제로 데인 것**을 `[MUST]`로 주입했다 — 워크트리/허브 구분, 절대경로(PM cwd 드리프트 3회), `Write` 차단 → heredoc, console BE 실행 위치(`ModuleNotFoundError` 134 errors 오답), brain-tool 인터프리터(PyYAML), `@header` 107 규정, 골든 표 사본 금지(TS-060).

Step 2에는 두 개를 더 못박았다:
- **`.git`을 파일로 생성** — 진짜 worktree의 `.git`은 78바이트 파일이고 `code-scan.js`가 `existsSync`로 파일/디렉터리를 구분하지 않는 것이 버그 원인이다. 디렉터리로 만들면 픽스처가 현실을 재현하지 못한다
- **`test-shard-policy.js` 개수 단정을 `>=`로 바꾸지 마라** — 107 선례대로 등호 유지 + 13으로 갱신

RED 판정 기준을 「실패했다」가 아니라 **「올바른 이유로 실패했는가」**로 줬다.

### Step 1 PM Gate — 부분 통과, 1건 재작업

통과분:
- 골든 표 `opal/core/references/hub-root-cases.json` — **C-1~C-7 7건, 파일 1개**. `test_paths.py`는 `parents[3]`, `test_brain_tool.py`는 `_TOOL_DIR.parents[2]`로 **각자 파생해 같은 파일**을 읽는다(TS-060 충족, 인라인 0건)
- RED 올바른 이유 확인 — console BE `ImportError: cannot import name 'paths'` exit 2 / brain-tool `AttributeError: module 'brain_tool' has no attribute 'hub_root'` **10건**
- 기존 142건 pass 유지(144 passed = 142 + 신규 2)
- `.json`은 code-scan `extensions`(`code-scan.js:99` — `.py`·`.js`·`.ts`·`.vue` 등)에 **없으므로** 골든 표에 `@header` 불필요 — 워커 판단이 옳다(H-10 해당 없음)

**재작업 1건 — TS-014 테스트가 두 가지로 잘못됐다.** 보고를 그대로 받지 않고 코드를 열어 확인했다.

```python
repo_root = _TOOL_DIR.parents[2]
worktree_subpath = repo_root / ".opal-worktrees" / "task_109" / ...
if not worktree_subpath.is_dir():
    self.skipTest(...)                      # 결함 1
result = ST.find_project_root(str(worktree_subpath))
self.assertEqual(result, repo_root.resolve())   # 결함 2
```

| 결함 | 내용 |
|------|------|
| **1. `skipTest` 신설** | **TS-033**이 「동적 탐색이 0건·2건에서 조용히 skip으로 강등되지 않는다」를 요구하고, 캡틴이 이 태스크 목표를 **`0 skipped`**로 결정했다(R-4 AC(e)). 새 skip을 만드는 것은 목표에 정면으로 역행한다 |
| **2. 단언 방향 오류** (더 중요) | `find_project_root`는 세그먼트 규칙이 아니라 **`.opal/MEMORY.json` 상향 탐색**이다(`state_tool.py:700-707`). 워크트리엔 `.opal/`이 없으니 올라가서 **허브**를 반환한다. 그런데 테스트는 `repo_root`(스위트를 워크트리에서 돌리면 **워크트리 자신**)와 비교한다 — 경로가 존재했어도 **틀린 것을 단정**한다 |

결함 2가 결함 1에 가려져 있었다. skip 때문에 **한 번도 실행되지 않아서** 틀린 단언이 드러나지 않았다. **skip이 위험한 이유가 이것이다** — 통과처럼 보이면서 그 안의 오류를 영구히 숨긴다.

Step 1b로 재작업 지시: `mkdtemp` 합성 픽스처(`hub/.opal/MEMORY.json` + `hub/.opal-worktrees/task_001/...` + **`.git`을 파일로**)로 다시 쓰고, 단정을 「**허브**를 반환한다」로 바로잡고, `skipTest`를 완전 제거. 목표 `10 failed / 145 passed / 0 skipped`.

`hub_root()`와의 실제 대조는 Step 10 GREEN 후에만 가능하므로 **억지로 넣지 말라**고 명시했다. 대신 의도를 docstring에 남기되 `TODO`·`FIXME` 같은 이력성 주석이 아니라 **테스트 의도 서술**로 적으라고 했다(107 「이력은 git이 갖는다」 규정 계열).

### Step 2 PM Gate — 통과. 그리고 내가 틀렸다.

Step 2(`test-hub-root.js` 신설)는 통과했다. RED 3건이 전부 올바른 이유로 났다:
- TS-011 — `hubRootFromPath` **미export**(`actual 'undefined' expected 'function'`)
- TS-022 CLI 블랙박스 — `header_source_unset`이 **여전히 난다**(`findProjectRoot()`가 워크트리 자신의 `.git`에서 멈춘다)
- TS-022 음성 — 설정을 워크트리 쪽에만 두면 현재 코드가 **자기 `.opal/code-scan.json`을 찾아 성공**해버린다(같은 버그의 반대 방향)

`.git`을 파일로 만든 것도 `fs.statSync(...).isFile() === true` 단정으로 테스트 안에서 확인했다. `test-shard-policy.js` 개수 단정은 `12`→`13` 갱신(등호 유지, `>=` 전환 아님 — 107 선례 준수). `TS-084`(전 `spawnSync` 호출이 `OPAL_HOME` 주입) 위반을 스스로 발견해 고친 것도 좋았다.

**그런데 워커가 「기준선에 이미 8건 실패가 있었다」고 보고했고, 나는 「사실과 다르다」고 판정했다. 워커가 맞았고 내가 틀렸다.**

```
$ cd <허브>/opal/tools/code-scan && node --test tests/*.js
ℹ pass 369 / ℹ fail 0

$ cd <워크트리>/opal/tools/code-scan && node --test tests/*.js
ℹ pass 361 / ℹ fail 11        # 11 = 의도된 RED 3 + 선재 8
```

내 §2.3은 「code-scan: 허브 369/0, 워크트리 369/0」이었다. **워크트리 값이 허브를 잰 것이었다** — Bash cwd가 호출 간 유지되는데 `cd` 없이 실행했다. **이 세션에서 같은 실수를 세 번째** 했다(앞서 console BE 측정 2회). 그러면서 워커들에게는 「절대경로만 쓰라」를 `[MUST]`로 주입하고 있었다.

8건 귀속 — 전건 **`.opal/` 부재** 원인이고, 이 태스크가 고치려는 문제 자체다:

| 분류 | 테스트 |
|------|--------|
| 직접 5 | `test-regression.js` TS-044(S-14)×2 · 077 TS-052(S-19) · 077 TS-057 / `test-shard-policy.js` TS-154 |
| 메타 3 | `test-regression.js` TS-062(S-16) · `test-shard.js` S-19 · `test-shard-policy.js` TS-080 |

**이건 R-3의 추가 검증면이다.** TS-022·TS-023은 `validate`의 `header_source_unset`·exit만 봤다. **code-scan 자신의 테스트 5건이 같은 원인으로 워크트리에서 실패하고 있었고 아무 시나리오도 그것을 보지 않았다.** 게이트 3회를 돌려 40→43 시나리오까지 만들었는데도 이 면이 비어 있었다 — 시나리오 설계가 아니라 **기준선 실측**이 그것을 드러냈다.

### Step 7 PM Gate — 통과, 그리고 같은 결함 1곳 추가 발견

Step 7은 잘 됐다. `_find_repo_task_dir`(2글롭·1건 정확 매칭·0/2건 `AssertionError`·핀 자기고백 메시지) 신설, S-31이 `"098-"` 접두사로 해석. **단언 5개 원문 보존**, `confirmed_ratio 0.75`·docstring 바이트 동일. 역검증도 실제로 돌렸다 — `"999-"`(0건)·`"0"`(100건)·`"1"`(11건) 전부 **fail**(skip 아님). 「098이 backup 아래」를 단정하지 않고 「접두사 1건」만 단정했다.

**워커 보고에서 새 사실이 나왔다: state-tool skip 6건.** 내 §2.3에는 skip 수가 아예 빠져 있었다. 열어보니:

| 건수 | 사유 | 판정 |
|------|------|------|
| **3** | `<자기 루트>/tasks/098-260821-…` 부재 | **이 태스크의 결함 그 자체.** `_REPO_ROOT_093 = _TOOL_DIR.parent.parent.parent`(자기 루트 3단 상향) + 폴더명 하드코딩 → **허브에서도 skip된다** |
| 3 | `/Volumes/Data/AiStudio/workspace/opal/tasks/093-…` | **표기가 다른 옛 저장소**(`AiStudio`) 절대경로 — 되살아날 수 없다. **범위 외**, 「영구히 실행되지 않는 테스트 3건」으로 별건 기록 |

098 3건은 R-4 AC(a)(「폴더명·**자기 저장소 루트**에 의존하지 않는다」)와 AC(e)(`0 skipped`) 양쪽에 걸린다. Step 7b로 `_find_repo_task_dir` **재사용**(새 헬퍼 신설 금지 — 같은 일을 하는 함수가 둘이면 TS-060이 막는 것과 같은 분기가 생긴다) 지시.

**Step 7b 결과 (PM 독립 검증)**: `400 passed / 0 failed / 3 skipped`. 헬퍼 정의 1개, `098-260821` 하드코딩 **0건**. skip 해제 후 3건 즉시 pass — 숨겨진 로직 결함은 없었고 순수 픽스처 경로 문제였다.

### §2.3 기준선 전면 재실측

4스위트 × 2위치를 각각 직접 실행해 교체했다. 정정된 것: code-scan 워크트리 `369/0` → **`361/8`** / state-tool·brain-tool **skip 수 신설** / brain-tool 워크트리 「`.opal` 부재로 미실행」 → **정상 실행됨**. §2.3.1(code-scan 8건 귀속)·§2.3.2(state-tool skip 6건 귀속)·§2.3.3(실행 규율) 신설.

§2.3.3에 「**워크트리 측정은 매 Bash 호출마다 `cd`한다 — 앞선 `cd` 없이 실행하면 허브 수치가 나오면서 워크트리 수치로 오독된다. PM이 3회 겪었고 code-scan 기준선 오류의 원인이 그것이다**」를 `[MUST]`로 넣었다. 내 실수를 규율로 남기는 것이 같은 실수를 반복하지 않는 유일한 방법이다.

**교훈**: 나는 워커 보고를 검증하는 습관은 지켰지만, **내 측정을 검증하는 습관은 없었다.** 워커 보고와 내 기준선이 충돌했을 때 나는 내 쪽을 기준으로 삼았다 — 근거는 「내가 직접 쟀다」였고, 그 측정 자체가 틀렸다. **충돌 시 양쪽을 다시 재는 것이 옳다.**

### GREEN 4건 병렬 (Step 4·10·11·13)

PLAN이 「런타임 분리, 파일 교집합 0」으로 병렬 판정한 조합을 동시 투입했다. 3구현 프롬프트에 **골든 표를 계약 SSOT로** 못박았다 — H-1(3구현 divergence)이 P0이고, 각자 계약을 해석하면 그게 실현된다.

### Step 13 PM Gate — 통과 (독립 검증)

| 검증 항목 | 결과 |
|---|---|
| `## ` 개수 (HEAD 대비) | `opal-harness.md` 12 → **12** / `task-process.md` 1 → **1** — 107 선례(절 번호 핀) 준수 |
| §2.5 (4) 신설 | `opal-harness.md:167` |
| 규칙 정의 원문 중복 | **2줄뿐**(§2.5 (4) 내부) — 다른 문서·코드에 0건 (TS-001) |
| 변경이력 | 양 문서 `(109)` 행 추가 |
| 골든 표 C-1~C-7 정합 | 7/7 |
| §2.5 (3) 무모순 | (3)=**소재**, (4)=**그 소재를 찾는 판정**으로 역할 배타. (4) 4항이 「**(3)의** `tasks/`·`.opal/`」로 명문 참조해 대상 집합을 재정의하지 않는다 |

`doctor.py` 예외를 부칙에 넣은 것도 확인했다 — PLAN이 「주석·테스트·문서 3중 고정」으로 설계했고 이것이 그 「문서」다. 빠뜨리면 다음 사람이 doctor를 「잔여 지점」으로 오해한다.

PM 직접 정정 1건: 변경이력 시각이 `10:40`으로 적혀 있었으나 실제 작업은 15:5x였다 → `15:50`으로 수정(처음 `15:55`로 고쳤다가 현재 15:52라 **미래 시각**이 되어 다시 정정).

### Step 4 — `blocked` 보고. 그리고 워커가 내 오류를 잡았다.

`paths.py` 구현은 완료됐고 **골든 표 C-1~C-7 전건 통과**한다. 회귀도 0(`test_routers.py` 실패 33 = 기준선 33, 증감 0). 구현이 깔끔하다 — `str.split`/`str.join`만 쓰고 `normpath`·`Path()` 왕복을 일절 하지 않아 **비매칭 시 `return path`로 입력 객체를 그대로 반환**한다(바이트 동일이 구조적으로 보장).

그런데 `test_paths.py` 1건이 실패하고, 원인이 **테스트 내부 모순**이다:

```
test_hub_root_matches_golden_case[C-7]        → hub_root(P + "/.opal-worktrees/task_1") == P
test_identity_cases_are_byte_identical[C-7]   → hub_root(P + "/.opal-worktrees/task_1") == P + "/.opal-worktrees/task_1"
```
`P != P + "/..."` 이므로 **동시 충족 불가.** 어느 쪽을 만족시켜도 나머지가 깨진다.

**C-7은 항등 케이스가 아니다.** 세그먼트가 **있으므로** 부모(프리픽스)를 반환한다. 항등은 세그먼트가 **없는** C-3·C-6뿐이다. SSOT 3곳이 일치한다:
- 골든 표 C-3 desc 「세그먼트가 전혀 없으면 입력을 그대로 반환(TS-012)」 / C-6 desc 「…입력을 그대로 반환(항등, TS-012 바이트 동일 대상)」 / **C-7 desc 「절대 프리픽스 자신이 허브다」**
- `opal-harness.md §2.5 (4)` 1항(프리픽스 직하 → 프리픽스 자신) vs 3항 항등은 「세그먼트가 **없으므로**」
- 내 `TEST-SCENARIO.md` TS-012 — 「**C-3·C-6**(항등 케이스)에서 바이트 동일」

**출처는 나다.** Step 1 디스패치 프롬프트에 「**항등 케이스(C-3·C-6·C-7)**는 입력 문자열과 바이트 동일 단정」이라고 쓰고, **바로 다음 줄에** 「C-7 최상위 직하 — 세그먼트가 절대 프리픽스 직하일 때 부모는 프리픽스 자신」이라고 썼다. 한 프롬프트 안에서 모순된 지시를 내렸다.

전파 경로:
```
내 Step 1 프롬프트 (오류)
  → test_paths.py:36        _IDENTITY_CASE_IDS   = {"C-3","C-6","C-7"}
  → test_brain_tool.py:2493 _HUB_ROOT_IDENTITY_IDS = {"C-3","C-6","C-7"}
  → 두 파일 @header description 「항등 케이스(C-3·C-6·C-7)」
  → 내 Step 4·10·11 프롬프트에 그대로 재기재
```
내 TEST-SCENARIO는 옳았고 골든 표도 옳았다. **프롬프트를 쓸 때 산출물을 다시 보지 않고 기억으로 썼다** — 그것이 원인이다.

Step 4 워커는 지시대로 테스트를 고치지 않고 `blocked`로 반환하면서 **내 프롬프트가 모순을 담고 있다는 것까지 지적**했다. 올바른 처신이다.

### 추가 발견 2건 (Step 1c로 묶는다)

**(1) 골든 표 C-1이 PLAN 설계와 다르다.**

| | PLAN §3.2.3 | 실제 파일 |
|---|---|---|
| C-1 | `hub/.opal-worktrees/task_109` → `hub` | `.opal-worktrees/task_109` → 프리픽스 |
| C-2 | `hub/.opal-worktrees/task_109/opal/tools/brain-tool` → `hub` | `.opal-worktrees/…` → 프리픽스 |
| C-7 | `.opal-worktrees/task_1` → 프리픽스 자신 | 동일 |

C-1·C-2가 `hub/` 세그먼트를 잃어 **C-7과 구조적으로 동일**해졌다. 7건 설계가 실효 6건이 되고, 「허브가 이름 있는 세그먼트로 존재하는 정상 케이스」가 사라졌다.

**계약 파일을 검증 도중에 바꾸지 않았다** — 세 워커가 이 표를 계약으로 읽으며 동시 검증 중이었다. 바꾸면 그들의 보고가 무엇에 대한 것인지 알 수 없게 된다.

**(2) Node 스위트에 TS-012가 없다.** `test-hub-root.js`는 TS-011(골든 동치) + TS-022×2뿐이고 **바이트 동일 단정이 0건**이다. PLAN Step 2 작업 내용에도 그 항목이 없었다. JS도 `path.normalize` 왕복으로 똑같이 깨지므로 3구현 중 1구현만 이 성질이 검증되지 않는 상태다.

### Step 1c 범위 (Step 10·11 완료 후)

구현자가 테스트를 고치면 RED-first가 무너지므로 test-agent에 보낸다.

| # | 대상 | 내용 |
|---|------|------|
| 1 | `test_paths.py:36` | `_IDENTITY_CASE_IDS` → `{"C-3","C-6"}` + `@header` description 정정 |
| 2 | `test_brain_tool.py:2493` | `_HUB_ROOT_IDENTITY_IDS` → `{"C-3","C-6"}` + `@header` description 정정 |
| 3 | `test-hub-root.js` | **TS-012 바이트 동일 단정 신설**(C-3·C-6) |
| 4 | `hub-root-cases.json` | C-1·C-2에 `hub/` 세그먼트 복원 (PLAN §3.2.3 설계) |

### Step 10 PM Gate — 통과. H-3(P0)을 차등 테스트로 검증했다.

`hub_root` 신설 + 3지점 적용(`_load_code_scan_json:917` · `cmd_ingest_scan:1302` · `resolve_brain_path:270`). 골든 표 7/7. 기존 146건 회귀 0, skip 0 유지.

**H-3 처리가 이 Step의 핵심이다.** 기존 argparse 기본값은 맨 문자열 `"."`이라 사용자가 `--brain-path .`을 직접 입력한 경우와 **구분 불가**였다. 워커는 값 비교 대신 `str` 서브클래스 센티넬을 썼다:

```python
class _DefaultBrainPath(str): ...
DEFAULT_BRAIN_PATH = _DefaultBrainPath(".")
if isinstance(brain_path_str, _DefaultBrainPath):
    p = (_hub_cwd() / brain_path_str).resolve()   # 기본값(cwd 파생) → 허브
else:
    p = pathlib.Path(brain_path_str).resolve()    # 명시값 → 그대로
```

argparse는 명시값을 받으면 기본값을 **평범한 `str`로 교체**하므로 `isinstance`가 구조적으로 구분한다. 기존 142건은 argparse를 우회해 평범한 `str`을 넘기므로 전부 미정규화 분기를 탄다.

**PM 독립 차등 검증** — 같은 명령이 인자 유무에 따라 다른 brain을 읽는지 실측:
```
명시 --brain-path <tmp>  → total 0        (tmp brain, 비어있음)
기본값 (워크트리 cwd)     → total 3        pages: <허브>/.opal/brain/pages/concept/worktr…
허브 brain git status    → (빈 출력)      무변경
```
P0 데이터 파괴 위험이 닫혔다. 「회귀 0」만으로는 이걸 증명하지 못한다 — **인자 유무로 답이 갈리는 것**을 봐야 한다.

미적용 판단도 근거가 좋다: `TEMPLATES_DIR`(`__file__` 파생)은 **실행 중인 스크립트 자신의 동거 자산**이므로 수렴하면 워크트리 코드가 허브 템플릿을 로드한다 — 「한 트리의 로직을 다른 트리의 데이터에 실행」이 되어 §2.5 (4) 4항에 걸린다.

### Step 11 — 진짜 설계 충돌 발견. 규칙 위반이었다.

허브는 안전했다: **TS-021 바이트 동일 27,614 B / exit 2 양쪽 동일**(PM 재현). 워크트리 `header_source_unset` 소거. 골든 표 7/7.

그런데 워크트리 code-scan이 **11 → 55 fail**. 워커가 비-워크트리 사본을 만들어 before/after를 떠서 **자기 변경 기인 신규 실패 0건**을 실증했다. PM이 기전을 직접 재현:

```
픽스처 tests/fixtures/codemap-repo/ — 자기 .opal/ 보유 (자기완결 프로젝트)
그 cwd에서 scan --json  →  total: 0        ← 픽스처 대신 허브로 납치
```

**이건 테스트 충돌이 아니라 규칙 위반이다.** §2.5 (4) **4항**은 「정규화는 **허브 고정 데이터** 참조에만 적용하고 **소스 트리 내용**에는 적용하지 않는다」고 규정한다. 그런데 `findProjectRoot()`는 **설정 조회와 스캔 루트를 겸한다.** 무조건 정규화하면 스캔 루트까지 허브로 옮겨져 4항을 위반한다. **PLAN의 「진입점 정규화」 설계 자체가 규칙 4항과 어긋나 있었다** — 게이트 3회를 돌렸는데도 이 어긋남은 드러나지 않았다. 드러낸 것은 **실행**이다.

PM 승인 1건: 워커의 지시 초과 1줄(`return process.cwd()` → `return start`). 함수 말미 폴백이 비정규화 cwd를 재주입해 TS-022 음성이 GREEN이 되지 않았다. 항등일 때 `start === process.cwd()`이므로 PLAN의 바이트 동일 근거는 유지되고 27,614 B 동일이 실증한다. 탐색 루프 본문 무변경.

### 소유자 결정 — 리터럴 우선

캡틴 선택: **「자기 `.opal/`을 가진 루트가 이긴다」.** 3안 중 이것이 §2.5 (4) 4항에 가장 가깝다.

**핵심 통찰**: `.opal/`은 (3)에 의해 **허브 고정**이므로, 워크트리 루트에서 `.opal/`만 보고 상향 탐색하면 **세그먼트 규칙 없이도 자연히 허브에 닿는다.** 지금 문제는 탐색이 워크트리 루트의 `.git`(**파일**)에서 멈추는 것이었다. 정규화는 그 탐색이 실패했을 때의 **폴백**이 옳은 위치다.

반영 3건:
- **Step 13b (PM 직접)** — §2.5 (4) 부칙에 「탐색 우선순위」 신설 + v1.19. `## ` 12 유지, 부칙 4건
- **Step 11b** — `findProjectRoot()` 2단 탐색(1차 리터럴 `.opal/`, 2차만 세그먼트 정규화). 2차 본문 무변경
- **Step 1c** — 테스트 계약 정정 5건

### Step 1c PM Gate — 통과 (독립 검증)

```
C-1  hub/.opal-worktrees/task_109  → hub      ← 복원 (C-7과의 중복 해소)
C-2  hub/.opal-worktrees/…         → hub      ← 복원
C-7  .opal-worktrees/task_1        → 프리픽스   ← 경계 유지
입력 중복 없음 / 7 케이스

_IDENTITY_CASE_IDS      = {"C-3", "C-6"}
_HUB_ROOT_IDENTITY_IDS  = {"C-3", "C-6"}

test_paths.py  → 9 passed / 0 failed
brain-tool     → 146 passed + 9 subtests / 0 failed / 0 skipped
```

**TS-022 음성을 둘로 쪼갠 것이 좋은 처리다** — 음성(어느 조상에도 `.opal/`이 없으면 `header_source_unset`)과 **신설 양성**(자기 `.opal/`을 가진 자기완결 프로젝트가 `.opal-worktrees` 하위에 있어도 자기 루트로 인식된다)으로 나눠 **양방향에서** 계약을 고정했다. 단정을 지우지 않고 **옳은 것으로 교체**했다. Node TS-012(바이트 동일)도 신설돼 3구현 전부가 그 성질을 검증한다.

### 내 오류 2건 — 같은 원인

| 오류 | 전파 |
|------|------|
| 「항등 케이스(C-3·C-6·**C-7**)」 | Step 1 프롬프트 → 두 Python 테스트 파일 상수·`@header` → Step 4·10·11 프롬프트 |
| 「워크트리 code-scan 369/0」 | §2.3 기준선 → 워커 보고를 「사실과 다르다」고 오판 |

**둘 다 산출물을 다시 보지 않고 기억으로 썼다.** 내 `TEST-SCENARIO.md` TS-012는 「C-3·C-6」으로 옳게 적혀 있었고 골든 표 desc도 옳았다. 프롬프트를 쓸 때 그 두 곳을 열어보지 않았다.

구현 워커 2명이 **독립적으로 같은 모순을 발견해 `blocked`로 반환**했고, 한 명은 「세 구현이 이 표 하나를 읽으므로 C-7을 항등으로 오독한 구현이 하나라도 생기면 그게 바로 H-1이다」까지 지적했다. 정확하다 — 내 프롬프트 오류가 실제로 P0 리스크를 만들고 있었다.

**규율화**: 디스패치 프롬프트에 계약·수치를 적을 때는 **해당 산출물 줄을 그 자리에서 읽어 인용**한다. 기억으로 쓰지 않는다.

### Step 11b PM Gate — 통과. 55 → 7.

| 검증 | 결과 |
|---|---|
| 허브 바이트 동일 (TS-021) | **27,614 B / exit 2 동일** (PM 재현). 워커는 조회 3커맨드도 추가 확인 — scan 123,913 B · summary 1,347 B · missing 13,921 B 전부 동일 |
| 픽스처 인식 (개정 목적) | 개정 전 **110 엔트리 전량 허브 자산** → 개정 후 **9 엔트리 전량 픽스처 자신** |
| 워크트리 전량 | 317/55 → **367 pass / 7 fail** |
| `test-hub-root.js` | **5/5** (TS-011 · TS-012 신설 · TS-022 · TS-022 음성 · TS-022 리터럴 우선 양성) |
| `hubRootFromPath` 무변경 | 확인, 골든 7/7 재확인 |

**1차 탐색이 허브를 안 바꾸는 이유**(워커 설명이 정확하다): 허브 루트에는 `.git`·`.opal/`·`CLAUDE.md`가 **모두 있다.** 개정 전에는 2차 루프가 `||` 단축평가로 `.git`을 먼저 만나 허브 루트를 반환했고, 개정 후에는 1차 루프가 `.opal/`을 만나 **같은 디렉터리**를 반환한다. 두 마커가 공존하므로 반환 문자열이 동일하고, 1차가 첫 반복에서 즉시 반환하므로 `hubRootFromPath`는 허브에서 아예 실행되지 않는다.

개정이 만드는 유일한 선순위 역전: 「가까운 조상이 `.git`만, 먼 조상이 `.opal/`」일 때 먼 쪽을 택한다. **그것이 정확히 워크트리 케이스**(워크트리 루트의 `.git` **파일** vs 허브의 `.opal/`)이고 §2.5 (4)가 요구하는 방향이다.

### 남은 7건 — 전건 귀속 확정, 구조적 잔존

`§2.3.4`를 신설해 문서화했다. **판정 규율: 「이 목록에 귀속되지 않는 실패가 0건인가」로 판정하고 총계 0을 요구하지 않는다.**

| 건 | 귀속 | 해소 |
|---|------|------|
| TS-044 ×2 · TS-154 | `__dirname` 기준 repoRoot(워크트리 루트)에 `.opal/` 요구. `findProjectRoot` 미경유 | 머지 후 허브 |
| 077 TS-057 | 스캔이 허브 트리를 보므로 **워크트리 신규 파일이 거기 없다** | 머지 후 허브 |
| TS-045 | `brain_tool.py` 기능 코드를 `git show HEAD:`와 바이트 동일로 고정 | **브랜치 커밋 시** |
| 집계기 3 (TS-062·TS-080·S-19) | 위 4건 캐스케이드 | 따라 해소 |

**077 TS-057은 규칙이 이미 명문화한 한계다** — §2.5 (4) 5항. 워커가 「허브 코드맵에 `test-hub-root.js`를 등록해 해소」를 대안으로 올렸으나 **채택하지 않았다**: 그것은 미머지 파일을 검증된 것으로 **위장**하는 처방이다. 규칙이 「워크트리 실행은 미머지 변경을 검증하지 않는다」고 이미 말했으므로, 이 실패는 규칙 준수의 증거이지 결함이 아니다.

**TS-045는 내가 태스크 107에서 만든 핀이다.** 107에서 「파일 전체 바이트 동일」 → 「`@header` 제외 후 기능 코드 바이트 동일」로 좁혔으나 **기능 코드에 대한 핀 자체는 남겼다.** 107 brain에 「태스크 시점 사실을 영구 회귀 단언으로 고정하면 후속이 구조적으로 걸린다」를 교훈으로 적어놓고 같은 회차에 그 패턴을 하나 더 만들었다. **4번째 사례**다. 별건: TS-045 은퇴 또는 재설계.

### Step 6 PM Gate — 통과 (독립 검증)

```
3파일: 1 failed / 24 passed / 0 skipped     ← skip 3 → 0
test_scanner RED: "2단 열거 합산 불일치: 3 / assert 3 == 4"  (단일 열거 미도입 = 정확한 사유)
H-7 2파일 git status: (빈 출력) 무변경
합성 픽스처 task_count 2/0 보존 (TS-046)
```

**021 태스크가 `tasks/backup/021-*`로 이관돼 있었고 접두사 탐색이 정확히 1건을 찾아 통과했다.** 허브에서도 나던 skip이 풀렸고 **숨겨져 있던 실패는 없었다** — 순수 픽스처 경로 문제였다.

`test_adapters.py`의 `pytest.skip` 1건을 `_find_repo_task_dir`의 `AssertionError`로 대체한 것이 정확하다 — 워커 표현대로 「실패 검출 강도는 오히려 강화」다.

**추가 관측 (별건)**: `test_parsers.py`에 `pytest.skip` **7건이 잠복**해 있다 — HEAD 대비 diff에 추가·삭제 0건이므로 전부 선재다. 현재 어느 것도 트리거되지 않고(그중 `AGENT.md 없음` 2건이 방금 해소된 것), 사유는 `fixture_doc_populated.json 없음` 4 · `AGENT.md 없음` 2 · `실 파일 없음` 1이다. R-4가 지정한 하드코딩 지점(3+3)에 없으므로 **범위를 넓히지 않았다.** 다만 TS-033이 막는 유형이 그대로 남아 있으므로 별건으로 올린다 — 자산 부재가 조용한 통과로 강등되는 분기 7개.
