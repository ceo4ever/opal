# SCENARIO GATE 2 — 목표-커버 루브릭 재판정 (판단축 ①⑤⑥)

> 실행 일시: 2026-09-07T14:25:00+09:00 | phase: `scenario-rubric` | iteration: **2**
> scenario_source: `tasks/109-260906-opds-태스크루트-해석-수렴/TEST-SCENARIO.md` (40 시나리오)
> 판정 주체: opal-evaluator-agent (Producer≠Evaluator, `scenario-gate.md` §4) — iteration 1과 **다른 세션**
> 규칙 SSOT: `opal/core/references/harness/scenario-gate.md` §2(6축)·§5-1(종료조건 임계)
> 관할: **①⑤⑥만**. ②③④는 test-tool `scenario-coverage-check` exit 0 기수령(`all_covered: true` / R 7 · F 6 · H 11 · TS **40**) — 재채점하지 않았다.
> CONTRACT.md: 부재 — `scenario-rubric`은 Phase 2 CONTRACT 병합 대상이 아니므로 판정에 영향 없음.
> 성격: **재채점**. iteration 1(①2/⑤1/⑥2, 평균 1.67, `pass`)의 gap 4건 반영 검증이 본 회차의 1차 임무다.

## 1. 검증 절차

1. `SCENARIO-GATE-1.md`·`.scenario-gate-history.json`으로 직전 판정 근거를 확정했다.
2. **G-1~G-4를 각각 「닫혔다/열려 있다」로 개별 판정**했다 — 문장 추가 여부가 아니라 **단정의 강도**로 판정했다.
3. **`TASK.md:132`(R-6) 원문을 직접 읽어** §4 매핑 표의 AC 문자를 한 글자씩 대조했다. Producer의 재정렬이 또 어긋났을 가능성을 전제했다.
4. `.scenario-coverage-input.json` 3플래그(goal 9 / adoption 13 / boundary 13)는 **Producer 산정값이므로 신뢰하지 않고** 본문 대조로 재검증했다.
5. **약화 검사** — G-2 반영이 이연이 아니라 **삭제**였는지 TS-023 문면과 TS-071 인수 범위를 대조했다.
6. **새 결함 검사** — TS ID 충돌·중복, §1 가설 표 ↔ §3 ↔ §4 ↔ 페이로드 4자 정합, 개수 표기, 상류(PLAN) 정합.

기계 대조 결과(ID 집합):

| 검사 | 결과 |
|------|------|
| 문서 TS ID 유니크 수 | **40** |
| 페이로드 시나리오 수 / 유니크 | **40 / 40** |
| 문서 − 페이로드 차집합 | **0** |
| 페이로드 − 문서 차집합 | **0** |
| ID 충돌·중복 | **0건** (TS-047·TS-071 신규 번호 충돌 없음) |
| iteration 1(38) → 2(40) 증분 | **+2 = TS-047, TS-071** (정확히 신설 2건, 삭제 0건) |

## 2. gap 반영 검증 — G-1~G-4

### G-1 (⑤ 감점 사유) — **닫혔다**

TS-047이 요구된 **세 단정을 모두** 담았다. 하나라도 빠지면 열린 것으로 판정할 기준이었다.

| 요구 단정 | TS-047 문면 | 판정 |
|---|---|---|
| ① 함수가 1개 | 「**열거 함수가 1개이고**」 | 충족 |
| ② 3지점이 **그것만** 호출 | 「**3지점(`scanner.py`·`routers/dashboard.py`·`routers/tasks.py`)이 그것만 호출한다**」 — 지점을 파일명으로 못박음 | 충족 |
| ③ 잔여 직접 열거 0건 | 「`grep`으로 **잔여 직접 `scandir`/`iterdir` 열거 0건**」 | 충족 |

세 절이 **한 시나리오 안에서 AND로 묶여** 있어, 함수를 1개 두고도 3지점이 각자 열거를 유지한 상태가 통과할 경로가 없다. 게다가 기대 결과가 자기 논거까지 적었다 — 「출력이 오늘 같은 것으로는 3지점이 각자 열거를 유지한 상태를 배제하지 못한다(**TS-040의 사각**)」. iteration 1이 지적한 **비대칭**(조립 축에는 TS-020의 grep 잔존0이 있는데 열거 축에는 등가물이 없음)이 해소되었다.

구형 잔존 4개 부류가 이제 전건 잔존0 단정을 가진다:

| 구형 부류 | 잔존0 단정 시나리오 | iteration 1 상태 |
|---|---|---|
| `<자기 루트>/tasks`·`.opal` 직접 조립 | TS-020 (정규화 미경유 0건) | 있었음 |
| 특정 태스크 폴더명 하드코딩 | TS-030 (리터럴 0건) | 있었음 |
| **3지점 각자 열거** | **TS-047 (함수 1개 + 그것만 호출 + 잔존 0건)** | **부재 → 신설** |
| 중복 구현·사본 | TS-010(구현 1개씩) · TS-060(표 파일 정확히 1개) | 있었음 |

신형 채택 3항도 전건 대응된다 — 3런타임 해석 함수 경유(TS-011/TS-012/TS-014·TS-020), **단일 열거 함수 호출(TS-047)**, 공유 골든 케이스 표(TS-060).

### G-2 — **닫혔다. 삭제가 아니라 이연이다**

TS-023 개정 문면: 「FE `@header` 3건 정비 후 **워크트리 `validate` exit 0**. 허브 exit 0은 코드 변경이 워크트리 한정이므로 **머지 후 TS-071에서 확인**한다(TS-070과 동일한 시점 규율)」.

**약화 검사 통과** — 원래 있던 「허브 exit 0」 인수가 **없어지지 않고 TS-071로 옮겨졌고**, 옮겨간 곳에서 오히려 강해졌다(TS-071은 허브 `validate` exit 0에 **현재값 exit 2 / 차단 3건**을 명기해 대비 판정으로 만들었다). 이연 대상을 **TS ID로 지목**했으므로 추적 경로도 끊기지 않는다.

### G-3 — **닫혔다. 장식이 아니다**

TS-071은 「CLOSE 게이트에서 채움」 셀만 있는 형식적 이연이 아니다. **판정 기준이 허브 현재 수치 대비 목표로 명시**되어 있다:

| TS-071 인수 | 현재값 명기 | §2.3 기준선 표 대조 |
|---|---|---|
| 허브 state-tool `0 failed` | **현재 1** | §2.3 「허브 현재 1 failed / 396」 **일치** |
| 허브 console BE `0 failed` | **현재 9** | §2.3 「허브 현재 **9 failed** / 348」 **일치** |
| 허브 `validate` exit 0 | **현재 exit 2 / 차단 3건** | TASK §배경 (6) FE 3파일 `uncovered:incomplete` 차단 **일치** |

자기 ID·`도구`·`실행 명령`·`결과`·`상세` 셀을 모두 갖췄고, 실행 방식 M1에 「**실행은 자동, 시점이 CLOSE**」를 분리 표기했다. 「머지 전에는 실행 불가이고 **그것이 정상이다**」를 조건 셀에 못박아, 미실행이 미달로 오독되는 경로도 막았다. §1 H-11 행·§4 R-5 AC(a)(b)·R-7 AC(c)에 전건 등재되어 매핑 고아가 아니다.

### G-4 — **닫혔다. TASK.md:132 원문과 한 글자씩 일치한다**

`TASK.md:132` R-6 AC 원문을 직접 읽어 대조했다.

| TASK.md:132 원문 | TEST-SCENARIO §4 매핑 | 지목 TS | 판정 |
|---|---|---|---|
| (a) 열거 함수가 1개이고 3지점이 그것만 호출한다 | R-6 AC(a) — 「함수 1개 · 3지점이 그것만 호출」 | **TS-047**, TS-040 | 일치 |
| (b) `grep`으로 잔여 직접 `scandir`/`iterdir` 열거 0건 | R-6 AC(b) — 「잔여 `scandir`/`iterdir` 0건」 | **TS-047** | 일치 |
| (c) 동결 코호트 21건이 관측 집합에 전건 포함된다 | R-6 AC(c) — 「동결 코호트 21건 전건 관측」 | TS-042 | 일치 |
| (d) 코호트 필터 중앙값이 재현된다 | R-6 AC(d) — 「코호트 필터 중앙값 재현」 | TS-041, TS-042 | 일치 |
| (e) 아카이브 구분이 필요한 지점(`tasks.py` archive 컬럼)의 기존 동작 무변경 | R-6 AC(e) — 「`archive` 컬럼 기존 동작 무변경」 | TS-043 | 일치 |

**AC(e) 본문 확인(요청 항목)** — 매핑 표 한 줄로 끝내지 않았다. TS-043의 **기대 결과 본문**이 확장되어 「+ **`tasks.py` `archive` 컬럼 기존 동작 무변경**(backup 소재 true · 직속 false)」로 **구체 기대값까지** 담고 있다. 표만 정렬하고 본문은 비어 있는 형태가 아니다.

iteration 1이 지적한 한 칸 밀림(AC(b) 소실 → (c)(d)(e) 당겨짐)은 전건 해소되었고, 미매핑이던 AC(e)도 등재되었다.

## 3. 판단축별 재판정

| 축 | it.1 | **it.2** | 근거 (시나리오 ID 지목) |
|----|------|------|------------------------|
| **① 목표 달성** | 2 | **2** | 목표(「허브·워크트리 **양쪽** `0 failed`」)의 **양쪽이 이제 각각 자기 ID를 가진다** — 워크트리 절반은 **TS-070**(§2.3 4스위트를 실행 위치·인터프리터 `[MUST]`까지 고정해 **절대 수치가 아니라 기준선 표 대비**로 판정), 허브 절반은 **TS-071**(현재 1·9·exit 2 대비 목표 0·0·exit 0, CLOSE 시점 명기). iteration 1에서 「TS-070 기대 결과 안의 한 문장」이던 허브 절반이 독립 시나리오로 승격되어 ①의 근거가 **보강**되었다(등급은 이미 상한 2). 목표 기전도 실 CLI로 검증된다 — **TS-022**(워크트리 cwd `validate` → `header_source_unset` 소거) · **TS-023**(FE 3건 정비 후 워크트리 exit 0) · **TS-025**(워크트리 cwd `brain-tool search` → 허브 brain `ok:true`) · **TS-035**(skip 수·`-rs` 사유 목록 허브 동일). 별개 축(위치 변경 내성)은 **TS-032**(098이 `tasks/backup/` 아래인 **현물 상태**에서 S-31 통과). **TS-061**(L3/M3)이 「검증됐다는 착각」 자체를 사람 판정으로 막아 목표 달성 선언의 진위를 보호한다 — 070 사건의 실패 양식에 대한 직접 방어. **시점 함정 재검사 통과**: TS-023·TS-070·TS-071·§2.3 각주 **4개 지점의 시점 규율이 서로 모순 없이 읽힌다**(§4 하단 참조) |
| **⑤ 채택/잔존** | **1** | **2** | **G-1이 실질적으로 닫혔다** — 감점 사유가 소멸했다. TS-047이 「함수 1개」·「3지점이 **그것만** 호출」·「잔여 직접 열거 **0건**」 **세 절을 AND로** 단정하므로, iteration 1이 통과 가능하다고 지목한 상태(3지점이 각자 열거를 유지한 채 오늘 답만 일치)가 배제된다. 이로써 구형 잔존 **4개 부류 전건**이 잔존0 단정을 갖고(TS-020·TS-030·**TS-047**·TS-010/TS-060), 신형 채택 **3항 전건**이 커버된다(TS-011/TS-012/TS-014·**TS-047**·TS-060 + 운영 채택 TS-022/TS-023/TS-025 + 결과 채택 TS-042 코호트 21건 전건 관측). iteration 1이 지적한 **비대칭의 논리적 뿌리**(TS-060 자신의 논거 「오늘 답이 같은 것과 내일도 같을 것은 다른 명제다」가 골든 표에만 적용되고 열거 함수에는 적용되지 않았다)가 TS-047 기대 결과에 **명문으로 이식**되었다. 잔여 권고는 G-6(grep 패턴이 API 이름 열거형) 1건이며, 같은 시나리오 안의 property 절(「그것만 호출」)이 그 사각을 덮으므로 감점하지 않는다 |
| **⑥ 경계/부정** | 2 | **2** | 경계·부정 시나리오 집합은 **이번 반영으로 변경되지 않았고**(boundary 13건 동일, 삭제 0건) iteration 1 근거가 그대로 유지된다. ⓐ **조용한 강등 차단** — **TS-033**(접두사 매칭 0건·2건에서 **skip이 아니라 실패**; PLAN 「`skipTest` 금지」와 정합). ⓑ **역검증(mutation)** — **TS-053**(코호트 데이터 인위 변경 시 두 테스트가 **실패**) — 「통과하도록 고친 것」과 「올바르게 고친 것」을 구별한다. 단언 약화(H-8) 방어 4단(TS-031 → TS-051 → TS-052 → TS-053) 유지. ⓒ **경로 이탈(P0 보안)** — **TS-045**(`../`·절대경로·구분자 전건 404, 「2단 열거로 조립 지점이 늘었으므로 **각 지점을 개별 확인**」). ⓓ **must-NOT 4건** — TS-024(명시 `--brain-path`가 허브로 리다이렉트되지 **않음**) · TS-034(소스 트리 참조 무변경 — 정규화하면 단언 의미가 **반전**) · TS-044(doctor가 허브로 치환하지 **않음**) · TS-046(합성 픽스처 `task_count` 2/0 불변). ⓔ **규칙 자체의 경계값** — TS-011이 실행하는 골든 표에 C-5 중첩 워크트리·C-6 유사 이름 비매칭(`.opal-worktrees-old` → 항등)·C-7 세그먼트 최상위 직하·C-3 항등이 포함되어 「세그먼트 완전 일치」가 3런타임 실행 대조로 고정된다. **하강 요인 검사 결과 없음** — 신설 2건은 인수를 늘렸을 뿐이고, TS-023 개정은 인수를 TS-071로 이동시켰을 뿐 삭제가 없다 |

> **iteration 1 판정에 대한 이견**: 없다. ⑤=1의 사유(G-1)를 재검증했고 당시 판정이 타당했다고 본다. 본 회차의 ⑤ 상향은 **직전 평가자에 대한 동조가 아니라 TS-047 신설이라는 문면 변화**에 근거한다 — TS-047이 없다면 ⑤는 1에 머물렀다.

## 4. 시점 규율 4자 정합 검사 (요청 항목 4)

| 지점 | 문면 | 정합 |
|---|---|---|
| §2.3 각주 | 「허브 `0 failed` 검증 시점은 **머지 후 CLOSE 게이트**다 — 코드 변경이 워크트리 한정이므로」 | 기준 |
| TS-023 | 워크트리 exit 0(즉시) / 허브 exit 0은 **머지 후 TS-071에서** | 일치 |
| TS-070 | 워크트리 4스위트 목표 도달 / 「허브 양 스위트 `0 failed`는 **머지 후 CLOSE 게이트에서** 확인」 | 일치 |
| TS-071 | **머지 후** 허브 실행 / 「머지 전에는 **실행 불가**이고 그것이 정상이다」 | 일치 |
| §4 매핑 R-7 AC(c) | 「TS-023(워크트리), **TS-071**(허브·머지 후) — **양쪽 exit 0 — 시점 분리**」 | 일치 |

**모순 0건.** 네 지점이 같은 규율을 말하고, 이연 대상이 ID로 특정되며, 「양쪽」의 완성 지점이 TS-071 하나로 수렴한다. iteration 1의 문서 내 불일치(TS-023만 시점 분리 누락)는 해소되었다.

## 5. 새 gap / 관찰

### 새 gap (감점 아님 — TEST 진입 전 처리 권고)

| # | 축 | 지목 | 내용 |
|---|----|------|------|
| **G-5** | 정합 (권고) | `PLAN.md:556-560` · `PLAN.md:438` | **G-4 반영이 TEST-SCENARIO만 정렬하고 상류 PLAN을 남겨, 이제 두 문서가 서로 다른 AC 문자를 말한다.** PLAN §3.5.5는 여전히 구버전 문자다 — TS-041→`R-6 AC(b)` · TS-042→`AC(b)` · TS-043→`AC(c)` · TS-044→`AC(d)`(TEST-SCENARIO는 각각 (d)·(c)(d)·(e)·비-AC로 정렬됨). PLAN:438 TS-023도 「워크트리·**허브** 양쪽 `validate` exit 0」로 **G-2 이전 문면이 잔존**한다. iteration 1이 진단한 G-4의 발생 기전이 「PLAN 문자를 TASK 개정 후에도 승계」였으므로, **원천이 그대로면 같은 드리프트가 재발할 경로가 열려 있다**. TEST-SCENARIO는 `TASK.md`(AC SSOT)와 일치하므로 채점 대상 산출물의 결함은 아니다 — 감점하지 않는다. **처방**: PLAN §3.5.5 AC 문자 재정렬 + PLAN:438 시점 분리 반영(또는 PLAN §x.5를 「TEST-SCENARIO §4가 SSOT」로 포인터화) |
| **G-6** | ⑤ (권고) | TS-047 grep 절 | 잔존0 단정의 grep 절이 **`scandir`/`iterdir` 두 API 이름만 열거**한다. `os.listdir`·`glob.glob`·`Path.glob`·`os.walk`로 열거하면 이 grep을 통과하면서 3지점 각자 열거가 되살아난다. `TASK.md:132` AC(b) 문면에는 충실하지만, **TS-060이 스스로 인용한 107 brain `prohibit-by-property-not-name`**(사례가 아니라 성질을 막는다)을 적용하면 이름 열거는 방패가 될 수 있다. **감점하지 않는 이유**: 같은 TS-047 안의 property 절(「3지점이 **그것만** 호출한다」)이 이 우회를 이미 배제하므로 시나리오의 판정력은 유지된다. **처방**: EXECUTE/TEST에서 grep 패턴을 열거 API 전체(`scandir`·`iterdir`·`listdir`·`glob`·`walk`)로 확장하고, 판정의 주절을 「3지점이 열거 함수만 호출」 property로 둔다 |

### 관찰 (채점 무영향)

- **O-1 §1 가설 표가 망라적이지 않다** — 페이로드 대비 §1 「시나리오」 셀 누락: H-1 3건(TS-001·TS-002·TS-010) · H-2 2건(TS-001·TS-020) · **H-4 1건(TS-040)** · H-6 1건(TS-030) · H-8 1건(TS-031). **§1 잉여(페이로드에 없는 ID)는 0건**이고 페이로드가 상위집합이므로 ②③④ 결정론 판정에 영향이 없다(`all_covered: true` 유지). H-4 행에서 TS-040이 빠지고 그 선두 자리에 TS-047이 들어간 형태이지만, **동일한 누락 패턴이 H-1·H-2·H-6·H-8에도 있으므로 이번 반영으로 생긴 삭제로 단정하지 않는다**(이 태스크 폴더에 TEST-SCENARIO.md의 git 이력이 없어 직접 diff로는 확인 불가). 다만 `scenario-gate.md` §3이 §1을 페이로드 원천의 하나로 지정하므로, 페이로드를 §1만으로 재생성하면 커버가 줄어든다 — **잠재 위험으로 기록**.
- **O-2 페이로드 TS-071의 `covers_requirements`에 R-7 누락** — §4는 TS-071을 R-7 AC(c)에도 매핑하는데 페이로드는 `["R-5"]`만이다. TS-023이 R-7을 물고 있어 결정론 커버는 유지되므로 게이트 결과에는 영향 없다. 문서↔페이로드 정합 차원의 기록.
- **O-3 §2.2 시나리오별 데이터 흐름 표에 TS-071 행이 없다** — TS-040~047 행은 「함수 1개·잔존 0」으로 정확히 갱신되었으나(G-1 반영이 §2.2까지 전파됨), TS-070 행만 있고 TS-071 행이 추가되지 않았다. 편집 누락이며 판정력에는 영향 없다.
- **O-4 플래그 과대 산정 2건 미해소(iteration 1 O-1·O-2와 동일)** — `TS-042`·`TS-061`의 `is_goal_scenario: true`. TS-042의 실체는 R-6 AC 기전 검증, TS-061은 「목표 달성」이 아니라 「목표 달성 **판정의 진위**」를 지키는 메타 방어다. 목표축이 TS-070·TS-071로 성립하므로 축 등급에 영향 없다. **축을 뒤집는 과대 산정은 없다.**
- **O-5 TS-043의 가설 귀속** — §4가 R-6 AC(e)를 H-5(경로 이탈) 행으로 적었으나 archive 컬럼 불변은 H-4(모수 증가) 계열 관심사에 가깝다. TS-046이 H-4 불변을 별도로 물고 있어 실질 커버 손실은 없다.
- **O-6 TS-026 표제·필드 불일치(iteration 1 O-4 미해소)** — 「### L3」 표제 아래 배치되었으나 `계층`은 L2, 실행 방식은 M1(문서 grep)이다. 편집 사항.
- **O-7 상류 개수 표기 불일치는 TEST-SCENARIO에 전파되지 않았다** — `TASK.md`는 R-3 제목·§확정된 설계 방향에서 「경로 조립 8곳」, §명확화 결과·§범위에서 「9곳」으로 갈린다(페이로드 goal 문장은 9곳). TEST-SCENARIO는 **수치를 단정하지 않고 property(TS-020 「미경유 지점 0건」)로 적었기 때문에** 이 불일치에 면역이다. 시나리오 설계상 옳은 선택으로 판정한다.

## 6. 종합

```json
{"scores": {"goal": 2, "adoption": 2, "boundary": 2}, "average": 2.0, "gaps": ["G-5: PLAN §3.5.5(:556-560) R-6 AC 문자·PLAN:438 TS-023 시점이 구버전 잔존 — TEST-SCENARIO만 정렬되어 상류↔하류 불일치 및 드리프트 재발 경로 (감점 아님, 권고)", "G-6: TS-047 grep 절이 scandir/iterdir 두 API 이름 열거형 — listdir/glob/walk 우회 가능. 같은 TS의 property 절이 덮으므로 감점 아님, 실행 시 패턴 확장 권고 (권고)"], "closed": ["G-1", "G-2", "G-3", "G-4"], "still_open": [], "verdict": "pass"}
```

| 항목 | it.1 | **it.2** | 통과선 |
|------|------|------|--------|
| ① 목표 달성 | 2 | **2** | ≥1 |
| ⑤ 채택/잔존 | **1** | **2** | ≥1 |
| ⑥ 경계/부정 | 2 | **2** | ≥1 |
| 평균 | 1.67 | **2.0** | ≥1.5 |
| 0점 축 | 없음 | **없음** | — |
| **verdict** | pass | **pass** | — |

판정 근거: `scenario-gate.md` §5-1 — 「판단축(①⑤⑥) 각 ≥ 1점(0점 축 없음) **AND** 평균 ≥ 1.5점」. 세 축 모두 통과선 충족, 평균 2.0 ≥ 1.5 → **수렴(PASS)**.

**iteration 1 gap 4건 전건 닫힘(`still_open: []`)**. G-1은 형식 추가가 아니라 **세 절 AND 단정**으로 닫혔고, G-2는 삭제가 아닌 이연이며, G-3은 현재값 대비 목표를 가진 실판정 시나리오이고, G-4는 `TASK.md:132` 원문과 한 글자씩 일치한다. 무진전(§5-3) 신호 없음 — 점수 +0.33, 열린 gap 4→0.

> **[MUST] 새 gap 2건(G-5·G-6)은 감점 사유가 아니다.** G-5는 **상류 PLAN**의 미정렬(채점 대상 산출물은 SSOT와 일치), G-6은 **같은 TS 안의 property 절이 덮는 잔여 권고**다. 다만 G-5는 iteration 1이 진단한 G-4 발생 기전(문자 승계)의 **원천을 그대로 남긴 것**이므로 TEST 진입 전 정리하는 편이 옳다. 반영 여부·방법의 판단과 실행은 **PM의 책임**이며, 본 에이전트는 판정과 제안만 반환한다(verdict-only, 산출물 무수정).

> **[참고] 게이트 통과 성립 요건**(§6 tool-gated 집행): test-tool `scenario-coverage-check` exit 0 **AND** 본 `scenario-rubric` verdict pass. 두 증거가 모두 존재하므로 게이트 PASS가 성립한다.
