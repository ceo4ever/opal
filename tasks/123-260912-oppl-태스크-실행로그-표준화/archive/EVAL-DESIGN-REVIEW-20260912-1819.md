# EVAL: 설계 루프 D6 명세 리뷰 2회차 — 태스크 실행 로그 표준화

- 실행 일시: 2026-09-12T18:19:00+09:00
- phase: `design-review` (2회차 — 재판정 대상은 1회차 미해결 U-1~U-10)
- target_artifacts: `PRD.md`, `TRD.md`, `CONTRACT.md`, `BACKLOG.md` + `backlog.json`
- 기준 원천: `CONTRACT.md` §5 루브릭절 (RB-1~RB-5, 통과선 포함)
- 1회차 보고서: `EVAL-DESIGN-REVIEW-20260912-1806.md` (수정하지 않았다)
- 판정 방법: **해소 주장을 근거로 쓰지 않았다.** `CONTRACT.md`는 1회차 시점 전문을 보존해 두고 현재 파일과 라인 단위 diff를 떠서 무엇이 추가·수정됐는지 전건 확인했고, 나머지는 해당 절을 직접 읽었다. U-1은 `backlog.json`의 `depends` 그래프로만 판정했다. 신규 조항의 인용 근거 8건을 원문에서 재확인했다.
- verdict-only. 어떤 파일도 수정하지 않았다.

---

## 종합 verdict: **pass**

| 축 | 1회차 | 2회차 | 변동 사유 |
|---|---|---|---|
| RB-1 계약 완전성 | 4 | **5** | 감점 3건(`scope` 값 집합 / token 유효기간 기본값·상한 / M-1 대상 함수 시그니처)이 §1.7.2·§1.7.3·§2.8로 전건 정의됐다. 남은 미정의 표면·필드를 찾지 못했다 |
| RB-2 계약 일관성 | 4 | **4** | 감점 4건(PRD 표vs주석, T10 AC-17 오기, TRD M-1 stale, token 명칭 3종) 전건 해소. 그러나 **신규 불일치 1건**(N-1: `surfaces.json` begin-worker 오류 집합이 §2.2 신설 코드 2종을 반영하지 않음)이 생겨 앵커 5의 "서로 모순 없이 교차 참조"에 미달한다 |
| RB-3 설계 정합 | 5 | **5** | 유지. MV-29 신설로 D-6이 계약 조항에 더해 기계검증 항목으로도 집행된다 |
| RB-4 범위 준수 | 4 | **5** | 감점 2건 해소 — Phase 0 승인 게이트가 `backlog.json`의 의존 구조로 구조화됐고(T01G), 예시 배정값 선점 외관이 §1.4·§1.6 인용 블록으로 차단됐다. 신규 조항의 비목표·Phase 경계 침범 0건 |
| RB-5 근거 인용 충실도 | 5 | **5** | 유지. 신규 조항 전건이 근거를 달고, 제안서에 값이 없는 2건(유효기간 기본값·상한)은 "PM 판정 위임 범위 내 설계 결정"으로 명시했다 |

- 전 축 4점 이상 → 통과. 2점 이하 축 없음. RB-3·RB-4 모두 4점 초과.
- drift 필요성: **no**.

---

## 1. U-1~U-10 재판정

| ID | 판정 | 근거 |
|---|---|---|
| U-1 | **해소** | `backlog.json`에 `T01G`(`depends:["T01"]`)가 신설되고 **T02의 `depends`가 `["T01G"]` 단일**로 재배선됐다. T02를 가리키는 다른 진입 경로가 없고(T03~T06이 모두 `depends:["T02"]`), T01 완료만으로 T02가 선택 가능해지는 경로는 그래프상 존재하지 않는다. T01G의 수용 기준 2번이 "사용자의 명시적 승인 발화를 수신했다"이고 `slice`가 "자율 완주 대상이 아니며 PM이 사용자에게 에스컬레이션해 해소한다"로 자율 통과를 차단한다 — 의존으로 게이트를 **대체**한 것이 아니라 게이트를 노드로 **승격**한 형태이므로 제안서 `:653-655`와 충돌하지 않는다 |
| U-2 | **해소** | `PRD.md`:209 Phase 1A 행에 AC-6·AC-15가, :210 Phase 1B 행에 AC-18이 추가돼 표와 분할 주석(:214-221)이 일치한다. AC-6·9·10·15·18 다섯 건 모두 양쪽 행에 표기되는 방식으로 통일됐다 |
| U-3 | **해소** (3소유자 전건) | PRD분: `PRD.md`:155-160 Phase 1A 인도물에 "단방향·멱등 가져오기 능력"과 "이 단계의 가져오기는 능력만 갖추는 것이고 완료 게이트와는 무관하다"가, :170-174 Phase 1B 인도물에 "가져온 사건이 워커 완료 게이트에 기여하지 않음을 집행"이 추가됐다. TRD분: `TRD.md`:326-327 Phase 1A 코드 목록에 가져오기 능력 + 배치 이유, :334 Phase 1B 코드 목록에 불기여 집행이 추가됐다. 백로그분: T03에 `AC-15(1A분)`과 `covers` 가져오기 표면 2종이 편입됐다. 표면 커버리지를 재대조한 결과 19/19 커버, 미정의 표면 참조 0건 |
| U-4 | **해소** | T10 수용 기준 2번이 `"R-16: 재접속·재개는 실행 식별자를 유지하고 명시적 재실행만 새 식별자를 발급한다"`로 정정됐다. AC-17은 T15가 원문대로 단독 보유한다 |
| U-5 | **해소** | `TRD.md`:227이 "**캐시 키 정합이 이 절의 핵심 위험이며, 처리 시점은 확정됐다**"로 교체되고 "근거: PM 판정 M-1 / `CONTRACT.md` §3.5. 개정 후 입출력 형태는 `CONTRACT.md` §2.8이 소유한다"로 확정 위치를 가리킨다. `## 미결 기술 쟁점`(:378)은 "**남은 미결 쟁점은 없다**" + M-1~M-5 해소 귀속표로 대체됐고, 5행 전건이 확정 위치(§3.5/§2.8, §3.2, §3.3·§1.6, §2.7·§2.2·MV-13, §3.4)를 가리킨다 |
| U-6 | **해소** | 신설 §1.7이 `scope`를 4축 폐쇄 집합으로 확정한다 — `run_id`(정확히 1개)·`worker_run_id`(정확히 1개)·`events`(5종 중 1건 이상, 기본 5종 전부)·`provenance_types`(고정 `["direct"]`). `events`에 넣을 수 없는 도구·PM 소유 사건 7종을 열거하고 발급 자체를 거부한다. §1.7.3이 기본 86,400초·절대 상한 604,800초·조용한 절단 금지를 확정하고, 신규 오류 코드 `capability_expiry_invalid`·`capability_scope_invalid` 2종이 §2.2에 등재되고 §2.3 `begin-worker` 행에도 반영됐다. **값의 근거와 정합성 확인**: 7일 상한은 제안서 §8.3의 원본 보존 기본 상한 7일(`docs/proposals/opal-task-run-log.md:556` "raw 기본 상한은 파일당 10 MiB·run당 100 MiB·7일")과 같은 축에 맞춘 것으로 "권한이 증거 보존 창보다 오래 살아남지 않게 한다"는 논거를 단다 — `TRD.md`:246과 모순 없다. 락 대기 상한(30,000 ms, §2.7)과는 축이 달라 충돌하지 않으며, §1.7.3이 "§2.7과 동일한 형태"(기본값 존재 + 설정 조정 + 절대 상한 + 0 이하 거부)를 명시적으로 복제해 두 정책이 같은 규칙 형태를 공유한다. 제안서에 없는 값임을 두 행 모두에 "**PM 판정 위임 범위 내 설계 결정**"으로 표기했다 |
| U-7 | **해소** | 신설 §2.8이 개정 전/후 시그니처를 병기한다 — `load_quiet_hours() -> QuietHours | None`(`start_minute`·`end_minute`·`time_zone` NamedTuple), `quiet_hours_token(QuietHours | None) -> str`(`f"{start}-{end}@{tz}"`, `None`→`"off"` 유지), `DEFAULT_QUIET_HOURS`에 `timeZone: "Asia/Seoul"` 추가. 6개 계약 조항(2층 머지 하위 키 / 파일 재기록 금지 / 서명 포함 / `"off"` 불변 / 캐시 1회 갱신 / stats 주입 계약 유지)이 붙는다. 개정 전 형태를 원문과 대조한 결과 `dashboard/backend/config.py`:39·:196·:203과 정확히 일치한다. 단 stats 경계의 주입 형태가 남는다 → N-2 |
| U-8 | **해소** | 신설 §1.1.3이 3종을 성격·존재 위치·로그 잔류 여부 표로 분리한다 — `worker_log_token`(비밀 원문, 로그에 절대 미잔류), `worker_log_token_id`(식별자, `wlt_<UUIDv4>`, `provenance.worker_log_token_id`와 `revoked`의 `data.token_id`가 **같은 값**임을 명시), `data.token_sha256`(단방향 검증자). constant-time 비교와 원문 없이 재구축 가능함까지 규정한다. 인용(제안서 §5.2·§6.4)을 원문 `:464-466`에서 확인했고 문장이 일치한다 |
| U-9 | **해소** | §1.4 예시 아래(:182)와 §1.6 예시 아래(:256)에 인용 블록이 각각 추가돼 "예시 값은 계약이 아니며 실제 배정은 `profiles.json`이 소유한다", "아래 명명 규칙 표는 채널 식별자와 표면 id의 대응만 확정하고 등급은 확정하지 않는다"로 선점 외관을 차단한다 |
| U-10 | **해소** | MV-29가 추가됐고 **결정론적 판정 형태를 갖췄다.** "동일하다"로 끝나지 않고 (1) 같아야 하는 필드를 열거하고(`event`, `actor.kind`, `provenance.type`, `provenance.recorded_by.kind`, 진행 사건은 `data.kind` / 종료 사건은 `reason_code` 분류), (2) 달라도 되는 필드를 명시하며(`provenance.source.*`와 실행마다 달라지는 식별자·timestamp), (3) 실패 조건을 못박는다("그 외 필드에 축별 분기가 나타나면 실패"). 고정 입력도 두 축의 fixture 쌍 (a)중간 진행 1건 (b)정상 종료 1건으로 지정됐고 대응 표면 2종이 달렸다 |

**해소율: 10/10.** 미해소·부분 해소 0건.

---

## 2. "확정 조항을 바꾸지 않았다"는 주장의 검증

1회차 시점 `CONTRACT.md` 전문(511행)과 현재 파일(632행)의 라인 단위 diff를 떴다. 결과는 다음과 같다.

- **순수 추가 5블록**: §1.1.3(14행), §1.4 예시 주석(2행), §1.6 예시 주석(2행), §1.7 전체(4개 하위절), §2.8 전체, MV-29 1행.
- **기존 행 수정 2건** — 둘 다 값·의미 변경이 아니라 참조·기본값 문장의 **추가**다.
  1. §2.2 `worker_token_invalid` 행: 원천 컬럼이 `§6.4` → `§6.4 / 본 계약 §1.7.2`. 조건·처리 문구는 글자 단위로 동일하다.
  2. §2.3 `run-log-tool.begin-worker` 행: 기존 불변식 문장은 그대로 두고 뒤에 `--scope`·`--expires-in` 기본값과 신규 거부 코드 문장이 덧붙었다.
- **삭제·값 변경 0건.** 스키마 필드, enum, 상한(16 KiB / 4 KiB / 128 / 512 KiB / 4 MiB / 30,000 ms), 사건 12종, 허용 조합 8행, 표면 17+2, 기존 MV-1~MV-28, §5 루브릭절과 통과선 전부 1회차와 바이트 동일하다.

**판정: 주장은 사실이다.** 다만 정확히 기술하면 "확정 조항 2개 행이 참조·기본값 문장 추가로 확장됐고, 확정된 값이나 판정 내용이 바뀐 곳은 없다"이다. §5 루브릭절 자체가 변경되지 않았으므로 이번 재판정의 기준도 1회차와 동일하다.

---

## 3. 신규 이슈 (1회차 미지적)

| ID | 이슈 | 심각도 | 되돌림 대상 |
|---|---|---|---|
| N-1 | **`surfaces.json`이 신설 오류 코드 2종을 반영하지 않았다.** §2.2·§2.3은 `run-log-tool.begin-worker`가 `capability_expiry_invalid`·`capability_scope_invalid`로 거부한다고 확정했으나, `surfaces.json`의 같은 표면 `response_shape.err`는 여전히 `["run_log_write_failed", "run_log_missing", "task_lock_timeout"]` 3종뿐이다(파일 갱신 시각 17:58 — 이번 회차에 손대지 않았다). §2.3이 "호출 형태·인자·응답 필드는 `surfaces.json`의 표면 11개가 **기계가독 원천**"이라고 선언하고 §4 `[MUST]`가 `surfaces.json`을 기계검증절의 필수 구성요소로 포함하므로, 지금은 기계가독 원천과 계약 문서가 같은 표면의 오류 집합을 다르게 말한다 | **중** | **D4** — `surfaces.json` begin-worker `err`에 2종 추가 |
| N-2 | **§2.8이 stats 경계의 주입 형태를 확정하지 않았다.** 조항 6은 "stats 모듈에 대한 주입 계약은 유지된다 — `stats.py`는 설정을 읽지 않으며 라우터가 필요한 값을 인자로 주입한다"로 **누가 읽는가**만 고정하고 **무엇을 넘기는가**는 정하지 않는다. 그런데 `load_quiet_hours`의 반환이 2튜플에서 3필드 NamedTuple로 바뀌므로, 라우터가 그 값을 그대로 넘기면 `stats.py`의 소비자 시그니처(`format_quiet_hours(quiet_hours: tuple[int, int] | None)` — `dashboard/backend/stats.py`:106, 및 `quiet_overlap_minutes`)가 3요소를 받는다. `stats.py` @header가 "`quiet_hours=(시작 분, 끝 분)` 2튜플을 **인자로 주입**받아"로 2튜플을 명시하고 있어, 이 경계 형태가 비어 있으면 구현자가 임의로 메우게 되고 `TASK.md` C-2가 동결한 Console 테스트(MV-25)와 직접 닿는다 | **중** | **D4** — §2.8에 "라우터가 stats에 주입하는 값은 `(start_minute, end_minute)` 2튜플을 유지한다" 등 경계 형태 1줄 추가 |
| N-3 | `BACKLOG.md` 머리말의 "최종 갱신: 2026-09-12 18:04"가 실제 갱신 시각(파일 mtime 18:15, T01G 신설·T03 covers 편입 반영본)과 어긋난다 | 하 | **D5** — 렌더 시각 갱신 |
| N-4 | T03의 `acceptance_criteria`·`covers`에는 가져오기가 들어갔으나 `slice` 서술("폐쇄형 사건 어휘와 허용 조합 검증, 출처 검증, 요청 식별자 멱등 판정, run 전역·주체별 순번 발급을 …")에는 가져오기 언급이 없다. 태스크 범위 서술과 수용 기준이 어긋나 디스패치 프롬프트가 범위를 좁게 전달할 여지가 있다 | 하 | **D5** — T03 `slice`에 1A분 가져오기 능력 추가 |

> N-1·N-2는 이번 보강이 새로 만든 표면·시그니처의 **가장자리**에서 생긴 것으로, 보강 범위 안의 이슈다. N-3·N-4는 이번 편집의 부수 누락이다. 네 건 모두 확정 조항의 오류가 아니라 **반영 누락**이며, 어느 것도 축 점수를 4점 미만으로 끌어내리지 않는다.

---

## 4. 축별 판정 상세

### RB-1 계약 완전성 — 5 (1회차 4)

| item | result | reason |
|---|---|---|
| `CONTRACT.md`::RB-1 | 5 | 1회차 감점 3건이 전건 정의됐다. §1.7.1이 `worker.capability.issued`의 `data` 4필드를 타입·필수·제약과 함께 확정하고, §1.7.2가 `scope` 4축을 폐쇄 집합과 거부 대상 열거로 닫고, §1.7.3이 기본값·절대 상한·조정 규칙·초과 시 처리를 확정하며, §2.8이 M-1 대상 두 함수의 개정 전/후 형태를 병기한다. 모든 표면의 인자·응답·오류가 계약 문서 기준으로 정의된 상태이며, 조건부 필수는 조건이 명시돼 있다. N-1은 계약 문서가 아니라 `surfaces.json`의 미반영이므로 RB-2에, N-2는 Console 내부 함수 경계이므로 별도 항목에 귀속시켰다 |

### RB-2 계약 일관성 — 4 (1회차 4, 유지)

| item | result | reason |
|---|---|---|
| 1회차 감점 4건 | PASS | U-2·U-4·U-5·U-8 전건 해소(§1 재판정표 참조). 특히 §1.1.3이 `provenance.worker_log_token_id`와 `worker.capability.revoked`의 `data.token_id`가 같은 값임을 명시해 명칭 3종의 단일 정의 지점을 만들었다 |
| 신규 diff 정합 | PASS | 이번에 추가된 값이 기존 정의와 충돌하지 않는다. §1.7.2의 `events` 5종이 §1.2의 worker 계열 사건 5종과 정확히 일치하고, 거부 대상 7종이 §1.2·§1.3의 actor 제약과 일치한다. §1.7.4의 폐기 원천 4종이 §1.2 `run.completed(reason=restart)`·terminal 정의와 일치한다. §2.8 조항 3이 MV-26의 판정과 같은 내용을 말한다 |
| `surfaces.json`::begin-worker::err | FAIL | N-1. 계약 §2.3이 기계가독 원천으로 선언한 자산이 §2.2 신설 코드 2종을 담고 있지 않다. 같은 표면의 오류 집합이 두 자산에서 다르게 나타나므로 앵커 5의 "모순 없이 교차 참조"에 미달한다 |

### RB-3 설계 정합 — 5 (유지)

| item | result | reason |
|---|---|---|
| D-1~D-9 집행 | 5 | 1회차 판정 그대로 유지된다(diff상 관련 조항 무변경). MV-29 신설로 D-6("플랫폼·채널 차이를 변환 계층에만 둔다")이 계약 조항(§3.1)에 더해 **기계검증 항목으로도** 집행되며, 1회차에 지적한 R-12 관찰 지점의 MV 부재(U-10)가 함께 닫혔다 |
| 신규 조항 vs TRD | PASS | §1.7이 D-5를 어기지 않는다 — 권한 검증은 기록 코어 소관이고 `state.json`을 읽지 않으며, 재구축 원천이 사건뿐이라 D-3(파생 색인)과도 정합한다. §2.8은 D-6·Console 읽기 전용 경계(§3.1)를 건드리지 않는다 |

### RB-4 범위 준수 — 5 (1회차 4)

| item | result | reason |
|---|---|---|
| Phase 0 게이트 | PASS | U-1 해소. 게이트가 문서 산문에 더해 실행 자산(`backlog.json`)의 구조로 존재한다 |
| 예시 배정값 | PASS | U-9 해소. §1.4·§1.6 두 예시 모두에 "계약이 아니다" 인용 블록이 붙어 Phase 0 실측 전 선점 외관이 제거됐다 |
| 신규 조항의 범위 | PASS | §1.7은 R-20·AC-9의 기존 범위 안이고 Phase 배정도 T04·T08에 이미 있다. §2.8은 §3.5가 1회차에 이미 확정한 Phase 1A 결정의 형태를 적은 것이지 새 범위를 당긴 것이 아니다. MV-29는 Phase 1A 변환기 범위(T07) 안이다. 비목표 4종(2단 커밋·correction 체인·Console 분석 화면·Phase 2/3)의 신규 유입 0건, `backlog.json` 신규 태스크 T01G도 Phase 0 게이트 노드로 범위 안이다 |
| Phase 2 종결 조항 | PASS | §3.4 종결 조항 무변경 유지. `TRD.md` 미결 표가 "남은 미결 없음"으로 닫히면서 M-5의 Phase 2 귀속이 재질의 없이 확정됐다 |

### RB-5 근거 인용 충실도 — 5 (유지)

| item | result | reason |
|---|---|---|
| 신규 조항 인용 | PASS | 인용 8건을 원문에서 재확인해 전건 일치를 확인했다 — 제안서 `:464-466`(SHA-256·scope·만료만 기록, constant-time, terminal과 `run.completed(reason=restart)`가 즉시 폐기 원천 → §1.1.3·§1.7.4와 일치), 제안서 `:556`(raw 기본 상한 7일 → §1.7.3의 상한 정렬 근거와 일치), `dashboard/backend/config.py:39`(`DEFAULT_QUIET_HOURS` 형태), `:196`·`:203`(`"off"` 반환), 같은 파일 @header("반환값 (시작 분, 끝 분)은 라우터가 stats.py에 인자로 주입한다 — stats.py는 설정을 읽지 않는다" → §2.8 조항 6과 일치). **줄번호 오기 0건** |
| 제안서에 없는 값 | PASS | §1.7.3의 기본 86,400초·상한 604,800초 두 행 모두에 "제안서에 값이 없으므로 **PM 판정 위임 범위 내 설계 결정**"을 명시하고 판단 근거(1회 디스패치 범위 / 증거 보존 창과의 정렬)를 함께 적었다 — 앵커 5가 요구하는 형태다 |

---

## 5. drift 필요성

- **판정: no.** 이번 회차는 확정 조항의 값·판정을 하나도 바꾸지 않았고(§2 diff 검증), 신규 조항은 전부 기존 계약의 빈칸을 메우는 추가다. 계약 개정 절차나 거버넌스 에스컬레이션 대상 없음.
- N-1·N-2도 계약 변경이 아니라 **반영 누락**(`surfaces.json` 동기화 / §2.8 1줄 보강)이므로 같다.
- 본 에이전트는 판정만 반환하며, 위 4건의 반영 여부와 방법은 PM(오케스트레이터)의 책임이다.
