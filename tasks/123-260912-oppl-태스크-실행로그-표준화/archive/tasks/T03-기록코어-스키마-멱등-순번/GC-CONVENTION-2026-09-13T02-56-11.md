# GC CONVENTION REPORT — 2026-09-13T02-56-11

## 1. 헤더

- 실행 일시: 시작 2026-09-13 02:56:11 / 완료 2026-09-13 02:56:11 / 소요 -
- 범위: `T03 변경분` / 대상 파일 4개
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` 존재 → 해당 문서 (§@header 규칙, docs/CONVENTIONS.md:219-225) + `opal/core/references/header-standard.md` §2.1·§4.2
- APPLY 수행 여부: N (수동 대기, read-only 진단 전담)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 3 |
| 심각도 분포 | Critical 0 / High 0 / Medium 3 / Low 0 / Info 0 |
| 자동 수정 가능 | 0 |
| 수동 조치 필요 | 3 |
| 파일별 상위 Top 3 | `run_log_core.py` (1) / `tests/test_run_log_tool.py` (1) / `README.md` (1) |
| 카테고리별 빈도 | 문서화 (3 파일) — 빈도 트리거 미발동(N=3 파일이나 동일 fingerprint 아님, 각기 별개 이슈) |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 0 |

---

## 3. 수정 대상 (체크리스트)

### Medium (3건)

- [ ] GC-001 [opal/tools/run-log-tool/run_log_core.py:6] `@header.description`에 서로 다른 태스크 번호 5종(`T04`·`T05`·`T06`·`T07`·`T11`)이 "…소관" 형태로 누적 기재됨
  - 카테고리: 문서화 (@header 이력 비기재 원칙)
  - 위반 기준: 프로젝트(`docs/CONVENTIONS.md:224` "코드 `@header`에는 현재 사실만 기재한다… 원칙 원문은 `opal/core/references/header-standard.md` §2.1이 소유한다") + `header-standard.md` §2.1("적용 범위: `@header` JSON 블록 전체")·§4.2(`description`이 "담지 않는 것" = "서로 다른 태스크 번호가 2개 이상 쌓이는 형태" — 출신 태스크 1개 단발 인용만 허용)
  - 설명: `description` 필드 말미에 "색인·조각 경계 전환·락 정책 변경은 T04, 상태 보관함·복구는 T05, redact() 본문·원본 상한은 T06, 채널 변환기는 T07, 가져온 사건의 완료 게이트 불기여 집행은 T11 소관이며 이 모듈은 건드리지 않는다"라는 문장이 있다. 서로 다른 태스크 번호가 5개(T04/T05/T06/T07/T11) 나열되어 §4.2의 "2개 이상 쌓이는 형태" 금지 임계값을 넘는다. 동일 패턴이 과거 T02 산출물에서 `GC-C001`로 2회 지적된 바 있다(디스패치 컨텍스트 "특히 볼 것" §1 참조).
  - 해결 방안: 태스크 번호 나열을 제거하고 "이 모듈이 다루지 않는 범위"만 태스크 번호 없이 사실로 서술하거나(예: "색인·조각 경계 전환·락 정책·상태 보관함·`redact()` 본문·채널 변환·완료 게이트 집행은 이 모듈이 다루지 않는다"), 소유 매핑이 반드시 필요하면 `tasks/{NNN}-*/PLAN.md`의 범위 경계 표로 이관한다.
  - 자동 수정: N
  - 참조: `opal/core/references/header-standard.md` §2.1·§4.2

- [ ] GC-002 [opal/tools/run-log-tool/tests/test_run_log_tool.py:6] `@header.description`이 "S-3~S-8은 T02… 판정, S-10~S-29는 T03… 판정이다"로 서로 다른 태스크 번호 2종(`T02`·`T03`)을 시점별로 나열함
  - 카테고리: 문서화 (@header 이력 비기재 원칙)
  - 위반 기준: 프로젝트(`docs/CONVENTIONS.md:224`) + `header-standard.md` §4.2 — "시점별 변경 단락의 append" 금지 사례에 해당(어느 시나리오 배치를 어느 태스크가 추가했는지의 시간순 서술)
  - 설명: `scenarios`/`task` 필드(이력 금지 예외 필드, header-standard.md §2 각주)가 이미 시나리오 id 목록을 담고 있어 이 정보를 별도로 보유한다. `description`에서까지 "S-3~S-8은 T02, S-10~S-29는 T03" 형태로 태스크별 시점 구분을 반복 서술할 필요가 없으며, 이는 §4.2가 금지하는 "서로 다른 태스크 번호 2개 이상" 패턴과 일치한다.
  - 해결 방안: `description`은 "run-log-tool 서브명령 계약 테스트. S-3~S-29 시나리오로 §1.1~§1.3 스키마·멱등·순번·가져오기 계약을 판정한다" 등 태스크 번호 없이 현재 사실만 남기고, 시나리오-태스크 매핑이 필요하면 PLAN.md/TEST-SCENARIO 자산에 둔다.
  - 자동 수정: N
  - 참조: `opal/core/references/header-standard.md` §4.2

- [ ] GC-003 [opal/tools/run-log-tool/README.md:8-22] 서브명령 목록이 실제 구현과 어긋남 — `import-agentic`/`import-oppl`을 "후속 태스크가 채운다"고 서술하지만 동일 변경분의 `run_log_tool.py:107-114,166-178`에 이미 두 서브명령이 구현되어 있음
  - 카테고리: 문서화 (인접 코드 관측 패턴 — README는 실제 구현 상태를 반영해야 함)
  - 위반 기준: 인접 코드 관측(§기준 선택 순서 3순위) — `docs/CONVENTIONS.md`에 README 서브명령 목록의 직접 규정은 없으나, README 자신이 "## 서브명령 (T02 범위 — 3종)"·"나머지 8개 서브명령(…`import-agentic`/`import-oppl`…)은 후속 태스크(T03~T06)가 채운다"라고 명시한 사실 자체가 이번 변경분과 모순됨
  - 설명: `run_log_tool.py`의 `build_parser()`(`opal/tools/run-log-tool/run_log_tool.py:166-178`)와 `run_log_core.py`의 `import_agentic`/`import_oppl`(각각 `run_log_core.py:1050`,`1279`)이 이번 변경분에서 이미 구현됐으므로, README의 "3종"·"후속 태스크가 채운다" 서술은 더 이상 현재 사실이 아니다.
  - 해결 방안: README §서브명령 절에 `import-agentic`/`import-oppl` 사용법을 추가하고, "T02 범위 — 3종"·"후속 태스크가 채운다" 문구를 현재 구현 상태(5종)로 갱신한다.
  - 자동 수정: N
  - 참조: `opal/tools/run-log-tool/run_log_tool.py:166-178`

---

## 4. 문서 업데이트 제안 (§9·§10, 트리거 발동 시만)

발동 없음 — 3건 모두 개별 파일 1건씩이라 빈도 트리거(N≥3 파일)에 해당하지 않고, 기존 CONVENTIONS.md 카테고리(문서화) 범위 안이라 새 카테고리 트리거도 없음.

---

## 5. 문서 작성 유도 (해당 시)

`docs/CONVENTIONS.md` 존재 확인 — 작성 유도 생략.
