# GC CONVENTION REPORT — 2026-09-12T21-16-46

## 1. 헤더

- 실행 일시: 시작 2026-09-12 21:16 / 완료 2026-09-12 21:16 / 소요 1분 미만
- 범위: `staged` (호출자 지정 target_files 고정) / 대상 파일 6개
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` (존재 — 유일 기준으로 적용)
- APPLY 수행 여부: N (수동 대기, read-only 진단 전담)

검사 대상:
- 신규: `opal/tools/run-log-tool/run_log_core.py`, `opal/tools/run-log-tool/run_log_tool.py`, `opal/tools/run-log-tool/run.sh`, `opal/tools/run-log-tool/README.md`, `opal/tools/run-log-tool/tests/test_run_log_tool.py`, `opal/tools/state-tool/tests/test_state_tool_run_log.py`
- 수정: `opal/tools/state-tool/state_tool.py`, `scripts/install-mac.sh`, `docs/CONVENTIONS.md`, `docs/PROJECT.md`

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 1 |
| 심각도 분포 | Critical 0 / High 0 / Medium 1 / Low 0 / Info 0 |
| 자동 수정 가능 | 0 |
| 수동 조치 필요 | 1 |
| 파일별 상위 Top 5 | opal/tools/state-tool/state_tool.py (1건) |
| 카테고리별 빈도 | 문서화 (1 파일) |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 0 (빈도 임계값 미달, 새 카테고리 없음) |

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

### High (0건)

### Medium (1건)

- [ ] GC-C001 [opal/tools/state-tool/state_tool.py:6] `@header.description`이 이번 diff로 추가된 신규 동작(`--run-log-mode` 분기, `RUN_LOG_STATE_ERROR_CODES` 테이블, `_error_template`/`_cmd_init_run_log`/`_atomic_write_state_json`/`_import_run_log_core`/`_run_log_core_dir`)을 전혀 반영하지 않는다 — `git show HEAD:opal/tools/state-tool/state_tool.py`의 description과 바이트 단위로 완전히 동일함을 확인함.
  - 카테고리: 문서화
  - 위반 기준: 프로젝트(`docs/CONVENTIONS.md` §@header 규칙 — "코드 `@header`에는 현재 사실만 기재한다") + `opal/core/references/harness/header-rules.md` §갱신 시점(4단) (a) "파일 변경과 같은 자리에서... 즉시 기록" 및 [MUST] "작업 완료 후 일괄 갱신 금지"
  - 설명: 이 파일은 이번 태스크(T02)에서 `--run-log-mode shadow` 초기화 경로, 신규 오류 코드 테이블, 신규 헬퍼 함수 5개, `import uuid`가 추가됐으나 `@header.description`은 개정 전(HEAD)과 완전히 동일하다. code-scan이 이 헤더로 파일 전체 Read를 대체하려는 워커에게 잘못된(누락된) 현재 상태를 전달한다.
  - 해결 방안: `description`에 `--run-log-mode`/`RUN_LOG_STATE_ERROR_CODES`/신규 헬퍼 함수의 현재 동작을 이력 서술 없이(제자리 교체 원칙) 추가 기술한다.
  - 자동 수정: N
  - 참조: `docs/CONVENTIONS.md` §@header 규칙, `opal/core/references/harness/header-rules.md` §갱신 시점

### Low (0건)

### Info (0건)

---

## 4. 문서 업데이트 제안 (§9·§10, 트리거 발동 시만)

트리거 미발동 — 빈도 임계값(N=3) 미달, 새 카테고리 없음.

---

## 5. 문서 작성 유도 (해당 시)

`docs/CONVENTIONS.md` 존재 — 작성 유도 생략.

---

## 6. 검증 상세 (참고)

- `@header` 필드·형식: 신규 `.py` 파일 4건(`run_log_core.py`, `run_log_tool.py`, `tests/test_run_log_tool.py`, `opal/tools/state-tool/tests/test_state_tool_run_log.py`) 전건 필수 필드(module/layer/domain/description/exports) 충족, `exports` 목록이 실제 공개 함수/클래스와 정확히 일치(사적 함수 미포함), 이력 전용 필드 없음. `.opal/code-scan.json`의 `headerSource: inline`과 인라인 기재 방식 일치. `code-scan validate`로 교차 검증 — 이 4개 파일에 대해 `uncovered`/`conflict`/`exports_not_found`/`worker_scope_violation` 0건.
- `run.sh`: `header-rules.md` §적용 대상 확장자 밖(셸 스크립트) — 적용 제외 명시 주석 존재, `state-tool`/`backlog-tool` 동일 관례.
- 도구 CLI 관례: `run.sh` 래퍼(venv python 호출)가 `state-tool`/`backlog-tool` 패턴과 동일. 응답 봉투가 `state-tool`의 평면 봉투와 다른 중첩 봉투를 쓰는 것은 `docs/run-log/CONTRACT.md` §2.1이 명시적으로 요구한 차이이며 README.md가 그 경계를 문서화함 — 위반 아님. `RUN_LOG_ERROR_CODES`/`RUN_LOG_STATE_ERROR_CODES`를 `state_tool.ERROR_CODES`와 물리적으로 분리한 것도 PLAN D-A 근거로 README에 명문화된 의도적 설계 — 위반 아님. 표준 라이브러리 전용 import 확인(`run_log_core.py`/`run_log_tool.py`: argparse/json/sys/contextlib/fcntl/os/pathlib/time/uuid/datetime만 사용).
- 문서 동기화: `docs/CONVENTIONS.md`·`docs/PROJECT.md` 모두 "20종→21종"으로 일치, `opal/tools/*/` 실제 디렉터리 수(21) 및 신규 도구명(`run-log-tool`) 일치. `docs/PROJECT.md` 문서 레지스트리에 `docs/run-log/{PRD,TRD,CONTRACT}.md`·`surfaces.json` 4건 추가, 대상 파일 4종 실재 확인.
- 테스트 관례: 신규 테스트 2건 모두 `unittest.TestCase` + 실 subprocess 호출 + `tempfile.TemporaryDirectory`, mock/patch/MagicMock 미사용 — `backlog-tool`/`state-tool` 기존 테스트 관례와 일치.
- 명명·구조: `run-log-tool`(kebab-case 디렉터리), `run_log_core.py`/`run_log_tool.py`(snake_case), `tests/test_run_log_tool.py` 배치가 `backlog-tool`/`memory-tool` 구조(README.md/run.sh/{tool}.py/tests/)와 일치. `docs/PROJECT.md` 폴더 구조맵과 정합.
- `.gitignore`에 `__pycache__/` 등록 확인 — untracked 목록에 노출된 `__pycache__/*.pyc`는 git-ignore 대상이며 커밋 위험 없음.
