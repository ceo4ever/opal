# GC CONVENTION REPORT — 20260915-1301

<!--
  체크박스 5단계 상태 기호 (APPLY 단계가 기입):
  [ ]  open    — 미처리 (신규)
  [x]  done    — 적용 완료  ← 주석: 적용 시각 YYYY-MM-DD HH:mm + 수정 요약
  [~]  pending — 보류       ← 주석: 보류 사유
  [?]  review  — 확인 필요  ← 주석: 판단 근거 / 해결 방안
  [!]  failed  — 실패       ← 주석: 실패 사유 / 권장 대안
-->

## 1. 헤더

- 실행 일시: 시작 2026-09-15 13:01 / 완료 2026-09-15 13:08 / 소요 약 7분
- 범위: `Framework` (단일 문서 모델 — `docs/CONVENTIONS.md`는 허브+링크 미적용, 프레임워크 전체 허브를 그대로 적용) / 대상 파일 8개
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` (존재 확인) — 보조: `docs/PROJECT.md` §네이밍 규칙·§프로젝트 구성, `.opal/AGENT.md` §금지사항, `opal/core/references/header-standard.md` §2.1(§@header 규칙이 지정하는 owner 문서)
- 대상 파일:
  - `opal/tools/run-log-tool/run_log_core.py`
  - `opal/tools/run-log-tool/run_log_tool.py`
  - `opal/tools/run-log-tool/tests/test_run_log_tool.py`
  - `opal/tools/state-tool/state_tool.py`
  - `opal/tools/state-tool/tests/test_state_tool_run_log.py`
  - `opal/core/setting.default.json`
  - `docs/run-log/surfaces.json`
  - `.gitignore`
- APPLY 수행 여부: N (본 에이전트는 read-only 진단 전담 — 수정 없음)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 1 |
| 심각도 분포 | Critical 0 / High 0 / Medium 1 / Low 0 / Info 0 |
| 자동 수정 가능 | 0 |
| 수동 조치 필요 | 1 |
| 파일별 상위 Top 5 | `opal/tools/run-log-tool/tests/test_run_log_tool.py` (1건) |
| 카테고리별 빈도 | 문서화 (1 파일) |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 1 (빈도 0건 + 새 카테고리 1건) |

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

### High (0건)

### Medium (1건)

- [ ] GC-C001 [opal/tools/run-log-tool/tests/test_run_log_tool.py:9-25 vs :1872,1984,2056,2155,2256,2336,2408] `@header.exports` 목록이 이번 변경으로 신설된 테스트 클래스 7개를 반영하지 않음
  - 카테고리: 문서화
  - 위반 기준: 프로젝트(`docs/CONVENTIONS.md` §구현 규칙 > @header 규칙 — "코드 @header에는 현재 사실만 기재한다") + 그 원칙이 소유를 지정하는 `opal/core/references/header-standard.md` §2("`exports`는 필수 — 외부에 노출하는 항목 목록")
  - 설명: 이번 diff가 `TestS1SecretMaskingAcrossThreePaths`(1872행)·`TestS2RedactIdempotentAndSizeOrder`(1984행)·`TestS3PreservedIdentifiersSurviveMaskingAlongsideSecrets`(2056행)·`TestS4SegmentBoundaryConcurrentBarrier`(2155행)·`TestS5SegmentBoundarySymlinkDefenseReuse`(2256행)·`TestS6ClosedSegmentImmutabilityCrossSegmentSequence`(2336행)·`TestS7DurationSpansAndReconcile`(2408행) 7개 테스트 클래스를 신설했으나, 파일 상단 `@header`의 `exports` 배열(9~25행)과 `description`은 이 diff에서 전혀 손대지 않아(`git diff --numstat` 726 삽입/0 삭제, 헤더 hunk 없음) 기존 시나리오(S-3~S-29)만 기재한 상태로 남아 있다. 자매 파일 `opal/tools/state-tool/tests/test_state_tool_run_log.py`는 같은 태스크에서 신설한 `TestWorkerDurationDerivedAndConflict`·`TestUnflaggedDurationPathByteIdentical`를 `exports`에 추가하고 `scenarios` 필드에 `TEST-SCENARIO(W-7).S-8`·`.S-9`까지 등재해 대비된다 — 같은 태스크의 두 테스트 파일이 서로 다른 갱신 수준을 보인다.
  - 해결 방안: `test_run_log_tool.py`의 `@header.exports`에 신설 7개 클래스명을 추가하고, `description`에 이번 태스크(123)의 S-1~S-7 신규 커버리지를 한 줄 반영한다(자매 파일의 갱신 방식을 참고).
  - 자동 수정: N (JSON 배열 편집이나 스캔·수동 확인 병행 권장)
  - 참조: `opal/core/references/header-standard.md` §2 필드 정의 (내부 문서, URL 없음)

### Low (0건)

### Info (0건)

---

## 4. 문서 업데이트 제안 (트리거 발동 시만)

<!-- 새 카테고리 트리거 발동 -->
- [ ] GC-DP-C001 [새 카테고리 트리거] "import 배치 규칙" → `docs/CONVENTIONS.md` §구현 규칙 아래 신설 제안
  - 근거: `docs/CONVENTIONS.md`에 Python import 위치·순서에 관한 절이 전혀 없음(전체 문서 `grep -i import` 0건). 이번 diff에서 `test_run_log_tool.py`가 `hashlib`/`multiprocessing`/`stat as _stat_module`/`uuid` 4개 stdlib import를 파일 최상단 기존 import 블록(57~65행, `io`~`unittest`)이 아니라 1808행 이후 신설 섹션 도입부(diff 23~26행, 실제 파일 약 1830행 부근)에 두었다 — 이 파일 안에 기존 사례가 없는 새 배치 패턴이다. 근거 절을 댈 `docs/CONVENTIONS.md` §N이 없어 **위반으로 올리지 않고** 관찰로만 남긴다(빈도 1개 파일 — 빈도 트리거 N=3 미충족, 새 카테고리 트리거는 발동).
  - 제안 내용: "Python stdlib/서드파티 import는 파일 최상단 import 블록에 모으고, 특정 테스트 섹션 전용 import를 본문 중간에 두지 않는다(예외: 순환 의존 회피용 지연 import)."

---

## 5. 문서 작성 유도 (해당 시)

- `docs/CONVENTIONS.md` 존재 — 작성 유도 생략.

---

## 부록. 디스패치 지정 확인 항목 (특히 봐 달라는 지점 — §3/§4 집계에 포함하지 않음)

1. **@header 규칙 — Work item 표기(W-6/W-7 등)**: **위반 아님.** `run_log_core.py`·`state_tool.py`의 `@header.description`에 새로 추가된 `(W-6, TASK C-4)`, `(D-P5, W-5)`, `(W-2)`, `(W-7, PLAN D-P8, CONTRACT §2.5 시간 절)` 등은 현재 동작을 서술하며 그 근거가 된 설계 결정 ID를 괄호로 인용하는 형식이다. 같은 diff의 변경 전(HEAD) 본문에도 `(TRD D-5 단방향 의존, AC-19/MV-24)`·`(GC-003)`·`(GC-006)`·`(D-9)`·`(§2.7)`·`(118 D-4)`·`(D-A, F-6)`·`(GC-008)`·`(C-3)` 등 동일 형식의 기존 인용이 이미 다수 존재했다(예: `run_log_core.py`의 변경 전 3행, `state_tool.py`의 변경 전 6행). `header-standard.md` §2.1이 금지하는 것은 "변경 이력을 누적 기재"(예: changelog 필드, 시점별 값 병기)이며, 현재 사실을 한 번만 서술하면서 근거를 인용하는 것은 `docs/CONVENTIONS.md` §구현 규칙 > Citation Rules("모든 주장은 근거를 인용한다")가 오히려 요구하는 방식과 같다. W-6·W-7 표기는 신규 필드 신설이나 이력 누적이 아니라 기존 인용 관례의 연장이므로 위반으로 판정하지 않는다.
2. **도구·배포 경계**: `git status`·`git diff` 확인 결과 `~/.opal/` 경로 직접 편집 0건, `install-mac.sh` 실행 흔적 없음. 변경은 전량 프로젝트 소스(`opal/tools/`, `docs/`, `.gitignore`)에서 수행됨 — 준수.
3. **플랫폼 분기**: 8개 대상 파일 diff 전체를 `claude|cursor|gemini|antigravity|codex` 키워드로 검색한 결과 신규 분기 추가 없음. `setting.default.json`의 `cursor` 키는 diff 밖(비변경 컨텍스트 라인)의 기존 model-mapping 설정이며 이번 변경과 무관 — 준수.
4. **오류 코드 테이블 물리 분리**: `run_log_core.RUN_LOG_ERROR_CODES`와 `state_tool.ERROR_CODES`/`RUN_LOG_STATE_ERROR_CODES`는 이번 diff 이후에도 여전히 3개의 독립 dict 리터럴로 남아 있다. 두 모듈 모두 `worker_duration_conflict` 코드를 각자 추가했으나(`run_log_core.py` 신규 diff 28행, `state_tool.py` 신규 diff 47~50행), `state_tool.py` 쪽 주석이 "이 항목은 state-tool 표면 전용이며 물리 분리를 유지한다(D-A)"라고 명시해 동일 코드명·별도 테이블·별도 메시지 템플릿 구조가 의도된 설계임을 밝히고 있다 — 테이블 병합·import 없음, 분리 관례 유지 확인.
5. **테스트 관례(run.sh subprocess + 디스크 검사, mock 금지)**: 두 테스트 파일의 신규 클래스 전체(`TestS1`~`TestS7`, `TestWorkerDurationDerivedAndConflict`, `TestUnflaggedDurationPathByteIdentical`)를 확인한 결과 `mock`/`Mock`/`patch(`/`MagicMock` 실사용 0건(주석상의 "쓰지 않는다" 서술만 존재). 모든 시나리오가 `_run`/`_run_state_tool`/`_run_direct` 등 `run.sh` subprocess 실행과 조각 파일·`state.json` 디스크 판독으로 판정한다. `test_run_log_tool.py`의 `core.redact()` 직접 호출(S-2)은 `_import_core()` 헬퍼를 통한 것으로, 이 헬퍼는 이번 diff 이전(465·641행)에도 이미 쓰이던 기존 관례의 연장이며 mock이 아니다 — 준수.
6. **네이밍·파일 구조**: 신규 함수(`reconcile_duration`, `reconcile_duration_check`, `cmd_reconcile_duration`, `_resolve_sole_terminal_worker_run_id`, `_reconcile_worker_duration_minutes`, `_active_segment_number`, `_select_write_segment` 등)와 신규 테스트 클래스(`TestS1`~`TestS7` 등)는 Python 파일 관례(snake_case 함수, PascalCase 클래스)를 따르며 `docs/PROJECT.md` §네이밍 규칙(Python 파일 snake_case)에 부합한다. `exports` 배열에 새 공개 함수만 등재하고 신규 비공개(`_` 접두) 헬퍼는 제외한 점도 두 코어 모듈에서 기존 관례와 일치한다(단, `state_tool.py`는 이 파일 자체의 기존 관례상 일부 `_` 접두 헬퍼도 `exports`에 등재해 왔으며 이번 신규 2건도 그 기존 패턴을 따름 — 새 규칙 아님).
