---
template: sdlc-v2
---
# TEST-SCENARIO: E2E 여정·조각 라이브러리 제안서 검토 지적 5건 반영

> 입력: [TASK.md](TASK.md), [PLAN.md](PLAN.md) | 작성자: PM

## Setup

- 환경: 로컬 워크트리 `/Volumes/Data/AIStudio/workspace/ai-framework/.opal-worktrees/task_141` (branch `feat/OP-TASK-141`, base `main`). 셸에서 `grep`·`sed`·`git`만 사용한다. 서비스·DB·브라우저 기동 없음.
- 검증 대상 파일: `docs/proposals/e2e-journey-fragment-library.md` (이하 `<제안서>`). 개정 전 원본은 `git show HEAD:docs/proposals/e2e-journey-fragment-library.md`로 얻는다.
- 공통 데이터: 없음.
- 대역 사용과 한계: 사용하지 않음. 이 태스크는 실행 코드를 만들지 않으므로 대체할 연동 대상이 없다.
- 실행 조건: 자동 실행. 사용자 협업 없음.
- RED 적용: 없음 — 전 시나리오 `구현 후`. 근거는 PLAN `Approach`의 RED-first 비적용 판정(`harness/red-first.md:15`·`:48`)이며, 실행 코드가 없어 관측 가능한 실패를 만들 수 없다.

## Scenarios

| ID | 검증 대상 | 조건 | 행동 | 기대 결과 | 방법·환경 | 시점 |
|---|---|---|---|---|---|---|
| S-1 | AC-1 | W-5 적용 후 `<제안서>` §2 해법 요약 표 | §2 표 블록을 `sed`로 잘라 헤더 행과 각 행을 읽는다 | 표 헤더에 착수 선행 조건을 식별하는 열이 있고, 그 열의 값으로 `Q-1`·`Q-6`·driver 적합성 스위트 3건이 모두 나타난다. 선행 조건 없는 축은 빈칸이 아니라 `없음`으로 채워져 있다 | 결정론 검사(shell `sed`+`grep -c`) · 로컬 워크트리 | 구현 후 |
| S-2 | AC-2, C-3 | W-4·W-5 적용 후 `<제안서>` §9 Q-1 행과 §2 선행 조건 칸 | Q-1 행 전문과 §2의 조각 축 행을 읽는다 | Q-1이 선행 조건 등급으로 표기되고, 승격 근거로 동결 spec의 사후 변경 불가(§1 표 1행)와 동결 RED `S-27`이 본문에 인용돼 있다 — 근거 없이 등급만 올린 상태가 아니다 | 결정론 검사(shell `grep -n`) · 로컬 워크트리 | 구현 후 |
| S-3 | AC-3, C-3 | W-3 적용 후 `<제안서>` §5 | §5 전문을 읽는다 | 신선도 키 구성에 선택 driver 정체와 달성 충실도가 포함되고, `order.json`으로 후보 순서를 바꾼 뒤 이전 `pass` 증적을 재인용할 수 없다는 판정이 문장으로 있으며, 충실도 등급은 정의를 복제하지 않고 `FIDELITY_ORDER`를 참조한다 | 결정론 검사(shell `grep -n`) · 로컬 워크트리 | 구현 후 |
| S-4 | AC-4, C-3 | W-1 적용 후 `<제안서>` §4 | §4 자격증명 [MUST] 불릿과 그 부속 문장을 읽는다 | (a) `value_ref`가 조각 밖 본문 `fill`을 덮지 못한다는 한계와 (b) 고칠 지점이 `redaction.py`의 키 이름 기반 판정이라는 사실이 둘 다 있고, 경로·줄번호 근거가 붙어 있다 | 결정론 검사(shell `grep -n`) · 로컬 워크트리 | 구현 후 |
| S-5 | AC-5, C-3 | W-2 적용 후 `<제안서>` §3 | §3 태스크·일회성 표와 그 직후 문단을 읽는다 | 승격 판정의 주체와 수단(`test-tool` exit 계약의 `pass` 증적)이 명시되고, 사람의 산문 판단만으로는 `docs/e2e/`로 승격할 수 없다는 금지가 문장으로 있다 | 결정론 검사(shell `grep -n`) · 로컬 워크트리 | 구현 후 |
| S-6 | AC-6 | W-4 적용 후 `<제안서>` §9 | Q-4 행과 Q-5 행을 나란히 읽는다 | Q-5 행이 존재하고, 그 처리 결과가 Q-4와 같은 표기 방식(취소선 처리한 원 제목 + `→` + 처리 결과)으로 읽힌다. 처리 결과가 `해소`·`삭제`·`이월`·`결번` 중 하나로 확정돼 있다 | 결정론 검사(shell `grep -n`) · 로컬 워크트리 | 구현 후 |
| S-7 | AC-7, C-1, C-2 | W-6 시점, 전 Work item 적용 후 | `sed -n '3p' <제안서>`와 `git status --porcelain`을 실행한다 | 3행의 `> 상태:`가 `검토`이고, `git status --porcelain`의 출력에 `<제안서>` 1건과 태스크 캡슐 폴더 외의 신규·수정 경로가 없다 | 결정론 검사(shell `sed`+`git status`) · 로컬 워크트리 | 구현 후 |
| S-8 | C-4, H-2 | W-6 시점 | 절 표제를 세고 Q-ID를 전수 나열한다. 이어 `grep -rn "e2e-journey-fragment-library" --include='*.md' opal/ docs/ skills/ README.md`로 외부 참조 지점을 관측한다 | 절 번호가 §1~§9로 유지되고 Q-1~Q-9가 빠짐없이 1회씩 나타나며(Q-5 신설 행 포함), 외부 참조가 절 번호·쟁점 ID 기반이라 이번 개정으로 끊긴 참조가 0건이다 | 결정론 검사(shell `grep`) · 로컬 워크트리 | 구현 후 |
| S-9 | C-5 | W-6 시점 | `grep -nE`로 변경이력 표제어 3종(변경이력, 변경 이력, Changelog)을 찾는다 | 적중 0건 — 수기 누적 변경이력 절이 만들어지지 않았다. 상단 `최종 갱신` 괄호 주석 1개는 이력 절이 아니므로 허용된다 | 결정론 검사(shell `grep`) · 로컬 워크트리 | 구현 후 |
| S-10 | H-1, C-3 | W-3 적용 후 `<제안서>` §5 | §5에서 driver 정체를 키에 넣은 문장의 주변 문맥을 읽는다 | driver 선택이 실행 환경(바이너리 유무·probe 결과)에 좌우돼 같은 commit에서도 정체가 흔들릴 수 있다는 한계가 명시돼 있다 — 키 확장이 재실행 생략을 무력화할 위험을 제안서가 숨기지 않는다 | 결정론 검사(shell `grep -n`) · 로컬 워크트리 | 구현 후 |
| S-11 | H-3, C-1 | W-6 시점 | `git status --porcelain opal/`과 `git diff --stat -- opal/`을 실행한다 | 두 출력이 모두 비어 있다 — §4에 코드 수정 요구를 적은 것이 실제 `redaction.py` 수정으로 번지지 않았다 | 결정론 검사(shell `git`) · 로컬 워크트리 | 구현 후 |
| S-12 | AC-1, AC-3, AC-4, AC-5 | 전 Work item 적용 후, 제안서를 처음 읽는 사람 관점 | 개정 전(`git show HEAD:...`)과 개정 후 §2를 나란히 놓고, §2 선행 조건 칸에서 출발해 Q-1·Q-6·§4·§5·§3 해당 지점까지 문서 내부 참조만으로 도달한다 | §2 한 표에서 출발해 5건 지적의 반영 지점 전부에 도달하며, 도중에 "어디를 봐야 하는지 모르는" 끊긴 참조가 없다. 개정 전 문서에서는 같은 경로가 성립하지 않는다(대조군) | manual(PM 직접 열람) · 로컬 워크트리 | 구현 후 |
| S-13 | AC-1, AC-2, C-4 | 전 Work item 적용 후. 대조군은 `git show HEAD:docs/proposals/e2e-journey-fragment-library.md` | 개정 전·후 §2 표 헤더와 §9 Q-1 행을 각각 추출해 비교한다 | **구형 잔존 0**: 선행 조건 열이 없는 개정 전 §2 표 구조가 남아 있지 않고, 등급 표기 없는 개정 전 Q-1 행 원문도 남아 있지 않다. **신형 채택**: 개정 후 §2 표에 선행 조건 열이, Q-1 행에 등급 표기가 각각 1회 이상 나타난다. 쟁점 ID는 양쪽 모두 Q-1~Q-9로 동일하다 | 결정론 검사(shell `git show`+`diff`+`grep`) · 로컬 워크트리 | 구현 후 |
