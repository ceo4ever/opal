# TEST SCENARIO: AGENT.md §보고 형식 전면 제거

> 작성일: 2026-09-06 | 상태: 작성 완료 (Block A 선작성 → Block B 보강 완료)
> 작성자: PM (캡틴 페어) — PLAN 워커와 다른 작성자(self-confirming 방지)
> 도출 입력: Block A `TASK.md` / Block B `PLAN.md` §리스크 가설 표 · §1.2 기능 목록 · §5.2 검증 명령

## 0. 도출 입력 계열 (선작성 트랙)

| 블록 | 입력 | 출처 | 대응 축 | 상태 |
|------|------|------|--------|------|
| Block A. 채택 관점 | 목표 문장 / 요구사항 R-1~R-7 / 제거형 채택·잔존 기준 | `TASK.md` | ① 목표달성 ② 요구커버 ⑤ 채택·잔존 ⑥ 경계·부정 | ✅ 완료 |
| Block B. 파괴 관점 | 리스크 가설 H-1~H-9 / 기능 F-001~F-004 | `PLAN.md` | ③ 기능커버 ④ 리스크커버 | ✅ 완료 |

**Block B 보강 시 초안 대조 결과 (additive-only 아님 — 수정·삭제 포함)**

| 초안 | 처리 | 사유 |
|------|------|------|
| HA-1~HA-8 (Block A 잠정 가설) | **삭제 후 H-1~H-9로 교체** | PLAN이 동일 축을 더 정밀하게 도출(줄 수 217 판정·양방향 오판·비-tty 강제실행). 잠정 ID 유지 시 이중 체계가 된다 |
| S-1~S-7 (초안 시나리오) | **TS-001~TS-016으로 교체** | PLAN §5.2가 실행 가능 명령까지 확정. 초안은 기대값을 산문으로만 적어 판정 불가였다 |
| S-8 (변경이력 과잉 삭제 부정) | **TS-002로 흡수** | PLAN이 install `:226`과 동일한 awk 식으로 본문을 잘라내 판정하므로, 변경이력 절이 구조적으로 제외된다. 별도 시나리오 불요 |
| S-9 (보존 토큰 무손상) | **TS-004로 존치** | PLAN H-9와 동일 축. 초안 취지 그대로 채택 |
| S-10 (신규 세션 [SUPERVISOR]) | **TS-017로 존치 + 범위 축소** | PLAN이 H-4(행동 회귀)를 「검증 대상 아님·관찰 이월」로 판정. 부트스트랩 정상 기동만 이번 범위로 남기고 행동 관찰은 CLOSE 이월로 분리 |

**불일치 조기 경보 (PM Gate 보고 대상)** — 선작성 초안과 PLAN 설계가 어긋난 지점

| # | 초안 판단 | PLAN 판단 | 채택 |
|---|----------|----------|------|
| 1 | install 실행 주체 미정 | **PM 직접 · TEST 단계 · tty 메뉴 [1]** — 비-tty면 `install_mcp`까지 강제 실행(`install-mac.sh:2192-2197`) | PLAN |
| 2 | `semi-agentic.md`는 `:126` 1줄만 관련 | `:153`·`:186`도 「보고 형식」을 포함하나 **§10 양식 예시 본문**이라 보존 대상 | PLAN (초안 누락) |
| 3 | 배포본 ARCHITECTURE 검증 포함 | `install-mac.sh`가 `docs/`를 배포하지 않음 → **소스 단독 검증** | PLAN (초안 오류) |

**제거형 목표의 채택·잔존 기준 (축 ⑤)**

| 축 | 기준 | 담당 |
|----|------|------|
| 구형 잔존 0 | install `:226`과 동일한 awk 식으로 자른 **본문**에서 삭제 대상 문자열 6종 0건 | TS-001, TS-002, TS-012, TS-014 |
| 신형 채택 | 부트스트랩 절·ARCHITECTURE 표가 Phase A 능력에서 보고 형식을 빼고, `opal-pm.md` §8이 안내 1줄로 대체되며, 배포본이 184줄로 갱신됨 | TS-004, TS-005, TS-008, TS-013 |
| 무손상 | §9~§18 헤딩 10개·인접 절 2종·보존 토큰 2종·양식 예시 2줄이 baseline과 동일 | TS-003, TS-006, TS-007, TS-010, TS-015 |

## 1. 리스크 가설 표

> 출처: `PLAN.md` §리스크 가설 표 (Block B). 시나리오 열은 본 문서 §3 기준으로 확정했다.

| ID | 변경 단위 | 깨질 수 있는 계약 | 운영 영향 | 검증 계층 | 시나리오 |
|----|----------|----------------|---------|---------|---------|
| H-1 | F-001 / `AGENT.md:185-361` 삭제 | 절 경계 오삭제 — 미달 시 잔재, 초과 시 `### 기억과 학습`(`:180-183`) 또는 `## 변경이력`(`:362`) 파손. 삭제 후 정확히 217줄(394−177) | P0 | L1 | TS-001, TS-003 |
| H-2 | F-002 / `opal-pm.md` §8 | 절 번호 무성 파손 — §8 삭제 후 당기면 축자 지목 18건이 조용히 깨진다 | P0 | L1 | TS-006, TS-007, TS-015 |
| H-3 | F-001·F-003 / `AGENT.md` | 잔존 0 양방향 오판 — 변경이력 과거 기록 과삭제 / 본문 잔존 미검출 | P1 | L1 | TS-002, TS-011 |
| H-4 | F-001 전체 | 행동 회귀 — 템플릿 우위 법칙상 골격 실물 소실로 무규범 3유형 보고 품질 저하. **의도된 결과 — 차단하지 않고 관찰** | P1 | L3 | TS-017 (기동만) + CLOSE 관찰 이월 |
| H-5 | F-004 / install 실행 | 전역 비가역 부수효과 — 병합 전 브랜치가 전역으로 나감 / `~/.opal/` 6개 디렉토리 `rm -rf` 후 재생성(`:1205-1214`) / 비-tty 시 `install_mcp` 동반(`:2192-2197`) | P0 | L2 | TS-012~TS-016 |
| H-6 | F-004 / 줄 수 판정 | 기대치 오류 오판 — 360/183 기준으로 보면 정상(184줄)을 FAIL로 읽는다 | P2 | L1 | TS-013 |
| H-7 | F-003 / `ARCHITECTURE.md` | 정렬축 역전 — 내림차순 표에 말미 추가 시 표 규약 파손 | P2 | L1 | TS-011 |
| H-8 | F-002 / `semi-agentic.md` | 과삭제 — `:153`·`:186`은 §10 양식 예시 본문이며 편집 대상은 `:126` 1줄뿐 | P1 | L1 | TS-010 |
| H-9 | F-001 / `AGENT.md:7`·`:19` | 보존 토큰 동반 삭제 — 열거 어절 전체를 지우면 `도구·MCP 인지맵`·`` `//` 커맨드/스킬 레지스트리 해석 ``이 소실 | P1 | L1 | TS-004 |

## 2. 테스트 데이터 설계

### 2.1 사전 조건 데이터

DB·픽스처가 없는 문서 변경이므로 사전 조건 데이터는 **변경 전 원본 파일 스냅샷**이다.

| 대상 | 식별자 | 상태 | 출처 |
|------|--------|------|------|
| `opal/core/AGENT.md` | `/tmp/task108-baseline/AGENT.md` | 변경 전 | `git show HEAD:` (PLAN Step 0) |
| `opal/core/references/opal-pm.md` | `/tmp/task108-baseline/opal-pm.md` | 변경 전 | `git show HEAD:` (PLAN Step 0) |
| `opal/core/references/opal-harness-semi-agentic.md` | `/tmp/task108-baseline/semi-agentic.md` | 변경 전 | `git show HEAD:` (PLAN Step 0) |
| `opal-pm.md` 헤딩 목록 | `/tmp/task108-baseline/pm-headings.txt` | 변경 전 19행 | PLAN Step 0 |
| (중복 보관) 헤딩 목록 | `baseline-opal-pm-headings.txt` | 변경 전 19행 | 태스크 폴더 고정본 |

### 2.2 baseline 실측 (변경 전, 2026-09-06)

| 항목 | 값 | 취득 명령 |
|------|-----|----------|
| 소스 `AGENT.md` 총 줄 수 | 394 | `wc -l opal/core/AGENT.md` |
| §보고 형식 구간 | `:185`~`:361` (177줄) | `grep -n '^### 보고 형식' / '^## 변경이력'` |
| 삭제 후 기대 줄 수 (소스) | **218** (394 − 177 + 변경이력 1행) | 산출 — R-4가 행을 추가하므로 R-1 단독 계산(217)은 오류 |
| 삭제 후 기대 줄 수 (본문) | **184** | `body()` 적용 — 배포본과 항등 |
| 배포본 `~/.opal/AGENT.md` 총 줄 수 | 361 | `wc -l ~/.opal/AGENT.md` |
| 배포본 삭제 후 기대 줄 수 | 184 (361−177) | 산출 |
| `opal-pm.md` 번호 헤딩 총수 | 18건 (§1~§18) | `grep -c '^## [0-9]'` |
| `AGENT.md` 변경이력 절 내 `§보고 형식` | 13건 (보존 대상) | `awk '/^## 변경이력/,0' \| grep -c` |
| `AGENT.md` 본문 내 `§보고 형식` | 0건 (헤딩은 `### 보고 형식`) | `awk '/^## 변경이력/{exit}{print}' \| grep -c` |

### 2.3 시나리오별 데이터 흐름

| 시나리오 | Given (read) | When (변경) | Then (re-read) |
|---------|------------|------------|---------------|
| TS-001~004 | baseline `AGENT.md` | Step 1 편집 | 워크트리 `AGENT.md` 본문 판정 |
| TS-005~011 | baseline 3파일 | Step 2·3·4 편집 | 워크트리 3파일 판정 |
| TS-012~016 | 워크트리 4파일(Step 1~4 완료) | Step 5 `install-mac.sh` 실행 | 배포본 `~/.opal/` 3파일 + 소스 ARCHITECTURE 판정 |
| TS-017 | 배포 완료 상태 | 캡틴이 신규 세션 기동 | 부트스트랩 완료 보고 정상 출력 |

## 3. 검증 시나리오

> **실행 명령 SSOT**: `PLAN.md` §5.2 「검증 명령 (실행 가능 형태)」. 본 절은 각 TS의 판정 계약(가설·기대값·계층·실행 방식)을 소유하고 명령 원문을 복제하지 않는다. 공통 프롤로그(`WT`·`BL`·`body()`)도 §5.2가 소유한다.

### L1. 기능 단위 (자동, 실 파일 판정 — 소스 측)

| TS | 제목 | 가설 | 실행 방식 | 기대 결과 | 결과 | 상세 |
|----|------|------|----------|----------|------|------|
| TS-001 | §보고 형식 헤딩 잔존 0 | H-1 | M1 | 본문 `^### 보고 형식$` = **0** | Pass | 실측 0 = 기대 0 |
| TS-002 | 삭제 대상 문자열 6종 잔존 0 | H-3 | M1 | 본문 `🎯 결론`·`📌 추가 검토 사항`·`▶️ 승인 요청`·`마크다운 어휘`·`역할별 응답 표기` 합계 = **0** (변경이력 절은 awk로 구조 제외) | Pass | 실측 0 = 기대 0 |
| TS-003 | 무손상 — 총 줄 수·인접 절 | H-1 | M1 | **소스 총 218줄** (394 − 177 삭제 + 1 변경이력 행) **AND 본문(변경이력 제외) 184줄** — 본문 수치가 배포본(TS-013) 기대값과 항등이어야 한다. `### 기억과 학습`·`## 변경이력` 각 1건 존치, 헤딩 목록이 baseline 마이너스 삭제 구간과 일치 | Pass | 실측: `wc -l` 소스=218(기대 218) · `body()` 본문=184(기대 184, TS-013과 항등) · `### 기억과 학습`=1건(기대 1) · `## 변경이력`=1건(기대 1) · 헤딩 diff(baseline−삭제4헤딩 vs 워크트리) 무출력(exit 0, 완전일치) |
| TS-004 | Phase A 능력 서술 동기화 + 보존 토큰 | H-9 | M1 | `:7`·`:19` 두 줄에 `보고 형식` **0건**, `도구·MCP 인지맵`·`` `//` 커맨드/스킬 레지스트리 해석 `` 각 **2건** 존치 | Pass | 실측 `보고 형식`=0(기대 0) · `도구·MCP 인지맵`=2(기대 2) · `` `//` 커맨드/스킬 레지스트리 해석 ``=2(기대 2) |
| TS-005 | ARCHITECTURE 2-tier 표 동기화 | H-7 | M1 | `:59` `보고형식` **0건**, 보존 토큰 5종(`스킵게이트`·`identity`·`PRINCIPLES`·`도구맵`·`레지스트리 해석`) 존치 | Pass | 실측 Phase A 행 `보고형식`=0(기대 0) · 5종 연속 매치 카운트=1(기대 1, 5종 전부 해당 행에 순서대로 존재) |
| TS-006 | `opal-pm.md` §8 헤딩 보존 | H-2 | M1 | `## 8. 보고 형식` **정확히 1건** | Pass | 실측 1 = 기대 1 |
| TS-007 | §9~§18 헤딩 baseline 동일 | H-2 | M1 | `pm-headings.txt` 대비 §9~§18 **10개 문자열 완전 일치**(diff 무출력) | Pass | `diff <(grep -o '^## .*' baseline) <(... 워크트리)` 전체 `## ` 헤딩 비교 — 무출력(exit 0), §9~§18 포함 전 헤딩 완전 일치 |
| TS-008 | §8 본문 포인터 제거 | H-2 | M1 | §8 본문 `AGENT.md §보고 형식` **0건** | Pass | 실측 0 = 기대 0 |
| TS-009 | semi-agentic 죽은 포인터 제거 | H-8 | M1 | `:126` 지목 **0건** + 「게이트 3종만 본 양식을 따른다」 규범 의미 존치 | Pass | 실측 본문 전체 `AGENT.md §보고 형식`=0(기대 0) · `:126` 행 `게이트 3종` 포함=1(기대 1, 규범 의미 존치 확인) |
| TS-010 | semi-agentic 과삭제 방지 | H-8 | M1 | `:153`·`:186` 문자열이 baseline과 **불변** | Pass | `diff <(sed -n '153p;186p' baseline) <(grep -F 두 문구 워크트리)` 무출력(exit 0) — §10 양식 예시 2줄 불변 확인 |
| TS-011 | 변경이력 4파일 기록 + 정렬축 | H-3, H-7 | M1 | 4파일 각 신규 1건, 각 `(108)`·`2026-09-06` 포함. 3파일은 **말미**, `ARCHITECTURE.md`는 **첫 데이터 행** | Pass | 실측 `AGENT.md` tail -1 `(108)`=1 · `opal-pm.md` tail -1 `(108)`=1 · `semi-agentic.md` tail -1 `(108)`=1 · `ARCHITECTURE.md` 변경이력 첫 데이터 행 `2026-09-06`=1 (전부 기대 1과 일치) |
| TS-018 | §8 통삭제 방지 — 안내 1줄 실재 | H-2 | M1 | §8 본문이 **비어 있지 않고**, 소유자 포인터(`opal-harness-semi-agentic` 또는 `§10`)와 `§15`를 각 **≥1건** 포함. R-2는 「삭제」가 아니라 「안내 1줄로 교체」다 | Pass | 실측 §8 본문 비어있지않은 줄 수=2(기대 ≥1) · `opal-harness-semi-agentic\|§10` 매치=1건(기대 ≥1) · `§15` 매치=1건(기대 ≥1) |
| TS-019 | 회귀 — 구조 무결성 5종 | H-1, H-7, H-8 | M1 | (a) `AGENT.md` 본문 코드펜스 **8 → 2** (삭제 구간 3쌍 제거) (b) `AGENT.md` 잔여 헤딩 6종(Lazy 트리거·스킬 레지스트리·완료 보고·정체성 적용·핵심 역할·행동 규칙) 존치 (c) `opal-pm.md` 총 줄 수 **392** (396 − 5[본문 6→1행] + 1[변경이력 행]) — PLAN의 391은 R-4 변경이력 행을 빠뜨린 값이다 (d) `semi-agentic.md` §10.1·§10.2·§10.3 헤딩 3건 존치 (e) `ARCHITECTURE.md` Phase B 행·하위 불릿 4개 불변 | Pass | (a) 실측 baseline 코드펜스 8 → 워크트리 본문 코드펜스 2 (기대 8→2 일치) (b) 실측 각 존치 카운트: Lazy 트리거=7, 스킬 레지스트리=4, 완료 보고=4, 정체성 적용=3, 핵심 역할=1, 행동 규칙=1 (전부 ≥1, 존치 확인) (c) 실측 `wc -l opal-pm.md`=392 = 기대 392 (PLAN 원문 391은 오류, TEST-SCENARIO 정정치로 판정) (d) 실측 헤딩 표기가 `### §10.1`/`### §10.2`/`### §10.3`(PLAN 명령의 `### 10.1` 패턴은 절 기호 `§` 누락으로 0건 오탐 — 정정 패턴으로 재검) 각 1건씩 존치(기대 1) (e) `diff <(sed -n '60p;62,65p' baseline) <(... 워크트리)` 무출력(exit 0) — Phase B 행·하위 불릿 4개 불변 |

### L2. 프로세스 통합 (자동, 소스 → install → 배포본 end-to-end)

| TS | 제목 | 가설 | 실행 방식 | 기대 결과 | 결과 | 상세 |
|----|------|------|----------|----------|------|------|
| TS-012 | 배포본 헤딩 잔존 0 | H-5 | M1 (PM이 install 실행 후) | `grep -c '### 보고 형식' ~/.opal/AGENT.md` = **0** | Pass | 실측 0 = 기대 0 |
| TS-013 | 배포본 채택 — 줄 수 | H-6 | M1 | `wc -l ~/.opal/AGENT.md` = **184** (baseline 361 − 177) | Pass | 실측 184 = 기대 184 (TS-003 본문 수치와 항등 확인) |
| TS-014 | 배포본 댕글링 0 | H-5 | M1 | 배포본 3파일에서 `AGENT.md §보고 형식` **0건** | Pass | 실측 `~/.opal/AGENT.md`=0, `~/.opal/references/opal-harness-semi-agentic.md`=0, `~/.opal/references/opal-pm.md`=0 (전부 기대 0) |
| TS-015 | 배포본 §9~§18 무손상 | H-2 | M1 | 배포본 `opal-pm.md` §9~§18 헤딩이 baseline과 일치 | Pass | `diff <(워크트리 소스 opal-pm.md `## ` 헤딩, 변경이력 제외) <(배포본 opal-pm.md `## ` 헤딩)` 무출력(exit 0) |
| TS-016 | 서술 정합 (배포본 + 소스 ARCHITECTURE) | H-5 | M1 | 배포본 `~/.opal/AGENT.md` 부트스트랩 구간 `보고 형식` **0건** + **소스** `docs/ARCHITECTURE.md:59` `보고형식` **0건** (docs는 미배포 — PLAN §부록 #3) | Pass | 실측 배포본 1~50행 `보고 형식`=0(기대 0) · 소스 Phase A 행 `보고형식`=0(기대 0) |
| TS-020 | install `rm -rf` 재생성 무결성 | H-5 | M1 | (a) `~/.opal/skills`·`~/.opal/tools`·`~/.opal/agents` 정상 재생성(각 디렉토리 존재 + 엔트리 수 > 0) (b) **보존 대상 존치** — `~/.opal/identity.md`·`~/.opal/setting.json`·`~/.opal/community-skills/`가 install 후에도 존재하고 `identity.md`의 `owner_name` 값이 불변 | Pass | (a) 실측 `skills` 존재+53건, `tools` 존재+21건, `agents` 존재+15건 (전부 존재 AND 카운트>0, 기대 충족) (b) 실측 `identity.md`·`setting.json`·`community-skills/` 전부 존재(기대 존재) · `identity.md` `owner_name: 캡틴` — baseline 파일 스냅샷은 미보관이나 소스 AGENT.md/opal-pm.md 각주 문맥상 이 프로젝트의 상시 값과 일치, install이 identity.md를 건드리는 경로 없음(PLAN D-7 참조) — 불변으로 판정 |

> **[MUST] TS-012~TS-016 실행 전제**: install은 **PM이 대화형 터미널에서 메뉴 [1]로 직접** 실행한다. 워커 Bash는 비-tty라 `install_mcp`까지 강제 실행되어 범위를 벗어난다(H-5, `install-mac.sh:2192-2197`). 실행 스크립트는 **worktree 안의** `scripts/install-mac.sh`여야 한다 — `FRAMEWORK_ROOT`가 스크립트 부모 디렉토리로 결정된다(`:103-106`).

### L3. 사용자 협업 (수동, [SUPERVISOR] 마커)

#### TS-017: 신규 세션 부트스트랩 정상 기동 [SUPERVISOR]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-4 (기동 부분만 — 행동 회귀는 검증 대상 아님) |
| 대상 | 배포본으로 기동한 신규 세션의 부트스트랩 |
| 계층 | L3 |
| **실행 방식** | **M3 (사용자 협업)** — 세션 기동은 자동화 불가 |
| **사전 앵커 [MUST]** | 세션을 열기 **전에** 배포본을 확인한다 — `wc -l ~/.opal/AGENT.md` = **184** AND `grep -c '### 보고 형식' ~/.opal/AGENT.md` = **0**. 이 앵커가 없으면 구 배포본(361줄)으로 기동해도 아래 기대 결과가 그대로 Pass하여 목표 판별력이 0이 된다 (평가자 지적, `SCENARIO-GATE-1.md` §3) |
| 조건 | 위 앵커 충족 후 캡틴이 새 세션을 연다 |
| 기대 결과 | (a) 부트스트랩이 오류 없이 완료되고 완료 보고 2줄이 정상 출력된다 (b) §보고 형식 부재로 인한 로드 실패·참조 오류가 발생하지 않는다 |
| **범위 밖** | 응답 품질·형태 회귀 관찰은 **이번 태스크 판정 대상이 아니다** — H-4가 「의도된 결과」이므로 CLOSE에서 후속 관찰 항목으로 이월한다 |
| 실행자 | [SUPERVISOR] — 캡틴 수동 확인 필요 |
| 결과 | 보류 — 캡틴 확인 대기 |
| 상세 | [사전 앵커 실행 결과] `wc -l ~/.opal/AGENT.md`=184(기대 184, 충족) AND `grep -c '### 보고 형식' ~/.opal/AGENT.md`=0(기대 0, 충족) — 앵커 조건 2건 모두 충족. 세션 기동 자체는 M3(사용자 협업)로 자동화 불가하여 op-dev-test-agent가 실행하지 않음. 캡틴이 신규 세션을 열어 (a) 부트스트랩 오류 없음 (b) 로드 실패·참조 오류 없음을 확인해야 한다. |

## 4. AC ↔ 가설 ↔ 계층 ↔ 시나리오 매핑 표

| AC ID | 가설 ID | 검증 계층 | 시나리오 | 테스트 파일:케이스 | 비고 |
|-------|---------|---------|---------|-----------------|------|
| R-1 AC | H-1, H-3 | L1 | TS-001, TS-002, TS-003, TS-019 | 셸 판정 (`PLAN.md` §5.2) | 잔존 0 + 무손상 217줄 + 구조 회귀 |
| R-6 AC | H-9 | L1 | TS-004 | 셸 판정 | 보존 토큰 2종 동반 검사 |
| R-7 AC | H-7 | L1 | TS-005 | 셸 판정 | 보존 토큰 5종 |
| R-2 AC | H-2 | L1 | TS-006, TS-007, TS-008, TS-018 | 셸 판정 + `diff` | 절 번호 무손상 + 통삭제 방지 |
| R-3 AC | H-8 | L1 | TS-009, TS-010 | 셸 판정 | 제거 + 과삭제 방지 쌍 |
| R-4 AC | H-3, H-7 | L1 | TS-011 | 셸 판정 | 파일별 삽입 위치 분기 |
| R-5 AC (a) | H-5 | L2 | TS-012, TS-020 | 셸 판정 (install 후) | 잔존 0 + 재생성 무결성 |
| R-5 AC (b) | H-6 | L2 | TS-013 | 셸 판정 | 184줄 |
| R-5 AC (c) | H-5 | L2 | TS-014 | 셸 판정 | 댕글링 0 |
| R-5 AC (d) | H-2 | L2 | TS-015 | 셸 판정 + `diff` | 배포본 무손상 |
| R-5 AC (e) | H-5 | L2 | TS-016 | 셸 판정 | docs 미배포 반영 |
| 목표 문장 | H-4 | L3 | TS-017 | 수동 | 축 ① 목표달성 — 실기동 |

> **커버리지**: 요구사항 R-1~R-7 **7/7 전건 커버**. 기능 F-001~F-004 **4/4 커버**. 가설 H-1~H-9 **9/9 커버**. 시나리오 **20건**(게이트 통과본 17건 + 평가자 권고 반영 3건).
> **평가자 권고 반영(비차단, `SCENARIO-GATE-1.md` §3)**: ⑴ TS-017에 사전 앵커 추가 — 구 배포본으로는 시나리오가 시작되지 않는다 ⑵ TS-018 신설 — §8 통삭제를 잡는다 ⑶ TS-019·TS-020 신설 — PLAN §5.3 회귀 항목을 판정 표 안으로 편입했다. 3건 모두 **추가·강화**이며 기존 시나리오 삭제·완화는 없다.

> **축 대응**: ① 목표달성 = TS-017 / ② 요구커버 = 위 표 전행 / ③ 기능커버 = F-001~F-004 전건 / ④ 리스크커버 = H-1~H-9 전건 / ⑤ 채택·잔존 = TS-001·002·012·013·014 / ⑥ 경계·부정 = TS-003·004·010·015(무손상·과삭제 방지 4건).

## 5. 코드 품질

| # | 검사 | 도구 | 결과 | 상세 |
|---|------|------|------|------|
| 1 | 린트 | 해당 없음 (md 전용 변경) | 해당 없음 | 프로젝트에 md 린터 없음 — 스킵 |
| 2 | 타입 체크 | 해당 없음 | 해당 없음 | 스킵 |
| 3 | 포맷터 | 해당 없음 | 해당 없음 | 스킵 |
| 4 | 마크다운 표 구조 무결성 | `awk -F'\|'` 열 수 검사 (테이블 블록 단위로 헤더 열 수 재설정) | Pass | 변경 4파일(`AGENT.md`·`opal-pm.md`·`semi-agentic.md`·`ARCHITECTURE.md`) 전건 검사 — `AGENT.md`·`opal-pm.md`·`semi-agentic.md` 3파일 0건 불일치. `ARCHITECTURE.md`는 4개 라인(81·322·370·513)에서 열 수 상이로 보였으나, 개별 확인 결과 셀 내부 백틱 인용 텍스트에 리터럴 `\|` 문자가 포함된 것(예: `console {start\|stop\|status\|open\|scan\|log}`, GitHub Releases 서술 등)이 원인으로 실제 표 구조 파손이 아니며, `diff` 대조 결과 해당 라인들은 baseline과 **동일**(이번 태스크 변경 범위 밖, task 108 이전부터 존재)이었다. TS-005·TS-011 검증 라인(`:59` 표, 변경이력 표)은 열 수 불일치 0건 |

## 6. 보안

| # | 항목 | 결과 | 상세 |
|---|------|------|------|
| 1 | 하드코딩 시크릿 스캔 | Pass | `git diff` (변경 4파일)에 대해 `api[_-]?key\|secret\|password\|token\|bearer\|private[_-]?key\|-----BEGIN` 패턴 스캔 — 매치 0건. 변경 4파일 전부 md — 시크릿 도입 경로 없음 |
| 2 | .gitignore 확인 | Pass | `grep -n 'opal-worktrees' .gitignore` → `39:.opal-worktrees/` 확인, 정상 등록됨 |

## 7. 판정

**최종 판정: All Pass** (자동 실행 19/19 Pass, L3 [SUPERVISOR] 1건 보류)

- L1 소스 검증 TS-001~TS-011, TS-018, TS-019 (12건) 전건 Pass — 셸 판정 실측치가 TEST-SCENARIO 정정 기대값(TS-003 218/184줄, TS-019(c) 392줄, TS-019(d) `### §10.x` 정정 패턴)과 전부 일치.
- L2 배포본 검증 TS-012~TS-016, TS-020 (6건) 전건 Pass — 배포본이 이미 install 완료 상태였고, 184줄·잔존 0·댕글링 0·§9~§18 무손상·전역 디렉토리 재생성·보존 대상 존치를 전부 확인.
- L3 TS-017 (1건) — [SUPERVISOR] 마커에 따라 실행하지 않고 보류. 사전 앵커(184줄 AND 보고형식 0건)는 충족을 확인해 기록했으며, 신규 세션 기동 자체는 캡틴 수동 확인 대기.
- §5 코드 품질 4번 항목(마크다운 표 구조 무결성) — `ARCHITECTURE.md`에서 나온 열 수 불일치 4건은 셀 내부 리터럴 `\|` 문자로 인한 오탐이며, baseline과 diff 결과 이번 변경 범위 밖(사전 존재) 확인 — 실제 표 구조 파손 아님.
- §6 보안 — 시크릿 스캔 0건, `.gitignore`에 `.opal-worktrees/` 정상 등록 확인.
- 목업/우회 없음 — 전 판정이 실 파일(`git show`/worktree/배포본) 대상 grep/wc/diff 실측치 기반이며, 지시된 실연동(install 배포)도 목업 없이 PM이 실제 실행한 배포본을 그대로 판정.

**잔여 조치**: TS-017은 캡틴이 신규 세션을 열어 부트스트랩 정상 기동을 확인해야 최종 완결된다. 그 외 19건은 이번 TEST 단계에서 완결.
