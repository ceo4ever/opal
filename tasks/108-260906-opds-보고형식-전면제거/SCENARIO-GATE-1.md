# SCENARIO GATE 1 — 목표-커버 루브릭 판단축 채점

> 실행 일시: 2026-09-06T12:58:00+09:00 | phase: `scenario-rubric` | iteration: 1
> scenario_source: `tasks/108-260906-opds-보고형식-전면제거/TEST-SCENARIO.md`
> 참고 입력: `TASK.md`(목표·R-1~R-7) · `PLAN.md`(F-001~F-004·H-1~H-9·§5.2 검증 명령) · `.scenario-coverage-input.json`
> 규칙 SSOT: `~/.opal/references/harness/scenario-gate.md` §2(6축·판정 주체 분리) · §5-1(종료조건 임계)
> 판정 범위: 판단축 **①목표달성 · ⑤채택/잔존 · ⑥경계/부정** 3축만. 결정론축 ②③④는 test-tool 소관이며 본 채점 대상이 아니다.
> CONTRACT.md: 부재 — `scenario-rubric`은 Phase 2 CONTRACT 병합을 건너뛰는 별도 트랙이므로 판정에 영향 없음.

## 1. 판단축별 판정

| 축 | 점수 | 근거 | gap |
|----|------|------|-----|
| ① 목표 달성 | **1** | 운영 계층 검증은 성립한다 — 목표의 최종 산출물인 전역 배포본을 TS-012(`grep -c '### 보고 형식' ~/.opal/AGENT.md` = 0) · TS-013(`wc -l` = 184) · TS-014(배포본 3파일 댕글링 0) · TS-016(배포본 부트스트랩 구간 `보고 형식` 0)이 실물로 판정한다. 이는 소스 검사가 아니라 전 세션이 실제로 Read하는 아티팩트에 대한 판정이므로 "상시 로드 코어에서 절이 빠졌는가"의 직접 증거다. **그러나 사용자 계층 검증이 공동화되어 있다** — `is_goal_scenario: true`로 표시된 유일한 시나리오 TS-017의 기대값이 「부트스트랩 오류 없이 완료 + 완료 보고 2줄 정상 출력」인데, 이 기대값은 **구 배포본(361줄, 절 잔존)으로 기동해도 동일하게 Pass한다**. 즉 TS-017은 단독으로 목표 판별력이 0이며, 목표 판별은 전적으로 파일 계층(TS-012~016)에 의존한다. 「행동 관찰을 범위 밖으로 뺀 것」 자체는 목표 문장이 "대체 규범은 이번 범위에 두지 않는다"로 명시하고 H-4를 「의도된 결과」로 판정했으므로 정당하나, 축소 과정에서 **세션 계층의 목표 판별 앵커까지 함께 빠졌다** — 「새 세션이 갱신된 배포본을 로드했는가」를 세션 측에서 교차 확인하는 항목이 없다. 통과선(≥1)은 충족하나 2점 앵커("사용자·운영 계층에서 목표를 직접 검증")에는 미달한다 | 통과선 충족 — 정식 gap 아님 (§3 권고 R-1 참조) |
| ⑤ 채택/잔존 | **2** | **구형 잔존 0**: TS-001(본문 `^### 보고 형식$` 0) · TS-002(`🎯 결론`·`📌 추가 검토 사항`·`▶️ 승인 요청`·`마크다운 어휘`·`역할별 응답 표기` 합계 0) · TS-008(§8 본문 포인터 0) · TS-009(`:126` 지목 0) · TS-012(배포본 헤딩 0) · TS-014(배포본 3파일 댕글링 0). 소스·배포본 양쪽 경계를 install strip 식(`install-mac.sh:226`)과 **동일한 awk 표현식**으로 통일해(M-3) 변경이력 과거 기록 13건의 오판을 구조적으로 배제했다 — H-3 양방향 오판에 대한 실질 방어다. **신형 채택**: 줄 수 단독 의존이 아니다 — TS-013(184줄)은 4개 채택 근거 중 하나일 뿐이고, TS-016이 배포본 부트스트랩 구간을 직접 판정하며, TS-004(보존 토큰 `도구·MCP 인지맵` 2건 · `` `//` 커맨드/스킬 레지스트리 해석 `` 2건)와 TS-005(보존 토큰 5종)가 「보고 형식을 뺀 새 서술」의 positive 존치를 카운트로 확인한다. 두 하위 축 모두 실행 가능한 기대값을 갖는다 | 없음 |
| ⑥ 경계/부정 | **2** | AC를 뒤집어 적은 형식적 시나리오가 아니라 **PLAN 리스크 가설의 실패 모드에서 역산된 검출식**이다. TS-003은 `diff <(baseline 헤딩 − 삭제 구간 5개) <(현재 헤딩)`으로 과삭제·미삭제를 **양방향** 검출하고 「정확히 217줄」 등호 판정을 건다. TS-010은 `diff <(sed -n '153p;186p' baseline) <(grep -F …)`로 §10 양식 예시 2줄의 바이트 불변을 확인해 H-8 과삭제를 실측 검출한다. TS-007/TS-015는 `^## ` 헤딩 목록 전체 diff이므로 §8 삭제·재번호 시 무성 파손이 아니라 diff 출력으로 드러난다(H-2 P0). TS-011은 ARCHITECTURE 내림차순 첫 데이터 행을 지정 판정해 H-7 정렬축 역전을 잡는다. 경계값이 부등호가 아닌 등호(217 / 184 / 1건 / 2건 / 5종)라 초과·미달 양쪽이 Fail로 떨어진다 — 제거형 목표의 실패 모드인 과삭제가 실제로 판정 대상이다 | 없음 |

## 2. 종합 판정

```json
{"scores": {"goal": 1, "adoption": 2, "boundary": 2}, "average": 1.67, "gaps": [], "verdict": "pass"}
```

| 항목 | 값 | 임계 (§5-1) | 판정 |
|------|-----|------------|------|
| goal | 1 | ≥1 | 충족 |
| adoption | 2 | ≥1 | 충족 |
| boundary | 2 | ≥1 | 충족 |
| 0점 축 | 없음 | 0점 축 없을 것 | 충족 |
| average | 1.67 | ≥1.5 | 충족 |
| **verdict** | **pass** | — | — |

> 본 verdict는 게이트 PASS의 두 증거 중 판단 파트 1건이다. `scenario-gate.md` §6에 따라 test-tool `scenario-coverage-check` exit 0(누락 0)과 **함께** 성립한다.

## 3. 잔여 권고 (통과선 충족 — 재작성 강제 아님)

verdict가 pass이므로 §4 재작성 루프 대상은 아니다. 다만 아래 3건은 채점 중 확인된 판별력 공백이며, TEST-SCENARIO.md를 손대는 다음 기회 또는 CLOSE 이월 시 반영을 권고한다.

| # | 대상 축 | 내용 |
|---|--------|------|
| R-1 | ① | **TS-017에 목표 판별 앵커 1줄 추가** — 현재 기대값은 구 배포본으로도 Pass한다. 「신규 세션의 부트스트랩 완료 보고 또는 Phase A 능력 서술이 갱신본 문언(보고 형식 어절 부재)과 일치한다」 같은 관측 1건을 넣으면 행동 품질 관찰을 범위 밖에 둔 채로도 세션 계층 판별력이 회복된다. 행동 회귀 관찰(H-4) 이월 판단 자체는 타당하므로 유지 |
| R-2 | ⑤ | **§8 대체 문안의 positive 미검증** — TS-006(헤딩 1건)·TS-008(포인터 0건)은 워커가 §8 본문을 교체하지 않고 **통삭제**해도 둘 다 Pass한다. R-2는 "본문을 안내 1줄로 교체"이므로, PLAN §3.2.2 (1) 문안 계약 3요소 중 소유자 포인터(`opal-harness-semi-agentic.md` §10 · 본 문서 §15) 존치를 grep 1건으로 확인하면 통삭제 변형이 막힌다. PLAN §5.3의 「`opal-pm.md` 총 줄 수 391」 회귀 항목이 이를 간접 커버하나 TS 번호가 없어 §3 판정 표에 편입되지 않았다 |
| R-3 | ⑥ | **H-5(P0)의 파괴 경로가 TS 미편입** — `install_opal()`이 `~/.opal/{skills,agents,references,templates,tools,dashboard-server}`를 `rm -rf` 후 재생성하는데(`install-mac.sh:1205-1214`), TS-012~016은 전부 3파일 내용 판정일 뿐 재생성 무결성과 보존 대상(`identity.md`·`community-skills/`) 존치를 판정하지 않는다. PLAN §5.3 회귀 6번에 체크박스로만 있고 TS 번호가 없다. 같은 이유로 삭제 구간의 코드펜스 3쌍 짝 검사(§5.3 회귀 1번, `grep -c '^\`\`\`'` 8→2)도 판정 표 밖이다 |

