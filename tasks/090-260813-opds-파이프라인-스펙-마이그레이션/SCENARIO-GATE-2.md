# SCENARIO GATE 2 — 목표-커버 루브릭 재판정 (판단축 ①⑤⑥)

> 실행 일시: 2026-08-13T16:40:00+09:00 | phase: `scenario-rubric` | iteration: 2
> scenario_source: `tasks/090-260813-opds-파이프라인-스펙-마이그레이션/TEST-SCENARIO.md`
> normalized_payload: `tasks/090-260813-opds-파이프라인-스펙-마이그레이션/.scenario-coverage-input.json`
> 판정 주체: opal-evaluator-agent (verdict-only · readonly)
> 규칙 SSOT: `opal/core/references/harness/scenario-gate.md` §2(6축 · 판정주체 분리) · §5-1(종료조건 임계)
> 직전 판정: `SCENARIO-GATE-1.md` (①2 · ⑤1 · ⑥2, average 1.67, gap 1건)

> **판정 범위**: 판단축 ①⑤⑥만 채점한다. ②③④는 test-tool `scenario-coverage-check`가 결정론으로 판정했다(exit 0 / `all_covered: true` — requirements 9 / features 7 / hypotheses 18 / scenarios 18). 본 보고서는 그 축을 재판정하지 않는다 (scenario-gate.md §2 [MUST]).

---

## 1. 판단축별 판정 (iteration 1 대비)

| 축 | 정의 | iter 1 | **iter 2** | 통과선 | 변화 |
|----|------|--------|-----------|--------|------|
| ① 목표 달성 | 사용자/운영 계층에서 목표를 검증하는 시나리오 존재 | 2 | **2** | ≥1 | 유지 (변경 없음 — 재확인) |
| ⑤ 채택/잔존 | 교체형 목표 = 구형 잔존0 · 신형 채택 검증 시나리오 존재 | 1 | **2** | ≥1 | **+1 (gap 해소)** |
| ⑥ 경계/부정 | 경계값·부정 경로 시나리오 존재 | 2 | **2** | ≥1 | 유지 (S-18 역방향 검증으로 실질 강화, 상한 도달로 점수 불변) |

**scores**: `{"goal": 2, "adoption": 2, "boundary": 2}` · **average**: `2.00`

---

### ① 목표 달성 — 2점 (유지)

**재확인 결과 — 변경 없음, iteration 1 근거 그대로 성립한다.**

- S-17(§3 L3, `[SUPERVISOR]`, `TEST-SCENARIO.md:333-345`)이 그대로 존치한다 — 배포본 `~/.opal/skills/*/references/pipeline.json` 대상, 새 세션 실사용, deprecation 경고 미출력, 행 수 oppl 19 / oppd 13.
- §4 매핑 표의 목표 행(`:370` `목표 (9→10/10 전환, 실사용 가능) | H-1, H-5 | L2 + L3 | S-13, S-17 | [T090/L3-GOAL]`)이 요구(R-N) 행과 별도로 유지된다. 정규화 페이로드에서도 S-17만 `is_goal_scenario: true`다.
- 운영 계층 실행형(S-13 · S-14 · S-16)도 무변경이다.

**이번 반복의 영향 평가**: TASK.md 완료기준에 **(0) 레포 전역 지시 지점 0건**이 신설되었으나(`TASK.md:113`), 이는 잔존(⑤축) 명제이지 새 목표 계층 관측점을 요구하지 않는다. S-10·S-18(L1)이 이를 받는다. ①축 채점에는 영향이 없다.

---

### ⑤ 채택/잔존 — 2점 (iteration 1: 1점, gap 해소)

**iteration 1 gap의 성격 재확인** — "S-10 검증 범위(pilot SKILL.md 10개) < 목표 문장의 주장 범위(호출자 0건)". 즉 **검증되는 명제와 주장하는 명제의 불일치**였다. PM은 두 선택지 중 A안(범위 확장)을 택했다.

**반영이 실질적인지 — 4개 층 전부에서 확인했다**

| 층 | iteration 2 반영 | 인용 |
|----|----------------|------|
| 완료기준 | **(0) 신설** — "레포 전역에서 deprecated `.md` 파싱 경로를 지시·예시하는 문서 지점 0건(도구 자신 제외)" | `TASK.md:113` |
| 요구 | **R-9 신설** — 4곳을 file:line으로 명시, AC에 "지시·예시하는 표현 0건 + 변경이력 1행" + 제외 대상 명시 | `TASK.md:165-169` |
| 리스크 | **H-18 신설, P0** — "남기면 다음 사람이 복사해 구형 경로를 재도입" | `TEST-SCENARIO.md:28` |
| 시나리오 | **S-10 확장** + **S-18 신설** | `TEST-SCENARIO.md:218`, `:254-267` |

**커버 실효성 — 지적한 4곳이 실제로 잡히는가 (독립 실측)**

레포 전역 재스캔(`grep -rn "rows-from" --include="*.md" ...`, `tasks/**`·`.git` 제외) 결과, pilot SKILL.md 6종과 도구 자신 문서를 뺀 **살아있는 지시**는 정확히 4곳이다:

- `opal/core/references/tools.md:84` — 시놉시스 `[--rows-from <path-to-skill.md>]`
- `opal/core/references/tools.md:152` — 실행 예시 `--rows-from ~/.opal/skills/opal-pilot-project/SKILL.md` (직전 `:148` 주석 `# SKILL.md에서 행 구성 자동 파싱` 포함)
- `opal/core/references/harness/task-process.md:49`
- `opal/skills/op-task/SKILL.md:223`

즉 **iteration 1이 지적한 집합과 실측 집합이 정확히 일치**하며, R-9·S-18의 대상 열거가 누락 없이 완전하다. PLAN도 이를 `tools.md:148` 주석까지 포함해 지정 문자열로 확정했다(`PLAN.md:1347`, §3.8.2 4곳 표 `:539-542`).

**주장 ↔ 검증 범위의 일치가 회복되었다**

- S-10 기대 결과가 **잔존(pilot)** / **잔존(레포 전역)** / **채택** 3분할로 재작성되었다 — 전역 절은 스코프(`opal/`·`docs/`·`README.md`)와 제외(`state_tool.py`·`state-tool/README.md`)를 명시한다(`TEST-SCENARIO.md:218`).
- 실행 가능한 술어가 PLAN에 있다 — **P4-b 전역 스캔 명령**이 제외 필터(도구 디렉토리, pilot 변경이력 행 `| v[0-9]`)까지 포함해 확정되어 있다(`PLAN.md:1003-1011`). iteration 1이 요구한 "범위·제외의 명시"가 충족된다.
- 열거형(S-18, 4곳 결정론 대조) + 개방형(S-10 전역 스캔) **두 방식이 병존**한다. 열거 밖 신규 지점이 생겨도 개방형이 잡는다.

**채택 측은 iteration 1 그대로 3중 유지** — ① 문서 채택(S-10 후반, `pipeline.json` 매칭 10파일) ② 런타임 채택(S-13, 20회 init · deprecation 경고 0회) ③ 실사용 채택(S-17).

**S-18의 역방향 검증은 형식이 아니라 유효한 방어다**

`state_tool.py`·`state-tool/README.md` **변경 0건** 요구(`TEST-SCENARIO.md:263` ④, `PLAN.md:1014-1015` `git status --porcelain -- ... | wc -l # = 0`)는 세 가지 이유로 실질적이다.

1. **이번 반복이 새로 만든 리스크를 정확히 겨눈다.** 범위를 "레포 전역 `.md` 지시 0건"으로 넓힌 직후, 워커가 도구 자신의 **정확한** 분기 설명(`README.md:55`)·에러 메시지(`state_tool.py:104`)까지 "잔존"으로 오인해 지우는 과잉 정정이 새 실패 모드다. DEC-11이 이를 불가침으로 못 박고 S-18 ④가 도구로 집행한다.
2. **기존 어떤 시나리오도 이를 대신 잡지 못한다.** S-11(배포 경계)은 변경 파일이 `opal/` 하위인지만 보므로 `opal/tools/state-tool/state_tool.py` 수정을 **통과시킨다**. S-3의 해시 대조 6파일은 opsdd 연쇄 대상이라 도구 파일을 포함하지 않는다. 제약 (a)("소스 무변경")는 문장으로만 존재했다 — S-18 ④가 이를 검증하는 **유일한 관측점**이다.
3. 술어가 결정론이다(`git status --porcelain` 출력 0줄). 판단 여지가 없다.

**판정**: 2점 앵커("구형 잔존0·신형 채택 모두 검증")를 충족한다. 잔존은 목표가 주장하는 범위(레포 전역)에서, 채택은 3중으로 검증되며, 확장이 만든 과잉 정정 리스크까지 역방향으로 봉인된다. **"지적을 반영했으므로 가점"이 아니라, 반영 결과가 실측 지점 집합과 일치하고 실행 가능한 술어를 갖췄기 때문에 2점이다.**

---

### ⑥ 경계/부정 — 2점 (유지 — 상한 도달)

**iteration 1의 5건은 전부 무변경 존치한다** — S-3(계수+해시로 "안 했다"를 증명) · S-11(배포 경계) · S-12(줄 단위 무개명) · S-15(레포 오염) · S-16(`--na` 후 CLOSE 진입). 경계값 근거 S-5 · S-4 · S-8 · S-1도 동일하다.

**이번 반복의 강화 (점수 불변, 상한 2 이미 도달)**

- S-18이 **역방향(부정) 시나리오**를 1건 추가했다 — "무엇을 바꿨는가"가 아니라 "무엇을 **바꾸지 않았는가**"를 검증한다(`TEST-SCENARIO.md:263` ④). 정규화 페이로드에서도 S-18은 `is_boundary_scenario: true`다.
- 이로써 부정 경로가 5건 → **6건**이 되었고, 특히 **도구 소스 무변경**이라는 태스크 최상위 제약 (a)에 처음으로 관측점이 생겼다.

**iteration 1의 비감점 관찰 2건은 미해소** — ① S-4에 negative control 부재(검사 대상 0건이어도 통과 가능) ② S-16 단방향(guard가 무력화돼도 PASS). 두 건 모두 부정 경로의 **부재**가 아니라 기존 경로의 강화 여지이므로 이번에도 감점하지 않는다. 다만 iteration 1에서 그대로 이월된 항목임을 기록한다.

---

## 2. 종합 verdict

```json
{"scores": {"goal": 2, "adoption": 2, "boundary": 2}, "average": 2.0, "gaps": [], "verdict": "pass"}
```

**verdict: `pass`** — scenario-gate.md §5-1 임계 2조건 충족.

1. **0점 축 없음** — ①2 · ⑤2 · ⑥2.
2. **평균 ≥1.5** — (2+2+2)/3 = **2.00** (iteration 1: 1.67).

결정론 파트(②③④)가 exit 0 · `all_covered: true`이므로 §6 tool-gated 집행의 두 증거가 모두 성립한다.

**gaps**: 없음 (통과선 미달 축 0건).

**진전 판정** (§5-3 무진전 신호 대비): ⑤축 1 → 2, gaps 1건 → 0건. **명백한 진전**이며 무진전 에스컬레이션 대상이 아니다.

---

## 3. 관찰 — 신규 발견 (감점 아님 · EXECUTE 전 권고)

통과선·앵커에 영향을 주지 않으므로 `gaps`에 넣지 않는다. 다만 **실행 시 마찰이 예상되는 지점**이므로 기록한다. 반영 판단·실행은 PM 책임이다.

### (1) [P1] 전역 스캔이 미열거 히트를 낼 수 있다 — `state-template.md:94`

PLAN P4-b 스캔 명령은 `"도메인 치환값" 참조` 패턴을 포함한다(`PLAN.md:1006`). 이 패턴은 R-9가 열거한 4곳 외에 다음을 추가로 잡는다.

- `opal/core/references/harness/state-template.md:94` — "오케스트레이터 SKILL.md 'STATE.md 도메인 치환값'에 해당 스킬의 파이프라인 현황판 **행 예시가 명시됨**"

이 줄은 `task-process.md:49`·`op-task/SKILL.md:223`과 **같은 species(행 원천을 SKILL.md로 가리키는 포인터)**이나 `--rows-from`을 명시하지 않아 R-9 열거에서 빠졌다. P4-b의 판정 기준이 "`SKILL.md`를 **행 원천·인자로 지시**하는 줄 0건"(`PLAN.md:1011`)이므로, 실행 시 이 히트를 두고 "0건 위반인가 / 미러 표 안내라 무해한가"를 워커가 즉석 판단하게 된다. 그 판단이 그때그때 달라지면 완료기준 (0)의 "0건"이 흔들린다.

> 권고: EXECUTE 전에 `state-template.md:94`를 **① R-9 정정 대상에 편입**하거나 **② 명시적 범위 밖(사유: 미러 표 안내이며 D-5로 표는 존치)**으로 확정하라. 참고로 `qa-standards.md:46`도 같은 패턴에 걸리나 QA 산출물 오버라이드 문맥이라 본 태스크와 무관하다.

### (2) [P2] 완료기준 (0)의 "레포 전역"과 S-10 스코프(`opal/`·`docs/`·`README.md`)가 문언상 불일치한다

`.opal/brain/**`가 스코프 밖인데 완료기준 (0)에는 그 제외가 적히지 않았다("도구 자신" 제외만 명시, `TASK.md:113`). 실제로 브레인에 서술형 잔존이 있다.

- `.opal/brain/pages/concept/legacy-row-address-gate-insertion-regression.md:28` — "opsdd는 … `--rows-from`가 그 표를 파싱하는 **레거시 경로다**" → 본 태스크 완료 시 사실과 어긋난다(사용 지시가 아니라 **사건 기록**이라 정정 대상은 아니다).
- `.opal/brain/pages/entity/state-tool.md:43` — 도구 분기 설명. 전환 후에도 사실이다.

> 권고: 완료기준 (0) 괄호를 "(도구 자신의 에러 메시지·분기 설명, `.opal/brain/**` 서술·이력 문서 제외)"로 1줄 보강하면 문언과 검증 스코프가 정확히 일치한다. iteration 1 gap과 **같은 species의 축소판**이므로 남겨두지 말 것을 권한다.

### (3) [P3] PM Gate 자기점검 문구가 갱신되지 않았다

`TEST-SCENARIO.md:402` "H-1~H-17 전건, S-1~S-17 전건" · `:408` "TASK.md R-1~R-8 전체" — 둘 다 H-18 · S-18 · R-9 추가 이전 문장이다. 본문(§1·§3·§4)과 정규화 페이로드는 18/18/9로 정합하므로 매핑 자체는 문제없다(②③④ exit 0로 확인됨). 자기 점검 체크박스만 실제 커버 범위를 과소 기술한다.

> 권고: `H-1~H-18` · `S-1~S-18` · `R-1~R-9`로 정정.

## 변경이력

| 버전 | 날짜 | 변경내용 |
|------|------|---------|
| v1.0 | 2026-08-13 | iteration 2 재판정 — ①2·⑤2·⑥2, average 2.00, verdict pass, gaps 0건. ⑤축 1→2(iteration 1 gap 실질 해소: 실측 4지점 집합 = R-9 열거 집합, P4-b 실행 술어 확보, S-18 역방향 유효성 확인). 신규 관찰 3건 기록(감점 아님) |
