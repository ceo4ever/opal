# SCENARIO GATE 2 — 목표-커버 루브릭 판단축 채점 (①⑤⑥)

> 실행 일시: 2026-08-02T15:52:00+0900 | phase: `scenario-rubric` | iteration: 2
> 채점자: opal-evaluator-agent (verdict-only, readonly)
> scenario_source: `tasks/081-260802-opds-워커중단-복구프로토콜/TEST-SCENARIO.md` (S-1~S-19, H-1~H-14)
> 규칙 SSOT: `opal/core/references/harness/scenario-gate.md` §2(6축)·§5-1(종료조건 임계)
> 판정 범위: ①목표달성 · ⑤채택/잔존 · ⑥경계/부정 **3축만**. ②③④는 test-tool 결정론 판정 완료분(exit 0 / all_covered).
> 채점 방식: Producer 자기보고 불신 — 변경점 5건을 저장소 실물(`opal-pm.md`, `opal-harness.md`, `~/.opal/AGENT.md`, `PLAN.md` §4.1)과 대조 확인함.

## 1. 판단축별 판정

| 축 | 1회차 | 2회차 | 통과선 | 판정 |
|----|-------|-------|--------|------|
| ① 목표 달성 | 1 | **2** | ≥1 | 충족 — 블라인드 재현(L2) + 의미 왜곡 판정(L3) 2계층으로 목표를 직접 검증 |
| ⑤ 채택/잔존 | 1 | **2** | ≥1 | 충족 — 잔존0 기존분 + 로드→프롬프트 도달→실사용 준수 채택 3단 검증 신설 |
| ⑥ 경계/부정 | 2 | **2** | ≥1 | 충족 유지 — S-19로 off-by-one 잔여 빈틈까지 해소 |
| **평균** | 1.33 | **2.00** | ≥1.5 | **충족** |

---

## 2. ① 목표 달성 — 2점 (1 → 2)

**목표(TASK.md:9)**: "다음 PM이 규칙만으로 동일 대응을 재현할 수 있게 한다."

### G-1 해소 확인 (실물 대조)

`TEST-SCENARIO.md:307-309`를 원문 확인했다. 1회차 감점 3항이 모두 문언으로 반전됐다.

| 1회차 감점 사유 | 2회차 상태 | 근거 |
|---|---|---|
| 검증 대상이 "탐색 가능성"이었다 | "재현"으로 교체 — 상황 입력(600초 스톨)만 주고 **산출 대응**을 R-1~R-3과 대조 | `:309` |
| 정답 노출 + 저자 자가수행 | **정답 미고지** 명문화 — TASK·PLAN·TEST-SCENARIO 미제공, 프롬프트에 `1회`·`3단계`·`3개` 수치 포함 금지 | `:308` |
| "M2 자동화 불가" 단정 | **M2로 승격** — 신규 컨텍스트 워커 디스패치 | `:307` |

- **Fail 조건이 명확하다**: "3요소 중 1개라도 누락되거나 다른 값을 도출하면 Fail"(`:309`) — "확인한다"류 자의적 종결이 아니다. 실패 시 역추적 절차("어느 문서에서 경로가 끊겼는지 워커 응답으로")까지 규정.
- **캡틴 확인이 S-15(L3)로 정상 분리**됐다(`:349-368`). S-15는 자동 대조가 못 잡는 **의미 왜곡**만 담당하며, Fail 예시 3종("1회"를 재개 총 횟수 아닌 의미로 해석 / 실측 판정을 워커 자기보고로 대체 / 분할 기준을 입력 파일 수로 오해)을 구체 열거해 판정 자의성을 줄였다(`:358`). PM 요청 양식도 "080에서 캡틴이 실제로 했던 대응과 실질 동등한가"라는 대조 기준을 제시(`:366`).
- 앵커 "2: 사용자·운영 계층에서 목표를 직접 검증" 충족 — S-13(L2 운영) + S-15(L3 사용자).

### 잔여 지적 (감점 없음)

1. **워커에게 대상 문서 3종 경로를 미리 준다**(`:305`). 실제 다음 PM은 "어느 문서에 있는지"부터 모른다 — 탐색 단계가 면제되므로 재현 난이도가 실사보다 낮다. 다만 목표문 자체가 "규칙**만으로**"이므로 규칙 제공은 전제에 해당하고, 규칙 도달 여부는 S-16이 별도 커버한다 → 2점 유지.
2. **실제 프롬프트 문안이 시나리오에 고정돼 있지 않다** (`실행 명령: {PM이 Step 8에서 채움}`). 블라인드성은 정답을 아는 PM이 나중에 쓰는 프롬프트에 달려 있다. `:308`의 금칙 수치 3개가 사후 감사 가능한 형태로 명시돼 있어 실효 통제가 있지만, 실행 시 프롬프트 원문을 결과란에 보존해야 검증이 성립한다.

---

## 3. ⑤ 채택/잔존 — 2점 (1 → 2)

**교체형 성격**: "PM 세션 프롬프트에만 존재하던 완화책 → 하네스 SSOT 이관".

### 잔존0 측 (1회차 유지, 충분)

S-8(수치 리터럴 전역 유일성) / S-6(규율 원문 복제 0건) / S-4(§7.4 추가분 1줄·숫자 부재) / S-11(candidate 0건 · promoted 2건). 이중 소스 잔존 검증은 1회차부터 충족 상태였다.

### 채택 측 — G-2·G-3·G-4로 신설된 3단 체인

1회차 지적의 본질은 "등재·배포는 설치이지 채택이 아니다"였다. 2회차는 **로드 → 프롬프트 도달 → 실사용 준수**의 전 구간에 시나리오를 배치했다.

| 단계 | 시나리오 | Fail 조건 | 실물 검증 결과 |
|---|---|---|---|
| ① 로드되는가 | **S-16** (H-12, 신설) | 두 로드 조건이 같으면 **Fail + PLAN M-1 재검토**(`:232`) | 전제 사실 확인됨 — `opal-pm.md:54` "워커에게 작업을 디스패치하기 전에 **매번**" + Step 0~7 전 단계 열거, `:57` "Lazy 트리거: 워커 디스패치 직전"(무조건). 대비군 `opal-harness.md:102` "병렬 디스패치 시"(조건부). 검사 가능한 실질 명제다 |
| ② 프롬프트에 닿는가 | **S-17** (H-13, 신설) | "1건이라도 누락되면 Fail — REQ-4 설계를 재검토"(`:326`) | 081 자신의 EXECUTE 프롬프트 원문을 `AGENTIC-LOG.md`에 보존 후 대조하도록 절차 규정 |
| ③ 실제로 지키는가 | **S-18** (H-14, 신설) | "계획(PLAN §4.1)과 실제 실행이 일치 — 불일치 시 Fail"(`:341`) | PLAN.md:663-667 실물 대조 — Phase1=2 / Phase2=1 / Phase3=2 수치가 정확. S-18의 기대치가 계획서와 일치하며 날조 아님 |

- **G-2가 실질 해소됐다.** 1회차 최대 결함("앵커 이동이 헛수고여도 전 시나리오 Pass")이 제거됐다 — S-16은 081이 변경하지 않는 문서를 검사해 앵커 이동의 **전제 자체를 반증 가능하게** 만들었고, 반증 시 PLAN 재설계로 되돌리는 경로까지 규정했다. 시나리오 개수 늘리기가 아닌 검증력 증가다.
- **G-3·G-4는 dogfooding 실증**으로 구현됐다. 교체 대상이 "프롬프트에만 있던 규율"이므로 프롬프트 도달 관측이 교체 성립의 직접 증거다. S-17 ②가 "PM의 즉흥 작문이 아니라 템플릿 인용임"까지 요구해 표층 통과를 막으려 한 점도 인정한다.
- H-12~H-14 신설분이 §1 가설 표(`:21-23`)와 §2.2 데이터 흐름(`:64-66`), §4 매핑 표에 모두 등재돼 결정론 축(②③④)의 매핑 완전성을 깨지 않았다.

### 잔여 지적 (감점 없음, 실행 전 정정 권고)

1. **S-16 ②의 앵커가 틀렸다.** `:228`·`:232`가 "`~/.opal/AGENT.md` Lazy 트리거 테이블"에서 `parallel-execution.md`의 조건부를 확인하라고 지시하는데, 실측 결과 `~/.opal/AGENT.md`에는 `parallel-execution` 문자열이 **0건**이다(grep 확인). 해당 조건부 항목은 `opal/core/references/opal-harness.md:102`에 있다. 그대로 실행하면 grep 미스가 나고 ③ "두 조건이 다르다" 판정이 자의적이 된다 — 대상을 `opal-harness.md` §7 Lazy 표로 정정하면 그대로 성립한다.
2. **S-17 ②는 Phase 1에 대해 성립할 수 없다.** PLAN.md:664-666상 Phase 1(Step 1·2) 디스패치는 Phase 2(Step 3, 주입 템플릿 신설)보다 **선행**한다. 즉 Step 1·2 프롬프트 발행 시점에는 인용 대상 템플릿이 아직 존재하지 않으므로 "템플릿 인용임이 확인"이 물리적으로 불가하다. Step 1·2는 PLAN §3.3.2 문안 기준으로, Step 4·5는 실 템플릿 기준으로 대조하도록 시점을 나눠 적어야 판정이 결정론적이 된다.
3. **S-18 ①은 구조상 거의 자동 통과**다(계획치가 이미 2/1/2로 상한 이하). 실질 검증력은 ③(계획-실행 일치)에 있으므로, 실행 시 "계획대로 갔는가"가 아니라 **디스패치 로그의 실측 산출 파일 수**를 세는 형태여야 한다.

---

## 4. ⑥ 경계/부정 — 2점 (유지)

- 1회차 근거(부정 7건 · 파괴 경계 4건 · 도구 실패 경계 2건)는 그대로 유지된다.
- **G-5가 S-19로 해소**됐다(`:238-251`). 1회차에 "비차단 빈틈"으로 남겼던 off-by-one을 ①"3개 초과 = 3개 분할 불요 / 4개 분할 의무" 단일 해석 ②"3개 이상"·"약 3개" 모호 표현 부재 ③**계수 규칙**("같은 경로는 1개로 계수") 명시 3항으로 검사한다. ③은 1회차 gaps에 없던 항목으로, 동일 파일 중복 계수 모호성까지 스스로 확장한 것 — 지시 이행을 넘어선 보강으로 인정한다.
- 2점 상한이므로 추가 가점은 없다.

---

## 5. 종합 판정

```json
{
  "scores": { "goal": 2, "adoption": 2, "boundary": 2 },
  "average": 2.0,
  "gaps": [],
  "verdict": "pass"
}
```

- 0점 축 없음 → §5-1 첫 조건 충족.
- 평균 2.00 ≥ 1.5 → **수렴(PASS)**. 게이트 루프를 종료하고 EXECUTE로 진행 가능.
- G-1~G-5 전건 해소. 시나리오 개수만 늘린 형태가 아니라, 1회차에 "전 시나리오 Pass 상태로 통과하던 실패"(앵커 이동 무효·프롬프트 미도달·자기적용 미준수)가 각각 반증 가능한 Fail 조건을 갖게 됐다.

## 6. 비차단 권고 (게이트 통과에 영향 없음 — EXECUTE 전 정정 권고)

| # | 대상 | 내용 |
|---|------|------|
| N-1 | S-16 `:228`,`:232` | 대상 앵커 `~/.opal/AGENT.md` → `opal/core/references/opal-harness.md` §7 Lazy 표(`:102`)로 정정. 현 앵커에는 `parallel-execution` 항목이 실재하지 않음 |
| N-2 | S-17 `:326` | ② 인용 판정 기준을 Phase별로 분리 — Step 1·2(템플릿 신설 이전)는 PLAN §3.3.2 문안, Step 4·5는 실 템플릿 대조 |
| N-3 | S-13 `:311` | 실행 시 프롬프트 원문을 결과란에 보존 — 금칙 수치 3개 미포함이 사후 감사 가능해야 블라인드성이 증명된다 |
| N-4 | §7 PM Gate 체크 | stale 3건 — "H-1~H-11"(→H-14), "[SUPERVISOR] 마커 … (S-13)"(→S-15), "목표달성 시나리오 S-13(L3)"(→S-13은 L2, L3는 S-15). 판정에는 무영향이나 자기기술 불일치 |

> 본 보고서는 verdict만 반환한다. TEST-SCENARIO.md·PLAN.md·TASK.md는 수정하지 않았다(readonly 준수). N-1~N-4 반영 여부는 PM 판단이다.
