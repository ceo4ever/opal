# PLAN: status 문서 슬라이스 — out/status.md 생성

> 작성일: 2026-07-17 | 입력: TASK.md(디스패치 프롬프트), CONTRACT.md(참조 전용)
> 모드: Flat (단일 기능)

## 1. 태스크 개요 + 기능 리스트업

### 1.1 요약
`samples/T01-정상슬라이스/out/status.md` 마크다운 파일을 신규 생성한다. H1 제목 1개 + 본문 2줄 이상을 포함해야 하며, 이는 067 stream 채널·journal 실생성 실증(TEST S-14 fixture)을 위한 최소 산출물이다. 파일 시스템 쓰기 1건 외 다른 부수효과는 없다.

### 1.2 기능 목록
| F-ID | 기능명 | 포함 요구사항 | 우선순위 | 의존 |
|------|--------|-------------|---------|------|
| F-001 | status 문서 슬라이스 생성 | AC-1, AC-2 | P0 | 없음 |

### 1.3 기능 의존 그래프 (ASCII)
Flat 모드 — 단일 기능이므로 생략.

---

## 리스크 가설 표

| ID | 변경 단위 | 깨질 수 있는 계약 | 운영 영향 | 검증 계층 권고 | 시나리오 후보 |
|----|----------|----------------|---------|------------|------------|
| H-1 | out/status.md 파일 생성 | 파일 부재(경로 오타/디렉토리 미생성) → MV-1 실패 | P1 | L1(파일 존재) | TS-1 |
| H-2 | status.md 내용 포맷 | H1(`# `) 누락 → MV-2(`grep -c '^# '`) = 0 → 실패 | P1 | L1(내용 grep) | TS-2 |
| H-3 | 쓰기 경계 위반 | `samples/T01-정상슬라이스/` 밖 파일 생성 → 계약 RB-1 경계 위반 | P0 | L1(경로 검증) | TS-3 |

---

## 2. 기능별 분석 (Flat)

### 2.1 관련 파일 맵
| 영역 | 경로 | 역할 | 변경 유형 |
|------|------|------|----------|
| 공통 | `samples/T01-정상슬라이스/out/status.md` | 실증 산출물 (마크다운) | 신규 |
| 공통 | `samples/T01-정상슬라이스/out/` | 산출물 디렉토리 | 신규 (없으면 생성) |

### 2.2 현재 구현
현재 `out/` 디렉토리 및 `status.md` 파일 모두 부재. 신규 생성 대상.

### 2.3 영향 범위
- 호출자/피호출자 없음 (독립 파일 산출).
- 067 TEST S-14 fixture가 이 산출물을 소비 (stream 채널·journal 실생성 실증).
- 공유 상태 없음. 회귀 리스크 없음.

---

## 3. 기능별 설계 (Flat)

### 3.1 파일 변경 계획

**신규 생성**
| # | 경로 | 영역 | 역할 | 근거 |
|---|------|------|------|------|
| 1 | `out/status.md` | 공통 | H1 제목 1개 + 본문 2줄 이상 마크다운 | CONTRACT.md §1 (산출물·형식) |

**수정**
| # | 경로 | 영역 | 변경 내용 요약 | 근거 |
|---|------|------|--------------|------|
| — | 없음 | — | — | — |

### 3.2 산출물 계약 3필드 (CONTRACT.md RB-1 대응)

> [MUST] `CONTRACT.md §1`: 아래 3필드를 계약대로 준수한다. CONTRACT.md·STATE.md 직접 수정 금지.

| 필드 | 값 |
|------|-----|
| **산출물 경로** | `tasks/067-260717-opd-루프액션-스트림-모니터링/samples/T01-정상슬라이스/out/status.md` (task_folder 기준 `out/status.md`) |
| **형식** | 마크다운 — H1 제목(`# `) 정확히 1개 + 본문 2줄 이상 |
| **경계** | `samples/T01-정상슬라이스/` 폴더 밖 파일 생성/수정 금지 |

### 3.3 status.md 콘텐츠 설계
- 1행: H1 제목 (`# ` 접두, 1개만) — 예: `# T01 정상 슬라이스 status`
- 2행 이후: 본문 2줄 이상 (실증 목적·생성 맥락 기술)

> 근거: CONTRACT.md §1 형식, MV-2(`grep -c '^# '` ≥1)

### 3.4 환경 변경
해당 없음.

### 3.5 배치/마이그레이션
해당 없음.

### 3.6 테스트 시나리오 (AC ↔ TS 매핑) — CONTRACT.md RB-2 대응
| TS-ID | AC 매핑 | MV 매핑 | 유형 | 명령 | 기대 결과 |
|-------|---------|---------|------|------|----------|
| TS-1 | AC-1 (파일 존재) | MV-1 | L1 | `test -f out/status.md` | exit 0 |
| TS-2 | AC-2 (H1 존재) | MV-2 | L1 | `grep -c '^# ' out/status.md` | ≥1 |
| TS-3 | 경계 (RB-1) | — | L1 | 생성 파일 경로가 `samples/T01-정상슬라이스/` 하위인지 확인 | 위반 0건 |

> MV-1·MV-2 전부 커버 (RB-2 pass 조건 충족).

---

## 4. 통합 실행 계획

### 4.1 Phase 그룹핑
| Phase | 기능 | Step | 실행 | 비고 |
|-------|------|------|------|------|
| P1 | F-001 | Step 1 | 순차 | 단일 파일 생성 |

### 4.2 실행 체크리스트
> 총 1개 Step | Phase 1개 | 실행 모드: 단순

#### Step 1: out/status.md 생성
- [x] 완료
- **소속 기능**: F-001
- **영역**: 공통
- **agent**: opal-task-agent (범용, 단일 파일 쓰기)
- **파일**: `samples/T01-정상슬라이스/out/status.md`
- **작업 내용**: `out/` 디렉토리 확보 후, H1 제목 1개 + 본문 2줄 이상 마크다운을 작성. 경로가 task_scope(`samples/T01-정상슬라이스/`) 하위임을 준수.
- **완료 기준**: `test -f out/status.md` exit 0 AND `grep -c '^# ' out/status.md` ≥1
- **테스트**: TS-1, TS-2, TS-3
- **실행 방법**: direct (Write 1건)
- **의존**: 없음

### 4.3 병렬/순차 판별 근거
| 관계 | 근거 |
|------|------|
| 단일 Step | 파일 1건 생성 — 병렬화 대상 없음 |

---

## 5. QA 체크리스트 (기능-QA 매트릭스)

### 5.1 기능별 QA
| F-ID | QA 항목 | TS-ID | Pass 조건 |
|------|---------|-------|----------|
| F-001 | 파일 존재 | TS-1 | `test -f` exit 0 |
| F-001 | H1 포맷 | TS-2 | `grep -c '^# '` ≥1 |
| F-001 | 본문 2줄 이상 | TS-2 보강 | H1 제외 본문 라인 ≥2 |
| F-001 | 쓰기 경계 준수 | TS-3 | task_scope 밖 파일 0건 |

### 5.2 회귀 테스트
- [x] 기존 파일 미변경 (out/status.md 외 생성/수정 없음)

### 5.3 코드/문서 품질
- [x] H1이 정확히 1개인지 확인 (초과 금지)
- [x] 본문이 최소 2줄 이상

### 5.4 보안
- [x] task_scope 밖 경로 쓰기 없음 (경계 위반 = 비가역/계약 위반 리스크)
- [x] CONTRACT.md·STATE.md 미수정

---

## 6. 복잡도 판별
| 기준 | 값 | 판정 |
|------|---|------|
| Step 수 | 1개 | 단순 |
| 변경 파일 수 | 1개 | 단순 |
| 모듈 범위 | 단일 | 단순 |
| 작업 유형 | 파일 생성 | 단순 |
| 외부 의존성 | 없음 | 단순 |
| **실행 모드** | **단순** | |

---

## 7. 실행 아키텍처
단순 모드 — 해당 없음 (Step 8 스킵).

---

## 8. 기술 컨텍스트

### 8.1 기술 스택
| 영역 | 기술 | 적용 스킬 |
|------|------|----------|
| 공통 | 마크다운 (텍스트 파일) | 없음 |

### 8.2 사용 MCP
| MCP | 조회 결과 요약 |
|-----|--------------|
| 없음 | 라이브러리 의존 없음 |

### 8.3 참조 문서 (설계 결정 근거)
| # | 유형 | 문서/사이트 | 경로/URL | 참조 이유 |
|---|------|-----------|---------|----------|
| D-1 | 설계 | CONTRACT.md | `<task_folder>/CONTRACT.md` | 산출물 경로·형식·경계·기계검증절(MV-1/MV-2)·루브릭(RB-1/RB-2) 원천 |

> docs/CONVENTIONS.md: 본 산출물은 프레임워크 코드가 아닌 실증용 마크다운 fixture로, 코드 예시·설계 결정에 영향을 주는 [MUST]/네이밍 규칙 해당 없음 — 인용 스킵.

---

## 9. 리스크 및 대응 (기능-리스크 연결)
| # | 리스크 | 관련 F | 영향 | 대응 |
|---|--------|--------|------|------|
| R-1 | out/ 디렉토리 부재로 쓰기 실패 | F-001 | P1 | Write가 상위 디렉토리 자동 생성 확인, 실패 시 mkdir 후 재시도 |
| R-2 | H1 누락 → MV-2 실패 | F-001 | P1 | 1행을 `# ` 접두 제목으로 고정 |
| R-3 | task_scope 밖 경로 생성 | F-001 | P0 | 절대경로가 `samples/T01-정상슬라이스/` 하위인지 생성 전 확인 |
