# GC CONVENTION REPORT — 2026-08-04T22-02-37

<!--
  체크박스 5단계 상태 기호 (APPLY 단계가 기입):
  [ ]  open    — 미처리 (신규)
  [x]  done    — 적용 완료  ← 주석: 적용 시각 YYYY-MM-DD HH:mm + 수정 요약
  [~]  pending — 보류       ← 주석: 보류 사유
  [?]  review  — 확인 필요  ← 주석: 판단 근거 / 해결 방안
  [!]  failed  — 실패       ← 주석: 실패 사유 / 권장 대안
-->

## 1. 헤더

- 실행 일시: 시작 2026-08-04 22:02:37 / 완료 2026-08-04 22:07:50 / 소요 약 5분 13초
- 범위: `all` (Framework 영역 — Markdown/YAML/Bash/Node.js) / 대상 파일 31개 (git 실측 changed_files, 태스크 083)
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` 존재 — 허브+링크 모델 적용 대상이나 해당 문서 §참고에 "OPAL 프레임워크 자체는 단일 `docs/CONVENTIONS.md`를 사용한다(단일 진입점)"로 명시되어 있어 상세 링크 문서 없음 — 허브 전체를 그대로 적용
- APPLY 수행 여부: N (본 에이전트는 진단 전담 — 소스 미수정)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 2 |
| 심각도 분포 | Critical 0 / High 0 / Medium 0 / Low 1 / Info 1 |
| 자동 수정 가능 | 0 |
| 수동 조치 필요 | 2 |
| 파일별 상위 Top 5 | opal/core/references/opal-harness.md (1건) / opal/tools/code-scan/tests/test-discover.js 외 10개 파일 (중복 패턴 1건, 대표 파일 기준 집계) |
| 카테고리별 빈도 | 문서화(변경이력 형식) 1개 파일 / 코드 품질(테스트 헬퍼 중복, 추가 제안) 11개 파일 |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 1 (빈도 1건 + 새 카테고리 0건) |

**PM 지정 확인 항목 6종 — 결과 요약**

| # | 확인 항목 | 결과 |
|---|----------|------|
| 1 | 변경이력 기재 의무(5~6종 참조 문서) | 6개 문서(`docs/PROJECT.md`, `docs/ARCHITECTURE.md`, `opal/core/references/tools.md`, `.../harness/header-rules.md`, `.../opal-harness.md`, `.../pm/code-scan-management.md`) 전부에 083 행 존재 — 형식 위반 1건(Low, 아래 GC-C001) |
| 2 | 배포 경계(`~/.opal/` 직접 편집 금지) | 위반 없음 — 전 변경이 프로젝트 소스(`opal/`, `scripts/`, `docs/`)에 한정됨 (git status 실측) |
| 3 | 하드코딩된 플랫폼 분기 금지 | 위반 없음 — `install-mac.sh` 변경은 SEED_KEYS 루프 일반화이며 신규 Claude/Cursor/Gemini 분기 없음 |
| 4 | 네이밍 일관성(신규 함수·에러 코드·CLI 플래그) | CONVENTIONS.md에 JS 함수/CLI 플래그 네이밍 규칙 자체가 없어 위반 판정 불가 — 관찰: 기존 스타일(camelCase 함수·snake_case 에러코드·kebab CLI 플래그)과 일관됨 |
| 5 | `@header` 메타블록 갱신 | 위반 없음 — `code-scan.js`(description/note 갱신) 및 신규/수정 테스트 파일(11종) 전부 @header 보유·갱신 확인 |
| 6 | 폐기된 `manifestMaxBytes(ctx)` 헬퍼 잔여 참조 | 위반 없음 — 소스 전량 grep 결과 함수 정의·호출 모두 제거됨. 테스트 파일 내 2건 언급은 RED-first 문서 주석(구 동작을 설명하며 삭제를 검증하는 문구)으로 정상 |

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

### High (0건)

### Medium (0건)

### Low (1건)

- [ ] GC-C001 [`opal/core/references/opal-harness.md:330`] 변경이력 v6.9(083) 행에 시각(HH:mm) 누락
  - 카테고리: 문서화
  - 위반 기준: 프로젝트(CONVENTIONS.md §변경이력): "일시 형식: `YYYY-MM-DD HH:mm` (KST 기준)"
  - 설명: `| v6.9 | 2026-08-04 | §9 등록 도구 표 code-scan 행 현행화 — ... (083) |` 행의 일시 컬럼이 날짜만 기재되어 있고 시각이 없다. 동일 표의 직전 행들 — `v6.7 | 2026-07-28 23:28 | ... (077)`, `v6.8 | 2026-08-02 16:03 | ... (081)` — 은 시각을 포함하고 있어, 083 행만 형식이 이탈했다(같은 표 안에서도 v6.2/v6.6처럼 과거에 시각 누락 선례가 있어 완전히 고립된 것은 아니나, CONVENTIONS.md가 명시한 형식과는 어긋난다).
  - 해결 방안: 실제 작성 시각(KST)을 확인해 `2026-08-04 HH:mm` 형식으로 보정
  - 자동 수정: N (실작성 시각 확인 필요 — 임의 시각 기입 금지)
  - 참조: `docs/CONVENTIONS.md` §변경이력

### Info (1건)

- [ ] GC-C002 [`opal/tools/code-scan/tests/test-discover.js` 외 10개 파일] 테스트 홈 격리 상수(`HOME_ABSENT`) + 안내 주석이 파일마다 거의 동일하게 중복
  - 대상 파일(11): `test-discover.js`, `test-feature.js`, `test-header-source.js`, `test-hook.js`, `test-resolve-header.js`, `test-scaffold.js`, `test-scope-filter.js`, `test-target.js`, `test-validate.js`, `test-regression.js`, `test-shard.js`
  - 카테고리: 코드 품질 (getsentry/code-review 보조 참조 — CONVENTIONS.md에 근거 규칙 없어 **추가 제안**으로만 표시, 위반 아님)
  - 설명: `const HOME_ABSENT = path.join(FIX, 'shard-policy', 'homes', 'absent');` 선언과 "083부터 code-scan이 ~/.opal/setting.json의 shardPolicy를 읽는다 — OPAL_HOME을 주입하지 않으면…" 안내 주석이 11개 테스트 파일에 거의 동일한 문구로 반복된다. 기능적으로는 문제가 없으나(각 파일은 독립 실행 가능한 CLI 블랙박스 테스트라는 이 스위트의 설계 원칙과도 부합), 문구·경로를 한 곳에서만 바꿀 수 없어 향후 `homes/absent` 경로가 바뀌면 11곳을 동시에 고쳐야 한다.
  - 해결 방안(제안): `tests/helpers/`(신규) 같은 공용 모듈에 `HOME_ABSENT`와 `run()`/`runHook()`의 `OPAL_HOME` 주입 패턴을 1회 정의하고 각 테스트 파일이 import하는 구조를 검토. 다만 이 스위트가 "mock 금지·실 subprocess만 사용"(각 파일 상단 명시)이라는 격리 원칙을 갖고 있으므로, 공용화가 그 원칙과 상충하지 않는지 오너 판단이 필요하다 — 자동 적용 대상 아님
  - 자동 수정: N
  - 참조: TBD — 프로젝트에 테스트 헬퍼 공용화 규칙이 없음. 참고로 getsentry/code-review의 일반 중복 최소화 가이드 수준

---

## 4. 문서 업데이트 제안 (트리거 발동 시만)

- [ ] GC-DP-C001 [빈도 트리거] "테스트 하네스 `OPAL_HOME` 격리 상수/주석 중복" (11개 파일) → `docs/CONVENTIONS.md`에 테스트 헬퍼 공용화 규칙 신설 검토 제안
  - 근거: 단일 실행(태스크 083) 내 11개 파일 — 빈도 임계값 N=3 초과
  - 제안 내용: "code-scan 테스트 스위트처럼 각 테스트 파일이 CLI를 블랙박스로 spawn하는 구조에서, 환경 격리용 상수·헬퍼 함수가 3개 파일 이상에서 동일 문구로 반복되면 `tests/helpers/`(또는 동등 위치) 공용 모듈로 추출한다. 단, '파일별 독립 실행·mock 금지' 원칙과 충돌하지 않는 범위로 한정한다." — 다만 이 제안은 GC-C002가 위반이 아닌 추가 제안이라는 점과 마찬가지로, CONVENTIONS.md에 새 규칙을 넣을지 여부는 소유자 판단 사항이며 본 에이전트가 자동 반영하지 않는다.

---

## 5. 문서 작성 유도 (해당 시)

`docs/CONVENTIONS.md` 존재 — 작성 유도 생략.

---

## 부록 — 확인 범위 (git 실측 changed_files, 31개)

**구현 소스**
- `opal/tools/code-scan/code-scan.js` (v1.5.0 → v1.6.0, `git diff` 1,476줄)

**설정/배포**
- `scripts/install-mac.sh`, `opal/core/setting.default.json`

**테스트**
- `opal/tools/code-scan/tests/test-shard-policy.js` (신규)
- `opal/tools/code-scan/tests/test-{discover,feature,header-source,hook,regression,resolve-header,scaffold,scope-filter,shard,target,validate}.js` (수정 11종)
- `opal/tools/code-scan/tests/fixtures/shard-policy/**` (신규 픽스처 트리 24파일)
- `opal/tools/code-scan/tests/fixtures/shard-{goal,violations}/**/.opal/{code-map/index.json,code-scan.json}` (수정 8종)

**문서**
- `docs/PROJECT.md`, `docs/ARCHITECTURE.md`
- `opal/core/references/tools.md`
- `opal/core/references/harness/header-rules.md`
- `opal/core/references/opal-harness.md`
- `opal/core/references/pm/code-scan-management.md`

**기타(허브 소스 범위 밖, 참고만)**: `.opal/MEMORY.json` — 런타임 브레인 인덱스 데이터로 코드/문서 컨벤션 체크 대상 아님(체크 생략).
