# DONE: 워커 중단 복구 프로토콜 + 디스패치 산출량 상한 + 증분 저장 규율 SSOT화

> 완료일: 2026-08-02 18:04 | 적용 스킬: opds | 모드: semi-agentic | 태스크: 081

## 1. 무엇을 했나

078·080 두 태스크에서 반복된 워커 인프라 실패(스톨·연결 종료)의 완화책이 **PM 세션 프롬프트에만 존재하고 어느 SSOT에도 없었다**. 080에서 재적용이 누락되자 같은 실패가 5회 재발했다. 이 완화 조합을 하네스 SSOT 3곳에 규칙으로 등재하여, 다음 PM이 문서만으로 동일 대응을 재현할 수 있게 했다.

핵심은 "새 규칙 발명"이 아니라 **"이미 실증된 대응의 SSOT 승격"**이다 — 078에서 도출(실패 3연속 → 대응 후 전량 성공), 079에서 재현(선제 적용 → 실패 0건)된 조합이다.

## 2. 변경 내역

| # | 파일 | 변경 | 버전 |
|---|------|------|------|
| 1 | `opal/core/references/opal-harness.md` | §1 자동 루핑 제약 표에 "워커 프로세스 비정상 종료" 행 + 보충 note — 재시도 1회(동일 컨텍스트 재개), 초과 시 새 컨텍스트 분할 재배치 | v6.8 |
| 2 | `opal/core/references/harness/pm-review-gate.md` | `### 워커 중단 시 산출물 실측 판정` 절 신설 — ①git 실측 ②PLAN 체크리스트 대조 ③잔여만 재배치([MUST] 완료분 덮어쓰기 금지). 역할 라인 갱신 | v1.9 |
| 3 | `opal/core/references/pm/dispatch-process.md` | Step 6에 항목 5 "산출량 상한"(3개 초과 분할·동일 파일은 묶어 순차 편집) + 워커 컨텍스트 주입 템플릿에 전 워커 공통 고정 2항목(증분 저장·입력 축소) + 하단 note | v1.6 |
| 4 | `opal/core/references/harness/parallel-execution.md` | §7.4에 "산출량 상한(참조)" 1줄 + `## 변경이력` 절 신설 | v1.1 |
| 5 | `opal/skills/op-dev-execute/SKILL.md` | Step 4 → "체크리스트 갱신 및 증분 저장" 확장, SSOT 참조 연결(문언 복제 0건) | v2.4 |
| 6 | `.opal/MEMORY.json` | 개선 후보 2건 `candidate` → `promoted`, history 081 행 append | - |

## 3. 설계 핵심 — 규칙 1소유자 매핑

Governance(하위 문서는 재서술하지 않고 참조) 집행을 위해 **각 수치·절차에 단일 소유 문서**를 지정했다.

| 규칙 요소 | 유일 소유 문서 | 타 문서 |
|----------|--------------|--------|
| 재시도 상한 = 1회 | `opal-harness.md` §1 | 참조만 |
| 실측 판정 3단계 | `harness/pm-review-gate.md` | 참조만 |
| 산출 파일 상한 = 3개 (관측 기반 잠정치) | `pm/dispatch-process.md` Step 6 | 참조만 |
| 증분 저장·입력 축소 규율 원문 | `pm/dispatch-process.md` 주입 템플릿 | 참조만 |

검증: 각 리터럴이 정확히 1개 파일에서만 발견됨(S-8).

## 4. 앵커 교정 — 초안 대비 가장 큰 변경

초안은 산출량 상한을 `parallel-execution.md` §7.4에 두려 했다. 그러나 이 문서의 로드 조건은 **"병렬 디스패치 시" 조건부**이고, 080에서 실패한 Step 2는 **단일 디스패치**였다 — 즉 그 자리에 규칙을 두면 정작 필요한 순간에 로드되지 않는다.

SSOT를 **무조건 로드되는** `pm/dispatch-process.md` Step 6로 옮기고, §7.4에는 참조 1줄만 남겼다. 이 전제 자체를 반증 가능하게 만들기 위해 S-16(두 문서 로드 조건이 실제로 다른지 검사, 같으면 Fail → 설계 재검토)을 시나리오에 넣었다.

## 5. 검증 결과

| 항목 | 결과 |
|------|------|
| 시나리오 | 19건 중 **18 Pass / 0 Fail** (S-14는 관측 전용·Fail 비대상) |
| 목표-커버 게이트 | 1회차 fail(1.33) → 재작성 → **2회차 pass(2.00)** — coverage-check exit 0 + evaluator verdict pass |
| 회귀 | `test-regression.js` **36/36 pass** |
| 참조 무결성 | 신규 상호 참조 **6/6 OK**, dangling 0건 |
| 수치 유일성 | 3/3 (각 1파일 단독 소유) |
| 배포 일치 | `~/.opal/` 배포본 strip 기준 **5/5 일치** (PM·test-agent 독립 2회 확인) |
| 보안 | 시크릿·실경로 노출 0건 |

### 목표달성 검증 — 블라인드 재현 테스트 (S-13)

태스크 문서 접근을 차단하고 프롬프트에서 수치를 제거한 상태로, 신규 컨텍스트 워커에게 "워커가 600초 스톨했다" 상황만 제시했다. 워커는 배포된 규칙 문서 3종만 읽고 **3요소를 자력으로 도출**했다 — 재시도 1회·git 실측 3단계(Write 금지·Edit 지시까지)·3개 초과 분할, 근거 문서 경로와 행번호 인용 포함. 캡틴이 실무 타당성을 판정하여 S-15도 Pass 확정.

## 6. 자기적용 — 규칙이 만들어진 당일 3회 사용됨

| # | 상황 | 적용한 규칙 |
|---|------|-----------|
| 1 | `install-mac.sh`가 10분 타임아웃(exit 143)으로 중단 | **실측 판정 3단계** — 배포본 5/5 일치 확인 → 완료 판정 → 재실행 안 함(완료분 덮어쓰기 회피) |
| 2 | `dispatch-process.md`를 REQ-3·REQ-4가 동시 수정 | **동일 파일은 분할 금지·같은 디스패치 순차 편집** — 덮어쓰기 0건 |
| 3 | 전 워커 디스패치(6회) | **증분 저장·입력 축소 주입** — 중단 0건, 산출물 유실 0건 |

배치별 산출 파일 수 2·1·2개로 신설 상한(3개) 준수(S-18).

## 7. 이번 태스크에서 잡아낸 자기 결함 3건

PM(알투) 자신의 산출물에서 발견되어 정정한 항목이다.

| # | 결함 | 발견자 | 조치 |
|---|------|--------|------|
| 1 | 목표 시나리오가 **정답을 미리 고지하고 작성자 본인이 확인**하는 구조 — 재현성이 아닌 탐색가능성만 검증 | opal-evaluator-agent (게이트 1회차) | 블라인드 재현 테스트로 전면 교체 |
| 2 | S-16 앵커 오기 — `~/.opal/AGENT.md`에 `parallel-execution` 문자열 0건(실제는 `opal-harness.md` §2) | opal-evaluator-agent (게이트 2회차) | 앵커 정정 |
| 3 | 행번호 앵커 드리프트 — 같은 파일 §1을 편집하면서 그 파일 102행을 앵커로 사용 → 105행으로 밀림 | opal-test-agent | 4개소를 §섹션 기준으로 교체 |

**교훈**: 같은 파일을 수정하는 태스크에서 그 파일의 **행번호를 검증 앵커로 쓰면 자기 편집으로 stale해진다**. 값·섹션 기준으로 조회해야 한다.

## 8. 잔여 미해결 / 후속 후보

| # | 항목 | 성격 |
|---|------|------|
| F-1 | 재개 카운터 + 산출량 상한 tool-gating (임계값 확정 후) | 헌법 §Enforce 이행 — 임계값 3은 4~9 구간 미검증 잠정치 |
| F-3 | 모델 하향(opus→sonnet) 효과 검증 | 078 조합 중 인과 미확인분 |
| F-4 | `opal-pm.md:66` "검토 11항목" 열거형 포인터 stale (실제 14항목) | 선행 결함, 본 태스크 비변경 |
| F-5 | `install-mac.sh` 장시간 소요(10분 초과) 원인 규명 | 본 태스크 무관 선행 이슈 |
| F-6 | O1(§7.6) 교차 참조가 조건부 로드 문서를 가리켜 추적 단절 | **블라인드 워커가 발견** — 이번에 고친 것과 동일 유형 |

- `install-mac.sh` 전체 재실행은 **캡틴이 직접 수행**한다(본 태스크 5파일 배포는 반영·검증 완료).

## 9. 산출물

```
tasks/081-260802-opds-워커중단-복구프로토콜/
├── TASK.md               요구사항 REQ-1~REQ-6
├── PLAN.md               F-001~F-004 / 8 Step / 확정 삽입 문언 리터럴
├── TEST-SCENARIO.md      가설 H-1~H-14 / 시나리오 S-1~S-19 / 최종 All Pass
├── SCENARIO-GATE-1.md    게이트 1회차 채점 (fail 1.33)
├── SCENARIO-GATE-2.md    게이트 2회차 채점 (pass 2.00)
├── AGENTIC-LOG.md        PM 판단 기록 / 디스패치 주입 기록 / install 중단 실측 판정
├── STATE.md              파이프라인 현황판
└── DONE.md               본 문서
```
