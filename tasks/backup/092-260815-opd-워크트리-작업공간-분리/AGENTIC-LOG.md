# AGENTIC-LOG: 태스크 작업공간 worktree 분리 (`--worktree`/`--wt` 축 신설)

> 모드: agentic | 시작: 2026-08-15 14:10 | 스킬: //opd

## 요약

| 항목 | 건수 |
|------|------|
| 게이트 판단 | 14회 (Pass: 13 / Fail: 1) |
| 3회 초과 Gate | 0건 (Critical: 0 / Normal: 0 / Minor: 0) |
| 오류 발견 | 8건 |
| 수정 지시 | 9건 (반영: 9 / 미반영: 0) |
| PM 의사결정 | 13건 |
| 개선 사항 | 4건 (fw 3 / local 1) |
| 에스컬레이션 | 1건 (RED 워커 → DEC-7 설계 공백) |

## 대행 일지

| # | 시점 | 단계 | 카테고리 | 내용 | 결과 |
|---|------|------|----------|------|------|
| 1 | 2026-08-15 14:10 | TASK | DECISION | 스킬 선택 — 캡틴이 `//opd --agentic`로 명시 호출. 변경 대상이 도구 신설 1종 + 참조 문서 4종 + 스킬 2종으로 다영역이고 실환경 동작검증(TEST)이 필수라 opds가 아닌 opd(Full Task) 적정 판단 | 확정 — opd/agentic |
| 2 | 2026-08-15 14:10 | TASK | DECISION | pilot 10종 SKILL.md 미변경 결정 — `--wt` 축 정의를 `opal-harness.md` SSOT 1곳에 두고 집행은 worktree-tool이 담당. 근거: 태스크 091이 동일 기조(산문 규칙 → 도구 집행)로 pilot 10종을 감량한 직후이며, 여기서 다시 10종에 산문을 복제하면 091을 되돌리는 셈 | TASK.md C-9·범위 제외에 기재 |
| 3 | 2026-08-15 14:10 | TASK | DECISION | lock 해시 기반 심볼릭 재사용 설계 폐기 — 대화 중 실측(APFS CoW 클론 지원 + pnpm store 동일 볼륨)으로 재설치 디스크 비용이 0에 수렴함이 확인되어, 파일시스템이 이미 해결한 문제에 추상화를 얹지 않기로 판단(PRINCIPLES §2). setup 지연 실행은 **시간** 절감 목적으로만 존치 | TASK.md C-7에 기재 |
| 4 | 2026-08-15 14:56 | ANALYSIS | ERROR | ANALYSIS.md §1.2·§4-2가 "`jsonschema` 라이브러리가 프로젝트 전체(`requirements.txt`)에 부재"를 근거로 제시했으나 실측 결과 `~/.opal/.venv`에 `jsonschema 4.26.0`이 설치되어 있고 state-tool에는 `requirements.txt` 자체가 없다. 근거가 사실과 다름 | 정정 대상 |
| 5 | 2026-08-15 14:56 | ANALYSIS | FIX | (#4 대응) PM이 ANALYSIS.md §1.2 표·§4-2에 정정 주석을 직접 삽입. 결론(런타임 미강제 → F-5 리스크 없음)은 "`state_tool.py`가 `state.schema.json`을 로드하는 코드가 없고 유일 언급이 주석 1줄(`state_tool.py:464`)"이라는 실측만으로 성립하므로 **유지**. 다만 F-1이 "jsonschema를 쓸 수 없어서 hand-rolled"라는 거짓 전제를 세우지 않도록 PLAN 결정 사항으로 이관 | 반영 완료 |
| 6 | 2026-08-15 14:56 | ANALYSIS | DECISION | 워커가 Write 도구 차단(서브에이전트 보고파일 가드)으로 Bash heredoc 폴백 사용 — TASK.md에 없는 방식이라 사후 승인 대상. 산출물 무결성 실측(201줄·24KB·섹션 6개 정상·내용 절단 없음) 확인 후 **승인**. ANALYSIS.md는 op-dev-analysis SKILL.md가 규정한 필수 산출물이지 임시 요약이 아니므로 폴백이 정당 | 폴백 승인 |
| 7 | 2026-08-15 14:56 | ANALYSIS | GATE | ANALYSIS PM Gate 강화 검토 — ①TASK 요구 커버(접합면 6곳 + 추가 조사 4항목 전건 응답) ②Artifact Gate(파일 실존·내용 충실) ③인용 규칙(참조표 D-1~D-16 + 인라인 줄번호 준수) ④TASK.md C/F 항목과 일관 ⑤핵심 주장 4건 PM 직접 실측 대조(jsonschema·install chmod 블록·CONVENTIONS 브랜치 규칙·opi 경로) — 1건 근거 오류 정정 후 나머지 전건 일치 | **Pass** (오류 1건 정정 후) |
| 8 | 2026-08-15 15:10 | PLAN | DECISION | **DEC-6 신설(R-13 종결)** — PLAN이 "TASK §제약 'DB 동시성 경고'가 F-001~F-009 어느 AC에도 미편입"을 지적하고 PM 판단을 요청. 판정: ①캡틴 확정 제약이므로 조용한 제외 불가 ②원안(마이그레이션 태스크 판정)은 도구가 태스크 성격을 알 입력 계약이 없어 스키마·파이프라인 확장 필요 → Simplicity First 저촉 ③도구가 실제 관측 가능한 사실(동시 활성 슬롯 수)로 경고를 대체하면 3줄 비용에 F-009(b)와 동일한 비차단 경고 계열이라 새 개념 무도입 | Step 4에 `diagnose_concurrent_slots()` 추가 + H-16 신설 + PLAN §DEC-6 기재 |
| 9 | 2026-08-15 15:10 | PLAN | GATE | PLAN PM Gate 강화 검토 — ①TASK 요구 F-1~F-9 전건 커버(§1.2 기능 목록 대조) ②§4.2 실행 체크리스트 21 Step 전건에 소속 F-ID·영역·agent·완료기준·의존 기재 ③리스크 가설 H-1~H-15 작성(요청 필수 5종 전부 포함) ④설계 빈틈 = DEC-1~DEC-5 전건 종결(재차 미룸 0건) ⑤핵심 근거 5건 PM 직접 실측 대조 — `jsonschema` Required-by:mcp·`opal/tools/requirements.txt` 미선언 / `origin/HEAD`=`refs/remotes/origin/main` / `code-scan.json` exclude에 `specs` 존재 / `install-mac.sh` code-scan chmod 블록 위치 / 3문서 "18종" 표기 — **전건 일치** ⑥미승인 폴백 0건 | **Pass** (DEC-6 보정 후) |
| 10 | 2026-08-15 15:36 | TEST-SCENARIO | DECISION | RED-first 트랙 영역별 분기 판정 — 도구 CLI(`worktree_tool.py`)·`state_tool.py`는 red-first §1.5 "비즈니스 로직"+"API 계약"으로 **RED-first 강제**, 참조 문서·스킬·설정은 "설정·문서"로 **구현 후 검증**. 혼합 태스크라 영역별 분기가 필요했고 모호분은 안전측 채택 | TEST-SCENARIO §0.1 기재 |
| 11 | 2026-08-15 15:36 | TEST-SCENARIO | DECISION | M2(E2E) 의무 트리거 **면제** 판정 — 트리거 3종(FE 화면·인증/인가·외부 API)을 변경 영역과 1:1 대조해 전건 미해당 확인. 근거 없이 누락한 것이 아님을 대조표로 남김 (iteration 2 Evaluator가 "면제 정당" 확인) | TEST-SCENARIO §0.2 기재 |
| 12 | 2026-08-15 15:41 | TEST-SCENARIO | ERROR | 목표-커버 게이트 iteration 1 **fail**(goal 1/adoption 1/boundary 1, 평균 1.00 < 1.5). 핵심 지적: 목표 문장의 주어가 "OPAL 태스크 파이프라인"인데 플래그→훅→도구→state 접합을 한 번도 실행하지 않아, **훅이 create를 호출하지 않게 접합돼도 23개 시나리오가 전건 PASS**하는 상태(070 동형 공백). 그 외 `status` 0회 호출·config 부재 경로 0건·중복 생성 방향 누락 등 gaps 7건 | 재작성 대상 |
| 13 | 2026-08-15 15:45 | TEST-SCENARIO | FIX | (#12 대응) gaps G-1~G-7 전건 반영 — S-24(파이프라인 관통)·S-25(status+CLOSE 안내)·S-26(config 부재 부수효과 0)·S-27(중복 생성 거부) 신설, S-1 값 정합·S-14 부적합 3종·S-18 실환경 2슬롯 확장, 가설 H-17~H-20 추가. 시나리오 23→27, 가설 16→20 | 반영 완료 |
| 14 | 2026-08-15 15:53 | TEST-SCENARIO | ERROR | iteration 2 Evaluator가 **R-3**으로 지적: §2.1·§2.2·§4.2·§7이 iteration 1 수치("23행", "H-1~H-16")로 잔존해 **§7 자기 인증이 실제와 불일치**. RED 워커가 §2.1/§2.2를 fixture 원천으로 읽으므로 실해가 있음 | 정정 대상 |
| 15 | 2026-08-15 15:53 | TEST-SCENARIO | FIX | (#14 대응) 부속 표 4곳 동기화 — §2.2에 S-24~S-28 5행 추가, §2.1에 fixture 4종 추가, §4.2 완료기준 ⑦을 H-1~H-21/S-1~S-17·S-21~S-28로 갱신, §7 자기 인증 수치를 실측 재계산값(가설 21·자원 21·흐름 28·시나리오 28)으로 교체. 아울러 권고 R-1(S-24에 문서 3종 문안 grep ⓓ 추가)·R-2(S-28 신설) 채택 | 반영 완료 |
| 16 | 2026-08-15 15:53 | TEST-SCENARIO | GATE | 목표-커버 게이트 iteration 2 **pass**(goal 1/adoption 2/boundary 2, 평균 1.67 ≥ 1.5, 0점 축 없음). tool-gated 두 증거 확보 — coverage-check exit 0 AND evaluator verdict pass. **판정 후 편집에 대한 명시**: 판정 시점 산출물은 27건이며 이후 R-1~R-3 반영으로 28건이 되었다. 전부 **additive**(시나리오 삭제·기대결과 약화 0건)이고 Evaluator 자신의 권고를 이행한 것이며, coverage-check를 재실행해 exit 0을 재확인했다. Producer가 판단축을 자가 채점해 pass를 만든 것이 아니다 | **Pass** |
| 17 | 2026-08-15 16:20 | EXECUTE | GATE | RED 게이트 — PM 직접 실측으로 증거 확인. worktree-tool 36 failed(구현체 부재가 유일 원인, fixture ERROR 0) / state-tool 2 failed·306 passed(기존 304 전량 유지, 회귀 0) / `verify --red-check` 3항 pass. RED 워커의 S-3·S-15 제외 판단 승인 — 둘 다 TEST-SCENARIO가 이미 M1 Bash 단발로 규정한 일과성 검증이고, S-15는 `~/.opal/` 덮어쓰기라 워커 Guards와 직접 충돌. TEST 단계 1회성 확인으로 이관 | **Pass** |
| 18 | 2026-08-15 16:23 | EXECUTE | DECISION | 워커 자진 신고 폴백 승인 — BE-B(Step 7·8)가 PLAN.md 체크박스를 `[x]`로 고쳤다가 Scope 제한 위반임을 스스로 인지하고 즉시 원복. PM 실측 결과 `git diff --stat PLAN.md` 공백 확인. 자진 신고 + 원복 완료이므로 Gate Fail 아님 | 승인 |
| 19 | 2026-08-15 16:23 | EXECUTE | GATE | Step 7·8 PM Gate — ①`pytest state-tool` **308 passed**(기존 306 + 신규 2), failed 0 ②변경 규모 `state_tool.py` +9/-1·`schema` +4 (Surgical Changes 준수) ③조건부 대입 실코드 확인(`getattr(args,"worktree",None)` — 무조건 대입 아님) ④**하위호환 실증** — PM이 직접 `--worktree` 없이 init 실행해 `state.json` 최상위 키 9종에 `worktree` **부재** 확인(문서 주장이 아닌 실행 증거) | **Pass** |
| 20 | 2026-08-15 16:35 | EXECUTE | ERROR | **PM 자신의 디스패치 프롬프트 결함** — 배치 1 세 워커 모두에 "Scope 제한: 이 밖의 파일은 절대 수정하지 마라"를 워커 간 파일 충돌 방지 목적으로 넣었으나, 이 문구가 op-dev-execute가 **수행하도록 규정된** PLAN.md 체크박스 갱신·TEST-SCENARIO.md 실행 명령 칸 기입(TEST-SCENARIO §역할 분배 표)까지 함께 금지했다. 결과적으로 BE-B는 원복(Step 7·8 미체크), TASK-C는 유지(Step 9·11·13·17 체크)로 **같은 지시를 다르게 해석**해 산출물 상태가 불일치했다 | 정정 대상 |
| 21 | 2026-08-15 16:35 | EXECUTE | FIX | (#20 대응) PM이 PLAN.md Step 7·8 체크박스를 직접 `[x]`로 보정해 6개 Step 완료 표시를 정합화. 후속 배치 디스패치부터는 Scope 제한 문구를 "**코드·문서 소스 파일** 중 아래 목록 외 수정 금지. 단 태스크 폴더 산출물(PLAN.md 체크박스·TEST-SCENARIO.md 실행 명령)은 규정된 갱신 대상이므로 예외"로 교정한다 | 반영 완료 |
| 22 | 2026-08-15 16:35 | EXECUTE | GATE | Step 9·11·13·17 PM Gate — ①`opal-harness.md` **§2.5 소수점** 삽입 확인, §1~§10 기존 번호 **전부 불변**(H-13 회피 성공) ②§2.5 필수 3항목 실내용 확인 ③**dangling 인용 0건** — PM이 헤딩 집합 대 인용 집합을 직접 대조(1차 검증 시 PM 스크립트 정규식이 `## 1. Guards`의 마침표를 처리 못 해 오탐 9건 발생 → 정규식 수정 후 재검증) ④`code-scan.json` diff **1행**, 들여쓰기 보존, `code-scan scan` exit 0 ⑤전체 44 insertions / 1 deletion — 전건 추가형, 기존 문구 삭제 0 | **Pass** |
| 23 | 2026-08-15 16:50 | EXECUTE | DECISION | **워커 PLAN 이탈 승인 (폴백 승인 의무)** — BE-A가 PLAN §3.2.3의 `worktree add -b <branch>` 단일 명령을 `add --detach` → `checkout -b` 2단계로 분리. 근거(워커 재현 검증): git이 브랜치 ref를 먼저 쓰고 `.git/worktrees/` 등록에서 실패할 수 있어 **고아 브랜치**가 남고 `_rollback()`이 권한 제약 시 이를 못 지운다 → PLAN 자신의 **H-7(롤백 원자성)이 깨진다**. 분리하면 `add` 실패 시 브랜치가 애초에 생기지 않는다. monorepo는 detached→브랜치가 동일 커밋이라 sparse 파일이 물질화되지 않아 `reset --hard HEAD` 1줄 추가. **판정: PLAN 설계 의도를 더 잘 만족하는 개선이므로 승인**(harness-agentic §3 "더 나은 방식이라면 PM 승인 후 허용"). PLAN §4.2 Step 3에 승인 주석 삽입해 문서-코드 불일치 차단 | 승인 + PLAN 기록 |
| 24 | 2026-08-15 16:50 | EXECUTE | GATE | Step 1~6 PM Gate — ①`pytest worktree-tool` **34 passed / 2 failed** 재현 ②잔여 2건이 실제 문서 의존임을 PM이 실측 확인(`task-process.md`에 '4.5' 0건 · `opd SKILL.md`에 'worktree' 0건) — 코드 결함 아님 ③테스트 파일 무변경(untracked 신규만) ④이탈 지점 실코드 확인(`worktree_tool.py:456-480`, 근거 주석 동반) ⑤산출물 767행(본체 683 + run.sh 10 + 스키마 55 + 템플릿 19) | **Pass** (교차 의존 2건은 배치 2로 이월) |
| 25 | 2026-08-15 17:03 | EXECUTE | ERROR | **실환경 전용 차단성 결함 발견(revup)** — `remove` 성공 후 빈 슬롯 껍데기(`task_NNN/workspace/`, 파일 0개)가 남고, 같은 태스크 번호로 `create` 재실행 시 `WORKTREE_EXISTS`로 **영구 차단**된다. `list`는 `entries: []`(슬롯 없음)로 답하는데 `create`는 "존재한다"고 거부 — 도구 내부 모순. pytest가 놓친 이유: S-9는 레포별 경로(`g.wt_path`)만 단언했고 S-27은 **살아 있는** 슬롯으로만 시험해 이 경로를 밟지 않았다. `remove` 후 재작업은 흔한 경로라 수정 필수 | 수정 루프 진입 |
| 26 | 2026-08-15 17:03 | EXECUTE | GATE | Step 19(revup 유형 A) 실환경 검증 — 생성 ✅(레포 2곳 worktree, `feat/OP-TASK-092`, 레이아웃 메인 동형, `.gitignore` 1행, `base_ref=origin/main` 동결) / **목표 격리 실증** ✅(슬롯 092 커밋 후 슬롯 093·메인 `workspace/backend` 공히 status 0건·브랜치 불변, 3자가 각각 다른 브랜치 체크아웃) / lazy setup ✅(`bun install` 미실행, `pending_setup` 열거) / DEC-6 동시 슬롯 경고 ✅(1개째 없음→2개째 출현, 비차단) / 3중 가드 ✅(`GUARD_UNPUSHED` 거부 → 해소 후 성공, 브랜치 잔존) / **기준선 완전 원복 확인**(루트 변경 0건, 브랜치·worktree 원상) | **조건부 Pass** (#25 결함 수정 후 재검증 필요) |
| 27 | 2026-08-15 17:10 | EXECUTE | DECISION | **캡틴 지시 — 경로 리네임 `.opal_worktree/` → `.opal-worktrees/`** 반영. PM이 실측 근거로 3안을 제시(현행 유지 / `.opal-worktrees/` 개명 / `.opal/worktree/` 이동)했고, `.opal/`이 커밋 추적 대상(revup 430·mams 168·opal 261 파일)이라 대용량 비추적 파생물과 섞으면 안 된다는 이유로 `.opal/` 하위 이동은 기각. 캡틴이 개명안을 선택. **16파일 116건 일괄 치환, 잔여 0건.** 치환 전후 `pytest` 공히 **36 passed** — 리네임이 행위 중립임을 증명. 테스트 파일도 4+18건 치환했으나 이는 **소유자 지시에 의한 사양 변경**이지 통과를 위한 테스트 약화가 아니다(red-first §3 위반 아님 — 결과 건수·단언 내용 불변) | 반영 완료 |
| 28 | 2026-08-15 17:25 | EXECUTE | DECISION | **DEC-7 확정 — 판정 기준을 "존재"에서 "점유"로** (#25 결함 + RED 워커 에스컬레이션 대응). RED 워커가 "슬롯 루트만 고치면 `BRANCH_EXISTS`에 막힌다"는 후속 공백을 임의 판단 없이 블로커로 올렸고, PM이 임시 저장소에서 git 실동작 4건을 실측(브랜치 잔존 / `add <path> <기존브랜치>` 재사용 성공 / 점유 중 브랜치는 git 자체 거부 / `worktree list --porcelain`로 점유 판별)한 뒤 확정. 슬롯 판정=worktree 등록 여부, 브랜치 판정=점유 여부, 미점유 브랜치는 재사용, `remove`는 슬롯 루트까지 회수. S-27(살아 있는 슬롯 거부)은 판정 기준 하나로 자연히 보존된다 | PLAN §1.4 DEC-7 + H-22 기재 |
| 29 | 2026-08-15 17:30 | EXECUTE | FIX | (#25 대응) 구현 워커가 DEC-7 4행 구현 — `_worktree_entries`/`_dest_registered`/`_branch_occupied` 신설, `cmd_create` pre-flight 교체 + 재사용 분기, `cmd_remove` 슬롯 루트 회수. **41 passed**(기존 36 + S-29 5건), 테스트 무변경(skip/xfail 0). PM이 revup 실환경에서 결함 시나리오 재현 검증 — create→remove→**슬롯 루트 제거 확인**→같은 번호 재생성 **성공**(브랜치 재사용)→살아 있는 슬롯 중복은 **여전히 거부** | 반영 완료 |
| 30 | 2026-08-15 17:35 | EXECUTE | ERROR | PM 자신의 누락 — PLAN에 H-22만 넣고 **TEST-SCENARIO.md에 S-29를 기재하지 않았다**. 구현 워커가 "S-29 행이 없어 실행 명령 칸을 채울 대상이 없다"고 보고해 발견. 산출물 간 정합이 깨진 상태였다 | 정정 대상 |
| 31 | 2026-08-15 17:35 | EXECUTE | FIX | (#30 대응) TEST-SCENARIO.md에 §1 H-22 · §2.2 데이터 흐름 · §3 S-29 본문(5 케이스) · §4.1/§4.2 매핑 · §7 자기 인증 수치(29행·H-1~H-22) 전건 추가. **"단위 테스트 36건 전건 GREEN 상태에서 실환경 검증이 차단성 결함을 잡아냈다"는 사실을 S-29 주석에 증거로 남김** | 반영 완료 |
| 32 | 2026-08-15 17:40 | EXECUTE | GATE | Step 19·20 실환경 재검증 — **revup(유형 A)**: 결함 시나리오 완전 해소 + 격리 실증(3자 독립 브랜치) + 3중 가드 작동 + 기준선 완전 원복. **mams(유형 B)**: sparse가 `workspace/`만 체크아웃하고 `tasks/`·`.opal/`·`100.기획`·`200.개발`·`900.문서` 전부 부재 확인, `copy[]` 3파일 복사, lazy setup으로 `.venv`·`node_modules`·`.next` 미생성, **슬롯 13MB**(메인 1.9GB 대비), 사전 2건 기준선 원복 | **Pass** |
| 33 | 2026-08-15 17:50 | EXECUTE | GATE | Step 21 `UV_CACHE_DIR` 이전 (PLAN §3.9.3 6단계 + 검증 4항) — 구 캐시 10.9GiB/639,911파일 clean, `~/.zshrc` 멱등 export(2회차 추가 생략 확인), interactive 셸에서 `uv cache dir → /Volumes/Data/.uv-cache` 확인, `st_dev` 동일(16777230), mams `uv sync` exit 0, **볼륨 경고가 `warnings[]`에서 사라짐**(H-12 반증 성공). **실디스크 측정: 이전 전 슬롯당 263MB 실복사 → 이전 후 8.7MB (약 30배 절감)** — apparent 229MB 대비 APFS CoW가 작동. 첫 슬롯은 캐시 재구축 포함이라 242.5MB, 2번째부터 8.7MB | **Pass** |
| 34 | 2026-08-15 17:50 | TEST | GATE | L1/L2 전수 검증 — 기능 시나리오 **26/26 Pass**(S-1~S-17·S-21~S-29), 자동 회귀 349 passed(worktree-tool 41 + state-tool 308, PM 기준값 일치), Bash 단발 2건(S-3 pilot diff 1건 · S-15 배포본) Pass, **보안 4/4 Pass**(시크릿·gitignore·커맨드 인젝션 `shell=True` 0건·경로 이탈 차단). 워커 편집 사고 1건 자진 신고(S-5~S-7 경계 손상 후 복구) → PM이 헤딩 29종 중복 0·누락 0·L3 3건 결과 칸 미접촉을 직접 재검증해 Gate Fail 아님 판정 | **Pass** |
| 35 | 2026-08-15 18:00 | TEST | FIX | 코드 품질 Partial Fail 해소(fix 루프 1/3) — mypy 2건은 `err_response()`에 `-> NoReturn` **1곳** 추가로 동시 해소, `ruff format`은 신규 파일만 적용. **`state_tool.py`는 092 범위 밖 기존 포맷이라 의도적 미접촉**(2,600행 전체 포맷 시 092 변경 3곳이 묻힘 — Surgical Changes). PM이 포맷 후 로직 불변을 직접 대조(ERROR_CODES 18종·서브명령 4종·회귀 349 유지) | 반영 완료 |
| 36 | 2026-08-15 18:05 | TEST | ERROR | **PM 절차 오류** — TEST PM Gate 체크리스트에 "컨벤션 자동 진단(GC-CONVENTION-*.md Critical/High 0건)"이 있는데 **검사를 실행하기 전에 게이트 행을 mark**했다. 순서가 뒤바뀐 상태로 게이트를 통과시킨 셈 | 정정 대상 |
| 37 | 2026-08-15 18:10 | TEST | FIX | (#36 대응) 사후에라도 `opal-convention-checker`를 디스패치해 실행. 결과 **Critical 0 / High 0 / Medium 0 / Low 0 — 위반 0건**이므로 게이트 판정 자체는 유효했다. PM이 표본 검증으로 확인 — 신규 Python 3종 @header 전건 존재, 문서 9종 + install-mac.sh 변경이력 `(092)` 행 전건 존재. **다만 "결과가 좋았으니 괜찮다"가 아니라 순서 위반 자체를 기록으로 남긴다** — 위반이 있었다면 잘못된 통과가 됐을 것이다 | 반영 완료 |
| 38 | 2026-08-15 18:20 | CLOSE | DECISION | 캡틴이 "CLOSE 진입 승인" 발화. L3 3건(S-18·S-19·S-20)에 대한 **개별 PASS 발화는 없었으므로**, TEST-SCENARIO에 "캡틴 CLOSE 진입 승인으로 수용"으로 정확히 기록하고 개별 승인으로 위조하지 않았다. `test.user_confirm` 행을 `--owner user`로 mark하여 CLOSE 진입 게이트 충족 | 기록 정확성 유지 |
| 39 | 2026-08-15 18:25 | CLOSE | DECISION | 관련 문서 갱신 판정 — `ARCHITECTURE`·`CONVENTIONS`·`PROJECT` 3종은 Step 16에서 완료. `SECURITY.md`(보안 기준 무변경 — 신규 취약 표면 없고 커맨드 인젝션·경로 이탈은 기존 기준으로 방어 확인)·`README.md`(pilot 개념·철학 무변경 — 도구 신설이지 pilot 추가 아님)·아키텍처 다이어그램 HTML(3층 구조 무변경)은 **갱신 불요**로 판정 | no-op |
| 40 | 2026-08-15 18:30 | CLOSE | ERROR | **PM 실수 재발** — 회고 `improve-tool` 3건 호출이 전건 실패(`ok:false`). 원인은 zsh가 unquoted 변수를 단어 분리하지 않아 공통 인자 묶음 `$R`이 한 덩어리로 전달된 것. **세션 초반 git 함수에서 겪은 것과 동일한 원인을 재차 밟았다.** 진단 과정에서 만든 테스트 메모리 항목이 잔존 | 정정 대상 |
| 41 | 2026-08-15 18:32 | CLOSE | FIX | (#40 대응) 변수 없이 인자를 직접 전개해 3건 재기록 성공(fw 2 + local 1). 테스트 항목은 `update --status dead` → `delete`로 정리 — memory-tool 무손실 가드가 active 행 즉시 삭제를 실제로 거부해 2단계를 강제했다. `.opal/MEMORY.json` 정상 확인(active 5 / total 7 / history 5) | 반영 완료 |
| 42 | 2026-08-15 18:35 | CLOSE | IMPROVE | 회고 하드스텝 개선 후보 3건 기록 — ①(fw) op-dev-execute Scope 제한이 규정된 산출물 갱신까지 금지해 워커 간 해석 불일치 유발 ②(fw) PM Gate 체크리스트 중 도구 실행 항목의 선후가 미강제(산문 리마인더뿐 — 헌법 Enforce 적용 대상) ③(local) 도구 신설 시 상태 전이 **왕복 경로**를 시나리오에 명시(단방향만으로도 커버리지 지표는 만족하므로 결정론 게이트가 못 잡는다). 세션 초반 기록분 포함 fw-inbox 3건 누적 | 기록 완료 |
| 43 | 2026-08-15 18:40 | CLOSE | GATE | brain ingest — 페이지 3건 생성(entity `worktree-tool` / concept `워크스페이스 축` / concept `존재→점유 판정 전환`). PM 검증: 파일 3건 실재(5,136·5,009·3,863 bytes), `index.md` 등록, **개인 호칭 오염 0건**(brain 재사용 지식 규율 준수). 워커가 "목표-커버 게이트 070형 공백 사례"는 기존 페이지와 중복이라 의도적 제외 — 타당 판정. 최종 회귀 **349 passed** | **Pass** |
