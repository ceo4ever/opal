# GC CONVENTION REPORT — 260909-184729

## 1. 헤더

- 실행 일시: 2026-09-09 18:47 KST
- 범위: Task 112 worktree `HEAD` 대비 변경 파일, 현재 Task 112 계약, hub-fixed `.opal/AGENT.md`·`.opal/worktree.json`
- 제외: 과거 `tasks/`·`brain/`, 동시 작업 Task 113 파일, 삭제되지 않은 비변경 기존 파일
- 에이전트: `opal-convention-checker`
- 기준: `docs/CONVENTIONS.md` 전체. 문서 이력의 원문은 해당 문서가 지정한 `opal/core/references/opal-doc-standard.md` §4~§5
- 진단 방식: 읽기 전용. 소스·상태·테스트 결과 파일 미수정

## 2. 요약 지표

| 지표 | 값 |
|---|---:|
| 총 이슈 수 | 4 |
| 심각도 분포 | Critical 0 / High 2 / Medium 2 / Low 0 / Info 0 |
| Critical/High 수 | 2 |
| 자동 수정 가능 | 1 |
| 수동 조치 필요 | 3 |
| 파일별 상위 | `opal-pilot-sdd/SKILL.md` (1), `spec-guide.md` (1), `TEST-SCENARIO.md` (1), `PLAN.md`·신규 shell test (1), registry (1) |
| 카테고리별 빈도 | 문서화 1건(3개 위치), 파일 구조 2건, 네이밍 1건 |

## 3. 수정 대상

### Critical (0건)

없음.

### High (2건)

- [ ] GC-C001 [복수 위치] 수기 누적 이력 금지 계약과 반대되는 현행 지시·테스트 기대값 존재
  - 파일·라인:
    - `opal/skills/opal-pilot-sdd/SKILL.md:361`, `:371`
    - `opal/skills/opal-pilot-sdd/references/spec-guide.md:15`
    - `tasks/112-260909-opd-파일럿-스킬-구조통합/TEST-SCENARIO.md:25`
  - 카테고리: 문서화
  - 위반 기준: `docs/CONVENTIONS.md` §문서 이력 표준, §문서 이력 계약; `opal-doc-standard.md` §4~§5
  - 설명: opsdd `STATE.md` 예시는 `SPEC 변경 이력`을 고유 자유 기재 절로 유지하고, `spec-guide.md` 상단은 변경 이력 기록을 지시한다. 같은 guide의 `:241`·`:251`은 반대로 수기 누적 이력 절을 만들지 말라고 하므로 행동 계약이 자기모순이다. Task 112 S-7도 문서 이력에 `(112)`가 있어야 PASS로 판정하여 C-6·W-10과 충돌한다.
  - 해결 방안: opsdd `STATE.md` 예시의 `SPEC 변경 이력` 항목을 제거하고, `spec-guide.md:15`를 git/task SSOT 문구로 치환한다. S-7 기대값은 변경대상 문서의 수기 누적 이력 절 0건으로 바꾼다. 검사는 `## 변경이력`만이 아니라 Changelog/history/revisions 및 동일 의미 제목까지 다룬다.
  - 자동 수정: N
  - 참조: `opal/core/references/opal-doc-standard.md` §4~§5

- [ ] GC-C002 [`scripts/tests/test_install_skill_cleanup.sh:1`] PLAN 변경 경계에 없는 신규 파일
  - 카테고리: 파일 구조
  - 위반 기준: `docs/CONVENTIONS.md` §구현 규칙 → §Guards(구현 금지·승인 게이트); `/Users/iskang/.opal_v2/PRINCIPLES.md` §5 “계획에 이름이 적힌 것만 손대다”
  - 설명: PLAN W-6의 변경 대상은 `scripts/install-mac.sh`, `scripts/install/windows.ps1`, `opal/tools/opal-cli/lib/update.sh`, `scripts/tests/test_archive_contents.sh`, `scripts/tests/test_download_contract.sh`이다(`PLAN.md:34`). 실제 생성된 `scripts/tests/test_install_skill_cleanup.sh`는 명명된 파일·디렉토리 범위에 없다. 신규 RED 테스트라는 목적은 W-6 설명으로 추론되지만 변경 경계를 대체하지 못한다.
  - 해결 방안: W-6 `변경 대상`과 W-8 검증 대상에 신규 테스트 파일을 명시하거나, 이미 계획된 테스트 파일 하나로 계약을 이관한다.
  - 자동 수정: N
  - 참조: `docs/CONVENTIONS.md` §구현 규칙

### Medium (2건)

- [ ] GC-C003 [`opal/core/references/opal-skills-registry.json:88`] opsdd 설명이 현행 pipeline과 불일치
  - 카테고리: 파일 구조
  - 위반 기준: `docs/CONVENTIONS.md` §약어 — registry SSOT, §도구 우선 원칙; 동일 항목 `pipeline`이 현행 단계 SSOT
  - 설명: description은 `SPEC→VERIFY→PLAN→TASKS→VERIFY→LOOP→DONE`을 노출하지만, 같은 항목의 pipeline은 `TASK→SPEC→REVIEW→DESIGN→EXECUTE→VERIFY→CLOSE`다(`:99`). 삭제한 SPEC-VERIFY/TASKS-VERIFY 시대의 서술이 활성 registry에 남아 있다.
  - 해결 방안: description을 현행 pipeline·REVIEW 검증 소유권에 맞게 제자리 갱신한다.
  - 자동 수정: Y
  - 참조: `opal/core/references/opal-skills-registry.json:86-99`

- [ ] GC-C004 [`scripts/tests/test_install_skill_cleanup.sh:1`] 신규 shell 파일명이 kebab-case 규칙과 불일치
  - 카테고리: 네이밍
  - 위반 기준: `docs/CONVENTIONS.md` §언어 규칙, §네이밍 규칙 → 파일/폴더
  - 설명: 파일명은 영문 kebab-case, Python만 snake_case 예외다. 신규 `.sh` 파일은 snake_case다. 다만 `scripts/tests/` 기존 shell 테스트가 snake_case를 사용하는 현황과 충돌하므로, 현행 컨벤션을 우선해 이슈로 판정하되 심각도는 Medium으로 낮춘다.
  - 해결 방안: `test-install-skill-cleanup.sh`로 변경하고 모든 참조를 같이 갱신하거나, shell test에 snake_case를 허용할 것이면 `docs/CONVENTIONS.md`에 예외를 명시한다.
  - 자동 수정: N
  - 참조: TBD — 프로젝트 `docs/CONVENTIONS.md` §네이밍 규칙

### Low (0건)

없음.

### Info (0건)

없음.

## 4. 문서 및 하네스 확인

### 심각도 트리거

- [ ] GC-DP-C001 [심각도 트리거] Critical/High 2건. TEST PM Gate의 `Critical/High 0건` 계약을 아직 만족하지 못함.

### 빈도 트리거

- [ ] GC-DP-C002 [빈도 트리거] 수기 이력 잔여가 3개 파일·계약 위치에서 동시 감지됨. 현재 W-10/W-8의 `## 변경이력` 정확 문자열 검사를 문서 표준의 의미 기반 금지 범위로 확장할 필요.

### 하네스 선행 집행 여부

- 현재 구조에서 수기 이력 금지의 정책 SSOT는 `opal-doc-standard.md` §4~§5이고, `docs/CONVENTIONS.md:128-130`, `:233-236`이 이를 참조한다.
- `opal/core/references/opal-harness.md`와 `opal/core/references/harness/`의 현행 파일에서는 문서 이력 금지를 결정론적으로 선행 검사하는 계약이 검색되지 않았다.
- 현재 차단 지점은 `opal-pilot-dev/references/pipeline.json:29-30`의 TEST PM Gate가 요구하는 `GC-CONVENTION-*.md Critical/High 0건`이다. 따라서 “컨벤션 이전의 하네스 자동 제거/차단”으로 구현된 상태가 아니라, 문서 표준 정책을 컨벤션 진단이 집행하는 상태다.
- 하네스 선행 강제가 의도라면 별도 결정론 검사기와 스킬·에이전트 문서 수정 시의 게이트 배선이 필요하다. 이 항목은 현재 `docs/CONVENTIONS.md`가 요구하는 구현 계약은 아니므로 위반 수에는 합산하지 않았다.

### 통과 확인

- 경로·alias: `skill-registry validate` 성공, `//opd`·`//opds` shared canonical path 테스트 포함 Node 19건 PASS.
- SDD 구조: 최상위 `op-sdd-*` 0건, 내부 스킬 3종·persona 이동 확인.
- Worktree hub 경계: hub `.opal/worktree.json` `repos[]`에 `.opal`·`tasks`·`memory` 없음. `.opal/AGENT.md`는 hub에서만 문서 표준 포인터로 갱신됨.
- 형식: worktree·hub 대상 `git diff --check` PASS, 변경 Markdown 말미 개행 확인.
- @header: `code-scan validate --changed` 대상은 shell 확장자 미지원 및 test 디렉토리 제외로 모두 skip되었음. 현재 변경분에 적용되는 선언적 `@header` 위반은 확인되지 않음.

## 5. 문서 작성 유도

`docs/CONVENTIONS.md` 존재. 초안 생성 유도 생략.

## 6. 판정

**FAIL — Critical 0 / High 2.** GC-C001·GC-C002를 해소하고 동일 범위를 재검사해야 TEST PM Gate의 컨벤션 계약을 통과한다.
