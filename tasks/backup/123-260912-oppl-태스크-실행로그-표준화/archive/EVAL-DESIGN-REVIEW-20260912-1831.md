# EVAL: 설계 루프 D6 명세 리뷰 3회차 (종료 판정) — 태스크 실행 로그 표준화

- 실행 일시: 2026-09-12T18:31:00+09:00
- phase: `design-review` (3회차 — 재판정 대상은 2회차 신규 N-1~N-4, 겸 설계 루프 종료 판정)
- target_artifacts: `PRD.md`, `TRD.md`, `CONTRACT.md`, `BACKLOG.md` + `surfaces.json`, `backlog.json`
- 기준 원천: `CONTRACT.md` §5 루브릭절 (RB-1~RB-5, 통과선 포함) — 이번 회차에도 무변경
- 선행 보고서: `EVAL-DESIGN-REVIEW-20260912-1806.md`(1회차), `EVAL-DESIGN-REVIEW-20260912-1819.md`(2회차). 둘 다 수정하지 않았다
- 판정 방법: 해소 주장을 근거로 쓰지 않았다. (1) `CONTRACT.md`를 1회차 보존본과 라인 diff해 이번 추가·수정을 전건 식별했고, (2) **MV-30을 내가 직접 스크립트로 실행해** `CONTRACT.md` §2.2·§2.2.1과 `surfaces.json`을 파싱·대조했으며, (3) N-2는 `dashboard/backend/stats.py`와 `routers/` 실제 호출부를 읽어 B-1~B-4의 실현 가능성을 확인했고, (4) N-3은 `backlog-tool` 소스를 읽어 결함 규명의 타당성을 확인했다
- verdict-only. 어떤 파일도 수정하지 않았다

---

## 종합 verdict: **pass** — 설계 루프 종료 조건 충족

| 축 | 1회차 | 2회차 | **3회차(최종)** | 통과선 | 판정 |
|---|---|---|---|---|---|
| RB-1 계약 완전성 | 4 | 5 | **5** | ≥4 | PASS |
| RB-2 계약 일관성 | 4 | 4 | **5** | ≥4 | PASS |
| RB-3 설계 정합 | 5 | 5 | **5** | ≥4 (3 이하 반려) | PASS |
| RB-4 범위 준수 | 4 | 5 | **5** | ≥4 (3 이하 반려) | PASS |
| RB-5 근거 인용 충실도 | 5 | 5 | **5** | ≥4 | PASS |

- 전 축 5점. 2점 이하 축 없음. RB-3·RB-4 반려 조건 미해당.
- **미해결 0건.** 설계 확정을 막는 항목 없음.
- drift 필요성: **no**.

---

## 1. N-1~N-4 재판정

| ID | 판정 | 근거 |
|---|---|---|
| N-1 | **해소 (지적 범위를 넘어선 해소)** | `surfaces.json` `run-log-tool.begin-worker`의 `err`가 6종(`run_log_missing`·`capability_scope_invalid`·`capability_expiry_invalid`·`run_log_write_failed`·`task_path_not_absolute`·`task_lock_timeout`)으로 확장돼 §2.2 신설 2종이 반영됐다. 자체 발견했다는 4유형도 파일에서 확인된다 — `append`의 `err`가 7종→10종(`redaction_failed`·`run_log_missing`·`task_path_not_absolute` 추가), `export`에 `run_log_missing` 추가, `init`을 포함한 CLI 17종 전부가 공통 2종 보유. 고정 장치로 §2.2.1 대응표(23행)와 MV-30(4항 판정)이 신설됐다 |
| N-2 | **해소** | 신설 §2.8.1이 B-1~B-4로 계층 경계를 확정했고, **실제 코드에서 성립 가능함을 확인했다**(아래 §2 상세). B-2가 열거한 6개 함수가 `stats.py`에 모두 실재하고 전부 `quiet_hours: tuple[int, int] | None`이며(`:106`, `:123`, `:224`, `:298`, `:421-424`, `:497-499`), B-3이 요구하는 좁히기 지점도 라우터 2파일에 국한된다. MV-25·MV-26에 판정 조건이 추가됐다 |
| N-3 | **범위 밖 — PM 판단 타당** | 소스에서 결함을 확인했다. `replace_backlog_section`(`opal/tools/backlog-tool/backlog_tool.py:228-234`)은 `before = md_content[:start_idx]`를 **그대로 보존**하고 마커 사이만 교체한다. "최종 갱신" 줄은 `_build_new_backlog_md`(:241 이하) 템플릿에서 마커 **바깥**(3행)에 찍히므로, 이후 어떤 갱신에서도 초기 생성 시각에 고정된다. `backlog.json.updated_at`은 `2026-09-12 18:26`으로 정상 갱신돼 있어 기계 SSOT는 정확하다. `BACKLOG.md`는 `TASK.md` C-5가 손편집을 금지한 3-SSOT 파생물이므로 이 태스크에서 고치면 오히려 제약 위반이며, 도구 수정은 본 태스크 범위(`TASK.md` §Affected users and systems)에 없다. **문서 결함이 아니라 도구 결함으로 처리하는 것이 타당하다** |
| N-4 | **해소** | T03 `slice`가 "…run 전역·주체별 순번 발급, 그리고 기존 실행 일지·플랫폼 원본의 **단방향·멱등 가져오기 능력**을 기록 코어와 기록 CLI에 구현한다. 가져온 사건의 완료 게이트 불기여 집행은 **1B(T11) 소관**이다…"로 갱신돼 수용 기준·`covers`와 범위 서술이 일치한다. 표면 커버리지 재대조 결과 19/19, 미커버·미정의 참조 0건 |

---

## 2. 독립 검증 결과

### 2.1 MV-30을 직접 실행했다 (PM 결과를 재현한 것이 아니라 독립 판정)

`CONTRACT.md` §2.2 표와 §2.2.1 대응표를 파싱하고 `surfaces.json`의 `kind=cli` 17개 표면 `response_shape.err`와 대조하는 스크립트를 작성해 4항을 그대로 판정했다.

| MV-30 판정 항 | 내 실행 결과 |
|---|---|
| (a) §2.2 표의 모든 코드가 §2.2.1에 발생 표면을 갖는다 | **PASS** — §2.2 코드 23종, §2.2.1 행 23종, 양방향 차집합 0 |
| (b) §2.2.1이 지정한 표면의 `err`에 그 코드가 있다 | **PASS** — 위반 0건. "전 CLI 표면 17종" 지정 2종(`task_path_not_absolute`·`task_lock_timeout`)도 전수 충족 |
| (c) `surfaces.json`의 모든 `err` 값이 §2.2 표에 존재한다 | **PASS** — 고아 코드 0건. err 합집합 크기 23 = §2.2 코드 수 23 |
| (d) `kind=cli` 17개 표면 전부가 공통 2종을 갖는다 | **PASS** — 누락 표면 0건 |

**MV-30은 실행 가능한 판정이다.** 고정 입력(두 자산)과 고정 판정(4항 boolean)을 가지며, 사람의 눈 대조 없이 파싱·집합 연산만으로 참·거짓이 갈린다 — 내가 실제로 그렇게 실행했다. `adapter.*` 2종을 제외하는 근거도 §2.2.1 말미에 명시돼 있다("CLI가 아니므로 오류 봉투를 직접 반환하지 않고, 변환기가 관측한 실패는 자기가 호출한 `run-log-tool.append` 표면의 오류로 드러난다") — 제외가 판정 회피가 아니라 계층 성격에서 따라 나온다.

**세 자산이 한 변경 단위로 묶였는가: 예.** §2.2.1이 "이 대응을 바꿀 때는 §2.2 표·§2.2.1 대응·`surfaces.json` 세 곳을 **같은 변경 단위**로 갱신한다"로 규범을 두고, MV-30이 그 규범을 기계 판정으로 강제한다. 규범과 집행이 짝을 이루므로 1회차·2회차에서 반복 발생한 "계약 문서만 고치고 기계가독 자산이 뒤처지는" 유형이 구조적으로 차단된다.

### 2.2 §2.8.1 B-1~B-4가 실제 코드에서 성립하는가

계약 문장이 아니라 코드로 확인했다.

| 조항 | 검증 결과 |
|---|---|
| B-1 (`QuietHours`가 stats 공개 시그니처에 없음) | **성립 가능.** `stats.py`는 이 태스크에서 수정 대상이 아니며, §2.8의 개정은 `config.py`와 라우터에 국한된다 |
| B-2 (주입 형태는 기존 2튜플 유지) | **열거가 실제와 일치한다.** B-2가 든 6개 함수가 모두 실재하고 전부 `quiet_hours: tuple[int, int] | None`이다 — `format_quiet_hours`(:106), `quiet_overlap_minutes`(:123), `row_durations`(:224), `task_static_stats`(:298), `task_live_stats`(:421-424), `workflow_stats`(:497-499). 누락·오기 없음 |
| B-3 (해석은 통계 호출 이전에 종료) | **실현 가능하고 범위가 국한된다.** 실제 소비 지점은 라우터 2파일뿐이다 — `routers/dashboard.py:123`(`load_quiet_hours`) → `:126`(`quiet_hours_token`) → `:225`(`workflow_stats(completed, quiet_hours)`), `routers/tasks.py:573` → `:581`(토큰) → `:677`(`task_live_stats(..., quiet_hours=quiet_hours)`) 및 같은 파일의 정적 집계 호출. 3필드 값은 토큰에만 그대로 넘기고 통계 호출에서 `(start_minute, end_minute)`로 좁히면 되며, `stats.py`·동결 fixture는 손대지 않는다 |
| B-4 (동결 표본 보존) | **논리적으로 따라 나온다.** C-2가 동결한 표본이 닿는 stats 공개 시그니처가 하나도 바뀌지 않으므로 회귀 표면이 생기지 않는다. 판정은 MV-25(정적: stats 인자 타입과 `QuietHours` 미출현)와 MV-26(행위: 통계에 확정 분 구간만 전달)이 나눠 진다 |

**`blockers: 없음`(C-2 충돌 아님) 주장도 타당하다.** C-2가 요구하는 것은 기존 테스트·동결 fixture의 무회귀이고, B-1~B-4는 그 표면을 건드리지 않는 방향으로 변경을 가둔다.

### 2.3 "확정 조항 무변경" 재검증

1회차 보존본과 현재 파일(682행)의 라인 diff 결과, 이번 회차(2→3) 변경은 다음이 전부다.

- **순수 추가 3블록**: §2.2.1(33행), §2.8.1(§2.8 뒤), MV-30(1행).
- **기존 행 수정 2건**: MV-25·MV-26. 둘 다 기존 판정 문장이 **글자 단위로 보존**된 채 뒤에 "**추가 판정: …**"과 근거 컬럼 참조가 덧붙었다. 기존 판정이 약화되거나 대체된 곳은 없다.
- **삭제·값 변경 0건.** 스키마 필드, enum, 상한(16 KiB / 4 KiB / 128 / 512 KiB / 4 MiB / 30,000 ms / 86,400초 / 604,800초), 사건 12종, 허용 조합 8행, 표면 17+2, MV-1~MV-24·MV-27~MV-29, §5 루브릭절과 통과선 전부 이전 회차와 동일하다.
- `surfaces.json`: 표본 대조 결과 `err`는 **추가만** 있고 제거 0건이며(`init` 3종 동일, `begin-worker` 3→6 상위집합, `append` 7→10 상위집합), `request_shape`·`response_shape.ok`·표면 id·`kind`·`auth`는 이전 회차와 동일하다. 표면 수 19, `origins` 미선언 유지.

**판정: 주장은 사실이다.** 정확히는 "MV 2개 행이 추가 판정 문장으로 확장됐고, 확정된 값·판정 내용이 철회되거나 바뀐 곳은 없다".

---

## 3. 축별 최종 점수와 변동 사유

| 축 | 점수 | 사유 |
|---|---|---|
| RB-1 계약 완전성 | **5** (유지) | 새로 정의가 비는 표면·필드를 찾지 못했다. §2.2.1은 오히려 "어느 표면이 어느 오류를 내는가"라는, 2회차까지 어느 절도 명시하지 않던 정보를 계약으로 끌어올렸다. §2.8.1은 §2.8이 남긴 경계 형태의 빈칸(2회차 N-2)을 닫았다 |
| RB-2 계약 일관성 | **4 → 5** | 2회차의 유일한 감점 사유(N-1)가 해소됐고, 그 과정에서 지적 범위 밖 4유형의 누락까지 정정됐다. 결정적으로 §2.2.1이 오류↔표면 대응의 **단일 정의 지점**이 되고 MV-30이 세 자산의 양방향 일치를 기계로 강제하므로, 앵커 5("모든 값이 단일 정의 지점을 갖고 … 서로 모순 없이 교차 참조")를 문서 서술이 아니라 실행 가능한 판정으로 충족한다. 내가 독립 실행해 위반 0건을 확인했다 |
| RB-3 설계 정합 | **5** (유지) | 이번 추가가 TRD 결정과 충돌하지 않는다. §2.8.1은 D-6(플랫폼·계층 분리)과 TRD `## Console 읽기 전용 경계 유지`를 강화하는 방향이고, §2.2.1·MV-30은 §9.1 표면 계약의 집행 수단이다. D-1~D-9 집행 상태 무변경 |
| RB-4 범위 준수 | **5** (유지) | 신규 조항의 비목표·Phase 경계 침범 0건. §2.8.1 말미가 "Phase 2에서 UTC 사건을 집계하며 시간대별 겹침을 계산할 때도 이 경계를 유지한다"로 Phase 2를 **끌어오지 않고 경계만 선언**해 §3.4와 같은 종결 조항 형태를 유지한다. MV-30·§2.2.1도 기존 17개 표면의 오류 집합을 정리한 것이지 표면을 늘리지 않았다 |
| RB-5 근거 인용 충실도 | **5** (유지) | §2.8.1이 `config.py` @header의 기존 경계 선언("`stats.py`는 설정을 읽지 않는다")을 인용해 새 조항을 기존 규범에서 도출하고, §2.2.1의 공통 2종은 §3.2·§2.7을 근거로 단다. MV-30은 §2.2.1과 제안서 §9.1을 근거로 단다. 인용 오기 0건 |

---

## 4. 종료 판정

### (1) 미해결이 0건인가 — **예, 0건이다**

| 회차 | 제기 | 해소 | 범위 밖 이월 | 잔여 |
|---|---|---|---|---|
| 1회차 | U-1~U-10 (10건) | 10 | 0 | 0 |
| 2회차 | N-1~N-4 (4건) | 3 | 1 (N-3) | 0 |
| 3회차 | — | — | — | **0** |

N-3만 "범위 밖 이월"이며, 이는 미해결 설계 항목이 아니라 **다른 자산(`backlog-tool`)의 확인된 결함**이다. 본 태스크의 산출물·계약에는 영향이 없고(`backlog.json`이 기계 SSOT이며 정상), 이 태스크에서 손대면 오히려 C-5를 어긴다.

### (2) 남은 항목의 성격 — 설계 확정을 막는 것 0건

아래 2건은 미해결이 아니라 **인수인계 관찰 사항**이다. 설계 확정을 막지 않으며 구현 태스크에서 자연히 닫힌다.

| ID | 관찰 | 성격 | 닫히는 지점 |
|---|---|---|---|
| O-1 | §2.8.1 B-3의 준수 지점은 라우터 2파일의 통계 호출부다(`routers/dashboard.py:225`, `routers/tasks.py:677` 및 같은 파일 정적 집계 호출). 좁히기를 빠뜨린 채 3필드 값을 그대로 넘기면 `stats.py:110`의 `start, end = quiet_hours` 언패킹에서 실패한다 — 즉 **실수가 조용히 통과하지 않고 기존 테스트에서 즉시 드러난다** | 비차단. 계약은 이미 조항으로 고정했고 판정도 MV-25·MV-26에 있다 | T06(시간계 구현) |
| O-2 | N-3의 `backlog-tool` 결함(`BACKLOG.md` 머리말 "최종 갱신"이 마커 바깥이라 초기 시각에 고정)은 별도 추적이 필요하다. 이 태스크의 백로그 산출물을 읽는 사람이 갱신 시각을 오해할 수 있다 | 비차단. 기계 SSOT(`backlog.json.updated_at`)는 정확하다 | 프레임워크 개선 항목(이월 확인 권고) |

### (3) 축별 최종 점수와 통과선 — 전 축 5점, 전건 충족

`CONTRACT.md` §5 통과선 3조 모두 충족: ① 전 축 4점 이상 ✔ ② 2점 이하 축 없음 ✔ ③ RB-3·RB-4가 3점 초과 ✔.

**설계 루프 종료 판정: 통과.** 3회차에 걸쳐 제기된 14건(U 10 + N 4)이 전건 처리됐고, 마지막 회차에서 제기된 새 설계 이슈는 없다. 판정 대상 4개 산출물과 2개 기계가독 자산(`surfaces.json`·`backlog.json`)이 서로 모순 없이 교차 참조되며, 그 일치가 MV-30이라는 실행 가능한 판정으로 고정됐다.

---

## 5. drift 필요성

- **판정: no.** 이번 회차도 확정 조항의 값·판정을 바꾸지 않았다(§2.3 diff 검증). 계약 개정 절차나 거버넌스 에스컬레이션 대상 없음.
- 본 에이전트는 판정만 반환한다. 종료 선언과 다음 단계(D7 잠금·구현 진입) 결정, O-1·O-2의 인수인계는 PM(오케스트레이터)의 책임이다.
