# GC CONVENTION REPORT — 20260915-1302

<!--
  체크박스 5단계 상태 기호 (APPLY 단계가 기입):
  [ ]  open    — 미처리 (신규)
  [x]  done    — 적용 완료  ← 주석: 적용 시각 YYYY-MM-DD HH:mm + 수정 요약
  [~]  pending — 보류       ← 주석: 보류 사유
  [?]  review  — 확인 필요  ← 주석: 판단 근거 / 해결 방안
  [!]  failed  — 실패       ← 주석: 실패 사유 / 권장 대안
-->

## 1. 헤더

- 실행 일시: 2026-09-15 13:05 (KST)
- 범위: `Console BE` / 대상 파일 4개
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` 존재 확인 — 단일 진입점(허브+링크 없음). Console BE(`dashboard/backend/`) 영역 상세 링크 문서 없음 → 허브 공통 규칙 전체 적용 (`opal/core/references/conventions-hub-model.md` 규약에 따라 상세 링크 미정의 영역은 허브 전체 적용)
- 보조 참조: `docs/PROJECT.md` §프로젝트 구성(Console BE = Python·FastAPI·uvicorn) 확인. `~/.opal/community-skills/getsentry/code-review/SKILL.md`는 현재 환경에 설치되어 있지 않아(경로 부재) 카테고리 8(코드 품질) 보조 참조를 적용하지 못했다 — 이 결여는 위반 판정에 영향 없음(advisory 자료 부재일 뿐)
- APPLY 수행 여부: N (본 에이전트는 read-only 진단 전담, 소스 파일 미수정)
- 대상 파일: `dashboard/backend/config.py`, `dashboard/backend/routers/tasks.py`, `dashboard/backend/routers/dashboard.py`, `dashboard/backend/tests/test_config.py` (전량 존재 확인, `project_root` 이탈 없음, `git diff`로 변경분 전체 확인 — checked_files == target_files)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 0 |
| 심각도 분포 | Critical 0 / High 0 / Medium 0 / Low 0 / Info 0 |
| 자동 수정 가능 | 0 |
| 수동 조치 필요 | 0 |
| 파일별 상위 Top 5 | 해당 없음 (위반 0건) |
| 카테고리별 빈도 | 해당 없음 |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 0 (빈도 0건 + 새 카테고리 0건) |
| 관찰(위반 아님, 참고용) | 3건 |

**결론**: `docs/CONVENTIONS.md`에서 근거를 댈 수 있는 위반은 발견되지 않았다. `docs/CONVENTIONS.md`는 OPAL 프레임워크 자체(스킬/에이전트 네이밍, @header 규칙, Citation Rules, 커밋 규칙, State 관리 등)를 규율하는 문서이며, Console BE(Python/FastAPI) 코드의 일반 스타일(변수·클래스 네이밍 세부, import 정렬, 타입 설계 패턴 등)에 대한 구체 규칙은 포함하지 않는다. 이번 변경의 성격상 특히 봐 달라고 요청된 6개 지점을 아래와 같이 확인했다.

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

### High (0건)

### Medium (0건)

### Low (0건)

### Info (0건)

해당 없음 — `docs/CONVENTIONS.md` 근거로 판정 가능한 위반이 없다.

---

### 요청 지점별 확인 결과 (위반 아님 — 근거 문서 없음/충족 확인)

| # | 요청 지점 | 확인 결과 | 근거 |
|---|---|---|---|
| 1 | `QuietHours` 타입 설계(tuple 서브클래스 + 부착 속성) | `docs/CONVENTIONS.md`에 Python 타입 설계 패턴(tuple 서브클래스 vs NamedTuple 등)에 대한 규칙 없음 → 위반 판정 불가, 관찰로만 하단 기재. `__eq__`·`__hash__` 미오버라이드 확인(코드 재확인 결과 일치) — `config.py:168-202` | `docs/CONVENTIONS.md`에 해당 절 없음 |
| 2 | @header 규칙(사실 서술·이력 미기재, task 태그) | 4개 파일 모두 `@header`에 현재 사실만 기술, 신규 태스크 번호 태그 추가 없음 확인. `routers/tasks.py`·`routers/dashboard.py`의 `[T103]`·`[T103 R-21]` 태그는 `git diff` 확인 결과 이번 변경으로 신규 추가된 것이 아니라 HEAD에 이미 존재하던 컨텍스트 라인(diff에서 앞뒤 문맥으로만 등장, `-`/`+` 표시 없음) — 위반 아님 | `docs/CONVENTIONS.md` §구현 규칙 > @header 규칙 |
| 3 | 레이어 경계 (config가 라우터/모델/캐시 미import, 라우터는 주입만) | `config.py` import 목록: `json, os, re, threading, pathlib, dataclasses, zoneinfo` — 라우터·모델·캐시 미포함, 준수. 라우터(`tasks.py:570-597`, `dashboard.py:120-135`)는 `load_quiet_hours()` 반환값을 `(start_minute, end_minute)`로 좁혀 전달만 하며, `timeZone` 검증·IANA 해석(`_resolve_time_zone`)은 `config.py` 내부에만 존재 — 설정 해석 로직이 라우터로 유출되지 않음, 준수 | `docs/CONVENTIONS.md`에 레이어 경계 명시 절은 없으나(관측 근거만) 위반 사실 자체가 없음 |
| 4 | 플랫폼 분기 (OS·플랫폼 조건문 신규 여부) | 4개 파일 `git diff` 전체에서 OS/플랫폼 조건 분기 신규 추가 없음 확인 (`zoneinfo` 표준 라이브러리 사용만 추가) | `docs/CONVENTIONS.md` §구현 규칙 > 플랫폼 분기 격리 (해당 절은 스킬·에이전트 본문 대상으로 범위가 좁으나, 확인 결과 위반 사실 자체가 없어 무관) |
| 5 | 테스트 관례(`OPAL_SETTING_PATH` tmp_path monkeypatch 격리) | 신규 헬퍼 `_isolate_quiet_hours()`(`test_config.py:465-479`)가 기존 `TestLoadQuietHours._isolate`와 동일하게 `monkeypatch.setattr(config_module, "OPAL_SETTING_PATH", global_path)`로 tmp 경로 격리. 신규 2클래스(`TestQuietHoursTimeZoneMerge`, `TestQuietHoursTokenTimeZoneSignature`) 전 테스트가 이 헬퍼 또는 순수 인메모리 `QuietHours` 생성만 사용 — 실제 `~/.opal/setting.json` 접근 없음, 관례 준수 | `docs/CONVENTIONS.md`에 해당 절 없음(기존 코드 관례와의 일치만 확인) |
| 6 | 네이밍 (Python 네이밍 · CONVENTIONS.md 규칙 일치) | 파일명 전량 snake_case 유지(변경 없음), `docs/CONVENTIONS.md` §네이밍 규칙(파일/폴더)의 유일한 Python 관련 규정("Python 파일은 snake_case")과 일치. 클래스·함수·변수 세부 네이밍(PascalCase 클래스명 등)에 대한 프로젝트 규칙은 `docs/CONVENTIONS.md`에 없음(OPAL 컴포넌트 네이밍 체계는 `opal-*`/`op-*` 스킬·에이전트 전용이라 Console BE 일반 코드에는 미적용) | `docs/CONVENTIONS.md` §네이밍 규칙 |

---

### 관찰 (참고용 — 위반 아님, 근거 문서 없음)

- OBS-01 [`dashboard/backend/tests/test_config.py:465-479`, `669-694`] RED 단계 서술 잔존 가능성
  - 설명: 신규 테스트 2클래스의 클래스 docstring이 "GREEN 구현 전이므로 이 시점의 config.py에는 QuietHours 타입도 ... 없다 — 아래는 전부 RED다(구현 전)"(469행), "현재는 QuietHours 자체가 없으므로 ImportError가 정상(RED)이다"(669행)로 기술되어 있으나, 같은 변경분의 `config.py`에 `QuietHours`가 이미 구현되어 있어(168행) 이 서술은 현재 diff 기준으로는 더 이상 사실이 아니다.
  - 근거를 댈 CONVENTIONS.md 절 없음(테스트 docstring의 시제 정합성에 대한 프로젝트 규칙 부재) → 위반 아닌 관찰로 분류.
  - 참고: 기능적으로는 무해(테스트는 통과 대상 값을 검증하므로 GREEN에서도 정상 동작) — 다만 RED-first 관례를 따르는 이후 독자가 이 서술만 보고 미구현 상태로 오인할 수 있어 opds 후속 단계에서 GREEN 반영 문구로 정리 여부 확인을 권장.

- OBS-02 [`dashboard/backend/config.py:6`(헤더) vs `config.py:168-202`(클래스 docstring)] 동일 설명 중복
  - 설명: `QuietHours`의 동등성·해시 계약 설명("`__eq__`·`__hash__`는 오버라이드하지 않고 ... 완전히 호환된다")이 파일 상단 `@header` JSON의 `description` 필드와 클래스 자체 docstring에 거의 동일 문장으로 중복 기재되어 있다.
  - 근거를 댈 CONVENTIONS.md 절 없음(중복 문서화 금지 규칙 부재) → 위반 아닌 관찰.
  - 참고: `@header` §구현 규칙은 "현재 사실만 기재"만 요구하며 중복 자체를 금지하지 않아 위반 아님. 가독성 측면에서만 참고.

- OBS-03 [환경 제약] getsentry/code-review 보조 참조 자료 부재
  - 설명: `~/.opal/community-skills/getsentry/code-review/SKILL.md` 경로가 현재 실행 환경에 존재하지 않아 카테고리 8(코드 품질) advisory 체크에 활용하지 못했다.
  - 대체 조치: `ruff check`(프로젝트 lint 설정 파일 없음 — ruff 기본 규칙셋)로 4개 파일 정적 검사, 전량 통과(`All checks passed!`). `python3 -m py_compile` 구문 검사도 전량 통과.
  - 근거를 댈 CONVENTIONS.md 절 없음 → 위반 아닌 관찰(환경 제약 기록용).

---

## 4. 문서 업데이트 제안 (§9·§10, 트리거 발동 시만)

트리거 미발동 — 위반 0건이므로 빈도 트리거(N=3 이상 파일)·새 카테고리 트리거 모두 해당 없음.

---

## 5. 문서 작성 유도 (해당 시)

`docs/CONVENTIONS.md` 존재 — 작성 유도 생략.

---

## 참고: 실행 로그

- `git -C {project_root} diff -- dashboard/backend/config.py dashboard/backend/routers/tasks.py dashboard/backend/routers/dashboard.py dashboard/backend/tests/test_config.py` 로 변경분 전량 확인
- `ruff check <4개 파일>` → `All checks passed!` (프로젝트 lint 설정 파일 없음, ruff 기본 규칙 적용)
- `python3 -m py_compile <4개 파일>` → 구문 오류 없음
- trailing whitespace / tab 혼용 / EOF 개행 누락 grep 점검 → 전량 이상 없음
- `test_routers.py` 실패 36건은 지시에 따라 판정 대상에서 제외(워크트리 환경 의존 결함, 별도 태스크 분리분)
