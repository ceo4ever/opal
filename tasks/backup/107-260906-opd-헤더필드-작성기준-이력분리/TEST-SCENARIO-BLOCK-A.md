# TEST SCENARIO — Block A 선작성 원문 (보존본)

> 보존 목적: 목표-커버 게이트 iteration 1의 **G-7**(Block A 원문 미보존 → 흡수 14건 중 9건 대조 불가 → 판정 불능) 해소.
> 이 파일은 Block B 보강 **이전**의 `TEST-SCENARIO.md` 원문이며, PM이 2026-09-06 세션에서 작성한 그대로다. 이후 수정하지 않는다(감사 기준선).
> 흡수·수정·생존 판정의 대조는 `TEST-SCENARIO.md` 상단 대조표와 이 파일을 나란히 두고 수행한다.

> 작성일: 2026-09-06 | 상태: Block A 선작성 완료 / Block B 보강 대기 (당시 상태)
> 작성자: PM + 캡틴 페어 | 도출 입력: **TASK.md 단독** — PLAN.md 미독(관점 편향 차단, `harness/red-first.md` §1.6)

> **선작성 트랙 채택 근거**: 이 태스크의 핵심 목표(「이력은 @header에 남기지 않는다」 규정 신설)는 **파괴 관점으로 환원되지 않는다** — R-1·R-2는 문서 개정이라 "깨질 수 있는 계약"이 없어 리스크 가설 표만으로 도출하면 목표 달성 검증이 통째로 누락된다. 또한 「헤더 내 이력 라인 → git」은 **교체형 목표**여서 채택/잔존 축(⑤)이 실재한다.

## Block A 16시나리오 원문 요약표

> 각 시나리오의 원문 필드(가설 매핑·도출 계열·대상·계층·실행 방식·조건·기대 결과·도구)를 보존한다. 당시 「가설 매핑」은 전건 `<!-- PENDING-BLOCK-B -->`였다.

| ID | 제목 | 도출 계열(축) | 대상 | 계층 | 방식 | 조건 | 기대 결과 |
|----|------|--------------|------|------|------|------|----------|
| S-A1 | 4필드 각각에 「담는 것 / 담지 않는 것 / 예시」 3요소가 존재한다 | ① 목표달성 · ② 요구커버(R-1 AC(a)) | `header-standard.md` 신설 4필드 가이드 | L1 | M1 | 개정 후 문서를 파싱해 4필드 각각의 가이드 블록 추출 | 4필드 × 3요소 = 12개 요소 전건 존재. 하나라도 빠지면 FAIL |
| S-A2 | `description`·`note`의 「담지 않는 것」에 변경 이력·태스크 번호가 명시된다 | ② 요구커버(R-1 AC(b)(c)) | 4필드 가이드 중 `description`·`note` 행 | L1 | M1 | 두 필드의 「담지 않는 것」 셀 본문 추출 | 양쪽 모두 「변경 이력」·「태스크 번호」 명시. `note`가 이력의 **대체 저장소가 아님**이 함께 적힘 |
| S-A3 | 기존 §4 `exports` 가이드 원문이 바이트 단위로 무변경이다 | ⑥ 경계·부정(R-1 AC(e)) | `header-standard.md` §4 | L1 | M1 | 개정 전 HEAD의 §4와 개정 후 §4 추출·대조 | 바이트 동일. 4필드 가이드는 §4를 고쳐 쓰는 게 아니라 **별도 절로 신설** |
| S-A4 | 「이력은 git이 갖는다」 원칙이 근거 원문 인용과 함께 명문화된다 | ① 목표달성 · ② 요구커버(R-2 AC(a)(b)) | `header-standard.md` + `header-rules.md` | L1 | M1 | 두 문서에서 원칙 문장 추출 | (1) `opal-doc-standard.md:28` 원문 인용 (2) 이력 소재 **2곳**(git 로그·태스크 폴더 DONE.md) 명시 |
| S-A5 | 경고가 발생해도 exit code가 변하지 않는다 (비차단) | ② 요구커버(R-3 AC(a)) | `code-scan.js` `validate` | L1 | M1 | 이력성 `description` 픽스처만 있고 차단 위반 0건인 저장소에서 `validate` 실행 | 경고 출력되지만 **exit 0** · `ok: true` 유지 |
| S-A6 | 경고가 차단 채널이 아닌 비차단 채널로 분류된다 | ② 요구커버(R-3 AC(b)) | `code-scan.js` `blockingViolations` 필터 | L1 | M1 | `validate --json` 출력 파싱 | 신규 경고가 `blockingViolations`에 **미포함**, `counts`에 독립 집계 필드로 등장 |
| S-A7 | 진성 18건을 전건 탐지하고 `F-00X`·`TS-0XX` 오탐 0건이다 | ① 목표달성 · ② 요구커버(R-3 AC(c)) | R-3 신설 감지 로직 | L2 | M1 | **정리 전** 코퍼스(109파일) 전수에 감지 로직 적용 | 탐지 집합이 진성 18건과 **정확히 일치**. 순수 오탐 9건 전건 미탐지. 경계 3건은 PLAN 방침대로 판정 |
| S-A8 | 정리된 18건의 `@header` JSON 파싱이 전건 정상이다 | ② 요구커버(R-4 AC(d)) | 정리 대상 18파일 | L1 | M1 | 정리 후 `code-scan scan --json`으로 재파싱 | 18건 전건 파싱 성공 + `description`이 비어 있지 않음(제거가 아니라 축약) |
| S-A9 | 정리 과정에서 `exports`·`module`·`layer`·`domain`이 변하지 않는다 | ⑥ 경계·부정(R-4 AC(e)) | 정리 대상 18파일 | L1 | M1 | 정리 전 HEAD와 정리 후의 4필드를 파일별 대조 | 18파일 × 4필드 전건 동일. 하나라도 다르면 FAIL |
| S-A10 | `note` 2건의 이력성 서술도 함께 제거된다 | ② 요구커버(R-4 AC(f)) | `code-map-hook.js`·`code-scan.js`의 `note` | L1 | M1 | 정리 후 두 파일의 `note` 값을 감지 로직에 통과 | 두 `note` 모두 이력성 판정 해제. `description`만 고치고 `note`를 남기면 FAIL |
| S-A11 | 회귀 — code-scan 344 / state-tool 400 유지 | ② 요구커버(R-5 AC(a)~(d)) | 두 테스트 스위트 | L2 | M1 | 전 변경 반영 후 실행 | code-scan 344 passed/0 failed · state-tool 400/3/0 · 메타 3건 통과 · `--changed` 차단 증가 0건 |
| S-A12 | [잔존 0] 「헤더 내 변경이력 라인」 규정 문구가 프로젝트 전역에서 사라진다 | ⑤ 채택·잔존(구형 잔존 0) · R-2 AC(d)(e) | `header-standard.md`·`header-rules.md`·`docs/CONVENTIONS.md` 3문서 | L2 | M1 | 3문서 전문에서 「헤더 내 변경이력」 계열 지시문 검색 | **0건**. 세 문서가 서로 다른 말을 하지 않음. `CONVENTIONS.md:218` 어구 교체됨 |
| S-A13 | [채택] 신설 가이드가 실제 워커 경로에 도달한다 | ⑤ 채택·잔존(신형 채택) | `header-rules.md` → `header-standard.md` 4필드 가이드 참조 경로 | L2 | M1 | EXECUTE 워커가 실제로 읽는 문서에서 신설 가이드로의 참조 존재 확인 | 참조 존재. **규정만 신설하고 도달 경로가 없으면 워커는 그 규정을 영영 읽지 않는다** — 이 경우 FAIL |
| S-A14 | [부정] 이력이 없는 정상 파일은 경고되지 않는다 | ⑥ 경계·부정 | R-3 감지 로직 | L1 | M1 | (1) 태스크번호 0개 (2) 임계값 바로 아래 (3) `description` 필드 부재 (4) `description` 빈 문자열 4종 픽스처 | 4종 전건 무경고. 특히 (3)(4)에서 예외·크래시 미발생 |
| S-A15 | [부정] 정리로 사라지는 이력이 git에 실재함을 확인한 근거가 산출물에 남는다 | ② 요구커버(R-4 AC(c)) · ⑥ 경계·부정 | EXECUTE 산출물 | L2 | M1 | 18파일 각각 제거된 태스크번호가 git 로그 또는 DONE.md에 실재하는지 확인한 기록 검사 | 18건 전건 확인 근거 존재. **미확인 1건이라도 있으면 FAIL** |
| S-A16 | 정리된 `description`이 여전히 파일의 역할을 알려주는가 [SUPERVISOR] | ① 목표달성(R-4 AC(b) 역할 한 줄 보존) | 정리 후 18파일의 `description` | L3 | **M3** | 캡틴이 정리 전/후 대조. 특히 `state_tool.py` 11791자 → 축약본 | 처음 보는 사람이 정리 후 `description`만으로 파일 역할을 파악 가능. 이력만 빠지고 역할이 남아야 함 |

## 당시 Block A 자기 점검 (원문)

| 항목 | 판정 |
|------|------|
| PLAN.md를 읽지 않고 도출했는가 | ✅ TASK.md 단독 |
| 축 ① 목표달성 시나리오 존재 | ✅ S-A1 · S-A4 · S-A7 · S-A16 |
| 축 ② 요구커버 — R-1~R-5 전건 매핑 | ✅ |
| 축 ⑤ 채택·잔존 시나리오 존재 | ✅ S-A12(잔존 0) · S-A13(채택) |
| 축 ⑥ 경계·부정 최소 1건 | ✅ S-A3 · S-A9 · S-A14 · S-A15 (4건) |
| 보강 대기 마커 배치 | ✅ §1 리스크 가설 표 · §2 · §4 가설 ID 열 · 각 시나리오 가설 매핑 행 |
| 목표-커버 게이트 호출 | ⬜ 미호출(선작성 시점 호출 금지 — `scenario-gate.md` §4) |

## 당시 §1·§2·§4 상태

- §1 리스크 가설 표: 전체 `<!-- PENDING-BLOCK-B -->` (PLAN 미독이므로 H-N 없음)
- §2.1·§2.2: 전체 보강 대기
- §4 매핑 표: 19행, 「가설 ID」 열 전건 보강 대기. AC ID는 R-1 AC(a) ~ R-5 AC(a)~(d) + `(F-NNN 커버)` 1행
- 보강 대기 마커 총 39개
