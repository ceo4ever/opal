# SCENARIO-GATE 판정 — iteration 2

> 실행: 2026-08-21T07:54:33Z | phase: `scenario-rubric` | pilot: opds
> 입력: `tasks/098-260821-opds-근거등급-확정판정-트랙강등/TEST-SCENARIO.md` (35건) + `.scenario-coverage-input.json`
> 기준 SSOT: `opal/core/references/harness/scenario-gate.md` §2(6축)·§5-1(종료조건)
> 채점 범위: 판단축 ①목표달성 ⑤채택·잔존 ⑥경계·부정 — ②③④는 test-tool 결정론 소관(재판정 안 함)

## 결론

- **verdict: pass** — goal 2 / adoption 2 / boundary 2, 평균 **2.0** (§5-1 임계: 각 축 ≥1 AND 평균 ≥1.5 충족).
- iteration 1 gaps **G-1·G-2 모두 실측으로 해소 확인**. 비차단 권고 2건(S-34·S-35)도 흡수됐다.
- 미달 축 0건이므로 `gaps[]`는 비어 있다. 단 **정정 5건(C-1~C-5)**은 EXECUTE 진입 전 반영 대상이다 — 특히 C-1은 iteration 1 본 평가자의 지시 오류에서 비롯됐다.

---

## 1. 판단축별 판정

| 축 | 점수 | 근거 (실측) |
|----|------|------------|
| ① 목표 달성 | **2** (iter1: 1) | 목표 문장 3절 전건에 목표-계층 시나리오가 존재하고, 핵심 절(도구 집행)에 **in-task 자동 경로가 신설**됐다. S-31 전제를 직접 검증했다 — 본 태스크 `TASK.md`는 `[결정]` 태그 14항(`TASK.md:59-72`) + `## 명확화 결과` 4행(`:86-92`)을 갖춘 신 스키마 실파일이며, 저장소 내 유일하다. `의존 사실` 4셀 모두 백틱 경로 스팬을 보유하므로 `citation_missing` 0건이 성립한다(PLAN `:195` "백틱 스팬 0건일 때만 부여"). 3셀은 디렉토리 보유 경로(`opal/skills/opal-pilot-dev/SKILL.md`·`opal/tools/state-tool/state_tool.py:2225`·`.opal/MEMORY.json`)로 등급 패턴 매핑 대상이고, 실존 확인됨(`state_tool.py` 총 2618줄 > 2225). S-31의 "인용 기재 3행" 표현은 이 3셀과 정합한다. R-5(강등 라우터)·R-7(경량화 체감)의 L3 잔존은 타당하다 — F-004는 도구 코드가 아니라 오케스트레이터 산문 배선이므로 실행 경로가 사람 발화다. |
| ⑤ 채택/잔존 | **2** (iter1: 1) | **잔존 면** — S-11(R-2)의 교체 대상 실존 확인(`citation-rules.md:236-239` "인용 생략 허용 / 추론·경험 기반 결정") + S-32(R-7)의 1차 대상 인용 정확(`opal/skills/op-dev-plan/references/plan-guide.md:139` = "각 파일의 핵심 구현 내용을 명세한다 … **클래스/함수 시그니처와 핵심 로직**을 정의한다") + S-21·S-27(중복·복제 0건). **채택 면** — S-12(R-7 자기적용) + S-33(R-3 자기적용) 2건. S-13의 축 표기 정정이 문서(`TEST-SCENARIO.md:646`)와 정규화 페이로드 양쪽에 반영됐다(페이로드 실측: S-13에 `is_adoption_scenario` 부재, adoption 플래그 = S-11·S-12·S-32·S-33 4건). |
| ⑥ 경계/부정 | **2** (iter1: 2 유지) | 부정 3건(S-15 E5 단독·S-16 경로 부재·S-2 인용 부재) + 경계 4건(S-14 미매칭·S-19 상호배타·S-20 임계 3점·S-26 E1·E3) + **과잉 차단 대조군 3건**(S-17 결정 무인용 확정 유지 / S-34 정규 4형식 통과 / S-35 E5 동반 통과). 오강등 방향과 오통과 방향이 모두 대조군을 갖는다. |

**계산**: (2 + 2 + 2) / 3 = **2.0** ≥ 1.5, 0점 축 없음 → `verdict: pass`.

---

## 2. iteration 1 gaps 해소 확인

| gap | 처리 | 실측 판정 |
|-----|------|----------|
| G-1 ①축 in-task 자동 경로 | S-31 신설(L2, `FX-SELF`) + S-1·S-3 면제 근거를 "R-6 실작동 한정"으로 축소(`TEST-SCENARIO.md:13`, `:605`) | **해소** — 면제 범위 축소는 사실에 부합한다. R-4 도구 집행은 실파일로 in-task 검증 가능하고, R-6 거동은 후속 태스크가 없어 불가하다 |
| G-2 ⑤축 R-7 잔존·R-3 채택 | S-32·S-33 신설, S-13 축 표기 정정 | **해소** — 단 S-32 대상 목록에 C-1, S-33 단언 깊이에 C-3 |
| 비차단 (a) 형식 변형 | S-34 신설 + H-13(P0) 신규 가설 등재(`PLAN.md:61`, `TEST-SCENARIO.md:37`) | **흡수** — 가설 표까지 연동됐고 H-1~H-12는 무접촉 |
| 비차단 (b) E5 동반 | S-35 신설(양성 대조군) | **흡수** |

---

## 3. 정정 지시 (비차단 — 점수 미달 아님, EXECUTE 진입 전 반영)

### C-1 [필수] S-32 대상 문서 목록이 실제 교체 대상을 빗나간다 — 평가자 지시 오류 정정

iteration 1 G-2가 "`op-dev-analysis/SKILL.md`에도 동일 검사를 병기하라"고 지시했고 Producer는 그대로 반영했다. **그 지시가 틀렸다.** 실측:

- `grep -n "핵심 로직\|핵심 구현 내용" opal/skills/op-dev-analysis/ opal/skills/op-dev-plan/` → 히트 3건, **전건이 `op-dev-plan` 쪽**이다. `op-dev-analysis/SKILL.md` 히트 **0건**.
- 미포착 잔존 지점: **`opal/skills/op-dev-plan/SKILL.md:204`** = `{핵심 로직 흐름, 함수/클래스 시그니처, 외부 의존성}`. 이 파일은 Step 3 대상 파일 목록에 이미 포함돼 있다(`PLAN.md:315`).
- 현행 S-32는 히트 0건 파일을 검사하므로 **공허하게 PASS**하고, 진짜 잔존은 통과시킨다 — S-32가 막겠다고 선언한 신·구 공존 구멍이 그대로 남는다.
- 추가 오탐 주의: `plan-guide.md:96`("기존 코드 구조, 핵심 로직 흐름 파악")은 ANALYSIS 절 서술이지 설계 절 구 조항이 아니다. grep 범위를 §3.N.2 설계 절로 한정하지 않으면 정상 서술을 잔존으로 오판해 FAIL한다.

→ S-32 대상을 `plan-guide.md`(§3.N.2 한정) + `op-dev-plan/SKILL.md:204`로 교체하고, `op-dev-analysis/SKILL.md`는 제외하라.

### C-2 [권고] S-31의 오라클이 존재 수준에 머문다

기대 결과가 "exit 0 + 등급 부여 + `confirmed_ratio` 산출 + 3행 `citation_missing` 아님"이다. PLAN §3.3.2 계약을 대입하면 결과가 예측 가능하다 — `목표` 행의 `` `citation-rules.md` §2 ``는 디렉토리 없는 파일명이므로 `unknown`(PLAN `:191`), 나머지 3행은 E4/E2 부여. 즉 **행별 판정과 `confirmed_ratio` 기대값(3/4)까지 단언 가능**하다. 현행 문구는 전 셀이 `unknown`으로 반환되는 퇴화 구현도 "등급 부여"로 통과시킬 수 있다. 목표 문장 3절이 "확정률을 트랙 판정 입력으로 삼아"이므로, 확정률의 **값**을 실파일에서 고정하는 것이 ①축의 실질이다.

### C-3 [권고] S-33의 두 단언이 현 산출물에서 자동 충족된다

실측 — 확정 방향 14항은 **전건이 `[결정]`** 태그이고(`TASK.md:59-72`), `의존 사실` 4셀에 `-` 는 **0건**이다(`:88-92`). 따라서 "14항 전건 태그" · "`-` 허용 조건 미위반" 두 단언은 검사 없이 성립한다. 실질 채택 면은 다음이다:

- 확정 방향 3·4·8항은 본문에 `` [사실] 근거: `` 를 포함하는데, **3항·8항은 인용 0건**이다(3항은 타 프로젝트 경로 기재 금지 정책과 충돌, 8항은 저장소 내 검증 가능 사실인데 무인용). R-2 AC(a)("사실 주장의 인용 생략 불허")의 자기위반 후보이며 현행 S-33은 이를 검출하지 않는다.
- `범위` 행 사실 "`opal/skills/opal-pilot-dev/SKILL.md` 419줄 내 미존재"는 실측 **420줄**과 불일치한다. 사실 진술의 *정확성* 축은 35건 중 어느 시나리오도 검사하지 않는다.

→ S-33에 "`[사실]` 태그 근거의 인용 존재(또는 면제 사유 명시)" 단언을 추가하면 채택 면이 실효를 얻는다.

### C-4 [PM 라우팅] `grade_unknown`의 성격이 PLAN 내부에서 충돌한다

`PLAN.md:190`은 "`citation_missing`이 아니라 `grade_unknown`으로 라우팅해 PM 판단에 넘긴다 — 차단하지 않기 위한 방향"이라 하고, `:196`은 `grade_unknown`을 **미확정 사유 코드 4종**에 넣는다. 미확정이면 `confirmed_ratio`가 내려가고 이는 트랙 강등 A축 임계에 직접 들어간다 — 목표 문장 3절의 메커니즘이다. 이 저장소 문서 관행상 `` `파일명` §N `` 표기가 흔하므로 `unknown` 발생률이 구조적으로 높다. 계약이 확정되지 않으면 S-14·S-31·S-34의 기대 결과가 서로 다른 전제 위에 선다. 본 게이트 채점 대상은 아니며(계약 판정은 PM 소관), 판정만 반환한다.

### C-5 [경미] 문서 내부 정합

`TEST-SCENARIO.md:701` PM Gate 마지막 항목이 "목표달성 시나리오 3건(S-1·S-3·S-30)"으로 남아 S-31이 빠졌다. §4 매핑 표(`:641`)·페이로드는 S-31을 목표 축으로 계상하고 있어 불일치다.

---

## 4. 판정 주체 경계 확인

- ②요구커버·③기능커버·④리스크커버는 채점하지 않았다 — test-tool `scenario-coverage-check` 결정론 소관(`scenario-gate.md:21-23,27`). 선행 결과(exit 0 / requirements 7·features 5·hypotheses 13·scenarios 35)는 참고로만 확인했고 재판정하지 않았다.
- 본 보고서는 verdict-only다. `TEST-SCENARIO.md`·`PLAN.md`·`TASK.md`를 수정하지 않았다. C-1~C-5 반영은 Producer(PM + 캡틴 페어) 책임이다(`scenario-gate.md:73,77`).
- 게이트 PASS는 결정론 exit 0과 본 판단축 verdict pass가 **모두** 성립할 때 선언된다(`scenario-gate.md:93-99`). iteration 2에서 양쪽이 성립한다.

## 5. 결과 계약

```json
{"scores": {"goal": 2, "adoption": 2, "boundary": 2}, "average": 2.0, "gaps": [], "verdict": "pass"}
```
