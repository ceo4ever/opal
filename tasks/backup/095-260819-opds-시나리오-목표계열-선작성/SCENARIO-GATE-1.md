# SCENARIO-GATE-1: 목표-커버 게이트 판단축 채점 (scenario-rubric)

> 실행 일시: 2026-08-19T18:50:00+09:00 | phase: `scenario-rubric` | iteration: 1
> 판정자: opal-evaluator-agent (verdict-only · mutate 금지)
> scenario_source: `tasks/095-260819-opds-시나리오-목표계열-선작성/TEST-SCENARIO.md` (S-1~S-16, 16건)
> 참고 입력: `TASK.md`(목표·R-1~R-6·제약 7종) / `PLAN.md`(F-001~F-006·H-a~H-i·TS-001~029·§규칙 소유권 표) / `.scenario-coverage-input.json`
> 기준 SSOT: `opal/core/references/harness/scenario-gate.md` §2(6축·판정주체 분리)·§5-1(종료조건 임계)
> CONTRACT.md: 부재(opds 파이프라인) — `scenario-rubric`은 CONTRACT 루브릭절 병합 대상이 아니므로 판정 영향 없음

## 판정 범위 [MUST]

본 보고서는 `scenario-gate.md:20,24,25` 판단축 **①목표 달성 · ⑤채택/잔존 · ⑥경계/부정 3축만** 채점한다.
②요구 커버·③기능 커버·④리스크 커버는 `scenario-gate.md:27` [MUST]에 따라 test-tool `scenario-coverage-check` 결정론 소관이며(exit 0 / requirements 6 · features 6 · hypotheses 9 · scenarios 16) 본 판정에서 재판정하지 않았다.

자기적용 구조(`TEST-SCENARIO.md:9-36` §0)는 **가점 요소로 취급하지 않았다**. 아래 점수는 시나리오 집합 자체의 충분성만으로 산정했으며, self-confirming 퇴행(task:004 "AC 중심 당연한 시나리오 양산") 여부를 별도 검토 항목으로 두었다.

---

## 1. 판단축별 판정

| 축 | 점수 | 통과선 | 판정 |
|----|------|--------|------|
| ① 목표 달성 | **2** / 2 | ≥1 | PASS |
| ⑤ 채택 / 잔존 | **2** / 2 | ≥1 | PASS |
| ⑥ 경계 / 부정 | **2** / 2 | ≥1 | PASS |
| **평균** | **2.0** | ≥1.5 | PASS |

### ① 목표 달성 — 2점

**앵커 대조**: "2 = 사용자·운영 계층에서 목표를 직접 검증". 두 계층이 모두 실재한다.

| 근거 | 인용 |
|------|------|
| 사용자 계층 | `TEST-SCENARIO.md:370-393` S-15 [SUPERVISOR] — 캡틴이 (1) 선작성 의도가 오해 없이 읽히는가 (2) §0 표기가 규칙과 정합한가 (3) **다음 태스크에서 이 규칙만 읽고 동일 절차를 재현할 수 있는가**를 판정한다. (3)이 목표 문장("트랙을 SSOT 문서에 신설")의 실질 성립 여부(=재현성)를 직접 겨눈다. `실행 방식 M3` 사유("규칙이 의도한 판단을 유도하는가는 사람 판정 영역", `:379`)가 명시되어 자동화 회피가 아닌 계층 선택임이 확인된다. |
| 운영 계층 | `TEST-SCENARIO.md:272-290` S-11 — 규칙 문언 존재가 아니라 **실행 증거**(AGENTIC-LOG 타임스탬프 순서 · 게이트 호출 1회)를 Then으로 요구한다. `:284` 기대 결과 ②③④가 "선작성 착수 < PLAN.md 수신 < 보강 < 게이트 호출" 순서와 호출 횟수를 검증 대상으로 고정한다. |
| 문언 검사와의 분리 | S-1~S-6(`:90-196`)은 grep 산출물 검사(②요구 커버축)로 명시 배치되고, ①축은 `:427` §4.1에서 S-15에 단독 귀속되어 있다 — 즉 집합이 "문언 추가 확인"에 ①축을 대리시키지 않았다. |

**self-confirming 퇴행 검토(감점 사유 탐색 결과 부존재)**: `:433`이 지목한 선작성 고유 3건(S-11·S-12·S-15)은 PLAN이 정의한 TS-001~029 어디에도 대응이 없다(실측: PLAN.md `:307-608` TS 전건 확인 — 채택 검증·음성통제·목표 달성 판정 TS 없음). 즉 선작성 트랙이 PLAN 관점만으로는 도출되지 않는 시나리오를 실제로 산출했고, 반대로 "AC를 그대로 되읽는 당연한 시나리오"는 S-1~S-6 6건으로 억제되어 16건 중 과반을 넘지 않는다.

### ⑤ 채택 / 잔존 — 2점

**전제**: 본 태스크는 교체형이 아니다(기존 순차 흐름 유지 + 선작성 추가, `TASK.md` D-2/D-8). 따라서 "구형 잔존 0" 반쪽은 N/A이고, 채택 반쪽의 충분성으로 판정했다.

| 근거 | 인용 |
|------|------|
| 실행 채택 | `TEST-SCENARIO.md:272-290` S-11 + `:290` 설계 의도 — "본 태스크는 신설이므로 '규칙 문언이 추가됨'은 채택 증거가 **아니다**. 규칙을 따라 실제 선작성이 일어났다는 실행 증거가 있어야 채택으로 인정한다." 070 실패모드("삭제만 하고 채택 안 함")와의 대조가 명시적으로 설계에 반영되어 있다. |
| 경로 채택 | `TEST-SCENARIO.md:200-216` S-7 (`:210` 축 = "② 요구 커버 · ⑤ 채택") — install 재배포 후 `~/.opal/` 배포본과 소스의 strip-후 diff 0을 요구한다. 프로젝트 소스만 고친 규칙은 런타임에 채택되지 않으므로, 배포 경로 검증은 채택 축의 실질 구성요소다. |
| 존재/작동 구분 | `:424-429` §4.1이 ⑤축 대상을 "완료기준 — 규칙 채택 / S-11(실행 증거 기반)"으로 못박아, 문언 존재 검사(S-1~S-6)와 채택 검증을 축 수준에서 분리했다. |

**요청받은 쟁점 — "S-11의 실행 증거가 채택 검증으로 충분한가"**: 충분하다(통과선 기준). 단 아래 §2 관찰①의 증거 강도 한계를 동반한다 — 감점선(<1점 = 채택 미검증)에는 해당하지 않으므로 점수는 2점을 유지하고 보강 권고로 분류했다.

### ⑥ 경계 / 부정 — 2점

**앵커 대조**: "2 = 경계·부정 경로 시나리오 존재". 부정 경로 2건이 실재하며 둘 다 "실패를 기대 결과로 고정"한 진성 음성통제다.

| 구분 | 근거 |
|------|------|
| 부정 ① | `TEST-SCENARIO.md:292-310` S-12 — `PAYLOAD-MISSING-B`(features·hypotheses는 선언되고 각 시나리오 covers_* 는 빈 배열, `:66`)로 `scenario-coverage-check`를 호출해 **FAIL을 기대 결과로** 요구하고, 이어 `PAYLOAD-FULL`로 PASS 수렴까지 2단 확인한다(`:80`). 페이로드 구조상 F 6건·H 9건 전건이 `missing`으로 산출되므로 FAIL 기대는 도구 의미론과 정합한다. 즉 "선작성만 하고 보강을 건너뛴 상태"가 게이트를 통과할 수 없음이 실증 대상으로 잠긴다 — 본 태스크가 신설하는 규칙의 최대 실패모드(D-3 위반)에 대한 정확한 음성통제다. |
| 부정 ② | `TEST-SCENARIO.md:348-366` S-16 — `plan.plan_md` 미완 state.json에 `advance --task-step plan.scenario_gate`를 호출해 `stage_transition_violation` + exit 1을 기대 결과로 요구한다. 도구 무변경(제약 ①) 하에서 규율 위반이 차단됨을 실증하는 경로로 유효하다. Block B 보강 시 PLAN TS-014를 흡수해 신설된 항목이며(`:32`, PLAN.md `:426`), additive-only 금지 원칙이 실제로 작동한 흔적이다. |
| 경계 7건 | S-8(`:218` `.py` diff 0 + 회귀 pass) / S-9(`:236` spec-validate 10/10 + opd 16행·opds 11행 동일 + pipeline.json diff 0) / S-10(`:254` 소유권 표 grep 매트릭스 — 정의 1곳·참조 문서 본문 0줄) / S-13(`:312` §1 diff 0 + 강제 트랙 5종 목록 동일 + 약화 예외 문언 부존재) / S-14(`:330` op-dev-plan diff 0) + 위 S-12·S-16. |
| 제약 대비 | TASK.md §제약 조건 7항 중 도구 변경 0→S-8, STATE 행 불변→S-9, RED-first 순서 불변→S-13, 배포 경계→S-7, SSOT 단일화→S-10, 변경이력 의무→S-6 6항이 실질 방어된다. S-13이 "신설 절이 §1을 약화시키는 예외 문언을 포함하지 않는다"(`:324` ③)까지 요구하는 점은 단순 diff-0을 넘어선 의미 경계 검사다. |

---

## 2. 비차단 관찰 및 보강 권고 (점수 미달 아님 — gaps 아님)

> 아래 3건은 세 축 모두 통과선을 만족한 상태에서의 품질 권고다. `gaps[]`에는 포함하지 않으며, 게이트 재작성 사유가 아니다. 반영 여부는 PM 판단이다.

**관찰 ① (⑤축 증거 강도)** — S-11 기대 결과 ②③(`:284`)의 원천이 `AGENTIC-LOG.md`이며, 이 로그는 PM 자기서술 산출물이다. 또한 PLAN.md 자체가 H-c로 "선작성 초안이 STATE 행 밖 산출물이라 추적 불가·유실"을 P1로 지목했다(`TEST-SCENARIO.md:48`). 결과적으로 "18:23 선작성" 시점을 독립 검증할 불변 아티팩트가 없다 — 실측상 파일 mtime은 `PLAN.md 18:37 < TEST-SCENARIO.md 18:44`로 선작성 시점을 증명하지 못한다. 반면 "게이트 호출 1회"(④)는 `SCENARIO-GATE-{N}.md` 파일 수·state.json으로 독립 검증 가능하다. 권고: S-11의 채택 증거 우선순위를 자기서술 로그가 아닌 독립 관측치(게이트 호출 횟수·state.json step 타임스탬프)에 두고, 로그 대조는 보조 증거로 격하.

**관찰 ② (⑥축 제약 커버 2건)** — §4.1이 "⑥ 경계/부정 = 제약 6종"으로 요약하나(`:429`), TASK.md §제약 조건은 7항이며 다음 2항은 기계 검증 시나리오가 없다. (a) **검증 2원화 불변**(작성자≠PLAN 워커) — S-1이 문언 존재만, S-11 ②가 순서만 검증하고 "선작성 주체가 `opal-plan-agent`가 아님"을 직접 확인하는 Then이 없다. (b) **후속 분리**(opsdd·oppl·oppd 3 pilot 배선 제외) — S-14는 `op-dev-plan`·`op-dev-test-scenario/SKILL.md` diff 0만 요구하고(`:337`), 범위 밖 3 pilot SKILL.md diff 0 검증이 없다. PLAN TS-029도 동일 범위이므로 PLAN 유래 누락이 아니라 양 문서 공통 공백이다. 권고: S-14에 3 pilot SKILL.md `git diff --stat` 0건을 1항 추가.

**관찰 ③ (S-16 주장 범위)** — S-16 기대 결과가 "선작성 시점에 게이트를 호출하려는 시도가 차단"으로 서술되나(`:360`), 도구가 차단하는 것은 `plan.scenario_gate` **행 advance(mark)**이며 `op-scenario-gate` 스킬 호출 자체는 아니다. 따라서 H-d 운영 영향을 P1로 낮춘 근거(`:49`, `:366`)는 "행 mark 차단 + 문서 규율"의 합이지 도구 단독 차단이 아니다. 시나리오 유효성에는 영향이 없으나(기대 결과 자체는 검증 가능), 기대 결과 문장의 주장 범위를 "행 advance 차단"으로 좁히면 오독 위험이 제거된다.

---

## 3. 결과 계약

```json
{
  "scores": { "goal": 2, "adoption": 2, "boundary": 2 },
  "average": 2.0,
  "gaps": [],
  "verdict": "pass"
}
```

**종합 verdict: `pass`**

근거 — `scenario-gate.md:83` §5-1 종료조건 1: 판단축(①⑤⑥) 각 ≥1점(0점 축 없음) **AND** 평균 ≥1.5. 실측 2·2·2 / 평균 2.0으로 두 조건 모두 충족한다. 결정론 파트(②③④ 누락 0, exit 0)와 합쳐 `scenario-gate.md:89-91` §6 tool-gated PASS 2증거가 성립한다.

> 본 에이전트는 판정만 수행했다. `TEST-SCENARIO.md`·`TASK.md`·`PLAN.md`를 포함한 어떤 대상 산출물도 수정하지 않았으며, §2 관찰 3건의 반영 여부는 PM(오케스트레이터) 책임이다.
