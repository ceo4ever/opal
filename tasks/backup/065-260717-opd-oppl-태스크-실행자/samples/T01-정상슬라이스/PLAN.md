# PLAN: T01 — hello 문서 슬라이스 미시설계

> op-dev-plan 산출물 · CONTRACT.md 준수 (065 TEST S-7 fixture)

## 1. 산출물 명세 (RB-1 — 3필드)

| 필드 | 값 |
|------|-----|
| (1) 경로 | `tasks/065-260717-opd-oppl-태스크-실행자/samples/T01-정상슬라이스/out/hello.md` |
| (2) 형식 | 마크다운 — H1 제목 1개 + 본문 2줄 이상 |
| (3) 경계 | `samples/T01-정상슬라이스/` 폴더 밖 파일 생성/수정 금지 |

CONTRACT §1의 3필드를 그대로 승계한다.

## 2. 구현 접근

- `out/` 디렉토리를 태스크 폴더 내부에 생성한다 (없으면).
- `out/hello.md`를 다음 구조로 작성한다:
  - `# ` 로 시작하는 H1 제목 정확히 1개 (예: `# Hello`)
  - H1 아래 본문 텍스트 2줄 이상 (인사말 + 목적 설명 등 최소 2개 문단/라인)
- 폴더 밖 파일은 일절 생성/수정하지 않는다 (경계 준수).

예시 골격:

```markdown
# Hello

이 문서는 T01 정상 슬라이스의 산출물입니다.
oppl 태스크 실행자가 CONTRACT를 준수하여 생성했습니다.
```

## 3. 테스트 시나리오 → 검증명령 매핑 (RB-2)

| 시나리오 ID | 설명 | 커버 MV | 검증명령 | 기대 |
|-------------|------|---------|----------|------|
| TS-1 | out/hello.md 파일이 실제로 존재하는지 확인 | MV-1 | `test -f out/hello.md` | exit 0 |
| TS-2 | 파일 내 H1 제목(`# `)이 1개 이상 존재하는지 확인 | MV-2 | `grep -c '^# ' out/hello.md` | ≥1 |

CONTRACT §2의 기계검증절 MV-1·MV-2를 전부 커버한다.

## 4. 실행 체크리스트

- [ ] `out/` 디렉토리 생성 (폴더 내부)
- [ ] `out/hello.md` 작성 (H1 1개 + 본문 2줄 이상)
- [ ] TS-1 (`test -f out/hello.md`) 통과 확인
- [ ] TS-2 (`grep -c '^# ' out/hello.md` ≥1) 통과 확인
