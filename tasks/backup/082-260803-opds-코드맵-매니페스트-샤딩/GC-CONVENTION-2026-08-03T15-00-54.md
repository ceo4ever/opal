# GC CONVENTION REPORT — 2026-08-03T15-00-54

> **PM Gate 판정: 통과 (PASS)** — Critical 0건 / High 0건.

<!--
  체크박스 5단계 상태 기호 (APPLY 단계가 기입):
  [ ]  open    — 미처리 (신규)
  [x]  done    — 적용 완료  ← 주석: 적용 시각 YYYY-MM-DD HH:mm + 수정 요약
  [~]  pending — 보류       ← 주석: 보류 사유
  [?]  review  — 확인 필요  ← 주석: 판단 근거 / 해결 방안
  [!]  failed  — 실패       ← 주석: 실패 사유 / 권장 대안
-->

## 1. 헤더

- 실행 일시: 완료 2026-08-03 15:00:54 (KST)
- 범위: `all` (태스크 082 changed_files 전체 — staged 아님)
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` 존재 — 단일 진입점 모델(허브+링크 미적용, 문서 §참고 명시). 허브 전체를 기준으로 적용
- 점검 대상 재확인: `git status --porcelain` + `git diff --stat` 실측 (8개 수정 파일 + `test-shard.js` + 픽스처 5디렉토리 + `tasks/082-.../` 신규 폴더). `.opal/MEMORY.json` 수정분은 디스패치 프롬프트의 "점검 대상 (실측 목록)"에 없어 이번 점검에서 제외(런타임 메모리 데이터, 소스 컨벤션 대상 아님). `tasks/082-.../` 산출물 문서는 지시대로 제외
- APPLY 수행 여부: N (본 에이전트는 진단 전담 — 소스 미수정)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 1 |
| 심각도 분포 | Critical 0 / High 0 / Medium 0 / Low 0 / Info 1 |
| 자동 수정 가능 | 0 |
| 수동 조치 필요 | 0 (Info, 조치 불요 — 기록용) |
| 파일별 상위 Top 5 | docs/PROJECT.md (1건) / docs/ARCHITECTURE.md (1건, 동일 이슈 공유) |
| 카테고리별 빈도 | 문서화(변경이력 표기 형식) — 2 파일 |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 0 (빈도 0건 + 새 카테고리 0건 — 트리거 미발동) |

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

### High (0건)

### Medium (0건)

### Low (0건)

### Info (1건)

- [ ] GC-C001 [docs/PROJECT.md:179, docs/ARCHITECTURE.md:402] 변경이력 신규 행이 "일시 HH:mm + semver" 형식이 아닌 "날짜만" 형식을 사용
  - 카테고리: 문서화
  - 위반 기준: 참고 — `docs/CONVENTIONS.md` §구현 규칙 > 변경이력 작성 의무("일시는 `YYYY-MM-DD HH:mm` (KST), 버전은 semver") 및 §파일 구조 > 변경이력("일시 형식: `YYYY-MM-DD HH:mm` (KST 기준)"). 단, 이 두 절은 원문상 "스킬·에이전트·참조 문서"(§변경이력 작성 의무 첫 줄)를 대상으로 명시하며, `docs/PROJECT.md`/`docs/ARCHITECTURE.md`는 프로젝트 최상위 상태 문서로 이 범주에 명시적으로 포함되는지 문서에 명문 규정이 없다 — "기준 모호"로 판단해 Info로만 기록
  - 설명: 신규 행 `docs/PROJECT.md:179` (`| 2026-08-03 | 코드맵 매니페스트 샤딩 — ... code-scan v1.5.0 (Task 082) |`)과 `docs/ARCHITECTURE.md:402` (`| 2026-08-03 | tools/ 표 code-scan 행에 매니페스트 샤딩 반영 ... code-scan v1.5.0 (Task 082) |`)는 "날짜 | 변경 내용" 2열 표에 시각 없이 날짜만 기입하고, 버전은 별도 열이 아니라 본문에 인라인(`code-scan v1.5.0`)으로만 표기했다. 반면 같은 태스크에서 함께 수정한 `opal/core/references/tools.md:975`(`v2.10 | 2026-08-03 13:20 | ...(082)`)와 `opal/core/references/harness/header-rules.md:157`(`v1.6 | 2026-08-03 13:20 | ...(082)`)는 "버전 | 일시 | 변경내용" 3열 형식을 정확히 준수한다
  - 근거(비위반 판단 이유): `docs/PROJECT.md`/`docs/ARCHITECTURE.md`의 "## 변경이력" 표는 태스크 082 이전부터(예: `docs/PROJECT.md:180` 2026-08-02 행, `docs/ARCHITECTURE.md:403` 2026-08-02 행 등 선행 수십 행 전부) 동일하게 "날짜만 + 버전 인라인 표기" 형식을 일관되게 사용해 왔다. 이번 신규 행은 그 기존 표 형식을 그대로 따른 것이며 새로 도입된 편차가 아니다(Surgical Changes — 인접 표 전체를 이번 변경 범위로 확대하지 않음)
  - 해결 방안: (조치 불요) 두 문서의 "## 변경이력" 표 형식을 스킬/참조 문서와 동일한 3열(버전/일시/변경내용)로 통일할지 여부는 CONVENTIONS.md 소유자가 별도로 결정할 사안 — 이번 태스크의 신규 행 자체는 기존 표와 정합적이므로 수정 대상 아님
  - 자동 수정: N
  - 참조: `docs/CONVENTIONS.md` §구현 규칙 > 변경이력 작성 의무, §파일 구조 > 변경이력

---

## 4. 문서 업데이트 제안 (§9·§10, 트리거 발동 시만)

트리거 미발동 (빈도 트리거: 동일 fingerprint 3파일 이상 미충족 / 새 카테고리 트리거: 없음).

---

## 5. 문서 작성 유도 (해당 시)

`docs/CONVENTIONS.md` 존재 — 작성 유도 생략.

---

## 6. 항목별 확인 결과 상세 (디스패치 프롬프트 "특히 확인할 컨벤션 항목" 대응)

1. **§언어 규칙** (코드/변수/필드명 English, 주석 한국어) — 위반 없음.
   - 신규 상수 `SHARDS_DIR`(`_shards`) / `SHARD_LABEL_RE` / `DEFAULT_MANIFEST_MAX_BYTES` — 전부 영문 UPPER_SNAKE_CASE, 기존 파일의 `VERSION`/`HEADER_READ_BYTES`/`CODE_LAYER_STANDARD` 등과 동일 스타일 (`opal/tools/code-scan/code-scan.js:36-38`).
   - 신규 함수 `resolveShards`/`isShardManifestPath`/`baseManifestAbsForShard`/`manifestMaxBytes` — 전부 영문 camelCase 동사구, 기존 `resolveHeaderSource`/`resolveScopeIn`/`mirrorPathForDir` 네이밍 패턴과 일치 (`code-scan.js:56, 81-88, 100`).
   - 코드 내 주석은 한국어, 문자열 리터럴(에러 메시지 등)은 영문 — 기존 파일 관례 유지.

2. **§네이밍 규칙** (kebab-case) — 위반 없음.
   - 신규 픽스처 디렉토리 전량 kebab-case: `shard-repo`, `shard-package`, `shard-violations`, `shard-multi-scope`, `shard-goal`, 그 하위 `bad-label/{escape,slash,uppercase}`, `broken-base`, `dir-mismatch`, `duplicate-key`, `oversize-shard`, `oversize`, `reserved-name`, `shard-missing`, `undeclared` — CONVENTIONS.md §네이밍 규칙 > 파일/폴더 "kebab-case 사용"과 일치.
   - 신규 파일 `test-shard.js`도 kebab-case.
   - 샤드 라벨 정규식 `SHARD_LABEL_RE = /^[a-z0-9]+(?:-[a-z0-9]+)*$/`(`code-scan.js:37`)가 kebab-case를 런타임으로 강제 — 문서 규칙과 방향 일치.

3. **§변경이력 작성 의무** — `tools.md`(§341행대 신설 + 하단 v2.10, `tools.md:975`)와 `header-rules.md`(v1.6, `header-rules.md:157`)는 일시(KST, HH:mm)·semver·태스크 번호 `(082)` 3요소를 모두 정확히 갖춤. `code-scan.js` 하단 changelog(`code-scan.js:631`)도 `v1.5.0 — 2026-08-03 13:20 (082)` 형식으로 3요소 모두 충족. `docs/PROJECT.md`/`docs/ARCHITECTURE.md`는 §3 Info 항목 참조(날짜 전용 기존 표 형식 유지, 신규 편차 아님).

4. **§@header 규칙** — `test-shard.js:1-12`에 `@header` 블록 존재, `layer: "test"`, `task: "082"`, `scenarios` 배열(S-1~S-23, S-25, S-26) 채워짐. 위반 없음.

5. **§구현 규칙 > 배포 경계** — `git status --porcelain` 실측 결과 모든 변경이 `docs/`·`opal/`(프로젝트 소스) 내부이며 `~/.opal/` 경로 변경 0건. 위반 없음.

6. **§플랫폼 분기 격리** — 변경분(`code-scan.js` diff 641줄 전량 검토) 내 Claude/Cursor/Gemini/Antigravity 플랫폼 조건문 신설 없음. 위반 없음.

7. **§파일 구조 / 태스크 산출물 구조** — 디스패치 프롬프트 지시에 따라 `tasks/082-.../` 산출물은 점검 대상에서 제외.

---

## 7. 반환 계약 메모

- `changed_files`에는 본 보고서 1개 파일만 포함 (소스 미수정, 진단 전담).
