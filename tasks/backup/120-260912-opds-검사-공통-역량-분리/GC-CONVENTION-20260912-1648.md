# GC CONVENTION REPORT — 20260912-1648

<!--
  체크박스 5단계 상태 기호 (APPLY 단계가 기입):
  [ ]  open    — 미처리 (신규)
  [x]  done    — 적용 완료  ← 주석: 적용 시각 YYYY-MM-DD HH:mm + 수정 요약
  [~]  pending — 보류       ← 주석: 보류 사유
  [?]  review  — 확인 필요  ← 주석: 판단 근거 / 해결 방안
  [!]  failed  — 실패       ← 주석: 실패 사유 / 권장 대안
-->

## 1. 헤더

- 실행 일시: 시작 2026-09-12 16:48:00 / 완료 2026-09-12 16:54:00 / 소요 약 6분
- 범위: `explicit` (호출자 명시 목록, git 조회 없음) / 대상 파일 13개
- 에이전트: opal-convention-checker (skill_path: `opal/skills/op-gc-convention/SKILL.md` — 태스크 120이 신설한 프로젝트 소스, 첫 실사용)
- 기준 문서: `docs/CONVENTIONS.md` 존재 (단일 문서 모델 — OPAL 프레임워크 자체는 허브+링크 미적용, 허브 전체를 기준으로 적용)
- APPLY 수행 여부: N (본 실행은 진단 전담 — read-only, 수정 없음)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 2 |
| 심각도 분포 | Critical 0 / High 0 / Medium 1 / Low 1 / Info 0 |
| 자동 수정 가능 | 1 |
| 수동 조치 필요 | 1 |
| 파일별 상위 Top 2 | `opal/core/references/opal-skills-registry.json` (1건) / `opal/skills/op-gc-security/SKILL.md` (1건) |
| 카테고리별 빈도 | 문서화 (2개 파일 — 빈도 트리거 미발동, N<3) |
| Critical/High 수 | **0** (PM Gate 통과 기준 충족) |
| 문서 업데이트 제안 수 | 1 (새 카테고리 트리거 1건) |

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

없음

### High (0건)

없음

### Medium (1건)

- [ ] GC-001 [opal/core/references/opal-skills-registry.json:795-934] 최상위 `changelog` 배열이 버전별 변경 내역을 수기로 누적한다 (15개 항목, 최신 3.17.0은 이번 태스크 120이 추가)
  - 카테고리: 문서화 (수기 누적 이력 절)
  - 위반 기준: 프로젝트(`opal/core/references/opal-doc-standard.md` §5 — `docs/CONVENTIONS.md` §문서 이력과 버전이 이 절을 지시)
  - 설명: `opal-doc-standard.md` §5는 SKILL/AGENT/harness/**reference** 문서에서 이름과 무관하게(변경이력·Changelog·history·revisions) 수기 누적 이력 절 생성을 금지하고, 이력은 git과 태스크 기록이 소유하도록 규정한다. `opal-skills-registry.json`은 `opal/core/references/` 소속 reference 문서이며, 최상위 `changelog` 배열이 해당 패턴과 일치한다. 이번 태스크가 이 패턴을 처음 만든 것은 아니나(기존 14개 항목이 이미 존재), 3.17.0 항목을 추가하며 누적을 이어갔다.
  - 해결 방안: `changelog` 배열을 제거하고 최상위 `version` 필드만 유지한다. 변경 근거는 git 커밋 로그(태스크 번호 기반)로 충분히 추적된다. 기존 15건을 즉시 삭제할지 별도 아카이브로 이관할지는 소유자 결정이 필요하다.
  - 자동 수정: N (구조 변경 + 기존 15건 처리 방식 결정 필요)
  - 참조: `opal/core/references/opal-doc-standard.md` §5
  - **확인 요청**: 배열 삭제 여부와 기존 항목 보존 방식(삭제 vs 아카이브)은 소유자 판단이 필요하다. 다른 도구가 `version` 필드는 소비할 수 있으나 `changelog` 배열 자체를 소비하는 코드는 발견되지 않았다(`grep -rn changelog opal/tools/ opal/core/ scripts/` 결과 이 파일 외 소비처 없음).

### Low (1건)

- [ ] GC-002 [opal/skills/op-gc-security/SKILL.md:20] `output_dir`/`task_folder` 별칭 우선순위 문구가 대칭 스킬 `op-gc-convention/SKILL.md`에는 없음
  - 카테고리: 문서화 (스킬 간 일관성)
  - 위반 기준: 프로젝트(`opal/core/references/opal-doc-standard.md` §2 — 같은 사실은 일관되게 기술)
  - 설명: `op-gc-security/SKILL.md` 20행은 "전환 기간 동안 `task_folder`를 alias로 허용하고 둘 다 오면 `output_dir`이 우선한다"고 명시하지만, `op-gc-convention/SKILL.md` 21행은 "`task_folder` 이름으로 전달돼도 같은 값으로 받는다"까지만 쓰고 두 값이 동시에 올 때의 우선순위를 명시하지 않는다. 두 스킬은 `opal-pilot-gc` CHECK 단계가 동일 패턴(요소×체커)으로 병렬 디스패치하는 대칭 쌍이라 같은 별칭 규칙을 공유해야 한다.
  - 해결 방안: `op-gc-convention/SKILL.md`의 `output_dir` 행에 `op-gc-security`와 동일한 우선순위 문구를 추가한다.
  - 자동 수정: Y (문구 삽입, 단순 치환)
  - 참조: TBD — 전용 스타일 가이드 없음, 대칭 스킬 쌍 대조로 발견

### Info (0건)

없음

---

## 4. 문서 업데이트 제안 (§9·§10, 트리거 발동 시만)

### [빈도 트리거] (0건)

발동 없음 (동일 fingerprint가 3개 이상 파일에서 관측되지 않음).

### [심각도 트리거] (0건)

발동 없음 (Critical·High 0건).

### [새 카테고리 트리거] (1건)

- [ ] GC-DP-C001 [새 카테고리 트리거] `op-gc-*` 컴포넌트 네이밍 접두사 → `docs/CONVENTIONS.md` §컴포넌트 네이밍 체계 표 신설 제안
  - 근거: 이번 태스크(120)가 `op-gc-security`·`op-gc-convention`·`op-gc-report` 3개 단계 스킬을 신설하며 `op-gc-*` 접두사를 도입했다. `docs/CONVENTIONS.md` §컴포넌트 네이밍 체계 표(`opal-pilot-*`/`op-dev-*`/`op-task-*`/`opal-task-*`/`opal-{domain}-agent`/`op-sdd-*`/`opal-*`)에는 `op-gc-*`가 등재되어 있지 않다 (`grep -n "op-data\|op-gc" docs/CONVENTIONS.md` 결과 0건). 같은 표에 기존 `op-data-*`(태스크 019 신설)도 등재되어 있지 않아, 이 표가 신규 단계 스킬 그룹 추가 시 갱신되지 않는 패턴이 반복되고 있다.
  - 제안 내용: `docs/CONVENTIONS.md` §컴포넌트 네이밍 체계 표에 `op-gc-*` → "GC(진단) 단계 스킬" 행을 추가하고, 동일 기회에 기존 누락분 `op-data-*` → "데이터 설계 단계 스킬" 행도 함께 보강한다.
  - 참고: `docs/PROJECT.md`·`docs/ARCHITECTURE.md`는 이번 태스크가 `op-gc-*` 관련 표·트리 구조·개수를 정확히 갱신했다(43개 스킬 수 등 일치 확인) — 갱신 누락은 `docs/CONVENTIONS.md` §컴포넌트 네이밍 체계 표 1곳에 한정된다.

---

## 5. 문서 작성 유도

`docs/CONVENTIONS.md` 존재 확인 — 작성 유도 안내 없음.

---

## 부록 — 검사 방법 근거

- 13개 대상 파일 전문 Read, `opal/core/references/harness/gc-finding-schema.md`(finding 필드·envelope·fingerprint·판정 SSOT)와 `op-gc-convention/references/convention-categories.md`(카테고리 8종) 기준 적용.
- `git diff 81363d1^ 81363d1 -- <파일>`로 태스크 120의 실제 변경분과 현재 상태를 대조해 이번 태스크가 새로 만든 내용과 기존 패턴을 구분했다.
- 코드성 파일(`scripts/tests/task113_bootstrap_audit.py`, 1행 diff)은 `python3 -m py_compile`로 컴파일 확인, `opal-skills-registry.json`은 `python3 -c "json.load(...)"`로 유효성 확인 — 둘 다 정상.
- 13개 파일 전건에 대해 탭 문자·행 끝 trailing whitespace·EOF 개행 누락을 확인했다 — 위반 0건.
- `grep -niE '변경이력|changelog|change log|revision history|## history|revisions'`로 9개 대상 마크다운 파일의 §5 위반(수기 누적 이력 절)을 스캔했다 — 마크다운 파일 전건 0건, JSON 파일(`opal-skills-registry.json`)에서만 1건 발견(GC-001).
