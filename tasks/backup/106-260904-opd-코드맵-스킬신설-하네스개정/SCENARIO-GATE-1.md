# SCENARIO GATE 1 — 목표-커버 루브릭 판단축 채점 (①⑤⑥)

> 실행 일시: 2026-09-04T21:52:00+09:00 | phase: `scenario-rubric` | iteration: 1
> 판정 주체: opal-evaluator-agent (verdict-only · mutate 금지)
> scenario_source: `tasks/106-260904-opd-코드맵-스킬신설-하네스개정/TEST-SCENARIO.md` (S-1~S-22, S-21 결번 아님 / S-1~S-20·S-22 = 21건)
> 보조 판독: `TASK.md`(R-1~R-6·AC) · `PLAN.md`(F-001~F-006·H-1~H-12·DEC-1~DEC-6 §3.4.2 훅 게이트 ①~⑦) · `.scenario-coverage-input.json`(정규화 페이로드 플래그)
> 기준 SSOT: `opal/core/references/harness/scenario-gate.md` §2 루브릭 6축 · §5-1 종료조건
> 판정 범위: **①목표달성 · ⑤채택/잔존 · ⑥경계/부정 3축만**. ②요구커버·③기능커버·④리스크커버는 test-tool `scenario-coverage-check` 결정론 판정 소관(`all_covered: true`, R 6 / F 6 / H 12 / S 22)이며 본 판정에서 대신 판정하지 않는다(§2 [MUST] 판정 주체 분리).
> CONTRACT.md: 부재(opd 1차 접합). `scenario-rubric`은 CONTRACT 루브릭절 병합 대상이 아니므로(별도 트랙) 판정에 영향 없음.

## 1. 판단축별 판정

| 축 | 점수 | 통과선 | 근거 | gap |
|----|------|--------|------|-----|
| ① 목표 달성 | **1** / 2 | ≥1 | 3국면 중 **생성 국면만** 운영 계층 직접 검증. 갱신 국면은 L1 문면 판독 전용 | 있음(§2.1) |
| ⑤ 채택/잔존 | **2** / 2 | ≥1 | 교체형 2요건(구형 잔존0 · 신형 채택) 전건 커버 + 런타임 채택까지 관측 | 없음 |
| ⑥ 경계/부정 | **1** / 2 | ≥1 | 부정 경로 1건 강함(S-9). 그러나 훅 게이트 7단 중 4분기 미커버 | 있음(§2.3) |

**scores**: `{"goal": 1, "adoption": 2, "boundary": 1}` · **average**: `1.33`

## 2. 축별 판정 상세

### 2.1 ① 목표 달성 — 1점

**목표 문면**: "…최초 구축·환경설정을 독립 스킬 `opcmb`로 제공하고, **갱신 트리거와 소비 2단 절차를 하네스에 편입**하여 자산의 **생성–갱신–소비 3국면을 완결**한다." → 목표는 3국면 복합이므로, 목표 달성 검증도 국면별로 성립해야 한다.

| 국면 | 요구 | 담당 시나리오 | 검증 계층 | 판정 |
|------|------|-------------|---------|------|
| **생성** | R-1 | S-20(manifest 시퀀스 완주 → `.opal/code-map/index.json`·패키지 매니페스트 실제 생성 · `validate` exit 0) · S-21(소유자 리뷰 게이트, L3 [SUPERVISOR]) | L2 + L3 | **충족** — 임시 프로젝트에서 자산이 실제로 생성되고, 사람 게이트 작동까지 관측한다. 2점 앵커("사용자·운영 계층에서 목표를 직접 검증")에 정면 부합 |
| **소비** | R-3 + R-4 | 규율 문면 S-7(L1) / **집행** S-9·S-10(L2 실 CLI 블랙박스 — 인용 없으면 EXECUTE 진입 exit 1 차단, 있으면 통과) | L1 + L2 | **대체로 충족** — "실제 PM 행동을 바꾸는지"가 도구 게이트로 관측된다(S-9의 `state.json` 무변경까지 확인). 2차 grep 전환 규율 자체는 문면 전용이나 이는 도구 집행 대상이 아닌 산문 규율이어서 감점 사유로 보지 않는다 |
| **갱신** | R-2 | S-4(§12 표 유지 행+각주) · S-5((4단) 전환) · S-6(미수행 탐지 조건 문면) | **L1 전용** | **미충족** — 3건 전부 `grep`/`git diff` 문면 판독이다. R-2는 도구 집행 신설이 없으므로(Step 7·8 = 문서 개정만) 문면 외 관측 수단이 시나리오 집합에 존재하지 않는다 |

**갱신 국면 gap의 실질** — 문면 판독만으로 목표 달성을 선언할 수 없는 이유:

1. S-6이 검증하는 것은 "(d) 행에 미수행 탐지 조건이 **문장으로 쓰여 있다**"뿐이다. 그 탐지 조건 자체 — "(b) CLOSE 게이트 `newly_uncovered` 누적" — 이 실제로 발동하는지는 어느 시나리오도 관측하지 않는다.
2. 유일한 인접 관측인 S-11 ③은 `validate --changed state_tool.py` → `newly_uncovered` **0**, 즉 **통과 경로만** 본다. 탐지가 작동함을 보이려면 @header 미갱신 상태에서 `newly_uncovered` ≥1이 관측되어야 한다. 이 반대 경로가 없다.
3. 정규화 페이로드의 객관 증거도 동일하다 — `is_goal_scenario: true`인 2건(S-20·S-21)의 `covers_requirements`가 **둘 다 `["R-1"]`**이다. 즉 목표 시나리오로 선언된 것이 생성 국면(R-1)에만 걸려 있고, R-2(갱신)·R-3(소비)에 걸린 목표 시나리오는 0건이다.
4. `scenario-gate.md` §1 070 사건 근거가 겨냥하는 결함형과 동형이다 — 매핑 표(②③④)는 R-2↔S-4/S-5/S-6로 완전하지만, 그 시나리오들이 "목표 달성 관점에서 충분한가"는 별개 판단이며 여기서는 불충분하다.

**0점이 아닌 이유**: 목표 검증 시나리오가 부재한 것이 아니다(S-20·S-21이 생성 국면을 운영 계층에서 직접 검증하며, 소비 국면 집행도 L2 런타임으로 관측된다). 3국면 중 1국면만 문면에 머무르므로 "존재하나 불완전" = 1점이 정확하다.

**해소 방향(제안, 반영은 PM 책임)**: L2 경량 트랙 갱신 게이트의 **탐지 발동**을 관측하는 L2 시나리오 1건 추가. 예 — 임시 자원에서 코드 파일을 @header 미갱신 상태로 변경한 뒤 `code-scan validate --changed <file>` → `newly_uncovered` ≥1 관측(= (d) 행이 인용한 탐지 조건이 실재함) 및 그 상태에서 CLOSE 진입이 차단됨을 확인. 이는 기존 도구만으로 실행 가능하며 신설 도구를 요구하지 않는다.

### 2.2 ⑤ 채택/잔존 — 2점

**교체형 대상 식별**: R-4가 유일한 교체형 목표다(PM 자기판정 → 도구 판정). R-2·R-3은 교체형이 아니라 **가산형**이며, 시나리오가 이를 정확히 반영한다.

| 요구 | 성격 | 잔존 처리 | 시나리오 |
|------|------|----------|---------|
| R-4 | **교체형** | 구형(자기판정 문면) 소멸이 목표 | S-22 |
| R-2 | 가산형 | `header-rules.md` (a)(b)(c) 3행 · §12 「미적용」 5행 **원문 무변경이 정답** | S-5·S-4가 무변경을 명시 검증 |
| R-3 | 가산형 | `opal-pm.md:237` 소유자 오버라이드 문단 **보존이 정답** + 신설 규율과 구분·비모순 | S-7이 무변경·구분 1줄·비모순 1줄 3항 검증 |

**S-22의 교체형 2요건 충족**:
- **(a) 구형 잔존 0** — 항목 14에 "PM이 판정한다" 계열 문면 **0건**을 `grep`으로 판정. 개정 전 상태가 그 문면을 보유함을 조건절에 명기(§2.2 Given이 "개정 전 `pm-review-gate.md` 항목 14 (자기판정 문면 보유)")하여 before/after 대비가 성립한다 — 잔존0 판정의 전제(구형이 실제로 있었다)가 확보된 형태다.
- **(b) 신형 채택** — 판정 수단이 `state-tool verify --code-scan-citation-check` **exit code**로 명시 + 집행 지점 + 스킵 조건 3종 존재를 검증. 나아가 신형이 문면 채택에 그치지 않고 **실제로 작동함**이 S-9(exit 1 차단)·S-10(exit 0 통과)에서 L2 런타임으로 관측된다. 문면 채택 + 런타임 채택 2중 관측이다.
- 부수 무변경(항목 1~13·자가 진단 절)까지 범위 봉쇄가 걸려 있다.

**비차단 관찰 1건**(감점하지 않음): DEC-2 근거 ③은 "`build_gate_payload`가 `checklist`를 `mark` stdout으로 결정론 반환하므로 PM 도달이 보장된다"고 채택 근거를 세우지만, 신설 checklist 항목이 실제 `mark` stdout에 실리는지는 시나리오가 보지 않는다(S-13은 배열 길이 4→5, S-15는 stdout **키 집합** 동일성만 확인). 신형 채택의 최종 전달 링크가 미관측이나, ⑤축 2요건은 이미 충족되어 있으므로 앵커상 2점을 유지하고 개선 제안으로만 남긴다.

### 2.3 ⑥ 경계/부정 — 1점

**존재하는 커버(0점이 아닌 근거)**:

| 유형 | 시나리오 | 강도 |
|------|---------|------|
| 부정 경로 | **S-9** — 인용 0건 → `ok:false`·`code_scan_citation_unmet`·**exit 1** + `mark` 동일 거부 + `state.json` 바이트 무변경 | 강함. RED-first 트랙 지정(`--red-check` ON)까지 걸림 |
| 경계 | **S-11** — 미보급 프로젝트(`headerSource: inline` + code-map 부재) 오탐 0건, stdout·stderr 양축 발화 0건 | 강함 |
| 경계 | **S-15** — EXECUTE 보유 8 파이프라인 전건 graceful skip, 예기치 않은 거부 0건 | 강함(발동 반경 P0) |
| 대조 정상 | S-10 — 인용 충족 exit 0 | 부정 경로의 대조군으로 성립 |

**2점이 아닌 근거 — 훅 게이트 ①~⑦(PLAN §3.4.2) 분기별 커버 대조**:

| 게이트 | 분기 | 커버 시나리오 | 판정 |
|--------|------|-------------|------|
| ① 발동 조건(EXECUTE 첫 행 아님) | `return` 무영향 | S-15(간접 — 8종 EXECUTE 첫 행 진입) | 부분 |
| ② **`force` 우회 허용** | `return` + 의사결정 로그 | **없음** | **미커버** |
| ③ 자산 게이트 → `code_scan_unavailable` | skip | **없음** — S-11은 이 레포에 `.opal/code-scan.json`이 **존재**하고 `headerSource: inline`이 유효하므로 ③이 아니라 ⑤에서 스킵된다(기대 결과가 `reason: doc_only_task`로 명기) | **미커버** |
| ④ 산출물 게이트 → `plan_md_absent` | skip(하위호환) | **없음** — S-14는 `show`만 호출하며 훅 경로가 아니다 | **미커버** |
| ⑤ 적용 범위 게이트 → `doc_only_task` | skip | S-11 ① | 커버 |
| ⑥ **`auto_pass` 거부** | `err` | S-12(문면·주석으로 **순서만** 확인) | **런타임 미커버** |
| ⑦ 판정 | exit 1 / `return` | S-9 · S-10 | 커버 |

추가 미커버 1건 — PLAN §3.4.2 (5)가 계약한 **플래그 상호배타 거부**(`--code-scan-citation-check`를 `--clarification-check`/`--evidence-check`와 동시 지정 시 `evidence_check_flag_conflict` 동형 거부, "무성 무시 방지"가 설계 의도)에 대응하는 시나리오가 0건이다. 이는 전형적 부정 입력 경계다.

**특히 문제적인 조합 — H-7(P0)의 검증 강도**: H-7은 "`auto_pass` 거부를 graceful skip **앞**에 두면 문서 전용 태스크에서 오탐"이라는 P0 가설이고, PLAN이 이를 `[MUST] ⑥은 ③④⑤ 뒤에 둔다`로 못박았다. 그런데 이 순서 계약의 검증은 (i) S-12 = 코드 주석·문면 판독 (ii) S-11 = 런타임이나 **`auto_pass`를 켜지 않은 경로**의 2건뿐이다. 즉 P0 순서 계약이 실제로 지켜졌는지를 결정하는 조합 — **문서 전용 태스크 + `auto_pass` 지정 → 거부가 아니라 skip** — 을 관측하는 시나리오가 없다. 순서를 거꾸로 구현해도 현 시나리오 집합은 전건 Pass할 수 있다(S-11은 `auto_pass` 미지정이라 ⑥에 닿지 않고, S-12는 문면만 본다). 경계축의 실효 커버 공백으로 가장 무게가 큰 항목이다.

**해소 방향(제안, 반영은 PM 책임)**: L2 시나리오 2건 추가로 대부분 해소된다 — ① 문서 전용 임시 태스크 폴더 + `auto_pass` 경로 호출 → **거부가 아니라 skip(`doc_only_task`)** 관측(H-7 P0 순서 계약의 런타임 증거) ② `force` 우회 호출 → 진행 + 의사결정 로그 기재 관측(게이트 ②). 여력이 있으면 ③ `.opal/code-scan.json` 부재 임시 프로젝트 → `code_scan_unavailable` skip ④ PLAN.md 부재 폴더 → `plan_md_absent` skip ⑤ 플래그 동시 지정 → conflict 거부. ③~⑤는 기존 `/tmp/opal-ts106/` fixture 변형으로 저비용 추가가 가능하다.

## 3. 종합 판정

**scores**: `{"goal": 1, "adoption": 2, "boundary": 1}`
**average**: `(1 + 2 + 1) / 3 = 1.33`

**수렴 기준 대조** (`scenario-gate.md` §5-1):

| 조건 | 기준 | 실측 | 판정 |
|------|------|------|------|
| 0점 축 부재 | 각 축 ≥1점 | goal 1 · adoption 2 · boundary 1 | **충족** |
| 평균 임계 | ≥1.5점 | **1.33** | **미달** |

### verdict: **fail**

0점 축은 없으나 평균이 임계(1.5) 미달이다. 재작성 1회로 수렴 가능한 거리이며, 미달 두 축(①·⑥) 각각에 시나리오 1건씩 추가하면 goal 2 / adoption 2 / boundary 2 → 평균 2.0으로 수렴한다(①은 §2.1 제안, ⑥은 §2.3 제안 ①②가 핵심).

**gaps**:
1. **①목표달성** — 3국면 중 갱신 국면(R-2)의 검증이 L1 문면 판독(S-4·S-5·S-6) 전용이다. (d) 행이 인용한 탐지 조건("CLOSE 게이트 `newly_uncovered` 누적")이 실제로 발동함을 관측하는 시나리오가 0건이며, 유일한 인접 관측인 S-11 ③은 `newly_uncovered` 0(통과 경로)만 본다. `is_goal_scenario: true` 2건(S-20·S-21)의 `covers_requirements`가 둘 다 `["R-1"]`로 생성 국면에만 걸려 있다. → @header 미갱신 상태에서 탐지 발동(`newly_uncovered` ≥1)과 CLOSE 차단을 관측하는 L2 시나리오 1건 필요.
2. **⑥경계/부정** — `state_tool.py` 훅 게이트 7단 중 ②(`force` 우회)·③(`code_scan_unavailable`)·④(`plan_md_absent`) 3분기가 시나리오 0건이고, ⑥(`auto_pass` 거부)은 문면(S-12)으로만 확인된다. 특히 H-7(P0)의 `[MUST]` 순서 계약(⑥은 ③④⑤ 뒤)은 "문서 전용 태스크 + `auto_pass`" 조합이 미커버여서, 순서를 거꾸로 구현해도 현 시나리오 집합이 전건 Pass할 수 있다. PLAN §3.4.2 (5)의 플래그 상호배타 거부도 미커버. → 최소 `auto_pass` × 문서 전용 skip, `force` 우회 2건 추가 필요.

**비차단 개선 제안**(점수에 미반영): DEC-2 채택 근거인 "checklist → `mark` stdout 결정론 반환으로 PM 도달 보장"이 관측되지 않는다(S-13은 배열 길이, S-15는 stdout 키 집합만). 신설 checklist 항목 문면이 `mark` stdout에 실제로 실리는지 확인하는 단정 1줄을 S-13 또는 S-15 기대 결과에 덧붙이면 ⑤축 채택 사슬이 완결된다.

---

> 본 보고서는 판정 전용이다. TEST-SCENARIO.md 및 그 외 산출물은 수정하지 않았다(verdict-only · mutate 금지). 게이트 행 mark는 호출자(op-scenario-gate) 권한이다.
