# AGENTIC-LOG — 124 워크트리 multi-repo 캡슐 소유권

> agentic 모드 PM 대행 기록. 게이트 판단 근거·의사결정·오류·폴백을 시간순으로 남긴다.

| # | 시각 | 단계 | 유형 | 내용 | 결과 |
|---|------|------|------|------|------|
| 1 | 2026-09-12 17:58 | TASK | DECISION | 워크트리 생성 전 제안서 개정분 312줄이 미커밋임을 감지. `--wt`는 `main` base-ref로 워크트리를 만들므로(`task-process.md:30,41`) 커밋 없이 진행하면 워커가 v1 명세를 보게 된다. 캡틴께 3안 제시 후 "제안서만 커밋" 선택 | 커밋 `4d23cb0` |
| 2 | 2026-09-12 17:58 | TASK | GATE | `pilot.start` load 5문서·receipt verify ok. 모드 판정 `agentic`, 워크스페이스 축 `--wt`. 입력 `-wt`(단일 하이픈)를 `--wt`로 해석 — 모드 플래그 개수에 포함하지 않는 직교 축(`harness/worktree.md` §모드 축과 직교) | Pass |
| 3 | 2026-09-12 17:58 | TASK | GATE | `stage.task` load 3문서·`state-tool event-verify` ok | Pass |
| 4 | 2026-09-12 17:59 | TASK | DECISION | 번호 124 발급(`memory-tool task-number --bump`). `worktree-tool create` ok — `task_path_ownership_version: 2`, `artifact_repo: "."`, base_ref `main`, branch `feat/OP-TASK-124`. 워크트리에서 제안서 v4 키워드 14건 확인 → 명세 원본이 브랜치에 실재함을 실측 | 생성 완료 |
| 5 | 2026-09-12 17:59 | TASK | WARNING | `create` warnings 2건 — uv 캐시 볼륨 상이(슬롯당 `.venv` 실복사), 동시 슬롯 3개(공유 자원 충돌 주의). 둘 다 비차단 진단이며 이번 태스크는 Python 가상환경·개발 DB·포트를 쓰지 않으므로 영향 없음 | 비차단 |
| 6 | 2026-09-12 18:00 | TASK | ERROR | `state init`을 `opal-pilot-dev/references/pipeline.json`(opd 전체 프로필, 16행)으로 호출해 ANALYSIS·TEST-SCENARIO 행이 잘못 생성됨. opds는 `pipeline-short.json`(11행)이 소유 | 자체 감지 |
| 7 | 2026-09-12 18:00 | TASK | DECISION | `--force --note`로 재init하여 11행으로 정정. 행 구성이 파이프라인 진행 순서를 결정하므로 다음 단계 진입 전에 해소해야 하는 오류로 판단 | 정정 완료 |
| 8 | 2026-09-12 18:01 | TASK | GATE | TASK.md 계약 검사 `verify --clarification-check` → `pass`, `template: sdlc-v2`. 필수 5절 충족, C-1~C-9·AC-1~AC-15 고유 ID 부여. 범위 제외 2건(pug 실환경 파일럿, `repos[]` 원소 지정)을 Affected users and systems에 명시 | Pass |
| 9 | 2026-09-12 18:02 | PLAN | GATE | `stage.plan`(2문서)·`worker.dispatch`(4문서) load·verify ok. `opal-plan-agent` 디스패치(model advanced). TASK 사용자 확인 행은 agentic 자동 승인(`auto_approved: [2]`) | Pass |
| 10 | 2026-09-12 18:12 | PLAN | GATE | PLAN.md 직접 Read 검증. D-1~D-22·W-1~W-8·H-1~H-5. 제안서 인용 17건을 워커가 워크트리 현재 파일에서 전건 재확인했고 3건은 끝줄/시작줄 1줄 차이를 실측값으로 정정. AC-1~AC-15·C-1~C-9 전건이 Work item에 매핑됨 | Pass |
| 11 | 2026-09-12 18:12 | PLAN | BLOCKER | 워커가 H-1을 EXECUTE 착수 전 차단으로 반환 — 루트가 `repos[]` 경로를 ignore하지 않으면 자식 worktree 생성 직후 루트 slot이 `?? workspace/`로 dirty가 되고(`worktree_tool.py:799`), `cmd_remove`가 제거 이전에 전 entry 가드를 먼저 돌아(`:1184-1202`) 역순 회수를 도입해도 루트가 항상 `GUARD_DIRTY`. AC-9·AC-10 도달 불가. C-1에 따라 구현안을 임의 변경하지 않고 PM 판단 요청 | 에스컬레이션 수신 |
| 12 | 2026-09-12 18:14 | PLAN | DECISION | PM 확정 — **R-5 신설**(대안 "슬롯 dirty 판정에서 `repos[]` 제외" 기각). 근거 2축: (a) `_inspect`는 layout 무관 단일 판정기라 예외를 넣으면 루트가 실제로 보는 사용자 변경까지 무시되고 monorepo·비워크트리 dirty 경로에 분기가 생겨 C-2·C-3 근거가 약해진다 (b) 실사용 대상 pug는 루트 `.gitignore:3`이 `workspace/`를 "하위는 각자 독립 git 레포 → 루트에서 추적하지 않음" 근거와 함께 이미 등재해 R-5를 만족함을 직접 실측. 자동 복구 대신 명시 차단이라는 제안서 §4.4·§6.3의 일관된 규율과도 맞음 | R-5 채택 |
| 13 | 2026-09-12 18:15 | PLAN | DECISION | C-1 준수 순서로 SSOT부터 갱신 — 제안서 §4.1에 R-5(판정 `check-ignore -q`)와 기각 근거 절 추가, §8.1에 대응 수용 기준 추가. 이어서 TASK.md AC-3·AC-4를 5종으로 정정하고 AC-16 신설, PLAN.md에 D-23 신설·D-3/D-4 갱신·W-1 ⑫/W-4 ① 흡수·H-1 해소 기록. 잔여 차단 항목 0건 | 정합 완료 |
| 14 | 2026-09-12 18:16 | PLAN | GATE | `verify --clarification-check` pass(AC-16 추가 후 재검), `--plan-contract-check` pass(W-1~W-8), `--code-scan-citation-check` pass. `plan.plan_md` mark | Pass |
| 15 | 2026-09-12 18:18 | PLAN | GATE | `stage.test_scenario`(red-first·scenario-gate 2문서) load·verify ok. PM이 TEST-SCENARIO.md 작성(S-1~S-28) — PLAN 작성자와 분리(scenario-gate.md Producer 계약) | Pass |
| 16 | 2026-09-12 18:19 | PLAN | GATE | `scenario-coverage-build` ok(requirements 25·hypotheses 5·scenarios 28) → `scenario-coverage-check` **exit 0**. 결정론 3축 통과. 판단 3축은 `opal-evaluator-agent` scenario-rubric에 디스패치 — Producer(PM)≠Evaluator 매반복 분리 | 진행 중 |
| 17 | 2026-09-12 18:21 | PLAN | GATE | 목표-커버 게이트 pass — `coverage-check` exit 0 + evaluator scenario-rubric ①2·⑤2·⑥2(평균 2.0, 0점 축 없음, gaps 0). 두 증거 동시 충족 확인 후 `.scenario-gate-history.json` 1회차 기록, `plan.scenario_gate` mark | Pass |
| 18 | 2026-09-12 18:22 | PLAN | GATE | PM Gate 7항목 중 "AC/C → Work items 완료 기준 연결"에서 **C-1 미연결 검출**. W-8에 "제안서↔구현 항목 대조(S-22)" 단계를 신설해 연결하고 완료 기준에 C-1 추가. 재검증 후 미연결 0건, `--plan-contract-check`·`--code-scan-citation-check` 재통과. `plan.pm_gate` mark | Fail→보정→Pass |
| 19 | 2026-09-12 18:23 | EXECUTE | GATE | `stage.execute`(coding-principles·red-first 2문서) load·verify ok. PLAN 사용자 확인 행 agentic 자동 승인(`auto_approved: [6]`). `scenario-init` 28건 등록(`red_required` 21건) | Pass |
| 20 | 2026-09-12 18:23 | EXECUTE | DECISION | P1 병렬 2건 디스패치 — W-1은 `opal-test-agent` RED 모드(구현자와 분리, `worktree_tool.py` 수정 금지), W-2는 `opal-task-agent`(`harness/worktree.md` 단일 파일). 두 워커의 변경 대상이 겹치지 않아 병렬 안전 | 디스패치 |
| 21 | 2026-09-12 18:26 | EXECUTE | GATE | W-2 산출물 직접 Read 검증 — `## multi-repo 캡슐 소유권 계약` 신설(101줄 삽입 전용), 하위 9절이 PLAN W-2 ①~⑨에 1:1 대응. D-21 준수(불변식·cone 계약을 소유권 참조만 하고 재서술 0건), C-5 준수(cone 미도입), C-7 준수(제안서 인용 **0건** — grep 실측). `git status`상 변경이 `worktree.md` 1건으로 배정 경계 준수 | Pass |
| 22 | 2026-09-12 18:41 | EXECUTE | BLOCKER | W-1이 블로커 2건 반환 — ① S-8·S-27은 `red_required`로 선언됐으나 성격상 실패 불가라 거짓 RED를 만들지 않고 미기록(잠금 미달 2건) ② `git check-ignore -q <rel>`은 그 경로에 추적 파일이 있으면 rc=1을 반환해, D-3의 "R-1~R-5 먼저, 그다음 overlap" 순서면 V6가 R-5로 먼저 걸려 AC-6 도달 불가 | 에스컬레이션 수신 |
| 23 | 2026-09-12 18:42 | EXECUTE | DECISION | ① 확정 — S-8(기존 `TASK_ARTIFACT_REPO_MISSING` 계약 보존)과 S-27(R-5 필요성 반증 대조군)은 구현 전후 동일 관측이 정상. TEST-SCENARIO `시점`을 `구현 후`로 정정(red_required 21→19). 워커가 거짓 RED를 만들지 않은 판단이 옳다 | 정정 |
| 24 | 2026-09-12 18:43 | EXECUTE | DECISION | ② 확정 **D-24** — git 실측(`-q` rc=1 / `--no-index` rc=0)으로 워커 보고 재확인 후 판정 순서를 **R-1~R-4 → overlap → R-5**로 고정. `--no-index` 대안은 R-5 의미를 "실효 ignore"에서 "규칙 존재"로 약화시켜 기각. 두 조건은 배타적 원인이므로 원인별 전용 오류가 더 행동 가능한 진단이다. C-1 순서대로 제안서 §4.1·§4.4 → `harness/worktree.md` → PLAN(D-24·D-3·W-4) → TEST-SCENARIO(S-9) 갱신 | 확정 |
| 25 | 2026-09-12 18:44 | EXECUTE | DECISION | 폴백 승인(agentic §3 폴백 승인 의무) — S-14는 존재하지 않는 ref로 pre-flight 통과 후 생성 국면만 실패시키고, S-15·S-16은 `git worktree lock`/`unlock`을 쓴다. 원안 "추적 파일 수정"은 `GUARD_DIRTY`에 먼저 걸려 `WORKTREE_REMOVE_FAILED` 경로 관측 불가 — 워커 판단이 정확. TEST-SCENARIO 조건 칸을 그 방식으로 정정해 문서·테스트 일치 | 승인 |
| 26 | 2026-09-12 18:46 | EXECUTE | ERROR | **PM 실수** — `red_required` 정정 반영을 위해 `scenario-init`을 재호출했으나 이 명령은 spec을 통째로 덮어쓰며 `red_confirmed`를 항상 false로 강제한다(056/ADD-1). 백업 증거로 재기록하려 했으나 플래그를 `--scenario`로 잘못 씀(정답 `--id`) → 19건 전부 실패, 실패 확인 전 백업 파일 삭제로 증거 텍스트 소실 | 증거 19건 소실 |
| 27 | 2026-09-12 18:47 | EXECUTE | DECISION | 복구 경로로 PM 자가 재작성이 아니라 **W-1 워커 재개**를 선택 — RED 증거는 작성자가 실제 관측한 사실이어야 하고(red-first §1.5-3), PM이 텍스트를 지어내면 증거 성격이 무너진다. 테스트 코드·미구현 상태가 그대로라 재현 가능함을 확인한 뒤 재관측·재기록 요청 | 재개 요청 |
| 28 | 2026-09-12 18:50 | EXECUTE | GATE | W-1 재기록 완료 직접 검증 — `scenario-status` `red_confirmed: 19/19`, 증거가 증거 재생이 아니라 **개별 재실행 재측정**(S-9·S-26 원문 확인: 실패 명령·exit 1·AssertionError 원문·미구현 계약 포함). 테스트 파일 이번 턴 변경 0(`843 insertions` 유지), 기존 83건 GREEN·H-2 보호 4건 무수정 통과. `scenario-lock` 성공(`locked: true`) | Pass |
| 29 | 2026-09-12 18:51 | EXECUTE | DECISION | P2~P4(W-3→W-4→W-5)를 단일 워커에 직렬 디스패치 — `worktree_tool.py` 단일 파일이고 변경 지점이 서로의 반환값을 소비해 병렬 이점이 없다(PLAN Approach). RED 테스트 수정 금지와 monorepo 분기 무변경을 [MUST]로 주입 | 디스패치 |
| 30 | 2026-09-12 19:03 | EXECUTE | GATE | W-3~W-5 구현 수신 — RED 19건 전건 GREEN, 스위트 118 passed/0 failed(착수 전 28 failed/90 passed). H-2 보호 4건 무수정 통과. monorepo 분기에 `sparse-checkout`·`taskCapsuleCone` diff 0줄 | Pass |
| 31 | 2026-09-12 19:04 | EXECUTE | BLOCKER | 워커가 자진 신고한 계약 이탈 확인 — 2축 멱등 판정과 제거 실패 관측이 layout 분기 없이 전 layout에 적용(`worktree_tool.py:1417`). `harness/worktree.md:88`이 해당 절을 "monorepo·비워크트리 경로는 이 절의 어떤 항목도 소비하지 않는다"로 한정하므로 규범 위반이고, monorepo 실패 표면 2종(`WORKTREE_NOT_FOUND`→skip / →`WORKTREE_REMOVE_FAILED`)이 바뀌어 C-2·C-3과 충돌 | 규범 위반 검출 |
| 32 | 2026-09-12 19:05 | EXECUTE | DECISION | multi-repo 한정으로 확정. monorepo 확장(returncode 미검사 + 무조건 `_delete_meta`가 함께 만드는 데이터 손실 경로)은 타당한 개선이나 "신규 계약은 전부 multi-repo 분기 안에 갇힌다"는 이 태스크의 안전 논거를 우선 — DONE 후속 항목으로 이연. W-7 `cmp`가 성공 경로만 측정해 놓칠 수 있는 지점이라 지금 차단 | 한정 확정 |
| 33 | 2026-09-12 19:08 | EXECUTE | GATE | 보정 직접 검증 — `multi_repo = meta.get("layout") == "multi-repo"`(`:1423`)로 2축 판정(`:1438`)·returncode 관측(`:1486`)·역순(`:1432`) 한정, `_rollback`은 `preserve_on_failure` 인자로 분리(`:999·:1189`). 워커가 meta 동결 layout을 쓴 판단 타당 — remove가 `load_config`를 호출한 적이 없어 회수 시점 재해석은 DEC-3이 봉인한 결함류를 되살린다. 스위트 118 passed 유지 | Pass |
| 34 | 2026-09-12 19:09 | EXECUTE | GATE | 워커가 C-2 비공허성을 직접 실증 — `HEAD:worktree_tool.py`와 현재 버전을 동형 monorepo fixture에서 "경로 부재 + 등록 잔존" 상태로 강제한 뒤 `remove`를 `--force` 유무 2경로로 실행해 출력 바이트 동일 확인(legacy 응답 복원). 미측정이 아니라 복원임을 증거로 고정 | Pass |
| 35 | 2026-09-12 19:10 | EXECUTE | DECISION | W-6 디스패치 — `@header` 갱신 시 "회수 계약은 multi-repo 분기 전용"을 명시하도록 지시. 빠지면 독자가 전 layout 적용으로 오해하며, 방금 닫은 이탈이 문서 층에서 재발한다 | 디스패치 |
| 36 | 2026-09-12 19:18 | EXECUTE | BLOCKER | W-6가 선존재 결함 신고 — `worktree_tool.py`의 `@header`가 유효 JSON이 아니라 `code-scan scan`이 이 파일을 조용히 건너뛴다(`2 file(s)`). `validate`의 `coverage 50% (1/2)` OK는 파일이 분모에서 빠진 결과이고, 그 통과는 S-25를 공허하게 만든다. `HEAD`에도 존재 | 규범 아닌 도구 결함 |
| 37 | 2026-09-12 19:19 | EXECUTE | DECISION | 후속 이연이 아니라 **이번 범위에서 수정** 확정 — 이번 태스크가 소유한 파일이고, 파싱 불가 헤더는 C-9의 "현재 사실 갱신"을 소비 불가로 만들며, W-6가 방금 적은 신규 계약 서술이 코드맵에 실리지 않는다. 수정은 따옴표 이스케이프 2문자로 기계적 | 범위 내 수정 |
| 38 | 2026-09-12 19:22 | EXECUTE | GATE | W-6 완료 직접 검증 — 균형 괄호 추출 후 `json.loads` OK(keys 6종, `exports` 14건·`depends` 무변경 확인), `code-scan scan` **3 file(s)**로 `worktree_tool.py` 등재, `validate --changed` **coverage 100% (2/2)**(이전 50%), 스위트 118 passed 유지. `@header`에 "회수 계약은 multi-repo 분기 전용, monorepo·비워크트리는 기존 `WORKTREE_NOT_FOUND` 유지" 명시 확인 | Pass |
| 39 | 2026-09-12 19:23 | EXECUTE | DECISION | W-7 디스패치 — 119 §0.1 "데이터 루트 고정·코드만 스왑" 재사용을 [MUST]로 고정하고, **monorepo 실패 표면 전용 측정 1건을 추가 지시**. 구현 중 실패 표면이 바뀌었다 되돌려진 이력이 있어 성공 경로 6명령만 측정하면 놓친다 | 디스패치 |
| 40 | 2026-09-12 19:40 | EXECUTE | GATE | W-7 회귀 증거 직접 검증 — `cmp` **42쌍 차이 0줄·정규화 0회**(monorepo 18 + 비워크트리 18 + 실패 표면 6). 비공허성: 배포본 3파일 sha256 base↔after 전부 DIFF, after에 신설 식별자 11회(base 0회). H-3 미실현 확증 — cfg 반환 키가 8→10으로 늘었는데도 6명령 바이트 동일. `init` 초안 monorepo 8키 791바이트 동일. 실 `~/.opal/` mtime 17:02 불변(C-8) | Pass |
| 41 | 2026-09-12 19:44 | EXECUTE | GATE | W-8 직접 검증 — 제안서↔구현 대조 21항목 전건 일치, 제안서에 없는 계약 0건, 회수 계약의 multi-repo 한정도 대조표에 포함. 잔여 인용 0건(태스크 폴더 3파일은 `proposal-lifecycle.md` §절차 1 제외 대상). `git mv`로 archives 이동(`RM` 인덱스 등록, 삭제 아님), 선행 제안서 헤더 `**미이관**` → `**§8 대체**` 포인터 교체. `docs/proposals/` 잔존 0건 | Pass |
| 42 | 2026-09-12 19:45 | EXECUTE | DECISION | `execute.implement` mark. W-8이 이동으로 깨진 상대링크 1건(`./archives/...` → `./...`)을 함께 고쳤다고 보고 — 배정 파일 범위 안이고 내용 변경이 아니라 승인 | EXECUTE 완료 |
| 43 | 2026-09-12 19:46 | TEST | GATE | `stage.test`(scenario-gate·qa-standards 2문서) load·verify ok. `opal-test-agent` BE 모드 디스패치 — 전체 스위트 1회 통과를 28건 증거로 뭉뚱그리지 말고 **시나리오별 개별 실행 증거**를 남기도록 지시. 이미 수집된 회귀 증거는 절 번호 인용을 허용 | 디스패치 |
| 44 | 2026-09-12 19:52 | TEST | GATE | TEST 판정 직접 검증 — `scenario-status` `passed: 28, failed: 0, blocked: 0`, `red_confirmed_required: 19/19`. 증거가 시나리오별 개별 실행(pytest 21건·33 케이스, 결정론 4건, 회귀 2건은 절 번호 인용). S-20에서 회귀 4건 함수 본문 md5가 HEAD와 전건 SAME임을 확인해 "무수정 통과"를 실증 | Pass |
| 45 | 2026-09-12 19:53 | TEST | GATE | C-8 재확인 — 배포본 sha256 `2bb746daa1e4…` = HEAD, 워크트리 변경본 `21f6c12d0ec5…`와 상이. 19:37 `~/.opal/` mtime 갱신은 타 세션 install이며 124 변경분 미배포 | Pass |
| 46 | 2026-09-12 19:54 | TEST | ERROR | **PM 하네스 위반** — `dispatch-process.md` Step 0의 "[MUST] 매 워커 디스패치 직전에 새로 수행"을 어기고 18:05 발급 receipt를 재사용. 19:37 타 세션 배포로 manifest 해시가 바뀌어 stale이 됐고, 이후 디스패치 3건(W-8·TEST·컨벤션)이 stale receipt 수령 | 위반 발생 |
| 47 | 2026-09-12 19:55 | TEST | DECISION | 영향 판정 후 결과 채택 — 문서 집합 동일(4종), `dispatch-process.md` 1건만 +437바이트(`actor=pm` preflight 문구)로 **PM 지침 변경이지 워커 문서 변경이 아니다**. 세 워커 산출물의 실질에 영향 없음. 신규 receipt 발급·verify ok로 게이트 재수립 후 이후 디스패치에 사용 | 결과 유지 |
| 48 | 2026-09-12 19:55 | TEST | DECISION | 컨벤션 워커의 계약 이탈 판정 — role 계약 §3은 stale 시 즉시 blocked인데 재load·재검증 후 진행. 문면 위반이나 실질은 더 안전하고 **스스로 신고**했다. 결과 채택하되 FW 개선 후보로 기록 — 워커 문서가 불변인 드리프트에 전면 왕복을 강제하는 계약에 "재load·재검증 후 계속" 경로가 없다 | 채택 + 개선후보 |
| 49 | 2026-09-12 19:56 | TEST | GATE | 컨벤션 진단 PASS_WITH_ADVISORIES — Critical 0/High 0(Gate 항목 4 충족), Medium 2·Low 1은 `docs/CONVENTIONS.md`가 `header_history` 비차단으로 명시 지정한 축. 워커가 행 길이 오탐(바이트 146행 → 문자 2행)을 스스로 걷어낸 점 확인 | Pass |
| 50 | 2026-09-12 19:57 | TEST | DECISION | GC-001·GC-003 중 **124 추가분만** 보정 지시 — C-9는 "현재 사실"을 요구하므로 변경 서술은 124 범위 내 결함이다. 092~119 선행 누적은 다른 태스크의 근거라 미조치(후속). GC-002(총량 5,487자) 역시 선행 서술 재작성이 필요해 후속 | 범위 한정 |
| 51 | 2026-09-12 20:01 | TEST | GATE | 보정 직접 검증 — 변경 서술 잔존 **0건**, `description` 5,487→5,465자, JSON 파싱 OK·`exports` 14 무변경, multi-repo 전용 한정·R-1~R-5·`check-ignore` 사실 전부 보존. 스위트 118 passed 유지. 워커가 `test_worktree_tool.py`는 124 추가분에 변경 서술이 없어 무변경 판정 — 타당 | Pass |
| 52 | 2026-09-12 20:02 | TEST | GATE | PM Gate 4항목 통과 — ① TEST-SCENARIO.md에 결과·증거 기록 없음(매칭 2건은 `WORKTREE_REMOVE_FAILED` 오류 코드명) ② 28건 전건 `result=pass`+evidence 존재 ③ 스위트 118 passed ④ 컨벤션 Critical/High 0. `test.run_tests`·`test.pm_gate` mark | Pass |
| 53 | 2026-09-12 20:05 | CLOSE | GATE | 캡틴 CLOSE 진입 승인 수신(`test.user_confirm` owner=user). `stage.close`(additional-work·memory-learning·proposal-lifecycle 3문서) load·verify ok | Pass |
| 54 | 2026-09-12 20:07 | CLOSE | ERROR | `close.done_md` mark가 `worker_duration_undeclared`로 차단 — 워커 디스패치 규범 단계 3행(PLAN·EXECUTE·TEST 작업)이 소요 미기록. 하네스 duration_ms를 분 환산해 선언(9/65/19분) 후 통과 | 자체 해소 |
| 55 | 2026-09-12 20:09 | CLOSE | DECISION | 회고 하드스텝 — FW 개선 3건(code-scan 파싱 불가 헤더 무보고 skip / `--changed` 공백 인자 fail-open / worker role 계약의 stale receipt 재수립 경로 부재), 로컬 PM 개선 2건 기록 | 5건 기록 |
| 56 | 2026-09-12 20:11 | CLOSE | ERROR | **PM 경계 위반** — 로컬 개선 2건을 `--project-root <허브>`로 기록해 허브 `.opal/MEMORY.json`에 직접 append했다. `memory-learning.md` §워크트리 memory 명령 경계는 워크트리 태스크의 `append --kind memory`를 **index 요청으로 지연**하도록 규정한다. 본문 .md도 생성되지 않아 인덱스 행만 dangling | 위반 발생 |
| 57 | 2026-09-12 20:13 | CLOSE | DECISION | 원복 후 계약 경로로 재기록 — `memory-tool delete --orphan --ref`로 허브 행 2건 제거(허브 diff 원복 확인), 워크트리 `.opal/memory/`에 본문 2건 작성 후 `append --kind memory --task-path`로 재기록해 `deferred: true` 확인. 캡슐 소유권이 이번 태스크의 주제인 만큼 여기서 경계를 넘기지 않는 편이 맞다고 판단 | 정정 완료 |
| 58 | 2026-09-12 20:14 | CLOSE | GATE | 제안서 아카이브 판정(`proposal-lifecycle.md` §절차 1) — 잔여 인용 **0건** 확인, §절차 2(이동) 경로 성립. 이관은 W-8이 이미 `git mv`로 수행 | Pass |

