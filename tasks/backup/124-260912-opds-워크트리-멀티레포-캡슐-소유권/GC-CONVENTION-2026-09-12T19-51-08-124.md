# GC CONVENTION REPORT — 2026-09-12T19-51-08

<!--
  체크박스 5단계 상태 기호 (APPLY 단계가 기입):
  [ ]  open    — 미처리 (신규)
  [x]  done    — 적용 완료  ← 주석: 적용 시각 YYYY-MM-DD HH:mm + 수정 요약
  [~]  pending — 보류       ← 주석: 보류 사유
  [?]  review  — 확인 필요  ← 주석: 판단 근거 / 해결 방안
  [!]  failed  — 실패       ← 주석: 실패 사유 / 권장 대안
-->

## 1. 헤더

- 실행 일시: 시작 2026-09-12 19:46:50 / 완료 2026-09-12 19:51:08 / 소요 4분 18초
- 범위: `all` (단일 CONVENTIONS.md 프로젝트) / 대상 파일 12개 (변경 5건 + 태스크 폴더 산출물 7건)
- 에이전트: opal-convention-checker (스킬 `op-gc-convention`, `~/.opal/skills/op-gc-convention/SKILL.md`)
- 기준 문서: `docs/CONVENTIONS.md` 존재 — 초안 생성 유도 생략
- APPLY 수행 여부: N (read-only 진단, 수동 대기)
- 최종 판정: **PASS_WITH_ADVISORIES** (blocking 0건 / advisory 3건)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 3 |
| 심각도 분포 | Critical 0 / High 0 / Medium 2 / Low 1 / Info 0 |
| 자동 수정 가능 | 0 |
| 수동 조치 필요 | 3 |
| 파일별 상위 Top 5 | `worktree_tool.py` (2건) / `tests/test_worktree_tool.py` (1건) |
| 카테고리별 빈도 | 문서화 (2 파일) |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 0 (빈도 트리거 미달 — 2파일 < N=3, 새 카테고리 0건) |

3건 전부 **동일 축**이다 — `@header`의 `description` 필드에 변경 이력이 누적된다.
나머지 검사 영역(네이밍·들여쓰기·파일 구조·죽은 코드·미사용 import·import 순서)은 위반 0건이다.

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

없음.

### High (0건)

없음.

### Medium (2건)

- [ ] GC-001 [`opal/tools/worktree-tool/worktree_tool.py:6`] `@header.description`에 변경 이력 서술이 누적된다
  - 카테고리: 문서화
  - 위반 기준: 프로젝트 `opal/core/references/header-standard.md` §2.1 이력 비기재 원칙 (T0) — "`@header`의 어느 필드에도 **변경 이력을 기재하지 않는다**", "필드를 갱신할 때는 이전 값 옆에 덧붙이지 않고 **제자리에서 교체**한다", 적용 범위에 `description`이 명시됨. 연결: `docs/CONVENTIONS.md` §@header 규칙
  - 설명: 대표 문장 — "오류 코드 5종 TASK_ARTIFACT_REPO_INVALID·TASK_ARTIFACT_REPO_UNSUPPORTED·TASK_ARTIFACT_REPO_OVERLAP·CONFIG_UNKNOWN_REPO·WORKTREE_REMOVE_FAILED**가 신설됐고 기존 코드의 문구·의미는 바뀌지 않았다**." 이는 현재 사실이 아니라 이번 변경의 서술이다. 같은 축의 변경 상대 서술이 3건 더 있다 — "`task_artifacts` 미설정 multi-repo는 **종전대로** …", "monorepo는 **기존** 무조건 정리 동작 유지", "monorepo·비워크트리는 … **그대로 유지**한다". git diff 관측상 이번 태스크는 기존 description을 제자리 교체하지 않고 문장을 삽입·추가했다 (3,263자 → 5,487자, 삭제 0문장).
  - 해결 방안: 변경 상대 서술을 제거하고 현재 계약만 남긴다. "신설됐고" / "종전대로" / "기존 … 유지" / "그대로 유지"가 붙은 문장을 현재 사실 문장으로 바꾸거나 삭제한다. 이번 태스크의 변경 서술은 `tasks/124-*/DONE.md`가 갖는다 (§2.1이 지정한 이력 소재 2곳: git log, DONE.md).
  - 자동 수정: N
  - 참조: `opal/core/references/header-standard.md` §2.1

- [ ] GC-003 [`opal/tools/worktree-tool/tests/test_worktree_tool.py:6`] `@header.description`이 태스크 번호별 절을 누적한다
  - 카테고리: 문서화
  - 위반 기준: 프로젝트 `opal/core/references/header-standard.md` §2.1 (T0) — 위와 동일 규칙
  - 설명: description이 092 · 112 · 118 · 119 · 124 각 태스크의 절을 이어 붙여 보관한다. 과거 변경 서술도 남아 있다 — "119는 태스크 092의 stale 문서 단언 3건(…)을 현재 문면으로 교체했다", "118 S-3·119 S-7은 불변식 보존형이라 구현 전에도 PASS하는 회귀 보호 케이스다". 이번 태스크는 124 절을 append했고 삭제한 문장은 없다(826자 → 1,134자). 태스크마다 절이 하나씩 늘어나는 구조다.
  - 해결 방안: 태스크 번호별 누적 절을 현재 커버리지 서술로 통합한다 — 이 파일이 지금 검증하는 시나리오 집합과 검증 방식(CLI subprocess · 실 git fixture · mock/patch 금지)만 남기고, 어느 태스크가 언제 무엇을 바꿨는지는 git log와 DONE.md에 맡긴다.
  - 자동 수정: N
  - 참조: `opal/core/references/header-standard.md` §2.1

### Low (1건)

- [ ] GC-002 [`opal/tools/worktree-tool/worktree_tool.py:6`] `description`이 "한 줄 요약" 필드 계약을 벗어난다
  - 카테고리: 문서화
  - 위반 기준: 프로젝트 `opal/core/references/header-standard.md` §2 필드 정의 (T0) — `description`은 "파일의 역할 **한 줄 요약**"
  - 설명: 실측 5,487자 단일 문자열, 문장 40개 이상. 이 파일에서 100자를 넘는 행은 이 한 행뿐이며(추가 1,151행 중 100자 초과 2행이 전부 `@header.description`이다), `@header`의 목적인 "파일 전체를 Read하는 토큰 낭비를 줄인다"(§1)가 역전된다.
  - 해결 방안: description은 역할 요약으로 줄이고, 계약 상세는 이미 소유자가 있는 `opal/core/references/harness/worktree.md`를 가리킨다 — 해당 포인터 문장("계약 원문은 `opal/core/references/harness/worktree.md`가 소유한다")은 이미 description 안에 존재하므로, 그 뒤 상세 서술은 중복이다.
  - 자동 수정: N
  - 참조: `opal/core/references/header-standard.md` §2 필드 정의 · §1 목적

### Info (0건)

없음.

---

### 집행 수준에 대한 주석 (판단이 갈리는 지점)

3건 모두 `source_tier: T0`이며 `gc-finding-schema.md` §5상 **blocking 가능**한 계층이다.
그럼에도 `disposition: advisory`로 둔 근거는 `docs/CONVENTIONS.md` §@header 규칙이
이 축의 관측을 **명시적으로 비차단으로 지정**했다는 사실 하나다 —
"`code-scan validate`의 `header_history` **비차단 경고**가 누적 서술을 관측한다".

다만 `header-standard.md` §2.1의 규칙 **문면 자체는 무조건적**이며 예외를 두지 않는다.
비차단은 도구의 관측 방식에 대한 지정이지 규칙의 완화가 아니므로,
PM이 이 3건을 blocking으로 승격하는 판단은 근거가 있다. 검사자가 임의로 면책하지 않고 그대로 올린다.

추가 사실 — description 누적은 이번 태스크가 시작한 것이 아니라 092·112·118·119를 거쳐 이어져 온
**선행 상태**이며, 태스크 124는 그 위에 append했다. 따라서 조치 범위를 이번 추가분으로 한정할지
description 전체를 재작성할지는 PM 결정 사항이다.

---

## 4. 문서 업데이트 제안 (§9·§10, 트리거 발동 시만)

트리거 미발동 — 제안 없음.

- 빈도 트리거: 동일 fingerprint 기준 최대 1파일, 동일 `rule_id`(§2.1) 기준 2파일 < 임계값 N=3.
- 새 카테고리 트리거: 발견된 카테고리(문서화)는 `docs/CONVENTIONS.md` §@header 규칙 · `header-standard.md` §2.1이 이미 규정한다. 신설 제안 없음.

---

## 5. 문서 작성 유도 (해당 시)

`docs/CONVENTIONS.md` 존재 — 작성 유도 생략.

---

## 6. 통과 확인 항목 (위반 없음)

기준이 적용되어 검사했고 위반이 나오지 않은 항목이다. 판정 근거로 남긴다.

| 항목 | 기준 | 관측 |
|------|------|------|
| 제안서 아카이브 이관 | `docs/CONVENTIONS.md` §제안서 생명주기 **[MUST]** "아카이브 이동의 선행 조건은 잔여 인용 0건" | `opal/**`·`docs/**`에서 구 경로 `docs/proposals/opal-worktree-multirepo-ownership.md`를 규범 근거로 가리키는 문장 **0건**. 잔여 언급은 태스크 폴더 이력(TASK/PLAN/REGRESSION-EVIDENCE)뿐이며 PLAN W-8이 범위에서 제외한 대상이다. `archives/opal-worktree-task-ownership.md:12`의 링크는 상대 경로 `./opal-worktree-multirepo-ownership.md`이고 두 문서가 같은 `archives/`에 있어 정상 해석된다 |
| 제안서 상태 어휘 | 같은 절 — `제안`·`검토`·`적용완료`·`폐기` 4개만 | 두 문서 모두 `> 상태: 적용완료`. 아카이브 6건 전건이 어휘 준수 |
| 삭제 아닌 이동 | 같은 절 — "삭제하지 않고 이동한다" | `git status`가 `RM`(rename)으로 관측. 원문 보존 |
| 규범 SSOT 귀속 | 같은 절 — "제안서는 규범 SSOT가 될 수 없다" | 이관 문서 헤더가 `harness/worktree.md` §multi-repo 캡슐 소유권 계약을 소유자로 명시, 해당 절이 실재 |
| 수기 누적 이력 절 | `opal-doc-standard.md` §5 | 변경 5파일 + 태스크 폴더 .md 7건 전건에서 `변경이력`·`Changelog`·`history`·`revisions` 계열 헤더 **0건** |
| 파일 네이밍 | `docs/CONVENTIONS.md` §네이밍 규칙 (Python snake_case, 문서 kebab-case) | 변경 전건 준수 |
| 태스크 폴더 네이밍 | 같은 절 — `{NNN}-{YYMMDD}-{스킬약어}-{태스크명}`, 앞 3요소 ASCII, 공백 금지, 태스크명 한글 | `124-260912-opds-워크트리-멀티레포-캡슐-소유권` 준수 |
| 식별자 네이밍 | 같은 절 (코드/변수/필드명 English, Python snake_case) | 추가 `def` 전건 snake_case. 테스트 명명 `t124_s{N}_*`은 인접 패턴(`t118`·`t119`)과 일치 |
| 들여쓰기·포맷 | 카테고리 2 (탭 혼용 / EOF 개행 / trailing whitespace) | 변경 5파일 전건 위반 0 — 탭 0, trailing whitespace 0, EOF 개행 정상 |
| 행 길이 | 포매터·린터 설정 부재(pyproject/setup.cfg/.flake8/ruff/.editorconfig 없음) → 기준 없음 | 참고 관측: 추가 1,151행 중 문자 기준 100자 초과 2행이며 둘 다 `@header.description`(GC-001·GC-003으로 이미 계상). 코드 본문은 초과 0행 |
| 미사용 import | 카테고리 5 | AST 검사 실질 0건 (`from __future__ import annotations`는 false positive) |
| @header 파싱·커버리지 | `docs/CONVENTIONS.md` §@header 규칙, `.opal/code-scan.json` `headerSource: inline` | `code-scan validate --changed` OK(exit 0), `code-scan scan opal/tools/worktree-tool/` → 3 file(s) 정상 파싱. 선행 파싱 오류(따옴표 미이스케이프) 해소 확인 |
| 실행 지시문 문서 기준 | `opal-doc-standard.md` §3 | `harness/worktree.md` 신규 절은 입력·출력 계약, 분기·종료 조건, `[MUST]` 경계 중심. 병기된 근거 문장은 판정 순서 역전을 막는 집행 경계 서술이라 §3 "도입 경위·논의 과정"에 해당하지 않는다고 판정 |
| 문서 frontmatter | 카테고리 6 | `harness/worktree.md`는 frontmatter 보유. 단 harness 참조 문서 24건 중 11건만 보유해 강제 컨벤션이 성립하지 않음 — 검사 비활성 |
| State 규칙 | `docs/CONVENTIONS.md` §State 관리 **[MUST]** state.json 직접 편집 금지 | `STATE.md`가 "파이프라인 현황의 SSOT는 `state.json`" 포인터를 유지하고 행 상태를 복제하지 않음. 의사결정 로그 1건은 `state-tool`이 기입한 `gate_artifact_force` 계열 기록 |
| 도구·배포 경계 | `docs/CONVENTIONS.md` §배포 경계 | `~/.opal/` 배포본 변경 0건 (변경 전건이 `opal/`·`docs/` 소스) |
| 플랫폼 분기 격리 | `docs/CONVENTIONS.md` §플랫폼 분기 격리 | 추가 코드에 플랫폼 조건문 0건 |
| Citation 규칙 | `docs/CONVENTIONS.md` §Citation Rules | 적용 대상(TASK/PLAN/ANALYSIS/QA 산출물) — 표본 확인상 `{경로}:{라인}`·`문서 §섹션` 형식 유지 |

### 비활성 검사 영역과 사유

| 영역 | 사유 |
|------|------|
| 카테고리 7 (import 순서) | 파이썬 2파일 모두 import 블록 변경 없음 — 이번 변경 범위 밖 |
| 카테고리 4 (죽은 코드) | 신규 헬퍼 전건이 호출부를 가짐(`_build_init_draft_base`는 `_build_init_draft`가, `_root_*`는 `cmd_create`·`_build_init_draft`가 소비). 미참조 export 0건 |
| 카테고리 8 (코드 품질) | `docs/CONVENTIONS.md`에 대응 규칙이 없어 "추가 제안"으로만 표시 가능한 영역 — 제안할 관측 없음 |
| 커밋 메시지 규칙 | 미커밋 상태(`git status` 기준)라 판정 대상 아님 |

---

## 7. 실행 근거

- `git status --porcelain` / `git diff --stat` — 변경 5건 확인 (RM 1 / M 4)
- `~/.opal/tools/code-scan/run.sh validate --changed` → OK (exit 0)
- `~/.opal/tools/code-scan/run.sh scan opal/tools/worktree-tool/` → 3 file(s), `@header` 파싱 정상
- `grep -rn 'opal-worktree-multirepo-ownership'` — 이관 잔여 인용 검사
- 문자 기준 행 길이·trailing whitespace·탭·EOF 개행 검사 (변경 5파일 전건)
- 포매터·린터 설정 부재 확인 — `pyproject.toml`·`setup.cfg`·`.flake8`·`ruff.toml`·`.editorconfig` 없음
- AST 기반 미사용 import 검사 (Python 2파일)
- 태스크 폴더 `.md` 7건 — `opal-doc-standard.md` §5 · 버전 사본 · State SSOT 포인터 검사

## 8. 적용 기준 문서

- `docs/CONVENTIONS.md` (T0)
- `opal/core/references/header-standard.md` (T0)
- `opal/core/references/opal-doc-standard.md` (T0)
- `opal/core/references/harness/gc-finding-schema.md` — finding 필드·판정·fingerprint SSOT
- `docs/PROJECT.md` — 네이밍 규칙
- `~/.opal/skills/op-gc-convention/references/convention-categories.md` — 카테고리 가이드

## 9. 산출물

- 보고서: `tasks/124-260912-opds-워크트리-멀티레포-캡슐-소유권/GC-CONVENTION-2026-09-12T19-51-08-124.md`
- finding JSON: `tasks/124-260912-opds-워크트리-멀티레포-캡슐-소유권/gc-findings-convention-2026-09-12T19-51-08-124.json`

baseline `none` — `gc-finding-schema.md` §7에 따라 현재 finding 3건 전부 `new`로 표기한다.
