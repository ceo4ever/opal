# GC CONVENTION REPORT — 2026-08-22

## 1. 헤더

- 실행 일시: 시작 2026-08-22 15:00:00 / 완료 2026-08-22 15:12:00 / 소요 약 12분
- 범위: `diff` (opal/core/AGENT.md, opal/core/references/opal-pm.md — 지정 변경 범위 한정) / 대상 파일 2개
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` (존재 — v1.8.0)
- APPLY 수행 여부: N (readonly 지시, 보고서만 생성)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 4 |
| 심각도 분포 | Critical 0 / High 0 / Medium 1 / Low 1 / Info 2 |
| 자동 수정 가능 | 1 |
| 수동 조치 필요 | 3 |
| 파일별 상위 | opal/core/AGENT.md (4건) / opal/core/references/opal-pm.md (0건) |
| 카테고리별 빈도 | 문서화(변경이력 형식) 1건 / 문서화(포맷 스타일) 1건 / 참고(선재 결함) 2건 |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 0 (트리거 미발동 — 아래 §4 참조) |

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

### High (0건)

### Medium (1건)

- [ ] GC-C001 [opal/core/AGENT.md:314-315] 변경이력 `v4.9`·`v5.0` 행에 시각(HH:mm) 누락
  - 카테고리: 문서화 (변경이력 형식)
  - 위반 기준: 프로젝트(`docs/CONVENTIONS.md` §변경이력, §변경이력 작성 의무 — "일시는 `YYYY-MM-DD HH:mm`(KST)")
  - 설명: 본 태스크(099)가 AGENT.md에 추가한 변경이력 5행(`v4.8`~`v5.2`) 중 `v4.8`(`23:42`)·`v5.1`(`14:27`)·`v5.2`(`15:05`) 3행은 시각을 포함하나, `v4.9`·`v5.0` 2행은 날짜(`2026-08-22`)만 있고 시각이 없다. 같은 태스크가 한 번에 추가한 행 사이에서도 형식이 갈려, 규칙 미준수가 우발이 아니라 태스크 내부 비일관으로 드러난다.
  - 해결 방안: `v4.9`·`v5.0` 두 행의 일시 값에 실제 작성 시각(KST, `HH:mm`)을 보강한다. (참고: `opal-pm.md` `v1.8` 행은 AGENT.md `v4.8`과 동일 시각 `23:42`로 정합하게 기재되어 있어, 동일 세션 다른 행에서는 시각 표기가 정상 작동함이 확인된다.)
  - 자동 수정: N (정확한 작성 시각은 기계적으로 복원 불가 — 소유자/작성자 확인 필요)
  - 참조: `docs/CONVENTIONS.md` §변경이력, §변경이력 작성 의무

### Low (1건)

- [ ] GC-C002 [opal/core/AGENT.md:198-199] 골격 예시 코드펜스 앞 공백 줄 부재
  - 카테고리: 문서화 (포맷 스타일)
  - 위반 기준: `docs/CONVENTIONS.md`에 명문 규칙 없음 — CommonMark 일반 관례 기반 제안(추가 제안, 위반 아님)
  - 설명: "`📌 잔여·보류·유의`의 3어: ... **적용 범위(H-9)**: ..." 산문 문단 바로 다음 줄에 공백 줄 없이 ` ``` ` 코드펜스가 시작된다. 일부 렌더러에서는 문제없이 파싱되나, 문단-코드블록 사이 빈 줄을 두는 쪽이 렌더링 안전성이 높다.
  - 해결 방안: 코드펜스 시작 전에 빈 줄 1개 삽입.
  - 자동 수정: Y (공백 줄 삽입 — 기계적 단순 수정)
  - 참조: TBD — CommonMark Spec §4.5 Fenced code blocks (https://spec.commonmark.org/)

### Info (2건)

- [ ] GC-C003 [opal/core/AGENT.md:306-307] 변경이력 `v4.3`·`v4.4` 중복 채번(2쌍, 4행) — **선재 결함, 099 비귀책**
  - 카테고리: 문서화 (변경이력 형식)
  - 설명: `v4.3`(2026-07-13 10:20)·`v4.4`(2026-07-13 10:33) 행과 별개로 `v4.3`(2026-07-10 13:12)·`v4.4`(2026-07-10 13:43) 행이 중복 존재한다(§정체성 적용 관련, L2/054 태스크). 디스패치 지시에 따라 본 태스크(099)가 만든 결함이 아니며 Surgical 원칙상 본 태스크 범위에서 수정 대상이 아니다. 기록 목적으로만 표기.
  - 해결 방안: 별도 태스크에서 두 쌍 중 하나를 재채번 정정.
  - 자동 수정: N
  - 참조: TBD — 별도 태스크 재채번 시 참조할 `opal/core/references/harness/task-process.md` §태스크 번호 채번 규칙과 유사한 semver 채번 규율 문서 (현재 미정)

- [ ] GC-C004 [opal/core/AGENT.md 변경이력 표 전체] 버전 표기가 `vX.Y` 2-세그먼트로 통일 — CONVENTIONS.md 명시 `vX.Y.Z` 3-세그먼트 semver와 불일치
  - 카테고리: 문서화 (변경이력 형식)
  - 설명: `docs/CONVENTIONS.md` §변경이력은 "버전: semver (`vX.Y.Z`)"를 명시하나, `AGENT.md` 변경이력 표는 `v3.8`부터 `v5.2`까지 전 행이 `vX.Y` 2-세그먼트로 일관 기재되어 있다(099 이전부터의 파일 전체 관행). 본 태스크가 추가한 `v4.8`~`v5.2` 5행도 이 기존 표기를 그대로 따랐을 뿐 새로 도입한 편차가 아니다.
  - 해결 방안: 표기 방식을 3-세그먼트로 통일할지 여부는 파일 전체 재채번이 필요한 별도 결정 사안 — 099 범위 밖.
  - 자동 수정: N
  - 참조: `docs/CONVENTIONS.md` §변경이력

---

## 4. 문서 업데이트 제안 (트리거 발동 시만)

트리거 미발동 (빈도 트리거: 동일 fingerprint 3파일 이상 조건 — 대상 파일 2개뿐이라 구조적으로 미충족. 새 카테고리 트리거: 전 이슈가 기존 "문서화" 카테고리 내).

---

## 5. 문서 작성 유도 (해당 시)

`docs/CONVENTIONS.md` 존재 — 작성 유도 생략.

---

## 검증 결과 요약 (지시 §특히 확인할 것 대응)

| # | 확인 항목 | 결과 |
|---|-----------|------|
| 1 | 변경이력 형식(일시 KST·semver·태스크 번호) | 부분 위반 — `v4.9`·`v5.0` 시각 누락(GC-C001, Medium). semver·태스크번호(099)는 5행 전부 정상 |
| 2 | semver 채번 연속성 | 정상 — AGENT.md `v4.7→v4.8→...→v5.2`, opal-pm.md `v1.7→v1.8` 연속. 기존 `v4.3`/`v4.4` 중복은 선재 결함으로 별도 표기(GC-C003), 099 책임 아님 |
| 3 | 문서 네이밍·구조 컨벤션(헤딩 위계·표 형식·인용 표기) | 이상 없음 — 코드펜스 내 `## 🎯`/`## 📌`/`## ▶️`는 예시 리터럴이며 실제 문서 헤딩 위계(`### 보고 형식` → `## 변경이력`)를 훼손하지 않음 |
| 4 | SSOT 발췌·복제 금지 | 통과 — `opal-pm.md` §8 「핵심」 포인터는 구조 요약 1줄만 두고, 마크다운 어휘·10개 원칙 원문은 복제하지 않음. "전문은 AGENT.md §보고 형식이 소유한다" 명시 |
| 5 | 소유자 호칭 하드코딩 | 통과 — 신규 문안 전체에서 "캡틴" 등 개인 호칭 0건 확인(grep 검증) |

---

## 반환

```json
{"critical": 0, "high": 0, "medium": 1, "low": 1, "verdict": "pass", "findings": ["GC-C001", "GC-C002", "GC-C003", "GC-C004"]}
```
