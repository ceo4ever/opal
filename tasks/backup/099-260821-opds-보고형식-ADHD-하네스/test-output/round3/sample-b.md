opal/skills/ 42개 스킬을 references/ 보유 여부로 계열별 분류하고, 표준화 여부에 대한 권고안을 정리했다.
▶ 다음 액션: 권고안(2안) 채택 여부를 확인한다 (소요: 2분)

---
## 🎯 결론·근거
> **1️⃣ 표준화 편차는 무작위가 아니라 스킬 계열 단위로 나뉜다 [상]**
- 전체 42개 중 26개(62%)가 `references/`를 보유하고, 16개(38%)는 미보유다.
- `op-task-*`(4/4)·`op-data-*`(3/3)·`opal-pilot-*`(10/10) 3개 계열은 100% 보유로 이미 일관돼 있다.
- `op-sdd-*` 계열(`op-sdd-action-plan`·`op-sdd-plan`·`op-sdd-spec`·`op-sdd-verify`)은 4/4 전부 미보유이며, SKILL.md가 316~428줄로 `references/`를 가진 `op-dev-plan`(470줄)과 규모가 유사하다.
- `op-dev-*` 계열은 6/7 보유, `op-dev-wireframe`(102줄)만 예외다.
**단일 계열 내부 편차(`op-dev-wireframe`)와 계열 전체 편차(`op-sdd-*`)가 섞여 있어, 전면 표준화와 현행 유지 둘 다 실측과 어긋난다.**
> **2️⃣ opal-* 독립 유틸 스킬 8개는 전부 references 없이 SKILL.md 단일 파일이다 [중]**
- `opal-action-status`(79줄)·`opal-help`(211줄)·`opal-skill-creator`(224줄) 등 8개는 파이프라인 단계가 아닌 1회성 유틸/메타 스킬이다.
- 이 그룹은 크기와 무관하게(79~267줄) 전부 미보유로, 파이프라인 계열과 다른 자연 발생 패턴을 보인다.
**독립 유틸 스킬군은 references 부재가 계열 특성과 일치해, 표준화 강제 시 빈 디렉토리만 늘어날 위험이 있다.**

---
## 📌 잔여·보류·유의
- `op-spec-validator`·`op-brain-ingest`·`op-scenario-gate` 3개는 계열에 속하지 않는 단일 op-* 스킬로, 이번 분류에서 opal-* 유틸군과 동일하게 취급했다(유의).

---
## ▶️ 승인 요청
- [ ] 1) 계열 한정 표준화(2안)를 채택한다
  - **1안**: 42개 전체에 `references/` 강제 — 일관성은 최대지만 `opal-*` 유틸 8개에 빈 디렉토리가 생길 위험.
  - **2안(권고)**: 파이프라인 계열(`op-dev-*`·`op-task-*`·`op-data-*`·`op-sdd-*`·`opal-pilot-*`)만 `references/` 보유로 통일 — `op-dev-wireframe` 1곳 신설, `op-sdd-*` 4곳 신설(총 5곳 작업), 독립 유틸 8곳은 현행 유지.
  - **3안**: 현행 유지 — 작업량 0이지만 `op-sdd-*` 전체 계열 편차가 그대로 남는다.
