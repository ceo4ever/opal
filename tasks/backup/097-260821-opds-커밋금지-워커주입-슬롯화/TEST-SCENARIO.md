# TEST SCENARIO: 워커 커밋 금지 주입 슬롯화

> 작성일: 2026-08-21 | 상태: 실행 완료 (23/23 판정) (Block A 선작성 + Block B 보강 + 게이트 후 순증분 2건)
> 작성자: 알투(PM) | 도출 트랙: 목표계열 선작성 (`opal/core/references/harness/red-first.md` §1.6)
> 도출 입력 — Block A(TASK 유래): §작업 목표 / R-1~R-5 / §명확화 결과 · Block B(PLAN 유래): F-001~F-005 / H-1~H-12

## 선작성 착수 근거

| 판단 기준 (red-first §1.6 (f)) | 이 태스크 |
|------|------|
| 목표가 파괴 관점으로 환원되지 않는다 | ✓ 목표는 "규칙이 워커에게 **도달**한다"는 채택 관점이며, 파괴 관점은 "문자열이 존재한다"에 머물 위험이 크다 |
| 교체형 목표를 포함한다 | ✓ R-5 — CONVENTIONS 원문 → 하네스 포인터 교체 |
| PLAN 워커 예상 소요 > 선작성 소요 | ✓ 변경 대상 14파일 + 설계 쟁점 5건 |

## 보강 라운드 기록 (Block A → Block B)

| # | 보강 성격 | 내용 |
|---|----------|------|
| 1 | 추가 | S-18~S-21 신설 — H-2(각주 단정)·H-3(변경이력 사실)·H-4(원격 카운트)·H-6(gc 리터럴) 대응 |
| 2 | **수정** | S-2 판정식 보정 — "파일 전역 0건"을 **규범 구간 한정**으로 교체. PLAN H-3이 `dispatch-process.md:191`(v1.6 변경이력)의 "공통 고정 2항목"을 **과거 사실 기록**으로 식별했고, 전역 0건 집행은 사실 위조가 된다 |
| 3 | **수정** | S-6 판정식 보정 — gc 파일럿의 디스패치 프롬프트 리터럴(`opal-pilot-gc/SKILL.md:219`)을 "단일 형태 통일" 대상에서 **명시 제외**. PLAN H-6이 이를 규범 복제가 아닌 주입 산출물로 층 구분했다 |
| 6 | **수정** | S-4 판정식 보정 — `grep -c '(097)'` 단독은 과거 번호 체계의 2026-04-07 변경이력 행과 충돌한다(파일럿 3종 실측 각 2건). 날짜(`2026-08-21`) 결합 판정식으로 교체 |
| 5 | 추가(게이트 후) | S-22·S-23 신설 — evaluator 보강 권고 A-1(발신 프롬프트 리터럴층 무검증)·A-2(SSOT 절 불변 판정 부재). 강제 재작성 요구(gaps[])가 아니며 순증분이므로 결정론 커버리지 불변 |
| 4 | 확장 | S-4 대상을 "변경 파일 전건 변경이력"으로 확장 (H-12 — 12개 문서) |

> 선작성 초안 대비 불일치 2건(S-2·S-6 판정식)은 PLAN 설계가 초안의 AC 해석을 정정한 사례이며, PM Gate 조기 경보로 보고 대상이다.

## 1. 리스크 가설 표

> PLAN.md §리스크 가설 표 H-1~H-12 전건 전재.

| ID | 변경 단위 | 깨질 수 있는 계약 | 운영 영향 | 검증 계층 | 시나리오 |
|----|----------|----------------|---------|---------|---------|
| H-1 | F-001 `pm/dispatch-process.md:94-95` 슬롯 | 「전 워커 공통 고정」 항목 집합이 3건으로 확정되고 표기 행 수가 계약이 된다. 표기 누락 시 PM이 조건부 항목으로 오독해 스킵 가능 | P0 | L1 | S-1, S-3 |
| H-2 | F-002 `:115` 각주 | 현행 각주의 "근거 문서가 없는 운영 규율이다"는 집합 전체 단정이다. 신규 항목은 하네스 §1 파생이므로 개수만 바꾸면 각주가 거짓이 된다 | P1 | L1 | S-18 |
| H-3 | F-002 변경이력 표 | `:191`(v1.6)에 "공통 고정 2항목"이 사실 기록으로 존재한다. 전역 0건 집행은 과거 사실 위조 | P1 | L1 | S-2, S-19 |
| H-4 | F-004 `op-dev-execute/SKILL.md:97` | 개수 서술이 참조 문서 쪽에 원격 복제되어 있다. 미수정 시 SSOT 3항목 ↔ 소비자 2항목 즉시 불일치 | P0 | L1 | S-20 |
| H-5 | F-003 파일럿 10종 | 파일럿의 `[PM 컨텍스트 주입]` 블록은 PM 대상 지시이며 워커 프롬프트 본문이 아니다. 워커 전달 경로로 오인해 10곳 열거 시 복제 고착 | P1 | L2 | S-5, S-6 |
| H-6 | F-003 `opal-pilot-gc/SKILL.md:219` | gc의 `[MUST] 커밋 금지`는 디스패치 프롬프트 리터럴 본문이다 — 규범 복제가 아니라 주입 산출물. 층 구분 없이 통일 AC 적용 시 유효 방어를 제거 | P1 | L2 | S-21 |
| H-7 | F-005 `docs/CONVENTIONS.md` | 커밋 금지 원문이 2곳(`:203` §Guards, `:188` §커밋 규칙)이다. TASK.md는 `:203`만 지목 — `:188` 누락 시 AC(a) 미충족 | P0 | L1 | S-8 |
| H-8 | F-005 `docs/PROJECT.md:223` | 레지스트리 용도 서술이 CONVENTIONS.md의 Guards 원문 보유를 전제한다. 방치 시 SSOT 간 내부 모순 잔존 | P1 | L2 | S-10 |
| H-9 | F-005 순서 | R-5를 R-1보다 먼저 적용하면 커밋 금지 도달 경로가 일시적으로 0개가 된다 | P0 | L2 | S-12 |
| H-10 | 전 변경 | `pm/dispatch-process.md`·`docs/CONVENTIONS.md`·`opal-pilot-gc/SKILL.md`는 기존 도구 테스트가 실제로 읽는 파일이다. 편집 구간 어긋나면 무관한 테스트가 깨진다 | P1 | L2 | S-16 |
| H-11 | 전 변경 | 배포본(`~/.opal/`)과 소스가 이원화 상태다. 소스만 고치면 이번 세션 런타임에는 미적용 — "적용됐다" 오판정 위험 | P1 | L1 | S-13 |
| H-12 | 전 변경 | 스킬·참조 문서 수정 시 변경이력 행 추가가 의무다. 12파일 누락 시 컨벤션 위반 | P2 | L1 | S-4 |

## 2. 테스트 데이터 설계

### 2.1 사전 조건 데이터

> 코드·DB 변경 0건 태스크이므로 "테이블→대상 파일", "식별자→경로", "출처→원본 상태 취득 방법"으로 치환한다.

| 대상 파일 | 식별자 | 상태 | 출처 |
|--------|--------|------|------|
| 주입 템플릿 | `opal/core/references/pm/dispatch-process.md` | 공통 고정 2항목(94-95행), 각주 「2항목」(115행), v1.6 변경이력 「2항목」(191행) | 변경 전 git HEAD 사본 |
| 단계 스킬 | `opal/skills/op-dev-execute/SKILL.md` | §절대 금지 6행(108-115행), `:97` 원격 카운트 「2항목」 | 변경 전 git HEAD 사본 |
| 파일럿 | `opal/skills/opal-pilot-*/SKILL.md` (10종) | 주입 블록 3종 보유 / 7종 미보유, `pm/dispatch-process.md` 참조 0건, gc 리터럴 1건(`:219`) | 변경 전 grep 계수 |
| 프로젝트 컨벤션 | `docs/CONVENTIONS.md` | 커밋 금지 원문 2건(`:188`, `:203`) | 변경 전 git HEAD 사본 |
| 문서 레지스트리 | `docs/PROJECT.md` | `:223` 용도 서술에 "커밋 규칙, 구현 규칙(Guards/...)" | 변경 전 git HEAD 사본 |
| 규칙 SSOT | `opal/core/references/opal-harness.md` | §1 커밋 규칙 원문(40-42행) — **불변 대상** | 변경 전/후 shasum 대조 |
| 배포본 | `~/.opal/**` | 착수 시점 상태 | 변경 대상 대응 배포본 shasum |
| git | HEAD | 착수 시점 커밋 | `git rev-parse HEAD` |

### 2.2 시나리오별 데이터 흐름

| 시나리오 | Given (read) | When (변경) | Then (re-read) |
|---------|------------|----------------|---------------|
| S-1, S-3 | dispatch-process.md 94-95행 사본 | Step 1 ① 슬롯 1행 삽입 | 표기 행 3건 + 신규 문언 필수 요소 재확인 |
| S-2, S-18, S-19 | `:115` 각주 + `:191` 변경이력 | Step 1 ②③ 각주 재작성·이력 추가 | 규범 구간 3항목 / 성격 2분류 / v1.6 기록 보존 |
| S-4 | 변경 대상 12파일 변경이력 표 | 전 Step | 097 행 각 1건 |
| S-7, S-20 | op-dev-execute 6행 + `:97` | Step 2 ①②③ | 7행 + 포인터 / 원격 카운트 제거 |
| S-5, S-6, S-21 | 파일럿 10종 grep 계수 | Step 3~6 | 참조 10/10 / 열거형 0 / gc 리터럴 보존 |
| S-8, S-9 | CONVENTIONS `:188`·`:203` | Step 7 (Phase 2) | 원문 0건 + 포인터 1건 |
| S-10 | PROJECT.md `:223` | Step 7 | 레지스트리 정합 |
| S-11 | 개정 후 dispatch-process 구조 | - | 조건부 스킵 각주 비적용 확인 |
| S-12 | PLAN §4.1 Phase 배치 + STATE 저널 | Step 8 | R-5가 R-1 후행 Phase / 저널 기록 1건 |
| S-13 | 배포본 shasum | 전 Step (install 미실행) | shasum 동일 |
| S-14 | 산출물 전체 | 전 Step | 외부 프로젝트 참조 0건 |
| S-15 | 착수 시점 HEAD | 전 Step | HEAD 불변 |
| S-16 | 기존 테스트 baseline | 전 Step | 전건 pass·감소 0 |
| S-17 | 확정 문언 | - | 캡틴 수용 판정 |

## 3. 검증 시나리오

### L1. 기능 단위 (자동, 실 파일 판정)

#### S-1: 주입 슬롯에 신규 항목이 3번째로 존재한다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-1 |
| 대상 | `pm/dispatch-process.md` §워커 컨텍스트 주입 템플릿 §핵심 제약 (F-001) |
| 계층 | L1 |
| **실행 방식** | **M1 (테스트 도구 — grep 결정론 판정)** |
| 조건 | Step 1 ① 완료 |
| 기대 결과 | `← 전 워커 공통 고정` 표기 행이 정확히 3건이고, 신규 행이 `git commit`·`git push`·`git reset`·`git rebase` 4종과 워킹트리 보존 문구를 포함한다 |
| 도구 | grep -c |
| 실행 명령 | `grep -c '← 전 워커 공통 고정' opal/core/references/pm/dispatch-process.md` |
| 결과 | Pass |
| 상세 | 출력 `3` (94·95·96행). 96행 신규 행 원문 확인: `- [MUST] git 이력 변경 금지: \`git commit\`·\`git push\`·\`git reset\`·\`git rebase\`를 실행하지 않는다. 변경은 워킹트리에 남기고 완료 보고한다. 커밋·머지는 소유자 권한이다(규칙 SSOT: \`opal/core/references/opal-harness.md\` §1 커밋 규칙).  ← 전 워커 공통 고정` — 4종 서브명령 + 워킹트리 보존 문구 전건 포함 |

#### S-2: 규범 구간의 개수 서술이 3항목으로 갱신되었다 [판정식 보정]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-3 |
| 대상 | `pm/dispatch-process.md` 규범 구간(81~180행) (F-002) |
| 계층 | L1 |
| **실행 방식** | **M1** |
| 조건 | Step 1 ② 완료 |
| 기대 결과 | **규범 구간 한정** 「공통 고정 2항목」 0건 AND 「공통 고정 3항목」 >= 1건. 변경이력 구간은 판정 범위에서 제외한다(S-19가 별도 검증) |
| 도구 | sed 범위 + grep -c |
| 실행 명령 | `awk 'NR>=81 && NR<=180' opal/core/references/pm/dispatch-process.md \| grep -c '공통 고정 2항목'` / `awk 'NR>=81 && NR<=180' <동일파일> \| grep -c '공통 고정 3항목'` |
| 결과 | Pass |
| 상세 | 규범 구간(81~180행) 「공통 고정 2항목」 = 0, 「공통 고정 3항목」 = 1 (>=1 충족). 변경이력 구간(184행 이후)은 판정 범위에서 제외 — S-19가 별도 검증 |

#### S-3: 신규 항목이 규칙 SSOT 포인터를 병기한다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-1 |
| 대상 | `pm/dispatch-process.md` 신규 [MUST] 항목 (F-001) |
| 계층 | L1 |
| **실행 방식** | **M1** |
| 조건 | Step 1 ① 완료 |
| 기대 결과 | 신규 항목에 `opal-harness.md` §1 포인터가 존재하고, 하네스 §1 원문 전문 재서술은 없다 |
| 도구 | grep + 판독 |
| 실행 명령 | `sed -n '96p' opal/core/references/pm/dispatch-process.md` |
| 결과 | Pass |
| 상세 | 신규 행에 `(규칙 SSOT: \`opal/core/references/opal-harness.md\` §1 커밋 규칙)` 포인터 존재. 하네스 §1 원문("커밋은 사용자가 명시적으로 요청할 때만 수행한다...") 전문 재서술 없음 — 포인터만 병기 |

#### S-4: 변경 파일 전건에 변경이력 행이 추가되었다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-12 |
| 대상 | 변경한 12개 문서의 변경이력 표 (F-002~F-005) |
| 계층 | L1 |
| **실행 방식** | **M1** |
| 조건 | 전 Step 완료 |
| 기대 결과 | 변경이력 표를 보유한 변경 파일 전건에 **`2026-08-21` 일시 + `(097)`** 행이 1건씩 추가되고, 일시가 KST 실측값이다. **판정식 주의**: `grep -c '(097)'` 단독은 과거 번호 체계의 2026-04-07 행(구 태스크 097)과 충돌해 2를 반환한다 — 날짜 결합 판정식을 사용한다 |
| 도구 | grep -l + 계수 |
| 실행 명령 | `for f in <변경 14파일>; do grep -cE '^\|.*2026-08-21.*\(097\)' "$f"; done` (날짜+`(097)` 결합 판정식 — `(097)` 단독 계수는 2026-04-07 구 097 행과 충돌하므로 배제) |
| 결과 | Pass |
| 상세 | 변경이력 표를 보유한 변경 파일 14/14(`pm/dispatch-process.md`, `op-dev-execute/SKILL.md`, 파일럿 10종, `docs/CONVENTIONS.md`, `docs/PROJECT.md`) 전건에서 각 1건씩 검출. 일시는 15:19~15:30(KST) 범위의 실측값. 참고: changed_files 목록은 13개로 명시됐으나 실제 git status 변경분은 위 14개 문서(+ `.opal/MEMORY.json` state 파일, 변경이력 표 없음이라 대상 제외)로, 14개 전건 확인함 |

#### S-7: 절대 금지 표에 항목이 추가되었다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-4 |
| 대상 | `op-dev-execute/SKILL.md` §절대 금지 (F-004) |
| 계층 | L1 |
| **실행 방식** | **M1** |
| 조건 | Step 2 ① 완료 |
| 기대 결과 | 표 데이터 행이 7건이고, `| 7 |` 행 이유 칼럼에 `opal-harness.md` §1 포인터가 있다 |
| 도구 | grep -c |
| 실행 명령 | `awk '/^### 절대 금지/,/^### 보안 가드레일/' opal/skills/op-dev-execute/SKILL.md \| grep -cE '^\| [0-9]+ \|'` (헤딩 구간 격리 — `sed -n` 행번호 하드코딩 대신 헤딩 앵커 사용) |
| 결과 | Pass |
| 상세 | 표 데이터 행 수 = 7 (기존 6 + 신규 `\| 7 \|`). #7 행 이유 칼럼: "커밋·머지는 소유자 권한 — 자동 커밋 금지(`opal/core/references/opal-harness.md` §1 커밋 규칙). 변경은 워킹트리에 남긴다: PM의 산출물 실측 판정 근거가 워킹트리이므로(`opal/core/references/harness/pm-review-gate.md` §워커 중단 시 산출물 실측 판정) 이력 변경은 판정 입력을 파괴한다" — 포인터 존재 |

#### S-8: [교체형 — 구형 잔존 0] CONVENTIONS.md 커밋 금지 원문 2곳이 제거되었다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-7 |
| 대상 | `docs/CONVENTIONS.md` `:188`(§커밋 규칙) + `:203`(§Guards) (F-005) |
| 계층 | L1 |
| **실행 방식** | **M1** |
| 조건 | Step 1 완료 후 Step 7 적용 |
| 기대 결과 | 커밋 승인 조건을 서술하는 원문이 **2곳 전부** 0건 — `:203` 단독 처리는 FAIL |
| 도구 | grep -c (전수) |
| 실행 명령 | `grep -cE '커밋은 (캡틴\|사용자)[이가] 명시적으로 요청할 때만 수행' docs/CONVENTIONS.md` (조사 결합 `이\|가` 정정 — `이`만 쓰면 1건만 검출되는 함정 회피) |
| 결과 | Pass |
| 상세 | 출력 `0` — `:188`(§커밋 규칙 §규칙) · `:203`(§구현 규칙 §Guards) 2곳 원문 전부 제거 확인. `:203` 단독 처리가 아니라 2곳 전부 0건 |

#### S-9: [교체형 — 신형 채택] CONVENTIONS.md에 하네스 포인터가 존재한다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-7 |
| 대상 | `docs/CONVENTIONS.md` (F-005) |
| 계층 | L1 |
| **실행 방식** | **M1** |
| 조건 | Step 7 완료 |
| 기대 결과 | 커밋 규칙을 `opal-harness.md` §1로 위임하는 포인터 문장이 1건 이상 존재하고, §커밋 규칙 절이 커밋 메시지 형식·단위 규칙으로서 여전히 유효하다 |
| 도구 | grep + 판독 |
| 실행 명령 | `sed -n '183,210p' docs/CONVENTIONS.md`(§커밋 규칙 §규칙 절 판독) |
| 결과 | Pass |
| 상세 | `:188` "커밋 실행 시점 규칙(사용자 요청 시에만 수행 · 자동 커밋 금지)은 \`opal/core/references/opal-harness.md\` §1 Guards가 소유한다 — 본 절은 커밋 **메시지 형식·단위**만 규정한다." 포인터 1건 존재(§Guards 근거 행 `:203` 포함 총 2건). §커밋 규칙 절은 커밋 메시지 한국어·태스크당 1커밋 원칙·CLOSE 히스토리 규칙으로 여전히 유효 |

#### S-13: [부정] 배포본이 변경되지 않았다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-11 |
| 대상 | `~/.opal/**` 변경 대응 파일 |
| 계층 | L1 |
| **실행 방식** | **M1** |
| 조건 | 전 Step 완료, install 미실행 |
| 기대 결과 | 배포본 shasum이 착수 시점과 동일 — 소스만 변경. 동시에 "소스 변경 = 런타임 적용"이 **아님**이 완료 보고에 명시된다 |
| 도구 | shasum 대조 |
| 실행 명령 | `shasum -a 1 ~/.opal/references/pm/dispatch-process.md` / `shasum -a 1 ~/.opal/skills/op-dev-execute/SKILL.md` |
| 결과 | Pass |
| 상세 | 배포본 dispatch = `9ea6073794065e851dc1252fe7e6f835b051b023` (BASELINE.md 값과 동일), 배포본 op-dev-execute = `a25f66ec5580919b4ddb7284e164f91905d7e113` (동일) — install 미실행으로 배포본 무변경 확인. 소스(`opal/...`)만 변경되었고 런타임(`~/.opal/...`)에는 미적용 — "소스 변경 = 런타임 적용"이 아님을 본 보고에 명시 |

#### S-14: [부정] 외부 프로젝트 참조가 산출물에 없다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-11 |
| 대상 | `tasks/097-*/**` + 변경 프레임워크 문서 전건 |
| 계층 | L1 |
| **실행 방식** | **M1** |
| 조건 | 전 Step 완료 |
| 기대 결과 | 발단 사례 프로젝트명 및 그 프로젝트 태스크 번호 표기가 0건 |
| 도구 | grep |
| 실행 명령 | `git diff <변경 14파일> \| grep -E '^\+' \| grep -viE '^\+\+\+' \| grep -iE '<발단 사례 프로젝트명>'` |
| 결과 | Pass |
| 상세 | 변경분(diff `+` 행)에서 발단 사례 프로젝트명·그 프로젝트 태스크번호 0건. 태스크 폴더 내 TASK.md·PLAN.md·SCENARIO-GATE-1.md에서 검출된 문구는 "외부 프로젝트 근거 인용 금지"라는 **제약 자체를 서술하는 문장**뿐이며 실제 프로젝트명·경로·태스크번호를 노출하지 않음 |

#### S-15: [부정·자기적용] 자동 커밋이 발생하지 않았다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-11 |
| 대상 | git 저장소 HEAD |
| 계층 | L1 |
| **실행 방식** | **M1** |
| 조건 | EXECUTE·TEST 완료 시점 |
| 기대 결과 | 착수 시점 HEAD와 동일 — 신규 커밋 0건, 변경은 전부 워킹트리 잔존. 이 태스크가 신설하는 규칙을 태스크 자신이 준수했음을 증명한다 |
| 도구 | git rev-parse HEAD 대조 |
| 실행 명령 | `git rev-parse HEAD` |
| 결과 | Pass |
| 상세 | 출력 `f3dd43d0c41d2b5840c3a074623f46b63513c065` — BASELINE.md 착수 시점 HEAD와 동일. 신규 커밋 0건, 변경 14파일 전부 워킹트리(unstaged)에 잔존(`git status --short` 확인). 태스크 자신이 신설 규칙(git 이력 변경 금지)을 준수함을 증명 |

#### S-18: 각주가 항목 성격 2분류를 서술한다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-2 |
| 대상 | `pm/dispatch-process.md` `:115` 각주 (F-002) |
| 계층 | L1 |
| **실행 방식** | **M1** |
| 조건 | Step 1 ② 완료 |
| 기대 결과 | 각주가 "근거 문서 없는 운영 규율"(증분 저장·입력 축소)과 "하네스 §1 파생 규칙"(신규 항목)을 구분 서술 — 집합 전체를 근거 없는 규율로 단정하는 문장이 잔존하지 않는다 |
| 도구 | 판독 + grep |
| 실행 명령 | `sed -n '113,120p' opal/core/references/pm/dispatch-process.md` |
| 결과 | Pass |
| 상세 | `:115` 각주가 헤드 문장(전 워커 공통 고정 3항목 정의) + 2개 bullet로 재작성됨: "증분 저장 · 입력 축소 — **근거 문서가 없는 운영 규율**"과 "git 이력 변경 금지 — 규칙 SSOT는 `opal-harness.md` §1... 본 항목은 그 워커 도달 경로"로 성격 2분류 서술. 집합 전체를 근거 없는 규율로 단정하는 단일 문장 잔존 0건 |

#### S-19: 변경이력의 과거 사실 기록이 보존되었다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-3 |
| 대상 | `pm/dispatch-process.md` 변경이력 v1.6 행 (F-002) |
| 계층 | L1 |
| **실행 방식** | **M1** |
| 조건 | Step 1 ③ 완료 |
| 기대 결과 | v1.6 행의 "전 워커 공통 고정 2항목" 기록이 **원문 그대로 1건 보존** — 과거 사실을 소급 수정하지 않았다 |
| 도구 | grep (변경이력 구간 한정) |
| 실행 명령 | `sed -n '/^## 변경이력/,$p' opal/core/references/pm/dispatch-process.md \| grep -n '공통 고정 2항목'` |
| 결과 | Pass |
| 상세 | v1.6 행(`\| v1.6 \| 2026-08-02 16:06 \| ... 전 워커 공통 고정 2항목(증분 저장 · 입력 축소) 추가 ... (081) \|`)이 원문 그대로 1건 보존 — 과거 사실 소급 수정 없음. 동일 구간에 v1.8 신규 행(2026-08-21, (097))도 확인 |

#### S-20: 원격 카운트 복제가 제거되었다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-4 |
| 대상 | `op-dev-execute/SKILL.md:97` (F-004) |
| 계층 | L1 |
| **실행 방식** | **M1** |
| 조건 | Step 2 ② 완료 |
| 기대 결과 | 참조 문서 쪽 개수 서술(「2항목」)이 제거되고 개수 없는 포인터로 대체 — 개수는 목록 소유 문서(`dispatch-process.md`)에만 존재한다 |
| 도구 | grep -c |
| 실행 명령 | `grep -n '전 워커 공통 고정' opal/skills/op-dev-execute/SKILL.md` |
| 결과 | Pass |
| 상세 | `:97` 문말이 "...§핵심 제약(전 워커 공통 고정 항목)이며, 본 스킬은 이를 복제하지 않는다 — 항목 수·문언은 그 문서가 소유한다."로 개정 — 개수 서술(`2항목`/`3항목`) 제거 확인. 개수는 목록 소유 문서(`dispatch-process.md`)에만 존재 |

#### S-21: gc 파일럿의 프롬프트 리터럴이 보존되었다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-6 |
| 대상 | `opal-pilot-gc/SKILL.md:219` (F-003) |
| 계층 | L1 |
| **실행 방식** | **M1** |
| 조건 | Step 3~6 완료 |
| 기대 결과 | `[MUST] 커밋 금지 (git commit 호출 금지)` 리터럴이 디스패치 프롬프트 본문에 **그대로 존재** — 규범 복제 제거 대상으로 오분류되어 삭제되지 않았다 |
| 도구 | grep |
| 실행 명령 | `grep -n '\[MUST\] 커밋 금지 (git commit 호출 금지)' opal/skills/opal-pilot-gc/SKILL.md` |
| 결과 | Pass |
| 상세 | `:221`(편집 후 행번호 이동, 원 `:219`)에 리터럴 그대로 존재 확인. 변경이력 `:534` 행에도 "`[MUST] 커밋 금지 (git commit 호출 금지)` 리터럴은 워커에게 전달되는 프롬프트 본문(주입의 산출물)이므로 무변경 보존"으로 명시 — 규범 복제 제거 대상으로 오분류되어 삭제되지 않았음 |

#### S-23: [경계] 규칙 SSOT 절이 불변 유지되었다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-7 |
| 대상 | `opal/core/references/opal-harness.md` §1 커밋 규칙 (전 F — 포인터의 참조 대상) |
| 계층 | L1 |
| **실행 방식** | **M1** |
| 조건 | 전 Step 완료 |
| 기대 결과 | §1 커밋 규칙 절의 원문이 착수 시점과 동일(shasum·grep 대조) — S-9·S-20·S-3의 포인터는 **대상 절이 온존할 때만** 기능하므로 SSOT 무변경이 포인터화의 전제 조건이다 |
| 도구 | shasum + grep |
| 실행 명령 | `git diff --stat -- opal/core/references/opal-harness.md` |
| 결과 | Pass |
| 상세 | diff 출력 없음 — `opal-harness.md`는 이 태스크의 변경 대상 14파일에 포함되지 않으며 git HEAD(=BASELINE 착수 시점 커밋 `f3dd43d0...`, S-15로 확정) 대비 바이트 단위로 무변경. §1 커밋 규칙 절(`:40-43`)도 당연히 원문 그대로. **판정식 보정 근거**: BASELINE.md의 shasum 값(`4b3041449a6511b2353478d2a8a82b4ce096c623`)은 원 취득 시 사용한 정확한 라인 경계(awk/sed 범위)가 태스크 산출물에 기록되어 있지 않아 재현 시도(§1 전체 `:8-70`, §커밋 규칙 `:40-42`, `:40-43` 등 4개 경계 시도) 전건 불일치했다. 그러나 `git diff` 무출력이 라인 경계 불확실성보다 강한 증거다 — 파일 전체가 HEAD 대비 0바이트 변경이므로 부분 구간(§1)도 자동으로 불변이 보장된다 |

### L2. 프로세스 통합 (자동, 전 문서 집합 대조)

#### S-5: 파일럿 10종 전부가 주입 SSOT 참조를 보유한다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-5 |
| 대상 | `opal/skills/opal-pilot-*/SKILL.md` 10종 (F-003) |
| 계층 | L2 |
| **실행 방식** | **M1** |
| 조건 | Step 3~6 완료 |
| 기대 결과 | 10종 전부가 `pm/dispatch-process.md` 참조를 보유 (착수 전 0건 → 10건). 미보유 0종 |
| 도구 | grep -l 계수 |
| 실행 명령 | `grep -l 'pm/dispatch-process.md' opal/skills/opal-pilot-*/SKILL.md \| wc -l` |
| 결과 | Pass |
| 상세 | 출력 `10` — 파일럿 10종(dev·dev-short·dev-wireframe·project·project-dev·project-loop·write-tech·sdd·gc·data-design) 전부가 `pm/dispatch-process.md` 참조를 보유. 미보유 0종. 착수 전 0건 → 10건 |

#### S-6: 파일럿 규범 서술이 단일 형태로 통일되었다 [판정식 보정]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-5 |
| 대상 | 파일럿 10종 규범 서술층 (F-003) |
| 계층 | L2 |
| **실행 방식** | **M1** |
| 조건 | Step 3~6 완료 |
| 기대 결과 | 규범 서술층에서 항목 열거형이 0건이고 포인터 형태만 존재. **디스패치 프롬프트 리터럴층(gc `:219` 등)은 판정 대상에서 제외**한다 — 층이 다르며 유효 방어다 |
| 도구 | grep 패턴 2종 교차 계수 (리터럴 구간 제외) |
| 실행 명령 | `grep -cE '1\. 하네스 Guards 핵심 규칙' opal/skills/opal-pilot-*/SKILL.md` (열거형 잔존 계수) + `grep -n '\[PM 컨텍스트 주입\]' opal/skills/opal-pilot-*/SKILL.md` (정규 문언 형태 확인, `gc:221` 프롬프트 리터럴 구간은 별도 S-21에서 확인하고 본 계수에서 제외) |
| 결과 | Pass |
| 상세 | 열거형(`1. 하네스 Guards 핵심 규칙`) 잔존 0건(10파일 전부 count 0). `[PM 컨텍스트 주입]` 블록 10종 전부가 동일 정규 문언("디스패치 프롬프트 첫 줄에 \`[WORKER]\` 삽입. 주입 항목·핵심 제약(전 워커 공통 고정 포함)은 \`pm/dispatch-process.md\` §워커 컨텍스트 주입 템플릿을 따른다 — 본 스킬은 항목을 열거하지 않는다.")로 단일 형태 통일 — dev·dev-short는 단계 payload만 문말에 추가(정보 손실 0). gc `:221`의 디스패치 프롬프트 리터럴 `[MUST] 커밋 금지`는 판정 대상에서 제외(층이 다름, S-21에서 보존 확인) |

#### S-10: 문서 레지스트리 용도 서술이 정합화되었다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-8 |
| 대상 | `docs/PROJECT.md:223` 레지스트리 행 (F-005) |
| 계층 | L2 |
| **실행 방식** | **M1** |
| 조건 | Step 7 완료 |
| 기대 결과 | 용도 서술이 CONVENTIONS.md 실제 내용과 일치 — Guards 원문 보유 전제 표현(`구현 규칙(Guards/...)`) 잔존 0건, 「커밋 규칙」은 커밋 메시지 형식·단위 의미로 정정 |
| 도구 | grep + 교차 판독 |
| 실행 명령 | `grep -n 'CONVENTIONS.md' docs/PROJECT.md \| grep -c '구현 규칙(Guards/'` + `sed -n '223p' docs/PROJECT.md` |
| 결과 | Pass |
| 상세 | `구현 규칙(Guards/` 잔존 0건. `:223` 행이 "네이밍, 파일 구조, 커밋 **메시지 형식·단위**, 구현 규칙(디스패치/@header/Citation/State/도구·배포 경계/플랫폼 분기). 승인 게이트·커밋 실행 시점 등 Guards 규칙 **원문**은 `opal/core/references/opal-harness.md` §1이 소유하고 본 문서는 포인터만 둔다"로 정정 — CONVENTIONS.md 실제 내용(v1.7.0 포인터화)과 정합. 「커밋 규칙」은 메시지 형식·단위 의미로 정정됨 |

#### S-11: [목표달성·경계] CONVENTIONS.md가 없는 프로젝트에서도 주입이 성립한다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-1 |
| 대상 | `pm/dispatch-process.md` 신규 항목 위치 + 조건부 스킵 각주 (F-001) |
| 계층 | L2 |
| **실행 방식** | **M1** |
| 조건 | Step 1 완료 |
| 기대 결과 | 신규 항목이 「CONVENTIONS.md 부재 시 자연 스킵」 각주의 적용 대상이 아니고, 문서 선별 결과와 무관한 상시 주입 슬롯에 위치한다 — 신규 opi 프로젝트에서도 주입이 보장된다 |
| 도구 | 문서 구조 판독 + grep |
| 실행 명령 | `sed -n '86,120p' opal/core/references/pm/dispatch-process.md` (신규 항목 위치 vs 조건부 스킵 각주 위치 대조) |
| 결과 | Pass |
| 상세 | 「`docs/CONVENTIONS.md` 부재 시 본 항목은 자연 스킵」 각주(`:113`)는 §핵심 제약 코드블록 **밖**에서 CONVENTIONS.md 유래 [MUST] 항목(2번째 행)에만 적용되는 별개 각주다. 신규 3번째 항목(`:96`, `← 전 워커 공통 고정`)은 그 각주보다 아래에 있는 별도 「전 워커 공통 고정 3항목」 블록(`:115` 각주)에 속하며 문서 선별 결과와 무관하게 상시 주입된다 — CONVENTIONS.md 없는 신규 opi 프로젝트에서도 주입 보장 |

#### S-12: [경계] R-5가 R-1 이후 Phase에 배치·적용되었다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-9 |
| 대상 | `PLAN.md` §4.1 Phase 그룹핑 + STATE 저널 (F-005) |
| 계층 | L2 |
| **실행 방식** | **M1** |
| 조건 | Step 8 완료 |
| 기대 결과 | F-005 Step이 Phase 2이고 F-001 Step(Phase 1) 완료 후 착수됐음이 STATE 저널에 1건 기록 — 도달 경로 0 구간이 발생하지 않았다 |
| 도구 | PLAN 판독 + state-tool show |
| 실행 명령 | `sed -n '433,443p' PLAN.md`(§4.1 Phase 표) + `sed -n '/## 의사결정 로그/,/## 블로커/p' STATE.md` |
| 결과 | Pass |
| 상세 | PLAN §4.1: F-001/F-002(Step 1, Phase 1) → F-005(Step 7·8, Phase 2, "Step 1 완료 후에만 착수" 명시). STATE.md 의사결정 로그 #1(R-1 Phase 1 선행 완료, 15:19)·#2(R-5 Phase 2 후행 적용 — "Step 1 산출(15:19) 완료·PM 실측 Pass 확인 후 착수") 2건 기록. `state.json` execute.implement 단계 note에도 동일 근거("R-1(Step 1) 완료... 이후에 R-5(Step 7)를 착수... 도달 경로가 0개가 되는 구간은 발생하지 않았다") 기록 — 도달 경로 0 구간 미발생 |

#### S-16: [회귀] 기존 도구·스펙이 영향받지 않았다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-10 |
| 대상 | worktree-tool·code-scan·memory-tool·state-tool 테스트, pipeline.json, spec-validate (전 F) |
| 계층 | L2 |
| **실행 방식** | **M1** |
| 조건 | 전 Step 완료 |
| 기대 결과 | 기존 pytest·js 테스트 전건 pass(baseline 대비 감소 0), `pipeline.json` diff 0, spec-validate pass |
| 도구 | pytest, node, git diff, state-tool spec-validate |
| 실행 명령 | `python3 -m pytest opal/tools/worktree-tool/tests/test_worktree_tool.py -q` / `python3 -m pytest opal/tools/memory-tool/tests/test_memory_tool.py -q` / `python3 -m pytest opal/tools/state-tool/tests/test_state_tool.py -q` / `node opal/tools/code-scan/tests/test-regression.js` / `git diff --stat -- '**/pipeline.json'` / `~/.opal/tools/state-tool/run.sh spec-validate <pipeline.json>` (파일럿 10종) |
| 결과 | Pass |
| 상세 | worktree-tool: **56 passed**(0 fail). memory-tool: **187 passed, 31 subtests passed**(0 fail) — `:2578` gc 경로 참조 무결성 포함. state-tool: **324 passed, 3 skipped, 84 subtests passed**(0 fail). code-scan test-regression.js: **36 tests, 36 pass, 0 fail**(exit 0) — `:325,731,733` CONVENTIONS.md §@header 검사 포함, `:669-672` 상당 dispatch-process.md 문자열 검사 포함(worktree-tool 스위트 내). `pipeline.json` 10종 git diff 0건 + spec-validate 10종 전부 `{"ok": true, "violations_count": 0}`. baseline 대비 실패 수 증가 0(baseline 취득 실패 — 착수 전 실행 기록 없음, 대신 현재 결과 전건 pass로 실패 항목 0이므로 증가 여부와 무관하게 회귀 없음 확정) |

#### S-22: [목표달성] 발신 프롬프트 리터럴층까지 도달 사슬이 이어진다

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-5 |
| 대상 | 파일럿 10종의 디스패치 프롬프트 템플릿 「핵심 제약」 필드 (F-003) |
| 계층 | L2 |
| **실행 방식** | **M1** |
| 조건 | Step 3~6 완료 |
| 기대 결과 | 착수 전 baseline(「핵심 제약」 필드 보유 5종 — dev·sdd·dev-short·dev-wireframe·gc / 미보유 5종 — project·write-tech·project-dev·project-loop·data-design) 대비 보유 수가 **감소하지 않고**, 미보유 5종은 R-3 포인터로 주입 템플릿을 참조해 PM이 「핵심 제약」을 구성하도록 위임 경로가 명시된다. 도달 사슬의 마지막 링크(실제 발신 프롬프트)가 무검증 상태로 남지 않는다 |
| 도구 | grep -c 계수 (baseline 대조) |
| 실행 명령 | `grep -ln '\*\*핵심 제약\*\*:' opal/skills/opal-pilot-*/SKILL.md` (템플릿 필드 패턴) + `grep -n '핵심 제약' opal/skills/opal-pilot-gc/SKILL.md`(gc는 `## 핵심 제약 (Guards)` 헤딩형이라 별도 패턴) |
| 결과 | Pass |
| 상세 | `**핵심 제약**:` 필드 보유 4종(dev·dev-short·dev-wireframe·sdd, git-diff로 원문 대비 무변경 확인) + gc의 `## 핵심 제약 (Guards)` 헤딩(`:219`, git-diff 무변경) = **5/10 보유, baseline과 동일(감소 0)**. 단순 `핵심 제약` 문자열 grep은 10/10으로 나오나(신규 `[PM 컨텍스트 주입]` 정규 문언이 "주입 항목·핵심 제약(...)은 ... 따른다"로 이 어휘를 공통 포함하기 때문) 이는 실제 템플릿 필드 보유와 다른 층이므로 **정밀 패턴(`**핵심 제약**:` 및 헤딩형)으로 재계수**해 baseline 정의와 일치시켰다(판정식 함정 유사 사례 — 표층 문자열과 필드 실체 구분). 미보유 5종(project·write-tech·project-dev·project-loop·data-design)의 신규 `[PM 컨텍스트 주입]` 블록이 "주입 항목·핵심 제약(전 워커 공통 고정 포함)은 `pm/dispatch-process.md` §워커 컨텍스트 주입 템플릿을 따른다"로 PM에 「핵심 제약」 구성을 위임하는 경로를 명시 — 도달 사슬의 발신 프롬프트 리터럴층까지 무검증 상태로 남지 않음 |

### L3. 사용자 협업 (수동, [SUPERVISOR] 마커)

#### S-17: 주입 문언의 워커 통제력 수용 확인 [SUPERVISOR]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-1 |
| 대상 | 신규 [MUST] 항목 최종 문언 (F-001) |
| 계층 | L3 |
| **실행 방식** | **M3 (사용자 협업)** |
| 조건 | 전 Step 완료 후 보고 |
| 기대 결과 | 캡틴이 금지 범위(commit·push·reset·rebase / add·stash·checkout 제외)와 표현이 워커 통제에 충분하다고 판정 |
| 실행자 | [SUPERVISOR] — 캡틴 수동 확인 필요 |
| 결과 | **Pass** — 캡틴 승인 (2026-08-21). 금지 범위·제외 범위·문언 전문을 제시한 상태에서 CLOSE 승인과 함께 문언 수용 |
| 상세 | L3/M3([SUPERVISOR]) 시나리오 — opal-test-agent는 기계 검증 불가로 실행하지 않고 PM에 반환한다. 확인 대상 문언(F-001, `pm/dispatch-process.md:96`): "git 이력 변경 금지: `git commit`·`git push`·`git reset`·`git rebase`를 실행하지 않는다. 변경은 워킹트리에 남기고 완료 보고한다. 커밋·머지는 소유자 권한이다(규칙 SSOT: `opal-harness.md` §1 커밋 규칙)." — 금지 범위(commit·push·reset·rebase)와 제외 범위(add·stash·checkout)가 캡틴 판정 대상 |

## 4. AC ↔ 가설 ↔ 계층 ↔ 시나리오 매핑 표

| AC ID | 기능 | 가설 ID | 검증 계층 | 시나리오 | 테스트 파일:케이스 | 비고 |
|-------|------|---------|---------|---------|-----------------|------|
| R-1 AC | F-001 | H-1 | L1 | S-1 | grep 판정 [T097/L1-R1] | 축 ② 요구커버 |
| R-1 근거 병기 | F-001 | H-1 | L1 | S-3 | grep 판정 [T097/L1-R1] | 축 ② |
| R-2 AC 개수 정합 | F-002 | H-3 | L1 | S-2 | grep 판정 [T097/L1-R2] | 축 ② — 판정식 보정 |
| R-2 각주 성격 | F-002 | H-2 | L1 | S-18 | 판독 [T097/L1-R2] | 축 ② |
| R-2 변경이력 보존 | F-002 | H-3 | L1 | S-19 | grep 판정 [T097/L1-R2] | 축 ⑥ 경계 |
| R-2 변경이력 추가 | F-002 | H-12 | L1 | S-4 | grep 계수 [T097/L1-R2] | 축 ② |
| R-3 AC 도달 | F-003 | H-5 | L2 | S-5 | grep 계수 [T097/L2-R3] | 축 ② |
| R-3 AC 단일 형태 | F-003 | H-5 | L2 | S-6 | grep 계수 [T097/L2-R3] | 축 ② — 판정식 보정 |
| R-3 층 구분 보존 | F-003 | H-6 | L1 | S-21 | grep 판정 [T097/L1-R3] | 축 ⑥ 부정 |
| R-4 AC | F-004 | H-4 | L1 | S-7 | grep 계수 [T097/L1-R4] | 축 ② |
| R-4 원격 카운트 | F-004 | H-4 | L1 | S-20 | grep 계수 [T097/L1-R4] | 축 ② |
| R-5 AC (a) 구형 잔존0 | F-005 | H-7 | L1 | S-8 | grep 판정 [T097/L1-R5] | 축 ⑤ 채택·잔존 |
| R-5 AC (b) 신형 채택 | F-005 | H-7 | L1 | S-9 | grep 판정 [T097/L1-R5] | 축 ⑤ |
| R-5 AC (c) 순서 | F-005 | H-9 | L2 | S-12 | PLAN 판독 + state show [T097/L2-R5] | 축 ⑥ 경계 |
| R-5 파급 레지스트리 | F-005 | H-8 | L2 | S-10 | grep 판정 [T097/L2-R5] | 축 ② |
| 목표 — 워커 도달 보장 | F-001 | H-1 | L2 | S-11 | 구조 판독 [T097/L2-GOAL] | 축 ① 목표달성 (선작성 고유) |
| 제약 ① 배포 경계 | 전 F | H-11 | L1 | S-13 | shasum 대조 [T097/L1-C1] | 축 ⑥ 부정 |
| 제약 ⑤ 외부 비참조 | 전 F | H-11 | L1 | S-14 | grep [T097/L1-C5] | 축 ⑥ 부정 |
| 제약 ④ 커밋 규칙 | 전 F | H-11 | L1 | S-15 | HEAD 대조 [T097/L1-C4] | 축 ⑥ 부정 (자기적용) |
| 완료기준 — 회귀 0 | 전 F | H-10 | L2 | S-16 | pytest·diff [T097/L2-REG] | 축 ⑥ 경계 |
| 목표 — 발신 리터럴 도달 | F-003 | H-5 | L2 | S-22 | grep 계수 [T097/L2-GOAL2] | 축 ① — evaluator 보강 권고 A-1 |
| 전제 — SSOT 절 불변 | 전 F | H-7 | L1 | S-23 | shasum·grep [T097/L1-SSOT] | 축 ⑥ — evaluator 보강 권고 A-2 |
| 목표 — 문언 수용성 | F-001 | H-1 | L3 | S-17 | 캡틴 확인 [T097/L3-GOAL] | 축 ① (기계검증 불가) |

**커버리지 자기점검**: R-1~R-5 전건 매핑 / F-001~F-005 전건 매핑 / H-1~H-12 전건 매핑(H-1 S-1·S-3·S-11·S-17 / H-2 S-18 / H-3 S-2·S-19 / H-4 S-7·S-20 / H-5 S-5·S-6 / H-6 S-21 / H-7 S-8·S-9 / H-8 S-10 / H-9 S-12 / H-10 S-16 / H-11 S-13·S-14·S-15 / H-12 S-4). 게이트 PASS 후 evaluator 보강 권고 2건을 추가 반영(S-22 → H-5 / S-23 → H-7).

## 5. 코드 품질

| # | 검사 | 도구 | 결과 | 상세 |
|---|------|------|------|------|
| 1 | 린트 | 해당 없음 (md 전용) | N/A | 코드 변경 0줄 — 전량 Markdown 개정, 린트 대상 없음 |
| 2 | 타입 체크 | 해당 없음 | N/A | 동일 |
| 3 | 포맷터 | 해당 없음 | N/A | 동일 |
| 4 | 문서 스펙 검증 | state-tool spec-validate | Pass | 변경 대상 파일럿 10종의 `references/pipeline.json` git diff 0건(무변경) + 10종 전부 `~/.opal/tools/state-tool/run.sh spec-validate <file>` 실행 결과 `{"ok": true, "violations": [], "violations_count": 0}` |

## 6. 보안

| # | 항목 | 결과 | 상세 |
|---|------|------|------|
| 1 | 하드코딩 시크릿 스캔 | Pass | `git diff <변경 14파일> \| grep -E '^\+' \| grep -viE '^\+\+\+' \| grep -iE 'api[_-]?key\|secret\|password\|token[[:space:]]*=\|AKIA\|sk-[a-zA-Z0-9]'` 결과 0건 |
| 2 | .gitignore 확인 | Pass | `.opal/*` 무시 규칙 확인. `.opal/MEMORY.json`은 예외 트래킹 대상(기존 정책)이며 본 태스크로 `last_task_number: 96→97` 갱신 — 시크릿 없음, 정책 위반 아님 |

## 7. 판정

**All Pass**

**집계**: S-1~S-23 **23건 전건 판정 — 23 Pass / 0 Fail / 0 보류** (S-17은 2026-08-21 캡틴 승인으로 Pass 확정). 기계검증 시점 집계는 **22 Pass / 0 Fail / 1 보류**(S-17, [SUPERVISOR] 캡틴 확인 대기, 임의 Pass 처리하지 않음). 회귀 스위트 4종(worktree-tool 56건·code-scan 36건·memory-tool 187+31건·state-tool 324+3skip+84건) 전건 pass, 실패 수 증가 0. 부정 검증 5건(S-13 배포본 shasum 동일·S-15 HEAD 동일·S-23 하네스 §1 무변경·S-14 외부 프로젝트 참조 0건·S-22 「핵심 제약」 보유 5/10 유지) 전건 충족. 코드 품질 §5 전건 N/A 또는 Pass, 보안 §6 전건 Pass, 목업(mock/patch/MagicMock) 잔존 0건.

S-17만 기계 검증 불가로 보류 처리했으며 나머지 전 시나리오가 실행 출력 증거로 Pass 확정되어 **All Pass**로 판정한다. Critical/Partial Fail 요건(핵심 기능 Fail, 보안 Fail, 일부 시나리오 Fail)에 해당하는 항목 없음.

### PM Gate 체크 (7대 강제 룰)

- [x] mock/patch/MagicMock 등 시나리오 본문에 부재 — 전 시나리오 실행 명령이 grep/shasum/pytest/node 실측이며 mock 대체 0건
- [x] 사전 조건 데이터 표(§2.1) 모든 칸 채워짐
- [x] 모든 시나리오에 Given/When/Then(§2.2) 3필드 채워짐
- [x] 가설↔시나리오 매핑(§4) 완전 (미매핑 시나리오 없음) — H-1~H-12 전건 매핑, §4 커버리지 자기점검 확인
- [x] L1/L2/L3 계층 명시 (모든 시나리오)
