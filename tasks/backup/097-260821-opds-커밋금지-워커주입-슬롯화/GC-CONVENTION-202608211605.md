# GC CONVENTION REPORT — 202608211605

## 1. 헤더

- 실행 일시: 2026-08-21 16:05 (KST)
- 범위: 문서(scope=문서) / 대상 파일 14개 (changed_files 전건 `.md`, 코드 변경 0줄)
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` (존재 — 본 태스크가 자기 자신을 수정한 파일이기도 함, v1.6.1 → v1.7.0)
- APPLY 수행 여부: N (진단 전담, 워커는 읽기 전용)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 3 (Medium 1 / Low 2), 별도 소견 1건 |
| 심각도 분포 | Critical 0 / High 0 / Medium 1 / Low 2 / Info 0 |
| 자동 수정 가능 | 0 |
| 수동 조치 필요 | 3 |
| 파일별 상위 | `opal/core/references/pm/dispatch-process.md` (1건, GC-C001) / 11개 SKILL·참조 문서 파일 (선재 위반 소견 1건, 다건 공동 귀속) |
| 카테고리별 빈도 | 문서화(변경이력 버전 형식) — 11 파일 [빈도 트리거, 선재] |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 1 (빈도 트리거 1건 — 선재 위반 성격이라 §4에 참고용으로만 등재) |

**최종 판정: PASS** (Critical 0 / High 0)

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

### High (0건)

### Medium (1건)

- [ ] GC-C001 [`docs/PROJECT.md` §문서 레지스트리 변경이력, 추가행] 판정 보류 — 기준 부재
  - 카테고리: 문서화 (변경이력 형식)
  - 위반 기준: 프로젝트(`docs/CONVENTIONS.md` §문서 규칙 §변경이력) — "스킬, 에이전트, 참조 문서의 변경이력은 일시(KST)를 포함한다" + 예시 표 컬럼 `| 버전 | 일시 | 변경내용 |`
  - 설명: 이번 태스크가 `docs/PROJECT.md`에 추가한 변경이력 행(`| 2026-08-21 15:30 | 문서 레지스트리 ... (097) |`)은 **2열**(`날짜 | 변경 내용`) 형식이며 버전 열이 없다. CONVENTIONS.md §변경이력 규칙의 명시 대상은 "스킬, 에이전트, 참조 문서" 3종이며 예시 표는 3열(버전/일시/변경내용)이다. `docs/PROJECT.md`는 이 3종 중 어느 것에도 명시적으로 해당하지 않는 "프로젝트 정의 SSOT" 문서이며, CONVENTIONS.md는 이 문서 유형의 변경이력 형식을 별도로 규정하지 않는다.
  - 판정: **기준 부재** — CONVENTIONS.md가 PROJECT.md류 문서의 변경이력 형식을 명시하지 않으므로 이번 태스크의 추가 행을 위반으로 판정하지 않는다. 다만 이 2열 형식은 `docs/PROJECT.md`의 기존 변경이력 표 전체가 채용 중인 **선재 형식**이며(맨 위 삽입 방식 = 최신순, 다른 12파일은 append 방식 = 오래된순), 이번 태스크는 기존 표의 지역 관행을 그대로 따랐을 뿐 새로운 이탈을 만들지 않았다.
  - 해결 방안: (선택) CONVENTIONS.md §변경이력 규칙의 적용 대상 목록에 "프로젝트 정의 문서(PROJECT.md 등)"를 명시적으로 포함하거나 명시적으로 예외 처리해 기준 공백을 없앨 것을 제안. 소유자 승인 필요 (자동 갱신 금지, 본 워커는 문서 수정 권한 없음).
  - 자동 수정: N
  - 참조: `docs/CONVENTIONS.md` §문서 규칙 §변경이력 (프로젝트 문서)

### Low (2건)

- [ ] GC-C002 [`opal/core/references/pm/dispatch-process.md:115`, `opal/skills/op-dev-execute/SKILL.md:214`, `opal/skills/opal-pilot-data-design/SKILL.md:93`, `opal/skills/opal-pilot-dev-short/SKILL.md:123`, `opal/skills/opal-pilot-dev-wireframe/SKILL.md:144`, `opal/skills/opal-pilot-dev/SKILL.md:162`, `opal/skills/opal-pilot-project-dev/SKILL.md:198`, `opal/skills/opal-pilot-project-loop/SKILL.md:216`, `opal/skills/opal-pilot-project/SKILL.md:246`, `opal/skills/opal-pilot-write-tech/SKILL.md:300`, `opal/skills/opal-pilot-gc/SKILL.md:180`] 변경이력 신규 행의 버전 표기가 semver(`vX.Y.Z`)가 아닌 2단(`vX.Y`) 형식 — **선재 위반의 연장**
  - 카테고리: 문서화 (변경이력 버전 형식)
  - 위반 기준: 프로젝트(`docs/CONVENTIONS.md` §문서 규칙 §변경이력 "버전: semver (`vX.Y.Z`)" / §구현 규칙 §변경이력 작성 의무 "버전은 semver")
  - 설명: 이번 태스크가 11개 파일에 추가한 변경이력 신규 행(`v1.8`, `v2.5`, `v1.5`, `v4.9`, `v3.8`, `v5.4`, `v5.7`, `v2.3`, `v4.1`, `v4.10`, `v1.14`)이 모두 `vX.Y` 2단 표기이며 CONVENTIONS.md가 요구하는 `vX.Y.Z` 3단 semver가 아니다. 단, 각 파일을 실측한 결과 이 11개 파일은 **이번 태스크 이전부터 전 행이 동일하게 2단 표기**를 사용해온 기존 관행이며(예: dispatch-process.md의 직전 행 `v1.7`, op-dev-execute/SKILL.md의 직전 행 `v2.4` 등), 이번 태스크는 그 파일 고유의 기존 번호 체계를 그대로 이어서 증가시켰을 뿐 새로운 이탈 유형을 만들지 않았다. 대조군으로 `docs/CONVENTIONS.md`(신규 행 `v1.7.0`)와 `opal/skills/opal-pilot-sdd/SKILL.md`(신규 행 `v3.11.0`)는 원래부터 3단 semver를 유지해온 파일이며, 이번 태스크의 신규 행도 그 파일의 기존 3단 규칙을 정확히 준수했다 — 즉 이번 태스크 자체는 파일별 기존 형식을 일관되게 따랐고, 위반은 순수하게 11개 파일에 선재하던 것이다.
  - 판정: **선재 위반 (이번 태스크 유래 아님)** — 빈도 트리거 조건(N≥3 파일)은 충족하지만, 신규 발생이 아니라 기존 패턴의 연장이므로 Critical/High 게이트에는 반영하지 않고 Low로 하향 등재.
  - 해결 방안: 11개 파일의 변경이력 표 전체(과거 행 포함)를 `vX.Y.0` 또는 유의미한 patch 번호로 일괄 정정하는 별도 후속 태스크 권고. 이번 태스크 범위(단일 행 추가)에서 과거 행까지 소급 수정하는 것은 범위 초과이며, 소유자 승인 후 별도 태스크로 처리할 것을 제안.
  - 자동 수정: N (과거 행 다건에 걸친 버전 재부여는 자동화 부적합 — 의미 있는 patch 증가치를 사람이 판단해야 함)
  - 참조: `docs/CONVENTIONS.md` §문서 규칙 §변경이력

- [ ] GC-C003 [`docs/CONVENTIONS.md:187` 근방, §커밋 규칙 §규칙 / §구현 규칙 §Guards] 기준 문서 자기 변경 — 형식은 준수, 내용 참조는 유효
  - 카테고리: 문서화 (자기 참조 무결성)
  - 위반 기준: 프로젝트(`docs/CONVENTIONS.md` §Citation Rules — "모든 주장은 근거를 인용한다")
  - 설명: 이번 태스크는 `docs/CONVENTIONS.md` 자신에서 커밋 실행 시점 규칙 원문 2건(§커밋 규칙 §규칙 1행, §구현 규칙 §Guards 1행)을 제거하고 `opal/core/references/opal-harness.md §1 커밋 규칙`을 가리키는 포인터로 대체했다. 실측 결과 해당 파일에 `## 1. Guards (제약)` 절 하위에 `### 커밋 규칙` 서브섹션이 실제로 존재하며("커밋은 사용자가 명시적으로 요청할 때만 수행한다...") 인용이 유효하다. `pm-review-gate.md §워커 중단 시 산출물 실측 판정` 인용(다른 파일 소재)도 실측 확인됨. 형식(`경로 §섹션`)과 실체 모두 CONVENTIONS.md §Citation Rules에 부합한다.
  - 판정: 위반 아님 (형식·실체 모두 확인 — Low로 등재한 이유는 "기준 문서 자기 변경"이라는 민감 지점이므로 검증 근거를 명시적으로 기록해 두기 위함, 조치 불요)
  - 해결 방안: 조치 불요. 참고: v1.7.0 changelog 항목 본문에 제거된 조항 2건과 이유("에이전트 행동 Guard를 코드 컨벤션 문서에 복제하면 프로젝트마다 존재 여부가 갈리는 우발 경로")가 명확히 기술되어 있어 추적 가능성도 양호.
  - 자동 수정: N (조치 불요)
  - 참조: `opal/core/references/opal-harness.md` §1 Guards §커밋 규칙 (실측 확인됨, `opal/core/references/opal-harness.md:40-42`)

### Info (0건)

---

## 4. 문서 업데이트 제안 (§9·§10, 트리거 발동 시만)

- [ ] GC-DP-C001 [빈도 트리거] "변경이력 버전 표기 2단(vX.Y) vs semver(vX.Y.Z)" (11개 파일) → CONVENTIONS.md §문서 규칙 §변경이력에 명확화 제안
  - 근거: 단일 실행 내 11개 파일에서 동일 패턴 확인 — 빈도 임계값 N=3 초과. 단, 이 11개 파일은 이번 태스크 이전부터 동일 패턴이었음(선재) — GC-C002 참조.
  - 제안 내용: (a) 과거분 포함 11개 파일 버전 표기를 semver로 일괄 정정하는 후속 태스크 발의, 또는 (b) CONVENTIONS.md §변경이력에 "기존 2단 표기 파일은 다음 patch 버전부터 3단으로 전환"과 같은 마이그레이션 경로를 명문화. 소유자 판단 필요 — 본 보고서는 제안만 하며 문서를 직접 수정하지 않는다.

- [ ] GC-DP-C002 [기준 공백] `docs/PROJECT.md`류 "프로젝트 정의 문서"의 변경이력 형식이 CONVENTIONS.md §변경이력 적용 대상(스킬/에이전트/참조 문서) 목록에 없음 → 대상 목록에 명시 추가 또는 예외 명문화 제안
  - 근거: GC-C001 — 기준 부재로 판정 보류한 항목의 재발 방지.
  - 제안 내용: CONVENTIONS.md §문서 규칙 §변경이력 첫 줄 "스킬, 에이전트, 참조 문서의 변경이력은..." 을 "스킬, 에이전트, 참조 문서, 프로젝트 정의 문서(PROJECT.md 등)의 변경이력은..."으로 확장하거나, PROJECT.md류는 별도 2열 형식(날짜/변경내용, 최신순)이 허용됨을 각주로 명시.

---

## 5. 문서 작성 유도

해당 없음 — `docs/CONVENTIONS.md` 존재.

---

## 6. 선재 위반 vs 이번 태스크 유래 분리 (요약)

| 구분 | 건수 | 내용 |
|------|------|------|
| 이번 태스크 유래 위반 | 0 | 신규 유형의 위반 없음 — 14개 파일 모두 각 파일의 기존 변경이력 형식(버전 체계·컬럼 구조·정렬 순서)을 정확히 따라 행을 추가함 |
| 선재 위반의 연장 (GC-C002) | 11 파일 | 2단 버전 표기(vX.Y) — 해당 파일들은 이전부터 동일 패턴이었고 이번 태스크가 새로 만든 이탈이 아님 |
| 기준 부재 (GC-C001) | 1 파일 | `docs/PROJECT.md` 변경이력 2열 형식 — CONVENTIONS.md가 이 문서 유형을 명시적으로 다루지 않음 |
| 검증 확인 (GC-C003) | 1 항목 | `docs/CONVENTIONS.md` 자기 변경의 인용 유효성 — 위반 아님, 실측 근거 기록용 |

## 7. 특기: 커밋 관련 규정 이동의 정합성 확인

`docs/CONVENTIONS.md`가 커밋 실행 시점 규칙 원문(§커밋 규칙 §규칙, §구현 규칙 §Guards) 2건을 제거하고 `opal-harness.md §1 커밋 규칙`으로 포인터화한 것과 짝을 이루어, `pm/dispatch-process.md`가 "전 워커 공통 고정" 항목에 "git 이력 변경 금지"를 신설하고, `op-dev-execute/SKILL.md` 절대 금지 표에 #7 행을 추가하고, 나머지 8개 파일의 `[PM 컨텍스트 주입]` 블록을 SSOT 포인터 1줄로 통일한 것을 실측 대조했다. 각 파일이 인용한 경로(`opal-harness.md §1 커밋 규칙`, `pm-review-gate.md §워커 중단 시 산출물 실측 판정`, `opal/core/AGENT.md §부트스트랩 §[WORKER 규칙]`)는 모두 실제 대상 섹션이 존재함을 확인했다. 문서 규칙(인용·변경이력 태스크 번호 괄호 표기) 관점에서는 14개 파일 전건이 준수 상태다.
