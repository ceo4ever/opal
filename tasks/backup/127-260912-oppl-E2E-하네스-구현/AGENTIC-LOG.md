# AGENTIC-LOG: OPAL 범용 E2E 하네스 구현 (제안서 태스크 3~9)

> 모드: agentic | 파일럿: //opd --agentic --wt | 시작: 2026-09-14 16:00
> 앞선 oppl 수행분의 일지는 `archive/AGENTIC-LOG.md`에 있다. 이 문서는 opd 전환 이후만 기록한다.

## 요약

| 항목 | 건수 |
|------|------|
| 게이트 판단 | 3회 (Pass: 3 / Fail: 0) |
| 3회 초과 Gate | 0건 (Critical: 0 / Normal: 0 / Minor: 0) |
| 오류 발견 | 3건 |
| 수정 지시 | 3건 (반영: 3 / 미반영: 0) |
| PM 의사결정 | 5건 |
| 개선 사항 | 0건 |
| 에스컬레이션 | 0건 |

## 대행 일지

| # | 시점 | 단계 | 카테고리 | 내용 | 결과 |
|---|------|------|----------|------|------|
| 1 | 2026-09-14 16:00 | TASK | DECISION | 캡슐 교체 — oppl → opd 전환(캡틴 지시). task_123 선례를 따라 태스크 번호·브랜치·워크트리를 유지하고, oppl 실행 산출물만 `archive/`로 내렸다. 계약 4종(PRD·TRD·CONTRACT·surfaces.json)은 캡슐 루트 유지 — `backup/`에 넣으면 동결 스냅샷으로 분류돼 살아있는 계약이 죽는다 | 완료 |
| 2 | 2026-09-14 16:05 | TASK | DECISION | 범위를 좁히지 않고 원 수용 기준을 승계 — AC-1~AC-14 유지 + T16 안전 결함을 AC-15로, `.oppl-run/` gitignore를 AC-16으로 승격. 이미 충족된 AC-3은 **(충족)** 표시와 근거 경로를 남겨 기준을 낮춘 것이 아님을 명시 | `verify --clarification-check` pass |
| 3 | 2026-09-14 16:18 | ANALYSIS | GATE | ANALYSIS Pass — Q1~Q7 전건이 `grep -n` 실측 근거와 함께 답변됨. PM 표본 재검증 5건 전부 일치: `console.sh:234-236` stop 분기가 `rec_pid`·`rec_app_dir`만 선언(started_at 미파싱) · `:304` status 분기만 `rec_started_at` 읽음 · `test_tool.py`에 `e2e` 서브파서 부재(4개뿐) · `resolver.py:107,157` playwright 후보 · `requirements.txt:29` `playwright>=1.40.0`. C-1(변경 0 계약) 위반 근거 0건 | Pass |
| 4 | 2026-09-14 16:18 | ANALYSIS | ERROR | `archive/README.md`의 "`started_at`이 기록만 되고 어디서도 읽히지 않는다"가 부정확 — ANALYSIS가 `console.sh:304`에서 `status` 분기가 읽되 안전성 검사·표시에만 쓰고 `stop` 판정표는 파싱하지 않음을 실측 | Minor |
| 5 | 2026-09-14 16:18 | ANALYSIS | FIX | #4 반영 — `archive/README.md` §미해결 안전 결함에 정정 문구 삽입("읽히되 stale 판정에 미사용") | 반영 완료 |
| 6 | 2026-09-14 16:19 | ANALYSIS | DECISION | 트랙 강등 검토(`track-routing.md` 핵심 질문 1회) — "이후 외부 영향이 있는 동작·계약·구조 결정을 새로 해야 하는가?" **예**. Q-2·Q-3 probe 결과에 따른 증적 capability 확정, driver 계약 구현 결정, AC-15 플랫폼 분기 배치(인라인 vs 헬퍼 추출), Playwright opt-in vs 완전 삭제 판정이 남아 있다. **`opd` 유지**, `opds` 강등 제안하지 않음 | opd 유지 |
| 7 | 2026-09-14 16:38 | PLAN | GATE | PLAN Pass — W-1~W-15, 실행 그룹 P1~P8. PM 재검증: `verify --plan-contract-check` pass(15 Work items 전건 계약 충족) · `--code-scan-citation-check` pass · AC-1~AC-16이 W-14 완료 기준에 전건 연결 · Risks가 신규 H-1~H-5만(RK-1은 T02 해소분이라 미기재) · Release and recovery에 source→installed 경로·AC-12 격리 install diff 판정·W 단위 롤백 명시 · C-1 위반(변경 0 계약 파일) 0건 | Pass |
| 8 | 2026-09-14 16:38 | PLAN | DECISION | ANALYSIS §Handoff 4건 결정 승인 — (1) T03을 전 후속의 선행으로 승격(`test_tool.py`에 `e2e` 서브파서 부재가 근거) (2) `test_tool.py` `e2e` 서브파서를 **한 시점에 한 W만 소유**: W-1 신설 → W-8 resume → W-9 status/clean → W-11 문구 (3) TS-040 선-갱신을 별도 W로 쪼개지 않고 W-11 내부 1순위 단계로 고정(별도 W면 RED 구간이 W 경계를 넘어 RK-5 완화가 무효) (4) AC-15는 인라인이 아니라 `_console_boot_epoch`·`_console_record_predates_boot` 헬퍼 2개 추출 — 부팅시각 2분기 + ISO8601 파싱 2분기로 4지점이고, 헬퍼여야 회귀 테스트가 스텁해 리부팅 조건을 실관측할 수 있다 | 승인 |
| 9 | 2026-09-14 16:39 | PLAN | ERROR | PM Gate 지적 2건 — (a) 23행 D-4 근거 셀에 마크다운 표 이스케이프 파이프 `\|` 잔존(도구가 셀 파싱 시 열 분리로 오인 가능, 워커가 자진 보고) (b) P3에서 W-7과 W-8이 같은 `executors/` 패키지를 신설하는데 `__init__.py` 소유권이 불명 — 같은 파일을 두 워커에게 나누면 `dispatch-process` Step 1 위반 | Minor |
| 10 | 2026-09-14 16:39 | PLAN | FIX | #9 반영 — (a) 이스케이프 파이프를 산문("또는")으로 치환 (b) `__init__.py`를 **W-7 단독 소유**로 명시하고 W-8은 생성·수정 금지를 셀에 박음. 계약 검사 2종 재실행 전부 pass | 반영 완료 |
| 11 | 2026-09-14 17:20 | PLAN | DECISION | main 동기화 — 캡틴 지시. 브랜치가 0커밋·미커밋 14항목이라 merge가 거부되므로 먼저 `de293e9`로 커밋(main 아닌 `feat/OP-TASK-127`에만)한 뒤 main 23커밋을 `--no-ff` 병합. 충돌은 `scripts/install-mac.sh` 변경이력 주석 1곳뿐(양쪽이 `v4.9` 추가) — main의 v4.9(122 self-pm-tool)를 앞에 두고 우리 것을 v4.10으로 내려 둘 다 보존. `bash -n` OK, `console_autostart` 실제 코드는 자동 병합됨 | 병합 완료 |
| 12 | 2026-09-14 17:25 | PLAN | GATE | 병합 후 C-9 기준선 재측정 — `test-tool` 88 passed(불변) · 비간섭 회귀 20/20 ALL PASS · `pkill|pgrep|killall` 양쪽 0건 · 사용자 7823 health 200. main 병합이 기준선을 흔들지 않았다 | Pass |
| 13 | 2026-09-14 17:28 | PLAN | ERROR | **PLAN 인용 무효화 확인** — main 태스크 131이 W-11 대상 다수를 이미 정리했다. `tools.md`는 W-16이 도구 절 13개를 레지스트리 표로 축소해 인용 `:526,528,530,682`가 소멸(playwright는 `:36` 레지스트리 행 1건만), `journey-flow.md`·`verification-loop-guide.md`는 playwright 참조 **0건**, `oppl SKILL.md:202`도 소멸. 동기화 없이 EXECUTE에 들어갔으면 워커가 존재하지 않는 줄을 고치려 들었을 것 | Critical(사전 차단) |
| 14 | 2026-09-14 17:30 | PLAN | FIX | #13 반영 — W-11 변경 대상을 재실측 값으로 교체하고 제외 대상을 명시. **AC-14 잔존 대상이 `verification.md:30,32,39,43` 4곳 + `oppl SKILL.md` 1건으로 좁혀졌다**(`:30`이 `real-usage` 정의 문장 보유, `:43`이 done 규범 선언). TS-040은 `test_tool_scan.py:843,860,872-873`으로 위치 불변이라 1순위 선-갱신 제약 유효. 계약 검사 2종 재실행 pass | 반영 완료 |
| 15 | 2026-09-14 17:21 | TEST-SCENARIO | ERROR | **표면 커버리지 구조적 결손** — `scenario-conformance`는 `surfaces.json` 40표면 각각에 `surface_ref` 일치 통과 시나리오 1건 이상을 요구하고 `surface_ref`는 단수 필드인데, S-1~S-28은 8표면만 참조해 32표면이 `surface_unverified`로 남는다. S-25의 `all_surfaces_green` 기대가 도달 불가였다 | Critical(사전 차단) |
| 16 | 2026-09-14 17:40 | TEST-SCENARIO | DECISION | 캡틴 결정 — 분모 축소(S-25 기대 하향)가 아니라 표면 단위 엔트리 32건 추가로 검증을 채운다. 근거: `CONTRACT.md` §B.5:625가 40표면 전수를 커버리지 게이트 분모로 확정하고 `surfaces.json`을 읽기 전용으로 둔다(C-8). 충실도 문턱을 표면 종류로 고정 — `driver-op`·`executor-op` `real-usage` / `http`·`cli` `real-http` | 승인 |
| 17 | 2026-09-14 17:45 | TEST-SCENARIO | GATE | 목표-커버 게이트 3회차 Pass — S-29~S-60 추가(28→60건) 후 `coverage-check` exit 0(`all_covered`, requirements 25/hypotheses 5/scenarios 60) + evaluator 3축 2/2/2 평균 2.0. 평가자 판정: `required_fidelity` + `profile` non-null의 verdict-json 강제 + 증적 5종으로 대역 통과 차단, 기존 8표면과 신규 32표면 교집합 0 | Pass |
| 18 | 2026-09-14 17:46 | TEST-SCENARIO | FIX | 평가자 권고 2건 PM 반영 — S-13이 `profile: null`·`required_evidence: []`라 `scenario.py:530-542` `assertion_required` 분기를 우회하는 구멍을 `profile: api`(§C.10 T02 선례)·증적 5종으로 차단, S-7을 executor-op 문턱에 맞춰 `real-http`→`real-usage` 상향. 시나리오 수·covers 불변이라 4회차 미호출 | 반영 완료 |
| 19 | 2026-09-14 18:00 | EXECUTE | ERROR | RED 작성 워커가 blocker 2건 반환 — S-14는 `console.sh`에 `started_at` epoch 변환·비교 공개 표면이 0건이라 실패 관측 불가, S-16은 `.gitignore:41`에 `.oppl-run/`가 선행 커밋 `2e56227`(태스크 066)로 이미 등재돼 AC-16 기충족. PM 실측 재검증 결과 둘 다 사실 | Normal |
| 20 | 2026-09-14 18:01 | EXECUTE | FIX | #19 반영 — S-14는 최초 문언이 비공개 헬퍼 단위 왕복을 겨냥해 `red-first.md` §2 검증 경계를 위반한 PM 작성 오류. 공개 관측 가능한 형태(부팅 이후 레코드→정상 종료 / 손상 레코드→fail-open)로 재정의하고 S-16과 함께 `red_required: false` 구현 후 회귀 가드로 전환. RED 대상 13→11건 | 반영 완료 |
| 21 | 2026-09-14 18:02 | EXECUTE | DECISION | `scenario-init`이 `red_required` 정정 시 `red_confirmed`를 전건 초기화(시드 우회 봉쇄)하므로 확인된 11건이 소실됐다. 이전 증거 재사용을 금지하고 두 워커에게 실패 재관측·재기록을 지시 — 전건 이번 실행 타임스탬프(18:02:04~18:02:08)로 재확인 후 `scenario-lock` 동결(`locked_at 18:02:30`) | 동결 완료 |
| 22 | 2026-09-14 18:12 | EXECUTE | GATE | W-2 Pass — PM 독립 재실행 23/23 ALL PASS. S-13 RED→GREEN(`stopped=false reason=stale_record`·`sentinel_alive=1`), 기존 20 케이스 불변, S-14 회귀 가드 2건 신규 통과. 플랫폼 분기가 `_console_boot_epoch`(`:75`)·`_console_record_predates_boot`(`:104`) 2헬퍼에 격리, `stop` `:311` 1회 호출, `status` 분기 무변경, `pkill\|pgrep\|killall` 0건, 사용자 7823 health 200 불변 | Pass |
| 23 | 2026-09-14 18:25 | EXECUTE | ERROR | W-1 자진 보고 이탈 2건 PM 실측 확인 — (a) 범위 밖 RED S-27 전건과 S-7 `TestRedactionOnFailedRun`이 **공허하게 green**(빈 run이라 `candidates==[]`·증적 부재로 유출도 0). W-3·W-5를 가드하지 못해 070 실패모드 재현 위험 (b) `CONTRACT.md` §B.1.1이 `--scenario`·`--task-path`를 필수로 선언하는데 동결 RED 4건이 인자 없이 호출해 구현이 선택 인자로 우회 — 계약과 구현이 경쟁 상태 | Critical |
| 24 | 2026-09-14 18:30 | EXECUTE | DECISION | 캡틴 승인 — (a) 계약이 SSOT이므로 §B.1.1을 개정하지 않고 두 인자를 필수로 되돌린다. RED 4건의 호출부는 구현자가 아닌 test-agent가 교정한다 (b) S-7·S-27은 "비어 있음으로 통과"가 불가능하도록 assertion 강화 — S-27은 `candidates[]` 비공백·후보1 시도 기록·retry/mode 전환 억제 각각 관측, S-7은 증적 5종 존재 선단언 후 마스킹 검사. 기대 강화는 §1.5 5항 약화 금지에 저촉되지 않는다. 근본 원인은 TEST-SCENARIO.md가 CLI 호출 인자를 미명시한 PM 작성 책임 | 승인 |
| 25 | 2026-09-14 18:50 | EXECUTE | FIX | #23(a) 해소 — test-agent가 S-1~S-4 호출부를 §B.1.1 3필수 인자로 교정(단언 불변)하고 S-7·S-27을 강화. S-2·S-3·S-4는 인자 교정만으로 통과(기대 약화 아님, 구현 진척), S-1은 온타깃 RED로 전환 — `project_root '.../task_127' != '/var/.../tmpXXXX'`. S-7은 증적 5종 실재 선단언에서 실패 + redaction 실패가 `infra_error` 대신 미처리 `PermissionError` traceback을 내는 것까지 포착 | 반영 완료 |
| 26 | 2026-09-14 19:05 | EXECUTE | FIX | #23(b) 해소 — W-1이 §B.1.1 3필수 인자를 argparse 층에서 집행하고 비계약 확장을 정리(`--project-root`·`--scenario-id` 제거, `--target` enum 복원, `blocked`/exit 19 우회 제거, stdout superset을 §B.1.1+4로 축소). S-1 근본 원인은 `resolve_target()`이 `--project-root or os.getcwd()`를 우선한 것 — 명시 인자 > cwd/git 탐색으로 우선순위 교정 | 반영 완료 |
| 27 | 2026-09-14 19:10 | EXECUTE | GATE | P1 Pass — PM 독립 재실행 `8 failed, 130 passed`. S-1~S-4 전건 통과, 잔여 실패 8건은 정확히 W-3·W-4·W-5·W-10 소유(S-5·S-6·S-7×2·S-12×2·S-26·S-27). `e2e_contract.py`·`scenario.py` diff 0(C-1), `pkill\|pgrep\|killall` 0건(C-9), 기존 4서브파서 무변경, `--help`가 §B.1.1 시그니처와 일치하고 인자 누락이 argparse에서 거부됨 | Pass |
| 28 | 2026-09-14 19:12 | EXECUTE | DECISION | W-1이 사전 확인 요청한 설계 판단 승인 — 후보 해석을 `sut_starting` 앞에 두어 `selected==0`이면 uvicorn·vite를 띄우지 않는다. 근거: §A.1.2 [MUST]가 `selected==0`일 때 verdict를 이미 `executor_unavailable`로 확정하므로 기동은 순수 부수효과(C-5)이고 S-3·S-4 예산을 초과시킨다. **단서: 이 단축은 `executor_unavailable` 경로에 한정하며, W-5·W-7이 후보를 등록해 진행 경로가 열리면 happy path는 §A.2.1 순서(`sut_starting`→`sut_ready`→`profile_resolved`→`executor_ready`)를 그대로 발행해야 한다.** `CONTRACT.md`는 개정하지 않는다 | 승인(단서부) |
| 29 | 2026-09-14 20:30 | EXECUTE | ERROR | W-5 blocker 4건 PM 실측 확인 — (a) `drivers/__init__.py`가 `agent_browser`를 import하지 않아 `registered_drivers()`가 비고 전 후보가 `no_registered_driver`로 떨어짐(조용한 미배선) (b) S-12·S-26 호출부가 §B.1.1 미정합(교정 누락) (c) S-26·S-27 spec `profile: null`이라 `executor_contract(None)` KeyError로 `candidates[]`가 구조적 공배열 — **PM 시드 책임** (d) S-27 강화 단언이 §A.1.2 outcome enum 밖 | Critical |
| 30 | 2026-09-14 20:35 | EXECUTE | DECISION | `test-scenario.json` 재시드 경로 폐쇄 — S-1~S-7이 이미 GREEN이라 재시드 시 RED 증거 재관측이 불가능해 `scenario-lock`이 영구 차단된다. 따라서 spec 결함(profile null·server_policy)은 재시드가 아니라 **테스트 자체 fixture**와 **발행 시점 정규화**로 우회한다 | 확정 |
| 31 | 2026-09-14 21:40 | EXECUTE | FIX | #29 전건 해소 — (a) `_BUILTIN_DRIVER_MODULES` 확장점 신설, import 실패를 `provider_unavailable`로 삼키지 않고 `DriverError`로 승격 (b)(c) test-agent가 §B.1.1 정합 + fixture에 profile 명시 (d) `candidate1.outcome == "selected"` + MV-38 기반 관측으로 교정. S-27 GREEN(공허 아님 — 실제 fixture 실행 근거) | 반영 완료 |
| 32 | 2026-09-15 00:10 | EXECUTE | ERROR | W-7+W-8이 executor 쪽 **동일 미배선** 보고 — `orchestrator._resolve_executor_candidates()` non-browser 분기가 레지스트리 미조회·`no_registered_executor` 하드코딩. 추가로 동결 spec S-11·S-28의 `server_policy: "local-only"`가 §A.9 enum(`keep`\|`terminate`) 밖임을 지적 — **PM 시드 책임**, MV-17 위반 대상 | Critical |
| 33 | 2026-09-15 00:30 | EXECUTE | DECISION | server_policy 처리 확정 — 발행 시점 정규화(enum 밖 → 보수적 `keep`, C-2)로 MV-17을 산출물 기준 충족하고 선언 원문은 `server_policy_declared`에 보존. **조용한 대체 금지** 명시. 이어 §A.2.3 키 의미 충돌(같은 키가 handoff=발행값 / journal=선언원문)을 발견해 (b)안으로 정정 — 계약 문구 무변경으로 코드를 계약에 맞춤 | 승인 |
| 34 | 2026-09-15 01:15 | EXECUTE | FIX | #32 해소 — orchestrator가 `executors.resolve_executor_candidates()`를 호출하고 `register()` 지점을 소유(W-7 설계 의도 유지, driver 패턴 미복제). `candidates[]` order 1~4 연속, api 후보가 실제 `/health` 호출의 `ECONNREFUSED` 원문 기록. §A.2.1 진행 경로 복원(`_SUT_DEPENDENT_EXECUTOR_TYPES`로 분기 근거 1지점화) | 반영 완료 |
| 35 | 2026-09-15 01:20 | EXECUTE | ERROR | S-26이 orchestrator의 §C.7 위반을 지목 — candidate `outcome: infra_error`가 `selected==0` 분기에 덮여 `executor_unavailable`로 위장. 인프라 실패를 후보 소진으로 오보하는 경로(MV-38 금지). 워커가 요청 전 자진 수정: `_candidate_gate()`가 selected 개수 검사 **앞**에서 infra_error 확정, 후보 원문 reason 보존(NR-7) | Critical(해소) |
| 36 | 2026-09-15 01:45 | EXECUTE | GATE | P3 Pass — `2 failed, 329 passed`. 잔여 2건은 W-10 소유 S-12. C-1 diff 0, C-9 0건, C-2·C-5 준수. `test_zero_executor_candidates_...` 재타깃 시 단언이 1후보→3후보 전수 + 빈 배열 통과 차단으로 **강화**됨 | Pass |
| 37 | 2026-09-15 01:50 | EXECUTE | DECISION | 캡틴 승인 — §A.1.2 [MUST]·MV-41을 "**필요 executor 타입별** selected 0/1개"로 개정. 근거: 최초 문언이 `EXECUTOR_MATRIX["hybrid"]["required"] == ("api","browser")`와 충돌하는데 `EXECUTOR_MATRIX`는 `e2e_contract.py:57`에 있고 C-1이 변경 0으로 동결한다. 태스크 125 확정 상위 계약이 이 태스크 문서보다 우선. 타입별 상한 1개 제약은 유지 — 약화 아님 | 승인 |
| 38 | 2026-09-15 11:30 | EXECUTE | GATE | P4 Pass — 전 스위트 **366 passed / 0 failed**, 동결 RED 11건 전건 GREEN. browser step runner 봉쇄 해제 시 워커가 테스트가 아니라 runner를 고쳤다(`resolve_candidates()`가 probe 후 driver 인스턴스를 버려 `--version` 재호출 → 메모이즈 레지스트리로 제거). 부수로 `_finalize`가 최종 상태 전이를 발행하지 않아 최종 판정이 이력에서 사라지던 §A.2.2 위반 결함을 발견·수정 | Pass |
| 39 | 2026-09-15 11:35 | EXECUTE | ERROR | PM 실측 — `agent_browser.py`가 §B.2 8연산 중 `probe`·`open`·`close` 3개만 구현(`op_snapshot`·`op_act`·`op_wait`·`op_assert`·`op_capture` 부재). 이 머신의 유일한 실 browser 후보가 UI 조작·semantic assertion 불가라 AC-4·AC-5가 구조적으로 불가능. W-5 자체 테스트 30건은 구현 범위만 덮어 공백 미노출 — **워커 자체 테스트 통과·통합 공백 패턴 3회차** | Critical |
| 40 | 2026-09-15 11:36 | EXECUTE | DECISION | 연산 부재를 `infra_error`가 아니라 `blocked`로 분류한 W-10 판단 승인 — driver가 아무 일도 하기 전에 나는 오류이므로 §C.7의 "실행 중 깨짐"과 다르고, 미구현이 서버·포트 장애와 같은 칸에 기록되면 무엇을 고칠지가 사라진다. 후보 미전환은 동일 유지(C-3) | 승인 |
| 41 | 2026-09-15 12:40 | EXECUTE | ERROR | **PLAN W-11 인용 3건이 실측과 불일치** — (a) RK-5 순서 제약이 false premise: `test_tool_scan.py:59` `_AGENT_MD`가 `opal/core/AGENT.md`를 가리키고 wtm agent 문서 참조가 0건이라 문서 이전이 TS-040을 RED로 만들 수 없다(역전 실험으로 확인) (b) `verification.md`의 playwright hit는 `:204` 이력행 1건이고 인용된 `:30,32,39,43`은 playwright 없는 fidelity 정의 줄 (c) `oppl SKILL.md`는 playwright 0건이고 `:228` `real-usage`가 이미 계약 참조형 | Normal |
| 42 | 2026-09-15 12:45 | EXECUTE | ERROR | **tool-scan 4건 실패가 main 상속 결함으로 확인** — PM 재검증: `main:opal/core/AGENT.md`에 `cmux-tool`·`playwright` 0건이고 테스트 파일은 main과 동일. 태스크 131 W-16의 레지스트리 표 축소가 이 테스트들이 단언하던 인지맵 구조를 제거했다. TS-040은 `:853`(`cmux-tool` 부재)에서 이미 실패해 playwright 정규식에 도달조차 못 한다. **이 태스크 범위 밖** — 고치려면 `opal/core/AGENT.md`(미소유)에 playwright fallback 산문을 되살려야 해 AC-14와 정면 충돌한다 | Critical(범위 밖) |
| 43 | 2026-09-15 12:50 | EXECUTE | DECISION | W-11 완료 판정 — 소비자 12개 분류(이전 8 · opt-in 2 · 범위제외 2) 근거와 함께 기록. playwright는 삭제가 아니라 **기본값에서 선언형으로 강등**(템플릿 주석 블록·playwright-tool CLI·MCP 정의·SECURITY 핀). AC-14 잔존 정의 문장은 `lib/scenario.py:119` `FIDELITY_ORDER` 한 곳으로 수렴. 부수로 `agents.md:273`·`mcps.md:12`의 wtm `Phase 1(WebFetch)` 서술이 `opal-wtm-agent/AGENT.md:136`(WebFetch 완전 제거)와 어긋난 drift를 정정 | 승인 |
| 44 | 2026-09-15 15:30 | EXECUTE | GATE | P5·P6 Pass — W-11(playwright 소비자 12개 분류: 이전 8·opt-in 2·범위제외 2) · W-13(기본 설치 제거, doctor 분모 축소, chromium 0회, 566MB 사용자 캐시 359→359 불변) · W-12(SUT http 16표면 중 15건 real-http 관통, PM이 verdict-json으로 표시 → 미검증 40→25) · W-5 잔여(§B.2 8연산 완성, §A.4 result enum 3곳 정정). 커밋 `591348b` 63파일, 워킹트리 clean | Pass |
| 45 | 2026-09-16 01:00 | EXECUTE | ERROR | **W-14 통합 검증이 미충족 3건·Critical 2건 적발** — AC-9 미충족(F-4: `orchestrator._open_executor()`가 human executor에 없는 `prepare`를 dispatch, `HUMAN_OPERATIONS`는 probe/handoff/resume뿐 → collaborative·manual이 `e2e run`으로 절대 실행 불가. 기존 단위 테스트는 orchestrator를 우회해 직접 호출해 미포착 — **통합 지점 공백 4회차**) · AC-12 blocked(`console_autostart()` 7823 하드코딩 + `install_dashboard()` FE 빌드가 소스 트리 `dist/`에 씀 → 격리 install 불성립) · AC-14 미충족(`verification-loop-guide.md:57,98`이 L3b를 `npx playwright test`로 정의 — PLAN이 '0건, 대상 제외'로 본 파일을 실측이 뒤집음) | Critical |
| 46 | 2026-09-16 01:05 | EXECUTE | ERROR | **F-5** `cleanup.json`이 `result=complete, leaked=[]`를 보고하고도 프로세스가 실제로 남았다 — uvicorn 2(61110·61112)·agent-browser 2+자동화 Chrome 2·vite 2(54184·54299). 보고 정확성 문제가 아니라 **안전 장치 문제**다: 대장이 거짓이면 수동 회수가 소유권 없이 PID만 보고 판단하게 된다 | Critical |
| 47 | 2026-09-16 01:10 | EXECUTE | ERROR | **F-3 — PM 시드 오류(3회차)**: S-10·S-40~S-44·S-7이 `profile:"api"` + `required_fidelity:"real-usage"`인데 api profile 충실도 상한은 `real-http`다(`real-usage`는 browser 후보 selected일 때만). 동결 spec도 `e2e_contract.py`(C-1)도 못 고치므로 **`all_surfaces_green`은 이 태스크에서 도달 불가**. 수정이 아니라 판정 대상 | Major |
| 48 | 2026-09-16 01:20 | EXECUTE | ERROR | **PM 직접 수행 중 C-2 위반** — 누출 6건 회수 시 agent-browser `default` 세션까지 종료시켰다. e2e run은 `opal-e2e-{run_id}` 이름을 쓰므로 `default`는 run 소유가 아니었다. **PID 정체만 확인하고 세션 소유권을 확인하지 않은 것**이 원인. 데이터 손실 없음(Chrome 프로필 5종 불변, 세션은 런타임 핸들이라 재생성됨), 사용자 Console 28736·7823 health 200 유지. 회수 전 확인 항목에 '리소스 이름이 `opal-e2e-*`인가'가 있었어야 했다 | Critical(자책) |
| 49 | 2026-09-18 10:30 | EXECUTE | FIX | W-14 적발분 PM 직접 수정 — **F-4**: `_open_executor()`가 executor 종류를 보지 않고 `prepare`를 dispatch하던 것을 `_wrap_human_executor()`로 분기(일반 executor도 `"prepare" in operations` 가드). **F-6**: 감시 grep에 `--exclude-dir=tests` — 금지 대상(구현)과 금지를 단언하는 테스트를 같은 칸에 넣지 않는다. **AC-14**: `verification-loop-guide.md:57,98`의 `npx playwright test`·`playwright 스크립트`를 계약 참조로 교체 | 반영 완료 |
| 50 | 2026-09-18 10:38 | TEST | ERROR | **PM이 검증 전 판정을 문서에 기재** — VERIFY-AC.md에 "AC-9는 F-4 수정으로 해소"라고 적었으나 실행해 보니 여전히 `infra_error`였다. 이 태스크 내내 워커들에게 지적해 온 "자체 테스트는 통과하나 통합에서 안 됨"을 PM이 문서에 그대로 쓴 것이다. 실측 3회로 배선 3건을 추가 발견 — (a) 시나리오 `handoff`가 runtime context 미전달 → `handoff_contract_incomplete` (b) handoff 발행 후 step 루프 계속 진행 → assertion 실패로 `fail` (c) verdict 조립이 `awaiting_human`을 일반 경로로 보내 증적 결손 계산 → `infra_error` | Critical(자책) |
| 51 | 2026-09-18 10:40 | TEST | GATE | **TEST Pass** — AC-9 전 구간 실측 관통(`awaiting_human`·exit 20·handoff 8필드·동일 run-id 재개·`completed:true`인데도 `fail`로 사람 제출 단독 pass 차단). C-9 4종: `test-tool` **424 passed**·console **23/23 ALL PASS**·`pkill\|pgrep\|killall` 0/0·C-1 diff 0. vitest 131은 main 상속(`710800d` 30케이스 삭제). AC 충족 14 / blocked 1(AC-12 범위 밖) / 미충족 0 | Pass |
| 52 | 2026-09-18 10:42 | TEST | GATE | **PM Gate Pass** — 금지사항 검사: `~/.opal` 직접 편집 0 · C-1 동결 파일 diff 0 · 수기 누적 이력 절 신규 0 · 동결 RED 무결(`locked:true`, red 11/11). C-7 플랫폼 분기 스캔에서 `runtime.py`·`agent_browser.py`가 잡혔으나 **둘 다 주석 안의 언급**이고 실제 분기는 `process.py` 한 곳뿐임을 확인 | Pass |
