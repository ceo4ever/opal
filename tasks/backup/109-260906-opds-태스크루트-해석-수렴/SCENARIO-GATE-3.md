# SCENARIO GATE — iteration 3 (scenario-rubric)

> 실행 일시: 2026-09-07T15:00:00+09:00 (KST)
> phase: `scenario-rubric` (판단축 ①⑤⑥ 전용 트랙 — Base Likert 루브릭 비적용)
> scenario_source: `tasks/109-260906-opds-태스크루트-해석-수렴/TEST-SCENARIO.md`
> 보조 대조: `TASK.md`(AC SSOT) · `PLAN.md`(상류) · `.scenario-coverage-input.json`(정규화 페이로드) · `SCENARIO-GATE-1.md` · `SCENARIO-GATE-2.md` · `.scenario-gate-history.json`
> 기준 SSOT: `opal/core/references/harness/scenario-gate.md` §2(6축)·§5-1(종료조건 임계)
> iteration: **3 / 상한 3**(`opal-harness.md` §1) — 반복 상한 마지막 회차
> 판정 범위: **①⑤⑥만**. ②③④는 `test-tool scenario-coverage-check` exit 0 재판정 완료(`all_covered: true`, R 8 / F 6 / H 11 / S 43)이며 본 보고서는 재채점하지 않는다(§2 [MUST] 판정 주체 분리). 단 **AC 문자 단위 정합**은 결정론 게이트가 보지 못하는 층이므로 §4 매핑 ↔ `TASK.md` 원문 대조는 관할 내로 수행했다.
> **CONTRACT.md 부재 — 본 phase는 CONTRACT 루브릭절 병합 대상이 아니다(전용 트랙).**

---

## 0. iteration 3의 재채점 사유

iteration 2의 `pass`(2/2/2, 평균 2.0)는 **R-1~R-7 / 40 시나리오** 기준 판정이었다. 이후 요구사항 집합이 바뀌었다.

| 변경 | 내용 |
|------|------|
| 소유자 결정 3건 | skip 범위 → **skip 0 목표 편입**(R-4 **AC(e)** 신설 + TS-035 강화 + §2.3 skip 사유 실측 표) / 허브 검증 시점 → **머지 후 push 전, 실패 시 `git reset --hard`**(TS-071) / TS-061 유지 |
| R-ID 충돌 정정 | `TASK.md` R-7(FE `@header`) ↔ `PLAN.md` R-7(이동값 단언) **동일 ID 이중 점유** 해소 — 이동값을 **R-8로 재배정**, R-7 = FE `@header` 확정, **TS-027·TS-028·TS-029 신설**, R-4 AC(d)→TS-041·TS-042 매핑 |
| 현재 규모 | requirements **R-1~R-8(8)** / features 6 / hypotheses 11 / scenarios **43** |

**anchor 배제 선언** — iteration 2 점수를 승계하지 않고, 신설 요구(R-4 AC(e) · R-7 AC(a)(b)(d) · R-8)와 그 검증자를 본문 대조로 독립 재채점했다. 페이로드 플래그(goal 9 / adoption 14 / boundary 14)는 Producer 산정값이므로 **전건 본문 검증**했다(§5).

---

## 1. 임무 1 — R-ID 충돌 정정의 타당성 검증

### 1-1. R-8 AC (a)(b)(c) ↔ PLAN F-006 원 담당 내용 (승계 손실 여부)

`PLAN.md` §2.6.2·§3.6.2가 F-006에 걸었던 보장을 `TASK.md` R-8 AC와 1:1 대조했다.

| PLAN F-006 원 보장 | 근거 | R-8 AC 승계 | 판정 |
|---|---|---|---|
| 동결값 **425/799를 그대로 단정**(코호트 필터 재계산으로 재현, E1-2 실측) | `PLAN.md:245-`, §3.6.2 | **AC(a)** 「코호트 필터 재계산으로 **425/799**를 단정하며 통과」 | **무손실** |
| 값 단정 자체를 삭제하지 않음(교정 = `ts021`이 확립한 이동값 규약 적용, `STATS-BASELINE.md` §6.1 [MUST] 준수로의 정렬) | §3.6.2 [MUST] | **AC(b)** 전단 「동결값 단정이 남아 있고(**값 단정 삭제 0건**)」 | **무손실** |
| `ts108` additive 불변식(`:1810-1817`, `pm==work`·`captain==wait` 축퇴) **무조건 단정 유지** · `ts137` `quiet_hours_applied is False`·`quiet_hours_label == ""` 무조건 유지 | §3.6.2 | **AC(b)** 후단 「additive 불변식·`quiet_hours` 계약 단정이 **무조건 형태로 보존**」 | **무손실** |
| 단언 유효성 역검증(H-8 reward hacking 방어) | §3.6.5 TS-053 | **AC(c)** 「코호트 데이터를 인위 변경하면 두 테스트가 **실패한다**」 | **무손실** |
| TS 매핑 TS-050→AC(a) · TS-051·052→AC(b) · TS-053→AC(c) | `PLAN.md:600-603` | TEST-SCENARIO §4 동일 | **일치** |

> **판정: 재배정 과정의 단언 약화 0건.** R-8 AC는 PLAN F-006의 네 보장을 전건 승계하며, AC(c)(역검증)는 오히려 TASK 수준 요구로 **격상**됐다(이전에는 PLAN 자기완결 TS였다).

**단 1건의 사실 오류를 발견했다 → G-11.** R-8의 「어디에」가 `dashboard/backend/tests/test_stats.py`로 적혀 있으나, `ts108`·`ts137`은 **`test_routers.py`**에 있다 — `PLAN.md` §2.6.1(`ts108` `:1826-1832` · `ts137` `:1885-1890`) · §3.6.1(변경 대상 = `test_routers.py`) · **실행 증거 E1-1**(`pytest dashboard/backend/tests/test_routers.py -k "… ts108 or ts137 …"` → `assert 857 == 425` · `assert 1127 == 799`)이 일치한다. 더구나 `test_stats.py:59`는 `TASK.md` §범위 ④에서 **사실오류로 제외**된 파일이다. 실질(AC·TS·PLAN 파일 맵)은 모두 옳으므로 **충돌 정정 자체는 sound**이나, SSOT 문서의 「어디에」 필드는 정정 대상이다.

### 1-2. TS-027·TS-028·TS-029 ↔ R-7 AC(a)(b)(d) (번호만 붙었는지 본문 검증)

| R-7 AC 원문 (`TASK.md`) | 신설 TS 본문 (TEST-SCENARIO §L2) | 판정 |
|---|---|---|
| **(a)** 3파일 `@header`가 `code-scan validate` **차단 판정을 통과** | **TS-027** 「FE 3파일이 `uncovered:incomplete` **차단 위반 집합에서 사라진다**(3건 → 0건). 차단 위반을 **집합으로** 대조 — 건수만 보면 3건이 사라지고 다른 3건이 생겨도 통과한다」 | **검증한다 (AC보다 강함)** — 건수 단정의 사각(대체 위반 유입)을 집합 단정으로 차단 |
| **(b)** 107 신설 규정 준수 — `description`에 서로 다른 태스크 번호 2개 이상 금지 · 이력 전용 필드 신설 금지 | **TS-028** 「`description`에 서로 다른 태스크 번호 **2개 이상 0건** · **이력 전용 필드 0건**(`header_history` 검출 0)」 | **검증한다 (AC 2조건 1:1 대응)** |
| **(d)** `counts` 기존 키 값 **회귀 0건** | **TS-029** 「`validate` `counts` **기존 키 값이 차단 3건 감소 외 이동 0**. `exports` 추가가 다른 집계를 건드리지 않는다」 | **검증한다** — E1-4가 차단 사유를 `uncovered/incomplete`(detail `exports`)로 실측했으므로 「`exports` 추가」가 정확한 변경 원천이고, 그 부작용 없음을 단정 |
| (c) 허브·워크트리 `validate` **exit 0** | TS-023(워크트리) + **TS-071**(허브·머지 후) — 시점 분리 | 기존 커버 유지 |

> **판정: 번호만 붙인 빈 매핑 아님.** 세 TS 모두 AC 문면의 판정 조건을 담고 있고, TS-027은 AC 문면보다 판정력이 높다. `PLAN.md:438-440`도 동일 문면으로 상류 정렬돼 있다.

### 1-3. `PLAN.md`의 「R-7 = 이동값」 잔존 grep (iteration 2 G-5 기전 재발 여부)

```
PLAN.md:14   → 「R-8로 신설 … (초판은 R-7로 적었으나 R-7은 FE @header 정비에 이미 배정된 번호였다 — 2026-09-07 정정)」
PLAN.md:58   → 「… 2건은 선재 실패라 R-8도 신설」
PLAN.md:88   → 「F-003 … + FE @header 정비 | R-3, R-7」   ← R-7 = FE @header
PLAN.md:91   → 「F-006 | 이동값 단언 규약 정렬 | R-8(신설)」 ← 이동값 = R-8
PLAN.md:93   → 정정 경위 + 「TASK.md §요구사항 표 갱신 완료(2026-09-07)」
PLAN.md:245·571·600-603 → F-006 제목·TS 매핑 전건 R-8
PLAN.md:1008 → 리스크 R-11 해소 기재
```

- **`PLAN.md` 산문에 「R-7 = 이동값」 잔존 0건.** 상류가 하류와 같은 의미로 정렬됐다 — iteration 2 G-5(하류만 고치고 상류를 둠)의 **재발 없음**.
- 다만 `PLAN.md:1004`의 **리스크 표 `R-7`**(신규 파일 `@header` 미보유 → `newly_uncovered` 차단 승격)은 **리스크 ID 네임스페이스**의 `R-7`이다(리스크 R-1~R-12 ↔ 요구 R-1~R-8 동형 접두사). 의미 잔존이 아니라 **네임스페이스 중첩**이며, 이번 충돌(TASK R-7 ↔ PLAN R-7)과 같은 오독 재료다. 산문 중복 아님 → 감점 무영향, 관찰로 기록.
- **정규화 페이로드에는 잔존이 남아 있다** — `TS-023.covers_features`에 **`F-006`**(R-7이 F-003으로 확정된 뒤의 구버전 링크, `PLAN.md:88` 대비) → **G-10**.

### 임무 1 결론 — `collision_fix_verdict: sound`

ID 재배정 방향(이동값 → R-8 / R-7 = FE `@header`)·AC 승계·TS 신설·상류 정렬 모두 옳다. 단 **G-11**(R-8 「어디에」 파일 오기) 1건과 **G-10**(페이로드 미전파) 1건이 정정의 미완 부분이다.

---

## 2. 임무 3 — skip 0 결정의 함의 검증

| 확인 항목 | 결과 |
|---|---|
| TS-035가 「새로 켜진 테스트의 **신규 실패 0건**」을 요구하는가 | **요구한다** — TS-035 기대 결과 말미 「**새로 켜진 테스트의 신규 실패 0건**(skip을 없앤 대가로 실패를 남기면 목표 미달)」. §2.3 [MUST] 블록·`TASK.md` R-4 AC(e)·`PLAN.md:500`에도 동일 문면 |
| 허브 skip 1건의 정체가 문서화됐는가 | **됐다** — §2.3 skip 사유 실측 표: `test_adapters.py:88`「실 태스크 디렉토리 없음」이 **허브에서도** skip 중이며 사유가 이 태스크가 고치는 하드코딩(찾는 폴더가 `tasks/backup/`으로 이동) |
| 워크트리 2건 해소 경로가 실재하는가 | **실재한다** — `test_parsers.py:19` `AI_FRAMEWORK_ROOT` → `.opal/MEMORY.md`·`.opal/AGENT.md` 참조(`PLAN.md:201`), Step 배정 있음(`PLAN.md:704-706`), 변경 파일 맵 등재(`PLAN.md:458`) |
| 0 skipped를 **테스트 삭제·조건 무력화**로 달성하는 경로가 막혔는가 | **막히지 않았다 → G-8.** TS-035·TS-070·TS-071 어디에도 **수집 수 불변** 단정이 없다. §2.3이 「수집 **358** 양쪽 동일」을 실측해 두었으므로 단정 재료는 이미 있다 |
| 상류(PLAN) 완료 기준이 결정을 승계했는가 | **부분 승계 → G-7.** `PLAN.md:500`(TS-035 행)·`:846`(F-004 완료 기준)·`:1032`(결정 기록)은 「양쪽 `0 skipped`」로 갱신됐으나, **Step 13 완료 기준 `PLAN.md:802`**와 **§5.2 회귀 표 목표 열 `PLAN.md:854`**는 「skip **수치·사유 동일**」 구문면 잔존이다. 「동일」은 허브 1 skipped ↔ 워크트리 1 skipped로도 충족되므로 **R-4 AC(e) 미달 상태에서 Step 13이 통과 판정될 수 있다** |

---

## 3. 임무 4 — TS-071 절차 안전성

| 요건 | TS-071 문면 | 판정 |
|---|---|---|
| 검증 시점 명기 | 조건: 「**머지 후 push 전**에 허브에서 §2.3 명령을 그대로 실행한다(소유자 결정 2026-09-07)」 | **명기** |
| 되돌림 조건 명기 | 조건: 「실패 시 `git reset --hard`로 **머지 자체를 되돌린다**」 / 기대 결과: 「**하나라도 미달이면 push하지 않고 되돌린다**」 | **조건·기대 결과 양쪽에 명기** |
| 판정 대상 3종 + 현재값 | 허브 state-tool 0 failed(현재 1) · 허브 console BE **0 failed / 0 skipped**(현재 9 failed / 1 skipped) · 허브 `validate` **exit 0**(현재 exit 2 / 차단 3건) | **현재값 대비 목표 — 실판정 가능** |
| 이연이 누락과 구별되는가(G-3 처방) | 자기 ID + `실행 명령`·`결과`·`상세` 셀 보유, `PLAN.md:442`에도 동일 문면 | **구별된다** |
| 원격 오염 방지 | 「원격에는 통과한 상태만 올라간다」 | **명시** |

> **판정: 안전하다.** 되돌림 조건이 조건절과 기대 결과에 이중 기재되고, 실패 시 push 금지가 문면으로 못박혀 있다. 잔여 위험은 「머지 커밋 이후 다른 작업이 끼어들면 `reset --hard`의 되돌림 범위가 넓어진다」이나, 이는 CLOSE 절차 규율(PM 소관)이며 시나리오 문면 결함이 아니다.

---

## 4. 임무 2 — 판단축 ①⑤⑥ 재채점

### ① 목표 달성 (0~2) → **2**

목표 = 「허브 루트 해석 규칙을 세우고 조립 9곳을 그 함수로 수렴시켜 **허브·워크트리 양쪽 `0 failed`**」 + 별개 축 「테스트의 실 태스크 폴더 하드코딩 제거」.

| 목표 구성 | 직접 검증 시나리오 | 계층 |
|---|---|---|
| 워크트리 `0 failed`(4스위트) | **TS-070**(§2.3 기준선 대비, 실행 위치·인터프리터 [MUST] 포함) | L2 운영 |
| 허브 `0 failed` + `validate` exit 0 | **TS-071**(머지 후 push 전, 되돌림 조건 포함) | L2 운영 |
| **양쪽 `0 skipped`**(신설 R-4 AC(e)) | **TS-035**(`-rs` 사유 목록 대조 + 신규 실패 0건) + TS-071(허브 절반) | L2 |
| 워크트리 참조가 실제로 동작 | TS-022(`header_source_unset` 소거) · TS-023(워크트리 exit 0) · TS-025(워크트리 `brain-tool search` `ok:true`) | L2 |
| (A) 위치 이동 내성 | TS-032(098이 `backup` 아래인 **현 상태**에서 S-31 통과) · TS-042(코호트 21건 전건 관측) | L1·L2 |
| 목표 달성 **판정의 진위** | **TS-061**(캡틴이 출력만 보고 「무엇이 검증됐는가」 판단 — M3) | **L3 사용자** |

- 신설 요구가 목표축을 **넓혔고**(0 skipped) 그 절반씩을 TS-035·TS-071이 실판정 형태로 들고 있다. 사용자 계층(L3 TS-061)·운영 계층(L2 TS-070/071) 양쪽에 목표 검증자가 존재 → 앵커 2 충족.
- 감점 검토: TS-035의 **시점 미분리**(G-9)는 허브 절반을 TEST 단계에서 요구하는 문면 모순이나, 허브 절반은 TS-071이 자기 ID로 보유하므로 **커버 누락이 아니다**(G-3와 달리 이연 대상이 실존). iteration 1이 동급 시점 결함(G-3)과 함께 ①=2를 준 선례와 정합 → **감점하지 않고 G-9로 기록**.

### ⑤ 채택/잔존 (0~2) → **2**

| 교체형 축 | 구형 잔존 0 단정 | 신형 채택 단정 |
|---|---|---|
| 자기 루트 직접 조립 | **TS-020** 「`cwd()`·`__file__` 파생 조립 중 **정규화 미경유 지점 0건**」 | TS-011·TS-012·TS-014(해석 함수 경유·3구현 동치·`find_project_root` 정합) |
| 폴더명 하드코딩 | **TS-030** 「대상 3파일에 특정 태스크 폴더명 리터럴 0건(접두사만 남음)」 | TS-032(접두사 동적 탐색으로 통과) |
| 3지점 각자 열거 | **TS-047** 「3지점 어디에도 디렉터리 순회 호출이 남지 않는다」 — 판정을 **성질 기준**(「이 지점이 `tasks/`를 스스로 순회하는가」)으로 두고 `scandir`·`iterdir`·`listdir`·`glob`·`rglob`·`walk` 전건 근사(G-6 반영) | TS-047 주절 「열거 함수가 1개이고 3지점이 **그것만** 호출」 + TS-040(이름 집합 항등) |
| 중복 구현·사본 | **TS-010**(런타임별 1개씩) · **TS-060**(골든 표 **정확히 1개**, 사본 0건 — 「오늘 답이 같은 것과 내일도 같을 것은 다른 명제다」) | TS-060(3스위트가 **모두 그 경로**를 읽는다) |
| 신설 R-7 잔존 축 | **TS-027**(FE 차단 위반 3건 → **집합에서 소거**, 대체 위반 유입까지 배제) | TS-023·TS-071(exit 0 = 정비 후 상태 채택) |

- 신설 요구 3건이 들어와도 잔존/채택 쌍이 **모두 property 단정**으로 유지된다. iteration 1의 ⑤=1 사유(G-1: 열거 축에 잔존0 등가물 부재)는 TS-047로 닫힌 상태가 유지되고, G-6 처방(이름 열거 → 성질 기준)이 문면에 반영돼 있다 → 앵커 2 충족.
- 감점 검토: R-8은 **교체형이 아니라 선재 실패 교정**이므로 잔존 축 대상이 아니다. 다만 「무필터 이동값 단정」 잔존을 성질로 막는 단정은 없다(`ts108`·`ts137` **이름 지목** 한정) — 범위를 2건으로 확정한 것은 `TASK.md` R-8 문면의 요구사항 수준 결정이므로 시나리오 결함으로 세지 않는다. 관찰로 기록(O-8).

### ⑥ 경계/부정 (0~2) → **2**

| 유형 | 시나리오 |
|---|---|
| 경계값(규칙 자체) | **C-5 중첩 워크트리(첫 출현 기준)** · **C-6 유사 이름 `.opal-worktrees-old` 비매칭** · **C-7 세그먼트 최상위 직하**를 골든 표가 보유(`PLAN.md:352-358`)하고 **TS-011**이 C-1~C-7 전건 동일 문자열, **TS-012**가 항등 케이스 바이트 동일을 단정 · TS-002(세그먼트 **완전 일치** 명시) |
| 부정 경로(보안) | **TS-045** 「`task_id`에 `../`·절대경로·구분자 → **전건 404**, 2단 열거로 늘어난 **각 지점 개별 확인**」(H-5 P0) |
| 조용한 강등 금지 | **TS-033** 「접두사 매칭 0건·2건일 때 **skip이 아니라 실패**」(`PLAN.md:470` `self.fail()` 고정) |
| 단언 유효성 역검증 | **TS-053** 「코호트 데이터를 인위 변경하면 두 테스트가 **실패한다**」 · **TS-051** 「값 단정 삭제 0건」 |
| 의미 반전 방지 | **TS-034** 「`test_config.py`·`test_deploy_smoke.py` 무변경 — 소스 트리 참조를 정규화하면 허브 소스를 검증하게 되어 단언 의미가 뒤집힌다」(H-7) |
| 격리 오염 방지 | **TS-024** 「명시 `--brain-path` 격리 유지, brain-tool 142건 회귀 0」(H-3) |
| 무변경 단정 | TS-021(허브 출력 **바이트 동일**) · TS-044(`doctor`가 **허브로 치환하지 않는다**) · TS-046(합성 픽스처 불변) · **TS-029**(신설 — `counts` 이동 0) |
| 신규 파일 차단 승격 | TS-015(HEAD 부재 파일 `newly_uncovered`) · **TS-028**(신설 — 107 규정 위반 0건, 「107의 성과를 되돌릴 실재 위험」) |
| 음성 케이스(CLI) | `PLAN.md:657` ③ 「설정을 워크트리 쪽에만 두면 `header_source_unset`이 난다」 |

- 신설 요구 3건 모두 부정형 단정을 동반한다(TS-027 집합 대조 · TS-028 0건 · TS-029 이동 0 · TS-035 신규 실패 0). 규칙 경계는 골든 표 C-5·C-6·C-7로 트랩까지 덮여 있다 → 앵커 2 충족.
- 감점 검토: **G-8**(0 skipped를 테스트 삭제·조건 무력화로 달성하는 경로 미차단)은 신설 AC(e)에 대한 실재하는 부정 경로다. 그러나 ⑥은 「경계·부정 경로 시나리오 **존재**」 축이고 위 9유형·13건이 실재하므로 「0: 정상 경로만」·1점 앵커에 해당하지 않는다 → **감점하지 않고 G-8로 기록**(반영 강권).

### 페이로드 플래그 본문 검증 ([MUST] Producer 산정값 불신)

| 플래그 | Producer | 본문 검증 | 판정 |
|---|---|---|---|
| goal 9 (TS-022·023·025·032·035·042·061·070·071) | 9 | 축 성립의 실체는 **TS-070·TS-071**(직접 목표) + TS-035(0 skipped) + TS-061(L3). TS-042는 실체가 R-6 AC 기전 검증, TS-061은 「목표 달성 판정의 진위」를 지키는 메타 방어 — **과대 산정 2건 미해소**(iteration 1·2 동일 지적) | **축 등급 무영향** — 축을 뒤집는 과대 산정 없음 |
| adoption 14 | 14 | 14건 전건이 잔존0 또는 채택 단정에 실체 대응(위 ⑤ 표). 과대 0건 | **정합** |
| boundary 14 | 14 | TS-002·TS-003은 「문서에 경계 규칙이 **명시**되는가」로 실행 경계 케이스가 아니다(약한 2건). 그러나 TS-011·TS-012가 C-5·C-6·C-7 실행 경계를 덮으므로 실효 경계 12건 이상 | **축 등급 무영향** |
| §1 가설 표 ↔ 페이로드 양방향 일치(iteration 2 O-1) | — | H-1~H-11 **전건 일치**(H-1 7 · H-2 5 · H-3 2 · H-4 5 · H-5 2 · H-6 3 · H-7 1 · H-8 4 · H-9 2 · H-10 1 · H-11 3, 차집합 0) | **O-1 닫힘** |
| §4 매핑 ↔ `TASK.md` AC 문자 | — | R-1(a~d) · R-2(a~c) · R-3(a~d) · R-4(a~e) · R-5(a~e) · R-6(a~e) · R-7(a~d) · R-8(a~c) **전 AC 문자 매핑 존재, 미매핑 0건** | **AC 단위 정합 확보** |
| §4 매핑 ↔ 페이로드 requirement 링크 | — | **불일치 4건** — TS-035(§4: R-4+R-5 / 페이로드: R-5) · TS-041·TS-042(§4: R-4 AC(d)+R-6 / 페이로드: R-6) · TS-071(§4: R-5+R-7 AC(c) / 페이로드: R-5). 이번 iteration 신설 링크가 페이로드에 미전파 | **G-10** |

---

## 5. 판단축별 판정

| 판단축 | 척도 | 점수 | 통과선 | 근거 (지목) | gap |
|--------|------|------|--------|------|-----|
| ① 목표 달성 | 0~2 | **2** | ≥1 | 워크트리 **TS-070** + 허브 **TS-071**(현재값 대비 목표·되돌림 조건 명기) + 신설 0 skipped **TS-035** + L3 사용자 판정 **TS-061** — 사용자·운영 계층 양쪽에서 목표 직접 검증 | 없음(G-9는 문면 권고) |
| ⑤ 채택/잔존 | 0~2 | **2** | ≥1 | 구형 4축 전건 잔존0 단정(**TS-020·TS-030·TS-047·TS-010/060**) + 신형 3축 채택 단정(**TS-011/012/014 · TS-047 · TS-060**) + 신설 R-7 잔존축(**TS-027** 집합 대조) | 없음(O-8은 관찰) |
| ⑥ 경계/부정 | 0~2 | **2** | ≥1 | 경계(골든 표 **C-5·C-6·C-7** ↔ TS-011·TS-012) + 부정(**TS-045** 404 전건 · **TS-033** skip 금지 · **TS-053** 역검증 · **TS-034** 의미 반전 · **TS-024** 격리) + 신설 3건 부정형(**TS-027·028·029**) | 없음(G-8은 반영 강권 권고) |

**0점 축 없음 / 평균 = (2+2+2)/3 = 2.00 ≥ 1.5 → `verdict: pass`**(`scenario-gate.md` §5-1)

---

## 6. gap · 관찰 (전건 감점 아님 — 지목 포함)

### 반영 강권 (신설 요구 방어에 직결)

| # | 지목 | 내용 | 처방 |
|---|------|------|------|
| **G-7** | `PLAN.md:802`(Step 13 완료 기준) · `PLAN.md:854`(§5.2 회귀 표 목표 열) | 소유자 결정(skip 0)이 **상류에 부분 승계**됐다. `:500`·`:846`·`:1032`는 「양쪽 `0 skipped`」로 갱신됐으나 `:802`·`:854`는 「skip **수치·사유 동일**」 잔존 — 「동일」은 허브 1 skipped ↔ 워크트리 1 skipped로도 충족되므로 **R-4 AC(e) 미달 상태로 Step 13이 통과 판정될 수 있다**. iteration 2 G-5 기전(하류 정렬·상류 잔존)의 **재발 지점** | `:802`·`:854`를 「양쪽 `0 skipped`」로 정렬 |
| **G-8** | **TS-035**(+TS-070·TS-071) | `0 skipped`를 **테스트 삭제·skip 조건 무력화**로 달성하는 경로가 어느 단정에도 막혀 있지 않다. §2.3이 「수집 **358** 양쪽 동일」을 실측해 두었으므로 단정 재료는 이미 있다. skip 0은 소유자가 「약화된 단언 제거」로 편입한 목표인데, 그 달성 수단이 다시 약화가 될 수 있다 | TS-035 기대 결과에 「양쪽 **수집 수 358 불변**(테스트 삭제·조건 무력화로 `0 skipped`를 달성하지 않는다)」 절 추가 |
| **G-9** | **TS-035** | 시점 규율 미분리 — 허브 절반은 머지 전 판정 불가인데 문면은 「허브·워크트리 양쪽」을 TEST 단계 F-004 표에서 요구한다. **G-2가 TS-023에 적용한 처방**(「허브 exit 0은 머지 후 TS-071에서 확인」 1문 명기)이 강화된 TS-035에는 적용되지 않았다. TS-071이 허브 0 skipped를 보유하므로 커버 누락은 아니나, §2.3 [MUST]가 경고한 **허브/워크트리 수치 오독**(PM 실측 중 2회 발생) 재료다 | TS-035에 「허브 절반은 **TS-071**에서 확인」 1문 추가 |

### 정합 (판정력 무영향)

| # | 지목 | 내용 |
|---|------|------|
| **G-10** | `.scenario-coverage-input.json` | 이번 iteration이 §4에 신설한 링크가 **페이로드에 미전파** — `TS-035`에 `R-4` 누락(AC(e) 신설분) · `TS-041`·`TS-042`에 `R-4` 누락(AC(d) 매핑분) · `TS-071`에 `R-7` 누락(**iteration 2 O-2 미해소**). 추가로 `TS-023.covers_features`에 **`F-006` 잔존**(R-7이 `PLAN.md:88`에서 F-003으로 확정된 뒤의 구버전 링크 — 충돌의 마지막 잔존) · `TS-070`·`TS-071`의 `F-006` 귀속은 임의(R-5는 전 기능 공통 게이트). R-ID 단위 커버는 유지되어 `all_covered: true`·exit 0에 영향 없음. 방향은 iteration 2 G-5의 **역방향**(문서를 고치고 페이로드를 둠)이며, `scenario-gate.md` §3이 페이로드를 정규화 계약으로 지정하므로 재생성 시 링크가 소실된다 |
| **G-11** | `TASK.md` R-8 「어디에」 | `dashboard/backend/tests/test_stats.py`는 **오기**다. `ts108`·`ts137`은 `test_routers.py:1826-1832`·`:1885-1890`(`PLAN.md` §2.6.1·§3.6.1, 실행 증거 E1-1)에 있고, `test_stats.py:59`는 §범위 ④에서 **사실오류로 제외된 파일**이다. R-8 재배정 시 유입된 사실 오류 — AC 실질은 무손실(§1-1)이므로 문면 정정만 필요 |
| **G-12** | `TASK.md` R-4 AC(a) | 「**4곳**이 특정 폴더명에 의존하지 않는다」 vs 실제 **3파일**(TS-030 「대상 3파일」 · `PLAN.md:481` `test_routers.py:1007`·`test_parsers.py:19`·`test_adapters.py:85`, `test_stats.py:59` 제외). iteration 2 **O-7**(R-3 「8곳」 ↔ 「9곳」)과 동류의 수량 드리프트이며 미해소. TEST-SCENARIO는 수치가 아니라 property로 단정하므로 면역 |

### 관찰

- **O-8** R-8의 「무필터 이동값 단정」 제거는 `ts108`·`ts137` **이름 지목** 한정이다 — 같은 성질의 단정이 다른 곳에 남아도 어느 TS도 막지 않는다(107 brain `prohibit-by-property-not-name`의 미적용 지점). 범위를 2건으로 확정한 것은 `TASK.md` R-8 요구사항 수준 결정이므로 시나리오 결함으로 세지 않는다. 별건 후보.
- **O-9** `PLAN.md` **리스크 ID 네임스페이스**(`R-1`~`R-12`)가 요구사항 ID(`R-1`~`R-8`)와 접두사를 공유한다 — `PLAN.md:1004`의 리스크 `R-7`(신규 파일 `@header`)은 요구 R-7과 무관하다. 이번 충돌과 동종의 오독 재료. 별건 개선 후보(리스크 접두사 분리, 예: `RK-N`).
- **O-10** iteration 2 편집 관찰 **미해소 2건** — §2.2 「시나리오별 데이터 흐름」 표에 **TS-071 행 부재**(O-3) · TS-026이 「### L3」 표제 아래 있으나 `계층` L2 / 방식 M1(O-6). 판정력 무영향.
- **O-11** `PLAN.md:71` R-5 AC(c) 해석 고정이 워크트리 조건부 skip을 `test_parsers.py:150,163,**217**`로 4개 지목하는데 §2.3 실측 표는 3건(`:150`·`:163`)이다. TS-035가 `-rs` **사유 목록 실측 대조**로 판정하므로 목록 차이는 판정에서 흡수된다.
- **O-12** 플래그 과대 산정 2건(`TS-042`·`TS-061` `is_goal_scenario`)은 iteration 1·2와 동일하게 미해소. 목표축이 TS-070·TS-071로 성립하므로 **축을 뒤집는 과대 산정은 없다**.

### 무진전 신호 (§5-3)

| 항목 | it.1 | it.2 | **it.3** |
|---|---|---|---|
| scores | 2/1/2 | 2/2/2 | **2/2/2** |
| average | 1.67 | 2.00 | **2.00** |
| 열린 gap | 4 | 0(권고 2·관찰 1) | **0 감점 / 권고 3 · 정합 3** |
| 시나리오 | 40 | 40 | **43** |
| 요구사항 | R-1~R-7(7) | R-1~R-7(7) | **R-1~R-8(8)** |

**무진전 아님** — 요구사항 집합이 +1(R-8) · AC +4(R-4 AC(e), R-7 AC(a)(b)(d) 실검증) · 시나리오 +3 확장된 상태에서 세 축이 통과선을 유지했다. iteration 2의 권고 G-5·G-6·O-1은 전건 닫혔고(상류 PLAN 정렬 확인 §1-3 · TS-047 성질 기준 · §1↔페이로드 양방향 일치), 신규 권고는 **신설 요구가 만든 새 표면**(skip 0 · R-8 재배정 · 페이로드 전파)에서 나왔다. 반복이 같은 자리를 돌지 않았다.

---

## 7. 종합

```json
{"scores": {"goal": 2, "adoption": 2, "boundary": 2}, "average": 2.0, "gaps": ["G-7: PLAN:802(Step 13 완료 기준)·PLAN:854(§5.2 목표 열)에 「skip 수치·사유 동일」 구문면 잔존 — 허브 1 skipped ↔ 워크트리 1 skipped로도 충족되어 R-4 AC(e)(양쪽 0 skipped) 미달 상태로 Step 13 통과 판정 가능 (감점 아님, 반영 강권)", "G-8: TS-035에 「양쪽 수집 수 358 불변」 단정 부재 — 테스트 삭제·skip 조건 무력화로 0 skipped를 달성하는 경로가 열려 있다(§2.3에 실측 재료 존재) (감점 아님, 반영 강권)", "G-9: TS-035 시점 규율 미분리 — G-2가 TS-023에 적용한 「허브는 TS-071에서 확인」 처방이 강화된 TS-035에 미적용. TS-071이 허브 절반을 보유하므로 커버 누락은 아님 (감점 아님, 권고)", "G-10: 페이로드 미전파 — TS-035에 R-4 누락, TS-041·TS-042에 R-4(AC(d)) 누락, TS-071에 R-7 누락(it.2 O-2 미해소), TS-023.covers_features에 F-006 잔존(충돌 구버전 링크). all_covered/exit 0 무영향 (정합)", "G-11: TASK.md R-8 「어디에」가 test_stats.py 오기 — ts108·ts137은 test_routers.py:1826-1832·1885-1890(PLAN §2.6.1·E1-1). test_stats.py:59는 §범위 ④에서 사실오류로 제외된 파일 (정합)", "G-12: TASK.md R-4 AC(a) 「4곳」 vs 실제 3파일(TS-030·PLAN:481) — it.2 O-7(8곳/9곳)과 동류 수량 드리프트 미해소. TEST-SCENARIO는 property 단정으로 면역 (정합)"], "closed": ["G-5", "G-6", "O-1"], "still_open": [], "verdict": "pass", "collision_fix_verdict": "sound"}
```

| 항목 | it.1 | it.2 | **it.3** | 통과선 |
|------|------|------|------|--------|
| ① 목표 달성 | 2 | 2 | **2** | ≥1 |
| ⑤ 채택/잔존 | 1 | 2 | **2** | ≥1 |
| ⑥ 경계/부정 | 2 | 2 | **2** | ≥1 |
| 평균 | 1.67 | 2.00 | **2.00** | ≥1.5 |
| 0점 축 | 없음 | 없음 | **없음** | — |
| **verdict** | pass | pass | **pass** | — |

판정 근거: `scenario-gate.md` §5-1 — 「커버리지 누락 = 0(②③④ hard gate) **AND** 판단축(①⑤⑥) 각 ≥1점(0점 축 없음) **AND** 평균 ≥1.5」. `scenario-coverage-check` exit 0(`all_covered: true`, R 8 / F 6 / H 11 / S 43) + 본 판정 세 축 2점 → **수렴(PASS)**. §6 tool-gated 집행의 두 증거가 모두 존재하므로 게이트 PASS가 성립한다.

**신설 요구 3계열 재검 결론**
- **R-4 AC(e)(skip 0)** — TS-035가 「양쪽 `0 skipped`」 + 「새로 켜진 테스트의 **신규 실패 0건**」을 문면으로 요구하고 허브 절반은 TS-071이 보유. 해소 경로(`test_parsers.py:19` · `test_adapters.py:85`)도 PLAN Step에 배정돼 있다. **다만 G-7·G-8·G-9가 이 요구 하나에 몰려 있다** — 목표는 옳게 들어왔고 방어가 얇다.
- **R-7 AC(a)(b)(d)** — TS-027·TS-028·TS-029가 AC 문면의 판정 조건을 실제로 담고 있고, TS-027은 AC보다 강하다(집합 대조). 번호만 붙은 매핑 아님.
- **R-8** — PLAN F-006의 네 보장을 무손실 승계, AC(c) 역검증은 TASK 수준으로 격상. **단언 약화 0건.**

> **[MUST] 감점 사유 없음 — 근거.** 세 축 모두 통과선의 상위 앵커에 해당하는 시나리오가 **본문 대조로 실재**한다(①: TS-070·TS-071·TS-035·TS-061 / ⑤: TS-020·TS-030·TS-047·TS-010·TS-060·TS-027 / ⑥: C-5~C-7↔TS-011·TS-012, TS-045·TS-033·TS-053·TS-034·TS-024). 신규 권고 6건은 ① **상류 문서·페이로드의 정합**(G-7·G-10·G-11·G-12) 또는 ② **채점 대상 산출물 안의 문면 보강**(G-8·G-9)이며, 어느 것도 「목표를 검증하는 시나리오가 없다」·「경계·부정 경로가 없다」·「구형 잔존/신형 채택 미검증」에 해당하지 않는다. iteration 1·2가 상류 미정렬(G-5)·이름 열거 한계(G-6)를 감점하지 않은 선례와 동일 기준을 적용했다.

> **[MUST] drift 필요성 절은 본 phase에 없다** — `scenario-rubric`은 Base Likert 트랙 전용 신호(계약 drift)를 산출하지 않는다.

> **[MUST] verdict-only.** 본 에이전트는 판정과 제안만 반환하며 `SCENARIO-GATE-3.md` 외 어떤 산출물도 생성·수정하지 않았다. 권고 6건의 반영 여부·방법 판단과 실행은 **PM의 책임**이다. **반복 상한 3회의 마지막 회차이며 `verdict: pass`이므로 사용자 에스컬레이션(§5-2) 대상이 아니다.**
