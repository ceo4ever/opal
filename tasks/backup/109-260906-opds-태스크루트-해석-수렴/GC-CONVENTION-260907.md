# GC CONVENTION REPORT — 260907

## 1. 헤더

- 실행 일시: 2026-09-07 (TEST PM Gate 디스패치)
- 범위: 태스크 109 변경 25파일 (`git diff HEAD~2 HEAD` 기준, 커밋 `880a486`+`70d6361`)
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` (존재) + `opal/core/references/header-standard.md`(§2·§2.1·§4.2, @header 규칙 원문 소유)
- APPLY 수행 여부: N (진단 전담, 수정 금지 지시에 따름)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 3 |
| 심각도 분포 | Critical 0 / High 1 / Medium 1 / Low 1 / Info 0 |
| 자동 수정 가능 | 0 |
| 수동 조치 필요 | 3 |
| 이 태스크 기인 | 3건 전건 |
| 선재(범위 외) | 0건 (알려진 2건은 별도 대상 아님 — 확인만 하고 미포함) |

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

### High (1건)

- [ ] GC-C001 [`docs/CONVENTIONS.md:269-270`] §구현 규칙 「허브 루트 해석」 절이 `opal-harness.md` §2.5 (4)의 규칙 원문을 사실상 그대로 재서술
  - 카테고리: 문서화 / 규칙 원문 복제 금지
  - 위반 기준: 이 태스크 R-2 AC(c) 및 `opal-harness.md` §2.5(4) 도입부 "본 규칙의 원문은 이 항 한 곳에만 존재한다... 정의를 복제하지 않는다" — `docs/CONVENTIONS.md` 자신도 269줄에서 "런타임별 구현은 규칙 원문을 재서술하지 않고... 포인터 주석만 둔다"고 선언하면서 정작 269-270행 자신은 포인터가 아니라 원문 실질 내용을 복제했다.
  - 설명: `docs/CONVENTIONS.md:269` "적용/제외 경계: 정규화는 허브 고정 데이터 참조에만 적용하고 소스 트리 내용을 다루는 경로(브랜치 소스 검증·빌드·배포 대상, 진단 도구)에는 적용하지 않는다 — 적용하면 워크트리가 자기 변경 대신 허브 소스를 보게 되어 검증의 의미가 뒤집힌다"는 `opal-harness.md` §2.5(4) 4항 "정규화는 허브 고정 데이터((3)의 `tasks/`·`.opal/`) 참조에만 적용한다. 소스 트리 내용을 다루는 경로(브랜치 소스 검증·빌드·배포 대상)에는 적용하지 않는다 — 적용하면 워크트리가 자기 변경 대신 허브 소스를 보게 되어 검증의 의미가 뒤집힌다"와 어순·어휘가 거의 동일하다. `docs/CONVENTIONS.md:270` "프로젝트 루트 판정은 자기 `.opal/`을 가진 첫 조상이 이긴다. 세그먼트 정규화는 그 탐색이 실패했을 때의 해석이며 탐색을 대체하지 않는다"도 `opal-harness.md` 부칙 「탐색 우선순위」 "실행 지점에서 상향 탐색해 자기 `.opal/`을 가진 첫 디렉터리가 프로젝트 루트다. 세그먼트 정규화는 그 탐색이 `.opal/`을 찾지 못했을 때의 해석이며, 탐색을 대체하지 않는다"의 재서술이다. 3구현(`paths.py`·`brain_tool.py`·`code-scan.js`)은 포인터 주석만 두어 규율을 지켰으나, `docs/CONVENTIONS.md` 자신은 규율을 어겼다.
  - 해결 방안: 269~270행을 "정규화 적용/제외 경계 및 탐색 우선순위의 원문은 §2.5(4)를 따른다"는 1줄 포인터로 압축하고, 실질 문장(경계 조건·되짚는 이유·"의미가 뒤집힌다" 등 서술)은 삭제한다.
  - 자동 수정: N
  - 참조: 태스크 요청서 §③ "규칙 원문 중복 금지"; `opal-harness.md:167` (§2.5(4) 도입부)

### Medium (1건)

- [ ] GC-C002 [`docs/CONVENTIONS.md:297`] 변경이력 표 행 삽입 위치·버전 채번 부정합 — v1.10.0(109) 행이 v1.9.0(107) 행보다 **먼저**(위에) 배치됨
  - 카테고리: 문서화 / 변경이력 작성 의무
  - 위반 기준: `docs/CONVENTIONS.md` §변경이력 작성 의무 — "버전 채번이 기존 계열과 정합해야 한다"(암묵), 및 표 관행상 신규 행은 기존 최신 행 다음에 추가한다. 직전 v1.9.0(107) 행 자체가 "직전 행이 v1.8.0 뒤에 v1.6.0으로 기재된 것은 106의 버전 표기 오류"라고 106의 순서 오류를 지적하며 자신을 "실제 최신인 v1.8.0을 기준으로 채번"했다고 명시했는데, 이번 109가 그 v1.9.0(107) 행보다 위에 v1.10.0을 끼워 넣어 **같은 유형의 순서 오류를 반복**했다.
  - 설명: 파일 296~298행 순서가 `v1.6.0(106)` → `v1.10.0(109)` → `v1.9.0(107)`로, 버전 숫자가 커지는 신규 행이 더 작은 버전 번호 행보다 위에 온다. 109 작업자가 107의 존재를 인지하지 못한 채 v1.8.0(098) 다음 빈 슬롯으로 v1.10.0을 채번한 것으로 보인다.
  - 해결 방안: 109 행을 v1.9.0(107) 행 **다음**으로 이동하고, 태스크 관계상 문제 없다면 버전 번호는 v1.10.0을 유지(107이 이미 v1.9.0을 점유하므로 순번 자체는 맞다 — 위치만 수정하면 됨).
  - 자동 수정: N (표 편집 판단 필요)
  - 참조: 태스크 요청서 §② "행이 있는가, 형식이 맞는가, 버전 채번이 기존 계열과 정합한가"; TBD — 사내 변경이력 표 정렬 관행 문서화 없음

### Low (1건)

- [ ] GC-C003 [`880a486`, `70d6361` 커밋 메시지] 커밋 Scope가 태스크 번호가 아닌 도메인명(`fw`/`code-scan`)
  - 카테고리: 코드 품질(커밋 규칙)
  - 위반 기준: `docs/CONVENTIONS.md` §커밋 규칙 §Scope "태스크 번호 사용: `feat(043): opal-doc-standard v2.0`"
  - 설명: 두 커밋 모두 `{type}({domain}): {설명} (109)` 형식 — Scope 자리에 태스크 번호 대신 `fw`/`code-scan`을 쓰고 태스크 번호는 메시지 말미 괄호로 별도 표기했다. 문면상 §Scope 규칙과 다르다. 다만 이 저장소의 선행 커밋(`5a8ad2b feat(fw): @header 이력 비기재 원칙 3층 적용 + 자산 43파일 정리 (107)`, `5a8ad2b` 등, 세션 시작 `git log` 참조)도 동일 패턴이라 **이 태스크가 새로 만든 관행이 아니라 저장소 전반의 기존 관행과 일치**한다 — 신설 위반이라기보다 문서-관행 불일치로 본다.
  - 해결 방안: 문서(§Scope)를 실제 관행(도메인 scope + 말미 태스크 번호 괄호)에 맞게 개정하거나, 향후 커밋부터 문서대로 Scope를 태스크 번호로 되돌릴지 소유자가 결정. 이 태스크의 커밋만 수정하는 것은 관행과의 정합성만 깨뜨리므로 권장하지 않는다.
  - 자동 수정: N
  - 참조: TBD — 커밋 컨벤션 vs 실제 관행 격차, 소유자 확인 필요

### Info (0건)

---

## 4. 문서 업데이트 제안 (트리거 없음)

트리거(빈도 N≥3, 새 카테고리) 발동 없음. GC-C003(커밋 Scope 관행 불일치)은 빈도상 저장소 전반에 반복되는 패턴이나, 이번 실행 대상 25파일 범위 안에서는 2건(커밋 2개)뿐이라 빈도 임계값(파일 수 기준 3) 미달로 트리거 미발동. 다만 관행과 문서의 지속적 불일치이므로 **참고 사항으로 기재**: `docs/CONVENTIONS.md` §Scope 예시를 실제 관행에 맞춰 갱신하는 안건을 별도로 상정할 것을 제안.

---

## 5. 문서 작성 유도

해당 없음 — `docs/CONVENTIONS.md` 존재.

---

## 개별 항목 판정 (요청 ①~⑤)

① `@header` 이력 비기재 원칙 — **위반 없음.** 신규/수정 10개 코드 파일의 `@header` 8필드(description·exports·depends·note 등) 전건을 문면 대조했다. `description`/`note`에 서로 다른 태스크 번호 2개 이상(`TASK_TAG_THRESHOLD=2`) 사례 없음(각 파일 최대 1개 태스크 번호 단발 인용, 예: `test-hub-root.js` description 말미 "(109)" 1건). 선언 8필드 밖 신설 필드 없음. "기존에는 …였는데" 식 이력 서술 없음. 유일한 예외 사례는 `opal/tools/code-scan/tests/test-hub-root.js:14-17`의 `@header` 블록 **밖** 평문 주석 "변경이력:\n  v1.0 … / v1.1 …"인데, 이는 `header-standard.md` §2.1이 `test-validate.js:39-48`을 선례로 명시적으로 적용 대상에서 제외한 패턴(블록 밖 평문 주석·`.md` `## 변경이력`)과 동일 구조이므로 **위반 아님**(별건 명시).

② 변경이력 작성 의무 — `opal-harness.md`(v1.18→v1.19, 태스크 요청서에 언급된 v1.19까지 정확히 반영)와 `harness/task-process.md`(v1.9→v1.10) 2건은 행 존재·형식(`YYYY-MM-DD HH:mm`, semver, 태스크 번호 괄호)·채번 정합 모두 이상 없음. `docs/CONVENTIONS.md`(v1.10.0)는 행 자체는 형식(일시·semver·태스크번호)이 맞지만 **표 내 위치가 v1.9.0(107) 행보다 앞선다** — GC-C002로 Medium 기재.

③ 규칙 원문 중복 금지(R-2 AC(c)) — 3구현(`paths.py`·`brain_tool.py`·`code-scan.js`)은 포인터 주석(`opal/core/references/opal-harness.md §2.5 (4)`)만 두고 규칙 절 자체(정의·깊이 무관·허브 항등·적용 대상 경계·의미 한계 5항, 부칙 3건)를 복제하지 않았다 — **준수**. `harness/task-process.md`도 포인터 1줄만 추가 — **준수**. 그러나 `docs/CONVENTIONS.md`는 "포인터만 둔다"고 선언한 문서 자신이 269~270행에서 §2.5(4) 4항과 「탐색 우선순위」 부칙의 실질 문장을 재서술 — **위반**(GC-C001, High).

④ 커밋 규칙 — 형식(`{type}({scope}): {한국어 설명}`)·Type(`feat`/`fix`)은 정확. Scope가 태스크 번호가 아니라 도메인명인 점은 문면과 다르지만 저장소 기존 관행(107 등)과 일치하는 선재 패턴 — Low로 기재(GC-C003), 이 태스크 신설 위반으로 단정하지 않음. "하나의 태스크 = 하나의 커밋" 원칙은 위반(2커밋)처럼 보이나, `fix(code-scan)` 커밋은 `feat(fw)` 커밋 이후 발견된 버그 수정으로 태스크 진행 중 정정 커밋이며 CONVENTIONS.md에 "원칙"이라 명시(강제 아님)되어 있어 판정 보류 — 규정상 확정 위반이라 부르지 않는다.

⑤ 네이밍·언어 규칙 — 신규 4파일(`paths.py`, `test_paths.py`, `hub-root-cases.json`, `test-hub-root.js`) 전건이 §네이밍 규칙(Python snake_case / 그 외 kebab-case)에 부합. 신규 공개 함수 `hub_root`(Python snake_case) · `hubRootFromPath`(JS camelCase, 기존 `code-scan.js` 파일 내 다른 함수 `findProjectRoot`/`resolveScope` 등과 동일 케이스 컨벤션) · `iter_task_dirs` · `resolve_task_dir`(Python snake_case) 모두 각 언어 관행과 §언어 규칙("코드/변수/필드명: English")에 부합, 위반 없음.

---

## 규정 부재로 판정 불가한 항목

- 커밋 Scope 관행(§Scope 예시와 실제 관행 불일치)의 "옳음" 여부 — `docs/CONVENTIONS.md`에 관행 변경을 승인하는 문구가 없어 확정 판정 불가(GC-C003에 TBD 표기).
- 변경이력 표의 삽입 위치 규칙(반드시 마지막 행 뒤에 추가해야 하는지) — `docs/CONVENTIONS.md` §변경이력 작성 의무에 "위치" 규정이 명시돼 있지 않음. 다만 v1.9.0(107) 행 스스로 106의 순서 오류를 지적한 선례가 있어 "정합"의 관행적 기준으로 삼아 Medium 판정.

---

## 반환 요약 (텍스트)

1. 보고서: `/Volumes/Data/AIStudio/workspace/ai-framework/tasks/109-260906-opds-태스크루트-해석-수렴/GC-CONVENTION-260907.md`
2. 등급별: Critical 0 / High 1 / Medium 1 / Low 1
3. Critical·High 전건: GC-C001만 해당(위 참조) — `docs/CONVENTIONS.md:269-270`, `opal-harness.md` §2.5(4) 원문 재서술, 이 태스크 기인.
4. ①~⑤ 판정: 위 "개별 항목 판정" 참조 — ①③⑤ 위반 없음(③은 CONVENTIONS.md 1건 예외), ②④는 각 1건 Medium/Low.
5. 규정 부재 판정 불가: 2건 (위 참조).
6. 막힌 것: 없음.
