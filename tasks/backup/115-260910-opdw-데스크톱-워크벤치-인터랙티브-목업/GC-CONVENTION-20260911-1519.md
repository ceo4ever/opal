# GC CONVENTION REPORT — 2026-09-11 15:19

## 1. 헤더

- 실행 일시: 시작 2026-09-11 15:19 / 완료 2026-09-11 15:19 / 소요 즉시
- 범위: `worktree(task_115)` / 대상 파일 7개 (`package-lock.json` 제외)
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` (허브, 단일 문서 모델 — 상세 링크 없음, 허브 전체 적용)
- APPLY 수행 여부: N (수동 대기 — 본 에이전트는 진단 전담)

검사 대상(워크트리 `/Volumes/Data/AIStudio/workspace/ai-framework/.opal-worktrees/task_115/` 기준):
- dashboard/frontend/src/workbench/types.ts
- dashboard/frontend/src/workbench/mock-adapter.ts
- dashboard/frontend/src/workbench/WorkbenchApp.tsx
- dashboard/frontend/src/workbench/WorkbenchApp.test.tsx
- dashboard/frontend/electron/main.cjs
- dashboard/frontend/vite.config.ts
- dashboard/frontend/package.json

`docs/CONVENTIONS.md`는 OPAL 프레임워크 자체의 스킬/에이전트/태스크 폴더 네이밍·브랜치·커밋 규칙이 주 내용이며, `dashboard/frontend/` 임의 코드에 대한 네이밍·들여쓰기·import 순서 규칙은 규정하지 않는다. 이 코드베이스에 실제로 적용 가능한 절은 **§구현 규칙 > @header 규칙**(코드 파일 상단 @header 기재 + "현재 사실만 기재, 이력은 git/DONE.md 소유")과 **§Citation Rules**, **§플랫폼 분기 격리**(OPAL 어댑터 계층 한정 — Electron `process.platform !== "darwin"` 표준 패턴은 본 절의 규율 대상 아님)뿐이다. 따라서 판정은 이 범위로 한정했다.

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 3 |
| 심각도 분포 | Critical 0 / High 0 / Medium 2 / Low 1 / Info 0 |
| 자동 수정 가능 | 0 |
| 수동 조치 필요 | 3 |
| 파일별 상위 Top | mock-adapter.ts (2건) / types.ts, WorkbenchApp.tsx, WorkbenchApp.test.tsx (각 1건, 빈도 트리거로 묶임) |
| 카테고리별 빈도 | 문서화(@header 정확성) 1파일 / 수기 이력성 서술 3파일 / 죽은 코드(미참조 export) 1파일 |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 1 (빈도 트리거 1건) |

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)
### High (0건)

### Medium (2건)

- [ ] GC-C001 [dashboard/frontend/src/workbench/mock-adapter.ts:7] `@header.exports` 배열이 실제 export 심볼과 불일치 — `findPaneForTab`, `collectLeaves` 누락
  - 카테고리: 문서화 (@header 정확성)
  - 위반 기준: 프로젝트(docs/CONVENTIONS.md §구현 규칙 > @header 규칙 — "코드 @header에는 현재 사실만 기재한다")
  - 설명: 선언된 `exports`는 `["MockWorkbenchAdapter", "WORKBENCH_STORAGE_KEY"]` 2개뿐이지만, 파일 말미(464행 `export function findPaneForTab`, 468행 `export function collectLeaves`)에 2개 함수가 추가로 export되어 있어 실제 사실과 어긋난다. `collectLeaves`는 `WorkbenchApp.tsx:35`에서 실제로 import되어 사용 중이므로 누락은 단순 누락이지 오탈자가 아니다.
  - 해결 방안: `@header.exports`에 `"findPaneForTab"`, `"collectLeaves"`를 추가해 실제 export 심볼과 정확히 일치시킨다.
  - 자동 수정: N
  - 참조: 프로젝트 docs/CONVENTIONS.md §구현 규칙 > @header 규칙

- [ ] GC-DP-C001 [빈도 트리거 — types.ts:6, WorkbenchApp.tsx:6, WorkbenchApp.test.tsx:6] `@header.description`에 폐기된 이전 설계 개념(Pinned 탭/Run/Activity/Verification/Result/@mention)에 대한 이력성 서술이 반복 등장
  - 카테고리: 문서화 (수기 누적 이력)
  - 위반 기준: 프로젝트(docs/CONVENTIONS.md §구현 규칙 > @header 규칙 — "코드 @header에는 현재 사실만 기재한다. 이력은 git 로그와 DONE.md가 갖는다")
  - 설명: 3개 파일의 description이 각각 "...는 C-10으로 제거됨", "...은 C-10으로 제거됨", "...폐기 테스트는 제거됨" 형태로 이전 라운드에 무엇이 삭제되었는지를 서술한다. 이는 현재 파일의 사실(무엇을 export/검증하는가)이 아니라 변경 이력이며, 동일 패턴이 3개 파일에서 반복되어 빈도 임계값(N=3)을 충족한다.
  - 해결 방안: description을 현재 계약만 서술하도록 축약한다(예: "...Surface(동적 탭+split)·Agent·Files/Changes 계약 (wireframe.md v3.0 §7)"). 폐기 사유·이전 개념 언급은 git 커밋 메시지 또는 tasks/115-.../DONE.md로 이관한다.
  - 자동 수정: N
  - 참조: 프로젝트 docs/CONVENTIONS.md §구현 규칙 > @header 규칙

### Low (1건)

- [ ] GC-C002 [dashboard/frontend/src/workbench/mock-adapter.ts:464] 미참조 export `findPaneForTab`
  - 카테고리: 죽은 코드
  - 위반 기준: base-convention-checklist (참조용 — docs/CONVENTIONS.md에 명시적 죽은 코드 정책 없음, getsentry/code-review 보조 참조 기준 적용)
  - 설명: `findPaneForTab`은 `export`되어 있으나 `src/workbench/` 어디에서도 import되지 않는다(`grep -rn "findPaneForTab" src/` 결과 정의 1건만 존재). `findPaneOfTab`(비공개, camelCase 유사명) 내부 헬퍼가 실제로 쓰이고 있어 혼동 가능성도 있다.
  - 해결 방안: 실제 소비처가 없다면 제거하거나, 향후 사용 예정이면 header exports에 등재하고 주석으로 용도를 명시한다. GC-C001과 함께 처리 시 export 목록 정합성도 같이 해결된다.
  - 자동 수정: N (외부 참조 가능성 확인 필요)
  - 참조: TBD — ESLint `no-unused-exports` 계열 규칙 링크

### Info (0건)

---

## 4. 문서 업데이트 제안 (빈도 트리거 발동)

- [ ] GC-DP-C001 [빈도 트리거] 이슈 "@header description 이력성 서술 잔존" (3개 파일: types.ts, WorkbenchApp.tsx, WorkbenchApp.test.tsx) → CONVENTIONS.md 기존 §구현 규칙 > @header 규칙 강화 제안
  - 근거: 단일 실행 내 3개 파일 — 빈도 임계값 N=3 충족
  - 제안 내용: "@header.description에는 '~로 제거됨/폐기됨/이전에는 ~였음' 등 변경 이력을 암시하는 서술을 금지한다. 이전 설계와의 차이는 git 커밋 메시지 또는 태스크 DONE.md에서만 기술한다."

---

## 5. 문서 작성 유도

- 해당 없음 — `docs/CONVENTIONS.md` 존재, 작성 유도 생략

---

## 참고 (컨벤션 체크 범위 밖 — 판정 아님)

- `npm run lint` 기존 오류 14건(`components/ui/*`, `hooks/use-mobile.tsx`, `pages/brain/BrainPage.tsx`, `pages/dashboard/DashboardPage.stats.test.tsx`)은 태스크 115 범위 밖이며 이번 위반으로 계상하지 않음.
- `electron/main.cjs`의 `process.platform !== "darwin"` 분기는 Electron 표준 종료 패턴이며, CONVENTIONS.md §플랫폼 분기 격리(Claude/Cursor/Gemini/Antigravity 어댑터 계층 한정)의 규율 대상이 아님 — 위반 아님.
- `mock-adapter.ts:125`의 mock diffPreview 문자열(`"- pinned tabs\n+ dynamic surfaces"`)은 UI에 표시되는 시뮬레이션 데이터이며 코드 주석/이력 서술이 아니므로 §@header 규칙 위반으로 계상하지 않음.
- `types.ts`의 `@header.exports` 배열은 실제 export된 18개 타입과 정확히 일치 — 위반 없음.
- `WorkbenchApp.test.tsx`(`exports: []`), `electron/main.cjs`(`exports: []`)는 실제로 export가 없어 일치 — 위반 없음.
