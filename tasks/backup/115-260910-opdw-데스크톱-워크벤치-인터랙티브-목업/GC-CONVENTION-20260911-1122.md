# GC-CONVENTION 보고서

## §1 헤더

- 실행 일시: 2026-09-11 11:22 (KST)
- 범위: 태스크 115 EXECUTE PM Gate — 컨벤션 자동 진단 (worktree 대상)
  - 문서 루트(허브): `/Volumes/Data/AIStudio/workspace/ai-framework/`
  - 코드 루트(검사 대상): `/Volumes/Data/AIStudio/workspace/ai-framework/.opal-worktrees/task_115/`
- 기준 문서 상태: `docs/CONVENTIONS.md` 단일 진입점 모델(허브+링크 미적용, OPAL 프레임워크 자체 규칙) — 전문 로드 완료. 프레임워크 내장 공통 컨벤션 기본값은 적용하지 않았다.
- 대상 파일(7건, main 대비 diff):
  - `dashboard/frontend/vite.config.ts`
  - `dashboard/frontend/package.json`
  - `dashboard/frontend/electron/main.cjs`
  - `dashboard/frontend/src/workbench/types.ts`
  - `dashboard/frontend/src/workbench/mock-adapter.ts`
  - `dashboard/frontend/src/workbench/WorkbenchApp.tsx`
  - `dashboard/frontend/src/workbench/WorkbenchApp.test.tsx`
  - (`package-lock.json`은 도구 생성물이라 제외)

## §2 요약 지표

| 심각도 | 건수 |
|---|---|
| Critical | 0 |
| High | 0 |
| Medium | 1 |
| Low | 0 |
| Info | 0 |

## §3 수정 대상

- [ ] **GC-C001** — `dashboard/frontend/src/workbench/types.ts:6` (헤더 `exports` 배열)
  - 카테고리: 문서화(@header 규칙)
  - 위반 기준: `docs/CONVENTIONS.md` §구현 규칙 > @header 규칙 — "코드 `@header`에는 현재 사실만 기재한다"
  - 설명: 이번 변경에서 `export interface PersistedUI { ... }`를 신규로 export했으나(파일 말미), 헤더 블록의 `exports` 배열은 `["WorkbenchState", "Task", "Run", "Environment", "AgentDefinition", "Binding", "ToolView", "TaskStatus", "RunStatus"]`로 갱신되어 `PersistedUI`가 누락됨. 헤더가 실제 export 목록과 불일치하는 "현재 사실 불일치" 상태.
  - 해결 방안: 헤더 `exports`에 `"PersistedUI"` 추가.
  - 자동 수정 가능: Y (문자열 배열 항목 추가)
  - 참조 URL: `opal/core/references/header-standard.md` §2.1, §7

이번 변경이 들여온 위반은 위 1건뿐이다. 그 외 신규/수정 대상 파일의 @header(mock-adapter.ts, WorkbenchApp.tsx, main.cjs)는 exports/depends/description이 실제 내용과 일치했다.

## §4 문서 업데이트 제안

- 트리거 발동 없음(빈도 트리거 N=3 미충족, 새 카테고리 트리거 없음, 심각도 트리거 없음 — Medium 1건은 기존 CONVENTIONS.md §구현 규칙 범위 내에서 판정 가능해 문서 갱신 불필요).

## §5 문서 작성 유도

- 해당 없음 (`docs/CONVENTIONS.md` 존재, 체크 정상 수행).

## 부가 확인 사항 (특히 확인 요청 항목)

- **네이밍 규칙**: 대상 파일명(`WorkbenchApp.tsx`, `mock-adapter.ts`, `types.ts` 등)은 이번 변경으로 신규 생성되지 않았고 기존 네이밍을 유지했다(선재 상태, 이번 diff 범위 아님). `docs/CONVENTIONS.md`의 kebab-case 규칙은 스킬/태스크/에이전트 폴더명에 명시적으로 적용되며, 프론트엔드 소스 파일(TS/TSX) 네이밍에 대한 별도 규정은 CONVENTIONS.md에 없어 판정 대상에서 제외(허브+링크 미적용, FE 상세 문서 부재).
- **Citation 규칙**: 이번 변경은 코드 파일(.ts/.tsx/.cjs/.json)이며 TASK.md/PLAN.md/ANALYSIS.md/QA 산출물이 아니므로 Citation Rules(§9 근거 등급) 적용 대상 아님. 코드 내 주석은 `wireframe.md §7`, `AC-7/AC-10/AC-11`, `C-5`, `C-6` 등 근거 태그를 이미 인용하고 있어 위반 없음.
- **수기 누적 이력 생성 금지**: `WorkbenchApp.test.tsx`, `mock-adapter.ts`, `types.ts`, `electron/main.cjs` 어디에도 "변경 이력" 절이나 누적 로그 주석이 추가되지 않았다. 위반 없음.
- **플랫폼 분기 하드코딩 금지**: `electron/main.cjs`의 `WORKBENCH_WINDOW_TITLE` 상수화, `page-title-updated` 이벤트 차단은 Electron 자체 API 사용이며 Claude/Cursor/Gemini 등 어댑터 대상 플랫폼 분기가 아니다. `AgentSheet` 컴포넌트의 `Runtime` 선택지(`Codex ACP` / `Claude ACP`)는 목업 도메인 데이터(사용자가 선택하는 런타임 옵션)이며 어댑터 계층의 플랫폼 조건문이 아니므로 위반 아님. 위반 없음.

## 선재 위반(참고, 이번 변경 계상 제외)

- `npm run lint` 기존 오류 14건(`components/ui/*`, `hooks/use-mobile.tsx`, `pages/brain/BrainPage.tsx`, `pages/dashboard/DashboardPage.stats.test.tsx`)은 태스크 115 범위 밖이며 이번 컨벤션 진단에서도 별도 계상하지 않았다(PM 사전 안내 반영).

## 판정

**status: PASS** — Critical/High 0건. Medium 1건(GC-C001)은 자동 수정 가능한 경미한 문서화 불일치이며 병합 차단 사유 아님.
