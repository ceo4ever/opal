# QA: PLAN — opal-pm.md 다운사이징 — pm/ 폴더 분리 최적화

> 검토일: 2026-04-17 | 판정: Pass

## 1. 요약

`opal-pm.md`(559줄)의 5개 Lazy 분리 대상 섹션(§5.2, §6, §9, §10, §11)을 `opal/core/references/pm/` 폴더 내 개별 파일로 추출하고, 원본을 stub으로 대체하는 계획이다. PLAN은 §1(현황 조사), §2(구현 계획 + 핵심 설계 + stub 형식), §3(7 Step 실행 체크리스트), §4(QA 체크리스트)로 구성되며, 실측 라인 수 기반의 구체적 설계가 포함되어 있다. TASK 목표 "~220줄 이하" 달성 불가 이슈(§3 Eager 유지 시 ~290줄이 하한선)를 문서화하고 소유자 판단을 EXECUTE 진입 전에 요청하도록 명시했다. 7개 AC 전체가 PLAN §2·§3에 매핑되며, 제약 조건(배포본 수정 금지, YAML frontmatter 없음, 변경이력 없음)도 핵심 설계에 반영되었다.

---

## 2. 검증 결과

### 범용 PLAN 검증 (GP)

| # | 검증 항목 | 결과 | 비고 |
|---|----------|------|------|
| GP-1 | 즉시 실행 가능성 | Pass | §3 Step 1~7의 파일 경로·작업 내용·완료 기준·테스트·의존성이 모두 명세됨. 라인 수 목표 블로커는 EXECUTE 전 소유자 확인 프로세스로 처리. |
| GP-2 | 의존성 순서 | Pass | Step 1~5 병렬 → Step 6 순차(1~5 완료 후) → Step 7 순차(6 완료 후). Phase 그룹핑 명시. |
| GP-3 | TASK 반영 | Pass | TASK.md §요구사항 7개 AC가 PLAN §2(N-1~N-5, M-1) + §3(Step 1~6) + §4(QA 체크리스트)에 각각 매핑됨. |
| GP-4 | 파일 목록 완전성 | Pass | 신규 5개(pm/*.md) + 수정 1개(opal-pm.md) + 배포본 1개(금지 명시) 포함. |
| GP-5 | 설계 구체성 | Pass | 추출 파일별 파일 헤더 형식, 이관 라인 범위, 내부 참조 치환 방법, stub 본문 예시까지 상세 명세됨. |
| GP-6 | 체크리스트 커버리지 | Pass | 7개 Step = 5개 신규 파일 + 1개 슬림화 + 1개 자체 검증. §4 QA 체크리스트 18항목이 모든 요구사항을 분해함. |

### 태스크 QA 체크리스트 선행 검증 (PLAN 기반 판단)

#### 기능 테스트 (8항목)

| # | 검증 항목 | 결과 | 비고 |
|---|----------|------|------|
| F-1 | `pm/specialist-agent.md` — §11 원본 6개 섹션 커버 | Pass | PLAN §2 N-1 및 §3 Step 1에 6개 섹션(개념/구조/생성시점/갱신트리거/탐색경로/분배규칙) 명시됨. |
| F-2 | `pm/orchestration.md` — §10 원본 3개 섹션 커버 | Pass | PLAN §2 N-2 및 §3 Step 2에 3개 섹션(인터페이스 계약/Batch 핸드오프/충돌 해소) 명시됨. |
| F-3 | `pm/code-scan-management.md` — §9 원본 3개 섹션 + 최소 JSON 구조 | Pass | PLAN §2 N-3 및 §3 Step 3에 3개 섹션과 JSON 코드블록 유지 요건 명시됨. |
| F-4 | `pm/self-improvement.md` — §5.2 세부 3개 섹션 포함, 트리거 테이블 미포함 | Pass | PLAN §2 N-4에 "[MUST] 트리거 테이블(:252-260)은 원본 opal-pm.md에 유지하고 이 파일에는 재수록하지 않는다"로 명시됨. |
| F-5 | `pm/context-injection.md` — §6 원본 7개 요소 + 감지 조건 테이블 포함 | Pass | PLAN §2 N-5 및 §3 Step 5에 7개 요소 목록과 테이블 포함 요건 명시됨. |
| F-6 | `opal-pm.md` 슬림화 — §5/§6/§8/§9~§11/변경이력 처리 상태 | Pass | PLAN §2 M-1 및 §3 Step 6의 작업 내용 8개 항목에 세부 처리 방안 명시됨. |
| F-7 | `opal-pm.md` stub — 5개 경로 + Lazy 트리거 조건 명시 | Pass | §2 M-1 stub 형식에 각 stub의 `opal/core/references/pm/{파일명}` 경로와 Lazy 트리거 조건 예시가 모두 포함됨. |
| F-8 | 슬림화 후 라인 수 목표 이하 여부 | Warning | PLAN §2 예상 라인 수 ~291줄로 TASK 목표 "~220줄 이하" 초과 예측. 단, PLAN이 이를 명시적 블로커 후보로 문서화하고, EXECUTE 진입 전 소유자에게 (A)~(C) 3가지 옵션 확인 요청 프로세스를 정의함. 목표 재조정 또는 §3 추가 분리 여부 소유자 확인 필요. |

#### 일관성 테스트 (5항목)

| # | 검증 항목 | 결과 | 비고 |
|---|----------|------|------|
| C-1 | 5개 추출 파일 헤더 형식(출처/Lazy 트리거/탐색 경로) 일치 | Pass | §2 공통 규칙에 `# {타이틀} > 출처 > Lazy 트리거 > 탐색 경로` blockquote 3줄 형식이 통일 정의됨. |
| C-2 | 내부 참조 풀 경로화 | Pass | N-1("§5" → "`opal-pm.md` §5"), N-3("§4 검토 절차 8번" → "`opal-pm.md` §4 검토 절차 8번") 치환 규칙 명시됨. |
| C-3 | `opal-pm.md` §1~§4, §7 본문 원본과 동일 | Pass | §2 M-1 완료 기준에 "§1~§4, §7 본문은 원본 그대로(수정 없음)"로 명시됨. TASK 제약 "§1~§4, §7은 건드리지 않음"과 정합. |
| C-4 | 섹션 번호 §1→§7→§9→§10→§11 순(§8만 건너뜀) | Pass | §2 M-1 "§8 섹션 제거 후 §9·§10·§11 번호는 유지" 및 §5 리스크 "섹션 번호 갱신 누락 위험 → 유지 결정"으로 명시됨. |
| C-5 | `~/.opal/references/opal-pm.md`(배포본) 미수정 | Pass | §1 관련 파일 테이블에 배포본 "❌ 금지(D-5)"로 명시. §2 공통 규칙 [MUST] 및 §5 리스크에도 반복 경고됨. |

#### 문서 품질 (5항목)

| # | 검증 항목 | 결과 | 비고 |
|---|----------|------|------|
| Q-1 | 한국어 본문 + 영어 코드/필드명 규칙 준수 | Pass | PLAN 본문 전체가 한국어로 작성되고, 파일 경로·코드블록·AC 등은 영어로 처리됨. |
| Q-2 | kebab-case 파일명 | Pass | specialist-agent.md, code-scan-management.md, self-improvement.md, context-injection.md, orchestration.md 모두 kebab-case로 정의됨. |
| Q-3 | YAML frontmatter 미포함 (추출 파일 5개) | Pass | §2 공통 규칙 [MUST] "YAML frontmatter 없음"으로 명시. §5 리스크 "YAML frontmatter 실수 삽입" 방지책(blockquote 3줄 형식만 사용, `---` 미사용)까지 기술됨. |
| Q-4 | 변경이력 섹션 미포함 (추출 파일 5개) | Pass | §2 공통 규칙 [MUST] "변경이력 섹션 없음"으로 명시됨. TASK 제약과 정합. |
| Q-5 | 이관 시 내용 손실 없음 | Pass | PLAN §2 각 N-{n}에 이관 대상 라인 범위(`opal-pm.md:NNN-MMM`)가 모두 명시되어 추적 가능. 자체 검증(Step 7)에서 7개 AC 순회 + 라인 수 통계 집계 수행 예정. |

---

## 3. 지적 사항

### Warning

**F-8: 슬림화 후 라인 수 목표(~220줄) 달성 불가 예측**

- **심각도**: Warning (PLAN 단계의 문제가 아니라 TASK 목표 설정 문제이며, PLAN이 이를 정확히 문서화하고 소유자 판단을 요청하고 있음)
- **내용**: TASK.md §작업 목표에 "~220줄로 줄인다"고 명시되어 있으나, PLAN §2 예상 라인 수 분석에 따르면 §3(125줄)을 Eager 유지하는 현 설계에서 슬림화 하한선은 ~291줄이다.
- **PLAN의 대응**: EXECUTE 진입 전 소유자에게 (A) §3 유지/~290줄, (B) §3 Step 0·6·7 추가 분리/~240~260줄, (C) 목표 완화/300줄 조정 중 선택을 요청하고, 결과를 STATE.md §의사결정 로그에 기록하도록 Step 6 완료 기준과 §5 리스크에 명시됨.
- **QA 권고**: EXECUTE 착수 전 소유자 확인을 완료하고, 선택 결과를 STATE.md에 기록할 것.

### 지적 사항 없음 (Info)

- **I-1**: N-4(`pm/self-improvement.md`) 설계에서 "상단에 '트리거 테이블은 `opal-pm.md` §5.2에 유지된다'는 1줄 안내 추가"를 명시했으나, 이것이 어떤 형식(blockquote vs 일반 문장)으로 작성될지는 PLAN에서 고정하지 않음. EXECUTE 시 일관성 유지에 주의 필요.

---

### 심각도 분류

- Critical: 없음
- Warning: 1개 (F-8 — 라인 수 목표 달성 불가, 소유자 확인 필요)
- Info: 1개 (I-1 — N-4 안내 문장 형식 미고정)

---

## 4. 교차 참조 검증

| 참조 산출물 | 검증 내용 | 결과 |
|------------|----------|------|
| TASK.md §요구사항 7개 AC | PLAN §2(N-1~N-5, M-1) + §3(Step 1~6) + §4(QA 체크리스트 F-1~F-8)에 모두 매핑됨 | Pass |
| TASK.md §제약 조건 | PLAN §2 공통 규칙 [MUST] + §1 관련 파일 테이블 + §5 리스크에 3개 제약(배포 금지/YAML frontmatter 없음/변경이력 없음) 모두 반영됨 | Pass |
| TASK.md §배경 분석 vs §확정된 설계 방향 해석 불일치 | PLAN §1 "불일치 기록"에서 §3 분리가 "배경 분석"에만 있고 "확정된 설계 방향"의 AC에 없음을 명시 → §3는 이번 태스크 범위 외로 명확히 확정. 후속 태스크 분기 안내도 포함. | Pass |
| TASK.md §기술 스택 | Markdown 파일 편집 — 모든 작업이 Markdown 편집 범위 내이며, 코드 구현 없음 | Pass |
| TASK.md §관련 문서 D-1, D-2 | PLAN §1에 D-1(opal-pm.md 실측 라인 범위) + D-2(pm/ 폴더) 포함. PLAN은 추가로 D-3~D-7을 확장 참조함. | Pass |

---

## 5. 판정

**Pass**

Critical 없음, Warning 1개(라인 수 목표 달성 불가). 해당 Warning은 TASK 목표 설정 문제로, PLAN 자체는 이를 정확히 분석하고 소유자 판단 프로세스를 명시하였다. PLAN만으로 즉시 EXECUTE에 착수할 수 있으며, Warning 해소는 EXECUTE 진입 전 소유자 확인(선택지 A/B/C)으로 처리할 수 있다.
