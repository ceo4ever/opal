# GC CONVENTION REPORT (재진단) — 2026-09-14 10:26 KST

<!--
  체크박스 5단계 상태 기호 (APPLY 단계가 기입):
  [ ]  open    — 미처리 (신규)
  [x]  done    — 적용 완료  ← 주석: 적용 시각 YYYY-MM-DD HH:mm + 수정 요약
  [~]  pending — 보류       ← 주석: 보류 사유
  [?]  review  — 확인 필요  ← 주석: 판단 근거 / 해결 방안
  [!]  failed  — 실패       ← 주석: 실패 사유 / 권장 대안
-->

## 1. 헤더

- 실행 일시: 시작 2026-09-14 10:20 / 완료 2026-09-14 10:26 KST
- 성격: **부분 재진단** — 1차(`GC-CONVENTION-202609140754.md`)의 High 1건 시정분 검증. 1차 PASS 항목(중점 1·3·4·5·6·7)은 디스패처 지시에 따라 재검사하지 않았다
- 기준 리비전: 커밋 `d2db762` + PM 시정분(미커밋 8파일)
- baseline: `tasks/131-260914-opd-루프파일럿-실행안정화/gc-findings-convention-202609140754.json`
- 에이전트: opal-convention-checker (스킬 `~/.opal/skills/op-gc-convention/SKILL.md`)
- 기준 문서: `docs/CONVENTIONS.md`, **`opal/core/references/tools.md` §공통 도구 출력 계약(신규)**, `opal/core/references/opal-doc-standard.md` §5, `opal/core/references/harness/citation-rules.md`
- APPLY 수행 여부: N (read-only 유지 — 어떤 소스·문서도 수정하지 않았다)

---

## 2. 요약 지표

| 지표 | 1차 | 2차 | 증감 |
|------|-----|-----|------|
| Critical | 0 | 0 | — |
| **High** | **1** | **0** | **-1 (해소)** |
| Medium | 3 | 4 | +1 (신규 1, 잔존 3) |
| Low | 1 | 2 | +1 (신규 1, 잔존 1) |
| pre-existing | 4 | 4 | — |
| 합계 | 9 | 10 | +1 |

**최종 판정: `PASS_WITH_ADVISORIES`** — `disposition: blocking` finding 0건.
**PM Gate 통과 조건(Critical/High 0건) 충족.**

> **disposition 축 정정 고지**: 1차 JSON은 Medium 3건(GC-C002·C003·C004)의 `disposition`을 `blocking`으로 두었다. 디스패처가 명시한 게이트 규칙은 "Critical/High 0건이 통과 조건, Medium 이하는 보고하되 차단하지 않는다"이므로, 2차에서는 Medium 이하 전건의 `disposition`을 `advisory`로 정렬했다. `severity`는 1차와 동일하며 변경하지 않았다. 이 축 변경을 숨기지 않고 기록한다.

---

## 3. 시정 검증

### 3.1 GC-C001 (High) — **해소 확인 ✅**

| 검증 항목 | 결과 |
|---|---|
| 거짓 인용 잔존 | **0건** — `grep -rn "CONVENTIONS.md.*도구 출력 계약\|CONVENTIONS 도구 출력 계약" opal/ docs/ scripts/` → 무출력 |
| 재지정 대상 절 실재 | **실재** — `opal/core/references/tools.md:8` `## 공통 도구 출력 계약` |
| 재지정 6개소 전수 | **6/6 완료** (아래 표) |
| 인용 형식 적합성 | **적합** — `docs/CONVENTIONS.md:228-234` §Citation Rules의 `docs/문서명 §섹션` 형식 충족 |

재지정 확인 (6개소 전수):

| # | 위치 | 재지정 후 인용 |
|---|------|---------------|
| 1 | `opal/tools/oppl-runtime-tool/README.md:14` | `` `opal/core/references/tools.md` §공통 도구 출력 계약 `` |
| 2 | `opal/tools/oppl-runtime-tool/oppl_runtime_tool.py:7` (@header description) | `opal/core/references/tools.md §공통 도구 출력 계약` |
| 3 | `opal/tools/oppl-runtime-tool/tests/test_oppl_runtime_config.py:19` | `opal/core/references/tools.md §공통 도구 출력 계약` |
| 4 | `opal/tools/oppl-runtime-tool/tests/test_oppl_runtime_admission.py:28` | `opal/core/references/tools.md §공통 도구 출력 계약` |
| 5 | `opal/tools/state-tool/tests/test_run_identity.py:16` | `[MUST] opal/core/references/tools.md §공통 도구 출력 계약` |
| 6 | `opal/tools/state-tool/tests/test_run_identity.py:85` | `tools.md §공통 도구 출력 계약` (약칭) |

> #6은 경로 없는 약칭이나, **같은 파일 `:16`이 전체 경로를 선행 정의**하므로 1차 GC-C002에 내가 제시한 해소 기준("최소 1개소에서 전체 경로를 1회 명시하고 이하 약칭")을 그대로 만족한다. 신규 finding으로 올리지 않는다.

신규 절 내용의 사실 정합성도 대조했다 — 성공 예시 `{"ok": true, "command": "mark", "row_id": 5, "stage": "PLAN"}`(`tools.md:17`)는 `state_tool.py` `cmd_mark`의 실제 `ok(command, row_id=…, stage=…, item=…)` 호출과 일치하고 기존 `tools.md:159` 예시와도 정합한다. 실패 예시의 `run_identity_missing`(`tools.md:18`)도 `oppl_runtime_tool.py:166`에서 실제로 방출된다. **예시 조작 없음.**

### 3.2 GC-C004 (Medium) — **해소 확인 ✅**

- `docs/CONVENTIONS.md:249` — `21종` → **`22종`**, 열거 끝에 `oppl-runtime-tool` 추가
- 실측 대조: `ls -d opal/tools/*/ | wc -l` = **22**
- **집합 일치 전수 확인**: 열거 22개와 디스크 22개를 집합 비교 → `목록에만 있고 디스크에 없음: []`, `디스크에만 있고 목록에 없음: []` — **완전 일치**
- 추가 시정(디스패처 보고분): `docs/CONVENTIONS.md:250`에 규범 소유처 포인터 1행 추가 — "도구 응답 형식·오류 코드 출처·종료 코드 성패 구분의 공통 규범은 `opal/core/references/tools.md` §공통 도구 출력 계약이 소유한다". 인용 대상 실재 확인 완료
- `opal/core/references/tools.md:1188`에 `## oppl-runtime-tool` 절 등록 확인

---

## 4. 시정이 만든 신규 finding (2건)

### Medium (신규 1건)

- [ ] GC-R002 [`opal/core/references/tools.md:26`] 신규 §오류 코드 규칙을 이번 태스크의 신규 도구가 충족하지 않음 (동시에 규칙이 기존 fleet 대다수와 충돌)
  - fingerprint: `abcb980430d88a89`
  - 카테고리: 도구 계약
  - 위반 기준: **신규** `opal/core/references/tools.md:26` §공통 도구 출력 계약 §오류 코드 — "실패 응답의 `error` 값은 자유 문자열이 아니라 **도구가 모듈에 선언한 코드 목록의 키**다. … 코드 목록은 각 도구가 자기 SSOT로 소유하며, 코드별 의미는 그 도구의 README가 **표로** 소유한다" (source_tier `T0`)
  - 설명(evidence) — 두 축이다:
    1. **in-scope 미충족**: `oppl_runtime_tool.py:74` `fail(command, error, message="", …)`는 `error`를 **자유 문자열로 그대로 수신해 방출**하며 어떤 선언 목록과도 대조하지 않는다. `ledger.py`가 선언한 상수는 `REGISTERED_PHASES:34`·`REQUIRED_CONFIG_KEYS:37`·`PHASE_STATUSES:58`·`FAILURE_STATUSES:61` 등이며 **오류 코드 목록은 없다**. 대조군: `backlog_tool.py:44` `ERROR_CODES` + `err():74-80`, `state_tool.py:136` `ERROR_CODES` + `err():272-278`(주석에 "code는 ERROR_CODES 키 중 하나여야 한다" 명시). 또한 README 요건("코드별 의미는 README가 **표로** 소유")도 미충족 — `oppl-runtime-tool/README.md:102`는 거부 코드 8종을 **산문 나열**로 두고, 비-거부 오류(`run_identity_missing`·`config_invalid` 등)는 카탈로그가 없다.
    2. **규칙 대 fleet 충돌**: 22개 도구 중 오류 코드 카탈로그를 선언한 도구는 **8개**(backlog-tool·brain-tool·git-sync-tool·memory-tool·state-tool·test-tool·tool-scan·worktree-tool)뿐이고 **14개가 미선언**(cmux-tool·code-scan·date·doctor·event-loader·improve-tool·opal-action-monitor·opal-agent·opal-cli·oppl-runtime-tool·playwright-tool·self-pm-tool·skill-registry·xlsx-tool)이다. 즉 신설 T0 규칙이 작성 시점에 자기 적용 대상 다수와 어긋난다.
  - impact: (1) 호출자가 코드로 분기하는데 방출 측에 폐쇄 집합 검증이 없어 오타·표류가 무증상으로 배포된다(대조군 두 도구는 코드 집합 완전성 회귀 테스트로 고정하고 있다). (2) 신설 규범이 fleet 실태보다 엄격해, 다음 컨벤션 검사부터 14개 도구가 일제히 위반으로 잡힐 수 있다.
  - 해결 방안:
    - in-scope: `oppl_runtime_tool.py`에 오류 코드 상수(거부 코드 8종 + `run_identity_missing`·`config_invalid` 등)를 선언하고 `fail()`이 그 키만 받도록 좁힌다. `README.md:97-102`의 산문 나열을 코드/의미 표로 승격한다.
    - 규칙 축: `tools.md:26`을 현 fleet에 맞춰 완화(권고)하거나, 유지하되 미선언 14개 도구의 전환 계획을 별도 태스크로 분리한다. **어느 쪽을 택할지는 소유자 결정 사항이며, 스킬 [MUST] 4(충돌은 판정하지 않고 보고한다)에 따라 이 보고서는 판정하지 않는다.**
  - 자동 수정: N
  - verification: `oppl_runtime_tool.py`가 오류 코드 상수를 선언하고 `fail()`이 미등록 코드를 거부한다; 또는 `tools.md:26`의 요구 수준이 변경된다.
  - 참조: `opal/core/references/tools.md` §공통 도구 출력 계약

### Low (신규 1건)

- [ ] GC-R001 [`opal/core/references/tools.md:1190-1195`] 신규 `## oppl-runtime-tool` 절이 문서 내 12/12 기존 절의 Markdown hard break 관례를 따르지 않음
  - fingerprint: `3b92aaf2b07c5507`
  - 카테고리: 들여쓰기·포맷 / 문서화
  - 위반 기준: 인접 문서에서 관측한 패턴 — `opal/core/references/tools.md`의 기존 12개 도구 절 **전건**이 `**용도**:`·`**실행 경로**:`·`**소스 경로**:`·`**의존성**:` bold 라벨 행을 **행말 2칸(Markdown hard break)** 으로 종료한다 (source_tier `T0` — 규칙 원문은 CONVENTIONS에 없고 문서 자체의 12/12 일관 관례다)
  - 설명(evidence): 실측 — `xlsx-tool`·`state-tool`·`code-scan`·`test-tool`·`brain-tool`·`tool-scan`·`memory-tool`·`improve-tool`·`opal-action-monitor` 각 hardbreak 3/4행, `cmux-tool`·`git-sync-tool`·`worktree-tool` 각 4/4행. 신규 `oppl-runtime-tool` 절은 라벨 6행 중 **hardbreak 0행**(`tools.md:1190`~`:1195` 전건 `no-break`).
  - impact: 렌더 시 6개 라벨 행이 하나의 run-on 문단으로 붙어, 같은 문서의 다른 12개 절과 시각적으로 어긋난다. 레지스트리를 훑어 읽는 소비자(사람·에이전트)의 스캔 가능성이 이 절에서만 떨어진다.
  - 해결 방안: `tools.md:1190`~`:1194` 5개 행 끝에 공백 2칸을 넣는다(마지막 라벨 행 `:1195`는 뒤가 빈 줄이므로 불필요 — 기존 절의 3/4 패턴과 동일).
  - 자동 수정: Y
  - verification: `awk 'NR>=1190 && NR<=1194 && !/  $/' opal/core/references/tools.md`가 무출력이다.
  - 참조: `opal/core/references/tools.md` 기존 12개 도구 절

### 신규 위반 없음이 확인된 축

| 검사 축 | 결과 |
|---|---|
| 수기 누적 이력 신설 | **없음** — `tools.md`의 `## 변경이력`은 선재 표이며, 이번 시정은 **행도 추가하지 않았다**(`git diff`에 변경이력 영역 변동 0). `opal-doc-standard.md` §5 관점에서 신설 0건 |
| 신규 절 문체·구조 | **적합** — `§공통 도구 출력 계약`은 `## → ###` 위계, 한국어 본문, 코드펜스 `json` 사용, 절 끝 `---` 구분자까지 문서 관례를 따른다. `§oppl-runtime-tool`의 bold 라벨 세트(용도/실행 경로/소스 경로/의존성)와 인용부 주석(`> 축 경계:`)도 `opal-action-monitor` 절 구조와 동형 (hard break 누락만 GC-R001) |
| 신규 절의 인용 실재성 | **적합** — `tools.md` 신규 2개 절이 인용하는 경로(`opal/tools/oppl-runtime-tool/README.md`, `~/.opal/tools/oppl-runtime-tool/run.sh`, `opal/tools/oppl-runtime-tool/`, `backlog.json`·`state.json`·`test-scenario.json`) 전건 실재. `docs/CONVENTIONS.md:250` 신규 포인터의 대상 절도 실재 |
| 시정 4파일 + 테스트 2파일의 기타 위반 | **없음** — 변경분은 전부 1행 치환(README·@header·테스트 4건) 또는 절 추가(tools.md·CONVENTIONS.md)이며, trailing whitespace·탭·EOF 개행 이상 0건. `oppl_runtime_tool.py:7` @header는 `description` 문자열 1개만 바뀌어 `exports`·`module`·`layer`·`domain` 불변 |
| 시정 범위 이탈 | **없음** — `git diff --stat` 상 시정분은 지시된 6파일 + 태스크 폴더 산출물뿐. 소스 로직 변경 0행 |

---

## 5. 1차 Medium/Low 잔존 확인 (시정 대상 아님 — 건수 변동 없음)

| ID | severity | 상태 | 근거 |
|---|---|---|---|
| GC-C002 | Medium | **잔존** | 문서명 없는 `제안서 §N` 인용 — `grep -rn "제안서 §" opal/tools/` = **48건**(1차 보고의 "약 30개소"는 대표 위치 열거 기준, 실측 총계는 48건이며 시정 전후 변동 없음). `oppl_runtime_tool.py:7` @header에도 `제안서 §6.2`·`제안서 §6.4` 그대로 잔존 |
| GC-C003 | Medium | **잔존** | `docs/proposals/opal-oppl-runtime-stabilization.md:3` 상태 여전히 `검토`. 폐쇄 집합의 규범 귀속이 제안서 절에 남아 있음. PM의 +10행 추가(§6.2 앞 호출 주체 문단)는 상태·귀속을 바꾸지 않았다 |
| GC-C004 | Medium | **해소** | §3.2 참조 |
| GC-C005 | Low | **잔존** | `ledger.py:129` `# state_tool.py:403 선례를 그대로 따른다` 불변. 실제 선례는 `state_tool.py:404` |
| GC-P01~P04 | pre-existing | **잔존** | 시정 대상 아님. 1차 보고서 §3-B 참조 |

> GC-C002의 실측 총계를 1차의 "약 30개소"에서 48건으로 정정한다. 1차는 대표 위치만 열거했고 총계를 추정치로 적었다 — 2차에서 기계 집계로 확정했다. 위반 자체가 늘어난 것이 아니다.

---

## 6. 최종 판정과 게이트

| 축 | 값 |
|---|---|
| `check_status` | `pass` (재검사 범위 전건 검사 완료, `missing_capabilities` 0) |
| schema §6 판정 | `PASS_WITH_ADVISORIES` — `disposition: blocking` 0건, advisory 6건 |
| **PM Gate (Critical/High 0건)** | **충족 — 통과** |
| baseline delta | `resolved` 2(GC-C001·GC-C004) / `persisting` 8 / `new` 2(GC-R001·GC-R002) |

권고 — 게이트는 통과하나 아래 2건은 CLOSE 전에 정리 판단이 필요하다:
1. **GC-C003**이 미해소인 한 `docs/proposals/opal-oppl-runtime-stabilization.md`는 `docs/proposals/archives/`로 이동할 수 없다(`docs/CONVENTIONS.md:142` "[MUST] 아카이브 이동의 선행 조건은 잔여 인용 0건"). CLOSE 단계의 제안서 판정이 이 지점에서 막힌다.
2. **GC-R002**의 규칙 축(신설 T0 규칙 vs 미선언 14개 도구)은 소유자 결정 전까지 다음 컨벤션 검사마다 재발한다.

---

## 7. 검사 실행 근거 (evidence)

- `grep -rn "CONVENTIONS.md.*도구 출력 계약\|CONVENTIONS 도구 출력 계약" opal/ docs/ scripts/` → 0건
- `grep -rn "도구 출력 계약"` 전수 → 재지정 6개소 + `tools.md:8`(절 정의) + `docs/CONVENTIONS.md:250`(포인터) + `tasks/131-*/PLAN.md:50`(태스크 산출물, 검사 범위 외)
- `grep -n "^## " opal/core/references/tools.md` → `:8 공통 도구 출력 계약`, `:1188 oppl-runtime-tool` 실재 확인
- 도구 인벤토리 집합 대조 — `docs/CONVENTIONS.md:249` 열거 22개 vs `os.listdir('opal/tools')` 22개 → 양방향 차집합 공집합
- hard break 실측 — `awk` 로 13개 도구 절의 bold 라벨 행 행말 2칸 유무 집계 (기존 12절 3~4행 / 신규 절 0행)
- 오류 코드 카탈로그 실측 — `find opal/tools/*/ -maxdepth 1 -name '*.py' -o -name '*.js'` + `grep -l "ERROR_CODES|ERROR_CATALOG|ERRORS = {"` → 8/22 보유
- `git diff --stat` → 시정분 8파일(태스크 폴더 3건 포함), 소스 로직 변경 0행
- `git status --porcelain` → 이 에이전트가 만든 파일은 GC 보고서·finding JSON 4건뿐(1차 2 + 2차 2)
- `node ~/.opal/tools/date/date.js datetime` → 2026-09-14 10:26 (KST)

## 8. 재검사 제외 범위 (디스패처 지시)

중점 항목 1(@header) · 3(수치 비복제) · 4(수기 누적 이력) · 5(배포 경계) · 6(네이밍) · 7(테스트 명명·배치)는 1차에서 PASS했고 디스패처가 재검사 제외를 지시했다. 단, 시정분이 이 축들을 건드리는지는 확인했다 — `@header`는 `description` 문자열 1개만 바뀌었고(구조·`exports` 불변), 나머지 5축에 닿는 변경은 없다.
