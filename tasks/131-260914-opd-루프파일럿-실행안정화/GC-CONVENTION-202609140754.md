# GC CONVENTION REPORT — 2026-09-14 07:54 KST

<!--
  체크박스 5단계 상태 기호 (APPLY 단계가 기입):
  [ ]  open    — 미처리 (신규)
  [x]  done    — 적용 완료  ← 주석: 적용 시각 YYYY-MM-DD HH:mm + 수정 요약
  [~]  pending — 보류       ← 주석: 보류 사유
  [?]  review  — 확인 필요  ← 주석: 판단 근거 / 해결 방안
  [!]  failed  — 실패       ← 주석: 실패 사유 / 권장 대안
-->

## 1. 헤더

- 실행 일시: 시작 2026-09-14 07:48 / 완료 2026-09-14 07:54 KST
- 범위: TEST 단계 PM Gate 컨벤션 자동 진단 / `scope: all` / 대상 파일 26개 (코드 9 · 테스트 8 · 문서 9)
- 기준 리비전: `d2db762` (작업본 클린 — `git status` 상 태스크 폴더 산출물 3건 외 변경 없음)
- 에이전트: opal-convention-checker (스킬 `~/.opal/skills/op-gc-convention/SKILL.md`)
- 기준 문서: `docs/CONVENTIONS.md` (존재 — T0), `opal/core/references/opal-doc-standard.md` §5, `opal/core/references/header-standard.md`, `opal/core/references/harness/header-rules.md`, `opal/core/references/harness/citation-rules.md`
- 실행 설정: `.opal/code-scan.json` (`headerSource: inline`, scope `framework`)
- APPLY 수행 여부: N (read-only 진단 전담 — 어떤 소스 파일도 수정하지 않았다)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 9 (이번 변경 5 / pre-existing 4) |
| 심각도 분포 | Critical 0 / High 1 / Medium 3 / Low 1 / Info 0 (+ pre-existing 4) |
| 자동 수정 가능 | 2 |
| 수동 조치 필요 | 7 |
| 파일별 상위 Top 5 | `oppl-runtime-tool/README.md` (3) / `oppl_runtime_tool.py` (2) / `ledger.py` (2) / `state-tool/tests/test_run_identity.py` (1) / `opal-action-monitor/README.md` (1) |
| 카테고리별 빈도 | 인용(citation) 6 / 제안서 생명주기 1 / 문서 레지스트리 1 / 문서 이력 1 |
| Critical/High 수 | **1** |
| 문서 업데이트 제안 수 | 2 (빈도 트리거 1 + 새 카테고리 0 + SSOT 누락 1) |

**최종 판정: `FAIL`** — `disposition: blocking` finding 1건(GC-C001). PM Gate 통과 조건(Critical/High 0건) 미충족.
`missing_capabilities` 0건, 전 대상 파일 검사 완료 → `check_status: pass`.

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

없음.

### High (1건)

- [ ] GC-C001 [`opal/tools/oppl-runtime-tool/README.md:14`] 존재하지 않는 절을 규범 SSOT로 인용 — `docs/CONVENTIONS.md §도구 출력 계약`
  - fingerprint: `3a10afe03f4fca69`
  - 카테고리: 문서화 / 인용(citation)
  - 위반 기준: 프로젝트 `docs/CONVENTIONS.md:228-234` §Citation Rules — "모든 주장은 근거를 인용한다 (`{경로}:{라인}` 또는 `docs/문서명 §섹션`)", `[MUST]` 토큰 항목은 인용 누락 시 산출물 부적합 (source_tier `T0`)
  - 발생 위치 전수 (6개소, 전부 `d2db762`가 신규 도입):
    1. `opal/tools/oppl-runtime-tool/README.md:14` — "모든 응답은 **단일 라인 JSON + exit code**(`docs/CONVENTIONS.md` §도구 출력 계약)"
    2. `opal/tools/oppl-runtime-tool/oppl_runtime_tool.py:7` — @header `description` 내 "출력은 단일 라인 JSON + exit code만이며(docs/CONVENTIONS.md §도구 출력 계약)"
    3. `opal/tools/oppl-runtime-tool/tests/test_oppl_runtime_config.py:19`
    4. `opal/tools/oppl-runtime-tool/tests/test_oppl_runtime_admission.py:28`
    5. `opal/tools/state-tool/tests/test_run_identity.py:16` — "[MUST] CONVENTIONS 도구 출력 계약"
    6. `opal/tools/state-tool/tests/test_run_identity.py:85`
  - 설명(evidence): `docs/CONVENTIONS.md`의 절 목록은 §언어 규칙 / §네이밍 규칙 / §파일 구조 / §브랜치 전략 / §커밋 규칙 / §구현 규칙(Guards·디스패치 의무·@header 규칙·Citation Rules·State 관리·도구 우선 원칙·배포 경계·플랫폼 분기 격리) / §루트 해석뿐이며 **"도구 출력 계약"이라는 절은 존재하지 않는다**. 레포 전수 grep 결과 `도구 출력 계약` 문자열은 위 6개소 + `tasks/131-.../PLAN.md:50`(태스크 산출물, 검사 범위 외)에서만 나타나고 `docs/CONVENTIONS.md` 본문에는 0건이다. 실제로 "모든 커맨드는 단일 라인 JSON으로 출력한다"를 서술하는 문서는 `opal/core/references/tools.md:124`(state-tool 절 한정)이며, 도구 공통 계약을 소유하는 절은 현재 어느 문서에도 없다.
  - impact: 신규 도구의 **출력 계약**이라는 규범 주장이 dangling pointer 위에 서 있다. 후속 워커·에이전트가 이 절을 열어 계약을 확인할 수 없고, 컨벤션 검사·리뷰가 근거 문서를 해석할 수 없다. 특히 본 게이트를 두 번 되돌린 인용 부정확 클래스와 동일 유형이며, `docs/CONVENTIONS.md`를 T0 SSOT로 소비하는 모든 경로에 컨벤션 드리프트를 전파한다.
  - 해결 방안 (택1, **소유자 승인 필요** — 기준 문서 자동 갱신은 이 에이전트의 금지 행위다):
    - (a) `docs/CONVENTIONS.md` §구현 규칙 아래 `### 도구 출력 계약` 절을 신설해 "모든 서브커맨드는 단일 라인 JSON + exit code만 반환한다"를 SSOT로 명문화하고, 6개소 인용을 그대로 유효화한다. 신설 시 `opal/core/references/tools.md:124`의 state-tool 한정 서술과의 소유권 경계를 함께 정한다.
    - (b) 절을 신설하지 않는다면 6개소의 인용을 실재하는 소유 문서로 재지정한다 — 예: `opal/tools/oppl-runtime-tool/README.md` §종료 코드(자기 문서 내부 계약) 또는 `opal/core/references/tools.md`.
  - 자동 수정: N (기준 문서 신설·재지정은 소유자 결정 사항)
  - verification: `grep -n "도구 출력 계약" docs/CONVENTIONS.md`가 1건 이상을 반환하거나(안 a), 위 6개소에서 `docs/CONVENTIONS.md §도구 출력 계약` 문자열이 0건이 된다(안 b).
  - 참조: `docs/CONVENTIONS.md` §Citation Rules · `opal/core/references/harness/citation-rules.md`

### Medium (3건)

- [ ] GC-C002 [`opal/tools/oppl-runtime-tool/ledger.py:36`] 문서명·경로 없는 `제안서 §N` 인용 (약 30개소)
  - fingerprint: `62a91996072a4083`
  - 카테고리: 문서화 / 인용(citation)
  - 위반 기준: `docs/CONVENTIONS.md:230` §Citation Rules — 인용은 `{경로}:{라인}` 또는 `docs/문서명 §섹션` 형식 (source_tier `T0`)
  - 발생 위치 (대표): `ledger.py:36, 49, 57, 67, 219` / `oppl_runtime_tool.py:7, 185, 275, 283, 426` / `oppl-runtime-tool/README.md:86, 131, 143, 182` / `opal_agent.py:231, 260, 733, 847, 945` / `opal_action_monitor.py:27` / `tests/test_ac_integration.py:58, 63` / `tests/test_oppl_runtime_admission.py:30, 35, 437` / `tests/test_oppl_runtime_config.py:31, 48` / `tests/test_monitor_delegation.py:18, 43, 55, 166`
  - 설명(evidence): 전부 `제안서 §6.2`·`제안서 §5`·`제안서 §7.2` 형태로 문서명도 경로도 없다. 같은 커밋이 `docs/proposals/`에 **두 개**의 제안서(`opal-oppl-runtime-stabilization.md`, `opal-oppd-v3-lean-project-execution.md`)를 함께 추가했으므로 "제안서"의 지시 대상이 문면상 확정되지 않는다.
  - impact: 절 번호만으로는 어느 제안서인지 해석이 갈리고, 제안서가 이동·아카이브되면 30개소가 동시에 끊긴다. 신규 도구의 폐쇄 집합(상태 7종·거부 코드 8종·필수 키 9종)이 전부 이 인용에 묶여 있다.
  - 해결 방안: 최소 1개소(각 파일 상단 또는 @header)에서 `docs/proposals/opal-oppl-runtime-stabilization.md §N` 전체 경로를 1회 명시하고 이하 약칭을 정의하거나, 각 인용을 전체 경로로 치환한다.
  - 자동 수정: Y (문자열 치환 — 단 대상 제안서 확정 후)
  - verification: `grep -rn "제안서 §" opal/tools/`의 각 파일에서 전체 경로 정의가 최소 1회 선행한다.
  - 참조: `opal/core/references/harness/citation-rules.md`

- [ ] GC-C003 [`opal/tools/oppl-runtime-tool/oppl_runtime_tool.py:185`] 채택 전 제안서를 구현의 규범 SSOT로 인용
  - fingerprint: `d6fcd181526f1be3`
  - 카테고리: 파일 구조 / 제안서 생명주기
  - 위반 기준: `docs/CONVENTIONS.md:133` §제안서 생명주기 — "제안서는 **소비형 입력물**이며 규범 SSOT가 될 수 없다 — 적용된 규칙의 원문은 해당 스킬·에이전트·harness owner 문서가 소유한다" / `docs/CONVENTIONS.md:142` "**[MUST] 아카이브 이동의 선행 조건은 잔여 인용 0건이다**" (source_tier `T0`)
  - 설명(evidence): `docs/proposals/opal-oppl-runtime-stabilization.md:3`의 상태는 `검토`(미채택)다. 그럼에도 `oppl_runtime_tool.py:185`(`admit — 제안서 §6.2 6개 검사`), `ledger.py:36/57/67`(필수 키 9종·상태 7종·T4b 3분기), `README.md:86/143/182`가 폐쇄 집합의 정의 원문을 제안서 절에 귀속시킨다. 구현이 완료된 지금 이 규칙들의 owner는 `oppl-runtime-tool/README.md`·`opal/agents/opal-loop-action-agent/AGENT.md`·`opal/skills/opal-pilot-project-loop/references/loop-control.md`여야 한다.
  - impact: 제안서를 `docs/proposals/archives/`로 이동하는 순간 약 30건의 인용이 끊기고, CLOSE 단계의 `stage.close` 제안서 판정 절차(`opal/core/references/harness/proposal-lifecycle.md`)가 "잔여 인용 0건" 선행 조건을 만족하지 못해 영구 차단된다.
  - 해결 방안: 폐쇄 집합(상태 7종 / 거부 코드 8종 / 필수 키 9종 / T4b 3분기)의 원문을 owner 문서 — `oppl-runtime-tool/README.md`와 `opal-loop-action-agent/AGENT.md` §T4b 실패 전이(`AGENT.md:227`) — 에 확정 이관하고, 코드 주석은 owner 문서를 가리킨다. 제안서 인용은 설계 배경(rationale)에 한해 남긴다.
  - 자동 수정: N
  - verification: 제안서 상태를 `적용완료`로 전환하고 `docs/proposals/archives/`로 이동할 때 `grep -rn "opal-oppl-runtime-stabilization" opal/ scripts/`가 0건이다.
  - 참조: `opal/core/references/harness/proposal-lifecycle.md`

- [ ] GC-C004 [`docs/CONVENTIONS.md:249`] 도구 인벤토리 SSOT가 신규 도구를 반영하지 않음
  - fingerprint: `5c61e3fe8ed09055`
  - 카테고리: 문서화 / 문서 레지스트리
  - 위반 기준: `docs/CONVENTIONS.md:245-250` §도구 우선 원칙 — "**전체 목록: `opal/tools/` (21종)**" (source_tier `T0`)
  - 설명(evidence): 실측 `ls -d opal/tools/*/` = **22개**. `docs/CONVENTIONS.md:249`는 여전히 "21종"이며 열거 목록(`event-loader`·`xlsx-tool`·`skill-registry`·`playwright-tool`·`improve-tool`·`cmux-tool`·`git-sync-tool`·`worktree-tool`·`date`·`doctor`·`tool-scan`·`opal-cli`·`opal-agent`·`opal-action-monitor`·`self-pm-tool`)에 `oppl-runtime-tool`이 없다. 같은 커밋이 `docs/PROJECT.md:173`에는 정상 등록했으므로 두 레지스트리가 어긋났다.
  - impact: "도구 우선 원칙"에 따라 워커가 기존 도구를 먼저 탐색할 때 신규 집행 도구가 목록에서 누락되어, 도구가 있는데도 직접 구현하는 경로가 열린다.
  - 해결 방안: `docs/CONVENTIONS.md:249`를 "(22종)"으로 고치고 열거에 `oppl-runtime-tool`을 추가한다. **기준 문서 수정이므로 오케스트레이터가 소유자 승인 후 수행한다**(이 에이전트의 행동 규칙 4).
  - 자동 수정: Y (단 소유자 승인 선행)
  - verification: `docs/CONVENTIONS.md:249`의 종수가 `ls -d opal/tools/*/ | wc -l` 결과와 일치하고 `oppl-runtime-tool`이 열거에 있다.
  - 참조: `docs/CONVENTIONS.md` §도구 우선 원칙 · `opal/core/references/harness/capability.md`

### Low (1건)

- [ ] GC-C005 [`opal/tools/oppl-runtime-tool/ledger.py:129`] 선례 인용의 라인 번호 1행 오차 + 비프로젝트 상대 경로
  - fingerprint: `0eea9f280e93f9f8`
  - 카테고리: 문서화 / 인용(citation)
  - 위반 기준: `docs/CONVENTIONS.md:230` §Citation Rules — `{경로}:{라인}` (source_tier `T0`)
  - 설명(evidence): 주석은 `# state_tool.py:403 선례를 그대로 따른다`이나, 인용 대상인 `os.environ.get("OPAL_HOME") or os.path.expanduser("~/.opal")`는 `opal/tools/state-tool/state_tool.py:404`에 있다. 403행은 `try:`다 (`grep -n "OPAL_HOME" opal/tools/state-tool/state_tool.py` → 398, 404 두 건뿐). 또한 경로가 `state_tool.py`라는 베어 파일명이라 프로젝트 루트 기준 경로가 아니다.
  - impact: 단건 오차이나 본 게이트가 두 번 되돌려진 인용 부정확 클래스에 속한다.
  - 해결 방안: 주석을 `# opal/tools/state-tool/state_tool.py:404 선례를 그대로 따른다`로 고친다.
  - 자동 수정: Y
  - verification: `sed -n '404p' opal/tools/state-tool/state_tool.py`가 `OPAL_HOME` 폴백 라인을 반환한다.
  - 참조: `opal/core/references/harness/citation-rules.md`

### Info (0건)

없음.

---

## 3-B. 선재 위반 (pre-existing — 이번 태스크 판정에서 분리)

> 아래 4건은 `git blame`으로 `d2db762` **이전** 커밋 소유임을 확인했다. 차단 계산에서 제외한다.

- [ ] GC-P01 [`opal/tools/opal-action-monitor/README.md:175`] 수기 누적 이력 절 `## 변경이력` (fingerprint `9077153f45a972df`)
  - 근거: `opal/core/references/opal-doc-standard.md:5절` — "SKILL, AGENT, harness, reference와 git으로 관리되는 프로젝트 산출물에는 `변경이력`·`Changelog`·`history`·`revisions` 등 이름과 무관하게 수기 누적 이력 절을 만들지 않는다" (`docs/CONVENTIONS.md:147-149` §문서 이력과 버전이 이 문서를 owner로 지정)
  - 소유: `e0a580b`(067) — 절과 v1.0·v1.1 행이 선재한다. `d2db762`는 v1.2 행 1개를 **기존 표에 추가**했다. 디스패처가 `loop-control.md:154`의 동종 표에 대해 명시한 "기존 표에 행 추가는 허용"과 동일 취급한다.
  - 참고: `opal/skills/opal-pilot-project-loop/references/loop-control.md:154` `## 변경이력` 표도 같은 클래스(`5c380a3` 소유, v1.3 행 추가)이며 디스패처가 명시적으로 허용했다. `opal/core/references/tools.md:1157`에도 동일 절이 있다. 절 자체의 정리는 별도 태스크 사안이다.
- [ ] GC-P02 [`opal/skills/opal-pilot-project-loop/references/loop-control.md:41`, `:148`] 구형 shim `opal/core/references/opal-harness.md §1` 인용 (fingerprint `32f117fe75a9ab0c`)
  - 근거: `docs/CONVENTIONS.md` §Citation Rules. `opal-harness.md:1-6`은 "구형 `opal-harness.md §N` 참조를 보존하는 호환 shim"이며 §1이라는 절은 없다. `opal-harness.md:52`의 호환 매핑표(`opal-harness.md §1 Guards` → `harness/guards.md`)로 **해석은 가능**하므로 끊긴 인용은 아니다.
  - 소유: `5c380a3`. 같은 파일에서 `d2db762`가 새로 쓴 문장(`loop-control.md:46` 이하)은 `harness/guards.md`를 직접 가리켜 한 파일 안에 두 세대 표기가 공존한다.
  - 권고: 후속 정리 시 `:41`·`:148`을 `opal/core/references/harness/guards.md` §자동 루핑 제약으로 통일한다.
- [ ] GC-P03 [`opal/tools/state-tool/README.md:549`] 존재하지 않는 라인 범위 인용 `opal/tools/xlsx-tool/run.sh:1-12` (fingerprint `8862d40dfd9dc289`)
  - 근거: 대상 파일은 11행이다(`wc -l`). 소유: `5c380a3`.
- [ ] GC-P04 [`opal/tools/state-tool/state_tool.py:3081-3082`] `code-scan.js` 라인 드리프트 (fingerprint `281fa79f20c7c088`)
  - 근거: 주석은 `DEFAULT_CONFIG.extensions(code-scan.js:43)`·`code-scan.js:351`을 인용하나, `code-scan.js:43`은 `HEADER_READ_BYTES` 값 근거 주석이고 `DEFAULT_CONFIG`는 `:97`, `user.extensions || DEFAULT_CONFIG.extensions`는 `:441`이다. 소유: `dd09b9f`(106).

---

## 4. 문서 업데이트 제안

- [ ] GC-DP-C001 [SSOT 누락 트리거] "도구 출력 계약" → `docs/CONVENTIONS.md` §구현 규칙에 `### 도구 출력 계약` 절 신설 제안
  - 근거: 6개소가 존재하지 않는 절을 T0 SSOT로 인용한다(GC-C001). 실질 규칙은 3개 도구(`state-tool`·`backlog-tool`·`oppl-runtime-tool`)가 이미 동형으로 구현하고 있으나 owner 문서가 없다.
  - 제안 내용: "OPAL 도구의 모든 서브커맨드는 **단일 라인 JSON + exit code**만 반환한다. 성공은 `ok: true` + exit 0, 실패는 `ok: false` + 비영 exit이며 진단 텍스트를 stdout에 섞지 않는다."
  - 함께 결정할 것: 오류 코드 카탈로그(`ERROR_CODES`) 보유 여부를 규칙으로 둘지. 현재 `backlog_tool.py:44`·`state_tool.py:136`은 `ERROR_CODES` 딕셔너리 + `ok()`/`err()` 헬퍼를 두는데 `oppl_runtime_tool.py:68,74`는 `emit()`/`fail()` 헬퍼만 두고 코드 카탈로그가 없다. `docs/CONVENTIONS.md`에 근거 규칙이 없어 **위반으로 판정하지 않았다**(convention-categories 카테고리 8 준용 — 추가 제안으로만 표시).
- [ ] GC-DP-C002 [빈도 트리거] 인용 카테고리 6건 / 5개 파일 — 빈도 임계값 N=3 초과
  - 근거: GC-C001(6개소) · GC-C002(약 30개소) · GC-C005 · GC-P02 · GC-P03 · GC-P04가 모두 `{경로}:{라인}`·`§섹션` 인용의 **실재성 검증 부재**라는 단일 원인을 공유한다.
  - 제안 내용: `docs/CONVENTIONS.md` §Citation Rules에 "인용 작성 시 대상 경로·라인·절의 실재를 기계적으로 확인한다"를 명문화하고, 검증 훅으로 승격할지 결정한다. 선례: `state-tool verify --code-scan-citation-check`(이미 PLAN.md Work items 경로에 존재).

---

## 5. 문서 작성 유도

`docs/CONVENTIONS.md` 존재 — 작성 유도 생략.

---

## 6. 중점 확인 항목 판정 (디스패처 지정 8종)

| # | 항목 | 판정 | 근거 |
|---|------|------|------|
| 1 | @header 현재 사실 규칙 | **PASS** | `headerSource: inline`(`.opal/code-scan.json`). 신규 2모듈 모두 @header 보유 — `oppl_runtime_tool.py:2-13`, `ledger.py:2-14`. `code-scan validate --scope framework` = `ok: true`, `exports_not_found 0` · `conflict 0` · `orphan 0` · `newly_uncovered 0`. `header_history` 2건은 `tool-scan/tests/test_tool_scan.py`·`worktree-tool/tests/test_worktree_tool.py`로 **검사 대상 밖 선재분**이다. AST 대조로 6개 모듈 선언 exports 전건이 실재함을 확인했다. 수정 모듈 `description`도 현재 사실과 일치 — `state_tool.py`는 `run-start`·`run_id` optional 필드·`event-verify`를, `opal_action_monitor.py:9`는 `runtime.json` 위임 + `depends`에 `oppl-runtime-tool/.oppl-run/runtime.json#ledger-schema`를, `backlog_tool.py:6`은 `_rerender_backlog_md`의 `최종 갱신` 동기화를 반영했다. `opal_agent.py:9`의 `task: "059, 067, 113, 131"`은 `header-standard.md` §2 단서(“`task`는 이력 필드 금지의 대상이 아니다”)에 해당하고 code-scan도 `header_history`를 발화하지 않았으므로 **도구 판정을 뒤집지 않는다**(스킬 [MUST] 5). |
| 2 | 도구 출력 계약 | **조건부 PASS** | 구현은 준수 — `oppl_runtime_tool.py:68-70` `emit()`이 `json.dumps(..., separators=(",",":")) + "\n"` 단일 라인만 stdout에 쓰고, `:74` `fail()`이 `ok:false` + 비영 exit을 낸다. `print()` 호출 0건, stderr 오염 0건. 종료 코드 0/1/2/3은 `README.md:29-35`에 표로 고정. **다만 그 계약이 인용하는 `docs/CONVENTIONS.md §도구 출력 계약`이 실재하지 않는다 → GC-C001(High).** `ok`/`err` 헬퍼명·`ERROR_CODES` SSOT는 `backlog_tool.py:44,69,74`·`state_tool.py:136,268,272`와 다르나(`emit`/`fail`, 카탈로그 없음) CONVENTIONS에 근거 규칙이 없어 위반으로 판정하지 않고 GC-DP-C001 제안으로만 올렸다. |
| 3 | 수치 비복제 | **PASS** | 재개 상한 `1`의 리터럴은 레포 전체에서 `ledger.py:55` `RESUME_LIMIT = 1` **단 1곳**이며, 바로 위 `:53-54`가 "수치의 SSOT는 `opal/core/references/harness/guards.md` §자동 루핑 제약"을 명시한다. `README.md:64`·`oppl_runtime_tool.py:142`·`AGENT.md:386-387`·`loop-control.md:41`·`SKILL.md:353`은 전부 참조만 두고 수치를 복제하지 않는다. 테스트도 수치를 하드코딩하지 않고 도구 노출값으로 검증한다(`test_oppl_runtime_admission.py:282`, `test_oppl_runtime_config.py:324-333`). |
| 4 | 수기 누적 이력 금지 | **PASS (신규 신설 0건)** | 변경된 9개 문서 중 이력 절을 **신설한 문서는 없다**. 신규 `oppl-runtime-tool/README.md`에도 이력 절이 없다. 기존 표에 행이 추가된 곳은 2건 — `loop-control.md:158`(v1.3, 디스패처가 명시 허용) 및 `opal-action-monitor/README.md:179`(v1.2). 후자의 절 자체는 `e0a580b`(067) 소유 선재분으로 GC-P01에 분리했다. |
| 5 | 배포 경계 | **PASS** | 소스에 `~/.opal/` **쓰기** 경로 0건. `ledger.py:128-130` `opal_home()`은 `OPAL_HOME` env 우선 + `~/.opal` 폴백으로 경로를 만들 뿐이고, 유일한 소비처 `:201` `global_path = opal_home() / "setting.json"`은 **읽기 전용** 설정 로드다. 실제 쓰기(`:334` mkdir / `:343` lock / `:368` temp write / `:372` atomic replace)는 전부 태스크 폴더 `.oppl-run/` 하위다. `run.sh:4`의 `$HOME/.opal/.venv/bin/python`은 배포된 인터프리터 **호출**이며 `backlog-tool/run.sh:3`·`state-tool/run.sh`와 동일 선례다. `scripts/install-mac.sh:1391-1395`가 배포본에 실행 권한을 부여하는 것은 install 경로의 정상 동작이다. |
| 6 | 네이밍 | **PASS** | `opal/tools/oppl-runtime-tool/` — 디렉토리 kebab-case, Python 파일 snake_case(`oppl_runtime_tool.py`·`ledger.py`), `run.sh` + `README.md` + `tests/` 구성이 `backlog-tool`·`state-tool` 관례와 동형. `run.sh`는 `backlog-tool/run.sh`의 구조 복제이며 파일 모드도 `100755`로 동일(`git ls-files -s`). @header `module` 값(`oppl_runtime_tool`·`ledger`)은 framework scope 전체에서 유일하다(중복은 선재 `conftest` 1쌍뿐). `docs/PROJECT.md:173`에 도구 행이 등록됐다. 단 `docs/CONVENTIONS.md:249` 인벤토리 미반영 → GC-C004. |
| 7 | 테스트 파일 명명·배치 | **PASS** | 8개 전건이 `{도구}/tests/test_*.py` — `oppl-runtime-tool/tests/test_oppl_runtime_config.py`·`test_oppl_runtime_admission.py`·`test_ac_integration.py`, `opal-agent/tests/test_opal_agent_runtime.py`·`test_opal_agent_cli_runtime.py`, `state-tool/tests/test_run_identity.py`, `backlog-tool/tests/test_backlog_header_sync.py`, `opal-action-monitor/tests/test_monitor_delegation.py`. 전건 @header 보유(`layer: test`). 포맷 위생도 전건 정상 — trailing whitespace 0, 탭 들여쓰기 0, EOF 개행 있음. |
| 8 | Citation 규칙 (전수) | **FAIL** | 26개 대상 파일의 `경로:줄번호` 인용을 **표본이 아니라 전수** 파싱해 실재·라인 범위를 기계 검증했다. 이번 변경 도입분: GC-C001(High, 6개소 — 존재하지 않는 절), GC-C002(Medium, 약 30개소 — 문서명 없는 `제안서 §N`), GC-C005(Low, 1개소 — 1행 오차). `docs/proposals/opal-oppl-runtime-stabilization.md:60/66/90/123`의 코드 인용 4건은 라인 범위까지 정확히 일치했다(검증 통과). 선재분 3건(GC-P02·P03·P04)은 분리했다. 새로 추가된 §단위 인용 중 `harness/header-rules.md §적용 대상 확장자`(`:84`), `AGENT.md §T4b 실패 전이`(`:227`), `harness/guards.md §자동 루핑 제약`(`:61`)은 전부 실재를 확인했다. |

---

## 7. 검사 실행 근거 (evidence)

- `~/.opal/tools/event-loader/run.sh verify --event worker.dispatch --receipt <scratchpad>/worker-dispatch-receipt.json` → `ok: true`, `verified_document_count: 4`, `manifest_sha256: cf0ae7a4664e516f1267056941effc8a283586ce7eb6998abe9ddc02dc355eee`
- `git log --oneline -1` → `d2db762 feat(131): OPPL 실행 상한·완료판정 도구 집행`; `git status --porcelain` → 태스크 폴더 산출물 3건 외 클린
- 입력 검증: `target_files` 26건 전부 존재하며 `project_root` 내부다 (부재 0건). `checked_files` == `target_files` → `status: pass`
- `~/.opal/tools/code-scan/run.sh validate --scope framework --json` → `ok: true`, coverage 26.6% (78/293), `counts.exports_not_found 0` · `conflict 0` · `orphan 0` · `newly_uncovered 0` · `header_history 2`(대상 외)
- AST 대조 스크립트로 6개 모듈의 @header `exports` 전건 실재 확인 (missing 0)
- 인용 전수 검증 스크립트 — `경로:줄번호` 패턴 및 `문서.md §섹션` 패턴을 26개 파일에서 추출해 파일 존재·행수 범위·헤딩 실재를 대조
- `git blame -L` 로 선재/신규 소유 커밋 판별 (GC-P01~P04)
- 포맷 위생: trailing whitespace / 탭 들여쓰기 / EOF 개행 — 신규·변경 코드·테스트 전건 이상 없음
- `node ~/.opal/tools/date/date.js datetime` → 2026-09-14 07:54 (KST)

## 8. 적용 기준 문서 (references)

- `docs/CONVENTIONS.md` (T0)
- `opal/core/references/opal-doc-standard.md` §5 (T0 — CONVENTIONS §문서 이력과 버전이 owner로 지정)
- `opal/core/references/header-standard.md` §2·§2.1 (T0)
- `opal/core/references/harness/header-rules.md` (T0)
- `opal/core/references/harness/citation-rules.md` (T0)
- `opal/core/references/harness/proposal-lifecycle.md` (T0)
- `opal/core/references/harness/guards.md` §자동 루핑 제약 (T0)
- `.opal/code-scan.json` (T0 — 실행 설정)
- `~/.opal/skills/op-gc-convention/references/convention-categories.md` (카테고리 가이드, 규칙 아님)
- `opal/core/references/harness/gc-finding-schema.md` (finding schema·판정 SSOT)

## 9. 비활성 카테고리와 사유

| 카테고리 | 상태 | 사유 |
|---|---|---|
| 2. 들여쓰기 | 활성 (위반 0) | 전건 통과 |
| 5. 미사용 import / 7. import 순서 | 비활성 | `docs/CONVENTIONS.md`에 Python import 순서·그룹 규칙이 없고 실행 설정(linter·formatter 설정 파일)도 레포에 없다. 관측 패턴만으로 판정하면 advisory 고정이며 차단 계산에서 제외되므로 finding을 만들지 않았다. |
| 4. 죽은 코드 | 부분 | @header `exports` 실재 대조로 갈음. 미참조 export는 CLI 도구 특성상 외부 소비 가능성이 있어 판정하지 않았다. |
| 8. 코드 품질 (community 참조) | 비활성 | `source_tier T2` — advisory 고정이며 `docs/CONVENTIONS.md`에 대응 규칙이 없어 위반 판정 대상이 아니다. |
