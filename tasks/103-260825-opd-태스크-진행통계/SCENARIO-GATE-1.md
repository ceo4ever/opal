# SCENARIO-GATE-1 — 목표-커버 게이트 판정 (iteration 1)

> 판정일: 2026-08-25 16:27 | 판정 주체: `opal-evaluator-agent` (생성자≠평가자, H-9)
> 대상: `TEST-SCENARIO.md`(1,175줄, TS 49건) | 입력: `TASK.md` · `PLAN.md`
> 규칙 SSOT: `~/.opal/references/harness/scenario-gate.md`

## 최종 verdict — **PASS**

`scenario-gate.md` §5-1 수렴 조건 충족 — 커버리지 누락 0(결정론 exit 0) **AND** 판단축 각 ≥1점(0점 축 없음) **AND** 평균 2.0 ≥ 1.5. 보강 항목 없음.

## 1. 결정론 파트 — `test-tool scenario-coverage-check`

정규화 페이로드는 스크래치패드에 생성했고 태스크 폴더는 미접촉이다.

| 항목 | 값 |
|------|-----|
| `all_covered` | `true` |
| `missing.requirements` / `.features` / `.hypotheses` | 전부 공집합 |
| counts | requirements 14 · features 6 · hypotheses 12 · scenarios 49 |
| exit code | 0 → **PASS** |

**음성 대조로 공허한 통과가 아님을 확인했다.** TS-016의 `covers_hypotheses`만 비운 변형 입력은 `{"error":"coverage_unmet","detail":{"missing":{"hypotheses":["H-9"]}}}` / exit 16으로 정확히 걸린다.

## 2. 판단 파트 — 루브릭 ①⑤⑥

| 축 | 점수 | 근거 |
|----|------|------|
| ① 목표 달성 | 2 | TS-060·TS-061이 `[SUPERVISOR]` L3로 목표 2개를 각각 직접 검증(병목 = 101의 TEST-SCENARIO 285분 대기 식별 / 대기 축소 = B-4 전환으로 21%·4%·54% 대조). TS-051·TS-052가 L2 베이스라인 대조로 뒷받침. 070 사건의 결함(목표 검증 시나리오 부재)이 재현되지 않음 |
| ⑤ 채택/잔존 | 2 | 교체 4축 전부 잔존0·채택 양면 검증 — TS-023(`workflow` 키 0건 + `skill`/`row_id` 채택) · TS-047(혼합 중앙값 「5시간 42분」 0건 + B-4 필터 동작) · TS-014(화이트리스트 5 → 전수 9) · TS-011(`row` 1~19 연속 · `updated_at` 빈 문자열 0건) |
| ⑥ 경계/부정 | 2 | 음수 clamp(TS-003) · 결측 3경로(TS-004) · 완료 태스크 `now` 무시(TS-006) · 짝수 n 중앙값 275.5→276 및 3.68%→4 반올림(TS-007) · `None`/0 포맷(TS-009) · 「0」과 「미기록」 구분(TS-015) · mtime 경계(TS-016) |

평균 **2.0** · `gaps` **0건**.

## 3. 작성 워커 자기보고 ↔ 평가자 실측 대조

| # | 워커 주장 | 실측 | 판정 |
|---|-----------|------|------|
| 1 | 49건, L1 30 / L2 17 / L3 2 | `#### TS-` 헤딩 49개, 계층 행 파싱 L1 30 · L2 17 · L3 2 | 일치 |
| 2 | RED-first 21건(TS-001~016·018·020~023) | 21건 전부 실질 「RED 기대 실패」 행 보유. 제외 4건(TS-017·024·051·052)은 「해당 없음 — 회귀/대조」 명시 | 일치 |
| 3 | H-1~H-12 전건 매핑, 미커버 0 | 도구 `all_covered:true`, hypotheses 12 | 일치 |
| 4 | 목표 4 · 경계 10 · 채택 5 | 목표 4 ✅ · 채택 5 ✅ · **경계 11** — §4.4 불릿에 `TS-036·TS-044`가 한 칸에 묶여 워커가 시나리오 수 대신 불릿 수를 셌다 | 경계만 불일치(과소 신고) |
| 5 | mock 부재 | `grep -oi "mock[a-z]*"` 결과 `mockup` 1건뿐 — TS-047의 `mockup/dashboard.html` 파일명. 가짜 객체 대체 0건 | 일치 |
| 6 | 17픽스처 빈 칸 0 | 17행 전건 4열 채움. 실 파일 직접 지시 7건(FX-101/086/102/103/089/LEGACY/COHORT) 존재 확인 | 일치 |

**기대값 원천 독립 재현** — 평가자가 `FX-101` 앵커 차분을 직접 계산해 425 = 작업 105 + 대기 320(75%)를 얻었고, 게이트 행 4건(row 4·7·10·17)·`.md` 9개가 실제와 일치했다. `FX-086` row 5 `plan.user_confirm` 15:46 < row 4 15:47 역행도 실재를 확인했다. 080~103 범위 `.md` 전수 **195**는 PM 실측과 동일하다.

## 4. 비차단 관찰 3건 (게이트 판정과 무관)

| # | 관찰 | 처리 |
|---|------|------|
| 1 | §4.4 경계축 건수 표기가 10이 아니라 11 | 문서 내 불릿 묶음 탓이며 시나리오 누락 0 — 표기만 어긋남, 비차단 |
| 2 | `FX-103` 스냅샷(16:03, row 9)이 이미 낡음 — 현재 행은 row 10 `test_scenario.scenario_gate` | 동결 복사본으로 선언돼 §0.5 이동값 규약 안. EXECUTE Step 본문에 「라이브 파일 직독 금지·동결본 사용」 지시 추가 필요 |
| 3 | `TASK.md` R-4 AC의 `task.user_confirm` 단정 · R-10 AC의 「`.md` 192개」가 미갱신 | **PM이 2026-08-25 16:29 반영 완료** — R-4를 `FX-102` 기준 + 동작 단정으로 교체, R-10을 하한 단정 + 반올림 원시값 병기로 교체 |

## 6. 후속 정정 — 「91」의 정체 (2026-08-25 16:45)

EXECUTE Step 1에서 베이스라인을 독립 재측정하던 중 원인이 특정됐다. `ANALYSIS.md` §4(6)의 「화이트리스트 91」은 **계수 오류가 아니라 스코프 엇갈림**이다.

| 스코프 | 전체 `.md` | 화이트리스트 |
|--------|-----------|-------------|
| `103` 폴더 **제외** | 190 | **91** |
| `103`의 `TASK.md`·`ANALYSIS.md` **포함** | 192 | **92** |

한 표의 같은 행에서 전체는 `103` 포함(192), 화이트리스트는 `103` 제외(91)로 세어 두 스코프가 섞였다. **같은 스코프로 세면 92가 옳고**, 결론은 바뀌지 않는다. `ANALYSIS.md` §8 `[PM 정정]` 행 문구를 이에 맞게 정밀화했다.

## 5. 절차 기록

본 보고서는 평가자가 아니라 **PM이 작성**했다. PM이 디스패치 프롬프트에 「태스크 폴더에 파일을 생성하지 마라」를 넣어 평가자가 산출물을 남길 수 없었고(PM 지시 오류), 판정 결과 전문이 응답으로만 반환됐다. 본 문서는 그 응답을 산출물로 옮긴 것이며 판정 내용은 평가자 원문이다.
