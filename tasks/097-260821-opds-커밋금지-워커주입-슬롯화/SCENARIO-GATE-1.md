# SCENARIO GATE 1 — 목표-커버 루브릭 판단축 채점

> 실행 일시: 2026-08-21T15:09:18+0900 | phase: `scenario-rubric` | iteration: 1
> 판정자: opal-evaluator-agent (verdict-only) | 규칙 SSOT: `opal/core/references/harness/scenario-gate.md` §2 / §5-1
> scenario_source: `tasks/097-260821-opds-커밋금지-워커주입-슬롯화/TEST-SCENARIO.md`
> 부수 입력: `TASK.md`(목표·R-1~R-5) / `PLAN.md`(F-001~F-005·H-1~H-12) / `.scenario-coverage-input.json`(정규화 플래그)
> 판정 범위: 판단축 ①목표달성 ⑤채택·잔존 ⑥경계·부정 **한정**. ②요구커버·③기능커버·④리스크커버는 test-tool `scenario-coverage-check` 결정론 판정 소관(exit 0, 전건 매핑 수신) — 본 보고서는 재판정하지 않는다.

## 1. 판단축별 판정

| 축 | 점수 | 통과선 | 판정 근거 (앵커 인용) |
|----|------|--------|----------------------|
| ① 목표 달성 | **1** / 2 | ≥1 | 통과 근거 — 목표(`"규칙이 워커에게 프레임워크 보증 경로로 도달"`)를 겨눈 시나리오가 문자열 존재 검증과 **분리되어 실재**한다. S-11은 "신규 항목이 「CONVENTIONS.md 부재 시 자연 스킵」 각주의 적용 대상이 아니고, 문서 선별 결과와 무관한 상시 주입 슬롯에 위치한다 — 신규 opi 프로젝트에서도 주입이 보장된다"를 판정식으로 삼아, TASK.md §핵심 판정 2가 지적한 비-도달 원인(조건부·우발 경로)을 직접 반증 대상으로 잡는다. S-1·S-2가 "파일에 문구가 있다"에 머무는 반면 S-11은 "그 문구가 조건부가 아니다"를 검증하므로, 지적된 간극의 **조건성 축은 메워졌다**. S-17(L3 [SUPERVISOR])이 금지 범위(commit·push·reset·rebase / add·stash·checkout 제외)의 통제 충분성을 사람 계층에서 수용 판정한다. 2점 미달 근거 — 도달 사슬의 마지막 링크가 어떤 시나리오에도 걸려 있지 않다. PLAN H-5가 "파일럿의 `[PM 컨텍스트 주입]` 블록은 **리마인더**이고 전달 기제가 아니다"라고 층을 명시적으로 분리했으므로, S-5(파일럿 10/10 포인터 보유)는 정의상 전달 기제 검증이 아니다. 그런데 실제 전달 기제인 **디스패치 프롬프트 리터럴층**에 대해서는, PLAN §2.3.2 형태 C 실측이 `sdd:114-121`만 「핵심 제약」 필드로 주입 템플릿 계약을 참조하고 `data-design:106`·`project-dev:407 harness_guards`는 해당 필드가 없음을 스스로 확인했음에도, 그 리터럴이 신규 항목을 실어 보내는지 판정하는 시나리오가 없다. 결과적으로 검증은 템플릿 파일 내부 구조(S-11)와 문언 수용성(S-17)에서 멈추고, 운영 계층에서 목표를 **직접** 관측하지 않는다(S-13이 배포본 무변경·install 미실행을 확정하므로 런타임 관측 자체는 범위 외임은 정당). |
| ⑤ 채택/잔존 | **2** / 2 | ≥1 | 교체형(R-5: CONVENTIONS 원문 → 하네스 포인터)의 두 갈래가 **독립 시나리오로 분리**되어 있다. 구형 잔존 0 — S-8이 "커밋 승인 조건을 서술하는 원문이 **2곳 전부** 0건 — `:203` 단독 처리는 FAIL"로 판정식을 세워, TASK.md가 `:203`만 지목한 누락(H-7의 `:188` §커밋 규칙)을 실패 조건으로 명문화했다. 부분 제거가 통과하지 못하는 판정식이므로 잔존 검증이 형식적이지 않다. 신형 채택 — S-9가 포인터 존재(≥1건)와 함께 "§커밋 규칙 절이 커밋 메시지 형식·단위 규칙으로서 **여전히 유효하다**"를 요구해, 원문 제거가 절 자체를 무의미하게 만들지 않았는지까지 판정한다. 존재 검증을 넘는 기능 축은 두 시나리오가 보강한다: S-10이 소비자 측 서술(`docs/PROJECT.md:223` "구현 규칙(Guards/...)")의 잔존 0건을 요구해 포인터가 문서 집합 안에서 실제로 정합하는지 대조하고, S-12가 "F-005 Step이 Phase 2이고 F-001 Step(Phase 1) 완료 후 착수됐음"을 STATE 저널 1건으로 요구해 교체 과정에 도달 경로 0 구간(H-9)이 없었음을 검증한다. |
| ⑥ 경계/부정 | **2** / 2 | ≥1 | 부정 경로 6건이 대체로 **통과가 자명하지 않은 실패 경로**를 겨눈다. 과잉적용 트랩 2건이 특히 실질적이다 — S-19는 S-2의 "0건" 판정식이 `:191` v1.6 변경이력의 "전 워커 공통 고정 2항목"을 소급 수정하는 사실 위조로 번지지 않았는지(H-3) "원문 그대로 1건 보존"으로 반대 방향에서 잠근다. S-21은 R-3 「단일 형태 통일」 AC가 `opal-pilot-gc/SKILL.md:219`의 `[MUST] 커밋 금지 (git commit 호출 금지)` 리터럴을 "규범 복제 제거 대상으로 오분류"해 유효 방어를 삭제하는 경로(H-6)를 차단한다. 두 시나리오는 다른 시나리오의 통과가 곧 이 시나리오의 실패가 되는 대립 구조이므로 형식 검사가 아니다. 나머지도 실측 가능한 실패 모드를 가진다: S-13은 배포본 shasum 동일과 함께 "소스 변경 = 런타임 적용이 **아님**"의 완료 보고 명시를 요구해 H-11 오판정을 겨냥하고, S-15는 이 태스크가 신설하는 규칙을 태스크 자신이 준수했는지를 HEAD 대조로 자기적용 검증하며, S-16은 `worktree-tool` 테스트가 편집 대상 파일(`dispatch-process.md`)을 실제로 읽는다는 PLAN §2.1.3 실측에 근거해 baseline 대비 감소 0을 요구한다. S-14(외부 프로젝트 참조 0건)는 상대적으로 통과가 쉬우나, TASK·PLAN이 발단 사례를 반복 언급하는 문맥에서 누출 위험이 실재하는 반증 가능 판정이다. 경계값 축도 S-11(CONVENTIONS 부재 프로젝트)·S-12(순서 경계)·S-19가 담당한다. |

## 2. 결과 계약

```json
{
  "scores": { "goal": 1, "adoption": 2, "boundary": 2 },
  "average": 1.67,
  "gaps": [],
  "verdict": "pass"
}
```

**verdict 산출**: §5-1 판단축 임계 — 세 축 각 ≥1점(0점 축 없음) **AND** 평균 ≥1.5점. 실측 `min=1`, `average=1.67` → **pass**.

`gaps[]`는 통과선 미달 축(<1점)에 대해서만 산출한다 — 미달 축 0건이므로 빈 배열이다. 아래 §3은 차단 요소가 아닌 보강 권고이며, 재작성 루프(§4-5) 진입 사유가 아니다.

## 3. 비차단 보강 권고 (advisory — 재작성 요구 아님)

| # | 대상 축 | 권고 | 근거 |
|---|--------|------|------|
| A-1 | ① | 디스패치 프롬프트 리터럴층 1건 신설 검토 — 파일럿 10종의 **실제 발신 프롬프트 리터럴**이 「핵심 제약」 필드(또는 등가 경로)를 보유해 공통 고정 항목을 실어 보내는지 판정. 현재 S-5는 리마인더 블록의 포인터 보유만 계수한다 | PLAN H-5(리마인더 ≠ 전달 기제) + PLAN §2.3.2 형태 C 실측(`data-design:106`·`project-dev:407`에 핵심 제약 필드 부재) |
| A-2 | ⑤ | 포인터 대상 무결성 판정 추가 검토 — §2.1 사전 조건 표가 규칙 SSOT(`opal-harness.md` §1 40-42행)를 "변경 전/후 shasum 대조" 불변 대상으로 선언했으나, 이를 판정하는 시나리오 ID가 없다. S-9의 포인터는 대상 절이 온존할 때만 기능한다 | TEST-SCENARIO §2.1 「규칙 SSOT … 불변 대상」 행 ↔ §3 시나리오 집합 대조 |

> 위 2건은 통과 판정을 되돌리지 않는다. 채택 여부는 PM 재량이며, 본 에이전트는 판정과 제안만 반환한다.

## 4. 판정 경계 고지

- 본 보고서는 판단축 ①⑤⑥만 채점했다. ②③④(요구·기능·리스크 커버)는 test-tool `scenario-coverage-check` 결정론 소관이며 본 에이전트가 대신 판정하지 않는다(§2 [MUST] 판정 주체 분리).
- 게이트 PASS 성립은 §6 tool-gated 집행에 따라 `scenario-coverage-check` exit 0 **AND** 본 `scenario-rubric` verdict pass 두 증거를 모두 요구한다. 전자는 수신 완료(all_covered=true), 후자는 본 보고서로 성립한다.
- 본 에이전트는 verdict-only다 — `TEST-SCENARIO.md`를 포함한 어떤 대상 파일도 수정하지 않았고, git 이력을 변경하지 않았다. 산출물은 본 보고서 1건이다.
