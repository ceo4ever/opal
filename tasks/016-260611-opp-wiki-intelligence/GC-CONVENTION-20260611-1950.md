# GC CONVENTION REPORT — 20260611-1950

<!--
  체크박스 5단계 상태 기호 (APPLY 단계가 기입):
  [ ]  open    — 미처리 (신규)
  [x]  done    — 적용 완료  ← 주석: 적용 시각 YYYY-MM-DD HH:mm + 수정 요약
  [~]  pending — 보류       ← 주석: 보류 사유
  [?]  review  — 확인 필요  ← 주석: 판단 근거 / 해결 방안
  [!]  failed  — 실패       ← 주석: 실패 사유 / 권장 대안
-->

## 1. 헤더

- 실행 일시: 시작 2026-06-11 19:50:00 / 완료 2026-06-11 19:52:00 / 소요 약 2분
- 범위: `all` / 대상 파일 2개
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` 존재 — 허브+링크 모델 (단일 문서, 링크 없음) / scope=all → 허브 전체 적용
- 기준 문서 로드 내역:
  - 허브: `docs/CONVENTIONS.md` (전체 섹션 파싱 완료)
  - 상세 링크: 없음 (OPAL 단일 문서 프로젝트)
- APPLY 수행 여부: N (수동 대기)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 4 |
| 심각도 분포 | Critical 0 / High 0 / Medium 1 / Low 1 / Info 2 |
| 자동 수정 가능 | 1 |
| 수동 조치 필요 | 3 |
| 파일별 상위 Top 5 | `test_brain_tool.py` (3건) / `brain_tool.py` (1건) |
| 카테고리별 빈도 | 죽은 코드 (1 파일) / import 순서 (1 파일) / 코드 품질 추가 제안 (1 파일) / 문서화 (1 파일) |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 0 (빈도 트리거 미발동 — 대상 파일 2개로 N=3 미만) |

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

### High (0건)

### Medium (1건)

- [ ] GC-C001 [`opal/tools/brain-tool/tests/test_brain_tool.py`:1238] 섹션 번호 주석 불일치 — "# 14" 기재이나 실제 13번째 테스트 클래스
  - 카테고리: 문서화
  - 위반 기준: 프로젝트(CONVENTIONS.md §구현 규칙 > Citation Rules) — 주석·섹션 번호는 실제 내용과 일치해야 함. 주석 불일치는 유지보수 시 혼란을 유발하며 인용 신뢰성을 저하시킨다.
  - 설명: 테스트 파일의 클래스 섹션 구분 주석이 `# 14. validate_frontmatter 단위 테스트`로 기재되어 있으나, 실제로는 TestInit(1), TestAddPage(2), TestIndex(3), TestLog(4), TestSearch(5), TestSyncHeader(6), TestLint(7), TestValidate(8), TestErrorCodes(9), TestDynamicPageTypes(10), TestAnalyze(11), TestIngestScan(12), TestValidateFrontmatter(13) 순서로 13번째 클래스다. 섹션 주석 내 구분선(`# ═══`)에서도 `# 14`로 기재되어 있어 일관성 오류가 있다.
  - 해결 방안: `test_brain_tool.py` L1236 `# 14.` → `# 13.` 으로 수정. 해당 섹션 구분 주석(`# ═══════ ... 14. validate_frontmatter`) 내 숫자도 동일하게 `13`으로 정정.
  - 자동 수정: Y (단순 숫자 치환)
  - 참조: TBD — Python 코드 주석 일관성 가이드 링크

### Low (1건)

- [ ] GC-C002 [`opal/tools/brain-tool/tests/test_brain_tool.py`:511,549,634] `__import__("yaml")` 인라인 호출 — 파일 상단 명시적 import 없이 사용
  - 카테고리: import 순서
  - 위반 기준: 프로젝트(CONVENTIONS.md §언어 규칙) — 코드/변수/필드명은 English, import는 파일 상단에 명시적으로 선언하는 것이 Python 관용. `__import__` 인라인 호출은 import 선언 의도를 파일 상단에서 파악할 수 없게 한다.
  - 설명: `TestSyncHeader.test_sync_header_with_stub_code_scan` (L511), `test_sync_header_stale_marking` (L549), `TestLint.test_lint_detects_stale_page` (L634) 등 3곳에서 `__import__("yaml").safe_dump(...)` 형태로 yaml을 인라인 호출한다. 파일 상단 import 블록에 `import yaml`이 없어 의존성 파악이 어렵다. 실제로는 `brain_tool.py`에서 import된 yaml이 이미 로드되어 있어 동작하지만, 테스트 파일 자체의 의존성 선언이 불완전하다.
  - 해결 방안: `test_brain_tool.py` import 블록(L24~34)에 `import yaml` 추가 후, L511/L549/L634의 `__import__("yaml")` → `yaml` 로 교체.
  - 자동 수정: Y (import 추가 + 단순 치환)
  - 참조: TBD — PEP 8 import 관련 가이드 링크 (https://peps.python.org/pep-0008/#imports)

### Info (2건)

- [ ] GC-C003 [`opal/tools/brain-tool/brain_tool.py`:123] bare `except Exception` 사용
  - 카테고리: 코드 품질 (추가 제안 — CONVENTIONS.md 명시 규칙 없음, 위반 판정 아님)
  - 위반 기준: 추가 제안 (getsentry/code-review 보조 참조 — CONVENTIONS.md §구현 규칙에 명시된 규칙 없음)
  - 설명: `load_page_types` 함수의 `except Exception:` (L123)은 YAML 파싱 실패를 포함한 모든 예외를 묵묵히 폴백 처리한다. 예상치 못한 예외(예: `PermissionError`, `UnicodeDecodeError`)도 동일하게 처리되어 디버깅이 어려울 수 있다.
  - 해결 방안: `except (yaml.YAMLError, OSError, ValueError):` 등 예상 예외 타입을 명시. 또는 최소한 로깅을 추가하여 무시된 예외를 추적 가능하게.
  - 자동 수정: N (의도 파악 후 수동 결정 필요)
  - 참조: TBD — Python exception handling best practices 링크

- [ ] GC-C004 [`opal/tools/brain-tool/tests/test_brain_tool.py`:1240] `TestValidateFrontmatter` 클래스에 `BrainTestCase` 대신 `unittest.TestCase` 직접 상속
  - 카테고리: 코드 품질 (추가 제안 — CONVENTIONS.md 명시 규칙 없음, 위반 판정 아님)
  - 위반 기준: 추가 제안 (getsentry/code-review 보조 참조)
  - 설명: `TestValidateFrontmatter`(L1240)는 `unittest.TestCase`를 직접 상속한다. 다른 테스트 클래스들은 `BrainTestCase`(tmpdir 격리 포함)를 상속하는데, 이 클래스만 다른 베이스 클래스를 사용해 일관성이 없다. `validate_frontmatter`는 순수 함수이므로 tmpdir 불필요하다는 의도가 있을 수 있으나, 주석으로 명시되어 있지 않다.
  - 해결 방안: 의도적 결정이라면 클래스 docstring에 "순수 함수 테스트 — 파일시스템 불필요"로 명시. 또는 테스트 파일 내 일관성을 위해 `BrainTestCase` 상속으로 통일.
  - 자동 수정: N (의도 확인 필요)
  - 참조: TBD — Python unittest 패턴 가이드 링크

---

## 4. 문서 업데이트 제안 (트리거 발동 시만)

트리거 발동 없음.

- 빈도 트리거: 대상 파일 2개 — 동일 fingerprint 3파일 이상 임계값(N=3) 미달. 발동 없음.
- 새 카테고리 트리거: 발견된 이슈 카테고리(문서화 / import 순서 / 코드 품질)는 모두 CONVENTIONS.md §구현 규칙 내 기존 규범 범위 내. 신규 카테고리 없음.
- Critical/High 이슈: 0건. 심각도 트리거 해당 없음.

---

## 5. 문서 작성 유도 (해당 시)

`docs/CONVENTIONS.md` 존재 — 작성 유도 생략.
