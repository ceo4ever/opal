# GC CONVENTION REPORT — 2026-09-09T19-28-00

<!--
  상태 기호:
  [ ] open / [x] done / [~] pending / [?] review / [!] failed
-->

## 1. 헤더

- 실행 일시: 시작 2026-09-09 19:28:00 / 완료 2026-09-09 19:32:12 / 소요 4분 12초
- 범위: `all` / 대상 파일 69개 (`git diff --name-only HEAD` 53개 + untracked 16개, 중복 제거)
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` 허브 전체. `scope=all`이므로 별도 영역 문서는 병합하지 않았다.
- 위임 기준: `docs/CONVENTIONS.md:128-130`이 지시한 `opal/core/references/opal-doc-standard.md` §5
- APPLY 수행 여부: N (진단 전용)
- 대상 구성: Markdown/MDC 56개, Python 11개, Bash 1개, JSON 1개

대상은 `.opal/AGENT.md`, `docs/` 3개, `opal/agents/` 15개, bootstrapper 4개,
`opal/core/` 및 reference 17개, pilot/creator skill 11개, tool 및 test 16개,
`scripts/tests/` 2개다. 태스크 폴더 산출물은 대상에서 제외했다.

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 0 |
| 심각도 분포 | Critical 0 / High 0 / Medium 0 / Low 0 / Info 0 |
| 자동 수정 가능 | 0 |
| 수동 조치 필요 | 0 |
| 파일별 상위 Top 5 | 해당 없음 |
| 카테고리별 빈도 | 해당 없음 |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 0 |

## 3. 수정 대상 (체크리스트)

### Critical (0건)

없음.

### High (0건)

없음.

### Medium (0건)

없음.

### Low (0건)

없음.

### Info (0건)

없음.

### 판정 증거

| 검사 | 프로젝트 기준 | 결과 |
|------|---------------|------|
| 경로·이름 | `docs/CONVENTIONS.md:9-28` | 새 파일의 일반 경로는 kebab-case, Python 파일은 snake_case이며 에이전트 15종은 `opal/agents/{agent-name}/AGENT.md`에 있다. 위반 0건. |
| Skill/Agent 구조 | `docs/CONVENTIONS.md:91-125` | 대상 SKILL.md 11개와 AGENT.md 15개 모두 YAML frontmatter 경계를 가진다. 위반 0건. |
| 문서 이력 | `docs/CONVENTIONS.md:128-130`, `opal/core/references/opal-doc-standard.md:61-68` | 대상 Markdown/MDC 56개에서 `변경이력`, `Changelog`, `history`, `revision(s)` 계열 헤더를 대소문자 무시 검색했다. 검출 0건. |
| 코드 @header | `docs/CONVENTIONS.md:201-207` | `code-scan validate --header-source inline --changed - --json` 결과 코드 대상 11개 모두 covered, coverage 100%, 전체 violation count 0, `header_history` 0이었다. 예: `opal/tools/event-loader/event_loader.py:1`, `scripts/tests/task113_bootstrap_audit.py:2`. |
| State 호출 표기 | `docs/CONVENTIONS.md:216-224` | 추가 diff에 신규 `--row` 호출 및 `state.json` 직접 편집 지시는 0건이다. |
| 플랫폼 분기 격리 | `docs/CONVENTIONS.md:240-243` | skill/agent 추가 diff에서 Claude/Cursor/Gemini/Antigravity 조건 분기는 0건이다. 플랫폼별 내용은 bootstrapper 어댑터에만 있다. |

비판정 보조검사로 `git diff --check HEAD` exit 0, 대상 파일 EOF 개행 누락 0건을
확인했다. 실행 증거는
`/Volumes/Data/AIStudio/workspace/ai-framework/tasks/113-260909-opd-부트스트랩-이벤트-하네스-경량화/TEST.md:3-57`의
S-1~S-12 12 pass, 회귀 suite 전체 pass, hardcoded secret 0건과 일치한다. 이 두 검사는
`docs/CONVENTIONS.md`에 없는 별도 규칙으로 위반을 만들지 않았다.

보조 참조인 `~/.opal/community-skills/getsentry/code-review/SKILL.md`는 설치 경로에 없었다.
`docs/CONVENTIONS.md`에 해당 보조 범주의 추가 강제 규칙이 없으므로, 자체 기준을 만들어
위반으로 판정하지 않았다.

## 4. 문서 업데이트 제안

빈도·심각도·새 카테고리 트리거가 모두 발동하지 않았다. 제안 없음.

## 5. 문서 작성 유도

`docs/CONVENTIONS.md` 존재 — 작성 유도 생략.
