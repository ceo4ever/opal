# GC-CONVENTION 보고서

## §1 헤더

| 항목 | 값 |
|------|-----|
| 실행 일시 | 2026-06-17 14:00 (KST) |
| 태스크 | 027 EXECUTE PM Gate — 컨벤션 진단 |
| scope | backend (Python 도구) |
| 기준 문서 상태 | `docs/CONVENTIONS.md` 존재 — 단일 문서 모델 (허브+링크 없음, 상세 링크 미정의) |
| 링크 파싱 결과 | 링크 없음 — 단일 진입점 적용 (`docs/CONVENTIONS.md` §구현 규칙 전체 적용) |
| 진단 대상 파일 | 3개 (아래 §3 참조) |
| check_enabled | true |

---

## §2 요약 지표

| 심각도 | 건수 |
|--------|------|
| Critical | 0 |
| High | 0 |
| Medium | 1 |
| Low | 1 |
| Info | 3 |
| **합계** | **5** |

**Critical/High 건수: 0건**

---

## §3 수정 대상

### 파일 1: `opal/tools/brain-tool/brain_tool.py`

#### GC-C001

```
[ ] GC-C001
파일:라인   : opal/tools/brain-tool/brain_tool.py:1–14 (@header 블록)
카테고리    : 문서화 (@header)
심각도      : Low
위반 기준   : docs/CONVENTIONS.md §구현 규칙 > @header 규칙 + opal/core/references/harness/header-rules.md §파일 수정 시
설명        : @header `description` 필드에 `[027]` 태그 변경사항 요약이 기재되어 있으나,
              `exports` 필드에 027에서 신규 추가된 public 인터페이스인
              `load_page_types`와 `DEFAULT_PAGE_TYPES`는 포함되어 있다.
              하지만 모듈 외부에서 직접 참조 가능한 `_norm`, `_score_page`는 exports에
              명시되지 않았다. _norm은 test_brain_tool.py가 `BT._norm` 형태로 직접 호출하므로
              사실상 공개 API처럼 사용된다.
해결 방안   : exports 필드에 `_norm`을 추가 (BT._norm으로 테스트에서 참조하므로 공개 헬퍼로 승인).
              또는 테스트에서 내부 헬퍼 직접 호출을 제거하고 공개 함수만 통해 검증하도록 리팩토링.
자동 수정   : false (exports 목록 변경은 의미적 판단 필요)
참조 URL    : 참조: TBD — opal/core/references/harness/header-rules.md §파일 수정 시 exports 갱신
```

---

### 파일 2: `opal/tools/brain-tool/tests/test_brain_tool.py`

#### GC-C002

```
[ ] GC-C002
파일:라인   : opal/tools/brain-tool/tests/test_brain_tool.py:1443–1493 (TestTermDraft027._write_term_page)
              및 1643–1665 (TestTermLint027._write_term_page)
카테고리    : 코드 품질 (중복 코드)
심각도      : Medium
위반 기준   : docs/CONVENTIONS.md §구현 규칙 > 구현 규칙 (코드 재사용 원칙); getsentry/code-review 추가 제안 보강
설명        : `_write_term_page` 메서드가 `TestTermDraft027`(라인 1471–1493)과
              `TestTermLint027`(라인 1643–1665) 두 클래스에 거의 동일한 내용으로
              각각 정의되어 있다.
              두 구현의 차이는 `sources` 기본값뿐이다:
              - TestTermDraft027: `sources: []`
              - TestTermLint027: `sources: [task:027]`
              이 중복은 향후 term 페이지 구조 변경 시 한쪽만 수정하는 실수로 이어질 수 있다.
해결 방안   : 공통 베이스 헬퍼를 `BrainTestCase`에 추가하거나, 별도 모듈 수준 함수
              `_make_term_page(brain_root, name, title, status, aliases, sources)`를
              정의하고 두 클래스 모두 이를 호출하도록 통합한다.
자동 수정   : false (클래스 구조 변경 필요)
참조 URL    : 참조: TBD — Python 코드 중복 제거 DRY 원칙 (PEP 8 / Python Best Practices)
```

#### GC-C003

```
[ ] GC-C003
파일:라인   : opal/tools/brain-tool/tests/test_brain_tool.py:1–14 (@header 블록, scenarios 필드 누락)
카테고리    : 문서화 (@header)
심각도      : Low
위반 기준   : opal/core/references/harness/header-rules.md §테스트 파일 전용 선택 필드
설명        : 테스트 파일의 @header에 `task: "027"` 필드는 포함되어 있으나,
              `scenarios` 선택 필드가 없다.
              header-rules.md에 따르면 테스트 파일(`layer: test`)은
              `task`(태스크 번호)와 `scenarios`(연결된 TEST-SCENARIO.md S-ID 목록)를
              함께 작성하도록 권장한다.
              현재 @header:
                "task": "027"
              누락:
                "scenarios": ["S-1", "S-2", ...] (027 TEST-SCENARIO.md 기준)
해결 방안   : @header에 `scenarios` 필드를 추가한다. 027 TEST-SCENARIO.md에서
              해당 테스트가 커버하는 S-ID 목록(예: S-1, S-2, S-3, R-6 등)을 확인하여 기입.
자동 수정   : false (TEST-SCENARIO.md 내용 확인 후 수동 기입 필요)
참조 URL    : opal/core/references/harness/header-rules.md §테스트 파일 전용 선택 필드
```

#### GC-C004

```
[ ] GC-C004
파일:라인   : opal/tools/brain-tool/tests/test_brain_tool.py:56–86 (make_args 기본값 `"type": None`)
카테고리    : 코드 품질 (테스트 신뢰성)
심각도      : Info
위반 기준   : getsentry/code-review 추가 제안 (CONVENTIONS.md §에 해당 규칙 없음 — 위반 아님)
설명        : `make_args` 헬퍼의 기본값 중 `"type": None`으로 설정되어 있어
              type 필터가 없는 검색을 기본으로 한다.
              이는 올바른 설계이나, 일부 구형 테스트(TestSearch.test_search_by_type_filter)는
              `make_args(type="entity")`와 같이 명시적으로 오버라이드하고 있다.
              반면 일부 테스트는 기본값 의존 여부가 명시적이지 않아 의도가 불분명하다.
              이는 현재 동작에 영향을 주지 않으나, 가독성 관점의 개선 여지가 있다.
해결 방안   : (선택적) 각 테스트에서 type 파라미터를 명시적으로 지정하여 의도를 명확히 한다.
자동 수정   : false
참조 URL    : 참조: TBD — Python unittest 가독성 모범 사례
```

---

### 파일 3: `opal/tools/brain-tool/templates/page-term.md`

#### GC-C005

```
[ ] GC-C005
파일:라인   : opal/tools/brain-tool/templates/page-term.md:1–13 (YAML frontmatter)
카테고리    : 파일 구조 (frontmatter 비표준 필드)
심각도      : Info
위반 기준   : getsentry/code-review 추가 제안 (CONVENTIONS.md에 frontmatter 필드 목록 규칙 없음 — 위반 아님)
설명        : page-term.md 템플릿의 frontmatter에 `actors`, `surfaces` 필드가 포함되어 있다.
              brain_tool.py의 REQUIRED_FRONTMATTER = ["type", "title", "created", "updated", "status"]
              및 OPTIONAL_FRONTMATTER = ["tags", "sources", "related"]에 정의되지 않은 필드이다.
              현재 `validate_frontmatter`는 미등록 필드를 오류로 처리하지 않으므로
              런타임에서는 문제가 없다.
              다만 SCHEMA.md §frontmatter 표준(OPTIONAL_FRONTMATTER 목록)에 이 필드들이
              반영되지 않으면, 향후 validate 강화 시 예상치 못한 실패가 발생할 수 있다.
해결 방안   : SCHEMA.md의 frontmatter 선택 키 목록에 `actors`, `surfaces`를 추가하거나,
              brain_tool.py의 OPTIONAL_FRONTMATTER 상수에 term 전용 키로 추가하고
              validate_frontmatter를 타입별로 분기하도록 개선한다.
자동 수정   : false (SCHEMA.md 및 brain_tool.py 동시 변경 필요)
참조 URL    : 참조: TBD — brain-tool SCHEMA.md §frontmatter 표준 정합성
```

---

## §4 문서 업데이트 제안

이번 진단에서 빈도 트리거(3개 이상 파일에서 동일 fingerprint) 및 새 카테고리 트리거는 발동되지 않았다.

### [새 카테고리 트리거] term 타입 관련 frontmatter 규칙 미정의

- **감지**: `page-term.md`가 `actors`, `surfaces`와 같은 term 도메인 전용 frontmatter 필드를 사용하나, `docs/CONVENTIONS.md`와 `brain_tool.py` 공식 스키마에 정의가 없다.
- **제안**: `docs/CONVENTIONS.md` §구현 규칙 또는 brain SCHEMA.md에 `term` 타입 전용 확장 frontmatter 필드 목록 섹션을 추가한다.
  - 예: `term` 타입 선택 필드: `aliases` (list), `actors` (list), `surfaces` (list)

---

## §5 문서 작성 유도

해당 없음. `docs/CONVENTIONS.md` 존재 — 체크 정상 완료.

---

> **진단 전담 보고서** — 이 에이전트는 소스 파일을 수정하지 않는다.
> 수정이 필요한 이슈는 오케스트레이터(opal-pilot-gc)의 CLOSE 단계에서 `//opds` 체인으로 이관한다.
