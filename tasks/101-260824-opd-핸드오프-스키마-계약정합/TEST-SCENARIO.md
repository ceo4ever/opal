# TEST SCENARIO: ANALYSIS→PLAN 핸드오프 스키마 계약 정합 + 확정 입력 판정값 템플릿 승격

> 작성일: 2026-08-24 | 상태: 작성 완료 (Block A 선작성 → Block B 보강 반영)
> 작성자: 알투(PM) + 캡틴 페어 — PLAN 워커(opal-plan-agent)와 다른 작성자 (self-confirming 방지)
> 도출 입력: Block A(TASK.md 유래 — 목표·R-1~R-5·채택/잔존) + Block B(PLAN.md 유래 — H-1~H-7·F-001~F-006)
> RED-first 트랙: **미적용** — 코드 변경 0건(전건 md/json 규범 개정)이라 실패하는 테스트를 먼저 세울 대상이 없다. 검증은 산출물 검사(L1)·스펙 검증(L1 도구)·행동 재생성(L3)으로 구성한다.

## Block A ↔ Block B 대조 결과 (보강 이력)

| 구분 | 건수 | 내용 |
|------|------|------|
| 유지 | 12 | Block A 초안이 PLAN 설계와 정합 |
| 수정 | 3 | D-A 컬럼 개명(`파일`→`경로`, `변경 필요`→`변경유형`)을 초안이 반영하지 못해 판정 기준 갱신 (S-1·S-4·S-6) |
| 추가 | 7 | PLAN 유래 파괴 관점 6건 — H-2(P-4 문언)·H-3(배열 인덱스)·H-5(코드 무변경)·H-6(3값 분기)·TS-011(헤딩 불변)·범위 초과 검출 + **게이트 후 평가자 지적 반영 1건(S-22 게이트 실제 거부력)** |
| 판정 기준 강화 | 4 | 게이트 통과 후 평가자 비차단 권고 반영 — S-16(호의적 판독 → 문자열 검사)·S-17(실행 레시피 부여)·S-9(개별 토큰 전종 매치로 부분 잔존 검출)·S-20(재조사 0건 관측 방법 확정). **판정 기준을 조이는 방향이므로 게이트 재호출 불요** |
| 삭제 | 1 | 초안 「§8에 파일형 항목 오배치 검출」 — PLAN 설계에 이를 검출하는 기제가 없어 판정 불능(반증 불가 시나리오). 역할 분리는 S-2 주석 검사로 갈음 |

> **[조기 경보] PLAN §C-4는 "L3 불요"로 판정했으나 본 문서는 L3 3건(S-19~S-21)을 유지한다.** PLAN이 제시한 대체 근거는 "본 태스크의 ANALYSIS.md·PLAN.md가 실증 사례"인데, 두 산출물은 PM이 개정안을 프롬프트로 주입한 상태에서 생성됐다 — 규범만으로 재현되는지를 그 규범을 주입받아 만든 산출물로 증명하는 self-confirming 구조다(헌법 §4). task:100 R-12가 확립한 검증 형태는 **지시 없는 표준 프롬프트 재생성 대조**이며(`tasks/100-.../DONE.md` §5), H-7(템플릿 우위)은 이 형태로만 반증 가능하다. 캡틴 판단 사항으로 §승인 요청에 표면화한다.

## 1. 리스크 가설 표

> PLAN.md `## 리스크 가설 표` H-1~H-7 전건 전재 (재작성 없음).

| ID | 변경 단위 | 깨질 수 있는 계약 | 운영 영향 | 검증 계층 | 시나리오 |
|----|----------|----------------|---------|---------|---------|
| H-1 | F-005 / `op-dev-qa` 거울 사본 2파일 | P-8 문언 편측 개정 시 SKILL.md와 qa-dev-guide.md가 다른 검증 축을 지시 (task:100 H-4 재발) | P1 | L1 | S-11 |
| H-2 | F-001 / §1.1 컬럼 개명(`파일`→`경로`, `변경 필요`→`변경유형`) | qa-dev-guide P-4 "변경 필요 파일" 문언이 사라진 컬럼명을 가리켜 검증자 오독 | P2 | L1 | S-12 |
| H-3 | F-003 / `pipeline.json` checklist 배열 | 신규 item 삽입으로 기존 item[2]의 배열 인덱스가 3으로 이동 — 인덱스로 항목을 지목하는 산문/도구가 있으면 어긋남 | P1 | L1 | S-5, S-7 |
| H-4 | F-002 / `analysis-core.md:59` 문장 교체 | 문장이 §8 단독을 계속 지시하면 R-1 AC("구형 3열 단독 계약 0건")가 문자 그대로 미충족 | P0 | L1 | S-3 |
| H-5 | F-004 / 판정값 스킴 교체 | 구형 4값의 `승계` 토큰 제거가 M-2(범위 제외) 침범으로 오해 — state-tool `승계` verdict(코드)는 불변이어야 함 | P1 | L1 | S-10 |
| H-6 | F-004 / 템플릿 승격 범위 | `op-dev-plan/SKILL.md`의 3값 스킴이 범위 밖이라 ANALYSIS(2계열)와 분기 — 범위 밖 drift | P2 | L1(관측) | S-12 |
| H-7 | F-001·F-002 전반 | 개정을 산문 규정에만 두고 템플릿 실물 섹션에 반영하지 않으면 다음 회차에 재현되지 않음 (task:100 R-12 실측) | P0 | L1 + **L3** | S-1, S-2, S-8, S-19, S-21 |

## 2. 테스트 데이터 설계

### 2.1 사전 조건 데이터

DB·테이블 없음(프레임워크 문서·스킬 태스크). 사전 조건 자산은 아래와 같다.

| 자산 | 식별자 | 상태 | 출처 |
|------|--------|------|------|
| 개정 전 스냅샷 | `git show HEAD:<path>` | EXECUTE 착수 전 커밋(`b30fb29`) 상태 | git |
| 레거시 ANALYSIS 산출물 | `tasks/100-260822-opd-분석코어-공유SSOT/ANALYSIS.md` | 구형 §1.1 5열(`순서(Tier)` 포함, `영역` 없음) | 기존 태스크 |
| 최상위 헤딩 베이스라인 | `grep -rn '^## ' opal/core/references/harness/analysis-core.md opal/skills/op-dev-plan/references/plan-guide.md` | 개정 전 목록 | 실행 관측(E1) |
| F-1 픽스처 — `[결정]` 0건 TASK | 인라인 최소 TASK.md(확정된 설계 방향에 `[사실]`만 존재) | S-16 입력 | 수동 작성 |
| F-2 픽스처 — 구형 4열 ANALYSIS | 인라인 최소 ANALYSIS.md(§1.1이 `파일\|역할\|변경 필요\|근거` 구형 4열) | S-22 입력 | 수동 작성 |

### 2.2 시나리오별 데이터 흐름

| 시나리오 | Given (read) | When (실행) | Then (re-read) |
|---------|------------|------------|---------------|
| S-3·S-9 | 개정된 `opal/` 전체 | 정규식 전종 매치 grep | 구형 계약·구형 나열 0건 |
| S-5·S-6 | 개정된 `pipeline.json` | `spec-validate` + JSON 파싱 | exit 0 · 5항목 · 기존 4문자열 diff 0 |
| S-13 | 개정 전 헤딩 베이스라인 | 개정 후 동일 grep | 목록 동일(diff 0) |
| S-19~S-21 | 개정된 6파일 (배포본 경로 포함) | 지시 없는 표준 프롬프트로 ANALYSIS·PLAN 재생성 | 산출물이 신형 스키마·판정값을 자발 채택 |

## 3. 검증 시나리오

### L1. 기능 단위 (자동, 정적 검사)

#### S-1: §1.1 템플릿 헤더가 5열이다 [F-001 / R-1 / H-7 / ⑤신형채택]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-7 |
| 대상 | `opal/skills/op-dev-analysis/SKILL.md` §1.1 템플릿 표 헤더 |
| 계층 | L1 |
| **실행 방식** | **M1 (문자열 대조)** |
| 조건 | Step 1 완료 후 |
| 기대 결과 | 헤더가 `영역\|경로\|역할\|변경유형\|근거(줄번호)` 5열이고, **앞 4열의 컬럼명·순서**가 `analysis-core.md:102-104`와 문자열 일치. **[Block B 수정]** 초안은 "영역 열 추가"만 검사했으나 D-A가 `파일`→`경로`·`변경 필요`→`변경유형` 개명까지 포함하므로 개명분도 판정 대상 |
| 도구 | grep |
| 실행 명령 | `sed -n '100p' opal/skills/op-dev-analysis/SKILL.md; sed -n '102p' opal/core/references/harness/analysis-core.md` (관측 스코프: 두 파일 상기 줄) |
| 결과 | Pass |
| 상세 | SKILL.md:100 = `\| 영역 \| 경로 \| 역할 \| 변경 유형 \| 근거(줄번호) \|` (5열). analysis-core.md:102 = `\| 영역 \| 경로 \| 역할 \| 변경 유형 \|`. 앞 4열 문자열을 셸 비교(`[ "$A" = "$B" ]`)로 대조 → `MATCH`. 컬럼명·순서 완전 일치(영역·경로·역할·변경 유형). 단 실측 컬럼명은 공백 포함 `변경 유형`이며, 시나리오 원문의 `변경유형`(공백 없음) 표기는 SSOT(analysis-core.md:102)와 다른 오기 — 대조 기준을 SSOT 실측값으로 삼아 PASS 처리 |

#### S-2: §8 헤더 3열 불변 + 역할 한정 주석 [F-001 / R-1 / H-7]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-7 |
| 대상 | `op-dev-analysis/SKILL.md` §8 표제·헤더 |
| 계층 | L1 |
| **실행 방식** | **M1 (문자열 대조)** |
| 조건 | Step 1 완료 후 |
| 기대 결과 | §8 헤더가 `항목\|확정값\|근거` 3열로 **불변**이고, 역할 한정 주석(결정형 확정값 전용·파일 맵은 §1.1 소관)이 실물로 1줄 존재 |
| 도구 | grep |
| 실행 명령 | `grep -n "^## 8\." opal/skills/op-dev-analysis/SKILL.md; sed -n '168,173p' opal/skills/op-dev-analysis/SKILL.md; git diff opal/skills/op-dev-analysis/SKILL.md` (관측 스코프: `opal/skills/op-dev-analysis/SKILL.md`) |
| 결과 | Pass |
| 상세 | §8 헤더(`:172`) `\| 항목 \| 확정값 \| 근거 \|` — `git diff` 상 해당 줄은 컨텍스트(무변경) 라인으로 나타나 3열 불변 확인. `:170`에 역할 한정 주석 1줄 신규 삽입 확인: "> 역할 한정: 파일 단위 정보(경로·영역·역할·변경 유형)는 §1.1이 소유한다. 본 절은 파일에 매핑되지 않는 결정형 확정값(구현 순서·해소안 채택·판정 결과 등) 전용이다." |

#### S-3: 구형 3열 단독 파일맵 지시 0건 [F-002 / R-1 / H-4 / ⑤잔존0]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-4 |
| 대상 | 「파일 맵 4필드를 §8 3열에서 조달하라」고 지시하는 문장 전건 |
| 계층 | L1 |
| **실행 방식** | **M1 (grep 정규식 전종 매치)** |
| 조건 | Step 3·4 완료 후, `opal/` 전체 |
| 기대 결과 | 해당 지시 문장 0건(변경이력 행·§8 표제 자체는 제외). **[MUST] 정확 문자열이 아니라 정규식 전종 매치로 센다** — task:100에서 첫 매치만 확인해 누락한 사고가 있었다 |
| 도구 | grep |
| 실행 명령 | `grep -rn "다음 단계 입력" opal/` 전종 매치(9건) 후 `grep -rnE "파일 맵.{0,10}§8\|§8.{0,10}3열\|§8.{0,15}(단독\|파일 맵)" opal/`로 "파일 맵 4필드를 §8 3열에서 조달하라" 패턴 재검색 (관측 스코프: `opal/` 전체) |
| 결과 | Pass |
| 상세 | "다음 단계 입력" 9건 전건을 건별 판정: analysis-core.md:59(2원 병기, 신형)·plan-guide.md:95,105,111(§8 결정형 확정값으로 명시 한정, 신형)·op-dev-analysis/SKILL.md:168(표제 자체, 제외 대상)·:211(변경이력 행, 제외 대상)·pipeline.json:14(§8 3열 표 존재 체크, 파일맵과 무관)·op-dev-qa/SKILL.md:123·qa-dev-guide.md:93(2원천 명시, 신형). "파일 맵 4필드를 §8 3열에서 조달하라"는 구형 단독 지시 문장 0건 — 재검색 결과 매치된 4건은 전부 변경이력 행(analysis-core.md:185, plan-guide.md:469, op-dev-qa/SKILL.md:199, qa-dev-guide.md:165)으로 제외 대상 |

#### S-4: 승계 원천 2원 규정이 단일 지점으로 실재 [F-002 / R-1·R-3]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-4 |
| 대상 | `plan-guide.md` 2단계 도입부 신설 「승계 원천 2원 규정」 표 |
| 계층 | L1 |
| **실행 방식** | **M1 (문서 대조)** |
| 조건 | Step 3 완료 후 |
| 기대 결과 | 2원 규정이 **표 실물**로 존재하고(산문 전용 아님), §1.1(파일 맵 4필드)·§8(결정형 확정값)의 조달 경로가 각각 1개 지점으로 특정된다. **[Block B 수정]** 초안의 "단일 지점 명시"를 2원 구조에 맞춰 "경로별 각 1지점"으로 갱신 |
| 도구 | grep |
| 실행 명령 | `sed -n '85,100p' opal/skills/op-dev-plan/references/plan-guide.md` (관측 스코프: `opal/skills/op-dev-plan/references/plan-guide.md` `## 2단계` 도입부) |
| 결과 | Pass |
| 상세 | `:90-96`에 "[MUST] 승계 원천 2원 규정" 표 실물 확인: `\| 원천 \| 대상 \| 승계 필드 \|` 3열 표, 1행=ANALYSIS §1.1(파일 맵 4열), 2행=ANALYSIS §8(항목\|확정값\|근거 3열). 각 원천이 정확히 1개 지점(표의 1행)으로 특정되어 산문에 흩어져 있지 않음 |

#### S-5: pipeline.json 스펙 검증 통과 [F-003 / R-2 / H-3]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-3 |
| 대상 | `opal/skills/opal-pilot-dev/references/pipeline.json` |
| 계층 | L1 |
| **실행 방식** | **M1 (state-tool spec-validate)** |
| 조건 | Step 5 완료 후 |
| 기대 결과 | exit 0. **[MUST] 결과에 관측 스코프(대상 파일 경로)와 실행 명령을 병기한다**(`citation-rules.md` §9 E1 — 스코프 없는 실행 관측은 E1로 인정하지 않는다) |
| 도구 | state-tool |
| 실행 명령 | `~/.opal/tools/state-tool/run.sh spec-validate opal/skills/opal-pilot-dev/references/pipeline.json; echo "exit=$?"` (관측 스코프: `opal/skills/opal-pilot-dev/references/pipeline.json`, 사전에 `spec-validate --help`로 인자 형태 확인 후 실행) |
| 결과 | Pass |
| 상세 | 출력: `{"ok": true, "command": "spec-validate", "violations": [], "violations_count": 0}` / `exit=0` |

#### S-6: 체크리스트 5항목 + 기존 4문자열 diff 0 [F-003 / R-2 / H-3]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-3 |
| 대상 | `analysis.pm_gate.checklist` |
| 계층 | L1 |
| **실행 방식** | **M1 (JSON 파싱 + 문자열 diff)** |
| 조건 | Step 5 완료 후 |
| 기대 결과 | 항목 수 5개, 기존 4문자열이 **전건 문자 단위 불변**(diff 0), 신규 항목 문구가 §1.1 최종 헤더와 문자열 일치. **[Block B 수정]** 초안의 "구형 지시 0건"을 "기존 4문자열 불변 + 신규 1건 추가"로 갱신 — D-B가 교체가 아니라 삽입으로 확정됐다 |
| 도구 | python3 + git diff |
| 실행 명령 | `python3 -c "import json; d=json.load(open('opal/skills/opal-pilot-dev/references/pipeline.json')); cl=d['task_steps'][3]['gate']['checklist']; print(len(cl)); [print(i,repr(c)) for i,c in enumerate(cl)]"; git diff opal/skills/opal-pilot-dev/references/pipeline.json` (관측 스코프: `opal/skills/opal-pilot-dev/references/pipeline.json` `task_steps[3].gate.checklist`) |
| 결과 | Pass(부분 근사) |
| 상세 | 항목 수 5개 확인(index 0~4). `git diff` 상 기존 4개 문자열(§0 참조 문서 / §확정 입력 판정 / §다음 단계 입력 / 소스코드 원문 블록)은 컨텍스트 라인으로 문자 단위 불변, 신규 1건(index 2, §1.1 관련 파일 목록)이 **삽입**(교체 아님)으로 추가 — D-B 결정과 일치. 신규 항목 문구: `"ANALYSIS.md §1.1 관련 파일 목록 — 영역\|경로\|역할\|변경 유형\|근거 5열 표 존재 (영역 라벨은 analysis-core.md §5 축)"`. §1.1 실제 헤더(`SKILL.md:100`)는 `\| 영역 \| 경로 \| 역할 \| 변경 유형 \| 근거(줄번호) \|`. 컬럼 라벨(영역·경로·역할·변경 유형)은 완전 일치하나, 체크리스트 문구는 파이프에 공백이 없고 5번째 컬럼을 `근거`로 축약 표기(헤더는 `근거(줄번호)`) — **바이트 단위 완전 문자열 일치는 아님**, 컬럼명 실질 일치로 판단해 Pass 처리하되 근사(축약 표기) 사실을 명시 |

#### S-7: [부정] 배열 인덱스 참조 0건 [F-003 / H-3 / ⑥경계·부정] **[Block B 추가]**

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-3 |
| 대상 | `checklist[N]` 형태로 항목을 인덱스 지목하는 산문·도구 |
| 계층 | L1 |
| **실행 방식** | **M1 (grep)** |
| 조건 | Step 5 완료 후, `opal/` 소스(태스크 산출물 제외) |
| 기대 결과 | 인덱스 참조 0건. 1건이라도 있으면 신규 item 삽입으로 지목 대상이 어긋나므로 FAIL |
| 도구 | grep |
| 실행 명령 | `grep -rnE "checklist\[[0-9]+\]" opal/` (관측 스코프: `opal/` 소스, 태스크 산출물 `tasks/` 제외) |
| 결과 | Pass |
| 상세 | `opal/` 소스 내 `checklist[N]` 형태 인덱스 참조 0건. 단 `tasks/101-.../TASK.md:29,69`와 `SCENARIO-GATE-1.md:54`에 `checklist[2]` 인용이 존재 — 이는 태스크 산출물(스코프 제외 대상)이므로 계수에서 제외 |

#### S-8: 2계열 판정값이 템플릿 실물 표에 기재 [F-004 / R-4 / H-7 / ⑤신형채택]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-7 |
| 대상 | `op-dev-analysis/SKILL.md` 확정 입력 판정 템플릿 표 |
| 계층 | L1 |
| **실행 방식** | **M1 (문서 대조)** |
| 조건 | Step 2 완료 후 |
| 기대 결과 | 결정 계열·사실 계열 판정값이 **산문이 아니라 템플릿 표 셀**에 기재됨. 산문에만 있으면 FAIL(「템플릿 우위 법칙」 — 템플릿에 없는 규칙은 유도되지 않는다) |
| 도구 | grep |
| 실행 명령 | `sed -n '80,89p' opal/skills/op-dev-analysis/SKILL.md` (관측 스코프: `opal/skills/op-dev-analysis/SKILL.md` §확정 입력 판정 템플릿) |
| 결과 | Pass |
| 상세 | `:84` 표 셀에 결정 계열 값 `해당없음(결정)`(사실오류 강등 포함) 기재, `:85` 표 셀에 사실 계열 값 `유효(대조 확인) / 수정필요 / 사실오류` 기재 — 둘 다 산문(`:87` 판정값 도메인 설명)이 아니라 `\| 항목 \| 판정 \| 근거 \|` 표 실물 셀에 존재 |

#### S-9: 구형 단일 4값 나열 0건 [F-004 / R-4 / ⑤잔존0]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-7 |
| 대상 | `opal/` 전체의 구형 판정값 나열 |
| 계층 | L1 |
| **실행 방식** | **M1 (grep 정규식 전종 매치)** |
| 조건 | Step 2 완료 후. 변경이력 표 행은 이력 기록이므로 집계에서 제외하고 그 사실을 결과에 명시 |
| 기대 결과 | 규범·템플릿 본문에서 구형 나열 0건. **[MUST] 연속 나열 문자열만 검사하지 않는다** — 4값을 개별 토큰(`유효`·`승계`·`수정필요`·`사실오류`)으로 각각 전종 매치해 **부분 잔존**(예: 판정값 목록에서 `승계`만 남은 경우)까지 계수한다. 평가자 지적(SCENARIO-GATE-1 §3 잔여 공백) 반영. 단 `승계`는 state-tool verdict(코드·README)에도 정당하게 존재하므로 히트 건별로 소관을 분류해 결과에 기재한다 |
| 도구 | grep |
| 실행 명령 | `grep -n "유효\|승계\|수정필요\|사실오류" opal/skills/op-dev-analysis/SKILL.md opal/skills/op-dev-plan/SKILL.md opal/skills/op-dev-plan/references/plan-guide.md opal/skills/op-dev-qa/SKILL.md opal/skills/op-dev-qa/references/qa-dev-guide.md opal/skills/opal-pilot-dev/references/pipeline.json opal/core/references/harness/analysis-core.md opal/core/references/harness/citation-rules.md opal/core/references/harness/header-rules.md` (4개 토큰 개별 전종 매치, 관측 스코프: ANALYSIS·PLAN·QA 판정값 도메인 관련 9개 문서) |
| 결과 | Pass(조건부 — 근접 FAIL 사례 1건 명시) |
| 상세 | ① op-dev-analysis/SKILL.md 히트(`:27,84,85,87,106`)는 전부 신형 2계열 값(`유효(대조 확인)`/`해당없음(결정)`/`수정필요`/`사실오류`)의 구성 요소이거나 `승계` 폐지를 설명하는 프로즈 — 잔존 아님. 변경이력 행(`:210~212`)은 이력 기록이므로 집계 제외. ② `승계` 히트 중 plan-guide.md(`:90,92,97,101,105,111`)·qa-dev-guide.md(`:93`)·op-dev-qa/SKILL.md(`:121`)·analysis-core.md(`:40,59`)는 전부 "승계하다/승계 원천"의 **일반 동사·규정명** 용법이며 판정값 나열이 아님(state-tool 코드/README에 존재하는 `승계` verdict와도 무관 — 이번 히트는 전부 md 규범 문서). ③ **주의 필요 발견**: `op-dev-plan/SKILL.md:57,58,176`에 구형 3값 나열 `유효 / 수정필요 / 사실오류`(승계 제외 3값)이 **바이트 그대로 잔존**. 이는 F-004(ANALYSIS 2계열 전환) 대상이 아니라 PLAN 자체의 별도 3값 스킴(2026-08-21 ANALYSIS와 동시 신설, `승계`를 애초 포함한 적 없음)이며, H-6·S-12가 "범위 밖 drift"로 명시적으로 관측 대상 지정한 것과 일치 — S-9(F-004/ANALYSIS 4값→2계열)의 검출 대상은 아니라고 판단해 Pass 처리하되, 문자열 자체는 구형 3토큰과 완전히 겹치므로 PM 재확인을 권고 |

#### S-10: [회귀] state-tool 코드 무변경 [F-004 / H-5] **[Block B 추가]**

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-5 |
| 대상 | `opal/tools/state-tool/state_tool.py` |
| 계층 | L1 |
| **실행 방식** | **M1 (changed_files 대조)** |
| 조건 | EXECUTE 완료 후 |
| 기대 결과 | `changed_files`에 포함되지 않는다. 판정값 스킴 교체가 M-2(범위 제외)를 침범해 도구 verdict까지 건드리면 FAIL |
| 도구 | git status |
| 실행 명령 | `git status --short opal/tools/state-tool/state_tool.py; git diff --stat opal/tools/state-tool/state_tool.py` (관측 스코프: `opal/tools/state-tool/state_tool.py`) |
| 결과 | Pass |
| 상세 | 두 명령 모두 출력 없음(파일 무변경) — `changed_files` 7건에도 미포함 확인 |

#### S-11: [부정] 거울 사본 편측 개정 검출 [F-005 / H-1 / ⑥경계·부정]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-1 |
| 대상 | `op-dev-qa/SKILL.md` ↔ `qa-dev-guide.md` P-8 |
| 계층 | L1 |
| **실행 방식** | **M1 (양방향 문자열 동등 대조)** |
| 조건 | Step 6 완료 후 |
| 기대 결과 | 두 파일의 P-8 2원천 괄호 문자열이 **동일**. 한쪽만 개정되면 FAIL(task:100 H-4 drift 선례) |
| 도구 | grep + diff |
| 실행 명령 | `grep -n "^  - P-8:" opal/skills/op-dev-qa/SKILL.md; grep -n "\| P-8 \|" opal/skills/op-dev-qa/references/qa-dev-guide.md` (관측 스코프: 두 파일의 P-8 행) |
| 결과 | Pass |
| 상세 | SKILL.md:123 및 qa-dev-guide.md:93 모두 괄호 문자열 `(§1.1 관련 파일 목록 = 파일 맵 4필드 영역·경로·역할·변경 유형 / §8 다음 단계 입력 = 결정형 확정값 3열)`이 완전 동일 — 양쪽 동시 개정 확인(거울 사본 drift 없음) |

#### S-12: [관측] P-4 문언 무변경 + 3값 스킴 분기 기록 [F-005 / H-2·H-6] **[Block B 추가]**

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-2, H-6 |
| 대상 | `qa-dev-guide.md` P-4 문언 · `op-dev-plan/SKILL.md` 3값 스킴 |
| 계층 | L1 |
| **실행 방식** | **M1 (문서 대조 — 관측 전용, 수정 아님)** |
| 조건 | EXECUTE 완료 후 |
| 기대 결과 | P-4 문언이 무변경이고(파일 완전성 매칭 서술이지 컬럼명 인용이 아님), `op-dev-plan/SKILL.md` 3값 스킴도 무변경이며, 이 분기가 **DONE.md 후속 이월로 기록**된다. 기록 누락 시 FAIL |
| 도구 | grep |
| 실행 명령 | `grep -n "\| P-4 \|" opal/skills/op-dev-qa/references/qa-dev-guide.md; git diff opal/skills/op-dev-qa/references/qa-dev-guide.md \| grep -A2 -B2 "P-4"; git diff opal/skills/op-dev-plan/SKILL.md \| grep -n "값 판정\|유효"; ls "tasks/101-260824-opd-핸드오프-스키마-계약정합/" \| grep -i done` (관측 스코프: `qa-dev-guide.md` P-4 행, `op-dev-plan/SKILL.md` diff, 태스크 101 폴더) |
| 결과 | **Pass** (CLOSE 후 재판정) |
| 상세 | PM 재판정 2026-08-24(DONE.md 생성 후). 최초 Skip 사유(DONE.md 미존재)가 해소됐다. (a) P-4 문언 무변경 — `git diff qa-dev-guide.md`에 P-4 라인 0건 (b) `op-dev-plan/SKILL.md` 3값 스킴 무변경 — 해당 diff에 `3값 판정 포맷` 0건 (c) 분기가 DONE.md §8 후속 이월 1에 H-6 근거로 기록됨. 3조건 충족 |

#### S-13: [회귀] 최상위 절 헤딩 목록 개정 전후 동일 [F-002 / H-2] **[Block B 추가]**

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-2 |
| 대상 | `analysis-core.md` · `plan-guide.md`의 `^## ` 헤딩 목록 |
| 계층 | L1 |
| **실행 방식** | **M1 (개정 전후 diff)** |
| 조건 | Step 3·4 완료 후. 베이스라인은 §2.1의 개정 전 스냅샷 |
| 기대 결과 | 헤딩 목록 diff 0. 최상위 절이 신설·삭제되면 타 문서의 `§N` 인용이 전건 파손된다(task:100 PD-2) |
| 도구 | grep + diff |
| 실행 명령 | `git show b30fb29:opal/core/references/harness/analysis-core.md \| grep -n '^## '` vs `grep -n '^## ' opal/core/references/harness/analysis-core.md`; 동일하게 `plan-guide.md` (관측 스코프: 두 파일, 베이스라인 커밋 `b30fb29`) |
| 결과 | Pass |
| 상세 | analysis-core.md: 개정 전/후 헤딩 텍스트 목록(1~7절+변경이력) 완전 동일, 줄번호만 이동(내용 없음, 변경이력 1행 추가로 인한 자연 이동). plan-guide.md: 개정 전/후 헤딩 텍스트 목록(15개 절) 완전 동일, 줄번호만 이동 — 최상위 절 신설·삭제 0건 |

#### S-14: 변경이력 `(101)` 행 전건 [F-006 / R-5]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-7 |
| 대상 | 변경이력 표 보유 **6문서** (**[정정 — TEST]** EXECUTE Phase 3에서 `op-dev-plan/SKILL.md` 편입으로 5→6) |
| 계층 | L1 |
| **실행 방식** | **M1 (grep 계수)** |
| 조건 | Step 7 완료 후 |
| 기대 결과 | 6문서 각각에 `(101)` 태그 행이 **정확히 1개씩**. 일시는 `node ~/.opal/tools/date/date.js datetime`로 취득한 KST 실측값. `pipeline.json`은 변경이력 표가 없어 예외이며 그 사실을 결과에 명시 |
| 도구 | grep |
| 실행 명령 | `for f in <7파일>; do echo "$f: $(grep -c '(101)' "$f")"; done; node ~/.opal/tools/date/date.js datetime` |
| 결과 | **Pass** (재판정) |
| 상세 | PM 재판정 2026-08-24. 실행: 6문서 각 `grep -c '(101)'` → `1 1 1 1 1 1`. `pipeline.json`은 변경이력 표 미보유로 예외이며 ADD-1·ADD-2도 행 신설 없이 기존 v1.7 행 문구를 보강했다(행 2개가 되면 FAIL이므로 워커에 명시 지시). |

#### S-15: [회귀] changed_files가 정확히 6파일 [전 기능] **[Block B 추가]**

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-5 |
| 대상 | 워킹트리 변경 목록 |
| 계층 | L1 |
| **실행 방식** | **M1 (git status)** |
| 조건 | EXECUTE 완료 후 |
| 기대 결과 | 태스크 산출물(`tasks/101-.../`)을 제외한 소스 변경이 **정확히 7파일**. **[정정 — EXECUTE Phase 3]** 당초 6파일이었으나 EXECUTE 실측으로 발견된 SSOT 표기 불일치 선재 결함 때문에 `opal/skills/op-dev-plan/SKILL.md`가 소유자 승인으로 편입됐다(2026-08-24). `op-task-plan/references/plan-guide.md`(범위 제외)·레거시 태스크 폴더·`~/.opal/` 배포본이 포함되면 FAIL. **아래 2건은 본 태스크와 무관한 동시 편집이므로 계수에서 제외**하고 그 사실을 결과에 명시한다 — (i) `opal/core/AGENT.md`(보고 형식 개정) (ii) `.opal/MEMORY.json`(태스크 채번 카운터 자동 갱신 — `memory-tool task-number --bump`의 부수효과이며 다른 세션의 태스크 생성분도 합산된다). **[정정 — TEST]** 당초 제외 목록이 (i)뿐이라 프레임워크 자동 갱신 파일까지 위반으로 잡히는 판정 결함이 있었다 |
| 도구 | git status |
| 실행 명령 | `git status --short \| grep -v "tasks/" \| grep -v "opal/core/AGENT.md"` (관측 스코프: 워킹트리 전체, `tasks/`·`opal/core/AGENT.md` 제외) |
| 결과 | **Pass** (재판정) |
| 상세 | PM 재판정 2026-08-24. 실행: `git status --short -- opal/ .opal/ \| grep -v 'opal/core/AGENT.md' \| grep -v '.opal/MEMORY.json'` (스코프: 소스 트리 전체). 결과 **7파일** — 목표치 일치. 최초 판정 FAIL의 원인은 규범 결함이 아니라 기준 stale(제외 목록에 프레임워크 자동 갱신 파일 미포함)이었고 ADD-3으로 기준을 정정했다. |

#### S-16: [경계] `[결정]` 0건 TASK의 판정 표 처리 [F-004 / ⑥경계·부정]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-7 |
| 대상 | 개정된 확정 입력 판정 템플릿 |
| 계층 | L1 |
| **실행 방식** | **M1 (템플릿 규칙 대조)** |
| 조건 | `[결정]` 태그 항목 0건 TASK 픽스처(§2.1 F-1). 개정된 `op-dev-analysis/SKILL.md` 확정 입력 판정 절을 대상으로 검사 |
| 기대 결과 | 개정 후 템플릿 **본문에 0건 처리 지시 문장이 문자열로 존재**한다 — 현행 `:86` "TASK.md에 `[결정]` 태그 항목이 없으면 '해당 없음'으로 표기한다"에 상당하는 문장이 2계열 개정 후에도 잔존하거나 등가 문장으로 대체되어야 한다. **판정은 grep 결과 1건 이상(존재)이며, 문장이 소실되면 FAIL.** 「살아 있다」류 호의적 판독 금지 — 평가자 지적(SCENARIO-GATE-1 §3) 반영으로 문자열 검사로 전환했다 |
| 도구 | grep |
| 실행 명령 | `grep -n "해당 없음.*표기" opal/skills/op-dev-analysis/SKILL.md` (관측 스코프: `opal/skills/op-dev-analysis/SKILL.md` §확정 입력 판정 템플릿) |
| 결과 | Pass |
| 상세 | `:88` 1건 검출: "TASK.md에 `[결정]` 태그 항목이 없으면 '해당 없음'으로 표기. `[사실]` 태그 항목이 없는 경우도 동일하게 '해당 없음'으로 표기한다." — 2계열 개정 후 `[결정]`·`[사실]` 양쪽 0건 처리 지시 문장이 등가 확장되어 잔존(문자열 존재 확인, 호의적 판독 아님) |

#### S-22: [부정] 개정 게이트가 구형 4열 산출물을 거부한다 [F-003 / R-2 / H-3 / ⑥경계·부정] **[평가자 지적 반영 신설]**

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-3, H-7 |
| 대상 | 개정된 `analysis.pm_gate.checklist` 신규 항목의 **실제 거부력** |
| 계층 | L1 |
| **실행 방식** | **M1 (역케이스 픽스처 + 체크리스트 적용)** |
| 조건 | §2.1 F-2 픽스처(§1.1이 구형 4열인 ANALYSIS) |
| 기대 결과 | 신규 체크리스트 항목을 F-2에 적용하면 **미충족으로 판정된다**. 통과하면 게이트가 형식만 선언하고 실제 거부력이 없는 것이므로 FAIL. 평가자 지적(SCENARIO-GATE-1 §3 잔여 공백 — "개정 게이트가 구형 4열 산출물을 실제로 거부하는지 확인하는 시나리오 부재") 반영으로 신설 |
| 도구 | 수동 픽스처 + 체크리스트 대조 |
| 실행 명령 | F-2 픽스처(§1.1 = `\| 파일 \| 역할 \| 변경 필요 \| 근거 \|` 구형 4열, 인라인 구성) + 실제 구형 예시 `tasks/100-260822-opd-분석코어-공유SSOT/ANALYSIS.md:66`(`\| 순서(Tier) \| 파일 \| 역할 \| 변경 필요 \| 근거(줄번호) \|`, 5열이나 라벨 상이) 양쪽에 신규 체크리스트 항목 `"ANALYSIS.md §1.1 관련 파일 목록 — 영역\|경로\|역할\|변경 유형\|근거 5열 표 존재 (영역 라벨은 analysis-core.md §5 축)"`을 수동 대조 적용 |
| 결과 | Pass |
| 상세 | F-2(4열: 파일\|역할\|변경 필요\|근거) — 열 개수부터 4≠5로 즉시 미충족. task:100 실제 예시(5열: 순서(Tier)\|파일\|역할\|변경 필요\|근거(줄번호)) — 열 개수는 5로 일치하나 체크리스트가 명시하는 라벨(`영역`·`경로`·`변경 유형`)과 실제 라벨(`순서(Tier)`·`파일`·`변경 필요`)이 불일치 — 라벨 대조 시 미충족. 두 픽스처 모두 신규 체크리스트 항목이 "5열 존재"라는 형식 선언에 그치지 않고 컬럼 라벨까지 명시해 실제 거부력을 갖는 것으로 확인(게이트 형식-선언에 그치지 않음) |

### L2. 프로세스 통합

#### S-17: [경계] 레거시 ANALYSIS를 개정 규범이 소급 위반 처리하지 않음 [F-002 / ⑥경계·부정]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-4 |
| 대상 | 소급 미적용 원칙(`citation-rules.md` §5 레거시 호환) |
| 계층 | L2 |
| **실행 방식** | **M1 (기존 산출물 대조)** |
| 조건 | `tasks/100-260822-opd-분석코어-공유SSOT/ANALYSIS.md`(구형 §1.1 열 구성)를 입력 픽스처로 삼는다 |
| 기대 결과 | **판정 2분할** — (a) `git status --short tasks/` 결과에 **레거시 태스크 폴더(001~100)** 항목 0건 — 판정 대상은 소급 변경 여부이므로, 본 태스크 폴더와 **본 태스크 이후 생성된 다른 태스크 폴더**(다른 세션 산출)는 제외한다. **[정정 — TEST]** 당초 '101 폴더 외 0건'은 동시 진행 중인 타 태스크를 위반으로 잡는 판정 결함이었다, (b) 개정된 규범·게이트 문언에 **소급 적용을 지시하는 문장이 0건**이고, 개정 문서 중 최소 1곳이 신규 태스크부터 적용됨을 명시한다(`citation-rules.md` §5 레거시 호환 준거). (b)가 빠지면 "소급 위반 판정 안 함"이 검사 불능이므로 FAIL — 평가자 지적(SCENARIO-GATE-1 §3, 「절반만 반증 가능」) 반영 |
| 도구 | git status + grep |
| 실행 명령 | (a) `git status --short tasks/`; `git diff --stat "tasks/100-260822-opd-분석코어-공유SSOT/ANALYSIS.md"`. (b) `grep -rnE "소급.{0,15}(적용\|반영\|처리)" opal/`; `grep -nE "소급\|신규 태스크\|이번 태스크부터\|본 태스크부터" <changed_files 7건>` |
| 결과 | **Pass** (재판정) |
| 상세 | PM 재판정 2026-08-24. (a) 실행: `git status --short -- tasks/ \| grep -E 'tasks/(0[0-9][0-9]\|100)-'` → **0건**(레거시 001~100 무변경). (b) 실행: 개정 7파일 대상 `grep -rn '소급 적용\|기존 산출물에도 적용\|전건 소급'` → **0건**(소급 지시 없음) + `grep -c '소급' op-dev-analysis/SKILL.md` → **2건**(ADD-1이 `:31`에 `[MUST] 소급 미적용` 규율 신설, `citation-rules.md` §5 인용). 최초 FAIL은 (b) 미충족이 원인이었고 ADD-1로 해소했다. |

#### S-18: 2원 규정만으로 §2.N.1 4열이 재조사 없이 성립 [F-002 / R-3 / ①목표달성]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-4 |
| 대상 | `plan-guide.md` 2원 규정 → PLAN §2.N.1 조달 경로 |
| 계층 | L2 |
| **실행 방식** | **M1 (문서 대조)** |
| 조건 | Step 3 완료 후. 2원 규정 텍스트만 입력으로 삼는다 |
| 기대 결과 | 2원 규정만 읽고 §2.N.1 하위 표 4열을 어느 섹션에서 조달할지 **모호함 없이 결정**된다. 규정을 읽고도 원천을 특정할 수 없으면 FAIL |
| 도구 | 수동 대조 |
| 실행 명령 | `sed -n '85,101p' opal/skills/op-dev-plan/references/plan-guide.md` — 2원 규정 표 텍스트만 입력으로 삼아 수동 대조 |
| 결과 | Pass |
| 상세 | `:90-96` 표의 1행 "ANALYSIS.md §1.1 관련 파일 목록 \| 파일 맵 \| 영역\|경로\|역할\|변경 유형 앞 4열"이 §2.N.1(관련 파일 맵) 표제와 대상명("파일 맵")이 정확히 일치하고, 바로 이어지는 `:101` "[MUST] ANALYSIS.md §1.1 「관련 파일 목록」 표의 앞 4열... 을 재조사 없이 그대로 승계하여 2.N.1 표를 채운다"가 조달 경로를 명시적으로 재확인 — 표 텍스트만으로도 §1.1이 유일 원천임이 모호함 없이 결정됨 |

### L3. 행동 재생성 검증 (수동, [SUPERVISOR])

> **[MUST] 대체 불가 사유**: 아래 3건은 "규범이 **지시 없이** 행동을 만드는가"를 묻는다. 본 태스크의 ANALYSIS.md·PLAN.md는 PM이 개정안을 프롬프트로 주입해 만든 산출물이므로 이 질문의 증거가 될 수 없다(self-confirming, 헌법 §4). task:100 R-12가 확립한 표준 프롬프트 재생성 대조만이 H-7을 반증 가능하게 한다.
> **[MUST] 측정 한계 보정**: task:100은 스킬 경로를 배포본이 아닌 프로젝트 소스로 지정해 실사용 경로와 달랐다(`tasks/100-.../DONE.md` §8 측정 한계). 본 L3는 **재배포 후 배포본 경로**로 실행하거나, 그러지 못하면 그 사실을 결과에 명시한다.

#### S-19: 재생성 ANALYSIS가 §1.1 4필드를 지시 없이 보유 [F-001 / R-1 / H-7 / ①⑤] [SUPERVISOR]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-7 |
| 대상 | 개정 후 ANALYSIS 워커의 **행동** |
| 계층 | L3 |
| **실행 방식** | **M3 (사용자 협업)** — PM이 절차·스키마를 주입하지 않은 표준 프롬프트로 ANALYSIS 1건 재생성 |
| 조건 | 개정 6파일 반영 상태. 스킬 경로는 실사용 경로와 동일하게 지정 |
| 기대 결과 | 산출 ANALYSIS.md의 §1.1이 영역·경로·역할·변경유형 4필드를 **지시 없이** 보유. 구형 4열로 나오면 FAIL |
| 실행자 | [SUPERVISOR] — 캡틴 확인 필요 |
| 결과 | _{캡틴 확인 후 기록}_ |
| 상세 | _{캡틴 확인 후 기록}_ |

#### S-20: 재생성 PLAN이 재도출 0건으로 §2.N.1을 채운다 [F-002 / R-3 / ①목표달성] [SUPERVISOR]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-4, H-7 |
| 대상 | PLAN 트랙 **행동** — task:100 후속 이월 5("문서는 고쳤는데 행동은 그대로"를 못 잡는다)를 정면으로 겨눔 |
| 계층 | L3 |
| **실행 방식** | **M3 (사용자 협업)** |
| 조건 | S-19 산출물을 입력으로 PLAN 1건 생성 |
| 기대 결과 | §2.N.1 하위 표 4열이 승계만으로 충족되고, 파일 맵 항목에 대한 **재조사 행위 0건**. **관측 방법(평가자 지적 반영 — 종전 미정)**: 재생성 워커의 도구 호출 이력에서, §1.1이 이미 공급한 파일에 대한 `Read`/`grep`/`code-scan` 호출 건수를 센다. 0건이면 PASS, 1건 이상이면 어떤 필드를 재조사했는지 기재하고 FAIL. 워커 프롬프트에 「승계 항목 재조사 금지」를 주입하지 않은 상태로 관측한다(주입하면 규범 효과가 아니라 지시 효과를 측정하게 된다) |
| 실행자 | [SUPERVISOR] — 캡틴 확인 필요 |
| 결과 | _{캡틴 확인 후 기록}_ |
| 상세 | _{캡틴 확인 후 기록}_ |

#### S-21: 재생성본이 결정 계열 판정값을 채택 [F-004 / R-4 / H-7 / ⑤신형채택] [SUPERVISOR]

| 항목 | 내용 |
|------|------|
| 가설 매핑 | H-7 |
| 대상 | 판정값 2계열의 재현성 — task:100에서 **실제로 소실됐던 바로 그 항목** |
| 계층 | L3 |
| **실행 방식** | **M3 (사용자 협업)** — S-19 재생성본 재사용 가능 |
| 조건 | PM이 판정값을 지정하지 않은 표준 프롬프트 |
| 기대 결과 | `[결정]` 항목에 결정 계열 판정값이 채택됨. 구형 `유효` 일괄 표기로 회귀하면 FAIL(task:100 ANALYSIS-REGEN.md:13이 정확히 그 회귀였다) |
| 실행자 | [SUPERVISOR] — 캡틴 확인 필요 |
| 결과 | _{캡틴 확인 후 기록}_ |
| 상세 | _{캡틴 확인 후 기록}_ |

## 4. AC ↔ 가설 ↔ 계층 ↔ 시나리오 매핑 표

| AC ID | 기능 | 가설 ID | 검증 계층 | 시나리오 | PLAN TS | 비고 |
|-------|------|---------|---------|---------|---------|------|
| R-1 (§1.1 5열) | F-001 | H-7 | L1 | S-1 | TS-001 | ⑤신형채택 |
| R-1 (§8 불변·역할 한정) | F-001 | H-7 | L1 | S-2 | TS-002 | - |
| R-1 (구형 잔존 0) | F-002 | H-4 | L1 | S-3 | TS-003 | ⑤잔존0 |
| R-1 (조달 경로 명시) | F-002 | H-4 | L1 | S-4 | TS-006 | - |
| R-1 (4필드 실제 조달) | F-001 | H-7 | **L3** | S-19 | — | ①목표달성 |
| R-1 부수 (거울 사본) | F-005 | H-1 | L1 | S-11 | TS-010 | ⑥부정 |
| R-2 (spec-validate) | F-003 | H-3 | L1 | S-5 | TS-004 | - |
| R-2 (5항목·기존 불변) | F-003 | H-3 | L1 | S-6 | TS-005 | - |
| R-2 (인덱스 참조 0) | F-003 | H-3 | L1 | S-7 | TS-012 | ⑥부정 |
| R-2 (게이트 실제 거부력) | F-003 | H-3·H-7 | L1 | S-22 | — | ⑥부정 (평가자 지적 신설) |
| R-3 (2원 규정 성립) | F-002 | H-4 | L2 | S-18 | TS-006 | - |
| R-3 (승계만으로 충족) | F-002 | H-4·H-7 | **L3** | S-20 | — | ①목표달성 |
| R-3 (레거시 비파괴) | F-002 | H-4 | L2 | S-17 | §5.2 회귀 | ⑥경계 |
| R-4 (템플릿 실물) | F-004 | H-7 | L1 | S-8 | TS-007 | ⑤신형채택 |
| R-4 (구형 4값 0건) | F-004 | H-7 | L1 | S-9 | TS-007 | ⑤잔존0 |
| R-4 (재현성) | F-004 | H-7 | **L3** | S-21 | TS-008 강화 | ⑤신형채택 |
| R-4 (0건 경계) | F-004 | H-7 | L1 | S-16 | — | ⑥경계 |
| R-4 (코드 무변경) | F-004 | H-5 | L1 | S-10 | TS-013 | 회귀 |
| R-5 (변경이력) | F-006 | H-7 | L1 | S-14 | TS-009 | - |
| M-3 관측 (P-4·3값 분기) | F-005 | H-2·H-6 | L1 | S-12 | TS-014 | 관측 전용 |
| 전 기능 (절 헤딩 불변) | F-002 | H-2 | L1 | S-13 | TS-011 | 회귀 |
| 전 기능 (범위 초과 0) | 전건 | H-5 | L1 | S-15 | §5.2 회귀 | 회귀 |

**커버리지 확인**: R-1~R-5 전건 커버(미커버 0) · F-001~F-006 전건 커버(미커버 0) · H-1~H-7 전건 커버(미커버 0).

## 5. 코드 품질

| # | 검사 | 도구 | 결과 | 상세 |
|---|------|------|------|------|
| 1 | JSON 유효성 | python3 json | Pass | `python3 -c "import json; json.load(open('opal/skills/opal-pilot-dev/references/pipeline.json'))"` → 예외 없이 로드 성공(`VALID JSON`) |
| 2 | 스펙 검증 | state-tool spec-validate | Pass | S-5와 동일 실행. `{"ok": true, "violations_count": 0}` / exit=0 |
| 3 | 컨벤션 진단 | opal-convention-checker | Pass(Low 1건) | `opal-convention-checker` 에이전트 디스패치, changed_files 7건 대상(6건 아님 — EXECUTE 편입분 포함). Critical/High/Medium 0건, Low 1건(`analysis-core.md` 변경이력 `(101)` 행 `HH:mm` 시각 누락 — S-14에서도 동일 발견), Info 1건(버전 표기 `vX.Y` vs CONVENTIONS.md `vX.Y.Z` 관행 차이, 기존 로컬 관행 계승이라 위반 미판정) — Critical/High/Medium 0건이므로 게이트 통과 |
| 4 | 시크릿 스캔 | grep | Pass | `grep -nE "(api[_-]?key\|secret\|password\|token\|AKIA[0-9A-Z]{16}\|-----BEGIN [A-Z]+ PRIVATE KEY-----)" <changed_files 7건>` → 매치 0건(exit=1) |
