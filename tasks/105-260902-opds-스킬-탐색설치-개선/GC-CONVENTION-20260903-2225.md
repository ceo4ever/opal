# GC CONVENTION REPORT — 2026-09-03T22:25 KST

## 1. 헤더

- 실행 일시: 시작 2026-09-03 22:25 / 완료 2026-09-03 22:33 / 소요 약 8분
- 범위: `all` / 대상 파일 4개
  - `opal/tools/skill-registry/tests/test-scan-risk.js` (신규)
  - `opal/tools/skill-registry/skill-registry.js` (+207/-2)
  - `opal/skills/opal-skill-manager/SKILL.md` (+132/-21, 328줄)
  - `docs/ARCHITECTURE.md` (4곳 변경)
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` 존재 (v1.8.0, Read 완료) — 프레임워크 내장 공통 컨벤션 기본값 미적용, 본 문서 조항만 적용
- APPLY 수행 여부: N (진단 전담, readonly)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 1 |
| 심각도 분포 | Critical 0 / High 0 / Medium 0 / Low 1 / Info 0 |
| 자동 수정 가능 | 0 |
| 수동 조치 필요 | 1 (사전 존재 결함 — 이번 변경 범위 밖) |
| 파일별 상위 Top 5 | opal-skill-manager/SKILL.md (1건) |
| 카테고리별 빈도 | 문서화(frontmatter) (1 파일) |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 0 (빈도/새 카테고리 트리거 모두 미발동 — N=3 미달) |

**PM Gate 판정: PASS** — Critical/High 0건.

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

### High (0건)

### Medium (0건)

### Low (1건)

- [ ] GC-C001 [`opal/skills/opal-skill-manager/SKILL.md:1-7`] YAML frontmatter에 `version` 키 부재
  - 카테고리: 문서화
  - 위반 기준: 프로젝트(`docs/CONVENTIONS.md` §YAML Frontmatter) — "version: {X.Y.Z}  # 스킬만" (필수 표기, `docs/CONVENTIONS.md:120`)
  - 설명: `docs/CONVENTIONS.md` §YAML Frontmatter는 스킬 frontmatter에 `version` 키를 요구하나, 실측 frontmatter(`name`/`description`만 보유, `opal/skills/opal-skill-manager/SKILL.md:1-7`)에는 없다. 단, `git diff HEAD -- opal/skills/opal-skill-manager/SKILL.md`로 확인한 결과 frontmatter 블록(1~7행)은 이번 변경(태스크 105) diff hunk에 포함되지 않은 **사전 존재 결함**이다 — 본문 하단 "## 변경이력" 표에는 v1.5 행이 semver로 정상 기재되어 있어(§변경이력 작성 의무 준수), frontmatter `version` 필드만 별도로 누락된 상태다.
  - 해결 방안: frontmatter에 `version: 1.5.0`(변경이력 표 최신 행과 정합) 추가. 이번 태스크(105) 변경분이 유발한 결함이 아니므로 PM Gate 차단 사유는 아니며, 후속 태스크(예: 105 CLOSE 또는 별도 정리)에서 처리 여부를 판단할 사안으로 보고한다.
  - 자동 수정: N (승인 후 PM/후속 워커가 처리)
  - 참조: `docs/CONVENTIONS.md:113-124` (YAML Frontmatter 규격)

### Info (0건)

---

## 4. 문서 업데이트 제안 (§9·§10, 트리거 발동 시만)

트리거 미발동 — 빈도 트리거(N=3 이상 파일)·새 카테고리 트리거 모두 조건 불충족. 해당 없음.

---

## 5. 문서 작성 유도 (해당 시)

`docs/CONVENTIONS.md` 존재 — 작성 유도 생략.

---

## 부록: 검사 근거 로그

### A. §@header 규칙 (`docs/CONVENTIONS.md:210-216`)

- `skill-registry.js` 상단 인라인 @header 블록 확인(`skill-registry.js:1-32`): `@module`/`@layer`/`@domain`/`@description`/`@exports` 전 필드 존재, `@exports`에 신규 `scan-risk` 서브명령 반영(`skill-registry.js:9-10`) — PM 확인 사항(`code-scan target` → `write_to: inline`)과 일치. **PM 확인 사항 재확인 — 일치.**
- `test-scan-risk.js` 상단 헤더 블록 확인(`test-scan-risk.js:1-13`): `@module`/`@layer`/`@domain`/`@task`/`@description`/`@depends`/`@scenarios` 필드 보유, 신규 파일이므로 code-scan target 판정 없이도 인라인 헤더 자체 작성 원칙 준수.

### B. §변경이력 작성 의무 (`docs/CONVENTIONS.md:242-246`)

- `skill-registry.js` 인라인 변경이력에 `v1.4 2026-09-03 00:45 KST … (105)` 행 확인(`skill-registry.js:12-15`) — 일시 KST, 태스크 번호 괄호 포함.
- `SKILL.md` 변경이력 표: `| v1.5 | 2026-09-03 00:52 KST | … (105) |` (`SKILL.md:328` 부근) — semver·KST·태스크 번호 전건 충족. **PM 확인 사항 재확인 — 일치.**
- `ARCHITECTURE.md` 변경이력 표: `| 2026-09-03 00:59 | … (태스크 105) |` — 기존 테이블 스키마(날짜/변경내용 2열, 버전 열 없음 — 해당 문서의 기존 행 전체가 동일 스키마)와 정합, 태스크 번호 포함. **PM 확인 사항 재확인 — 일치.**

### C. §배포 경계 (`docs/CONVENTIONS.md:248-253`)

- `git status --porcelain` 실측: 변경 파일은 `docs/ARCHITECTURE.md`, `opal/skills/opal-skill-manager/SKILL.md`, `opal/tools/skill-registry/skill-registry.js`, 신규 `opal/tools/skill-registry/tests/test-scan-risk.js` — 전량 프로젝트 소스(`opal/`, `docs/`) 내부이며 `~/.opal/` 하위 경로 0건. **PM 확인 사항 재확인 — 일치.**
- 커뮤니티 스킬 레지스트리 이원 경계: `SKILL.md` diff에 `~/.opal/references/community-skills-registry.json`(프레임워크 카탈로그)는 무수정 명시 유지(`SKILL.md:` "[MUST] `~/.opal/references/community-skills-registry.json`… 설치 시 수정하지 않는다" 라인, 기존 문구 무변경), `user-registry.json` additive 3필드(`trust`/`capabilities`/`scanned_at`)만 신설 — 이원 경계 위반 없음.

### D. §플랫폼 분기 격리 (`docs/CONVENTIONS.md:255-258`)

- `grep -n "process.platform\|win32\|darwin\|linux"` 결과: `docs/ARCHITECTURE.md`의 3개 매치는 모두 기존(diff 밖) install 스크립트 서술 행(368/476/479)이며 이번 diff hunk(§커뮤니티 스킬 절, §변경이력 신규 행, `tools/` 표 `skill-registry/` 항목)에는 플랫폼 조건문 0건. `skill-registry.js`·`test-scan-risk.js`·`SKILL.md` 신규/수정분에도 플랫폼 분기 0건.

### E. §도구 우선 원칙 (`docs/CONVENTIONS.md:235-240`)

- 신규 `scan-risk`는 `opal/tools/` 하위 기존 도구(`skill-registry.js`)에 서브명령으로 추가된 형태로, 신규 별도 스크립트 난립이 아니라 기존 CLI 확장 — 도구 우선 원칙에 부합.

### F. §Citation Rules (`docs/CONVENTIONS.md:218-223`)

- 본 카테고리는 TASK.md/PLAN.md/ANALYSIS.md/QA 산출물에 적용 범위가 한정된다. 검사 대상 4파일(SKILL.md·ARCHITECTURE.md·소스 2건)은 규정상 직접 적용 대상이 아니므로 별도 판정 생략(해당 없음).

### G. §State 관리 (`docs/CONVENTIONS.md:225-233`)

- `grep -n "state.json"` 결과 전건 `docs/ARCHITECTURE.md` 내 **서술/참조**(§구조·§State 행 설명) 문장이며, `state.json` 직접 편집(diff 내 `.json` 값 변경) 0건. 검사 대상 4파일에 `state.json` 자체는 포함되지 않음.

### H. §네이밍 규칙 (`docs/CONVENTIONS.md:14-28`)

- `test-scan-risk.js`: kebab-case 준수, `opal/tools/skill-registry/tests/` 하위 기존 파일(`test-validate.js`/`test-migrate.js`/`test-match.js`) 및 타 도구(`code-scan/tests/test-*.js` 등)와 명명 패턴(`test-{feature}.js`) 일치 — `find opal/tools -path "*/tests/*.js"` 실측 대조 완료.
- 상수 `RISK_PATTERNS`/`RISK_SCAN_EXTENSIONS` 등 UPPER_SNAKE_CASE는 `docs/CONVENTIONS.md`에 JS 식별자 케이스 규칙이 명문화되어 있지 않아 판정 대상 아님(위반도 근거도 없음 — 개인 취향 지적 배제).

### I. 코드 품질(getsentry/code-review 참조, 카테고리 8)

- ReDoS 방어: `RISK_PATTERNS` 10종 전건이 `isUnsafeRegex()` 기준(길이≤100, `.*`/`.+` ≤2회, non-nested quantifier)과 동일 규율을 자체 주석으로 명시(`skill-registry.js:668-670`), 테스트 [T11]이 이를 별도 회귀 검증 — `docs/CONVENTIONS.md`에 명문 규칙은 없으나 참고용 "추가 확인"으로만 기재(위반 아님).
- 검출 결과 참조 URL 정책상 Low 항목 GC-C001에도 조항 인용(`docs/CONVENTIONS.md:113-124`)을 포함하여 TBD placeholder 불필요.

---

## 변경이력

| 버전 | 일시 | 변경내용 |
|------|------|---------|
| v1.0 | 2026-09-03 22:33 | 초기 작성 — 4파일 컨벤션 체크, Critical/High 0건 확인 (105) |
