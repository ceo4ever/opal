# SCENARIO GATE 2 — 목표-커버리지 루브릭 판단축 채점 (①⑤⑥)

> 실행 일시: 2026-09-03 (요청 timestamp 2026-09-02T15:21:19Z) | phase: `scenario-rubric` | iteration: **2**
> 판정 주체: opal-evaluator-agent (판단축 ①⑤⑥ 전담) — ②③④는 test-tool `scenario-coverage-check` 결정론 관할(exit 0, `all_covered: true`, scenarios 40). 재채점하지 않았다.
> scenario_source: `/Volumes/Data/AiStudio/workspace/opal/tasks/105-260902-opds-스킬-탐색설치-개선/TEST-SCENARIO.md`
> 이전 이력: `.scenario-gate-history.json` iteration 1 — `{goal:1, adoption:1, boundary:2}`, average 1.33, `fail`, gaps G-1~G-6
> 기준 SSOT: `opal/core/references/harness/scenario-gate.md` §2(판단축)·§5-1(수렴 임계 = 각 ≥1 AND 평균 ≥1.5)
> Producer≠Evaluator: §부록 E-6의 PM 자기 반영 주장은 근거로 채택하지 않고, TEST-SCENARIO.md 본문·`PLAN.md`·`skill-registry.js` 실물로 독립 확인했다.

## 1. 이전 gap 닫힘 검증 (신규 gap 생성 아님 — 반영 실효성 판정)

| gap | 축 | 판정 | 독립 확인 근거 |
|-----|----|------|----------------|
| **G-1** | ① | **닫힘** | TS-080 기대 결과가 (a)~(f) 6항으로 치환됐고 전건이 기계 판정 가능하다 — (a) 「3건 이하 + `name`·`source_repo`·`license` 3필드 채워짐」 (b) 「`verdict`가 4단 중 정확히 1값」 (c) 「4축 × 후보 셀 빈칸 0건」 (d) 「RISKY 제외 or 『RISKY 0건』 기록」 (e) 「`경로:줄번호` 인용 ≥1건」 (f) 「신규 디렉토리 0건 && `user-registry.json` 변경 0건」. 「~할 수 있다」류 수용 경계 **잔존 0건**(기대 결과 열 전문 확인). PM 요청 양식이 「(a)~(f) 6개 체크 항목별로 **Pass / Fail** 하나씩만 회신」을 요구하고, 「막힌 지점」은 「판정과 별개인 **참고 의견** … 판정에 반영하지 않음」으로 명시 강등됐다 |
| **G-2** | ① | **닫힘**(단 실행 방식에 새 결함 — N-1) | TS-081 신설 확인(§3 L2). 종점 5항이 기대 결과에 실재한다 — (a) `{설치루트}/{vendor}/{name}/SKILL.md` 생성 (b) `user-registry.json` 10필드 항목 1건 + 3필드 채움 (c) `groups[vendor][]` 형상 유지 (d) `validate` errors 0건 (e) 카탈로그 무수정(mtime·내용 동일). 조건에 「**실 `~/.opal/community-skills/`를 건드리지 않는다**」 + `FX-APPROVE` 격리 루트가 §2.1에 등재됐다. `is_goal_scenario` 2건(TS-080·TS-081)으로 증가 |
| **G-3** | ⑤ | **닫힘** | (a) 표기 분리 실재 — §3 L1-a에서 TS-003·021·033·041·071·072가 「⑤ 잔존(회귀 보존)」, TS-061만 「⑤ **채택**」으로 라벨이 갈렸고, §4 비고와 §4 커버리지 표 ⑤행도 「**채택 실행** TS-035 + **채택 문언** TS-061 + **잔존(회귀 보존)** 7건」으로 동일하게 기술된다. (b) TS-035 신설이 문언 검사가 아니다 — `FX-MIXED`(무해 1 + RISKY 2) 실투입으로 (a) 추천 확정 (b) RISKY 2건 소거 (c) **사다리 2회 반복 동일 산출(재현성)**을 본다. TS-023(문언)의 실행 대응이 §4 비고에 축자 명기됐다(「⑥ 문언 검사 (실행 대응 = TS-035)」) |
| **G-4** | ⑥ | **닫힘** | §3 L1-a 헤더에 「**[표기] ⑥축 문언 검사와 실행 검사의 구분**」 블록이 신설되어 문언 5건(TS-004·018·023·032·052)과 실행 5건(TS-013·016·017·019·034)을 명시 분리하고 「두 종류를 등가로 합산 계상하지 않는다」를 선언한다. 표 본문의 「대응 축」 열도 문언 5건 전건에 「**문언 검사(경계 입력 미실행)**」가 붙었다. §4 커버리지 ⑥행에서 iteration 1의 「9건」 등가 합산 표기가 제거됐다(현재 「실행 5건 + 문언 5건 (등가 합산하지 않음)」) |
| **G-5** | ⑥ | **닫힘** | §2.1에 `FX-CAUTION`(medium severity만, 라이선스 확인됨, active hit 1건) 등재 + TS-019 신설(L1-b, M1). 4단 중 CAUTION이 실동작 미검증 상태를 벗어났다 — 기대 결과가 `verdict:"CAUTION"` + `hits[].severity == medium` + 「RISKY와 달리 추천 후보에서 제외되지 **않는다**」를 단정한다. 후보 1~2건 시 CAUTION 승격 여부도 기대 결과에 포함됐다(문언 확정 형태로 기재 — G-5가 요구한 「승격할지 판단해 기재」 충족) |
| **G-6** | 형식 | **닫힘** | §3 L1-a 표에 `제목` 컬럼이 2열로 추가되고 **25행 전건에 제목이 채워졌다**(TS-001~073 실사 확인, 빈칸 0). 조건 예외 3건 명기 확인 — TS-033 「**조건 예외**: … 개정 전 SKILL.md 스냅샷(`BL-SKILL`) 2건 대조」, TS-071 「**조건 예외**: 외부 참조 파일 `RF-ANCHOR` 동반 Read」, TS-073 「**조건 예외**: 대상이 `AR-SKILL`이 아니라 `AR-ARCH`」. `BL-SKILL`도 §2.1에 신규 등재됐다 |

**결론: G-1~G-6 전건 닫힘.** 형식적 문구 추가에 그친 항목은 없다 — 신설 3건(TS-019·TS-035·TS-081)은 모두 픽스처·기대 결과·계층·실행 방식을 갖춘 실질 시나리오이며, 재분류 2건(G-3·G-4)은 §3 라벨과 §4 비고·커버리지 표 3곳이 서로 일치한다.

## 2. 판단축별 판정

| 축 | 점수 | 판정 근거 |
|----|------|----------|
| ① 목표 달성 | **2** | 앵커 2점(「사용자·운영 계층에서 목표를 직접 검증」)을 충족한다. **사용자 계층** — TS-080(L3/M3)이 절차 완주를 검증하며 수용 경계가 (a)~(f) 6개 Pass/Fail 판정 항목으로 객관화됐다(G-1 닫힘). **운영/종점** — iteration 1의 결정적 결손이었던 목표 문장 종점(「승인 → 복사·등록」)이 TS-081로 실행 검증 편성됐고, 복사·registry 기록·`groups[vendor][]` 형상·`validate`·카탈로그 무수정 5항이 모두 기대 결과에 있다(G-2 닫힘). 즉 6단 중 5·6단이 목표 계층에서 닫혔다. 실 홈 오염 방지도 조건에 명문화됐다. 잔여 결함(N-1)은 **커버리지 결손이 아니라 실행 방식 지정 오류**이므로 본 축 점수에 감점하지 않고 별도 권고로 분리한다 |
| ⑤ 채택/잔존 | **2** | 추가형 목표에서 이 축이 묻는 「보존 대상 불변 + 신형 실채택」 양측이 모두 실질 충족됐다. **잔존측** — 회귀 보존 7건(TS-003·015·021·033·041·071·072)이 정직한 라벨로 표기되고, 그중 TS-015는 `BL-LIST` 스냅샷 대조 + `test_adapters.py::test_skill_adapter_list` + 기존 `tests/` 3파일 실행이라는 실동작 회귀다. **채택측** — iteration 1에서 TS-061 문언 대조 1건뿐이던 결손이 TS-035(`FX-MIXED` 혼합 후보 실투입 → 무해 채택 확정 + RISKY 소거 + 2회 반복 재현성)로 보강되어, 신설 판정 축이 실제로 후보 선택을 좌우하는지가 실행으로 검증된다. iteration 1의 「8건 등가 표기 과대 계상」 지적도 표기 분리로 해소됐다(G-3 닫힘) |
| ⑥ 경계/부정 | **2** | iteration 1의 2점을 유지한다. 실행형이 4건 → **5건**으로 늘었다 — 경계값 TS-016(`FX-REDOS` 100KB · 3초 타임박스)·TS-013(오탐 억제 4픽스처), 부정 경로 TS-017(`{ok:false, verdict:"UNKNOWN"}` + exit 1)·TS-034(RISKY 전량 제외 → 잔존 0 → NOT_FOUND 위임), 그리고 신설 TS-019(4단 중간값 CAUTION 실동작 — G-5가 지적한 유일한 미검증 판정값). 문언 검사 5건과의 등가 합산 표기도 제거됐다(G-4 닫힘). 축 정의를 실행형 5건이 독립 충족한다 |

## 3. 결과 계약

```json
{"scores": {"goal": 2, "adoption": 2, "boundary": 2}, "average": 2.0, "gaps": [], "verdict": "pass"}
```

- 0점 축: **없음** (각 축 ≥1 충족)
- 평균: (2+2+2)/3 = **2.0** ≥ **1.5** → `scenario-gate.md` §5-1 종료조건 **양단 충족**
- **verdict: pass** — 결정론 게이트(②③④ exit 0 / all_covered) + evaluator verdict pass 두 증거가 모두 성립한다. iteration 1 → 2 상승은 신설 3건(TS-019·TS-035·TS-081)과 표기 분리 2건이 각 축의 실질 결손을 직접 메운 결과이며, 점수 인플레가 아니다 — 축별로 「무엇이 새로 실행되는가」를 근거로 명시했다.
- `gaps[]`는 결과 계약 정의(「해당 축 <1점일 때만」)에 따라 **빈 배열**이다. 아래 §4는 점수 미반영 권고이며 게이트 통과를 막지 않는다.

## 4. 신규 검출 결함 (점수 미반영 · EXECUTE 전 처리 권고)

> iteration 1 gap의 반영 **과정에서 새로 생긴** 결함만 기재한다. 축 점수에는 반영하지 않았다(사유: 세 축 모두 커버리지 정의를 충족하며, 아래는 실행 방식·표기 층위의 결함이다).

| # | 대상 | 결함 | 근거 | 권고 |
|---|------|------|------|------|
| **N-1** | TS-081(주), TS-035(종) | **실행 방식 M1 지정이 구현 실물과 불일치하며 self-confirming 표면을 만든다.** TS-081은 「승인 → 복사 → user-registry 기록」을 `node --test`(M1)로 검증한다고 선언하나, `opal/tools/skill-registry/skill-registry.js` `main()` switch에는 `match`·`get`·`list`·`validate`·`migrate`·`parse-source-repo` 6종만 있고 **설치(복사·등록) 서브명령이 없다**. PLAN.md가 신설하는 코드도 `scan-risk` 1종뿐이다(§4.2 Step 2 「`case 'scan-risk'` + usage 1줄 추가」, F-002). 즉 복사·등록은 `SKILL.md` §2의 **에이전트 절차**이지 도구 코드가 아니므로, node 테스트가 복사·기록을 스스로 수행한 뒤 그 결과를 단정하면 검증 대상(절차 문서)이 검증되지 않고 「테스트가 만든 것을 테스트가 확인」하는 구조가 된다 — PLAN.md §C-5 self-confirming 회피 원칙(작성자≠구현자, `PLAN.md:1128-1130`)과 충돌한다. TS-035의 「추천 사다리 2회 적용」도 사다리가 코드가 아닌 문서 규칙이므로 동일 계열이다 | `skill-registry.js:671-707`(switch 6종) / `PLAN.md:883`(Step 2 작업 내용) / `PLAN.md` F-001~F-008 표(설치 도구화 항목 부재) / `PLAN.md:312,318`(6단의 복사는 §2 절차) | 둘 중 하나를 **EXECUTE 디스패치 전에 확정**하라 — (가) TS-081·TS-035의 실행 방식을 **M2(에이전트가 `HOME` 격리 상태에서 SKILL.md 절차를 실제 수행)** 로 재지정하고 수행자를 시나리오 작성자와 분리한다, 또는 (나) 설치 경로 오버라이드를 **PLAN.md §3에 구현 요건으로 명문화**(F-00N 신설 + Step 배정)한 뒤 M1을 유지한다. PM은 (나)의 필요성을 §부록 E-7로 자기 신고했으나 「PLAN 재작성 없이 디스패치 프롬프트 델타 주입」으로 처리했다 — 신규 구현 요건(경로 오버라이드)은 프롬프트 델타가 아니라 PLAN 설계면에 있어야 추적된다 |
| **N-2** | TS-019 | **G-4가 세운 문언/실행 분리가 시나리오 내부에서 다시 섞였다.** 「대응 축」이 「⑥ 경계·부정 — **실행 검사**」인데 기대 결과 3항 중 2항이 문언 검사다 — 「`AR-SKILL`의 CAUTION 행 동작이 … 로 기술되어 있고」, 「후보가 1~2건뿐일 때 … 여부가 **문언으로 확정되어 있다**」. 실행 단정은 `verdict:"CAUTION"` + `severity == medium` 2항뿐이다 | TEST-SCENARIO.md §3 L1-b TS-019 기대 결과 열 | TS-019의 실행 단정(`scan-risk` 출력)만 남기고 문언 2항은 TS-030(4단 판정 표) 기대 결과로 이관하거나, 「실행 검사 + 문언 검사 혼합」으로 라벨을 정정한다. G-4 처리 원칙(등가 합산 금지)을 시나리오 내부에도 적용 |
| **N-3** | TS-080 | **체크 항목 개수 표기 모순 1건** — 기대 결과 서두는 「아래 **5개** 체크 항목이 각각 Pass/Fail로 판정된다」인데 실제 열거는 (a)~(f) **6항**이고, 같은 행의 PM 요청 양식과 §4 커버리지 표는 「6개」로 적었다. 캡틴 회신 시 (f)가 누락될 여지가 있다 | TEST-SCENARIO.md §3 L3 TS-080 기대 결과 vs PM 요청 양식 | 「5개」 → 「6개」로 정정 (1문자 교정) |

## 5. 판정 주체 경계 확인

- ②요구커버·③기능커버·④리스크커버는 **채점하지 않았다** — `scenario-gate.md` §2 `[MUST]`에 따라 test-tool 결정론 관할이며, 그 결과(requirements 8/8 · features 8/8 · hypotheses 9/9, scenarios 40, 누락 0)를 전제로만 사용했다.
- 본 에이전트는 `TEST-SCENARIO.md`·`PLAN.md`를 포함해 **어떤 파일도 수정하지 않았다**. 생성물은 본 보고서 1건뿐이다(verdict-only·readonly).
- §4 N-1~N-3의 처리는 Producer(알투 PM) 책임이다. 세 축이 종료조건을 충족했으므로 `scenario-gate.md` §5-1에 따라 루프는 **수렴**하며, N-1~N-3은 게이트 재호출 사유가 아니라 EXECUTE 디스패치 전 교정 항목으로 이월한다. 특히 **N-1은 목표 종점 검증의 실효성에 직결**되므로 미처리 시 TEST 단계에서 TS-081이 무효 증거를 산출할 수 있다.
