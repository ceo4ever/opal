# SCENARIO GATE 1 — 목표-커버리지 루브릭 판단축 채점 (①⑤⑥)

> 실행 일시: 2026-09-03 (요청 timestamp 2026-09-02T15:13:05Z) | phase: `scenario-rubric` | iteration: 1
> 판정 주체: opal-evaluator-agent (판단축 ①⑤⑥ 전담) — ②③④는 test-tool `scenario-coverage-check` 결정론 관할(이미 exit 0, `all_covered: true`)
> scenario_source: `/Volumes/Data/AiStudio/workspace/opal/tasks/105-260902-opds-스킬-탐색설치-개선/TEST-SCENARIO.md`
> 정규화 페이로드 확인: `.scenario-coverage-input.json` — scenarios 37건 / `is_goal_scenario` 1 / `is_adoption_scenario` 8 / `is_boundary_scenario` 9
> 기준 SSOT: `opal/core/references/harness/scenario-gate.md` §2(루브릭 6축)·§5-1(수렴 임계) — CONTRACT.md 부재는 본 phase 판정에 영향 없음(Phase 2 병합 대상 아님)
> Producer≠Evaluator: 작성자는 알투(PM)+캡틴 페어. 본 보고서는 §부록 E-1~E-5의 PM 자기 정당화를 근거로 채택하지 않고 독립 채점했다.

## 1. 판단축별 판정

| 축 | 점수 | 판정 근거 | gap |
|----|------|----------|-----|
| ① 목표 달성 | **1** | 목표 검증 시나리오 `TS-080`이 L3/사용자 계층에 **존재**하므로 0점은 아니다. 그러나 두 가지 실질 결손으로 2점(「목표를 직접 검증」)에 미달한다. **(가)** 목표 문장(`.scenario-coverage-input.json` `goal`)의 종점인 「승인 → **복사·등록**」이 사용자·운영 계층에서 미검증이다 — `TS-080` 조건이 「승인 단계에서 **거부**를 선택해 실제 설치는 수행하지 않는다」이므로 설치 성공 경로가 전 시나리오에서 실행되지 않고, R-5(registry 기록)는 픽스처 `validate`(`TS-042`·`TS-043`)로만 대리된다. 즉 6단 중 5·6단이 목표 계층에서 열려 있다. **(나)** 유일한 목표 시나리오의 수용 경계가 「캡틴이 … 산출**할 수 있다**」로 기술되어 (a)~(e) 5산출물별 최소 수용 기준이 없다 — 객관 판정 가능한 부분은 「`~/.opal/community-skills/` 하위 신규 디렉토리·`user-registry.json` 변경 0건」 후단뿐이고, PM 요청 양식의 「절차 문언에서 막힌 지점을 알려 주십시오」는 판정 항목이 아닌 소감 수집이다. 자동 경로 0건 자체는 `TS-080` 실행 방식(M3, 실 네트워크·판정 재량)이 사유를 제시했으므로 단독 감점 사유로는 쓰지 않았다 | G-1, G-2 |
| ⑤ 채택/잔존 | **1** | 축 정의는 「**교체형 목표**=구형 잔존0·신형 채택」이다. `PLAN.md:1132`는 이 태스크를 「교체형이 아니라 기존 절차에 판정 축을 **추가**」로 판정했고 PM도 §부록 E-1에서 자신의 교체형 분류를 철회했다 — 따라서 「구형 잔존 0」 대칭은 성립하지 않고, 추가형에서 이 축이 묻는 것은 「보존 대상의 불변 유지 + 신형의 실채택」이다. 플래그 8건을 이 기준으로 재분류하면 **잔존/회귀 보존 7건**(`TS-003` 절 번호 잔존 · `TS-033` §6 문안 불변 · `TS-041` 기존 7필드 잔존 · `TS-071` 외부 앵커 무결 · `TS-072` `[MUST]` 금지문 잔존 · `TS-015` `list` 출력 계약 회귀 · `TS-021` 점수 표기 0건)은 실체가 있어 0점이 아니다. 그러나 **신형 채택 검증은 `TS-061`(위임 대상이 `skill-builder`가 아니라 `opal-skill-creator`로 표기) 1건뿐**이며 그것도 문언 대조다 — 신설 축(보안 4단·2층 비교 표·6단 흐름)이 절차 안에서 실제로 **채택되어 후보 판정을 좌우하는지**는 ⑤축 계상에 없다(`TS-030`·`TS-032`는 ②요구커버로, `TS-034`는 ⑥으로 계상). 결론: 8건 표기는 회귀(regression) 검사를 채택/잔존으로 **과대 표기**한 것이며, 축은 절반(잔존측)만 실질 충족한다 | G-3 |
| ⑥ 경계/부정 | **2** | 축 정의(「경계값·부정 경로 시나리오 존재」)를 실행형 4건이 명확히 충족한다 — **경계값**: `TS-016`(`FX-REDOS` 100KB 병리 입력 · 3초 타임박스), `TS-013`(오탐 억제 경계 4픽스처 negated/comment/fixture/prose). **부정 경로**: `TS-017`(존재하지 않는 경로 → `{ok:false, verdict:"UNKNOWN"}` + exit 1), `TS-034`(RISKY 3건 전량 제외 → 잔존 0 → NOT_FOUND 위임, L2 통합). 지적 쟁점(문언 존재 검사 계상)은 타당하다 — `TS-004`·`TS-018`·`TS-023`·`TS-032`·`TS-052` 5건은 산출물 정적 grep이며 경계 입력을 **실행하지 않는다**. 다만 이 태스크의 1차 산출물이 절차 문서(`SKILL.md`)라는 성격상 「0건·3건 미만 동작이 문언으로 규정되었는가」는 정당한 명세 검증이고, 무엇보다 축 충족은 실행형 4건으로 이미 독립 성립한다. 따라서 감점하지 않되 계상 라벨 과대와 미검증 경계(CAUTION/UNKNOWN 실동작, 후보 1~2건 실거동)는 gap으로 보고한다 | G-4, G-5 |

## 2. 결과 계약

```json
{"scores": {"goal": 1, "adoption": 1, "boundary": 2}, "average": 1.33, "gaps": ["G-1", "G-2", "G-3", "G-4", "G-5", "G-6"], "verdict": "fail"}
```

- 0점 축: **없음** (각 축 ≥1 조건 충족)
- 평균: (1+1+2)/3 = **1.33** < **1.5** → `scenario-gate.md` §5-1 「판단축 각 ≥1점 **AND** 평균 ≥1.5점」의 **후단 미충족**
- **verdict: fail** — 결정론 게이트(②③④)는 통과했으나 §6 tool-gated 집행의 두 번째 증거(evaluator verdict pass)가 성립하지 않는다. 재작성 1회 후 재호출 대상(동일 게이트 1건의 반복).

## 3. gaps — 재작성 지시

| # | 축 | 지시 |
|---|----|------|
| G-1 | ① | `TS-080` 기대 결과의 (a)~(e)를 **판정 가능한 체크 항목으로 치환**한다 — 예: (a) 후보 목록 = 3건 이하이며 각 행에 `source_repo`·`license` 채워짐 / (b) 1층 결과 = 후보별 `verdict` 값이 SAFE·CAUTION·RISKY·UNKNOWN 중 하나로 기록됨 / (c) 2층 비교 표 = 4축 × 후보 셀에 3단 판정어 또는 실측값이 빈칸 0 / (d) 4단 판정 = 후보별 1단 확정 + RISKY 후보의 추천 제외 여부 / (e) 추천 1개 + 근거가 `경로:줄번호` 형식 인용 ≥1건. PM 요청 양식의 「절차 문언에서 막힌 지점을 알려 주십시오」는 판정 기준에서 분리해 **참고 의견 수집 항목으로 명시 강등**한다 |
| G-2 | ① | 목표 종점(승인 → **복사·등록**)을 검증하는 시나리오를 **1건 신설**한다. `TS-080`의 거부 경로는 유지하고, 승인 경로는 실 네트워크 없이 재현 가능한 형태(예: 로컬 무해 픽스처 1건을 후보로 투입 → 승인 → `~/.opal/community-skills/{name}/` 생성 + `user-registry.json`에 10필드 항목 1건 추가 + `groups[vendor][]` 형상 유지 + `validate` error 0)로 L2/M1 편성한다. `is_goal_scenario`로 플래그하여 목표 시나리오가 1건 → 2건이 되게 한다 |
| G-3 | ⑤ | `is_adoption_scenario` 8건 표기를 **재분류**한다 — (a) §4 「비고」와 정규화 페이로드에서 회귀 보존 7건의 라벨을 「잔존(회귀 보존)」으로 정직하게 표기하고, 「채택」 표기는 실제 채택 검증에만 쓴다. (b) **신형 채택 검증을 ≥1건 보강**한다: 신설 판정 축이 절차 안에서 실제로 후보 선택을 바꾸는지 — 예 「무해 후보 1건 + RISKY 후보 1건을 동시 투입했을 때 추천이 무해 후보로 확정되고 RISKY 후보가 추천 목록에서 사라진다」(`TS-034`의 전량 RISKY와 달리 **혼합 후보**에서 신형 축의 판별력을 검증) 또는 「2층 4축이 동률일 때 추천 사다리가 순서대로 적용되어 동일 입력에 동일 후보를 2회 반복 산출한다(재현성 실행 검증)」. 현행 `TS-023`은 사다리의 **문언 존재**만 보므로 실행 검증이 아니다 |
| G-4 | ⑥ | 문언 존재 검사 5건(`TS-004`·`TS-018`·`TS-023`·`TS-032`·`TS-052`)의 `is_boundary_scenario` 라벨을 **재검토**한다 — 명세 검증으로서 유지는 무방하나, §4 「비고」에 「문언 검사(경계 입력 미실행)」임을 표기해 실행형 4건(`TS-013`·`TS-016`·`TS-017`·`TS-034`)과 구분한다. 게이트 통과 근거로 9건을 등가 계상하는 표기를 제거한다 |
| G-5 | ⑥ | 4단 판정의 **중간 2단 실동작 미검증**을 메운다 — 현행 실행형은 SAFE(`TS-012`)·RISKY(`TS-011`)·UNKNOWN(오류 경로 `TS-017`)만 실행하고 **CAUTION 판정을 산출하는 픽스처가 §2.1에 없다**. `FX-CAUTION`(예: severity medium hit만 존재, 또는 license 확인됨 + active hit 1건) 픽스처 1건을 추가하고 「CAUTION → 확인 게이트」 동작을 실행 검증하는 시나리오를 편성한다. 아울러 후보 수 **1~2건일 때의 실거동**(현행 `TS-004`는 문언 검사만)을 경계 시나리오로 승격할지 판단해 기재한다 |
| G-6 | 형식(점수 미반영) | §3 L1-a 25건 압축 표는 통일 형식(`op-dev-test-scenario/SKILL.md:98-109`)의 10필드 중 `계층`·`실행 방식`·`조건`을 표 전 공통 선언(「조건 = 개정 완료 산출물 1건, 실행 방식 = M1, 계층 = L1」)으로 대체했다 — 이 3필드는 값이 실제로 동일하므로 **실질 보존으로 인정**한다. 다만 두 결손을 교정하라: (a) **시나리오 제목이 25건 전건 부재**하다(통일 형식은 `#### S-N: {제목}`을 요구) → `제목` 컬럼 1열 추가. (b) **공통 선언이 사실과 다른 행이 있다** — `TS-033`은 「개정 **전** SKILL.md」, `TS-071`은 `RF-ANCHOR`, `TS-073`은 `AR-ARCH`를 추가 사전조건으로 요구하므로(§2.2 대조) 공통 `조건` 선언으로는 그 사전조건이 표에서 소실된다 → 해당 행에 `조건` 예외를 개별 표기하라 |

## 4. 판정 주체 경계 확인

- ②요구커버·③기능커버·④리스크커버는 **채점하지 않았다** — `scenario-gate.md` §2 `[MUST]`에 따라 test-tool 결정론 관할이며, 본 판정은 그 결과(requirements 8/8 · features 8/8 · hypotheses 9/9, 누락 0)를 전제로만 사용했다.
- 본 에이전트는 `TEST-SCENARIO.md`를 포함해 어떤 파일도 수정하지 않았다. 생성물은 본 보고서 1건뿐이다(verdict-only·readonly).
- gaps 반영(재작성)은 Producer(알투(PM)+캡틴 페어) 책임이며, 반영 후 §4-5 재작성 루프에 따라 동일 게이트를 재호출한다.
