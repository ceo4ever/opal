# PLAN: 역할 전환 메커니즘 v2 — 프로젝트 기반 자동 전환

> 작성일: 2026-04-12
> 입력: TASK.md
> 출력: PLAN.md

## 1. 현황 조사

### 관련 파일

| 파일 | 역할 | 변경 필요 |
|------|------|----------|
| `opal/core/AGENT.md` | 에이전트 정의 (소스) — 수정 대상 | **예** |
| `~/.opal/AGENT.md` | 에이전트 정의 (배포) — 직접 수정 금지 | 아니오 |
| `~/.opal/references/opal-harness.md` | 하네스 공통 인프라 (참조) | 아니오 |
| `~/.opal/references/opal-pm.md` | PM 행동 프로세스 (참조) | 아니오 |

### 현재 상태

**소스 파일 분석** (`opal/core/AGENT.md`, 340행):

수정 대상 섹션 5개를 실제 코드 기준으로 확인했다:

1. **`### 역할 전환` (108~129행)**: 비서/PM 두 역할, 5케이스 전환 테이블, 소유자 오버라이드 3가지(`//`, "PM으로 해", "그냥 해"). 문제: "비서가 PM 전환 제안" 방식으로 매번 질문 발생, PM→비서 복귀 규칙 부재, 전환 절차 미정의.

2. **`### 부트스트랩 완료 보고` (46~55행)**: 체크리스트에 PM 모드 진입 여부 표시 없음. 현재 identity/harness/PM/registry/references/model-mapping만 표시.

3. **`### 주도성` (145~155행)**: 153행에 "PM 전환 제안" 항목 존재 — v2에서는 PM이 기본이므로 의미 변경 필요.

4. **`### 보고 형식` (175~208행)**: PM 모드 표시(`📋 알투[PM]: {NNN}...`)는 태스크 진행 시에만 적용. PM 대화 표기(`알투[PM]:`)와 비서 표기(`알투:`)가 미정의.

5. **`### code-scan 활용 규칙 (비서 모드)` (130~143행)**: "(비서 모드)" 라벨이 v2에서 부적절 — v2에서는 프로젝트 내 = PM이므로 "비서 모드"가 프로젝트 내에 존재하지 않음. 단, TASK.md R1~R5 요구사항에 이 섹션 수정은 포함되지 않음.

**소스/배포 파일 차이**: "소유자" vs "캡틴" 치환 차이만 존재 (태스크 112 직전 커밋 `7ae3624`에서 일괄 교체). 코드(소스 파일) 기준으로 작업한다.

**하네스 구조 확인** (하네스 §1~§9):
- §1 Guards: 구현 금지 원칙, Git 사전 점검, 디스패치 의무, 커밋 규칙
- §2 Gates: QA Gate, PM Gate (interactive/agentic 모듈)
- §3 State: STATE.md 관리, State Gate
- §5 Observability: 행위 주체 선언
- §7 병렬 처리
- §8 @header 규칙
- §9 OPAL Tools

→ "그냥 해" 시 유지/미적용 구분에 이 구조를 사용한다.

### 영향 범위

- **직접 영향**: `opal/core/AGENT.md` 내 5개 섹션
- **간접 영향 없음**: 하네스, PM 행동 프로세스, 스킬 SKILL.md 등은 변경 불필요. 역할 전환은 AGENT.md 소관.
- **주의**: 변경 후 `install-mac.sh`로 배포 시 `~/.opal/AGENT.md`에 반영됨. 배포 전까지 소스/배포 차이 발생은 정상.

## 2. 구현 계획

### 파일 변경 계획

#### 신규 생성

| # | 파일 경로 | 역할 |
|---|----------|------|
| (없음) | | |

#### 수정

| # | 파일 경로 | 변경 내용 |
|---|----------|----------|
| 1 | `opal/core/AGENT.md` | 5개 섹션 v2 갱신 (R1~R5) |

#### 삭제

| # | 파일 경로 | 사유 |
|---|----------|------|
| (없음) | | |

### 구현 순서

| 순서 | 작업 | 파일 | 예상 난이도 |
|------|------|------|-----------|
| 1 | `### 역할 전환` 섹션 전면 교체 (R1 + R5) | `opal/core/AGENT.md` | 중 |
| 2 | `### 부트스트랩 완료 보고` PM 진입 상태 추가 (R4) | `opal/core/AGENT.md` | 하 |
| 3 | `### 주도성` PM 전환 제안 → 하네스 적용 제안 변경 (R3) | `opal/core/AGENT.md` | 하 |
| 4 | `### 보고 형식` PM 표기 v2 테이블화 (R2) | `opal/core/AGENT.md` | 하 |
| 5 | `### code-scan 활용 규칙` 라벨 검토 + 변경이력 추가 | `opal/core/AGENT.md` | 하 |

**순서 근거**: 동일 파일이므로 모두 순차. R1+R5는 핵심 역할 전환 정의이므로 먼저 확정하고, 나머지 섹션은 이에 일관되게 갱신한다.

### 핵심 설계

#### Step 1: `### 역할 전환` 섹션 전면 교체 (R1 + R5)

기존 108~129행을 삭제하고 아래 구조로 교체한다:

```markdown
### 역할 전환

알투는 **비서**와 **PM** 두 가지 역할을 수행한다.

#### 상태 정의

| 역할 | 활성 조건 | 설명 |
|------|----------|------|
| **비서** | 프로젝트 밖 (`.opal/AGENT.md` 미존재) | 일상 대화, 일반 업무 |
| **PM** | 프로젝트 내 (`.opal/AGENT.md` 존재) | 프로젝트 모든 상호작용 관리 |

#### PM 내 하네스 적용 기준

| 모드 | 트리거 | 하네스 적용 | 동작 |
|------|--------|-----------|------|
| PM(대화) | 분석/읽기/설명/토론 — 파일 수정 미수반 | 미적용 | PM이 직접 수행 |
| PM(태스크) | `//` 커맨드, 파일 수정 수반 작업 | 적용 | 워커 디스패치, Gate 검토 |

#### 자동 전환 트리거

| 방향 | 트리거 | 동작 |
|------|--------|------|
| 비서 → PM | 부트스트랩 완료 시 `.opal/AGENT.md` 존재 | PM 모드 즉시 진입 |
| PM → 비서 | 소유자가 "비서로" 지시 | PM 해제, 비서 전환 |
| PM → 비서 | 프로젝트 밖 주제로 대화 전환 | PM 해제, 비서 전환 |

#### 소유자 오버라이드

소유자은 다음 방법으로 역할/모드를 직접 지정할 수 있다:

| 명령 | 효과 | 상세 |
|------|------|------|
| `//` 커맨드 | PM(태스크) 강제 진입 | 스킬 파이프라인 + 하네스 전체 적용 |
| "그냥 해" | PM(대화) 상태에서 직접 수정 | 태스크 파이프라인 미적용 (아래 참조) |
| "비서로" | PM 해제, 비서 전환 | 프로젝트 컨텍스트 해제 |

#### "그냥 해" 시 하네스 적용 범위

| 구분 | 항목 | 적용 여부 |
|------|------|----------|
| **유지** | Guards (구현 금지 원칙 — 단, "그냥 해"가 실행 허가를 포함) | ✅ |
| **유지** | @header 규칙 (코드 파일 생성/수정 시) | ✅ |
| **유지** | OPAL Tools (도구 우선 원칙) | ✅ |
| **미적용** | 태스크 파이프라인 (TASK→PLAN→EXECUTE) | ❌ |
| **미적용** | STATE.md 생성/갱신 | ❌ |
| **미적용** | Gate (QA Gate, State Gate, PM Gate) | ❌ |
| **미적용** | Observability (행위 주체 선언) | ❌ |
| **미적용** | 워커 디스패치 | ❌ |

> "그냥 해" = **태스크 파이프라인 미적용**. PM(대화) 상태에서 PM 컨텍스트를 활용하여 직접 수정한다.
```

**설계 결정**:
- "PM으로 해" 오버라이드를 제거 — v2에서는 프로젝트 내 = 자동 PM이므로 불필요
- "비서로" 오버라이드를 추가 — PM→비서 복귀 경로 신설
- "그냥 해" 시 Guards의 구현 금지 원칙은 유지하되, "그냥 해" 자체가 실행 허가를 포함함을 명시
- R5 "그냥 해" 테이블을 역할 전환 섹션 내부에 배치 (별도 섹션 불필요)

#### Step 2: `### 부트스트랩 완료 보고` PM 진입 상태 추가 (R4)

기존 체크리스트 형식에 PM 모드 표시를 추가한다:

```markdown
### 부트스트랩 완료 보고

세션 첫 응답에 아래 형식의 한 줄 체크리스트를 포함한다:

\```
[부트스트랩] ✅ identity ✅ harness ✅ PM ✅ PM모드 ⏳ registry ⏳ references ⏳ model-mapping
\```

- ✅ Eager 완료 | ⏳ Lazy 대기 | ❌ 실패 | ⬜ 해당 없음
- `PM모드`: `.opal/AGENT.md` 존재 시 ✅ PM, 미존재 시 ⬜ 비서
- 브리핑이 있을 경우 이 줄을 브리핑 상단에 삽입한다
```

**설계 결정**: 기존 항목 뒤에 `PM모드` 항목을 추가. `.opal/AGENT.md` 존재 여부로 판정.

#### Step 3: `### 주도성` PM 전환 제안 수정 (R3)

153행의 "PM 전환 제안" 항목을 변경:

**기존**:
```
- **PM 전환 제안**: 코드/파일 수정이 필요한 작업을 감지하면, PM 프로세스(QA/검증 포함) 진행을 제안한다. 소유자이 "그냥 해"를 선택하면 비서 모드로 직접 수정한다 (하네스 미적용).
```

**변경 후**:
```
- **하네스 적용 제안**: 파일 수정이 필요한 작업을 감지하면, PM(태스크) 파이프라인(QA/검증 포함) 적용을 제안한다. 소유자이 "그냥 해"를 선택하면 PM(대화) 상태에서 직접 수정한다 (태스크 파이프라인 미적용).
```

**설계 결정**: v2에서는 프로젝트 내 = PM이므로 "PM 전환" 이 아니라 "하네스 적용"을 제안하는 것으로 변경. "비서 모드로 직접 수정" → "PM(대화) 상태에서 직접 수정"으로 v2 용어에 맞춤.

#### Step 4: `### 보고 형식` PM 표기 v2 테이블화 (R2)

기존 198~208행의 PM 모드 표시 + Observability 선언을 아래로 교체:

```markdown
**역할별 응답 표기**

| 역할/모드 | 접두사 형식 | 사용 상황 |
|-----------|-----------|----------|
| 비서 | `알투:` | 프로젝트 밖 대화 |
| PM(대화) | `알투[PM]:` | 프로젝트 내 분석/설명/토론 |
| PM(태스크) | `📋 알투[PM]: {NNN} {태스크명} \| {단계}` | 태스크 파이프라인 진행 중 |

**Observability 선언** (PM(태스크) 모드 한정)

툴 호출 직전/직후에 행위 주체를 한 줄로 선언한다. 선언 형식과 적용 시점은 하네스 §5 Observability 참조.
```

**설계 결정**: 3가지 표기를 테이블로 정리. Observability 선언 범위를 "PM 모드 한정" → "PM(태스크) 모드 한정"으로 명확화 (PM(대화)에서는 하네스 미적용이므로 Observability도 불필요).

#### Step 5: `### code-scan 활용 규칙` 라벨 변경 + 변경이력

- "(비서 모드)" 라벨을 제거하거나 v2에 맞게 변경: `### code-scan 활용 규칙`으로 단순화. v2에서는 프로젝트 내 = PM이며, code-scan은 PM(대화) 시에도 활용하므로 모드 한정이 불필요.
- 변경이력에 v1.9 행 추가.

## 3. 실행 체크리스트

> 총 5개 Step | Phase 1개
>
> | Phase | Step | 실행 | 비고 |
> |-------|------|------|------|
> | 1 | 1, 2, 3, 4, 5 | 순차 | 동일 파일 (`opal/core/AGENT.md`) |

### Step 1: 역할 전환 섹션 전면 교체 (R1 + R5)
- [x] 완료
- **파일**: `opal/core/AGENT.md` (108~129행)
- **작업 내용**: 기존 `### 역할 전환` 섹션(108~129행)을 삭제하고 v2 구조로 교체. 상태 정의 테이블, PM 내 하네스 적용 기준 테이블, 자동 전환 트리거 테이블, 소유자 오버라이드 테이블, "그냥 해" 하네스 적용 범위 테이블을 포함한다.
- **완료 기준**: (1) 상태 정의(비서/PM)가 존재하고 PM 기본 활성 조건이 "`.opal/AGENT.md` 존재" (2) PM 내 "대화"/"태스크" 2행 테이블 존재 (3) 기존 "제안 후 대기" 방식 제거됨 (4) PM→비서 복귀 트리거 존재 (5) 오버라이드 "그냥 해"/"비서로"/`//` 3가지 (6) "그냥 해" 유지/미적용 테이블 존재
- **테스트**: 섹션 내 키워드 검색으로 6개 AC 항목 확인
- **의존**: 없음

### Step 2: 부트스트랩 완료 보고 PM 진입 상태 추가 (R4)
- [x] 완료
- **파일**: `opal/core/AGENT.md` (46~55행)
- **작업 내용**: 체크리스트 예시에 `PM모드` 항목 추가. `.opal/AGENT.md` 존재 시 PM, 미존재 시 비서 표시 규칙 추가.
- **완료 기준**: (1) 체크리스트에 `PM모드` 항목 존재 (2) PM/비서 판별 조건 명시
- **테스트**: 체크리스트 형식이 기존 항목과 일관되는지 확인
- **의존**: Step 1 (역할 전환 용어 확정 후)

### Step 3: 주도성 섹션 PM 전환 제안 수정 (R3)
- [x] 완료
- **파일**: `opal/core/AGENT.md` (145~155행, 특히 153행)
- **작업 내용**: "PM 전환 제안" → "하네스 적용 제안"으로 변경. "비서 모드로 직접 수정" → "PM(대화) 상태에서 직접 수정"으로 v2 용어 갱신. "하네스 미적용" → "태스크 파이프라인 미적용"으로 명확화.
- **완료 기준**: (1) "PM 전환 제안" 문구 제거됨 (2) "하네스 적용 제안" 문구 존재 (3) "그냥 해" 동작이 역할 전환 섹션(Step 1)과 일관됨
- **테스트**: Step 1의 "그냥 해" 정의와 비교하여 일관성 확인
- **의존**: Step 1 (역할 전환 용어 확정 후)

### Step 4: 보고 형식 PM 표기 v2 갱신 (R2)
- [x] 완료
- **파일**: `opal/core/AGENT.md` (175~208행, 특히 198~208행)
- **작업 내용**: 기존 PM 모드 표시 + Observability 선언(198~208행)을 삭제하고 역할별 응답 표기 테이블로 교체. 비서(`알투:`), PM(대화)(`알투[PM]:`), PM(태스크)(`📋 알투[PM]: ...`) 3가지. Observability 범위를 PM(태스크) 한정으로 명확화.
- **완료 기준**: (1) 3가지 표기가 테이블로 정리됨 (2) `알투[PM]:` PM 대화 표기 정의됨 (3) `알투:` 비서 표기 정의됨 (4) Observability가 PM(태스크) 한정으로 명시됨
- **테스트**: 테이블의 3행이 TASK.md R2 AC와 일치하는지 확인
- **의존**: Step 1 (PM(대화)/PM(태스크) 용어 확정 후)

### Step 5: code-scan 라벨 변경 + 변경이력 추가
- [x] 완료
- **파일**: `opal/core/AGENT.md` (130행, 327~339행)
- **작업 내용**: (1) `### code-scan 활용 규칙 (비서 모드)` → `### code-scan 활용 규칙`으로 라벨 변경 (v2에서 프로젝트 내 비서 모드 없음). (2) 변경이력 테이블에 v1.9 행 추가: 역할 전환 v2 변경 요약.
- **완료 기준**: (1) "(비서 모드)" 라벨 제거됨 (2) 변경이력에 112 태스크 기록 존재
- **테스트**: Grep으로 "(비서 모드)" 잔존 여부 확인
- **의존**: Step 1~4 (전체 변경 완료 후 변경이력 기록)

## 4. QA 체크리스트

### 기능 테스트
- [x] R1: 상태 정의(비서/PM) 테이블 존재, PM 활성 조건이 "`.opal/AGENT.md` 존재"
- [x] R1: PM 내 하네스 적용 기준 테이블에 "대화"/"태스크" 2행 존재
- [x] R1: 비서→PM 자동 전환 트리거 존재, "제안 후 대기" 방식 제거됨
- [x] R1: PM→비서 복귀 트리거 존재
- [x] R1: 소유자 오버라이드에 "그냥 해"/"비서로"/`//` 3가지 포함
- [x] R2: PM 대화 표기(`알투[PM]:`), PM 태스크 표기(`📋 알투[PM]:...`), 비서 표기(`알투:`) 3가지 테이블
- [x] R3: "PM 전환 제안" 제거, "하네스 적용 제안"으로 변경, "그냥 해" 동작 v2 일관
- [x] R4: 부트스트랩 체크리스트에 PM 모드 진입 여부 표시 존재
- [x] R5: "그냥 해" 유지 항목(Guards, @header, OPAL Tools) / 미적용 항목(파이프라인, STATE, Gate, Observability, 워커 디스패치) 테이블 존재

### 일관성 테스트
- [x] 역할 전환 섹션의 용어(PM(대화)/PM(태스크)/비서)가 보고 형식, 주도성 섹션에서도 동일하게 사용됨
- [x] "그냥 해" 정의가 역할 전환 섹션과 주도성 섹션에서 일관됨
- [x] 기존 `//` 커맨드 처리 흐름에 영향 없음 (쌍슬래시 커맨드 섹션 변경 없음)
- [x] 하네스 참조(§1 Guards, §5 Observability 등) 번호가 현행 하네스와 일치
- [x] 부트스트랩 Eager 단계(1~7)의 기존 절차에 영향 없음

### 문서 품질
- [x] 한국어 본문 + 영어 코드/필드명 규칙을 따르는가
- [x] Markdown 테이블 정렬이 올바른가
- [x] 변경이력 버전 번호와 날짜가 올바른가

## 5. 리스크 및 대응

| 리스크 | 영향 | 대응 방안 |
|--------|------|----------|
| "그냥 해" 시 Guards 구현 금지 원칙과 실행 허가의 관계가 모호 | 소유자가 "그냥 해" 시 실행 허가로 간주해야 하나 불명확 | "그냥 해" = 실행 허가 포함으로 명시. Guards 행에 괄호로 설명 추가 |
| code-scan "(비서 모드)" 라벨 변경이 TASK.md R1~R5에 미포함 | R1~R5 AC에는 없지만 v2 일관성에 필요 | Step 5에 포함하되, 라벨 변경만 수행 (내용 변경 없음). PM에 보고 |
| 소스/배포 파일의 "소유자"/"캡틴" 차이 | v2 변경 후에도 배포 파일은 "캡틴" 유지 | 소스 파일 기준으로 작업. 배포는 install-mac.sh 실행 시 자동 반영 |
| Eager 단계에서 PM 모드 즉시 진입 시 `.opal/AGENT.md` 미존재 프로젝트 | PM 모드 미진입으로 비서 유지 — 의도된 동작 | 부트스트랩 체크리스트에서 비서 표시로 명확화 (R4) |
