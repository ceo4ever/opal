# GC CONVENTION REPORT — 20260917-0719

## 1. 헤더

- 실행 일시: 시작 2026-09-17 07:19:00 / 완료 2026-09-17 07:22:00 / 소요 3분
- 범위: `all` (docs/PROJECT.md "프로젝트 구성" prefix 매칭 결과 단일 영역 — 영역 분할 없이 단일 호출)
- 대상 파일 6개
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` 존재 — 해당 문서 기준 적용. 보조로 `opal/core/references/harness/header-rules.md`, `opal/core/references/harness/tool-output-contract.md`, `~/.opal/references/opal-doc-standard.md` §5, `docs/run-log/CONTRACT.md`, `docs/run-log/TRD.md`, `.opal/AGENT.md` §금지사항 적용
- APPLY 수행 여부: N (read-only 진단)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 1 |
| 심각도 분포 | Critical 0 / High 0 / Medium 0 / Low 1 / Info 0 |
| 자동 수정 가능 | 1 |
| 수동 조치 필요 | 0 |
| 파일별 상위 Top 5 | opal/tools/state-tool/tests/test_state_tool_run_log.py (1건) |
| 카테고리별 빈도 | 들여쓰기/포맷 (1 파일) |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 0 |

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

### High (0건)

### Medium (0건)

### Low (1건)

- [ ] GC-C001 [opal/tools/state-tool/tests/test_state_tool_run_log.py:1735-1737] 신규 추가된 `TestSchema10And11WithoutRunLogBlockUnchanged` 클래스 뒤에 빈 줄 3개(PEP8 관례상 2개 초과)
  - 카테고리: 들여쓰기
  - 위반 기준: 프레임워크 공식 style guide (PEP 8 — 최상위 클래스/함수 사이 빈 줄 2개 권장). `docs/CONVENTIONS.md`에 Python 빈 줄 규칙 명문 조항 없음 — T1 tier 참조
  - 설명: 이 diff가 신규 삽입한 `test_schema_1_1_task_without_run_log_block` 메서드 종료 직후 빈 줄이 3개(라인 1735~1737)이며, 바로 다음 최상위 클래스 `TestCompletenessCheckIndependentFromStructuralValidation` 사이 간격이 관례(2개)보다 1개 많다. 파일 내 다른 클래스 경계는 모두 빈 줄 2개를 사용한다
  - 해결 방안: 빈 줄 1개 제거해 2개로 정규화
  - 자동 수정: Y
  - 참조: https://peps.python.org/pep-0008/#blank-lines

### Info (0건)

---

## 4. 문서 업데이트 제안 (§9·§10, 트리거 발동 시만)

빈도 트리거(N=3 이상 파일)·새 카테고리 트리거 모두 미발동 — 해당 없음.

---

## 5. 문서 작성 유도 (해당 시)

`docs/CONVENTIONS.md` 존재 — 작성 유도 생략.

---

## 6. 세부 검토 근거 (참조용, [MUST] 확인 항목 대사)

이번 실행은 opal-pilot-gc 디스패치 프롬프트가 지정한 "특히 확인할 항목"을 전건 개별 검증했다. 근거는 실측(git diff)이며 추정하지 않았다.

1. **오류 코드 안정성** (`tool-output-contract.md` §오류 코드) — CONTRACT.md 변경분, `run_log_core.py`의 `validate_event()` 신규 분기, `state_tool.py`의 `_build_pm_activity_data()` docstring 갱신 모두 기존 `schema_invalid` 코드를 재사용했고 새 코드를 신설하지 않았다. 위반 없음.
2. **오류 코드 테이블 물리 분리** — `run_log_core.py`의 `RUN_LOG_ERROR_CODES`, `state_tool.py`의 `RUN_LOG_STATE_ERROR_CODES`·`ERROR_CODES` 3종 모두 이번 diff에서 병합·교차 참조되지 않았고 각 1회 정의 구조가 유지됨을 확인. 위반 없음.
3. **`@header` 현재 사실 원칙** — `run_log_core.py`·`state_tool.py`·`test_run_log_tool.py`·`test_state_tool_run_log.py` 4개 파일 모두 `description`/`exports`가 diff 내용과 일치하게 갱신됐고, 이전 값 뒤에 태스크 번호를 덧붙여 누적한 문장은 없다(교체 방식 준수). `test_state_tool_run_log.py`의 description은 "TASK-137 W-3 추가분은 …" "TASK-137 W-7은 …" 두 문장을 포함하나, 이는 현재 시점의 테스트 구성·근거를 설명하는 서술이지 이전 값에 이어붙인 이력 누적이 아니다(문장 전체가 현재 파일 상태에 대한 사실 진술). `exports`·`scenarios` 배열도 신규 클래스·시나리오 ID만 추가되어 사실과 일치. 위반 없음.
4. **수기 누적 이력 금지** (`.opal/AGENT.md` §금지사항, `opal-doc-standard.md` §5) — `docs/run-log/CONTRACT.md`, `opal/tools/state-tool/README.md` 두 Markdown 산출물의 diff를 확인한 결과 `변경이력`·`Changelog`·`history`·`revisions` 계열 헤더나 버전 표 신설 없음. 위반 없음.
5. **SSOT 중복** — `opal/tools/state-tool/README.md`의 `missing_pm_activity` 절 갱신분은 앵커 2종·항목 5키를 요약 서술하되 "정확한 조문은 CONTRACT §2.5가 소유한다"는 포인터로 끝나며, CONTRACT.md의 범위 한정 3항·대조 집합 정의·정렬 규칙 전문을 복제하지 않았다. 위반 없음.
6. **테스트 관례** — 두 테스트 파일의 신규 클래스(`TestPmActivityDataClosureInProcess`·`TestPmActivityDataClosureCli`·`TestPmActivityDataClosureScopeBoundary`·`TestCompletenessMissingPmActivity*`·`TestSchema10And11WithoutRunLogBlockUnchanged` 등) 전건이 `run.sh`/`core.append()` 직접 호출 + 디스크 조각·`state.json` 바이트 검사로 판정하며, `mock`/`patch`/`MagicMock`/`unittest.mock` import나 사용을 grep으로 재확인해도 검출되지 않았다. 위반 없음.
7. **단방향 의존** (`docs/run-log/TRD.md` D-5) — `run_log_core.py`의 신규 분기(`validate_event()` A4 폐쇄 검사)는 `actor`/`provenance`/`data` 인자만 참조하며 상태 파일 읽기나 `state_tool` import를 추가하지 않았다. 신규 테스트(`TestPmActivityDataClosureInProcess`)도 거부 후 `state.json`이 생성되지 않음을 직접 단언(assert)해 이 경계를 실측 검증한다. 위반 없음.

---

## 7. 실행 메타

- `checked_files` == `target_files` (6개, 전량 일치) — `status: partial` 강등 사유 없음
- `missing_capabilities`: 없음
- `check_status`: pass (Low 1건은 advisory이며 blocking 아님)
