# L3-JUDGMENT — [SUPERVISOR] 시나리오 6건 판정

> 실행 일시: 2026-08-22T00:15:00+09:00 | phase: L3-supervisor-judgment
> 판정 대상: `test-output/REPORT-A.md` · `REPORT-B.md` · `REPORT-C.md`
> 판정 기준: `TEST-SCENARIO.md` §3 L3 「공통 판정표 C-1~C-8」 + TS-023·024·025·029·030·031
> 규범 원문: `opal/core/AGENT.md` §보고 형식 (L185~L262)
> 3단 분리 상태: 규범 저자(PM) 불참 · 산출자 판정표 미열람 · 채점자 산출 불참 — **유지됨**

**종합 verdict: `fail`** — L3 6건 중 4건 pass, **2건 fail**(TS-024, TS-029).

---

## 1. 공통 판정표 적용 — 24칸

`[MUST] 규범 면제 항목 제외`: 골격 표는 `⑤ ▶️`를 "다음 액션이 있을 때"로 규정한다. REPORT-A는 승인이 불필요한 조사 결과형이므로 `▶️` 블록 부재는 규범 준수다. A의 **C-5·C-7은 N/A**로 처리하고 분모에서 제외한다.

### REPORT-A (조사 결과형 · 승인 불필요)

| # | 판정 | 근거 |
|---|------|------|
| C-1 | ☑ | L2 `▶ 다음 액션: 실측 결과 확인 (소요: 5분)` — 금지 3예시("~를 검토했습니다"·"~가 맞습니다"·"~를 관측했습니다")에 해당하지 않는 액션 지시. L1 "~요청으로 이해했습니다"는 ① 상태 재진술 헤더로 C-1 판정 범위 밖(C-1 문언 "첫 줄(상태 헤더 다음)") |
| C-2 | ☑ | L2 `(소요: 5분)` — 분 단위 명시 |
| C-3 | ☐ | 마지막 줄 L36 = `- 디스크 용량(du -sh) 기준 1위는 state-tool(2.2M)이나 ... 채택했다(유의).` — (i) 완료 항목 수/전체 수 **없음**, (ii) 잔여 항목 **없음**(📌 3항목 전부 `(유의)` 태그, `(잔여)` 0건) |
| C-4 | ☑ | 승인 요청 0건 — 혼재 성립 불가. 📌 3항목 전부 정보·유의로 단일 |
| C-5 | **N/A** | `## 🎯`(L6)·`## 📌`(L32) 사용, `## ▶️` 부재 — 승인 불요 보고이므로 규범 면제 |
| C-6 | ☑ | 📌 3항목 전부 집계 방법론 유의(확장자 한정·tests 제외·du 기준) — 소유자 결정 요구 0건 |
| C-7 | **N/A** | 승인 요청 자체가 없음 |
| C-8 | ☑ | `⭐`·`🔥`·`❗` 0건(grep 확인). 중요도는 `[상]`(L8)·`[중]`(L13)·`[하]`(L18)만. L24~28 `✅`는 어휘표 #13 "표 셀 안 항목 판정 전용" 범위 내 |

**소계 5/6** (유효 6칸, N/A 2칸 제외)

### REPORT-B (결정형 · 승인 필요)

| # | 판정 | 근거 |
|---|------|------|
| C-1 | ☑ | L2 `▶ 다음 액션: 권고안 승인 여부 확인 (소요: 1영업일)` — 상황 서술체 아님 |
| C-2 | ☑ | L2 `(소요: 1영업일)` — 영업일 단위 명시 |
| C-3 | ☐ | 마지막 줄 L46 = `질문: 안 3을 채택해 op-sdd-plan/spec/verify에 references/ 신설을 진행할까요, 다른 안을 원하시나요?` — 계수도 잔여도 없는 질문문. `(잔여)` 항목은 L35에 존재하나 **마지막 줄이 아님** |
| C-4 | ☑ | `▶️` 리스트 2항 전부 결정 항목(`- [ ]` 안 3 채택 여부 / `- [x]` 안1·안2 미채택 권고 확정). 정보는 📌(L35~36)로 분리 |
| C-5 | ☑ | `## 🎯`(L6) · `## 📌`(L33) · `## ▶️ 승인 요청`(L40) 3블록 전부 사용 |
| C-6 | ☑ | 📌 2항목 — L35 `(잔여)` 후속 확인 필요, L36 `(유의)` 실측치 42종. 둘 다 지금 답 불요 |
| C-7 | ☑ | 승인 요청 `- [ ]`(L42)이 `▶️` 블록 내부에 위치. 📌에 승인 항목 0건 |
| C-8 | ☑ | `⭐`류 0건. `[상]`·`[상]`·`[중]`만 사용. L27 `**안 3(권고)**`는 표 셀 내 문장 강조(어휘표 #7) |

**소계 7/8**

### REPORT-C (실패·오류형)

| # | 판정 | 근거 |
|---|------|------|
| C-1 | ☑ | L2 `▶ 다음 액션: 재현 결과 확인 (소요: 5분)` — 상황 서술체 아님 |
| C-2 | ☑ | L2 `(소요: 5분)` |
| C-3 | ☐ | 마지막 줄 L39 = `질문: README 보강을 별도 후속 태스크로 지금 등록할까요?` — 계수·잔여 모두 부재. 📌 1항목도 `(유의)` 태그 |
| C-4 | ☑ | `▶️` 리스트 1항(`- [x]` 권고 확정) 단일 — 정보 보고 혼재 0건. 정보는 📌(L30)로 분리 |
| C-5 | ☑ | `## 🎯`(L6) · `## 📌`(L28) · `## ▶️ 승인 요청`(L34) 3블록 전부 사용 |
| C-6 | ☑ | 📌 1항목 — L30 "README에 서브명령 미기술(유의)" 관측 서술. 결정 요구 없음 |
| C-7 | ☑ | 권고 확정 `- [x]`(L36)가 `▶️` 내부. 📌에 결정 항목 0건 |
| C-8 | ☑ | `⭐`류 0건. `[상]` ×3만 사용 |

**소계 7/8**

### 합계

| 구분 | 값 |
|---|---|
| 전체 칸 | 24 |
| N/A (규범 면제) | 2 — REPORT-A의 C-5·C-7 |
| 유효 분모 | **22** |
| ☑ 충족 | **19** |
| ☐ 미충족 | **3** — C-3 × 3건 전부 |

**미충족은 C-3 단일 항목에 3건 전부가 집중됐다.**

---

## 2. TS별 Pass/Fail 확정

| TS | 판정 조건 | 결과 | 근거 |
|----|----------|------|------|
| TS-023 | C-1 + C-2 동시 충족 | **pass** | 3건 전부 C-1 ☑ · C-2 ☑. 시간 표기 `5분`·`1영업일`·`5분` 전부 구체 단위, FAIL 신호("약간"·"금방") 0건 |
| TS-024 | C-1 + C-3 동시 충족 | **fail** | C-3이 3건 전부 ☐. 세 보고 모두 마지막 줄에 완료 계수·잔여 명시가 없다(A=유의 항목, B·C=질문문) |
| TS-025 | `- [ ]` == 1 AND C-4 | **pass** | 대상 = 승인 필요 보고 REPORT-B. `- [ ]` 1건(L42)·`- [x]` 1건(L43) → 미체크 정확히 1. C-4 ☑. REPORT-C는 `- [ ]` 0건이나 ▶️가 권고 확정 단일이라 "승인이 필요한 보고" 정의 밖 — 판정 대상 제외(§3-4 관측 기록) |
| TS-029 | 3건 × B-1~B-4 = 12칸 전건 해소 | **fail** | **11/12 해소**. 아래 대조표 참조 |
| TS-030 | C-8 — 2차 중요도 기호 0건 | **pass** | 3건 전부 `⭐`·`🔥`·`❗` 0건(grep 확인). 중요도 채널은 `[상]/[중]/[하]` 단일 |
| TS-031 | C-5 + C-6 + C-7 전건 충족 | **pass** | 대상 = 승인 필요 보고 B·C. 양쪽 모두 C-5 ☑ · C-6 ☑ · C-7 ☑. REPORT-A는 `▶️` 규범 면제로 대상 제외 |

### TS-029 대조표 — 문제 4종 × 3건

> **한계 명시(§3 TS-029 존중)**: before 원문이 태스크 폴더에 보존돼 있지 않다. 이 표는 **절대 개선폭이 아니라 문제 4종의 부재**를 판정한다. 정량 대조는 불가하다.

| # | before 문제 | after 판정 기준 | A | B | C |
|---|------------|----------------|---|---|---|
| B-1 | 첫 줄이 상황 서술이라 무엇을 할지 모른 채 본문을 읽어야 한다 | C-1 충족 | ☑ | ☑ | ☑ |
| B-2 | 소요 시간이 없어 지금 할지 미룰지 판단할 수 없다 | C-2 충족 | ☑ | ☑ | ☑ |
| B-3 | `▶️`에 승인 요청·정보 보고·질문이 섞여 결정 비용이 전가된다 | C-4 충족 | ☑ | ☑ | ☑ |
| B-4 | 마크다운 요소 용도가 고정돼 있지 않아 매번 다른 형태로 나온다 | 어휘표 12요소가 각 1의미로 사용 | ☑ | ☑ | **☐** |

**미해소 1칸 — REPORT-C × B-4**

- REPORT-C는 L10·L11·L16·L17·L22·L23에서 `- **원인**:` / `- **해결**:` 형태의 **행 선두 라벨 볼드**를 6회 사용한다.
- 어휘표는 볼드 의미를 위치로 분화하며("볼드는 **위치**로 의미를 분화한다: 단독 문단=초점 / 문장 내=강조 / 행 선두 라벨=정정"), **행 선두 라벨 볼드를 `**정정**`(#8, 앞선 서술의 철회·정정)에 배타 배정**한다.
- REPORT-C의 원인/해결 라벨은 정정이 아니므로, 동일 요소(행 선두 라벨 볼드)가 2의미를 갖게 됐다 → **1요소 1의미 붕괴**.
- 이는 산출자 과실이라기보다 **규범의 구조적 공백**이다 — 원칙 6이 "오류는 원인 → 해결 2단으로 쓴다"를 요구하는데 어휘표 13행 어디에도 그 라벨용 요소가 배정돼 있지 않다. §3-3 개선 지시 3번 참조.
- FAIL 신호 문언 그대로: "12칸 중 1칸이라도 미해소면 '개선됐다'의 근거가 성립하지 않는다."

> 참고 — 판정 유보 사례: REPORT-A는 L24~28 표에서 run.sh 유무를 `✅`로 표기한다. 어휘표 #13은 판정 이모지를 "표 셀 안 항목 판정 전용 — 본문 문장에는 쓰지 않는다"로 **위치 제약** 형태로 규정하며, A는 그 제약을 지켰다. 의미 제약(정정)을 어긴 REPORT-C 사례와 규칙 형태가 다르므로 미해소로 세지 않는다.

---

## 3. 규범 효과 판정 (감점 없는 관측 기록)

산출자 3명은 판정표를 열람하지 않고 `opal/core/AGENT.md` §보고 형식만 입력으로 받았다. 아래는 **규범 단독의 유도력** 관측이다.

### 3-1. 규범이 자연히 유도한 것

| # | 유도된 요소 | 관측 |
|---|-----------|------|
| 1 | 상단 2층 구조 | 3/3 동일 — L1 상태 재진술 + L2 `▶ 다음 액션: … (소요: N)` |
| 2 | 소요 시간 구체 단위 | 3/3 — `5분`·`1영업일`·`5분`. 비구체 표현("금방"·"곧") 0건 |
| 3 | 3블록 헤딩 + `---` 구분선 | 3/3 — `## 🎯 결론·근거` → `---` → `## 📌 잔여·보류·유의` → `---` → `## ▶️ 승인 요청` |
| 4 | 결론 항목 서식 | 3/3 — `> **1️⃣ 제목 [상]**` + 하위 `-` 근거 + 항목 끝 볼드 단독 문단(초점) 정확 재현 |
| 5 | `📌` 용도 분리 | 3/3 — 소유자 결정 항목 0건. 3어 태그 `(잔여)/(유의)` 실사용 |
| 6 | 승인 채널 단일화 | B·C 2/2 — `- [ ]`/`- [x]` + 하단 "승인해주시면 계속 진행하겠습니다"·"이의 없으면 이대로 진행하겠습니다" + 질문 1개 |
| 7 | 중요도 채널 단일화 | 3/3 — `[상]/[중]/[하]`만. `⭐` 2차 채널 0건 |
| 8 | **조건부 필수의 정확한 해석** | REPORT-A가 승인 불요 상황에서 `▶️`를 생략했다 — 골격 표 "다음 액션이 있을 때"를 산출자가 정확히 읽었다는 증거 |
| 9 | 오류 2단 서술 | REPORT-C 3항목 전부 원인 → 해결 순서(원칙 6). 사과·자책 서술 0건 |
| 10 | 1불릿 1문장 | 3/3 — 2문장 이상 늘어진 근거 불릿 0건 |

**규범 단독으로 골격·채널 단일화·조건부 필수까지 유도됐다 — 22칸 중 19칸이 판정표 없이 충족됐다.**

### 3-2. 규범이 유도하지 못한 것

| # | 미유도 요소 | 산출자 3인의 일치된 다른 해석 | 규범 측 원인 |
|---|-----------|---------------------------|------------|
| 1 | **마지막 줄 완료 계수(원칙 5)** | 3/3 전부 미이행. A=유의 항목으로 종료, B·C=`질문:` 줄로 종료 | **규범 내부 충돌** — 원칙 5는 "마지막 줄"에 계수를 요구하는데, 골격 코드블록 템플릿은 문서 최종 줄을 `질문: <1개>`로 못박는다. 같은 위치를 두 규정이 다투고, 산출자 3명 모두 시각적으로 강한 신호인 **골격 템플릿**을 택했다. TS-024 fail의 **단일 원인** |
| 2 | **첫 줄 동사형 종결(원칙 1)** | 3/3 전부 명사형 개조식 — "실측 결과 확인"·"권고안 승인 여부 확인"·"재현 결과 확인" | 원칙 1은 "동사형으로 연다"라고만 쓰고, 골격 템플릿의 `<액션>` 슬롯은 형태를 예시하지 않는다. 판정표 C-1은 「~한다/~하세요/명령어」 3형태를 명시하지만 **규범 원문에는 그 3형태가 없다**. C-1은 금지 3예시(상황 서술체) 기준으로 충족 처리했으나, 원칙 1의 문언 그대로는 0/3 |
| 3 | **원인/해결 라벨의 어휘 배정** | REPORT-C가 행 선두 볼드(`**원인**`/`**해결**`)를 사용 — 어휘표가 `**정정**`에 배타 배정한 위치 | 원칙 6이 "원인 → 해결 2단"을 **요구**하는데 어휘표 13행에 그 라벨용 요소가 **없다**. 규범이 요구한 구조를 표현할 어휘를 규범이 공급하지 않았다. TS-029 미해소 1칸의 원인 |
| 4 | **질문 ↔ 체크박스 대응** | REPORT-C의 `▶️`는 `- [x]` 1건뿐인데 질문 L39는 "지금 등록할까요?"로 체크박스에 없는 새 결정을 묻는다 | 원칙 7은 "질문 1개로 수렴한다"만 규정하고, 질문이 체크박스 항목을 가리켜야 한다는 대응 규칙이 없다. 결과적으로 소유자에게 체크박스 없는 결정이 1건 전가됐다(B-3의 잔재 형태) |

### 3-3. 개선 지시 — `opal/core/AGENT.md` §보고 형식 수정안

> Evaluator는 판정만 반환한다. 아래 수정의 반영은 PM(오케스트레이터) 책임이며, 본 채점자는 어떤 파일도 수정하지 않았다.

| # | 대상 문장 | 수정 방향 | 해소 대상 |
|---|----------|----------|----------|
| 1 | 원칙 5 — "완료는 눈에 보이게 한다 — 마지막 줄에 완료 항목 수/전체 수 또는 잔여 항목을 명시한다." | 위치 충돌을 제거한다. 예: "완료는 눈에 보이게 한다 — **`▶️` 체크리스트 직후·`질문:` 직전 줄**에 `완료: N/M` 또는 잔여 항목을 명시한다. `▶️`가 없는 보고는 `📌` 마지막 줄에 둔다." **동시에 골격 코드블록 템플릿에 `완료: N/M` 줄을 `질문: <1개>` 바로 위에 추가**한다 — 템플릿에 없으면 유도되지 않음이 3/3으로 실증됐다 | TS-024 (C-3 ×3) |
| 2 | 골격 표 ② 행 — "다음 액션 + 소요 시간(분/시간/영업일 등 구체 단위)" | 형태를 명시한다: "다음 액션(**동사형 종결 — 「~한다/~하세요/명령어」, 명사형 개조식 금지**) + 소요 시간". 템플릿 예시도 `▶ 다음 액션: 실측 결과를 확인한다 (소요: 15분)`로 교체 | 원칙 1 문언 준수 0/3 |
| 3 | 마크다운 어휘 표 #8 — "`**정정**`(라벨) \| 앞선 서술의 철회·정정" | 행 선두 라벨 볼드를 일반화하거나 전용 행을 신설한다. 예: "#8 `**{라벨}**`(행 선두) \| 라벨 — `**정정**`(철회·정정) / `**원인**`·`**해결**`(오류 2단)" — 원칙 6이 요구하는 구조에 어휘를 공급한다 | TS-029 B-4 미해소 1칸 |
| 4 | 원칙 7 — "의사결정은 체크리스트로 강제하고 질문 1개로 수렴한다" | 1문장을 덧붙인다: "**질문 1개는 `▶️` 체크박스 항목 중 하나를 가리켜야 한다 — 체크박스에 없는 새 결정을 질문으로만 묻지 않는다.**" | REPORT-C의 미대응 질문(B-3 잔재) |

---

## 4. 결론

| 항목 | 값 |
|------|-----|
| 충족 칸 | **19/22** (24칸 중 규범 면제 N/A 2칸 제외) |
| pass | TS-023 · TS-025 · TS-030 · TS-031 (4건) |
| **fail** | **TS-024** (C-3 3건 전부 미충족) · **TS-029** (12칸 중 11칸 해소) |
| 종합 verdict | **`fail`** |

**fail 사유 2건은 모두 산출자 과실이 아니라 규범 자체의 결함(원칙 5 ↔ 골격 템플릿 위치 충돌, 원칙 6 ↔ 어휘표 공백)이며, §3-3의 수정 4건을 반영한 뒤 경로 (1)로 재산출·재판정하면 12/12·22/22 도달이 가능하다.**

완료: 판정 6/6 · 미충족 시나리오 2건 · 개선 지시 4건
