`opal/skills/` 42종 중 `references/` 보유 여부를 실측하고 표준화 여부 결정안을 제시한다.
▶ 다음 액션: 권고안(2안) 채택 여부 확인 후 컨벤션 문서에 반영 (소요: 30분)

## 🎯 결론·근거

> **1️⃣ 42종 중 26종 보유, 16종 미보유 — 콘텐츠 상관성 없음 [상]**
- 전체 42개 스킬 중 `references/` 디렉토리 보유는 26개, 미보유는 16개다.
- `op-dev-plan`(보유, SKILL.md 470줄)과 `op-sdd-plan`(미보유, SKILL.md 428줄)은 같은 "plan" 계열인데도 구조가 다르다.
**보유 여부가 스킬 규모나 성격과 뚜렷하게 상관되지 않는다.**

> **2️⃣ personas/ 병존 패턴도 혼재 [중]**
- `op-dev-wireframe`(미보유)은 `personas/`만 있고, `opal-pilot-dev-wireframe`(보유)은 `references/`만 있다.
- 유사 이름의 두 스킬이 서로 다른 보조 디렉토리 구성을 쓴다.
**명명 유사 스킬 간에도 디렉토리 구성 규칙이 통일돼 있지 않다.**

| 안 | 내용 | 장점 | 단점 |
|---|---|---|---|
| 1안 | 전면 표준화 — 모든 스킬에 `references/` 필수화 | 구조 일관성 최대화 | 참조 콘텐츠 없는 16개 스킬에 빈 디렉토리 강제 생성 |
| 2안 (권고) | 콘텐츠 기준 표준화 — "외부 참조 자료 존재 시에만 `references/` 생성" 원칙을 컨벤션에 명문화, 기존 26/16 분포는 그대로 두고 신규 스킬부터 적용 | 빈 디렉토리 방지 + 향후 일관성 확보 | 기존 스킬 재정비는 별도 작업 필요(즉시 해소 안 됨) |
| 3안 | 현행 유지 — 규칙 미도입 | 작업량 0 | `op-dev-plan`/`op-sdd-plan` 같은 불일치가 계속 재생산됨 |

## 📌 잔여·보류·유의

- 기존 26개 보유 스킬의 `references/` 내용이 실제로 "외부 참조 자료" 기준에 부합하는지는 이번 조사 범위 밖이다(디렉토리 존재만 실측).
- 2안 채택 시 기존 스킬 16개 중 콘텐츠 기준 재분류 작업은 별건 태스크로 분리한다.

## ▶️ 승인 요청

- [ ] 2안(콘텐츠 기준 표준화)을 채택해 컨벤션 문서에 반영할지 승인
- [x] 1안(전면 표준화)은 빈 디렉토리 양산 부작용으로 배제 확정

완료: 1/2
질문: 2안(콘텐츠 기준 표준화) 채택을 승인해주시면 컨벤션 문서 반영을 계속 진행하겠습니다.
