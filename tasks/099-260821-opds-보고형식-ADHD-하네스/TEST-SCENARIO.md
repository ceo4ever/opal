# TEST SCENARIO: 보고 형식 개정 — ADHD 보고 하네스 v1.0

> 작성일: 2026-08-21 | 상태: **작성 완료 (Block A 선작성 + Block B 보강)**
> 작성자: 알투(PM) + 캡틴 페어 | 도출 입력: TASK.md(Block A) + PLAN.md F-001~006·H-1~H-9(Block B)

## 결론

- 총 **33건** — Block B 흡수 22건(TS-001~022) + Block A 고유 6건(TS-023~028) + **게이트 iteration 1 보강 3건**(TS-029~031) + TS-021b.
- 계층 분포: L1 정적 **25**건(TS-001~022 22 + TS-026·027·028 3) / L2 통합 1건(TS-021b) / L3 [SUPERVISOR] **6**건 = **32건**.
- **iteration 1 → 2 보강**: goal 1 / adoption 1 / boundary 2 (평균 1.33, 임계 1.5 미달)를 gaps 6건 전건 반영으로 해소했다.
  - ①축 — 판정을 "캡틴 육안"에서 **공통 판정표 C-1~C-8 충족 개수**로 교체(G-1), 재배포 없는 **공통 실행 경로 (1)** 신설로 검증 이연 해소(G-2), **TS-029 문제 4종 해소 대조** 신설(G-3). ①은 **1점 유지**였고 iteration 2 이월 4건을 추가 반영했다.
  - ⑤축 — **TS-030**(`⭐` 산출물 계층 잔존 0) 신설로 교체 주장 (a) 커버(G-4), **TS-031**(3블록 실사용) 신설로 **신형 채택 측 0건** 해소(G-5). TS-014는 배치 검사이므로 `is_adoption_scenario`를 false로 정정.
  - ⑥축 — TS-027 입력·판정식을 TS-026 동형으로 특정(G-6).
- **Block A 고유 6건 + 보강 3건 = L3 6건이 PLAN TS-001~022에 대응 0건**이다. PLAN 유래는 전부 문자열 존재 검사였다.
- **평가자 가점 3건 훼손 없음** — §4 「목표」 행에 L1 문자열 TS 미혼입 / §2.2 `[MUST]` 판정식 작성 규율(097 교훈) / TS-015·TS-016 상반 방향 쌍(H-3·H-4 동시 방어).
- **PLAN 체크리스트 보정 완료** — 검증 Step 대상이 `TS-001~TS-022`로 한정돼 TS-023~031이 실행되지 않을 구조였다(SCENARIO-GATE-1 §4 PM 확인 권고). PM이 `TS-001~TS-031`로 보정했다.

---

## 1. 리스크 가설 표

> PLAN.md §리스크 가설 표 H-1~H-9 전건 전재 (보강 완료 판정 조건 2).

| ID | 변경 단위 | 깨질 수 있는 계약 | 운영 영향 | 검증 계층 | 시나리오 |
|----|----------|----------------|---------|---------|---------|
| H-1 | F-001~005 (AGENT.md 순증) | **lean core** — 매 세션 Phase A 상시 로드(`opal/core/AGENT.md:17`), 050의 493→236 경감을 부분 되돌림 | P1 | L1 | TS-022 |
| H-2 | F-002 (어휘표) | **배포 strip** — `strip_deploy_md`는 `^## 변경이력$`만 제거(`scripts/install-mac.sh:222`), 어휘표는 런타임 100% 계상 | P1 | L1 | TS-008 |
| H-3 | F-003·F-004 (원칙 재편) | **규범 비손실** — 기존 원칙 5개 문구 소실 시 현행 규범이 조용히 폐기(`opal/core/AGENT.md:210-214`) | **P0** | L1 | TS-015 |
| H-4 | F-004 (도구 한정) | **상호모순** — 현행 무조건 강제(`:210`)에 "대등할 때만"을 추가만 하면 결정 불능 | **P0** | L1 | TS-016 |
| H-5 | F-002 (볼드 다중 용도) | **1요소 1의미**(D-4) — 볼드가 제목·초점·강조·`**정정**` 4용도 | P1 | L1 | TS-007 |
| H-6 | F-006 (`opal-pm.md` §8) | **발췌·복제 금지** — 포인터 절에 규칙 원문 이전 시 SSOT 2중화 | **P0** | L1 | TS-018 |
| H-7 | F-006 (변경이력) | **버전 채번** — `v4.3`·`v4.4` 각 2회 중복 실재(`:246-249`), 최신 행 미기준 시 3중복 | P1 | L1 | TS-020 |
| H-8 | 전체 (동시 세션) | **파일 점유** — 098이 14파일 점유, `docs/` 2파일은 읽기만 허용 | P1 | L1 | TS-021 |
| H-9 | F-001 (적용 범위) | **비보고 예외** — 한 줄 답변에 4층 골격 강제 시 가독성 역전(`:187`) | P1 | L1 | TS-004 |

---

## 2. 테스트 데이터 설계

### 2.1 사전 조건 데이터

DB 미사용. 검증 입력은 **실 파일**이다 (mock 금지 — `state-tool` 테스트 관행).

| 입력 | 경로 | 용도 |
|------|------|------|
| 개정 대상 1 | `opal/core/AGENT.md` | §보고 형식 `:185-232` · 변경이력 `:234-254` |
| 개정 대상 2 | `opal/core/references/opal-pm.md` | §8 `:100-109` |
| 기준선(before) | `git show HEAD:<경로>` | 증감 줄 수·규범 문구 대조 |
| strip 시뮬레이터 | `awk '/^## 변경이력$/{exit}{print}'` | 런타임 줄 수 산출 (H-2) |
| 격리 대조 | `git status --porcelain` | 098 점유 14파일 미접촉 (H-8) |

### 2.2 시나리오별 데이터 흐름

| 축 | 흐름 |
|---|---|
| 정적 문구 (TS-001~020) | 개정 후 파일 → **절 범위 특정** → 해당 구간 grep → 기대 문자열 대조 |
| 정량 (TS-008·022) | 개정 후 파일 → `wc -l` / strip 후 `wc -l` → 상한 비교 |
| 회귀 (TS-015) | before `git show` → 핵심 문구 5종 추출 → after 잔존 대조 |
| 격리 (TS-021) | `git status --porcelain` → 신규 변경 파일 집합 → 기대 집합과 차집합 |
| 대조군 (TS-026·027) | TS-014·016의 판정식 → 금지 목록 표 자체를 입력 → **통과해야 함** |
| 목표달성·채택 (TS-023~025·029~031) | 개정본 §보고 형식 주입(**재배포 불요**, 공통 실행 경로 (1)) → 보고 1건 산출 → **공통 판정표 C-1~C-8** 충족 개수 |
| 문제 해소 (TS-029) | `TASK.md` §배경 문제 4종(정성) → 경로 (1)로 보고 **3건** 산출 → 3×4=12칸 해소 대조표 (**before 원문 부재 — 축소 재정의**) |

> **[MUST] 판정식 작성 규율**: 문자열 존재가 아니라 **어느 절의 문자열인가**로 판정한다. 097 교훈 — "grep AC는 문자열 존재가 아니라 '어느 층의 문자열인가'를 판정해야 한다"(`.opal/MEMORY.json` history §097). 절 범위 특정 없이 파일 전체 grep하면 금지 목록·변경이력·설명 문언이 전부 히트해 오탐한다.

---

## 3. 검증 시나리오

### L1. 정적 판정 (자동, 실 파일 입력)

#### F-001 — 골격 3블록화 + 상단 2층

| TS | 가설 | Pass 조건 |
|----|------|----------|
| TS-001 | — | §보고 형식 내에 `## 🎯 결론·근거` · `## 📌 잔여·보류·유의` · `## ▶️ 승인 요청` 3종이 각 1회 이상 |
| TS-002 | — | `📌 잔여·보류·유의`의 용도가 "지금 답이 불필요" 취지로 **1문장 이상** 기술 |
| TS-003 | — | 액션 블록 규정에 "다음 액션"과 "소요 시간"이 **동시** 명시 |
| TS-004 | **H-9** | 비보고 자유 형식 예외 문구가 **잔존**하고, 신규 2층의 적용 범위가 "보고 응답"으로 한정 기술 |
| TS-032 | — | (R-0) `📌 잔여·보류·유의` 3어 매핑이 각 1줄 이상 기술 — 잔여/보류/유의 수용 대상 각각 |

#### F-002 — 마크다운 어휘 소절

| TS | 가설 | Pass 조건 |
|----|------|----------|
| TS-005 | — | 채택 표 데이터 행 **13개**, R-2 AC 13요소와 1:1 (판정 이모지 ✅/❌/⚠️ 행 포함, 「표 셀 전용」 제약 명시) |
| TS-006 | — | 금지 표 데이터 행 **9개**, R-2 AC 9건과 1:1 (`🚫` 행 포함) |
| TS-007 | **H-5** | "볼드는 **위치**로 의미를 분화한다" 취지 규정 1줄 존재 + 볼드 4행이 위치 기준으로 상호 배타 기술 |
| TS-008 | **H-2** | `awk '/^## 변경이력$/{exit}{print}' opal/core/AGENT.md`  결과에 어휘표가 **포함**됨 (런타임 계상 확인) |

#### F-003·F-004·F-005 — 원칙 절 재편 (10항목)

| TS | 가설 | Pass 조건 |
|----|------|----------|
| TS-009 | — | 신규 규칙 6건이 **6개 독립 불릿**. 한 불릿에 2건 이상 뭉치면 FAIL |
| TS-010 | — | 시간 추정 항목에 "분/시간/영업일" 등 **구체 단위** 문구 존재 |
| TS-011 | — | 오류 톤 항목에 "원인 → 해결 2단" 취지 문구 존재 |
| TS-012 | — | 승인 항목이 `- [ ] N) <제목>` + 하위 `-` 근거 형태로 규정되고, **`- [x]` 금지** 문구가 존재 |
| TS-013 | — | "승인 항목 1개로 수렴" 취지 문구 + `AskUserQuestion` 한정 조건이 **각각** 존재. 별도 `질문:` 줄이 템플릿에 **없음** |
| TS-014 | — | 중요도 단일화 규정 원문이 §보고 형식 내 **1곳에만** 기재 |
| TS-015 | **H-3** | 기존 5개 원칙 핵심 문구 **5종 전부 잔존** — `AskUserQuestion` · `자동 진행 금지` · `1불릿 1문장` · `[상]`/`[중]`/`[하]` · `자율` |
| TS-016 | **H-4** | "무조건/반드시 호출" 취지 문장 **0건**, `AskUserQuestion` 언급이 1개 항목으로 수렴 |

#### F-006 — 연동 문서 정합

| TS | 가설 | Pass 조건 |
|----|------|----------|
| TS-017 | — | `opal-pm.md` §8 본문이 3블록 골격 블록명을 **언급** |
| TS-018 | **H-6** | §8 본문에 어휘표·규칙 원문 **0건**, AGENT.md 포인터 유지 |
| TS-019 | — | 양 파일 변경이력에 `(099)` 표기 행이 **각 1행** |
| TS-020 | **H-7** | 신규 행 버전이 `v4.8`/`v1.8`이고 `grep -c "^| v4.8"` == **1** |

#### 전역 — 격리 · 상한

| TS | 가설 | Pass 조건 |
|----|------|----------|
| TS-021 | **H-8** | `git status --porcelain`의 본 태스크 신규 변경이 `opal/core/AGENT.md` · `opal/core/references/opal-pm.md` · `tasks/099-*/` **3항목에 한정** |
| TS-022 | **H-1** | **[폐기 2026-08-22]** 줄 수 상한 3종(95·305·285) 캡틴 지시로 삭제 — 판정 대상 아님. 대체 검사: `opal/core/AGENT.md` §보고 형식 외 절 **무변경**(`git diff` hunk 범위 한정) |

---

### ⑤⑥ Block A 고유 — 교체 검증 · 과잉 차단 대조군

#### TS-026 [대조군] 금지 요소를 **설명하는 문언**은 FAIL 처리되지 않는다

- **가설**: H-4·H-5 판정식의 오탐 (신규 — PLAN 미도출)
- **전제**: TS-014·TS-016의 판정식.
- **실행**: 금지 목록 표 자체(취소선·`<u>`·`⭐`를 문자열로 나열하는 8행)를 판정식에 입력한다.
- **기대**: **통과**. 금지 규칙을 서술하는 문서 자신이 FAIL하면 메타-순환 오탐이다.
- **근거**: 동형 선례 — `opal/tools/state-tool/state_tool.py:2010,2025`가 034에서 인라인 백틱 구간 제거(`re.sub`) 전처리로 해소.

#### TS-027 [대조군] 정상 사용 요소가 금지로 오탐되지 않는다

- **가설**: H-5 판정식의 오탐 (신규 — PLAN 미도출)
- **입력**: 개정 후 `opal/core/AGENT.md` §보고 형식 **본문 전체**.
- **판정식**: TS-006(금지 8행 대조)·TS-007(볼드 위치 분화)에서 쓰는 **금지 판정식 그대로**.
- **기대**: 오탐 **0건** — 본문에서 정상 사용된 표·인용문·볼드·인라인 코드가 금지로 걸리지 않는다.
- **FAIL 신호**: 정상 사용 요소가 1건이라도 금지로 판정되면, 판정식이 "어느 절의 문자열인가"를 보지 않는다는 뜻이다.

#### TS-028 [교체] 구 골격(2블록) 서술이 잔존 0

- **가설**: 교체형 목표 — 잔존 기준 (신규 — PLAN 미도출)
- **실행**: §보고 형식에서 "🎯 결론·근거 → ▶️ 진행" 형태의 **2블록 골격 서술**을 검색한다.
- **기대**: **0건**. 3블록으로 전면 교체.
- **FAIL 신호**: 2블록 문장이 병존해 어느 쪽이 규범인지 모호.

---

### L2. 통합 (실 파일 read → 판정 → re-read)

#### TS-021b [격리 재확인] 098 점유 14파일 diff 불변

- **가설**: H-8
- **실행**: 099 착수 전 스냅샷(`git diff --stat` 098 14파일)과 EXECUTE 후를 대조한다.
- **기대**: 14파일의 diff 내용·줄 수 **동일**.
- **FAIL 신호**: 타 세션 산출물 오염 → **즉시 블로커**.

---

### L3. 사용자 협업 (수동, [SUPERVISOR] 마커)

> 이 6건은 **grep으로 환원되지 않는다**. "읽는 즉시 행동으로 이어진다"는 문자열 존재가 아니라 독자 반응이다. PLAN TS-001~022에 대응 0건이며, 선작성 트랙을 켠 핵심 근거다.

#### [MUST] 공통 실행 경로 — 재배포 없이 실행한다

L3 6건의 **판정 대상 산출물**은 "개정본 §보고 형식을 규범으로 삼아 산출한 보고 1건"이다. install 재배포를 전제하지 않는다.

| 경로 | 내용 | 전제 |
|------|------|------|
| **(1) 기본 — 재배포 불요** | 개정 후 `opal/core/AGENT.md` §보고 형식을 입력으로 주입하고, 그 규범대로 보고 1건을 산출해 판정한다 | **없음** — TEST 단계에서 즉시 실행 가능 |
| (2) 대체 — 재배포 후 | install 재배포 + 새 세션에서 자연 발생 보고를 판정한다 | 소유자 지시 필요 |

- **[MUST] 산출 주체 2단 분리 (self-confirming 차단)**: (i) **산출 시점** — 산출자는 「공통 판정표」를 **보지 않고** 개정본 §보고 형식만 입력으로 받아 보고 1건을 만든다. (ii) **판정 시점** — 산출 후 [SUPERVISOR]가 판정표를 채운다. 이 분리가 없으면 측정되는 것은 규범 효과가 아니라 **체크리스트 준수 능력**이다.
- **[MUST]** 경로 (1)로 TEST 단계에서 실행한다. `TASK.md:105`·본 문서 §6이 "install 재배포 미실행 — 소유자 지시 대기"를 못박고 있으므로, (2)를 전제하면 검증이 CLOSE 이후로 이연된다.
- 경로 (2)는 **CLOSE 후 잔여 검증**으로 남긴다 — 재배포 시점에 6건을 1회 재확인하고 DONE.md에 기록한다.

#### [MUST] 공통 판정표 — 사전등록

판정은 "캡틴 육안"이 아니라 아래 체크 항목의 **충족 개수**로 확정한다. 실행자는 이 표를 그대로 채운다.

| # | 체크 항목 | 판정 |
|---|----------|------|
| C-1 | 첫 줄(상태 헤더 다음)이 **동사형 액션**이다 — 「~한다/~하세요/명령어」 형태. **상황 서술체 금지**: "~를 검토했습니다"·"~가 맞습니다"·"~를 관측했습니다" | ☐ |
| C-2 | 소요 시간이 **분/시간/영업일** 단위로 명시됐다 | ☐ |
| C-3 | **① 상태 재진술 헤더**가 진행/완료 상태를 계수형으로 담는다 — (i) `N행 중 M행 완료` 류 수치 또는 (ii) 잔여 항목 명시 | ☐ |
| C-4 | 승인 요청과 정보 보고가 **같은 리스트에 혼재하지 않는다** | ☐ |
| C-5 | `🎯` · `📌` · `▶️` 3블록이 **모두 사용**됐다 | ☐ |
| C-6 | `📌 잔여·보류·유의`에 **지금 답이 불필요한 항목만** 들어갔다 | ☐ |
| C-7 | 승인 요청이 `📌`가 아니라 **`▶️`에 있고, `▶️`에 `- [x]` 항목이 0개**다 | ☐ |
| C-8 | `⭐` 등 **2차 중요도 채널 출현 0건** | ☐ |

#### TS-023 [SUPERVISOR] 첫 줄이 행동을 준다

- **가설**: 목표 달성 (①축)
- **입력**: 공통 실행 경로 (1)로 산출한 보고 1건.
- **판정**: **C-1 + C-2 동시 충족**.
- **FAIL 신호**: 첫 줄이 "~를 검토했습니다" / "~가 맞습니다" 류 상황 서술 / 시간 표기가 "약간"·"금방" 등 비단위.

#### TS-024 [SUPERVISOR] 첫 줄과 마지막 줄만으로 판단이 선다

- **가설**: 목표 달성 (①축)
- **입력**: TS-023과 동일 보고의 **첫 줄·마지막 줄만** 발췌.
- **판정**: **C-1 + C-3 동시 충족** — 2항 체크. "판별된다"는 산문 판정을 쓰지 않는다.
- **FAIL 신호**: 두 줄 중 하나라도 없으면 본문을 읽어야 판단이 서는 구조다.

#### TS-025 [SUPERVISOR] `▶️`에서 답할 것이 하나로 보인다

- **가설**: 목표 달성 (①축) — PLAN TS-013은 "문구 존재"만 판정하므로 실동작 미검증
- **입력**: 승인이 필요한 보고 1건.
- **판정**: `- [ ]` 개수 == **1** **AND** `- [x]` 개수 == **0** **AND** C-4 충족.
- **FAIL 신호**: 미체크 2개 이상 / 승인·정보 항목 혼재.

#### TS-029 [SUPERVISOR] 진단된 문제 4종이 신규 규범 산출물에서 해소된다

- **가설**: 목표 달성 (①축) — **개선의 근거** (신규 G-3 / iteration 2에서 **축소 재정의**)
- **[정정] before 원문 부재** — `TASK.md:13-18`은 표본 보고 3건에서 **관측된 문제 4종의 요약**만 담고 있고, 표본 원문은 태스크 폴더 어디에도 보존돼 있지 않다(전수 grep 확인, SCENARIO-GATE-2 §4-1 #4). 원문 재작성 대조는 **실행 불가**이므로 아래로 축소 재정의한다.
- **before 앵커**: `TASK.md` §배경이 서술한 **문제 4종**(정성 기술). 원문 대조가 아니라 **문제 해소 여부** 판정이다.
- **입력**: 공통 실행 경로 (1)로 산출한 보고 **3건**(단일 표본의 대표성 결여를 상쇄).
- **판정**: 3건 × 4항 = **12칸 전건 해소**.

| # | before 문제 (`TASK.md` §배경) | after 판정 |
|---|------------------------------|-----------|
| B-1 | 첫 줄이 상황 서술이라 무엇을 할지 모른 채 본문을 읽어야 한다 | C-1 충족 |
| B-2 | 소요 시간이 없어 지금 할지 미룰지 판단할 수 없다 | C-2 충족 |
| B-3 | `▶️`에 승인 요청·정보 보고·질문이 섞여 결정 비용이 전가된다 | C-4 충족 |
| B-4 | 마크다운 요소 용도가 고정돼 있지 않아 매번 다른 형태로 나온다 | 어휘표 12요소가 각 1의미로 사용 |

- **FAIL 신호**: 12칸 중 1칸이라도 미해소면 "개선됐다"의 근거가 성립하지 않는다.
- **한계 명시**: 원문 before가 없으므로 이 시나리오는 **절대 개선폭**이 아니라 **문제 4종의 부재**를 판정한다. 정량 대조는 불가하다.

#### TS-030 [SUPERVISOR] `⭐` 2차 중요도 채널 잔존 0 — 산출물 계층

- **가설**: 채택/잔존 (⑤축) — **교체 주장 (a)** (신규, G-4)
- **왜 산출물 계층인가**: `⭐`는 규범 문서가 아니라 **표본 보고에서 관측**됐다(`TASK.md:99`). 문서 grep으로는 잔존 0을 말할 수 없다.
- **입력**: 공통 실행 경로 (1)로 산출한 보고 1건.
- **판정**: **C-8 충족** — `⭐`·`🔥`·`❗` 등 2차 중요도 기호 출현 **0건**, 중요도는 `[상]/[중]/[하]`로만 표기.
- **CLOSE 고정**: 이 검사를 CLOSE 체크 1항으로 고정한다 — PLAN FB-3(어휘표 강제 수단 부재)이 후속으로 미룬 "보고 산출물 감사"를 본 태스크 범위 안에서 1회 집행하는 형태다.

#### TS-031 [SUPERVISOR] 신형 3블록 골격이 실제로 채택된다

- **가설**: 채택/잔존 (⑤축) — **신형 채택 측** (신규, G-5)
- **왜 필요한가**: 기존 「교체」 행이 TS-016·TS-028(둘 다 **잔존 0** 방향)뿐이라 신형 채택 검증이 0건이었다.
- **입력**: 공통 실행 경로 (1)로 산출한 보고 1건.
- **판정**: **C-5 + C-6 + C-7 전건 충족**.
- **FAIL 신호**: 3블록 중 하나라도 미사용 / `📌`에 소유자 결정이 필요한 항목이 들어감 / 승인 요청이 `📌`에 위치.
- **비고**: TS-023(액션 블록)·TS-025(체크박스)는 골격의 **부분**만 덮는다 — 3블록 실사용은 이 시나리오가 유일하게 판정한다.

---

## 4. AC ↔ 가설 ↔ 계층 ↔ 시나리오 매핑 표

| R | AC 요지 | 가설 ID | 계층 | 시나리오 |
|---|---------|---------|------|----------|
| R-0 | 3어 매핑 각 1줄 | — | L1 | TS-032 |
| R-1 | 3블록 헤딩 + `📌` 용도 정의 + 액션 블록 2요소 | H-9 | L1 + L3 | TS-001, TS-002, TS-003, TS-004, TS-031 |
| R-2 | 채택 12행 + 금지 8행 | H-2, H-5 | L1 | TS-005, TS-006, TS-007, TS-008 |
| R-3 | 6개 독립 항목 + 구체 단위 + 원인→해결 2단 | H-3 | L1 | TS-009, TS-010, TS-011, TS-015 |
| R-4 | 체크박스 2종 + 질문 1개 + 도구 한정 | H-4 | L1 + L3 | TS-012, TS-013, TS-016, TS-025 |
| R-5 | 중요도 단일화 + 1곳 기재 | H-3 | L1 + **L3** | TS-014, **TS-030** |
| R-6 | §8 포인터 + 변경이력 각 1행 + 채번 | H-6, H-7 | L1 | TS-017, TS-018, TS-019, TS-020 |
| 목표 | 읽는 즉시 행동으로 이어진다 | — (①축, 가설 무관) | **L3** | TS-023, TS-024, TS-025, **TS-029**, **TS-031** |
| 교체 — 잔존 0 | 구 골격 · 무조건 강제 문장 · `⭐` 채널 | H-4 | L1 + **L3** | TS-016, TS-028, **TS-030** |
| 교체 — **신형 채택** | 3블록 골격이 실제로 쓰인다 | — | **L3** | **TS-031** |
| 경계 | 과잉 차단 0 | H-4, H-5 | L1 | TS-026, TS-027 |
| 제약 | lean core 상한 · 타 세션 미오염 | H-1, H-8 | L1 + L2 | TS-022, TS-021, TS-021b |

> 전 R(6/6) 커버 · 전 F(F-001~006) 커버 · 전 H(H-1~9) 커버. 미매핑 0.

> **[정정] TS-014는 채택 시나리오가 아니다** — Pass 조건이 "규정 원문이 1곳에만 기재"(배치·중복 검사)이므로 `is_adoption_scenario`를 **false**로 되돌렸다. 교체 주장 (a) `⭐` 잔존 0은 **TS-030**(산출물 계층)이 담당한다. 근거: SCENARIO-GATE-1 G-4 — "`⭐`는 규범 문서가 아니라 표본 보고에서 관측된 것이므로 문서 grep으로는 잔존 0을 말할 수 없다"(`TASK.md:99`).

---

## 5. 코드 품질

- 코드 변경 0줄 — 린트·타입·포맷 **N/A**. `git diff --stat`에 `.md` 외 확장자 0건으로 검증.
- 신규 파일 생성 **0건** (쟁점 A 채택안 (c) — `harness/report-format.md` 미생성).
- 마크다운 구조: 헤딩 위계 무붕괴 · 표 열 수 일치 · 코드펜스 개폐 균형.
- 변경이력 형식: `docs/CONVENTIONS.md` §변경이력 작성 의무 준수 (KST `YYYY-MM-DD HH:mm` + semver + `(099)`).
- §보고 형식 외 절 **무변경** — `git diff` hunk가 §보고 형식 + 변경이력에 한정.

## 6. 보안

- 시크릿 스캔: 변경 2파일에 자격증명·토큰·절대 홈경로 노출 **0건**.
- `.gitignore`: 신규 파일 0건이므로 갱신 불요.
- **개인 식별자**: 개정 문언에 소유자 호칭 리터럴("캡틴")을 신규 하드코딩하지 않는다 — 역할 일반어("소유자") 또는 `{owner_name}` 사용. TASK.md §(5) 별건 분리 범위(83건)를 늘리지 않기 위함.
- `~/.opal/` 배포 파일 직접 편집 **0건** — `git status`에 프로젝트 소스만.
- `git commit`·`push`·`reset`·`rebase` **미실행** — 변경은 워킹트리 잔류.
- install 재배포 **미실행** — 소유자 지시 대기.

## 7. 판정

### 보강 완료 판정 3조건

| # | 조건 | 현재 |
|---|------|------|
| 1 | 보강 대기 마커 잔존 0건 | ✅ 0건 |
| 2 | §1 리스크 가설 표에 PLAN H-N 전건 전재 | ✅ H-1~H-9 9/9 |
| 3 | §4 매핑 표 모든 행에 가설 ID·검증 계층 기재 | ✅ 11/11 행 |

### 목표-커버 게이트 이력

| iteration | 결정론 | goal | adoption | boundary | 평균 | verdict |
|---|---|---|---|---|---|---|
| 1 | exit 0 | 1 | 1 | 2 | 1.33 | **fail** |
| 2 | exit 0 (scen 32) | 1 | **2** | 2 | **1.67** | ✅ **pass** |

### L3 [SUPERVISOR] 판정 이력

| iteration | 산출 | 충족 칸 | fail TS | verdict |
|---|---|---|---|---|
| 1 | `test-output/sample-{a,b,c}.md` | 19/22 | TS-024 · TS-029 | ❌ fail |
| 2 | `test-output/round2/` (fix 1/3 후) | 17/20 (엄격 기준) | TS-023 · TS-024 · TS-029 | ❌ fail |
| **3** | `test-output/round3/` (fix 2/3 + 명세 변경 후) | **20/20** | **없음** | ✅ **pass** |
| 잔여 보정 | `test-output/round4/` (D-1~D-4 후) | — | — | ✅ 기계 검증 |

### D-1~D-4 보정 효과 (round3 → round4 기계 실측)

| # | 지표 | round3 | round4 |
|---|---|---|---|
| D-1 | 요소 앞 빈 줄 (원칙 8) | **0/4** | **4/4** |
| D-2 | 결론 항목 수 ↔ 초점 볼드 수 | 2/2 · 2/2 · **3/1** | 2/2 · 2/2 |
| D-3 | 승인 마무리 문장 | **없음** | **있음** |
| D-4 | 1불릿 다문장 위반 (원칙 9) | **1건** | **0건** |

### 「템플릿 우위 법칙」 — 4회 연속 성립

| 회차 | 관측 |
|---|---|
| iter 1 | 가설 제기 — 원칙 5(산문) vs 템플릿 `질문:` 충돌 시 산출자 3/3이 **템플릿**을 택함 |
| iter 2 | **대조 실험 확증** — 같은 회차·같은 산출자에 보정 1(템플릿 반영) 3/3 vs 보정 2(산문만) 0/3 |
| iter 3 | 재현 — 템플릿 실물화 5항목 전건 0% → 100% |
| **D-1~D-4** | **역방향까지 확증** — 템플릿이 항목을 붙여 쓰자 원칙 8이 3/3 → 0/3 회귀, 템플릿에 빈 줄을 넣자 0/4 → 4/4 복구 |

> **법칙**: 규범은 산문보다 템플릿이 이긴다. 템플릿에 없는 규칙은 유도되지 않고, **템플릿이 틀리면 산문 규칙이 조용히 죽는다.**
> **한계**: 템플릿 배치는 **필요조건이지 충분조건이 아니다** — `round3/sample-c.md`에서 초점 볼드가 템플릿에 있었음에도 1/3만 이행됐다.

> **수렴 경로**: iteration 1이 제시한 「⑤ 2점 경로 = G-4 + G-5」가 실현됐다. ①은 1점에 머물렀고, 그 잔여 3건은 아래 「iteration 2 이월 반영」으로 처리했다.

### iteration 2 이월 반영 (SCENARIO-GATE-2 §4-1 · §4-2)

| # | 지시 | 반영 |
|---|------|------|
| 1 | C-3 산문 술어("판별된다") 교체 | ✅ **(i) 완료 항목 수/전체 수 또는 (ii) 잔여 항목 명시** 계수형으로 교체 |
| 2 | C-1 분별 기준을 판정표로 이관 | ✅ C-1 행에 동사형 정의 + **상황 서술체 금지 3예시** 병기 |
| 3 | 경로 (1) 산출 주체 self-confirming | ✅ **[MUST] 산출 주체 2단 분리** 신설 — 산출 시 판정표 미열람, 판정은 [SUPERVISOR]가 사후 |
| 4 | TS-029 입력 부재 | ✅ **축소 재정의** — before 원문 부재를 명시하고 「문제 4종 부재」 판정으로 전환, 표본 3건으로 확대해 대표성 보강. 한계(정량 대조 불가)를 문서에 명기 |
| 4-2a | 계층 분포 L1 24→25 | ✅ 정정 (총계 32 불변) |
| 4-2b | TS-031 플래그 ↔ §4 「목표」 행 불일치 | ✅ §4 「목표」 행에 TS-031 추가 |

### gaps 반영 대조 (iteration 1 → 2)

| gap | 축 | 반영 |
|-----|---|------|
| G-1 판정표 사전등록 | ① | ✅ 공통 판정표 C-1~C-8 신설, TS-024를 C-1+C-3 2항 체크로 교체 |
| G-2 실행 전제 이연 해소 | ① | ✅ 공통 실행 경로 (1) 재배포 불요 명시 + (2)는 CLOSE 후 잔여 검증으로 분리 |
| G-3 before 표본 대조 | ① | ✅ TS-029 신설 — 표본 3건 × 문제 4종 대조표 |
| G-4 `⭐` 산출물 계층 | ⑤ | ✅ TS-030 신설 + CLOSE 체크 1항 고정, TS-014 adoption 플래그 정정 |
| G-5 신형 채택 TS | ⑤ | ✅ TS-031 신설 — C-5+C-6+C-7 |
| G-6 TS-027 특정 | ⑥ | ✅ 입력·판정식·기대 3요소 명시 (TS-026 동형) |

### 선작성 트랙 실측 (`red-first.md` §1.6 (f) 환류)

| 지표 | 값 |
|------|-----|
| Block A 산출 | 20건 |
| PLAN TS로 재배정·조건 교체 | 14건 (70%) |
| Block A 고유 잔존 | **6건** (30%) — TS-023~028 |
| 게이트가 추가로 요구한 것 | 3건 (TS-029~031) — 전부 **①⑤축**(채택 관점) |
| PLAN이 새로 찾은 P0 | 2건 (H-4 상호모순 · H-6 SSOT 2중화) |
| 실제 폐기 | **0건** |

→ 실제 폐기 0건이므로 `red-first.md` §1.6 (f) 부적격 신호(절반 이상 수정·삭제)에 해당하지 않는다. **선작성 적격.**

> **관측 기록**: 게이트가 요구한 보강 3건이 전부 ①⑤축(채택 관점)이었다. 선작성이 채택 관점을 겨냥했음에도 여전히 부족했다는 뜻이며, 095·098에서 관측된 "채택 관점 도출 난이도"가 재현됐다.
