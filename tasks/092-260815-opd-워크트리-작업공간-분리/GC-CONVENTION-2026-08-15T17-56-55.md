# GC CONVENTION REPORT — 2026-08-15T17-56-55

<!--
  체크박스 5단계 상태 기호 (APPLY 단계가 기입):
  [ ]  open    — 미처리 (신규)
  [x]  done    — 적용 완료  ← 주석: 적용 시각 YYYY-MM-DD HH:mm + 수정 요약
  [~]  pending — 보류       ← 주석: 보류 사유
  [?]  review  — 확인 필요  ← 주석: 판단 근거 / 해결 방안
  [!]  failed  — 실패       ← 주석: 실패 사유 / 권장 대안
-->

## 1. 헤더

- 실행 일시: 2026-08-15 17:56:55 (단발 체크 — 소요시간 미측정)
- 범위: `all` (092 changed_files 전체 — 신규 7 + 수정 12)
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` 존재 — 이 문서를 유일 기준으로 체크 수행. `~/.opal/skills/opal-pilot-gc/references/base-convention-checklist.md`는 카테고리 목록으로만 참조.
- APPLY 수행 여부: N (수동 대기 — 위반 없음)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 0 |
| 심각도 분포 | Critical 0 / High 0 / Medium 0 / Low 0 / Info 0 |
| 자동 수정 가능 | 0 |
| 수동 조치 필요 | 0 |
| 파일별 상위 Top 5 | 해당 없음 (위반 0건) |
| 카테고리별 빈도 | 해당 없음 |
| Critical/High 수 | 0 |
| 문서 업데이트 제안 수 | 0 (빈도 0건 + 새 카테고리 0건) |

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

### High (0건)

### Medium (0건)

### Low (0건)

### Info (0건)

이번 체크 범위(092 신규 7 / 수정 12 파일)에서 `docs/CONVENTIONS.md` 위반은 발견되지 않았다. 확인한 6개 특히-확인 항목과 근거는 다음과 같다.

1. **@header 규칙** (CONVENTIONS.md §구현 규칙 > @header 규칙) — 신규 Python 파일 3종(`worktree_tool.py`, `tests/conftest.py`, `tests/test_worktree_tool.py`) 모두 docstring 최상단에 `@header` JSON 블록 포함. `module`(snake_case) / `layer`(util·test) / `domain`(opal-workspace) / `description` / `exports` 5필드 완비, `depends`도 선택 기재. `run.sh`는 header-standard.md §6 적용 대상 확장자(`.py/.js/.ts/...`)에 셸스크립트가 없어 "적용 대상 아님" 주석으로 처리 — `git-sync-tool/run.sh`와 동일 패턴.
2. **네이밍 규칙** (CONVENTIONS.md §네이밍 규칙 > 파일/폴더) — 도구 디렉토리 `opal/tools/worktree-tool/`(kebab-case, `{기능}-tool/` 패턴)은 기존 `git-sync-tool/`·`state-tool/`과 정합. 파일명 `worktree_tool.py`/`conftest.py`/`test_worktree_tool.py`는 Python 규칙(snake_case) 준수. 템플릿 `opal/templates/worktree-multi-repo.json`·`worktree-monorepo.json`도 kebab-case.
3. **문서 변경이력** (CONVENTIONS.md §변경이력) — 수정된 스킬·참조 문서 8종(`opal-harness.md`, `harness/task-process.md`, `pm/dispatch-process.md`, `tools.md`, `opal-pilot-dev/SKILL.md`, `opal-project-init/SKILL.md`, `docs/ARCHITECTURE.md`, `docs/PROJECT.md`) 전부 "## 변경이력" 표에 신규 행 추가 확인. 일시 형식 `YYYY-MM-DD HH:mm`(KST) + 태스크 번호 `(092)` 괄호 표기 일관. `docs/CONVENTIONS.md` 자체도 v1.4.0 행에 KST 일시 + `(092 DEC-1)` 표기.
4. **도구 규약** (CONVENTIONS.md §도구 우선 원칙) — `worktree-tool/run.sh`는 `git-sync-tool/run.sh`와 완전 동형(venv 존재 체크 → exec 위임 → 미존재 시 JSON 에러 stderr). `worktree_tool.py`의 `ok_response`/`err_response`/`ERROR_CODES` dict 패턴은 `git_sync_tool.py`의 동일 함수·구조와 대조 확인 — 응답 계약(`{"ok":bool,"error":...}`) 일치.
5. **State 관리 규칙** (CONVENTIONS.md §State 관리) — `state_tool.py` 변경분은 `init --worktree <path>` 조건부 필드 추가 1건(헤더 설명 1행 + `cmd_init` 5행 + argparse 2행, `git diff`로 정확히 3개 hunk만 확인)뿐이며, 미지정 시 `state.json`에 키 자체를 생성하지 않아(`getattr` 가드) 기존 하위호환을 깨지 않는다. `state.schema.json`도 `worktree` 필드를 optional로만 추가(`required` 배열 무변경). STATE.md 표 행 상태 변경은 여전히 `state-tool` 서브 명령 경유만 사용하며 마크다운 직접 편집 흔적 없음. 이 규칙과 충돌하지 않는다.
6. **배포 경계** (CONVENTIONS.md §배포 경계) — 변경 파일 전부 `opal/`·`docs/`·`scripts/install-mac.sh`·`.opal/code-scan.json`(프로젝트 소스) 범위 내이며 `~/.opal/` 배포본 직접 편집 흔적 없음. `install-mac.sh`에 `worktree-tool/run.sh` `chmod +x` 배포 블록이 기존 `code-scan`/`improve-tool` 패턴과 동일 형태로 추가된 것을 확인.

**추가 확인(요청 범위 외, 정보성)**: `.opal/code-scan.json` exclude 목록에 `.opal-worktrees` 1행 추가는 CONVENTIONS.md에 직접 규정된 항목은 아니지만 §배포 경계·§도구 우선 원칙과 충돌 없음. `state_tool.py`/`test_state_tool.py`의 기존 포맷 이슈는 092 범위 밖(Surgical Changes)으로 판정되어 지적하지 않았다(참고 지시 반영).

---

## 4. 문서 업데이트 제안 (§9·§10, 트리거 발동 시만)

트리거 미발동 — 이번 체크에서 빈도 임계값(N=3 파일) 도달 이슈 없음, 새 카테고리 등장 없음.

---

## 5. 문서 작성 유도 (해당 시)

`docs/CONVENTIONS.md` 존재 — 작성 유도 생략.
