# SCENARIO GATE 2 — 목표-커버 루브릭 판단축 채점

> 실행 일시: 2026-08-15T15:45:42+0900 | phase: `scenario-rubric` | iteration: 2
> 판정 주체: opal-evaluator-agent (판단축 ①⑤⑥ 전담)
> scenario_source: `/Volumes/Data/AiStudio/workspace/opal/tasks/092-260815-opd-워크트리-작업공간-분리/TEST-SCENARIO.md` (S-1~S-27)
> 판정 입력(참조): `SCENARIO-GATE-1.md`(gaps G-1~G-7) · `PLAN.md`(TS-030·TS-031·TS-032·TS-050, §3.2.4, §3.4.2, §3.8.2~3, DEC-2·DEC-3) · `TASK.md`(F-1~F-9 AC, 완료기준 ①~⑦) · `.scenario-coverage-input.json`
> 규칙 SSOT: `opal/core/references/harness/scenario-gate.md` §2(6축·판정주체 분리) · §5-1(종료조건 임계)
> CONTRACT.md: 부재 (opd 1차 접합) — `scenario-rubric`은 Phase 2 CONTRACT 병합 대상이 아니므로 전용 3축 루브릭만 적용

> **판정 경계 [MUST]**: 결정론축 ②③④는 test-tool `scenario-coverage-check`가 exit 0(requirements 9 / features 9 / hypotheses 20 / scenarios 27, `all_covered: true`)으로 판정 완료했다. 본 보고서는 이를 대신 판정하지 않는다(scenario-gate.md §2). 아래 지적은 전부 **"있어야 할 시나리오가 애초에 존재하지 않음"**에 관한 것이다.

---

## 1. 판단축별 판정

| 축 | iteration 1 | **iteration 2** | 통과선 | 판정 |
|----|------------|----------------|--------|------|
| ① 목표 달성 | 1 | **1** / 2 | ≥1 | 충족(0점 아님) — 잔여 공백 1건 |
| ⑤ 채택/잔존 | 1 | **2** / 2 | ≥1 | 충족 |
| ⑥ 경계/부정 | 1 | **2** / 2 | ≥1 | 충족 |
| **평균** | 1.00 | **1.67** | ≥1.5 | **충족** |

**종합 verdict: `pass`** — 세 축 모두 ≥1점(0점 축 없음) AND 평균 1.67 ≥ 1.5 (`scenario-gate.md` §5-1).

---

## 2. gap 반영 검증 (주장 대조)

> Producer의 주장을 믿지 않고 산출물 본문과 설계 원천(PLAN/TASK)에서 직접 확인했다.

| gap | 반영 여부 | 실측 근거 | 품질 판정 |
|-----|----------|----------|----------|
| G-1 | **부분** | S-24 신설(§3 L2, H-17). 기대 결과 ⓐ`create` `ok:true` + `worktree_root` 경로 일치 ⓑ`state-tool show --format json`의 `data.worktree` 문자열 동일 ⓒ`create` 실패 시 `--worktree` 미전달 + `state init` 성공(DEC-2 비차단). PLAN Step 8 `[T092/L2-F4b]`에 "state-tool을 subprocess로 호출" 명시 — 실제 관통 실행이 맞다 | **실질 반영이나 절반**. §4 잔여공백 R-1 참조 |
| G-2 | **완전** | S-18 기대 결과가 ⓐⓑⓒ 3항으로 확장. ⓑ가 "슬롯 092 편집 시 슬롯 093·**메인 `workspace/`**의 `git status --porcelain` 비어 있고 브랜치 불변", ⓒ가 양쪽 `remove` 원복. 조건란도 "이어서 `create --task 093`으로 슬롯 2개 확보"로 갱신됨 | 목표 주장("여러 태스크 동시 수행")이 **실환경 계층**에서 처음 관측된다. 지적 공백을 정확히 닫음 |
| G-3 | **완전** | S-1 조건이 "①미지정 ②`--worktree <절대경로>` 지정" 양방향으로 확장. 기대 결과에 "`state["worktree"]`가 전달 절대경로와 **문자열 동일**(`null`·빈 문자열·상대경로 불가) + `show --format json`의 `data.worktree`가 같은 값" 명시 | 값이 `null`이어도 통과하던 구멍이 닫혔다. §4.1 F-5 AC 행도 "값 정합"으로 갱신 |
| G-4 | **완전** | S-25 신설(H-18). 4상태(dirty/unpushed/미머지/clean) 전부 `ok:true`이고 판정이 `remove`와 일치 + `opal-pilot-dev/SKILL.md` STEP 6 안내가 `status` 출력을 근거로 함을 grep. PLAN §3.2.4("`remove`와 동일 판정 함수 재사용하되 거부하지 않고 보고만")·§3.8.3(삽입 문안)과 정합 | `status`가 27개 시나리오 중 처음 호출된다. 공급자·소비자 양쪽 커버 |
| G-5 | **완전** | S-26 신설(H-19). ①`CONFIG_NOT_FOUND` ②`CONFIG_INVALID_JSON` + **양쪽 모두 부수효과 0**(`.gitignore` sha256 동일 · `.opal-worktrees/` 미생성 · 브랜치 미생성). PLAN §3.2.3 pre-flight 선행 순서와 정합 | 지적한 "부수효과 0"까지 그대로 반영. F-3 AC (3)항이 실증된다 |
| G-6 | **완전** | S-27 신설(H-20). `WORKTREE_EXISTS`/`BRANCH_EXISTS` 거부 + 기존 worktree·브랜치·`.meta/task_092.json` **무손상**. H-20 서술이 "H-7(롤백 후 재실행 허용)의 반대 방향"임을 명시 | 방향 대칭이 확보됨 |
| G-7 | **완전** | S-14 조건이 3종 → **6종**. ④`repos: []`→`CONFIG_INVALID_TYPE` ⑤`.git` 없는 디렉토리→pre-flight `NOT_A_GIT_REPO` + **worktree 0개·브랜치 0개**(PLAN TS-013, S-12/TS-014와 별개 계약임을 본문에 명시) ⑥프로젝트 밖 심볼릭 링크→`_is_inside()` 미해석 결정대로 **통과**(구현자가 `resolve()`로 바꾸면 깨지도록 고정) | 세 케이스 모두 설계 원천에 정확히 대응. ⑥의 "결정 고정" 의도까지 본문에 기술됨 |

**신규 가설 H-17~H-20 적정성 검토(자기 편의적 축소 여부)**: 4건 모두 §1 표에서 P0/P1 등급·검증 계층·시나리오가 채워졌고, 서술이 계약 수준(예: H-17 "훅이 create를 호출하지 않게 접합돼도 S-1~S-23이 전건 PASS하는 공백")으로 쓰여 자기 편의적 축소는 확인되지 않았다. **다만 H-17의 서술 범위와 S-24의 실제 검증 범위가 어긋난다** — H-17은 "`--wt` → 스텝 4.5 훅 → `create` → `state init` 관통 계약"이라고 선언하지만, S-24는 그 사슬의 **뒤 두 마디(도구↔도구)만** 실행한다. 앞 두 마디(`--wt` 인지, 훅 문안 존재)는 여전히 미검증이다(§4 R-1).

---

## 3. 축별 판정 근거

### ① 목표 달성 — 1점 (변동 없음)

**가점**: 목표 검증 시나리오가 **세 계층**으로 늘었다 — S-23(합성 fixture 2슬롯 간섭 부재) · S-18ⓑ(실환경 revup 2슬롯 + 메인 작업본 불변, 운영 계층) · S-24(도구 조합 계약 실행). iteration 1에서 지적한 "운영 계층 목표 검증이 단일 슬롯 확인에 의존" 상태는 해소됐다.

**여전히 2점이 아닌 이유 — 목표 문장의 첫 절("OPAL 태스크 파이프라인에 축을 신설하여")을 검증하는 시나리오가 아직 0건이다.**

축을 신설하는 실체는 문서 3종이다.

| 산출물 | 계약 원천 | 대응 시나리오 기대 결과 |
|---|---|---|
| `opal-harness.md` §2.5 (축 정의 SSOT) | TASK F-3 AC 3항목(①직교 축 선언 ②미사용 시 현행 유지 ③config 부재 시 동작) | **없음.** S-17은 `§N` dangling 여부만 본다(문안 무관), S-3은 pilot SKILL.md diff, S-26은 ③을 **동작으로** 실증할 뿐 문안 검증 아님 |
| `task-process.md` 스텝 4.5 (훅) | TASK F-4 AC · PLAN **TS-030**(4.5 존재 + create 호출·성공/실패 분기·롤백 금지 명시) · **TS-031**(스텝 3·4·5·6 문구 무변경) · **TS-032**(DEC-2 명문화) | **없음.** §4.1 F-4 행은 `S-3, S-24`를 적었으나 S-3의 기대 결과는 `git diff --stat opal/skills/opal-pilot-*/SKILL.md` 단독 — `task-process.md`를 **한 번도 열지 않는다** |
| `dispatch-process.md` `## 작업 경로` 블록 | TASK F-6 AC · PLAN **TS-050**("상대경로 금지·절대경로 주입" 문구 + 2필드 계약) | **없음.** §4.1 F-6 행도 `S-3, S-24` — 동일하게 해당 파일을 보지 않는다 |

즉 **`task-process.md`에 스텝 4.5를 아예 넣지 않아도 S-1~S-27 전건이 PASS한다.** S-24는 "스텝 4.5가 규정한 호출 순서를 **그대로 재현하는** 통합 테스트"(조건란 원문)이므로, 올바른 순서를 **테스트가 스스로 하드코딩**한다 — 문서가 그 순서를 지시하는지는 묻지 않는다. iteration 1이 지목한 결함 형태("도구는 만들어졌으나 파이프라인이 그것을 쓰는지 아무도 확인하지 않음")가 한 칸 좁아졌을 뿐 남아 있다.

이것이 무리한 요구가 아닌 근거: Producer는 **S-25에서 이미 동일 유형의 검증을 수행한다**(`opal-pilot-dev/SKILL.md` STEP 6 안내 문안 grep). 같은 기법을 축 정의 3문서에 적용하면 되고, 비용은 grep 1회다. 게이트가 "완벽"을 요구하는 것이 아니라, **목표 문장의 주어에 해당하는 산출물에 검증이 0건**이라는 사실이 남아 있어 2점을 주지 못한다.

→ 목표 시나리오 3계층 존재(0점 아님) + 목표 문장 첫 절 미검증(2점 아님) = **1점**

### ⑤ 채택/잔존 — 2점 (1 → 2)

- **잔존**: iteration 1 판정 유지 — 가산형 변경에서 "구형 경로 무손상" 불변식을 S-1(키 미생성)·S-2(STATE.md 바이트 동일)·S-3(pilot 9종 diff 0)이 정확히 겨눈다. §4.3 각주의 대체 해석은 여전히 타당하다.
- **채택**: iteration 1의 3대 공백이 모두 닫혔다. ①`worktree` **값 정합**을 S-1·S-24ⓑ가 이중으로 확인(전달 절대경로 ↔ `state.json` ↔ `show --format json` 3자 문자열 동일) ②`status`가 S-25로 4상태 호출되며 `remove`와 판정 일치·거부 없음을 확인 ③CLOSE 안내 게이트가 S-25의 grep으로 소비자 측까지 연결.
- 추가로 S-26이 "설정이 없는 사용자가 이유를 아는가"라는 채택 관점을 새로 커버한다(§4.3 표에 명시).

→ 잔존 견고 + 채택 3대 공백 해소 = **2점**

### ⑥ 경계/부정 — 2점 (1 → 2)

iteration 1이 표로 지목한 5종 미커버 경로가 전부 시나리오화됐다.

| iteration 1 지적 | iteration 2 대응 | 확인 |
|---|---|---|
| `CONFIG_NOT_FOUND` / `CONFIG_INVALID_JSON` | S-26 (+ 부수효과 0) | 반영 |
| `WORKTREE_EXISTS` / `BRANCH_EXISTS` 중복 거부 | S-27 (+ 기존 슬롯 무손상) | 반영 |
| `NOT_A_GIT_REPO` pre-flight 거부 | S-14 ⑤ (worktree 0개·브랜치 0개) | 반영 |
| `repos: []` | S-14 ④ (`CONFIG_INVALID_TYPE`) | 반영 |
| 심볼릭 링크 `_is_inside()` 미해석 | S-14 ⑥ (통과가 정답임을 고정) | 반영 |

기존 두터운 부정 경로(S-6~S-8 고유 코드 3종 · S-10 force 기록 · S-12 롤백 · S-13 base-ref 동결 · S-16 비차단 경고 · S-22 경고 비차단)와 합쳐 부정 경로 커버는 충분하다.

→ **2점** (잔여 항목은 §4 R-2, 감점 사유 아님)

---

## 4. 잔여 공백 — 다음 반복 없이 EXECUTE 전 반영 권고

> verdict가 `pass`이므로 `gaps[]`(0점 축 전용)에는 싣지 않는다. 아래는 **차단하지 않는 권고**이며, R-1은 게이트 통과 후에도 남는 실질 공백이므로 RED 작성 전 반영을 강력히 권한다.

**R-1 [①축 잔여 · 우선] 축 신설 3문서 문안 검증 시나리오 부재**
`opal-harness.md` §2.5 · `task-process.md` 스텝 4.5 · `dispatch-process.md` `## 작업 경로`에 대한 **문안 존재 검증 기대 결과가 0건**이다(PLAN TS-030·TS-031·TS-032·TS-050 전부 무대응). §4.1의 F-4·F-6 행이 `S-3`을 근거로 적었으나 S-3의 기대 결과는 pilot SKILL.md diff 단독이므로 **매핑이 의미상 거짓**이다(결정론 커버리지 도구는 이 유형을 잡지 못한다 — `scenario-gate.md` §1).
→ 조치(택1, 비용 grep 1회): **(a)** S-24 기대 결과에 ⓓ를 추가 — "`task-process.md`에 스텝 4.5가 존재하고 `worktree-tool create` 호출·`ok:true`→`--worktree` 전달·`ok:false`→롤백 금지+비차단 계속이 문안으로 명시됨(TS-030·TS-032), 스텝 3·4·5·6 라인은 `git diff`에서 무변경(TS-031)". **(b)** 또는 L1 M1 시나리오 S-28을 신설해 3문서를 한 번에 grep(§2.5 3항목 · 4.5 문안 · dispatch 절대경로 문구+2필드)하고 §4.1 F-3·F-4·F-6 행의 시나리오 열을 정정.

**R-2 [⑥축 잔여 · 낮음] 슬롯 부재 상태의 `remove`/`status`**
PLAN §3.2.2가 정의한 `META_NOT_FOUND`·`WORKTREE_NOT_FOUND`(remove/status 공통)와 §3.8.3의 "`status` 호출 실패·메타 부재·worktree 부재는 전부 **no-op**, CLOSE를 중단시키지 않는다"는 어느 시나리오에도 없다. `remove`를 두 번 실행하는 것은 흔한 실사용 경로다. → S-25 조건에 5번째 상태(슬롯 없음)를 추가하고 기대 결과에 "`status`가 `META_NOT_FOUND`를 반환해도 CLOSE 안내는 no-op으로 계속"을 넣는 것으로 충분하다.

**R-3 [문서 정합 · 게이트 축 외, 그러나 PM Gate 자기점검이 거짓]**
시나리오가 23→27로 늘었는데 **부속 표 4곳이 iteration 1 상태로 남아 있다.**
- §2.2 Given/When/Then 표: **S-24~S-27 4행 부재**(마지막 행이 S-23). 또한 S-14 행이 여전히 "부적합 config **3종** / 각각 `list --project-root` 호출"이어서 §3의 6종·`create` pre-flight 조건과 모순된다.
- §2.1 사전 조건 데이터: `worktree.json (부적합 **3종**)` 그대로. config 부재 프로젝트·깨진 JSON·`.git` 없는 디렉토리·프로젝트 밖 심볼릭 링크 fixture 미기재.
- §4.2 완료기준 ⑦ 행: 가설 `H-1~H-16`, 시나리오 `S-1~S-17, S-21~S-23` — 신규 4건이 회귀 전량에서 빠져 있다.
- §7 PM Gate 체크: "Given/When/Then 3필드 채워짐 **(23행)**", "H-1~**H-16** + H-목표 전건 연결"에 `[x]`가 찍혀 있다 — **실제와 불일치하는 자기 인증**이다.
→ 본 게이트의 판정축(①⑤⑥) 대상이 아니어서 감점하지 않았으나, RED 작성 워커가 §2.1/§2.2를 fixture 원천으로 읽으므로 **EXECUTE 진입 전 반드시 동기화**해야 한다.

**R-4 [참고 · 조치 불요]** S-25의 grep이 "안내가 `status` 출력을 근거로 삼는가"만 보고, PLAN §3.8.3이 `[MUST]`로 못박은 "**자동 제거하지 않는다**"(user sovereignty) 문안은 확인하지 않는다. R-1 (a)/(b) 반영 시 함께 넣으면 비용이 0이다.

---

## 5. 별도 검토 사항 (감점 사유 아님)

| 항목 | 판정 | 근거 |
|---|---|---|
| §0.2 M2 면제 | **정당**(iteration 1 판정 유지) | 트리거 3종 전부 미해당 — FE 화면·인증/인가·외부 API 없음 |
| S-18·S-19·S-20 M3 배정 | **정당** | 캡틴 작업 환경 직접 조작 + S-20은 비가역. G-2 반영으로 M3 범위가 커졌으나(2슬롯 왕복) 여전히 수동이 옳다 |
| S-24ⓒ와 S-26의 조건 중복(config 부재) | **문제 없음** | S-24ⓒ는 파이프라인 비차단(DEC-2 파이프라인 계층), S-26은 도구 부수효과 0(도구 계층) — 검증 대상 계약이 다르다 |
| §0.1 RED-first 트랙 분기 | 판정 범위 밖 | 기록만 함 |

---

## 6. 결과 계약

```json
{
  "scores": {"goal": 1, "adoption": 2, "boundary": 2},
  "average": 1.67,
  "gaps": [],
  "verdict": "pass"
}
```

**종합 verdict: `pass`** — 세 판단축 각 ≥1점(0점 축 없음) AND 평균 1.67 ≥ 1.5 (`scenario-gate.md` §5-1 수렴 조건). 결정론축 ②③④는 `scenario-coverage-check` exit 0으로 별도 충족되었으므로 §6 tool-gated 집행의 두 증거가 모두 성립한다.
단, ①축이 1점에 머문 실질 사유(§4 R-1: 목표 문장의 주어인 파이프라인 3문서에 문안 검증 0건)와 문서 정합 결함(§4 R-3)은 게이트 통과와 무관하게 남아 있으므로 EXECUTE/RED 진입 전 반영을 권고한다.

---

## 변경이력

| 버전 | 일시 | 내용 |
|------|------|------|
| v1.0 | 2026-08-15T15:45:42+0900 | iteration 2 채점 — goal 1 / adoption 2 / boundary 2, 평균 1.67, verdict pass. G-1~G-7 중 6건 완전 반영·G-1 부분 반영 확인. 잔여 권고 R-1~R-4 기재 |
