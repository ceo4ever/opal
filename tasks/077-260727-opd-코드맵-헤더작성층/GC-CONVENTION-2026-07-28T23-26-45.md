# GC CONVENTION REPORT — 2026-07-28T23-26-45

## 1. 헤더

- 실행 일시: 완료 2026-07-28 23:26:45 (KST)
- 범위: `all` (태스크 077 변경분) / 대상 파일 27개 (신규 11 + 수정 16, 참조문서 7종·docs 3종 포함)
- 에이전트: opal-convention-checker
- 기준 문서: `docs/CONVENTIONS.md` 존재 — 허브 단일 문서 모델 (링크 미사용, 프레임워크 자체 프로젝트)
- APPLY 수행 여부: N (진단 전담 — 소스/문서 수정 없음)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 3 |
| 심각도 분포 | Critical 0 / High 0 / Medium 0 / Low 3 / Info 0 |
| 자동 수정 가능 | 0 |
| 수동 조치 필요 | 3 |
| 파일별 상위 Top 5 | `opal/core/references/tools.md` (1건) / `docs/ARCHITECTURE.md` (1건) / 5개 참조문서 공동 1건(빈도 이슈) |
| 카테고리별 빈도 | 문서화 (7개 파일 관련) |
| Critical/High 수 | **0건 — PM Gate 통과 기준 충족** |
| 문서 업데이트 제안 수 | 1 (빈도 1건) |

**결론: Critical/High 0건. 발견된 3건은 모두 Low(문서 변경이력 형식/일관성) — PM Gate 차단 사유 없음.**

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

### High (0건)

### Medium (0건)

### Low (3건)

- [ ] GC-C001 [`docs/ARCHITECTURE.md`:82,94,247] 077 관련 본문 수정(도구 표 code-scan 헤더 작성층 설명 추가 등)이 있었으나 파일 자체의 "## 변경이력" 표(§398 이하)에 태스크 077 행이 추가되지 않음
  - 카테고리: 문서화
  - 위반 기준: **기준 부재** — `docs/CONVENTIONS.md` §변경이력 작성 의무(199행)는 "스킬·에이전트·참조 문서"만 명시하고 `docs/*.md` 허브 문서는 범위에 명문화되어 있지 않음. 다만 `ARCHITECTURE.md` 자신은 그간(Task 055~065) 실질 변경 시마다 1행씩 추가해온 자체 관행을 유지 중이며, 이번 변경만 예외적으로 행이 누락됨(동시 수정된 `docs/CONVENTIONS.md`·`docs/PROJECT.md`는 077 행이 정상 추가됨)
  - 설명: `tools/` 항목에 `code-scan/` 헤더 작성층 설명이 추가됐고 `.opal/MEMORY.md → MEMORY.json` 관련 서술도 갱신됐으나, 변경이력 표에는 대응 행이 없음
  - 해결 방안: `## 변경이력` 표 말미에 `| 2026-07-28 | ... (Task 077) |` 행 추가(형식은 기존 PROJECT.md/직전 행과 동일한 날짜-only 패턴을 따르는 이 파일 고유 관행 유지)
  - 자동 수정: N
  - 참조: TBD — 프로젝트 내부 관행(공식 외부 린트 규칙 없음)

- [ ] GC-C002 [`opal/core/references/tools.md`:880-881] 변경이력 표에 버전 `v2.6`이 두 행에 중복 사용됨(880행: memory-tool 섹션, Task 078 / 881행: code-scan validate 섹션, Task 077) — 077 항목이 직전 077 행(879행 `v2.5`)의 다음 버전으로 명확히 구분되지 않음
  - 카테고리: 문서화
  - 위반 기준: **기준 부재에 가까움** — `docs/CONVENTIONS.md` §변경이력은 "버전: semver"만 규정하고 유일성을 명문화하지 않음. 다만 `opal/core/references/harness/pm-review-gate.md`에도 `v1.4` 중복 전례(152~153행, Task 014)가 있어 "동일 일자 다중 변경 시 버전 유지" 패턴이 프로젝트 관행으로 이미 존재 — 명백한 신규 위반이라기보다 기존에도 느슨했던 지점
  - 설명: 077/078 두 태스크가 동일 파일을 병행 수정하며 버전 번호가 충돌한 것으로 추정됨(작업 순서상 077이 078보다 먼저 실행됐다면 077 쪽이 `v2.6`, 078 쪽이 `v2.7`이 되는 것이 자연스러움)
  - 해결 방안: 두 태스크 중 나중에 반영되는 쪽의 버전을 다음 순번으로 재부여(예: 077→v2.6 유지, 078→v2.7로 조정) — 단, 이는 077 소유가 아닌 078과의 교차 조정이 필요하므로 오케스트레이터 확인 필요
  - 자동 수정: N
  - 참조: TBD — 프로젝트 내부 관행(공식 외부 린트 규칙 없음)

- [ ] GC-C003 [빈도] `opal/core/references/harness/header-rules.md`:152 / `opal/core/references/harness/pm-review-gate.md`:156 / `opal/core/references/tools.md`:879,880,881 / `opal/core/references/opal-harness.md`:325 / `opal/tools/brain-tool/README.md`:142 — 077 태스크 중 추가된 변경이력 행 5개 파일에서 일시가 `YYYY-MM-DD HH:mm` 대신 날짜만 기재됨(예: `2026-07-28`), 동일 파일·동일 태스크의 바로 앞 행(예: header-rules.md v1.3 `2026-07-28 14:20`, pm-review-gate.md v1.6 `2026-07-28 15:10`, code-scan-management.md v1.2 `2026-07-28 15:10`)은 시각을 포함하여 내부 일관성이 어긋남
  - 카테고리: 문서화
  - 위반 기준: 프로젝트(`docs/CONVENTIONS.md` §변경이력, 104행 "일시 형식: `YYYY-MM-DD HH:mm` (KST 기준)")
  - 설명: 규칙상 시각(HH:mm) 포함이 명시돼 있고, 077 작업 내에서도 일부 행(재작업 이전분)은 시각을 기재했는데 재작업 시 추가된 후속 행에서 시각이 누락되어 같은 태스크 내에서도 형식이 혼재함. 단, 이 저장소에는 077 이전에도 시각 누락 행(예: opal-harness.md v6.6 `2026-07-23`, pm-review-gate.md 다수)이 다수 존재하는 만성적 패턴이라 077에 국한된 신규 결함은 아님
  - 해결 방안: 해당 5개 행에 KST 시각(HH:mm) 보강. 반복성이 높으므로(5개 파일) §4 문서 업데이트 제안 참조
  - 자동 수정: Y(단순 텍스트 삽입 — 단, 실제 작업 완료 시각 확인 필요하여 사람 확인 권장)
  - 참조: TBD — 프로젝트 내부 관행(공식 외부 린트 규칙 없음)

### Info (0건)

---

## 4. 문서 업데이트 제안 (트리거 발동 시만)

<!-- 빈도 트리거 발동 -->
- [ ] GC-DP-C001 [빈도 트리거] 이슈 "변경이력 일시 KST 시각(HH:mm) 누락" (5개 파일: header-rules.md/pm-review-gate.md/tools.md/opal-harness.md/brain-tool/README.md) → `docs/CONVENTIONS.md` §변경이력에 이미 규정은 존재하므로 신설이 아니라 **집행 강화** 제안
  - 근거: 단일 태스크(077) 내 5개 파일 — 빈도 임계값 N=3 초과
  - 제안 내용: "변경이력 행 작성 시 일시는 `YYYY-MM-DD HH:mm` 형식을 반드시 포함한다(날짜만 기재 금지)"는 문구를 굵게(MUST) 표기하거나, PM Gate 표준 검토 항목에 형식 체크를 추가하는 방안 검토

---

## 5. 문서 작성 유도 (해당 시)

존재 — 작성 유도 생략 (`docs/CONVENTIONS.md` 정상 로드됨)

---

## 부록 — 확인 완료 항목 (위반 없음)

- **네이밍**: 신규 Node 파일(`code-map-hook.js`, `run.sh`) kebab-case 준수. 테스트 파일 8종(`test-discover.js` 등) kebab-case + `test-` 접두 준수(CONVENTIONS.md에 "test-<verb>" 세부 패턴 명문 규정은 없음 — 기준 부재이나 일반 kebab-case는 충족)
  - 테스트 검증 상세는 `opal-security-checker`/기타 병렬 체크와 중복 방지 위해 본 보고서 범위 내에서만 확인
- **@header 규칙**: `code-scan.js`(v1.3.2)·`code-map-hook.js`·테스트 8개 파일 전부 필수 5필드(module/layer/domain/description/exports) + 테스트 전용 선택 필드(task/scenarios) 충족. `run.sh`는 셸 스크립트로 적용 대상 외(header-rules.md §적용 대상 확장자) — 파일 내 명시적 예외 주석까지 남겨 근거 인용 우수
- **변경이력 작성 의무**: `header-standard.md`(v1.3)·`header-rules.md`(v1.3/v1.4)·`pm-review-gate.md`(v1.6/v1.7)·`code-scan-management.md`(v1.2)·`tools.md`(v2.5/v2.6)·`opal-harness.md`(v6.7)·`brain-tool/README.md`(v1.3)·`docs/CONVENTIONS.md`·`docs/PROJECT.md` 전부 077 참조 행 존재(위 GC-C001 `ARCHITECTURE.md` 1건 제외)
- **배포 경계**: `~/.opal/tools/code-scan/*`가 소스(`opal/tools/code-scan/*`)와 바이트 동일(diff 없음) — 직접 편집 흔적 없음, `install-mac.sh` 재배포를 통한 정상 반영으로 확인됨. `.opal/code-scan.json`은 `.gitignore`(`.opal/*` 제외 규칙)상 로컬 런타임 생성 설정으로 버전관리 대상 아님(설계상 정상, `code-scan-management.md` §생성 시점과 일치)
- **언어 규칙**: 코드 식별자 전부 영문, 주석/설명은 한국어 — 혼용 규칙 준수. 코드 내 Korean 식별자 없음
- **죽은 코드/미사용 import**: `code-scan.js`·`code-map-hook.js` 정적 검토 결과 미사용 import·주석 처리된 코드 블록 없음(`node -c` 문법 검증 통과, `console.log`는 전부 의도된 CLI 출력이며 디버그 잔재 아님). 테스트 파일에 `.skip()`/`TODO`/`FIXME` 잔존 없음
- **테스트 픽스처**(`tests/fixtures/**`): `.opal/code-scan.json`의 `exclude` 목록에 `fixtures`가 이미 포함되어 code-scan 자체 스캔 대상에서 제외됨 — 헤더 미기재 픽스처(스키마 위반·orphan 등 의도적 결함 데이터)는 설계상 정상, 위반 아님(기준 부재 아닌 "적용 대상 아님"으로 판정)

---

## 반환 요약

- 보고서 경로: `tasks/077-260727-opd-코드맵-헤더작성층/GC-CONVENTION-2026-07-28T23-26-45.md`
- 총 3건 (Low 3 / Medium 0 / High 0 / Critical 0)
- **Critical/High 0건 — PM Gate 통과 기준 충족**
