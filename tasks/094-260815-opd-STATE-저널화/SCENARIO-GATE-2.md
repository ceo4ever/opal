# SCENARIO-GATE-2 — TEST-SCENARIO 목표-커버 루브릭 재채점 (판단축 ①⑤⑥)

> 실행 일시: 2026-08-16T01:00:00+09:00 | phase: `scenario-rubric` | iteration: 2
> 판정자: opal-evaluator-agent (verdict-only · readonly — 어떤 산출물도 수정하지 않음)
> scenario_source: `tasks/094-260815-opd-STATE-저널화/TEST-SCENARIO.md`
> 규칙 SSOT: `opal/core/references/harness/scenario-gate.md` §2 루브릭 6축 · §5-1 종료조건
> 채점 범위: **①목표달성 · ⑤채택/잔존 · ⑥경계/부정 3축만** (②③④ 결정론 축은 test-tool 소관)
> 부가 입력(검증용): `SCENARIO-GATE-1.md`, `TASK.md`, `PLAN.md`, `.scenario-coverage-input.json`
> 이전 회차: iteration 1 = fail (2/1/1, 평균 1.33)

---

## 0. 반영 검증 — 주장 대조 (본문 직접 확인)

> Producer 주장을 신뢰하지 않고 `TEST-SCENARIO.md` 본문·정규화 페이로드에서 각 항목을 실측했다.

| iter-1 지적 | 판정 | 실측 근거 |
|---|---|---|
| §2.1 [BLOCKING·⑥] 레거시 **쓰기** 경로 미검증 | **해소** | S-29 신설(L2, `TEST-SCENARIO.md:385-398`). 조건 = 093 **사본**에 `advance`→`mark --force --note`→`block` 연속. 기대 결과 5항 — 전건 `ok:true`·무예외 / 로그 1행 추가 / **마커·파이프라인 표·`## 현재 상태` 블록 바이트 동결(구간 diff 0)** / 중복 삽입 0건 / 원본 무변경. §1 H-4 행에 `S-29(쓰기 경로 — 핵심)` 매핑, §4 R-3 AC(a) 행에 S-29 추가, §2.2 흐름 표 등재. **동결 범위를 파일 전체가 아니라 3블록으로 한정**한 점이 정확하다 — S-21(`> 최종 갱신:` 헤더 갱신)·PLAN:413(헤더 갱신 + 로그 append는 허용)과 모순되지 않는다. |
| §2.2 [BLOCKING·⑥] `tmp_task_broken` 고아 fixture | **해소** | S-30 신설(L2, `:402-415`). `mark --force --note` **2회 연속**, 기대 결과 4항 — `ok:true` / 골격 append 복구 **+ 로그 1행 기재**(조용한 no-op 부정) / 기존 본문 무손실 / 2회차 골격 중복 append 0(멱등)·로그 2행. §2.1 자산 표의 고아가 §2.2·§3·§4·페이로드에서 전부 소비된다. `ensure_journal_skeleton` 세 분기 중 미검증이던 두 번째(정규식 미매칭 → append)를 정확히 겨눈다. |
| §2.3 [BLOCKING·⑤] R-6 AC(b) 신형 채택 미검증 | **해소** | S-31 신설(L3, `:483-496`). **단순 언급이 아니라 검증 가능한 기대 결과 3항**을 갖는다 — ① 3문서 **각각**에 `## 의사결정 로그`·`## 블로커` 기술 ② 3문서 **각각**에 `state-tool show` 조회 경로 명시 ③ `state-template.md` 템플릿 본문 ↔ `init` 실산출 골격 **일치(문서↔코드 drift 0)**. 도구 = grep + Python 대조. §4에 `R-6 AC(b) 신형 채택 \| H-9 \| L3 \| S-31 \| grep + 템플릿↔코드 대조 \| 교체형 (b)` 행 신설, §1 H-9에 `S-31(신형 채택)` 매핑. 페이로드 S-31 = `R-6/F-004/H-9, is_adoption_scenario: true`. `state-template.md`는 iter-1에서 전문 0회 등장이었고 지금은 §2.2·§3·신설 근거에 실재한다. |
| §2.4 [비차단] 잔존-0의 adoption 오라벨링 | **해소** | 페이로드 실측 — S-1·S-8·S-12·S-16·S-17 전부 `is_adoption_scenario` 플래그 없음. 현재 adoption 라벨 13건(S-2·7·9·10·11·13·14·15·18·20·24·28·31)을 전수 확인했고 **잔존-0 grep 시나리오는 한 건도 포함되지 않는다**. 과대 표시가 제거됐다. |
| §2.5 [비차단] S-28 Given 충실도 | **해소** | `:583` 조건이 "**실제 의사결정 2건 이상·블로커 1건 이상이 기재된 저널** — 094 자신을 신형 도구로 이어 완주한 저널 우선"으로 격상되고 **[MUST] S-18 임시 폴더 산출물로 대체 금지**가 명시됐다. 기대 결과에 **[MUST] 반증 조건 3종**((i) `show` 대신 STATE.md를 먼저 여는 습관 잔존 (ii) `state.json` 직독과 차이 없음 (iii) 도구 생성 형식 행뿐)이 FAIL 트리거로 배선됐다. |
| §2.5 부수 (완주 간극) | **해소(고지 형태)** | S-18 기대 결과에 `[간극 고지]` 추가 — 5명령 ≠ 완주임을 명기하고 완주 검증 책임을 S-28 Given으로, 잔여 간극을 DONE.md로 배정. 간극을 숨기지 않고 배선했다. |
| §2.6 [비차단] S-19 총량 결정론 가드 소실 | **해소** | `:569` 기대 결과에 ⑤ **[결정론 대조] `git diff` 기준 삭제 테스트 함수 총수 == 감사표 열거 건수(초과 삭제 0건)** + ⑥ 최종 `passed` 참고값 기록이 추가됐다. 하한을 되살리지 않아 padding 유도는 없고, 자기신고 의존만 차단된다 — 권고와 정확히 일치. |
| §2.7 [비차단] PM Gate 체크 사실 불일치 | **해소** | §2.2 데이터 흐름 표를 실측한 결과 **31행 전량**(S-1~S-31) 수록. 체크 문안에 정정 이력이 명시됐다. |

**중복·모순 검사 (신규 3건 도입 부작용)**: 없음.
- S-29 ↔ S-11·S-24 — 전자는 레거시 사본 **쓰기**, 후자는 읽기 전용 `show`. 자산은 공유하되 검증 대상이 배타적이다.
- S-30 ↔ S-6(iii) — S-6은 `ok:true`만, S-30은 **로그 기재·본문 무손실·멱등**을 assert. 상보 관계.
- S-31 ↔ S-12·S-16 — (b)측 대 (a)측. S-13과는 `harness/state.md`를 공유하나 주장이 다르다(세션 복원 절차 대 저널 구조·`show` 경로). 경미한 중복이며 모순 아님.
- 시나리오 총수: §2.2 31행 = §3 본문 31건(L1 10 / L2 11 / L3 10) = 페이로드 31건으로 **삼자 일치**.

---

## 1. 판단축별 판정

| 축 | 점수(0~2) | 판정 근거 |
|----|----------|----------|
| ① 목표 달성 | **2** | iter-1에서 이미 2점이던 구조(S-28 사용자 계층 + S-27→S-18→S-20 운영 계층 + "S-28 FAIL이면 설계 결정 재검토" 상위 규칙)가 유지되고, 검증력을 깎던 두 결함이 해소됐다. Given이 "빈 껍데기(도구 생성 형식 행)"에서 "실제 의사결정 2건·블로커 1건이 누적된 저널"로 격상돼 문항 ③("이 파일을 왜 남겼는가")이 **실질 판정 가능**해졌고, 반증 조건 3종이 FAIL 트리거로 명문화돼 **통과 편향이 구조적으로 억제**된다. 특히 반증 조건 (iii)은 S-18 산출물 대체 금지와 짝을 이뤄 알리바이 통과 경로를 이중으로 막는다. 완주 간극도 은폐 대신 고지·이관됐다. |
| ⑤ 채택/잔존 | **2** | 교체형 요구의 (a)/(b) **양측이 전건 시나리오를 갖는다** — R-1(a)S-1/(b)S-2·S-21, R-5(a)S-12·S-13/(b)S-10·S-11·S-24·S-25, **R-6(a)S-12·S-16/(b)S-31**, R-7(a)S-12/(b)S-14, R-9(b)S-16. iter-1의 유일한 공백이던 R-6 AC(b)가 S-31로 메워졌고, 그 시나리오는 "언급 확인"에 그치지 않고 **문서↔코드 drift 대조(③)**까지 포함해 PLAN F-004 #5("템플릿 전면 교체")의 실제 반영을 강제한다. 계측 결함(잔존-0의 adoption 오라벨링)도 페이로드 수준에서 제거돼 채택 커버가 더 이상 과대 표시되지 않는다. |
| ⑥ 경계/부정 | **2** | 선언된 경계 자산이 **전부 소비**된다(고아 0). 실패·부정 경로가 계층별로 고르다 — 파일 삭제(S-4)·권한 0444 fail-open(S-5)·마커 부재 3케이스(S-6)·제거된 플래그 exit 1(S-9)·레거시 표/SSOT 불일치(S-11)·배너 negative(S-24)·오삭제 역검증(S-14·S-26)·거짓 통과 차단(S-27)에 더해, iter-1이 지적한 **최고 난도 2건이 신설**됐다: 구형 파일 **쓰기**(S-29 — 바이트 동결·중복 삽입 0)와 **손상 저널 복구 + 멱등**(S-30 — 2회 연속 호출). H-1(P0)의 원인 지점인 "조용한 no-op"이 이제 정면으로 assert된다. |

**평균**: (2 + 2 + 2) / 3 = **2.00**

---

## 2. 잔여 gaps (전부 비차단 — 게이트 통과를 막지 않음)

> iter-1 지적 8건은 전건 해소됐다. 아래는 **이번 회차에서 새로 실측된** 사항으로, 점수를 깎지 않되 구현·TEST 단계 전에 정리할 것을 권고한다.

### 2.1 [문서 정확성 · 신규] §1 리스크 가설 표의 시나리오 컬럼이 §3 선언·페이로드의 **부분집합**

`§1 H-N 행의 시나리오 컬럼` ↔ `§3 각 시나리오의 "가설 매핑" 필드` ↔ `.scenario-coverage-input.json`을 기계 대조한 결과, §1이 6개 가설에서 10건을 누락하고 1건을 초과한다:

| H | §1 표 | §3 선언(=페이로드) | 누락 | 초과 |
|---|---|---|---|---|
| H-1 | S-3·S-4·S-5·S-29·S-30 | S-1·S-2·S-3·S-4·S-21·S-29·S-30 | S-1·S-2·S-21 | S-5 |
| H-4 | S-6·S-11·S-29 | S-6·S-8·S-11·S-29 | S-8 | — |
| H-5 | S-11·S-24 | S-10·S-11·S-20·S-24 | S-10·S-20 | — |
| H-6 | S-25 | S-23·S-25 | S-23 | — |
| H-7 | S-9 | S-9·S-22 | S-22 | — |
| H-9 | S-12·S-26·S-31 | S-7·S-12·S-15·S-16·S-17·S-26·S-31 | S-7·S-15·S-16·S-17 | — |
| H-11 | S-27 | S-18·S-27 | S-18 | — |

- **실질 커버리지는 온전하다** — 결정론 게이트(④)는 페이로드를 소비하고 페이로드는 H-1~H-13 전건을 덮으므로, 이 불일치가 시나리오 집합의 실제 구멍을 뜻하지는 않는다. 따라서 ①⑤⑥ 점수에 반영하지 않는다.
- 그러나 §7 PM Gate 체크의 `[x] 리스크 가설 표(§1) H-N ID와 시나리오 S-N 1:N 매핑 완전`은 **사실과 다르다** — iter-1 §2.7에서 정정한 §2.2 체크와 **동종의 결함**이며, 같은 라운드에서 함께 정정됐어야 한다. §1은 사람이 읽는 리스크 지도이므로, 누락 상태로 두면 "H-9의 검증은 3건뿐"이라는 오독을 유발한다.
- 부수: S-5는 §3·페이로드 모두 H-2·H-3만 선언하는데 §1 H-1 행에 등재돼 있고, S-4는 §3 선언이 `H-1`인데 페이로드는 `H-1·H-2·H-3`이다(페이로드가 넓은 방향이라 커버리지상 안전).
- **권고**: §1 시나리오 컬럼을 페이로드 기준으로 동기화하거나, 체크 문안을 "§4 매핑 표 기준"으로 정정하라. 재게이트 사유는 아니다.

### 2.2 [정밀도 권고 · S-31] "템플릿 ↔ 실산출 골격 일치"의 대조 범위가 미확정

- PLAN `:640`은 `state-template.md`에서 **파이프라인 행 구성 규칙·산출물 행 규칙은 존치**한다고 규정한다(state.json `rows[]` 규칙이라 STATE.md 본문과 무관). 즉 이 파일은 "STATE.md 템플릿 본문 + 존치 규칙 절"의 혼합이다.
- S-31 ③은 "`state-template.md`의 신형 템플릿 본문과 `init` 실산출 저널 골격이 **일치**"로만 쓰여 있어, 전체 파일 대조로 해석되면 존치 절 때문에 **거짓 FAIL**이, 느슨하게 해석되면 "대체로 비슷" 수준의 무력한 assert가 된다.
- **권고**: 대조 대상을 "템플릿 코드펜스 블록" 또는 "`##` 섹션 헤더 집합"처럼 **기계가 추출 가능한 단위**로 명시하라(예: 실산출 STATE.md의 `^## ` 헤더 시퀀스 == 템플릿 블록의 `^## ` 헤더 시퀀스, 순서 포함).
- 부수: S-31 ②는 3문서 **각각**에 `state-tool show` 명시를 요구하는데, R-6 AC 문언은 "3개 문서에서 … 명시됨"으로 문서별 분배가 열려 있다. 템플릿 파일 성격상 `show` 언급이 부자연스러울 수 있으므로, 구현 착수 전 AC 해석(문서별 AND / 3문서 합집합)을 확정해두면 TEST 단계 분쟁이 없다. **더 엄격한 방향이므로 게이트 결함은 아니다.**

### 2.3 [⑥ 보강 권고 · 신규] `--note` 문자열의 마크다운 표 파괴 입력 미검증

- 의사결정 로그는 **마크다운 표**이고 `--note` 값은 그대로 셀에 삽입된다. `|`·개행·백틱을 포함한 note가 들어오면 표 구조가 깨져 다음 회차의 `append_decision_log` 행 수 계산(R-2 AC, PLAN §3.1.2 (2))이 오작동할 수 있다 — 이번 태스크가 **보존하려는 바로 그 구조**를 사용자 입력이 파괴하는 경로다.
- 현재 31건 어디에도 note 이스케이프/새니타이즈 검증이 없다(S-3·S-4·S-5·S-29·S-30 전부 평문 note 사용).
- ⑥축은 이미 2점 수준의 폭을 확보했으므로 감점하지 않는다. 다만 S-30(멱등)이나 S-3(누적 무손실)에 `|`·개행 포함 note 케이스 1건을 얹는 것이 저비용·고수익이다.

---

## 3. 종합 판정

```json
{
  "scores": { "goal": 2, "adoption": 2, "boundary": 2 },
  "average": 2.0,
  "gaps": [
    "[문서 정확성·비차단] §1 리스크 가설 표의 시나리오 컬럼이 §3 선언·정규화 페이로드의 부분집합 — H-1/H-4/H-5/H-6/H-7/H-9/H-11에서 10건 누락·1건 초과(S-5가 H-1 행에 오등재). 실질 커버리지는 페이로드 기준 전건 충족이라 점수 미반영이나, PM Gate 체크 '§1 H-N↔S-N 매핑 완전 [x]'가 사실과 불일치하며 이는 iter-1 §2.7과 동종 결함이다.",
    "[정밀도 권고·비차단] S-31 ③ '템플릿↔실산출 골격 일치'의 대조 단위 미확정 — state-template.md는 PLAN:640에 따라 파이프라인/산출물 행 규칙을 존치하므로 전체 파일 대조 시 거짓 FAIL, 느슨한 해석 시 무력한 assert가 된다. '^## 헤더 시퀀스 동일(순서 포함)' 등 기계 추출 단위로 명시할 것. 부수로 S-31 ②(3문서 각각 show 명시)가 R-6 AC 문언보다 엄격하므로 해석을 사전 확정 권고.",
    "[⑥ 보강 권고·비차단] --note 값이 마크다운 표 셀에 직삽되는데 '|'·개행 포함 입력을 검증하는 시나리오가 31건 중 0건 — 이번 태스크가 보존하려는 의사결정 로그 표 구조를 사용자 입력이 파괴하는 경로다. S-3 또는 S-30에 케이스 1건 추가 권고."
  ],
  "verdict": "pass"
}
```

**종료조건 판정** (`scenario-gate.md` §5-1): 판단축 각 ≥1점(0점 축 없음) **AND** 평균 2.00 ≥ 1.5 → **수렴(PASS)**. 결정론 게이트(②③④)가 이미 exit 0 / `all_covered: true`이므로 §6 tool-gated 집행의 두 증거가 모두 성립한다.

**iter-1 대비 이동**: goal 2→2, adoption 1→**2**, boundary 1→**2** (평균 1.33 → 2.00). 이동의 실체는 **시나리오 3건 신설(S-29·S-30·S-31)과 계측 라벨 정정**이며, 문안 보강만으로 점수를 얻은 축은 없다. 070 재발 방지 관점의 핵심 조건 — "목표를 검증하는 시나리오가 존재하고, 그것이 실패할 수 있는 형태로 배선돼 있는가" — 는 S-28의 반증 조건 3종 + Given 격상으로 iter-1보다 강화됐다.

§2.1~§2.3 잔여 3건은 **비차단**이다. §2.1은 문서 정확성, §2.2·§2.3은 구현·TEST 단계에서 흡수 가능하므로 재게이트 없이 다음 단계(구현)로 진행할 수 있다. 다만 §2.2는 S-31이 이번 PASS의 근거 시나리오이므로, TEST 단계 진입 전 대조 단위를 확정할 것을 권고한다.

> 본 보고서는 판정만 수행했다. `TEST-SCENARIO.md`·`.scenario-coverage-input.json`을 포함해 어떤 산출물도 수정하지 않았으며, gap 반영은 PM(오케스트레이터) 책임이다.
