# SCENARIO-GATE-1 — TEST-SCENARIO 목표-커버 루브릭 채점 (판단축 ①⑤⑥)

> 실행 일시: 2026-08-16T00:30:00+09:00 | phase: `scenario-rubric` | iteration: 1
> 판정자: opal-evaluator-agent (verdict-only · readonly — 어떤 산출물도 수정하지 않음)
> scenario_source: `tasks/094-260815-opd-STATE-저널화/TEST-SCENARIO.md`
> 규칙 SSOT: `opal/core/references/harness/scenario-gate.md` §2 루브릭 6축 · §5-1 종료조건
> 채점 범위: **①목표달성 · ⑤채택/잔존 · ⑥경계/부정 3축만** (②③④ 결정론 축은 test-tool 소관 — 본 보고서 판정 대상 아님)
> 부가 입력(맥락 확인용): `TASK.md`, `PLAN.md`, `.scenario-coverage-input.json`

---

## 1. 판단축별 판정

| 축 | 점수(0~2) | 판정 근거 |
|----|----------|----------|
| ① 목표 달성 | **2** | 목표("STATE.md를 저널로 재정의하되 파일은 존치")를 **사용자 계층**(S-28 [SUPERVISOR] — 캡틴이 저널을 직접 열고 `show`로 조회한 뒤 3문항 판정)과 **운영 계층**(S-27 배포 동일성 → S-18 배포본 5명령 실동작 → S-20 `show` 2포맷 실반환)에서 각각 직접 검증한다. §4 매핑 표 하단에 "**목표달성 시나리오 = S-28**, 기계 검증(S-1~S-27) 전건 통과해도 S-28 FAIL이면 설계 결정 자체를 재검토"라는 상위 판정 규칙이 명문화되어 있어, 070형 결함(목표 검증 시나리오 자체의 부재)은 재발하지 않는다. 단 §2.3의 결함 2건은 검증력을 실질적으로 깎는다. |
| ⑤ 채택/잔존 | **1** | 교체형 4요구 중 3건(R-1·R-5·R-7)은 (a)잔존0·(b)채택이 **양쪽 모두** 시나리오로 검증된다. 그러나 **R-6 AC(b)(신형 채택 — 하네스 3문서에 저널 구조·`show` 조회 경로 명시)를 검증하는 시나리오가 없다.** §4 매핑 표는 R-6을 S-12·S-16(둘 다 구형 잔존 0 grep)에만 매핑했고 교체형 표기도 누락됐다. 즉 문서 개정 영역에서 "0건 세기"만 남고 "신형이 실제로 들어갔는가"가 빠진 축이 실재한다. |
| ⑥ 경계/부정 | **1** | 경계·부정 경로는 다수 도출되어 있다(S-4 파일 삭제 / S-5 권한 0444 fail-open / S-6 마커 부재 3케이스 / S-9 잘못된 플래그 exit 1 / S-11 레거시 불일치 / S-24 배너 극성 negative / S-14·S-26 오삭제 역검증). 그러나 **작성자 자신이 §2.1에 선언한 경계 자산 중 `tmp_task_broken`(손상 저널)이 어떤 시나리오에도 연결되지 않은 고아 fixture**이며, **레거시 STATE.md 사본에 쓰기 명령을 거는 경로가 전무**하다(레거시 사본은 S-11·S-24의 읽기 전용 `show`에만 쓰인다). 정상 경로 편향은 아니나, 최고 난도 실패 경로 2건이 누락됐다. |

**평균**: (2 + 1 + 1) / 3 = **1.33**

---

## 2. gaps — 재작성 대상

### 2.1 [BLOCKING · ⑥] 레거시 STATE.md 공존 **쓰기** 경로 미검증 — PLAN 후보 시나리오의 전사 소실

- H-4의 계약 위험은 "레거시 파일에서 **예외·중복 삽입·정규식 오매칭**"이다. 이는 레거시 파일에 `advance`/`mark`/`block`을 **거는** 순간에만 발현된다.
- PLAN 리스크 가설 표 H-4의 시나리오 후보는 명시적으로 다음이었다: *"S-4: `tasks/093-.../STATE.md` 사본에 `advance`/`mark`/`block` 연속 호출 → 무예외 + 의사결정 로그 정상 추가 + 레거시 표 **무변경(바이트 동결)**"*.
- TEST-SCENARIO §1은 H-4를 **S-6, S-11**에 매핑했는데, S-6은 신규 fixture(삭제/마커제거/임의텍스트)이고 S-11은 읽기 전용 `show`다. **PLAN이 지정한 레거시 쓰기 시나리오가 전사 과정에서 소실됐다.**
- 결과: `ensure_journal_skeleton`·`append_decision_log`·`update_state_md_header`가 마커·표·`## 현재 상태`를 보유한 구형 파일을 만났을 때의 동작(특히 [MUST] "레거시 표 무변경")이 **어떤 시나리오로도 검증되지 않는다**. §2.1이 "레거시 자산은 반드시 사본으로 조작"이라 [MUST]를 걸어놓고 정작 그 사본에 쓰기를 거는 시나리오가 없다.
- **요구**: 레거시 사본에 `advance`→`mark --force --note`→`block` 연속 호출 후 ① 전건 `ok:true` ② 의사결정 로그 1행 정상 추가 ③ 마커·표·`## 현재 상태` 블록 **바이트 동결** ④ 중복 마커/중복 표 삽입 0건을 검증하는 L2 시나리오를 신설하고 H-4에 매핑하라.

### 2.2 [BLOCKING · ⑥] `tmp_task_broken`(손상 저널) 고아 fixture — 골격 복구 append 분기 미검증

- §2.1 사전 조건 데이터 표는 `tmp_task_broken/STATE.md`(`## 의사결정 로그` 표 헤더만 제거)를 선언하지만, §2.2 데이터 흐름 표·§3 시나리오 28건·§4 매핑 표 어디에서도 이 자산이 **소비되지 않는다**(전문 grep 실측: 62행 1회 등장이 전부).
- 이 fixture가 겨냥한 분기는 PLAN §3.1.2 (2) `ensure_journal_skeleton`의 **세 분기 중 두 번째**(표 헤더 정규식 미매칭 → 파일 끝에 빈 표 블록 append)다. 나머지 두 분기는 S-4(md is None → 신규 생성)와 S-3(이미 존재 → 멱등)이 덮지만, **append 분기와 그 멱등성은 무검증**이다.
- S-6 (iii) "임의 텍스트로 덮어쓰기"가 유사해 보이나, S-6의 기대 결과는 `ok:true`·exit 0뿐이며 **의사결정 로그가 실제로 기재됐는지를 assert하지 않는다** — H-1("`append_decision_log`가 조용히 no-op하여 기재가 증발")의 원인 지점을 정확히 비껴간다.
- **요구**: 손상 저널(표 헤더 제거)에서 `mark --force --note` 실행 시 ① `ok:true` ② 골격이 append로 복구되고 로그 1행 기재 ③ **기존 본문 무손실**(삭제·치환 0) ④ 2회 연속 호출 시 골격 중복 append 0건(멱등)을 검증하는 시나리오를 신설하라.

### 2.3 [BLOCKING · ⑤] R-6 AC(b) 신형 채택 미검증 — 문서 개정이 "0건 세기"로만 검증됨

- R-6 AC는 교체형이다: (a) 3문서에서 표 템플릿·마커 명세·`## 현재 상태` 템플릿 0건 / (b) **저널 구조(의사결정 로그·블로커)와 `show` 조회 경로가 명시됨**.
- PLAN §5.1은 이를 정확히 반영했다 — F-004 행 "하네스 3문서 구형 0 / **신형 채택** | TS-013 | 표 템플릿·마커 명세·`## 현재 상태` 0건 **+ 저널·`show` 명시**".
- 그런데 TEST-SCENARIO의 S-13은 대상이 `verification-loop-guide.md`·`harness/state.md` **2문서**로 축소됐고, 기대 결과도 "세션 복원이 `show`로 기술 / 검증 루프 진행률 새 보관처 명시"로 좁아졌다. §4 매핑 표는 R-6을 S-12·S-16(둘 다 잔존 0 grep)에만 걸었다.
- 결정적으로 **`state-template.md`는 TEST-SCENARIO 전문에 단 한 번도 등장하지 않는다**(grep 실측 0건). PLAN F-004 개정 목록 #5가 "템플릿 **전면 교체**(저널 구조)"로 지정한 문서이자, 향후 모든 신규 STATE.md의 문서상 원형인데, 그 신형 템플릿이 실제로 등재됐는지 검증하는 시나리오가 없다. 나아가 **문서 템플릿(`state-template.md`)과 코드 산출물(`_build_new_state_md`)이 서로 일치하는가**라는 drift 검증도 부재하다 — S-1은 코드 산출물만, S-12는 구형 잔존만 본다.
- **요구**: 하네스 3문서(`opal-harness.md` §3 · `harness/state.md` · `harness/state-template.md`) 각각에 ① `## 의사결정 로그`·`## 블로커` 저널 구조가 기술되고 ② `state-tool show` 조회 경로가 명시됨을 확인하고, ③ `state-template.md`의 템플릿 본문과 `init` 실산출 STATE.md 골격이 일치함을 대조하는 시나리오를 신설해 R-6 AC(b)에 매핑하라(§4 비고에 "교체형 (b)" 표기 포함).

### 2.4 [비차단 · ⑤ 계측 결함] 정규화 페이로드가 잔존-0 시나리오를 `is_adoption_scenario`로 라벨링

- `.scenario-coverage-input.json`에서 S-12·S-16·S-17이 `is_adoption_scenario: true`로 표시돼 있다. 이 셋은 전부 **구형 잔존 0 grep**(현재시제 금지 패턴 0건 / "STATE.md…유일 SSOT" 0건 / `marker_missing` 서술 0건)으로, 교체형의 (a)측이지 (b)측이 아니다.
- 이 라벨링은 결정론 게이트 출력만 보는 관찰자에게 **채택 커버가 충분한 것처럼 보이게 만들며**, §2.3의 누락을 은폐하는 방향으로 작동한다. 재작성 시 (a)/(b)를 라벨 수준에서 분리하라(예: 잔존 검증에는 adoption 플래그를 부여하지 않는다).

### 2.5 [비차단 · ① 요청받은 판단] H-13/S-28 신규 추가는 게이트 우회인가

**판정: 우회가 아니다 — ①축에 실질 기여한다. 단 현재 설계로는 판정력이 약해 보강이 필요하다.**

- 우회가 아니라고 보는 근거: ① S-28은 자동 검증이 **원리적으로 불가능한 영역**(설계 결정 b안의 실사용 타당성)을 대상으로 하며, 자동 검증 가능한 영역을 사람에게 떠넘긴 흔적이 없다 — R-1~R-9의 기계 검증 가능 AC는 전부 S-1~S-27이 별도로 덮는다. ② 판정 문항이 3개로 구체화돼 있고 PM 표준 요청 양식까지 첨부돼 실행 가능하다. ③ "S-28 FAIL이면 설계 결정 자체를 재검토한다"는 상위 규칙이 명문화돼, 통과용 알리바이가 아니라 **되돌림 트리거**로 배선돼 있다. ④ 운영 계층 목표 검증(S-18·S-20)이 S-28과 독립적으로 존재하므로, ①이 사람 판단 단일점에 의존하지 않는다.
- 그럼에도 판정력이 약한 지점 2가지:
  - **(i) 판정 대상의 충실도 부족.** S-28의 Given은 "S-18 실증 태스크"인데, S-18은 임시 폴더에서 `init`→`advance`→`mark`→`block`→`add-row` 5명령을 기계적으로 연속 실행한 결과물이다. 이 저널의 의사결정 로그에는 실제 의사결정 서술이 없고 도구가 생성한 형식 행만 남는다. "의사결정 로그·블로커만 남은 구조가 **이 파일을 왜 남겼는가**에 답하는가"(문항 ③)는 **실제 의사결정이 누적된 저널을 봐야 판정 가능**하다. 현재 Given으로는 캡틴이 빈 껍데기를 보고 판정하게 된다.
  - **(ii) 자기확인 위험.** b안(파일 존치 + 파생 전면 제거)을 확정한 주체와 S-28의 수용성을 판정하는 주체가 동일한 캡틴이다. Producer≠Evaluator 원칙(scenario-gate §4 [MUST])의 문언상 대상은 아니나, 판정이 최초 결정을 추인하는 방향으로 기울 구조적 편향이 존재한다.
  - **요구(권고)**: S-28의 Given을 "본 태스크(094) 자신을 신형 도구로 이어 완주하거나, 실제 의사결정 2건 이상·블로커 1건 이상이 기재된 저널"로 격상하고, 문항 ③에 **반증 조건**(예: "진행 현황을 확인하려 STATE.md를 먼저 열게 되는 습관이 남는다면 FAIL")을 명문화하라.
- 부수 관찰: TASK.md §완료기준은 "신규 태스크 1건을 신형 구조로 **완주**"를 요구하지만, S-18은 5명령 실행이지 파이프라인 완주가 아니다(R-8 AC(a) 자체는 5명령이므로 요구 커버 축 위반은 아니다). 완료 판정 시 이 간극을 DONE.md에 명시하거나 S-18의 범위를 완주로 확장하라.

### 2.6 [비차단 · 요청받은 판단] S-19의 "숫자 하한 → 성질 기반" 전환은 검증력을 약화시켰는가

**판정: 방향은 타당하나, 전환 과정에서 결정론 안전장치가 함께 제거되어 **부분적으로 약화**됐다.**

- 타당한 부분: 25건이 D-1·D-2·R-3으로 **정당하게 삭제**되는 상황에서 `passed >= 308` 하한은 padding 테스트를 직접 유도한다. 숫자를 채우려고 작성한 테스트는 헌법 §4("완료는 검증된 동작")상 검증이 아니다. 성질 조건 4종(fail 0 / 삭제 25건 1:1 대응 / 신규 기능 5종 커버 / padding 0건)은 원래 하한이 대리하려던 것(회귀 커버리지 유지)을 더 직접적으로 겨냥한다.
- 약화된 부분: 전환 후 **총량에 대한 결정론 가드가 0**이 됐다. 남은 3조건(②③④)은 전부 사람·에이전트의 **감사 판단**이며, 특히 "삭제 25건"은 PLAN 회계값(-12-7-6)에 고정돼 있는데 **초과 삭제를 잡는 장치가 없다**. GREEN 루핑 중 손이 많이 가는 테스트가 조용히 추가 삭제돼도, 감사표가 그 사실을 스스로 신고하지 않는 한 S-19는 통과한다. 즉 자기신고 의존이다.
- **요구(권고)**: 하한을 되살리지 말고 **결정론 대조 1줄을 추가**하라 — `git diff` 기준 삭제된 테스트 함수의 **총 개수가 감사표 열거 건수와 정확히 일치**(초과 삭제 0건)함을 기계로 확인하는 조건을 S-19 기대 결과에 넣는다. 이는 padding을 유도하지 않으면서 무단 삭감만 차단한다. 아울러 최종 `passed` 수를 참고값으로 기록하도록 명시하라(PLAN §5.2가 이미 "308은 참고값이며 하한이 아님"을 규정했으므로 정합).

### 2.7 [비차단 · 문서 정확성] PM Gate 자체 체크의 사실 불일치

- §7 PM Gate 체크가 "모든 시나리오에 Given/When/Then(§2.2) 3필드 채워짐 [x]"로 표기돼 있으나, §2.2 데이터 흐름 표는 28건 중 **18건만** 수록한다(S-15·S-16·S-17·S-18·S-20·S-21·S-22·S-23·S-24·S-25 부재). 각 시나리오 표에 조건/기대 결과가 있어 실질 공백은 아니지만, **체크 표기가 사실과 다르다**. 재작성 시 §2.2를 28건 전량으로 채우거나 체크 문안을 실제 근거(§3 시나리오 표)로 정정하라.

---

## 3. 종합 판정

```json
{
  "scores": { "goal": 2, "adoption": 1, "boundary": 1 },
  "average": 1.33,
  "gaps": [
    "[⑥] 레거시 STATE.md 사본에 advance/mark/block 쓰기를 거는 시나리오 부재 — PLAN H-4의 후보 시나리오(레거시 표 바이트 동결 + 로그 정상 추가)가 전사 과정에서 소실. H-4 매핑(S-6·S-11)은 신규 fixture와 읽기 전용 show뿐이라 '예외·중복 삽입·정규식 오매칭' 위험을 검증하지 못한다.",
    "[⑥] §2.1이 선언한 tmp_task_broken(손상 저널) fixture가 어떤 시나리오에도 연결되지 않은 고아 — ensure_journal_skeleton의 'append로 골격 복구' 분기와 그 멱등성·본문 무손실이 무검증. S-6(iii)은 ok:true만 볼 뿐 로그 기재 여부를 assert하지 않아 H-1 원인 지점을 비껴간다.",
    "[⑤] R-6 AC(b)(신형 채택 — 하네스 3문서에 저널 구조·show 조회 경로 명시)를 검증하는 시나리오 부재. PLAN TS-013의 '저널·show 명시' 절반이 S-13으로 전사되며 2문서로 축소됐고, state-template.md는 TEST-SCENARIO 전문에 0회 등장. 문서 영역 교체형이 '0건 세기'(S-12·S-16)만 남았다.",
    "[⑤ 계측] 정규화 페이로드가 잔존-0 grep 시나리오(S-12·S-16·S-17)를 is_adoption_scenario=true로 라벨링해 채택 커버를 과대 표시한다.",
    "[① 보강권고] S-28은 게이트 우회가 아니며 ①에 실질 기여하나, Given이 'S-18 임시 폴더 5명령 산출물'이어서 판정 문항 ③(왜 이 파일을 남겼는가)을 실질 판정할 수 없다. 실제 의사결정이 누적된 저널로 Given을 격상하고 반증 조건을 명문화할 것. b안 결정자=판정자인 자기확인 편향도 병기.",
    "[① 보강권고] TASK 완료기준 '신규 태스크 1건 완주'와 S-18(5명령 실행)의 범위 간극.",
    "[검증력 약화] S-19의 성질 기반 전환은 방향은 타당하나 총량 결정론 가드가 0이 되어 초과 삭제가 자기신고에만 의존한다. 하한 복구 대신 '삭제된 테스트 함수 총수 == 감사표 열거 건수(초과 삭제 0)' 결정론 대조를 추가할 것.",
    "[문서 정확성] PM Gate 체크 '모든 시나리오에 §2.2 3필드 채워짐'이 사실과 불일치 — §2.2는 28건 중 18건만 수록."
  ],
  "verdict": "fail"
}
```

**종료조건 판정** (`scenario-gate.md` §5-1): 판단축 각 ≥1점 조건은 충족(0점 축 없음)하나, **평균 1.33 < 1.5**로 임계 미달 → **수렴 실패**. Producer(알투+캡틴)가 §2.1~§2.3의 BLOCKING gap 3건을 반영해 TEST-SCENARIO.md를 재작성한 뒤 iteration 2로 재진입한다. §2.4~§2.7은 동일 라운드에서 함께 반영할 것을 권고한다.

> 본 보고서는 판정만 수행했다. TEST-SCENARIO.md를 포함해 어떤 산출물도 수정하지 않았으며, gap 반영은 PM(오케스트레이터) 책임이다.
