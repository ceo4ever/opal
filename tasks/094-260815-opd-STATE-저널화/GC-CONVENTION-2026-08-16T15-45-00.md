# GC CONVENTION REPORT — 2026-08-16T15-45-00

## 1. 헤더

- 실행 일시: 2026-08-16 15:45 (KST)
- 범위: 태스크 094 변경분 전체 (worktree `.opal-worktrees/task_094`, 커밋 `b2804bd`·`9d901ea`) — 코드 3 + 문서 27
- 에이전트: opal-convention-checker (진단 전담, 읽기 전용)
- 기준 문서: `docs/CONVENTIONS.md` 존재 — 본 태스크가 개정한 **현재(HEAD) 버전**(변경이력 v1.6.1까지)을 유일 기준으로 사용. 프레임워크 내장 공통 규칙은 적용하지 않음.
- APPLY 수행 여부: N (수동 대기 — 본 에이전트는 진단만 수행, 수정은 오케스트레이터 CLOSE 단계에서 `//opds` 체인으로 이관)

---

## 2. 요약 지표

| 지표 | 값 |
|------|-----|
| 총 이슈 수 | 4 |
| 심각도 분포 | Critical 0 / High 1 / Medium 1 / Low 2 / Info 0 |
| 자동 수정 가능 | 1 (GC-C001, JSON 수기 교정) |
| 수동 조치 필요 | 3 |
| 파일별 상위 Top 5 | `opal/tools/state-tool/tests/test_state_tool.py` (1건) / `docs/CONVENTIONS.md` (1건) / 변경이력 HH:mm 누락 8개 문서 (1건, 빈도 트리거) / 커밋 메시지 1건 |
| 카테고리별 빈도 | 문서화(변경이력 형식) 8개 파일 / 파일 구조·헤더 무결성 1개 파일 / 커밋 규칙 1건 |
| Critical/High 수 | 1 (High만, Critical 0) |
| 문서 업데이트 제안 수 | 1 (빈도 트리거 1건) |

---

## 3. 수정 대상 (체크리스트)

### Critical (0건)

### High (1건)

- [ ] GC-C001 [`opal/tools/state-tool/tests/test_state_tool.py:6-18, 29-30`] @header 블록이 유효하지 않은 JSON — `description` 키 중복 + `exports` 배열 콤마 누락
  - 카테고리: 파일 구조 / 문서화 (@header 무결성)
  - 위반 기준: 프로젝트(CONVENTIONS.md §구현 규칙 > @header 규칙, L211-217 — "코드 파일을 생성·수정할 때 파일 상단에 @header 블록을 작성한다", 근거: `header-rules.md`·`header-standard.md` §7)
  - 설명: 병합 커밋 `9d901ea`("tests @header/exports: 신설 클래스 목록 합집합" 언급)에서 093/094 두 브랜치의 헤더를 병합하며, (1) 6번째 줄에서 시작한 `"description"` 키가 17번째 줄 `분리.",`에서 닫힌 직후 18번째 줄에 **두 번째 `"description"` 키가 재등장**한다(093 계열 요약 + "094 R-11 Step0" 요약이 별도 키로 남아 있음 — JSON 객체에 동일 키가 중복되어 첫 번째 값이 파서에 따라 유실/혼동됨). (2) `"exports"` 배열에서 29번째 줄 `"TestShowAsQueryStandard"` 뒤에 콤마가 없이 30번째 줄 `"TestT093AutoNaRemoval"`이 바로 이어져 **문법적으로 유효하지 않은 JSON**이 된다. `python3 json.loads()`로 파싱 시 `Invalid \escape` 오류(중복 키 이전에 이스케이프 오류가 먼저 걸림) 및 구조상 콤마 누락이 실측 확인됨. `code-scan`이 인라인 @header를 JSON으로 파싱해 커버리지·validate를 수행하는 도구 계약을 이 파일에서 깨뜨린다.
  - 해결 방안: 두 `description` 값을 하나로 병합(093 계열 문장 + 094 Step 0/R-11 계열 문장을 이어 붙여 단일 키로 재구성)하고, `exports` 배열의 `"TestShowAsQueryStandard"` 뒤에 콤마를 추가한다. 수정 후 `python3 -c "import json,re; ..."` 또는 `code-scan validate opal/tools/state-tool/tests/test_state_tool.py`로 유효 JSON 재확인.
  - 자동 수정: Y (텍스트 병합 + 콤마 삽입 — 다만 두 description 문장을 어떤 순서/방식으로 합칠지는 의미 판단이 필요해 기계적 자동화보다 워커 검토 권장)
  - 참조: `opal/core/references/harness/header-rules.md`, `opal/core/references/header-standard.md` §7

### Medium (1건)

- [ ] GC-C002 [`docs/CONVENTIONS.md:150` vs `docs/CONVENTIONS.md:156`] §태스크 산출물 구조 내 STATE.md 서술 — 트리 캡션과 상세 문장의 표준 문구 불일치(자기모순)
  - 카테고리: 문서화 / State 관리 규칙
  - 위반 기준: 프로젝트(CONVENTIONS.md §태스크 산출물 구조, 본 태스크가 v1.6.0/v1.6.1에서 직접 개정한 절) — 변경이력 v1.6.1은 "두 파일 모두 state-tool이 갱신한다"(부정확)를 "STATE.md는 저널 골격·의사결정 로그만 자동 갱신, 블로커·자유 기재는 PM 수동"으로 세분화했다고 명시하나, 이 정정은 156번 줄(상세 문장)에만 반영되었고 150번 줄(트리 캡션)은 그대로 남아 있다.
  - 설명: 150번 줄은 `STATE.md              의사결정 로그·블로커 저널 (state-tool이 기록, 조회는 show)`로, 괄호 안 "(state-tool이 기록, ...)"가 STATE.md 전체(블로커·자유 기재 포함)를 state-tool이 기록하는 것처럼 읽힌다. 그러나 바로 아래 156번 줄은 "블로커 내용·자유 기재는 PM이 수동으로 기록한다"고 명시해, state-tool이 자동 기록하는 범위(저널 골격+의사결정 로그)와 PM이 수동 기록하는 범위(블로커·자유 기재)를 구분한다. 같은 절 안에서 요약 문구(150)와 상세 문구(156)가 서로 다른 주체를 "기록자"로 암시하는 표준 문구 A/B 불일치다. 정확히 v1.6.1이 고치려던 바로 그 오해("두 파일 모두 state-tool이 갱신")가 트리 캡션 쪽에는 잔존한다.
  - 해결 방안: 150번 줄 캡션을 156번 줄과 정합되게 축약 — 예: `STATE.md              의사결정 로그·블로커 저널 (골격·로그는 state-tool 자동, 블로커·자유기재는 PM 수동)` 또는 단순히 `(조회는 show, 상세는 §State 관리 참조)`로 바꿔 트리 캡션이 독자적으로 오해를 유발하지 않게 한다.
  - 자동 수정: N (문구 재작성 필요)
  - 참조: TBD — 문서 내부 정합성 규칙(CONVENTIONS.md 자체, 별도 외부 린트 없음)

### Low (2건)

- [ ] GC-C003 [`docs/CONVENTIONS.md:280`; `opal/core/references/harness/header-rules.md:161`; `opal/core/references/harness/pm-review-gate.md:174`; `opal/core/references/harness/qa-standards.md:78`; `opal/core/references/harness/task-process.md:107`; `opal/core/references/opal-harness-interactive.md:193,194`; `opal/core/references/tools.md:1137,1162`; `opal/skills/opal-pilot-sdd/references/spec-plan-guide.md:296`] 변경이력 일시 형식에서 `HH:mm` 누락 — 날짜만 기재 (총 9개 행, 8개 파일)
  - 카테고리: 문서화 (변경이력 작성 의무)
  - 위반 기준: 프로젝트(CONVENTIONS.md §파일 구조 > 변경이력, L138 "일시 형식: `YYYY-MM-DD HH:mm` (KST 기준)" / §구현 규칙 > 변경이력 작성 의무, L245 "일시는 `YYYY-MM-DD HH:mm` (KST)")
  - 설명: 위 9개 행 모두 `2026-08-16`처럼 날짜만 적혀 있고 시각(`HH:mm`)이 없다. 같은 태스크에서 같은 파일(`docs/CONVENTIONS.md`)의 v1.6.0 행(L279)은 `2026-08-16 13:36`으로 정확히 형식을 지켰는데 바로 다음 v1.6.1 행(L280)만 시각이 빠져, 규칙 미준수가 이번 태스크 자체의 신규 산출물 내에서도 일관되지 않게 나타난다. 다만 이 8개 파일 상당수는 이번 태스크 이전부터도 시각 없는 과거 행(예: `pm-review-gate.md` v1.0/v1.1/v1.2, `opal-harness-interactive.md` v1.0~v1.2, `tools.md` v1.0)이 존재해, 이번 태스크가 새로 만든 결함이라기보다 기존에 널리 퍼진 미준수 관행을 답습·확산한 것에 가깝다. (동일 fingerprint가 8개 파일에서 발견 — 빈도 트리거 대상, §4 참조.)
  - 해결 방안: 위 9개 행의 날짜 뒤에 KST 기준 `HH:mm`을 보강한다(가능하면 실제 작업 완료 시각 기준. 정확한 시각 확인이 어려우면 태스크 종료 시점의 `date.js datetime` 출력으로 일괄 대체 가능).
  - 자동 수정: Y (형식 보강 — 정확한 시각 값 확정은 수동 확인 권장)
  - 참조: TBD — 사내 CONVENTIONS.md §변경이력 규칙(외부 린트 없음)

- [ ] GC-C004 [`9d901ea` 커밋 메시지 1행: `merge(094): main(093 사용자 확인 행 자동 승인 일원화) 병합 — R-11 편입 선결`] 커밋 type `merge`가 CONVENTIONS.md Type 표(feat/fix/refactor/chore/docs) 5종에 없음
  - 카테고리: 커밋 규칙
  - 위반 기준: 프로젝트(CONVENTIONS.md §커밋 규칙 > Type, L172-179 — 표에 등록된 type은 `feat`/`fix`/`refactor`/`chore`/`docs` 5종뿐)
  - 설명: `9d901ea`는 실제 2-parent git merge 커밋(`b2804bd` + `b41b06b`)이며, 메시지 형식(`{type}({scope}): {한국어 설명}`) 자체는 지켰으나 `type` 값 `merge`가 CONVENTIONS.md Type 표에 없다. 다만 이는 이번 태스크의 일회성 일탈이 아니라 093의 자체 merge-back 커밋(`b41b06b: merge(093): feat/OP-TASK-093 — 사용자 확인 행 자동 승인 경로 일원화`)에서도 동일하게 쓰인 기존 패턴이다 — worktree 브랜치를 main에 되돌려 merge하는 커밋에 대해 암묵적으로 `merge` type이 반복 사용되고 있으나 CONVENTIONS.md에는 문서화되어 있지 않다.
  - 해결 방안: 이미 커밋된 이력이므로 강제 재작성(rebase)은 권장하지 않는다. 대신 CONVENTIONS.md §커밋 규칙 Type 표에 `merge`(용도: worktree 브랜치 main 병합 전용) 6번째 항목을 추가해 기존 관행을 명문화할 것을 제안한다(§4 새 카테고리 트리거 참조).
  - 자동 수정: N (과거 커밋 메시지 수정 대상 아님 — 문서 보강만)
  - 참조: TBD — CONVENTIONS.md §커밋 규칙 자체(외부 린트 없음)

### Info (0건)

---

## 4. 문서 업데이트 제안 (트리거 발동 시만)

- [ ] GC-DP-C001 [빈도 트리거] 이슈 "변경이력 일시 `HH:mm` 누락" (8개 파일: `docs/CONVENTIONS.md`, `header-rules.md`, `pm-review-gate.md`, `qa-standards.md`, `task-process.md`, `opal-harness-interactive.md`, `tools.md`, `opal-pilot-sdd/references/spec-plan-guide.md`) → 규칙 재확인/강조 제안
  - 근거: 단일 태스크(094) 실행 내 8개 파일 — 빈도 임계값 N=3 초과
  - 제안 내용: CONVENTIONS.md L138/L245의 `YYYY-MM-DD HH:mm` 규칙은 이미 명문화되어 있으나 실제 준수율이 낮다. 신규 규칙 추가가 아니라, "일시 미기재 시 `state-tool`/`date.js`가 아닌 워커 임의 생략을 허용하지 않는다"는 취지의 실행 절차(예: PM Gate 자가 점검 항목에 "변경이력 행에 HH:mm 포함 여부" 1줄 추가)를 §구현 규칙 > 변경이력 작성 의무에 보강하는 것을 제안한다.

- [ ] GC-DP-C002 [새 카테고리 트리거] "merge 커밋 type" → CONVENTIONS.md §커밋 규칙 > Type 표 신설 제안
  - 근거: `093`(`b41b06b`)·`094`(`9d901ea`) 두 태스크에서 반복된 worktree merge-back 커밋 패턴이 현재 Type 표(feat/fix/refactor/chore/docs) 어디에도 속하지 않는다.
  - 제안 내용: Type 표에 `merge | worktree 브랜치를 main에 병합할 때 (merge-back 전용, 일반 태스크 커밋에는 사용 금지)` 행 추가.

---

## 5. 문서 작성 유도 (해당 없음)

`docs/CONVENTIONS.md` 존재 — 작성 유도 생략.
