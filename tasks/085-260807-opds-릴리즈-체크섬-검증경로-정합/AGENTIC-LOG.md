# AGENTIC-LOG: 릴리즈 체크섬 검증 경로 정합

> 모드: agentic | 시작: 2026-08-07 08:40 | 스킬: //opds

## 요약

| 항목 | 건수 |
|------|------|
| 게이트 판단 | 9회 (Pass: 7 / Fail: 2) |
| 3회 초과 Gate | 0건 (Critical: 0 / Normal: 0 / Minor: 0) |
| 오류 발견 | 6건 (설계 2 · 정적대조 7종 접수 · 워커 중단 2 · O-5 오수정 1) |
| 수정 지시 | 6건 (반영: 6 / 미반영: 0) |
| PM 의사결정 | 7건 |
| 개선 사항 | 1건 (Evaluator advisory 2/3 반영) |
| 에스컬레이션 | 2건 (`pwsh` 설치 여부 · 재배포 승인 — 둘 다 캡틴 판단 대기) |

## 대행 일지

| # | 시점 | 단계 | 카테고리 | 내용 | 결과 |
|---|------|------|----------|------|------|
| 1 | 2026-08-07 08:40 | TASK | DECISION | 결함 3지점(update.sh·install.sh·install.ps1)을 단일 태스크로 묶음 — 세 경로가 같은 URL 규약을 공유하므로 분리 시 규약 불일치 재발 위험 | 단일 태스크 확정 |
| 2 | 2026-08-07 08:40 | TASK | DECISION | `release.yml` 아카이브 형식 변경(`--prefix` 추가)을 범위에서 제외 — 이미 발행된 v0.6.7~v0.6.11이 전부 prefix 없음이라 호환 분기가 어차피 필요하고, 워크플로우 변경은 검증 비용만 추가 | 범위 제외, PLAN에서 재판단 여지 명시 |
| 3 | 2026-08-07 08:40 | TASK | ERROR | `install.sh` 무결성 검증 무음 스킵 발견 — grep 패턴 `opal.tar.gz`의 `.`이 와일드카드로 동작해 항목 미매칭. 소유자 보고 대상 결함이 최초 신고분(하드 실패) 외 1건 추가 | TASK.md A-3에 기재 |
| 4 | 2026-08-07 08:40 | TASK | GATE | TASK 사용자 확인 행 자율 통과 — 명확화 4요소 잠금 완료, 요구사항 6건 전부 Pass/Fail 판정 가능 AC 보유 | **Pass** (auto-pass) |
| 5 | 2026-08-07 08:55 | PLAN | ERROR | PLAN 워커가 TASK 미기재 결함 2건 추가 발견 — ① `update.sh:182` 빈 기대값 무음 통과(install.sh와 동일 결함 클래스) ② `update.sh:211` `\|\| tar` 관용 폴백이 prefix 없는 아카이브에서 발동하지 않아 루트 파일 무음 소실 | H-10·§3.1.1로 설계 반영 |
| 6 | 2026-08-07 08:55 | DECISION | DECISION | 3경로 코드 공유 불가 판정 수용 — `install.sh`가 curl-pipe-bash 단일 파일 자기완결을 보안 계약으로 삼고(`:44-48`), 세 파일 모두 소스 다운로드 이전에 실행되어 공유 런타임 위치가 없다. 규약 SSOT(§3.0)+헤더 각인+정적 대조로 대체 | 규약 단일화 채택 |
| 7 | 2026-08-07 09:00 | PLAN | GATE | 목표-커버 게이트 — coverage-check exit 0(요구6·기능6·가설10·시나리오17 전수 매핑) AND evaluator scenario-rubric 2/2/2 평균 2.0 pass. 두 증거 근거로 mark | **Pass** (tool-gated) |
| 8 | 2026-08-07 09:05 | PLAN | IMPROVE | Evaluator advisory 3건 중 2건 반영 — S-18(`pwsh` 런타임 등가 검증으로 Windows 없이 H-7 회수)·S-19(폴백 진입 4경로 완전성, (d) 재다운로드 실패=하드 실패). 나머지 1건(네트워크 순단 미재현)은 PLAN R-1에서 이미 수용 처리 | 2/3 반영 |
| 9 | 2026-08-07 09:05 | PLAN | GATE | PM Gate — PLAN.md 1228줄 직접 Read 검증. §4.2 6 Step 전부 소속 F-ID·완료기준·agent 배정 확인, TEST-SCENARIO 보안 5항목 존재, 변경 파일 4개로 Full Task 에스컬레이션 불요 | **Pass** |
| 11 | 2026-08-07 09:45 | EXECUTE | GATE | RED→GREEN 전환 확인 — PM 독립 실측 `bash scripts/tests/test_download_contract.sh` = PASS 26 / FAIL 0 (RED 기준선 2/24). 두 공유 헬퍼 `cmp` 바이트 동일, DRY-RUN 네트워크 0회 스텁 실증 | **Pass** |
| 12 | 2026-08-07 09:55 | EXECUTE | GATE | Step 4 정적 대조 — 8항목 중 6 일치 / **2 부분일치**(폴백 3동작·폴백 로그 문구, 둘 다 `install.ps1`). 완료 기준("8행 전부 일치") 미충족 | **Fail** (루핑 1회차) |
| 13 | 2026-08-07 09:55 | EXECUTE | ERROR | 불일치 7건 접수 — D-1 해시 비교 수단 3종 / D-2 `grep -F` 부분문자열 오탐 / D-3 ps1 sha 경로 설정 순서로 폴백 시 파일 미삭제 / D-4 ps1 폴백 사유 축약 / D-5 ps1 branch 분기가 사실과 다른 안내 / D-6 bash 체크섬 `case`에 `*)` 부재로 무음 통과 잔존 / D-7 dry-run 출력 문언 | CONTRACT-CROSSCHECK.md 기재 |
| 14 | 2026-08-07 09:55 | EXECUTE | DECISION | 7건을 교정/수용으로 분류 — **교정 4건**: D-2·D-3·D-5·D-6 + PS-1(Tls13 노출면 확대). 선정 기준은 "이 태스크가 제거하려는 결함 클래스(무음 통과·잠복 불일치)에 해당하거나, 이번 변경이 악화시킨 것". **수용 3건**: D-1(fail-closed·PLAN 지시대로 구현됨)·D-4(문구 축약, 무해)·D-7(코드 아닌 RG-8 문언 사안) — DONE.md에 근거 기록 | 4건 교정 지시 |
| 15 | 2026-08-07 09:55 | EXECUTE | DECISION | D-4·D-5의 발원지는 코드가 아니라 **PLAN §3.3.2가 §3.0 D-A(3경로 동형)와 어긋난 것**이라는 Step 4 분석 수용. 되돌릴 대상을 Step 2 구현이 아니라 규약 정합으로 잡고, 코드는 §3.0 D-A 기준으로 맞춘다 | 상위 규약 우선 |
| 16 | 2026-08-07 10:15 | EXECUTE | GATE | Step 4 재대조 2회차 — 8항목 전부 일치, fix 기인 회귀 0건. 잔여 관측 5건(N-1·N-2·N-4·N-5·O-1계열)은 전부 P3·fail-closed로 수용 | **Pass** |
| 17 | 2026-08-07 10:05 | EXECUTE | FIX | N-3(변경이력 시각 정체) PM 직접 정정 — fix 내용을 기존 버전 행에 병합하면서 시각이 fix 이전값으로 남아 있었다. 3파일 메타데이터 1줄씩 교체(로직 무관) | 반영 (ERROR #13 계열) |
| 18 | 2026-08-07 11:45 | TEST | ERROR | 테스트 워커가 실측 완료 직후 API 오류로 비정상 종료. **산출물 실측 판정 결과 0건**(TEST.md 미생성, TEST-SCENARIO.md 플레이스홀더 37개 잔존) — 워커 보고를 신뢰하지 않고 파일 상태로 확인 | 동일 컨텍스트 재개 1회 |
| 19 | 2026-08-07 12:00 | TEST | GATE | 9칸 실측 — PASS 6칸 / SKIP 3칸(`install.ps1`, `pwsh` 부재) / FAIL 0칸. 시나리오 17 PASS / 2 SKIP, 계약 테스트 26/26. **목표 시나리오 S-9는 구형 사본 대조까지 포함**(HEAD 사본 exit 1 재현 → 수정본 exit 0) | **Pass** |
| 20 | 2026-08-07 12:05 | TEST | GATE | TEST PM Gate — 테스트 워커 보고 결함 6건(O-1~O-6) 중 O-1(HEAD 대비 DRY-RUN 안내 회귀)·O-5(strip 무음 강등)를 차단 사유로 판정. O-2·O-3(도달 불가, 방어심층 존치)·O-6(사전존재)는 수용 | **Fail** (루핑 1회차) |
| 21 | 2026-08-07 12:10 | TEST | ERROR | bash fix 워커가 편집 완료 후 검증 단계에서 스톨(600s 무진전). 편집물은 디스크에 존재 — 재개 대신 **PM이 검증을 직접 수행**하기로 판단(잔여 작업이 읽기 전용 검증뿐이라 재개 비용이 이득을 초과) | PM 직접 검증 |
| 22 | 2026-08-07 12:20 | TEST | ERROR | **O-5 수정이 실제 실패 모드를 못 잡음을 PM 실측으로 발견** — `_dl_detect_strip`은 목록 조회 실패 시 빈 값이 아니라 `0`을 출력한다(입력 0줄 → root=0·tops=0 → 0). 따라서 "빈 값·비수치 거부" 가드로는 손상 tarball과 prefix 없는 정상 자산을 구분할 수 없다. 워커 보고를 그대로 수용했다면 미해결 결함을 해결로 기록할 뻔했다 | 재교정 필요 |
| 23 | 2026-08-07 12:25 | TEST | FIX | O-5 재교정 PM 직접 수행 — 호출부에 `tar -tzf` 선검사를 추가해 목록 조회 실패를 그 지점에서 하드 실패로 처리. 공유 헬퍼는 미접촉(바이트 동일 유지). 워커 2연속 실패(API 오류·스톨) + 변경 4줄 규모라 재디스패치가 비례하지 않는다고 판단 — 디스패치 의무는 정의된 단계에 적용되며 게이트 후속 미세 교정은 해당하지 않음 | 실증: 손상 tarball exit≠0 / 정상 자산 통과 |
| 24 | 2026-08-07 12:30 | TEST | GATE | fix 후 PM 독립 재검증 — 계약 테스트 26/26, `bash -n` 2파일, `shellcheck -S warning` 무출력, 공유 헬퍼 md5 불변, O-1 실증(태그→릴리즈 자산 경로·main→heads, 양쪽 curl 0회), O-5 실증(손상 tarball 하드 실패) | **Pass** |
| 25 | 2026-08-07 12:35 | CLOSE | DECISION | 재배포(`install-mac.sh`) 미수행 결정 — `~/.opal/`은 이 세션이 실제로 구동 중인 환경이라 검증 목적 교체는 위험 대비 이득이 없다. 대신 격리 `OPAL_HOME` + PATH 스텁으로 등가 검증. 재배포는 소유자 승인 사항으로 이관 | 캡틴 판단 대기 |
| 10 | 2026-08-07 09:10 | EXECUTE | DECISION | RED-first 트랙 집행 방식 확정 — PLAN은 "자동화 테스트 부재, 실행 증거 기록으로 대체"로 설계했으나 `scripts/tests/`에 bash 테스트 인프라가 실재하므로 `red-first.md` §5(인프라 부재 시에만 에스컬레이션)에 따라 우회 불가로 판단. 버그 수정=RED-first 강제(§1.5) 적용, 작성자≠구현자(§2) 유지 위해 opal-test-agent(mode: red)로 회귀 테스트 선작성 | PLAN에 없던 Step 0 추가 |
