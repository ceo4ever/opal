# SCENARIO-GATE-1 — TEST-SCENARIO.md 목표-커버리지 루브릭 판정 (op-scenario-gate)

> 실행 일시: 2026-09-04T07:50:00+09:00 | phase: scenario-rubric | iteration: 1
> 판정자: opal-evaluator-agent (verdict-only)
> scenario_source: `tasks/114-260903-opd-스킬위저드-신설/TEST-SCENARIO.md`
> 결정론 게이트(test-tool `scenario-coverage-check`): 이미 exit 0 통과 (requirements 11 / features 6 / hypotheses 9 / scenarios 29, missing 0). 본 보고서는 **판단축 ①⑤⑥만** 채점한다(②③④는 판정 대상 아님, `scenario-gate.md` §2 [MUST]).
> CONTRACT.md: 부재 (opd 1차 접합 — 본 phase는 Phase 2 병합 대상 아님, 영향 없음).

## 1. 축별 판정

| 축 | 점수(0~2) | 근거 |
|----|-----------|------|
| ① 목표 달성 | **2** | 목표는 「제안·설치·발동」 3요소. TS-023(L2/M1, 실제 `spawnSync` + fixture)이 「설치→발동 종단」을 실코드로 직접 검증한다 — 등재 후 `match`가 `found:true` + `resolved_path`에 실물 SKILL.md 경로를 반환하는지 실행 확인. TS-021·TS-022([SUPERVISOR]/M3)는 「제안(인터뷰)」을 사용자 계층에서 직접 검증하며, 기대 결과가 "질문 제시 + 후보 1건 이상 + 스킬명·출처·라이선스 3필드"로 구체적이어서 막연한 확인이 아니다. wizard의 핵심 산출(인터뷰 품질·자연어 제안)은 자연어 판단이 필요해 결정론 스크립트로 대체 불가능하므로, 이 요소에 한해 M3는 회피가 아니라 유일하게 성립 가능한 검증 방식이다(`test-scenario-guide.md` L3 정의: "FE 사용자 확인 또는 수동 부하 테스트 등 자동화 불가 항목에 한정"과 동일 계열). TS-011~TS-014(정적 grep)는 wizard 동작 자체를 검증하지 않는다는 지적은 타당하나, 목표 축의 실질 커버는 TS-021·022·023이며 이 3건이 실제로 사용자/운영 계층에서 목표 3요소를 분담해 직접 검증한다. |
| ⑤ 채택·잔존 | **1** | 구형 잔존0은 확인된다 — TS-013("`opal-skill-manager` 호출 지시 0건"), TS-019("`opal-skill-manager` 단독 지목 문장 0건", 2곳)가 정적 grep으로 검증한다. 그러나 **신형 채택의 실행-시간 검증이 없다**: TS-013·TS-019는 문서에 특정 문구가 "없다"는 것만 확인할 뿐, wizard가 실제로 실행될 때 manager를 호출하지 않고 skills.sh를 직접 쓰는지, R-9 라우팅 개정이 실제 미설치-매칭 상황에서 새 프로젝트 분기로 이어지는지를 확인하는 시나리오가 없다. 유일한 동적 실행 시나리오인 TS-021·TS-022(SUPERVISOR)의 기대 결과 어디에도 "manager 미호출"·"신규 라우팅 분기 사용"이 포함되어 있지 않다 — 즉 잔존0은 문서 차원에서만 닫혀 있고, 채택은 레지스트리 병합(TS-023, 간접 증거)을 빼면 실질적으로 미검증이다. |
| ⑥ 경계·부정 | **1** | 14건 중 레지스트리·경로 계층(TS-002·003·005·006·007·008·009·018·029, 8건)은 실 fixture + `spawnSync`로 부재/파손/미설치/충돌/스키마 경계를 코드 수준에서 실제로 검증해 견고하다. 그러나 **wizard 자체 절차의 부정 경로 5건(TS-024·025·026·027·028)은 전부 L3 정적 grep(M1)**이며, 이는 SKILL.md에 해당 규칙 문장이 "존재하는지"만 검사한다 — R-4 AC("검사 실패 시 설치를 진행하지 않는다는 규칙이 명시된다")를 문자 그대로 재확인하는 수준이라 사실상 AC를 되풀이한 self-confirming 시나리오(`scenario-gate.md` §1 "도출 엔진의 관점 편향"과 동일 위험)에 가깝다. RISKY 제외·scan-risk 실행 실패 시 미설치·검색 0건 위임·전건 거부 처리·비프로젝트 디렉토리 안전종료라는 5개 부정 경로 중 단 1건도 실제 wizard 실행(SUPERVISOR 포함)으로 재확인되지 않는다 — TS-021·022의 기대 결과는 정상 경로(후보 발견)만 다루고 이 5개 부정 분기를 지나가지 않는다. 문서에 규칙이 "쓰여 있다"와 wizard가 실행 시 실제로 그렇게 "행동한다"는 서로 다른 명제이며, 후자는 어떤 시나리오도 커버하지 않는다. |

## 2. 종합

- `scores`: `{"goal": 2, "adoption": 1, "boundary": 1}`
- `average`: **1.33** (= 4/3)
- 종료조건(`scenario-gate.md` §5-1): 각 축 ≥1점(0점 축 없음) 충족 — **AND** 평균 ≥1.5점 — **미충족** (1.33 < 1.5)
- **verdict: fail**

## 3. gaps

1. **[⑤ 채택 미검증]** R-3(wizard의 manager 비호출)·R-9(라우팅 개정)가 실제 실행 시점에 새 경로를 타는지 확인하는 시나리오가 없다. TS-013·TS-019는 "구형 문구 부재"만 정적으로 확인한다. TS-021·TS-022(SUPERVISOR) 기대 결과에 "manager를 호출하지 않았다"·"skills.sh를 직접 사용했다"는 관찰 항목이 없어, 유일한 동적 실행 지점에서조차 신형 채택이 재확인되지 않는다.
2. **[⑥ 부정 경로 self-confirming 위험]** TS-024·025·026·027·028(RISKY 제외/scan-risk 실패/검색 0건 위임/전건 거부/비프로젝트 안전종료) 5건이 모두 L3 정적 grep이며 R-3·R-4·R-1 AC 문장을 그대로 재확인하는 수준에 머문다. 실제 wizard 실행에서 이 5개 부정 분기 중 어느 것도 지나가지 않아, "문서에 규칙이 쓰여 있음"과 "wizard가 실제로 그렇게 행동함"을 구분하는 시나리오가 부재하다.
3. **[① 참고 — 미달 아님, 개선 여지]** "제안(propose)" 요소의 핵심 전제인 skills.sh 검색 명령 자체가 실제로 후보를 반환하는지에 대한 최소 결정론 스모크 체크(예: 알려진 검색어로 검색 명령을 1회 실행해 결과 ≥1건 확인)가 없다 — TS-013은 문서에 검색 명령이 "명시되어 있는지"만 확인한다. 목표 축 점수(2)에는 영향을 주지 않았으나(TS-021·022·023이 축 정의를 충분히 충족), 재작성 시 저비용으로 보강 가능한 지점이다.

## 4. 재작성 방향 (제안일 뿐 — 반영은 Producer 책임)

- ⑤ 보강안: TS-021 또는 TS-022의 기대 결과에 "인터뷰·검색·설치 전 과정에서 `opal-skill-manager` 호출 0건" 관찰 항목을 추가하거나, 별도 경량 M1 시나리오로 "wizard 절차 실행 로그/트레이스에 manager 참조 호출 0건"을 검증하는 방안 검토.
- ⑥ 보강안: TS-024·025·026·027·028 중 최소 1~2건을 SUPERVISOR(M3) 동적 시나리오로 승격하거나, RISKY 후보/검색 0건 fixture를 별도로 준비해 wizard를 실제로 그 분기까지 실행시켜 결과를 관찰하는 방식으로 전환 검토. 전건 승격이 비용 과다라면 최소 1건(예: TS-025 scan-risk 실행 실패 — 보안 게이트 회피 방지, P1)만이라도 동적 확인으로 전환할 가치가 크다.

---
*본 보고서는 opal-evaluator-agent가 verdict-only로 산출했다. TEST-SCENARIO.md 반영·재작성은 Producer(PM+사용자 페어)의 책임이다 (`scenario-gate.md` §4 [MUST] Producer≠Evaluator).*
