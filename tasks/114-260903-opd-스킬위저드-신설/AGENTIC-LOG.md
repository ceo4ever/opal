# AGENTIC-LOG: opal-skill-wizard 신설 — 프로젝트 적합 스킬 제안 + 프로젝트 스코프 설치

> 모드: agentic | 시작: 2026-09-03 23:19 | 스킬: //opd (opds에서 승격)

## 요약

| 항목 | 건수 |
|------|------|
| 게이트 판단 | 22회 (Pass: 21 / Fail: 1) |
| 3회 초과 Gate | 0건 (Critical: 0 / Normal: 0 / Minor: 0) — 최대 반복은 목표-커버 게이트 2회·PLAN 워커 재시도 4회(529 환경 장애) |
| 오류 발견 | 16건 |
| 수정 지시 | 14건 (반영: 14 / 미반영: 0) |
| PM 의사결정 | 43건 |
| 개선 사항 | 3건 (+ 이월 6건) |
| 에스컬레이션 | 4건 |

## 대행 일지

| # | 시점 | 단계 | 카테고리 | 내용 | 결과 |
|---|------|------|----------|------|------|
| 1 | 2026-09-03 23:19 | TASK | DECISION | Git 사전 점검 결과 작업 트리가 dirty(추적 7건 수정 + 미추적 7건). why: 하네스 §1 Guards는 "커밋/스태시를 제안한 후 진행"을 규정하며 커밋은 사용자 명시 요청 시에만 가능하다. 태스크 105의 미커밋 변경분이 `opal/tools/skill-registry/skill-registry.js`에 있어 EXECUTE 시 동일 파일 충돌 위험이 있으나, TASK 단계는 문서 작성만 수행하므로 충돌하지 않는다 | TASK 단계 진행. 커밋 처리는 EXECUTE 진입 전 캡틴에게 확인하도록 TASK.md §제약 조건에 명시 |
| 2 | 2026-09-03 23:20 | TASK | DECISION | 대화 인터뷰 7건(제안 범위·설치 스코프·분석 방식·착수 시점·설치 경로·registry·보안 검사)을 전부 `[결정]` 태그로 §확정된 설계 방향에 기록하고, PLAN 위임 항목 3건(name 충돌 우선순위·registry 파일 경로·약어)만 §명확화 결과 「미확정」 열에 남겼다. why: `op-task/SKILL.md` §인터뷰 결과 분류가 확정/미확정 2분류를 요구하며, 미확정은 PLAN 워커가 조사 후 결정한다 | 확정 9건 / 미확정 3건으로 분류 완료 |
| 3 | 2026-09-03 23:22 | TASK | ESCALATION | 조기 에스컬레이션 조건 충족 — TASK.md 요구사항 체크박스 11건(R-1~R-11)이 `opal-pilot-dev-short/SKILL.md` §조기 에스컬레이션 기준 「요구사항 항목 >= 8개」를 초과했다. 동 SKILL.md §Agentic 모드 §에스컬레이션 규칙이 "PM이 판단하여 자동 전환하지 않고, 사용자에게 에스컬레이션으로 보고한다"를 규정하므로 자율 판단하지 않았다 | 캡틴에게 Full Task(opd) 전환 여부 보고. PLAN 디스패치 보류 |
| 4 | 2026-09-03 23:28 | TASK | DECISION | 캡틴 승인('가')으로 opds→opd 승격 실행. 폴더 약어 변경(`114-260903-opds-…`→`114-260903-opd-…`)·TASK.md 헤더 `적용 스킬` 필드·AGENTIC-LOG 헤더를 갱신하고 `state-tool init --skill opd --rows-from opal/skills/opal-pilot-dev/references/pipeline.json --force --note`로 파이프라인을 11행→16행 재구성했다. why: `harness/task-process.md` §저장 경로 규칙이 폴더명에 스킬약어를 포함하도록 규정하므로 약어만 바꾸고 폴더명을 두면 정합이 깨진다. 승격 실행 절차(폴더 rename·state 재init)는 `harness/track-routing.md`가 강등만 규정하고 승격 절차를 명문화하지 않아 SSOT 공백 상태다 | 16행 재구성 완료. 승격 절차 SSOT 공백은 개선 후보로 이월 |
| 5 | 2026-09-03 23:28 | TASK | DECISION | opd STEP 1 [MUST] 트랙 강등 판정 수행 — A3(신규 개념: 새 모듈·스키마·외부 의존) 축이 미충족이다. 프로젝트 registry 스키마 신설(R-7)과 프로젝트 스코프 경로 해석(R-6)이 신규 개념 2건에 해당한다. `harness/track-routing.md` §1은 "4축 전건(AND) 충족 시에만 강등한다"를 규정하므로 강등 불발 | `opd` 유지 확정. 강등하지 않음 |
| 6 | 2026-09-03 23:29 | ANALYSIS | DECISION | ANALYSIS 디스패치 전 code-scan 사전 범위 파악에서 하류 의존을 발견했다 — `dashboard/backend/adapters/skill_adapter.py`가 `skill-registry list`를 읽기 전용으로 소비한다(`~/.opal/tools/code-scan/run.sh search "skill_adapter"` 실행 결과). why: 로더(`loadAllSkills()`)를 변경하면 이 소비자에 회귀가 전파될 수 있으나 TASK.md 요구사항에 회귀 표면으로 기재되지 않았다 | 분석 질문 Q4에 이 파일의 `list` 소비 방식 실측을 명시해 디스패치 |
| 7 | 2026-09-03 23:29 | ANALYSIS | GATE | ANALYSIS 워커 디스패치(model: standard). 분석 질문 Q1~Q10을 명시 주입하고, 핵심 제약 9건을 원문 인용으로 전달했다. `advance --task-step analysis.analysis_md` 응답의 `auto_approved: [2]`로 TASK 사용자 확인 행(2행)이 도구 자동 승인되었음을 관측했다(agentic 자동 승인 계약, `opal-harness-agentic.md` §4) | 디스패치 완료. 워커 결과 대기 |
| 8 | 2026-09-03 23:46 | ANALYSIS | ERROR | ANALYSIS.md §7 Q5·§8이 태스크 105 커밋을 `21037f6`으로 인용했으나, 현재 브랜치에서 해당 파일을 변경한 커밋은 `69f5ce1`이다. `git cat-file -t 21037f6`은 통과하지만 `git log --oneline -3 -- opal/tools/skill-registry/skill-registry.js`(스코프: 리포지토리 루트 main)에는 나오지 않는 별 브랜치 동일 메시지 커밋이다. 워커 보고를 그대로 수용하지 않고 PM이 직접 실측해 검출 | 사실 표기 오류 1건 확정. 실질 결론(105 커밋 완료·충돌 없음)은 유효 |
| 9 | 2026-09-03 23:46 | ANALYSIS | ERROR | ANALYSIS.md §확정 입력 판정 표와 TASK.md §확정된 설계 방향이 착수 트랙을 `//opds --agentic`으로 기재한 채였다. 캡틴 승인으로 opd 승격이 이미 반영된 상태(폴더·state 16행)와 불일치하는 stale 기재다. TASK.md §제약 조건의 「105 미커밋」 항목도 같은 성격으로 stale | 문서-실상 불일치 2건 확정 |
| 10 | 2026-09-03 23:46 | ANALYSIS | FIX | ERROR #8·#9 교정 — ANALYSIS.md 커밋 해시를 `69f5ce1`로 고치고 교정 사유·실측 명령·스코프를 괄호로 병기, 착수 트랙 행을 `//opd --agentic (opds에서 승격)`으로 갱신. TASK.md는 착수 트랙 문장에 승격 경위·근거를 추가하고 §제약 조건·§명확화 결과 「의존 사실」 열의 105 항목을 **[해소]**로 갱신했다. 워커 재지시 없이 PM이 직접 교정한 이유: 사실 표기 수준이며 실질 분석 결론에 영향이 없어 `opal-harness-agentic.md` §5 심각도 Minor에 해당 | 교정 완료. 워커 재지시 0회 |
| 11 | 2026-09-03 23:46 | ANALYSIS | GATE | ANALYSIS PM Gate — Pass. 강화 검토 6항목 확인: (1) 산출물 실존·199줄 내용 충실 (2) Q1~Q10 전건 답변, 판정 불능 0건 (3) 확정 입력 판정 9행 전건 판정 (4) §1.1 5열 표·§8 3열 표 존재 (5) 소스코드 원문 블록 0건 — `경로:줄번호` 인용으로만 기재 (6) 기존 테스트 기준선 41 pass/0 fail을 cwd 스코프 명시로 실측(citation-rules §9 (a) 충족). ERROR 2건은 PM 직접 교정으로 해소했고 미승인 폴백 0건 | Pass. `analysis.pm_gate` 행 ✅. PLAN 단계 진입 |
| 12 | 2026-09-03 23:50 | PLAN | DECISION | TEST-SCENARIO Block A 선작성 착수·완료(시나리오 28건). 도출 입력을 TASK.md 3종(목표 문장·요구사항 R-1~R-11·완료기준)으로 한정하고 PLAN.md를 열람하지 않았다. why: `harness/red-first.md` §1.6 (a) "이 3종 밖의 입력을 선작성에 쓰지 않는다 — 특히 PLAN.md를 읽지 않는다"(관점 오염 차단). 착수 판단은 동 §1.6 (f) 표 2행 충족 — 목표(제안·설치가 실제 동작)가 파괴 관점으로 환원되지 않고 PLAN 워커 예상 소요가 선작성보다 길다 | 축 ①②⑤⑥ 커버 완료. ③④축은 `<!-- PENDING-BLOCK-B -->` 마커로 대기 |
| 13 | 2026-09-04 00:00 | PLAN | ERROR | PLAN 워커가 API 529(Overloaded, server_error)로 조기 종료됐다. 산출물 실측 결과 PLAN.md 미생성 — 빈손 종료다(`ls` 실행 결과 태스크 폴더에 PLAN.md 부재). 워커 귀책이 아닌 서버측 일시 장애다 | 워커 비정상 종료 1건 확정 |
| 14 | 2026-09-04 00:00 | FIX | FIX | ERROR #13 대응 — `~/.opal/references/opal-harness.md` §1 자동 루핑 제약 표 「워커 프로세스 비정상 종료(스톨·응답 중 연결 종료) → 최대 재시도 1회(동일 컨텍스트 재개)」에 따라 동일 컨텍스트 재개를 1회 수행했다. 재개 지시에 (1) 읽은 문서 재독 불요 (2) 산출물은 PLAN.md 단일 (3) TEST-SCENARIO.md 읽기·쓰기 금지(작성자 분리 유지) (4) ANALYSIS PM 교정 반영분(커밋 `69f5ce1`·트리 클린·트랙 opd) (5) 재차 끊기면 확정분을 먼저 저장 후 종료를 추가 주입했다 | 재개 1회 소진. 동일 지점 재실패 시 새 컨텍스트로 분할 재배치 |
| 15 | 2026-09-04 00:05 | PLAN | ERROR | 재개된 PLAN 워커가 **동일 지점에서 재실패**했다 — 같은 API 529(Overloaded), 같은 모델(`claude-opus-5`). PLAN.md 여전히 미생성(`ls PLAN.md` 실행 결과 파일 없음). `~/.opal/references/opal-harness.md` §1 보충 "동일 컨텍스트 재개가 같은 지점에서 재실패하면 재시도를 즉시 중단한다"에 해당 | 재시도 중단. 재시도 예산 소진 |
| 16 | 2026-09-04 00:05 | PLAN | DECISION | 분할 재배치 적용 가능성을 판정한 결과 **부적합**이다. `pm/dispatch-process.md` §Step 6 항목 5는 산출 파일 수를 분할 기준으로 삼고 "동일 파일을 2개 이상 Step이 변경하면 분할하지 않고 같은 디스패치에 묶어 순차 편집한다"를 규정한다 — PLAN 단계의 산출은 `PLAN.md` 단일 파일이므로 분할이 오히려 규정 위반이다. 또한 529는 컨텍스트 크기가 아닌 서버측 용량 사유라 분할로 해소되지 않는다 | 분할 재배치 배제 |
| 17 | 2026-09-04 00:05 | PLAN | ESCALATION | 남은 선택지가 전부 규범 이탈이라 캡틴에게 에스컬레이션한다 — (가) 모델 강등(advanced→standard)은 `opal-pilot-dev/SKILL.md` STEP 3 "**model**: advanced" 규범 이탈이고 PLAN 품질(복잡 추론)에 직접 영향한다, (나) 동일 모델 재시도는 이미 소진한 재시도 예산의 초과다, (다) PM 직접 수행은 `opal-harness.md` §1 디스패치 의무 원칙("워커 디스패치로 정의된 단계를 PM이 직접 실행하지 않는다") 위반으로 배제한다. `opal-harness-agentic.md` §6 "하네스 Guards 위반 가능성" + "판단이 모호한 경우 확신이 없으면 에스컬레이션이 기본"에 해당하며, 동 §3 폴백 승인 의무상 PM 단독 승인으로 처리하지 않는다 | 캡틴 결정 대기. PLAN 디스패치 보류 |
| 18 | 2026-09-04 00:00 | PLAN | DECISION | 캡틴이 「승인」으로 응답 — 제시한 3후보 중 권고안 (가) 동일 모델(advanced) 재시도로 해석해 처리했다. why: `▶️` 블록에서 (가)를 권고로 명시했고 (나)·(다)는 각각 규범 이탈·중단이므로 무표기 승인은 권고안 수락으로 읽는 것이 합리적이다. 재시도 예산 초과는 캡틴 승인으로 덮었고 `state-tool status --set in_progress --note`로 근거를 state에 기록했다 | `blocked` → `in_progress` 복귀 |
| 19 | 2026-09-04 00:01 | PLAN | FIX | 재디스패치 시 **증분 저장 의무**를 최우선 제약으로 프롬프트 선두에 배치했다 — "PLAN.md를 한 번에 완성하려 하지 마라. 골격을 먼저 파일로 저장하고 섹션 확정마다 즉시 반영하라." why: 직전 2회 실패의 실질 피해는 529 자체가 아니라 **빈손 종료**였다(PLAN.md 미생성 2회). 증분 저장이 있으면 같은 오류가 재발해도 확정분이 남아 잔여만 재배치할 수 있다. 근거: `pm/dispatch-process.md` §워커 컨텍스트 주입 템플릿 §핵심 제약 전 워커 공통 고정 항목 「증분 저장」 | 새 컨텍스트로 재디스패치 완료 (model: advanced 유지) |
| 20 | 2026-09-04 00:01 | PLAN | DECISION | 재디스패치 프롬프트에 `TEST-SCENARIO.md` 읽기·쓰기 금지를 명문 주입했다. why: PM이 Block A를 이미 선작성했으므로 PLAN 워커가 이를 읽으면 작성자 분리가 무너진다 — `test-scenario-guide.md` §목적 1 "self-confirming 방지를 위해 PLAN 작성자(opal-plan-agent)와 다른 작성자가 수행" | 작성자 분리 유지 |
| 21 | 2026-09-04 00:10 | PLAN | ERROR | 3차 시도도 동일 실패 — API 529, 동일 모델(`claude-opus-5`), PLAN.md 미생성(`ls PLAN.md` 실행 결과 파일 없음). 증분 저장 지시가 실행 기회조차 얻지 못했다(워커가 첫 API 호출 단계에서 종료). 3회 연속 동일 지점·동일 사유 실패는 일시 장애가 아니라 **해당 모델의 가용성 차단**으로 판정한다 | 환경 차단 확정. `advanced` 경로로는 진행 불가 |
| 22 | 2026-09-04 00:10 | PLAN | DECISION | 관측 대조 — PM 자신은 `claude-opus-5[1m]` 변형에서 정상 동작 중이고 워커에 배정되는 `claude-opus-5`만 529를 반환한다. 따라서 장애는 OPAL 설정이나 프롬프트 문제가 아니라 특정 모델 용량 사유다 | 원인 국소화 완료. 프롬프트·설정 수정으로 해소 불가 |
| 23 | 2026-09-04 00:10 | PLAN | DECISION | `subagent_type: fork` 경로(부모 모델 `claude-opus-5[1m]` 상속으로 용량 우회 가능)를 검토했으나 **배제**했다. why: fork는 PM의 전체 대화 컨텍스트를 상속하며 그 안에 PM이 선작성한 TEST-SCENARIO Block A 시나리오 28건이 포함되어 있다 — `test-scenario-guide.md` §목적 1 작성자 분리("PLAN 작성자와 다른 작성자가 수행")를 무너뜨려 self-confirming 구조가 된다. 용량 우회 이득보다 검증 2원화 훼손 손실이 크다 | fork 경로 배제 |
| 24 | 2026-09-04 00:10 | PLAN | ESCALATION | 캡틴 승인(#18)은 **1회 재시도**에 대한 것이었고 3차 연속 실패는 새 정보다. PM이 추가 재시도를 자율 반복하지 않고 재에스컬레이션한다 — `opal-harness-agentic.md` §6 "올려야 할 것을 안 올리는 것도 PM 실패" + §3 폴백 승인 의무. 남은 선택지는 모델 강등(규범 이탈) 또는 대기 2택으로 좁혀졌다 | 캡틴 결정 대기. 산출물 3종 보존 |
| 25 | 2026-09-04 07:23 | PLAN | DECISION | PM이 직전에 낸 「모델 강등」 권고를 캡틴 질문("강등하려는 이유가 뭐지?")을 받고 **정정**했다. 강등의 실질 근거는 「지금 진행 가능한 유일 경로」 하나였고 이는 품질 근거가 아니라 정체 회피였다. 6개 결정 중 `get` 계약 변경·루트 판별은 트레이드오프 판단이라 모델 역량이 결과에 영향하며, 규범 `model: advanced`는 이런 단계를 위해 지정된 것이다. 후단 게이트(PM Gate·목표-커버 게이트)를 강등 정당화로 든 것도 순서 오류다 — 게이트는 미달을 잡는 장치이지 미달을 감수할 근거가 아니다 | 강등 미실시. 규범 유지 확정 |
| 26 | 2026-09-04 07:23 | PLAN | DECISION | 캡틴 질문("effort medium이 문제가 될 수 있나?")에 대한 판정 — 529와 effort는 무관하다. 동일 세션·동일 effort 설정 아래에서 워커 요청(`claude-opus-5`)만 거부되고 PM 요청(`claude-opus-5[1m]`)은 통과한 대조 관측이 반증이다. 529는 요청 수용 단계의 용량 거부이고 effort는 수용 후 추론 토큰 예산이라 성질도 다르다. 단 effort는 PM 자신이 수행하는 PM Gate 직접 검증·Block B 보강의 품질에는 영향하므로 `/effort high` 상향을 캡틴에게 권고했다 | 원인 판정 완료. effort는 529 변수 아님 |
| 27 | 2026-09-04 07:23 | PLAN | FIX | 캡틴 승인으로 `advanced` 4차 재디스패치 실행. 직전 실패(00:05)로부터 7시간 이상 경과해 용량 회복 가능성이 높은 조건이다(`state-tool status` 응답 timestamp 대조: `2026-09-04 00:05:56` → `2026-09-04 07:23:54`). 증분 저장 의무·TEST-SCENARIO 접근 금지·6개 결정 확정 요구를 동일하게 주입했다 | `blocked` → `in_progress`. 4차 디스패치 완료 |
| 28 | 2026-09-04 07:37 | PLAN | GATE | PLAN 워커 4차 시도 성공 — PLAN.md 954줄 산출. 소요 11분. PM Gate 강화 검토를 위해 **전문을 3회 분할 Read로 완독**했다(1-330 / 330-602 / 600-954) — 부분 열람 상태로 게이트를 판정하지 않았다(`opal-harness-agentic.md` §4 「산출물 직접 검증 의무」) | 완독 후 판정 착수 |
| 29 | 2026-09-04 07:37 | PLAN | ERROR | PLAN.md 내부 자기모순 1건 검출 — §3.1.2(a)의 `findProjectRoot()` 탐색 마커는 `.opal/skills-registry.json`(파일)인데, §3.3.2 §진입 훅은 "`findProjectRoot` 규약과 **동일한 마커**: `{project}/.opal/`"(디렉토리)라고 기재해 실제로는 서로 다른 마커를 가리킨다. 파생 결함 2건 — (i) registry 파일을 마커로 삼으면 최초 설치 전 루트를 찾지 못하는 **순환**이 생긴다(§3.1.4가 "부재가 정상 상태"로 규정한 것과 충돌) (ii) wizard 설치 루트와 로더 해석 루트가 갈릴 수 있다 | 설계 결함 확정. EXECUTE 그대로 진입 시 실제 버그화 |
| 30 | 2026-09-04 07:37 | PLAN | FIX | ERROR #29 교정 — 탐색 마커를 **`.opal/` 디렉토리로 통일**하고 §3.1.2(a)에 [MUST] 근거를, §진입 훅에 통일 사실을 명기했다. 선택 근거: `.opal/` 디렉토리는 OPAL 프로젝트 정의 자체와 정합한다([MUST] `~/.opal/AGENT.md` §부트스트랩 Phase B가 프로젝트 판별에 `.opal/`을 이미 사용). 회귀 검토 — `~/.opal/`이 항상 존재하므로 홈 경계 정지(종료 조건 ①)가 더 결정적이 되나 이미 [MUST]로 규정되어 있고(H-4/TS-008), registry 부재 시 `loadProjectRegistry()`가 `null`을 반환하므로 TS-002도 그대로 성립한다. 워커 재지시 대신 PM 직접 교정을 택한 이유: 교정 범위가 마커 정의 1곳 + 관련 서술 1곳으로 좁고, 529 재발 위험이 재지시 비용을 크게 만든다 | 교정 완료 (954→956줄). 워커 재지시 0회 |
| 31 | 2026-09-04 07:37 | PLAN | ERROR | 선작성 초안 대비 PLAN 설계 갭 1건 — PM이 Block A에서 도출한 S-A27(비프로젝트 디렉토리에서 wizard 호출 시 임의 경로 설치 방지)에 대응하는 설계가 PLAN에 없다. §진입 훅이 프로젝트 루트 확정만 언급하고 확정 실패(`findProjectRoot` → `null`) 시 동작을 규정하지 않는다. `opal-pilot-dev/SKILL.md` STEP 3이 지시한 「선작성 초안과 PLAN 설계의 불일치 = 조기 경보」에 해당 | 조기 경보 1건. Block B 보강에서 처리 |
| 32 | 2026-09-04 07:37 | PLAN | GATE | PLAN PM Gate — **Pass**. 체크리스트 4항 확인: (1) TASK.md 요구사항 R-1~R-11 전건이 F-001~F-006에 매핑, 미할당 0건(§1.2) (2) §4.2 실행 체크리스트 10 Step 각각에 소속 F-ID·영역·agent·파일·작업 내용·완료 기준·테스트·의존 전건 기재 (3) §리스크 가설 표 H-1~H-9 작성 + 검증 계층·시나리오 후보 포함 (4) 미확정 이월 0건, 후속 이월 3건은 근거 인용과 함께 §9에 등록. 추가 확인 — ANALYSIS 이관 6건 전건 확정(DEC-1~6) + DEC-7(약어 `osw`) 추가 확정, RED-first 하이브리드 판정, 회귀 기준선 41 pass/0 fail을 Step 5 완료 조건으로 명시. ERROR 2건은 #30 교정·#31 이월로 처리했고 미승인 폴백 0건 | Pass. `plan.pm_gate` ✅. TEST-SCENARIO 단계 진입 |
| 33 | 2026-09-04 07:46 | TEST-SCENARIO | DECISION | Block B 보강 완료. 보강 완료 판정 3조건을 **산문이 아닌 스크립트로 검사**했다 — 마커 잔존 0건(인라인 백틱 구간 `re.sub` 제거 후 검사로 메타-순환 오탐 회피, `test-scenario-guide.md` §Block B 조건1 지시) / H-1~H-9 전건 전재 / 매핑 표 19행 전부 가설·계층 기재(공란 0). 보강을 additive-only로 끝내지 않고 초안 17건을 TS-NNN으로 흡수·삭제, 12건은 유지·채번했다(동 §Block B [MUST]) | 3조건 충족. 게이트 호출 자격 확보 |
| 34 | 2026-09-04 07:46 | TEST-SCENARIO | DECISION | `test_scenario.test_scenario_md` 행을 `--worker-duration-unknown`으로 재마킹했다. why: 도구가 `worker_duration_missing` 경고를 반환했으나 이 행은 워커 미디스패치(PM+캡틴 페어 직접 수행)이므로 소요가 존재하지 않는다. `opal-harness.md` §3은 "생략(침묵)은 예외가 아니다 — 알 수 없으면 `--worker-duration-unknown`으로 미측정임을 밝힌다"를 규정한다 | 경고 해소. 침묵 통과 회피 |
| 35 | 2026-09-04 07:50 | TEST-SCENARIO | GATE | 목표-커버 게이트 iteration 1 — Step 3 결정론 `scenario-coverage-check` **exit 0**(`all_covered:true`, R 11 / F 6 / H 9 / 시나리오 29, 미커버 0). Step 4 판단축은 `opal-evaluator-agent`에 디스패치했다(Producer≠Evaluator — PM이 작성자이므로 자가 채점 금지, `scenario-gate.md` §4 [MUST]) | 결정론 축 ②③④ 통과 |
| 36 | 2026-09-04 07:54 | TEST-SCENARIO | ERROR | Evaluator iteration 1 판정 `verdict: fail` — `{goal:2, adoption:1, boundary:1}` 평균 1.33 < 임계 1.5. 지적 2건: (a) ⑤ 신형 채택의 실행-시간 검증 전무 — TS-013·019는 구형 잔존0만 정적 확인 (b) ⑥ wizard 부정경로 5건(TS-024~028)이 전부 정적 grep이라 AC 재확인 수준의 self-confirming이며, 보안 게이트(H-8/P1)조차 정적 확인에 그친다. **PM(작성자)이 놓친 결함을 독립 채점자가 검출한 사례** | 재작성 확정. 종료조건 판정 — 수렴 X / 반복상한 1<3 / 무진전 이력 없음 → `verdict: rewrite` |
| 37 | 2026-09-04 08:00 | TEST-SCENARIO | FIX | ERROR #36 반영 재작성. Evaluator 보강안은 M3(캡틴 수동) 승격이었으나 Producer는 **부작용 관측(side-effect observation)** 방식을 택했다 — wizard 행동은 파일시스템 변화로 나타나므로 「무엇이 생겼는가/생기지 않았는가」 검사로 M1 자동화가 성립하고, M3보다 재현 가능하며 회귀 감지가 된다. 신설 8건 — TS-030(project 스코프 기록=신형 채택) / **TS-031(설치 전후 전역 무변화=구형 manager 경로 미사용의 실행 증거)** / TS-032·033(scan-risk 실판정·오탐 부재) / TS-034~036(설치 부작용 부재로 부정 분기 관측) / TS-037(검색 스모크, 네트워크 미가용 시 Skip). TS-024~028은 삭제하지 않고 「정적 축」으로 격하하고 실행 축과의 짝 대응표를 명시했다. 사전 스냅샷 취득을 [MUST]로 못박아 「애초에 생길 수 없었던 경우」와의 혼동을 차단했다 | 시나리오 29→37건. 재채점 디스패치(iteration 2) |
| 38 | 2026-09-04 07:59 | TEST-SCENARIO | GATE | 목표-커버 게이트 iteration 2 — **PASS**. Step 3 결정론 exit 0(`all_covered:true`, 시나리오 29→37) + Step 4 Evaluator `verdict: pass` `{goal:2, adoption:2, boundary:1}` 평균 1.67 ≥ 1.5. ⑤가 1→2로 상승한 근거는 TS-030(신형 채택)·TS-031(전역 무변화)이 **같은 실행에 대한 긍정+부정 짝**이라 TS-030이 설치 완료를 양성 확정함으로써 TS-031의 무변화가 우연일 가능성이 배제된다는 것. Producer의 부작용 관측 방법론 자체도 타당 판정. 두 증거로만 mark했고 산문 판단으로 mark하지 않았다(`scenario-gate.md` §6 tool-gated) | Pass. `test_scenario.scenario_gate` ✅ |
| 39 | 2026-09-04 07:59 | TEST-SCENARIO | IMPROVE | 게이트는 통과했으나 Evaluator가 ⑥에 남긴 잔여 지적(TS-034~036이 "파일 생성 0건"만 단정해 「정상 차단」과 「조기 실패로 우연히 없음」이 구분되지 않는다)을 **재게이트 없이 반영**했다. 기대 결과를 **(a) exit 0 + (b) 사유 신호 존재 + (c) 부작용 부재 3조건 AND**로 강화하고 (b)의 구체 신호를 시나리오별로 특정했다(TS-034=RISKY 판정 출력 / TS-035=creator 위임 페이로드 최소 2필드 / TS-036=대상 부재 안내). why: 통과를 위한 조정이 아니라 품질 보강이므로 iteration을 올리지 않았다. 근거는 같은 문서 TS-002·003이 이미 exit 0을 명시하는 선례 | 반영 완료. iteration 미증가 |
| 40 | 2026-09-04 07:59 | EXECUTE | DECISION | EXECUTE 배치 구성 — Step 1(문서, `opal-task-agent`)과 Step 2+3(RED 테스트, `opal-test-agent` mode:red)을 **병렬 2배치**로 디스패치했다. Step 2와 Step 3을 한 디스패치에 묶은 근거: `pm/dispatch-process.md` §Step 6 항목 5 "동일 파일을 2개 이상 Step이 변경하면 분할하지 않고 같은 디스패치에 묶어 순차 편집한다(동시 편집 시 후행 저장이 선행 편집을 덮어쓰는 충돌 방지)" — 두 Step 모두 `test-project-registry.js` 단일 파일을 편집한다. Step 1과는 파일이 겹치지 않아 병렬 안전 | 2배치 병렬 디스패치 |
| 41 | 2026-09-04 07:59 | EXECUTE | DECISION | 두 워커 프롬프트에 **담당 Step 외 파일 수정 금지**를 명시 주입했다 — Step 1 워커는 `docs/ARCHITECTURE.md` 1개, RED 워커는 `test-project-registry.js` 1개로 한정하고 `skill-registry.js` 본체 수정을 절대 금지했다. why: 구현(Step 4)은 별도 워커 담당이며, RED 워커가 본체를 고치면 RED 증거가 무의미해지고 생성자≠구현자 분리(`red-first.md` §1.5 공통 불변 ②)가 깨진다. 추가로 Step 1 워커에게 **변경이력 행 추가 금지**를 지시했다 — Step 10(PM 직접)이 4개 문서에 일괄 추가하므로 중복 방지 | Scope 경계 주입 완료 |
| 42 | 2026-09-04 08:05 | EXECUTE | GATE | Step 1(ARCHITECTURE 스키마 반영) 완료 검토 — **Pass**. 워커 보고를 수용하지 않고 `git diff docs/ARCHITECTURE.md` 전문을 직접 대조했다(`opal-harness-agentic.md` §4 산출물 직접 검증 의무). 실측: +23/-1, 단일 파일. (a) 기존 「레지스트리 (이원)」 행의 카탈로그·사용자 등록분 서술이 **삭제·축약 없이 보존**되고 프로젝트 스코프만 append됨 (b) 12필드 표가 PLAN §3.4.2와 필드·필수·타입 전건 일치 (c) TS-016 5항(`source_repo`·`commit_sha`·`license`·`trust`·`scanned_at`) 전건 포함 (d) `[MUST] paths 미보유` 규칙 + 담당 주체 경계(osw/manager) 기재 (e) **변경이력 행 미추가** — Step 10 일괄 처리 지시 준수 (f) 다른 절 무변경 | Pass. 워커 재지시 0회 |
| 43 | 2026-09-04 08:05 | EXECUTE | DECISION | 워커가 지시한 단일 파일(`docs/ARCHITECTURE.md`) 외에 `PLAN.md` Step 1 체크박스를 `[x]`로 갱신했다. **폴백으로 판정하지 않고 사후 승인**한다 — `op-dev-execute` 스킬 자체가 체크리스트 갱신을 절차로 규정하므로 PM의 「1개 파일」 지시가 스킬 절차와 충돌한 것이며 워커의 임의 이탈이 아니다. `opal-harness-agentic.md` §3 폴백 승인 의무에 따라 승인 사실을 기록한다. 향후 디스패치에서는 「담당 Step 체크박스 갱신은 허용」을 명시해 지시 충돌을 없앤다 | 사후 승인. 미승인 폴백 0건 유지 |
| 44 | 2026-09-04 08:12 | EXECUTE | GATE | Step 2+3(RED) 완료 검토 — **Pass**. 워커 보고를 재현 검증했다: (a) `git status` 실측 — `skill-registry.js` 본체 **무변경**(`git diff --stat` 출력 0), 신규 파일 1개만 미추적 (b) `node opal/tools/skill-registry/tests/test-project-registry.js` **재실행 → exit 1**, 9 tests / 3 pass / 6 fail 재현 (c) `state-tool verify --red-check` → `red_evidence_missing: pass`. RED 증거 확정 | Pass. GREEN 진입 자격 확보 |
| 45 | 2026-09-04 08:12 | EXECUTE | DECISION | RED 3건 통과(TS-002 무회귀·TS-003 파손내성·TS-008 홈경계)를 **RED 미달로 보지 않았다**. why: 이 3건은 성질상 「지금도 통과하고 구현 후에도 통과해야」 하는 무회귀·보안 경계 가드다. 특히 TS-008은 구현 후 마커가 `.opal/` 디렉토리가 되면서 `~/.opal/`이 항상 존재하게 되어 **그때부터 진짜 방어선**이 된다(PM 마커 교정 #30의 부수 효과). 워커도 이를 정직하게 보고했다 | 3건 자명 통과 승인 |
| 46 | 2026-09-04 08:12 | EXECUTE | DECISION | 워커가 자진 보고한 테스트 보정 2건(TS-002 cwd 문자열 정규화 / fixture main 스킬에 실 SKILL.md 본문 부여)을 **약화가 아닌 정당한 보정으로 판정**했다. `normalizeCwd()` 구현을 직접 읽어 확인 — `JSON.stringify` 후 cwd 절대경로 문자열만 `<CWD>`로 치환하고 `deepStrictEqual`하므로, 프로젝트 항목 혼입 같은 **진짜 회귀는 그대로 검출**된다. 두 번째는 기존 `validate()`의 dangling path 검사가 무관한 오탐을 내는 것을 제거한 것 | 승인. 검증 강도 유지 확인 |
| 47 | 2026-09-04 08:12 | EXECUTE | DECISION | Step 4(GREEN) 디스패치 — 프롬프트에 **테스트 파일 수정 절대 금지 + 잘못됐다고 판단되면 고치지 말고 블로커 보고**를 명시했다. why: GREEN 워커가 테스트를 고쳐 통과시키면 RED-first 전체가 무의미해진다. 아울러 회귀 기준선(41 pass/0 fail, 4파일 개별 실행, cwd 리포지토리 루트)을 완료 기준 2번으로 못박고 fail 1건 시 중단·보고를 지시했다 | Step 4 디스패치 완료 |
| 48 | 2026-09-04 08:25 | EXECUTE | ERROR | Step 4(GREEN) 워커가 `status: blocked` 반환 — 9 tests / 7 pass / **2 fail**(TS-004·TS-009). 워커 주장은 "구현 결함이 아니라 macOS 심볼릭 링크 환경 아티팩트". **주장을 수용하지 않고 PM이 직접 재현 검증**했다: `readlink /var` → `private/var` / `os.tmpdir()` → `/var/folders/.../T` / 부모가 `spawnSync`에 넘긴 cwd와 자식의 `process.cwd()`가 불일치하고 `realpathSync` 후 일치. 실패 diff도 **오직 `/private` 접두사 유무**뿐이며 두 경로는 물리적으로 동일 파일이다 | 워커 주장 사실 확인. 구현 결함 아님 |
| 49 | 2026-09-04 08:25 | EXECUTE | GATE | Step 4 구현 품질 검토 — **Pass**. `git diff --stat` 실측 **+100/-2**, 단일 파일. 삭제 2줄은 `getCommand()` 반환문뿐이며 이는 DEC-5가 additive 확장을 위해 **명시적으로 허용한 유일한 수정점**이다. `loadAllSkills()` 기존 main/community/user 3블록은 `-` 라인 **0건** — additive 한정 준수 확인(TASK.md §제약). 회귀 기준선 41 pass/0 fail 유지 보고, CWE-22 방어는 `resolveProjectSkillPath()` 내 프로젝트 루트 하위 검증으로 구현 | Pass. additive 계약 준수 |
| 50 | 2026-09-04 08:25 | EXECUTE | DECISION | 워커가 PLAN에 없던 수정 1건을 자진 추가했다 — `findProjectRoot()`에서 `os.homedir()`를 `fs.realpathSync`로 정규화(`skill-registry.js:143`). **폴백 승인**한다(`opal-harness-agentic.md` §3 폴백 승인 의무). 근거: `os.homedir()`는 심볼릭 링크를 해석하지 않는데 `process.cwd()`는 해석하므로, 정규화 없이는 두 값이 같은 디렉토리를 가리켜도 문자열이 달라 **홈 경계 정지(H-4/P0)가 무력화**된다. 이는 PLAN §3.1.2(a) 종료조건 ①의 실효 성립 요건이므로 사변적 추가가 아니라 필수 보정이다 | 승인. 미승인 폴백 0건 유지 |
| 51 | 2026-09-04 08:25 | EXECUTE | FIX | TS-004·TS-009 보정을 **GREEN 워커가 아닌 테스트 작성자(`opal-test-agent`)에게 디스패치**했다. why: `red-first.md` §1.5 공통 불변 ② 작성자≠구현자 — 구현 워커가 자기 구현을 통과시키려 테스트를 고치면 RED-first가 무의미해진다. 보정 내용은 **양측 `realpathSync` 정규화 후 여전히 `strictEqual` 유지**이며, `assert.ok(existsSync)`로의 완화를 명시 금지했다(그건 아무 존재 파일이나 통과시킨다). 같은 파일 TS-002가 이미 `normalizeCwd()`로 동일 철학을 쓰고 있어 정책 통일이기도 하다. 추가로 **음성 통제 실험**(기대 경로를 틀린 값으로 바꿔도 여전히 실패하는지 1회 확인 후 원복)을 완료 기준에 넣어 약화 여부를 스스로 증명하게 했다 | 보정 디스패치. 검증 강도 유지 장치 부착 |
| 52 | 2026-09-04 08:32 | EXECUTE | GATE | TS-004·009 보정 완료 검토 — **Pass**. PM이 전건 재현: 신규 테스트 **exit 0, 9 tests / 9 pass / 0 fail** / 회귀 4파일 개별 실행 **11+5+9+16 = 41 pass / 0 fail**(cwd 리포지토리 루트) / 구현 본체 `git diff --stat` **+100/-2 불변**(보정이 구현을 건드리지 않음 증명). 워커가 수행한 **음성 통제 실험**(기대 경로에 `-NEGATIVE-CONTROL-114` 부착 → 8 pass/1 fail exit 1로 정확히 실패, 이후 원복 IDENTICAL 확인)이 정규화가 검증을 무력화하지 않음을 증명했다 | Pass. Step 4·5 완료 |
| 53 | 2026-09-04 08:32 | EXECUTE | DECISION | Step 5(회귀 기준선 대조)를 **별도 워커로 재디스패치하지 않았다**. why: `opal-harness.md` §1 디스패치 의무 원칙은 워커 단계를 PM이 **대체 실행**하는 것을 금지하는데, Step 5의 실행은 이미 워커 2명(GREEN 워커·보정 워커)이 각각 수행해 41 pass/0 fail을 보고했고 PM은 그 위에 독립 재현을 얹었을 뿐이다. 동일 명령을 세 번째로 돌리는 디스패치는 형식 충족일 뿐 새 증거를 만들지 않는다 | Step 5 워커 실행 증거로 충족 판정 |
| 54 | 2026-09-04 08:32 | EXECUTE | DECISION | Step 6(wizard SKILL.md — 태스크 핵심 산출물) 디스패치. model `advanced` 배정 — 2모드 절차·보안 게이트 순서·위임 계약을 한 문서에 정합시키는 설계 밀도가 높다. 프롬프트에 **TS-011~014·016·024~028·034~036 정적 검사 조건을 판정 문장 그대로 주입**하고, 작성 후 `grep` 자가 점검 6항(줄번호 포함 보고)을 완료 조건에 넣었다. why: 이 문서의 합격 여부는 전부 정적 검사로 판정되므로, 판정 기준을 작성 시점에 알려주면 사후 재지시 루프를 줄인다 | Step 6 디스패치 |
| 55 | 2026-09-04 08:42 | EXECUTE | GATE | Step 6(wizard SKILL.md, 319줄) 검토 — **Pass**. 워커 자가 점검을 재현하지 않고 **PM이 독립 grep으로 재검사**했다: (a) 절 구조가 PLAN §3.3.2 목차 표와 **1:1 일치**(진입 훅+비프로젝트 규칙 → §0~§9 → 변경이력, 누락·추가·순서변경 0) (b) `opal-skill-manager` 등장 7건 전부 참조·경계 서술이며 **호출 지시 0건** — L45 "안내일 뿐이며 이 스킬이 대신 실행하지 않는다", L144 "절차 실행을 다른 스킬에 넘기지 않는다"가 경계를 명문화 (c) `scan-risk` 게이트 §4(L146)가 복사 실행 §5.3(L200)보다 **앞선 위치**(TS-014 충족) (d) 3단 순서 L176 §5 제안(L180)→승인(L194)→설치(L200) (e) §8 스키마 5항 전건 (f) 비프로젝트 진입 규칙 L40~46에 침묵 종료 금지·파일 생성 0건 명시 (g) 변경이력 표는 v1.0 자리만 두고 날짜 미기재 — Step 10 일괄 지시 준수 | Pass. 핵심 산출물 확보 |
| 56 | 2026-09-04 08:42 | EXECUTE | DECISION | 워커 보고의 경미한 오기 2건을 확인했으나 **ERROR로 올리지 않았다** — manager 등장을 "6건"으로 보고했으나 실측 7건이고 줄번호가 1씩 밀렸다(L23→24 등). why: 실질 판정(전건 참조·호출 지시 0건)이 정확하고 어떤 결정도 바꾸지 않는다. 계수 오차를 ERROR로 올리면 일지가 신호 대 잡음비를 잃는다 | 관찰 기록만. 재지시 없음 |
| 57 | 2026-09-04 08:42 | EXECUTE | DECISION | Step 7·8·9를 **3배치 병렬 디스패치**했다 — 각각 `tests/test-project-registry.js` / `harness/skill-commands.md` / `opal-skills-registry.json`으로 **파일이 전혀 겹치지 않아** 동시 편집 충돌이 없다(`pm/dispatch-process.md` §Step 6 항목 5 역판정). 세 워커 모두에게 **변경이력 행 추가 금지**(Step 10 일괄)를 주입했고, Step 9에는 `domain` 값을 **추측 금지·실측 확인**으로 못박았다(PLAN이 EXECUTE 실측 확인 사항으로 이관한 유일 항목) | 3배치 병렬 디스패치 |
| 58 | 2026-09-04 08:42 | EXECUTE | IMPROVE | Step 7 완료 기준에 **음성 통제**를 넣었다 — 필수 필드 1개를 누락시킨 fixture에서 `validate`가 실제로 error를 내는지 확인하고, **error가 안 나면 그 사실 자체를 블로커로 보고**하도록 지시했다. why: TS-015는 "error 0건"을 단정하는 시나리오라, `validate`가 애초에 아무것도 검사하지 않아도 통과한다 — 통과의 의미를 보장하려면 반대 방향 실험이 필요하다. Step 4·보정 단계에서 음성 통제가 실효를 보인 패턴을 확산 적용했다 | 검증 의미 보장 장치 확산 |
| 59 | 2026-09-04 08:52 | EXECUTE | GATE | Step 8(라우팅 3중 분기) 검토 — **Pass**. `git diff` 실측 +9/-3 단일 파일. `opal-skill-manager` 잔존 2건이 모두 「그 외 community」 분기 문맥 안이라 **단독 지목 0건**(R-9 AC 충족), 교체 대상 2곳 모두 반영, `installed`·`ambiguous` 기존 의미 서술 보존, 평가 순서(ambiguous→project→community) 명시 | Pass |
| 60 | 2026-09-04 08:52 | EXECUTE | FIX | Step 8 산출물의 미완성 문장 1건을 PM이 직접 교정 — 「…project 분기에서만 반환되며」로 접속어미에서 끊겨 있었다. 「…반환된다(main·community 분기에는 없으므로 이 조건이 자연히 거짓이 된다)」로 보완. why: 전 pilot이 공유 소비하는 프레임워크 SSOT 문서에서 문장이 끊기면 해석 여지가 생긴다 | 교정 완료 |
| 61 | 2026-09-04 08:55 | EXECUTE | ERROR | Step 7이 **PLAN 설계 갭**을 검출했다 — `validate()`가 `isCommunitySkill()` false인 스킬을 전부 main으로 간주해 `paths` 배열을 필수 검사하는데(`skill-registry.js:563-568`), PLAN §3.4.2는 project 스킬에 **`paths`를 두지 않는다**고 확정했다. 결과적으로 스키마를 100% 준수하는 project registry도 `missing "paths" array`로 exit 1이 되어 **R-7 AC(error 0건)가 원천 성립 불가**다. PM 실측 확인: `grep isProjectSkill` → 정의(`:117`)와 `matchCommand`(`:379`) 2곳뿐, `validate()`에 분기 없음. PLAN §3.1.2 (a)~(f) 6건에 이 분기가 누락됐고 ANALYSIS Q3의 "validate는 기존 한계 상속·신규 이슈 아님" 판정도 이 점에서 틀렸다 | 설계 갭 확정. 테스트가 구현 갭을 정확히 검출 |
| 62 | 2026-09-04 08:55 | EXECUTE | DECISION | 워커가 **구현 본체를 고치지 않고 블로커로 반환**한 것을 올바른 처신으로 판정했다 — 테스트 담당 워커가 구현을 고쳤다면 작성자≠구현자 분리가 무너진다. 수정은 `opal-task-agent`(구현 담당)에 별도 디스패치했고, 범위를 **`isProjectSkill` 분기로 `paths` 검사만 건너뛰기** 1건으로 한정했다. project 항목의 다른 필수 필드 검증 추가는 **명시 금지** — 어떤 AC도 요구하지 않으며 `~/.opal/PRINCIPLES.md` §2 Simplicity First에 저촉된다(별건 이월) | 최소 수정 디스패치 |
| 63 | 2026-09-04 08:56 | EXECUTE | ERROR | Step 9 등재 후 `validate` 오류 2건 발견. **선존 여부를 `git stash`로 직접 실측**: 등재 **전** 오류 2건 = `op-scenario-gate: unregistered` + `opal-skill-wizard: unregistered`(Step 6이 폴더만 만든 상태) / 등재 **후** 2건 = `opal-skill-wizard: dangling`(등재됐으나 미배포) + `op-scenario-gate: unregistered`. 즉 **`op-scenario-gate` 오류는 114 착수 이전부터 존재하던 무관 결함**이며, 이로써 **R-10 AC 「validate error 0건」은 태스크 착수 시점부터 달성 불가능한 기준이었다** | AC 실현 불가 확정. 캡틴 에스컬레이션 대상 |
| 64 | 2026-09-04 08:58 | EXECUTE | GATE | `validate()` project 분기 수정 검토 — **Pass**. PM 재현: 신규 테스트 **11 tests / 11 pass / 0 fail exit 0**(TS-015·029 통과), 회귀 **41 pass / 0 fail**(`test-validate.js` 5 pass 유지 — 같은 함수를 건드렸으므로 최대 위험 지점이었다), `git diff --numstat` **+102/-2**로 직전 +100/-2 대비 정확히 **+2줄**. 추가 코드는 빈 `else if (isProjectSkill(skill)) {}` 분기 1개로 최소 변경이며 community·main 분기 무수정 | Pass. 최소 수정 확인 |
| 65 | 2026-09-04 08:58 | EXECUTE | DECISION | 워커의 **음성 통제 실험**을 승인 근거로 삼았다 — project 항목에서 `name`을 제거하자 `validate`가 여전히 `Skill missing "name" field`를 냈다. 이로써 분기 추가가 project 항목 검사를 **통째로 무력화하지 않았음**이 증명된다. 빈 분기는 「검사 생략」이지 「검사 면제」가 아니어야 하는데, 이 실험이 그 구분을 실측으로 확인했다 | 승인 |
| 66 | 2026-09-04 08:58 | EXECUTE | DECISION | Step 10(변경이력 4문서 일괄)을 **PM이 직접 수행**했다 — PLAN §4.2가 이 Step의 agent를 「PM 직접」으로 배정했으므로 디스패치 의무 대상이 아니다. 일시는 추측하지 않고 `node ~/.opal/tools/date/date.js date`로 KST 실측 취득했다([MUST] `docs/CONVENTIONS.md` §네이밍 규칙). 4문서 = `skill-commands.md`(v1.4) · `docs/ARCHITECTURE.md`(최신순 표 최상단 삽입) · `opal-skill-wizard/SKILL.md`(v1.0) · `opal-skills-registry.json`(changelog + version 3.13.0→3.14.0). JSON 파싱 검증 통과 | Step 10 완료. **10/10 Step 완료** |
| 67 | 2026-09-04 08:58 | EXECUTE | ESCALATION | R-10 AC 「`validate` error 0건」이 **착수 시점부터 실현 불가**임을 확인해 캡틴에게 올렸다(#63 실측 근거). 남은 2건 중 `op-scenario-gate: unregistered`는 114과 무관한 선존 결함이고, `opal-skill-wizard: dangling`은 등재됐으나 `~/.opal/skills/`에 미배포라 발생한다. 배포(`install-mac.sh` 실행)는 `~/.opal/` 전체를 재배포하는 시스템 영향 행위이므로 **PM 자율로 실행하지 않는다** | 캡틴 결정 대기. EXECUTE 행 마감은 별개로 진행 |
| 68 | 2026-09-04 08:35 | EXECUTE | DECISION | 캡틴이 「내가 방금 배포함」이라고 진술 — **사실 진술은 기억이므로 E1~E4 확인 대상**([MUST] `harness/citation-rules.md` §9 (f): "소유자가 사실을 진술하면 그것은 기억이므로 E1~E4 확인 대상이다")이라 실측했다. 확인: `~/.opal/skills/opal-skill-wizard/SKILL.md` 존재(20408 bytes) / 배포 registry 등재 / 배포된 `skill-registry.js`에 `isProjectSkill` 3건 반영 / 배포된 `skill-commands.md`에 wizard 2건 반영. **진술 사실 확인됨** | 배포 확인 |
| 69 | 2026-09-04 08:35 | EXECUTE | GATE | 배포로 **`dangling` 오류 해소 확인** — `validate` errors가 2건 → **1건**(`op-scenario-gate: unregistered`만 잔존)으로 감소. `match "osw"`의 `path`가 배포본 절대경로(`/Users/lucas/.opal/skills/opal-skill-wizard/SKILL.md`)로 해석되어 **`//osw` 발동 경로가 실제로 성립**했다(TS-017 충족). 잔여 1건은 `op-scenario-gate`가 registry에 0건 등재된 선존 결함으로 114 무관 | dangling 해소. TS-017 PASS |
| 70 | 2026-09-04 08:35 | EXECUTE | ERROR | 소스-배포본 불일치 1건 검출 — `diff`가 DIFFERS를 반환했다. 차이 내용을 확인한 결과 **오직 §변경이력 표 5줄**(319줄 vs 314줄)이며, 배포가 Step 10(변경이력 기재)보다 **먼저** 실행됐기 때문이다. 실행 동작에 영향이 없는 문서 메타데이터 차이이므로 블로커가 아니나, 배포본이 소스보다 뒤처진 상태다 | 재배포 필요 사항으로 기록. 기능 영향 0 |
| 71 | 2026-09-04 23:35 | TEST | GATE | TEST 워커 완료 — **35/35 실행 시나리오 PASS, FAIL 0건**, M3 2건([SUPERVISOR] 대기). PM 독립 재확인: **미채움 슬롯 0건**(103 태스크에서 158건 누락 사고가 났던 항목 — 스크립트로 `_{채움}` 잔존을 직접 탐지), PASS 언급 43·FAIL 0, 신규 테스트 exit 0, 회귀 41 pass | 시나리오 전건 PASS 확인 |
| 72 | 2026-09-04 23:35 | TEST | GATE | 리허설의 **실 홈 무오염 직접 검증** — 워커가 실 네트워크로 `jimliu/baoyu-skills`를 clone해 설치 리허설을 수행했으므로 전역 오염 위험이 실재했다. `ls ~/.opal/community-skills/` 실측 결과 vendor 8개(anthropics·getsentry·google-labs-code·modern-python·obra·openai·trailofbits·vercel-labs)뿐이고 **baoyu·jimliu 흔적 0건**, `user-registry.json`도 **여전히 부재**. 워커의 "전후 diff 0건" 주장이 독립 관측으로 확인됐다 | 무오염 확인 |
| 73 | 2026-09-04 23:35 | TEST | ERROR | TEST 워커가 **PM 작성 오류**를 검출했다 — §4 매핑 표가 TS-023의 테스트 케이스로 `tests/test-project-registry.js:[T114/L2-GOAL]`을 인용했으나 그 케이스는 **존재하지 않는다**. PM 실측 확인: 파일의 실제 케이스는 `[T114/L2-001]`~`009`·`015`·`029` 11개이고 `023`·`GOAL` 문자열 **0건**. 내가 Block A→B 보강 중 실재하지 않는 케이스명을 적었다 | 유령 인용 확정. PM 귀책 |
| 74 | 2026-09-04 23:35 | TEST | FIX | ERROR #73 정정 — TS-023을 「**리허설 관측**(TS-030과 동일 실행, 전용 케이스 없음)」으로 바로잡고, **자동 회귀는 `[T114/L2-001]`이 담당**함을 명기했다(등재+본체 존재 → `match found:true`·`scope:"project"`·`installed:true` = 발동 성립 조건이므로 실질 커버가 성립한다). 정정 사실과 검출 주체(TEST 워커 grep)를 표 안에 남겨 추적 가능하게 했다 | 정정 완료. 커버리지 갭 해소 |
| 75 | 2026-09-04 23:35 | TEST | DECISION | 컨벤션 자동 진단을 `opal-convention-checker`에 디스패치했다 — opd STEP 4 PM Gate 체크리스트가 "컨벤션 자동 진단 PASS(changed_files 컨벤션 적용 대상 ≥1건 시 발동, Critical/High 0건)"를 요구하고 changed_files 6건이 전부 적용 대상이다. 프롬프트에 **`docs/CONVENTIONS.md`가 유일 기준이며 없는 조항으로 지적 금지**를 명시하고, 커밋 메시지 항목은 미커밋 상태이므로 「해당 없음」 처리를 지시했다 | 컨벤션 진단 디스패치 |
| 76 | 2026-09-04 23:40 | TEST | ERROR | 컨벤션 진단 Medium 1건(GC-C001) — `skill-registry.js` 상단 `@header` 「변경이력:」 라인이 이번 태스크의 +102/-2 대폭 수정에도 갱신되지 않아 최신 항목이 여전히 태스크 105의 `v1.4`였다. 근거: `docs/CONVENTIONS.md` §구현 규칙 §@header 규칙 "변경이력은 별도 표(스킬·에이전트·참조 문서) 또는 **헤더 내 변경이력 라인**으로 갱신한다". **PM 지시 누락이 원인**이다 — 나는 워커들에게 "변경이력 행 추가 대상 아님(코드 파일)"이라고 지시했는데, 이는 `.opal/AGENT.md` §금지사항 문면(스킬·에이전트·참조 문서)만 보고 CONVENTIONS의 @header 라인 규정을 놓친 것이다 | PM 귀책 확정 |
| 77 | 2026-09-04 23:40 | TEST | FIX | ERROR #76 교정 — `@header` 변경이력에 `v1.5` 항목을 추가하고 신규 함수 4개·병합 단계·3개 분기 확장을 각각 1줄로 기재했다(additive 한정·CWE-22 방어·기존 필드 보존 사실 포함). 교정 후 회귀 재확인: CLI 정상 동작 / 신규 테스트 exit 0 / 회귀 **41 pass** — 주석 변경이라 기능 영향 0이나 확인 없이 넘기지 않았다 | 교정 완료. Medium 0건 |
| 78 | 2026-09-04 23:40 | TEST | DECISION | 컨벤션 Low 1건(wizard `version: "1.0"`이 semver `X.Y.Z` 규정과 형식 불일치)·Info 1건(ARCHITECTURE 변경이력 2열 구조)은 **교정하지 않았다**. why: 진단 워커 자신이 「기존 스킬(`opal-brain`·`opal-improve`·`opal-eli5`)도 동일 2-세그먼트 패턴」·「해당 문서가 태스크 049 이전부터 써온 자체 관행」이라고 근거를 붙여 **이번 태스크가 새로 도입한 이탈이 아님**을 명시했다. 여기서 고치면 [MUST] `~/.opal/PRINCIPLES.md` §3 "Don't improve adjacent code"에 저촉되고, 프레임워크 전역 정합 문제라 별도 태스크의 영향 분석이 필요하다 | 이월. 인접 개선 회피 |
| 79 | 2026-09-04 23:41 | TEST | GATE | TEST PM Gate — **Pass**. 6항 확인: (1) TEST-SCENARIO 전 시나리오 PASS(35/35, FAIL 0) + 미채움 슬롯 0건 (2) 코드 품질 3항은 「해당 없음(설정 부재)」으로 사실대로 기재 — 없는 도구를 있다고 쓰지 않았음을 확인 (3) 보안 5항 전건 PASS(시크릿 0건·`.gitignore`·CWE-22 가드·홈 경계·파손 JSON 내성) (4) 회귀 41 pass 유지 (5) 설계 피드백 미해결 빈틈 없음 — 발견 오류 전건이 교정 또는 근거 있는 이월로 처리됨 (6) **컨벤션 자동 진단 Critical/High 0건** + Medium 1건도 교정 완료 | Pass. CLOSE 진입 승인 요청 단계 |
| 80 | 2026-09-09 20:47 | CLOSE | GATE | CLOSE 진입 게이트 통과 — 캡틴의 명시 승인 발화(「확인」)를 받아 `test.user_confirm` 행을 `--owner user`로 mark했다. `opal-harness-agentic.md` §7은 "CLOSE 진입은 예외 — 다른 Gate는 PM 자율 통과 허용이나 사용자의 확인된 지시가 없으면 진입 불가"를 규정하며, 도구도 `--auto-pass`를 `agentic_close_gate_requires_user`로 거부한다. **PM이 자율 통과하지 않았다** | 사용자 승인 확보 |
| 81 | 2026-09-09 20:48 | CLOSE | DECISION | 관련 문서 최신화(op-brain-ingest 직전 필수 절차) 판정 — `docs/PROJECT.md` 폴더 구조맵의 `opal/skills/` 카운트가 **기재 42종 vs 실측 45종**으로 어긋났다(`ls -d opal/skills/*/` 실측). 114이 1종을 추가했고 나머지 2종은 선존 오차다. **45종으로 일괄 갱신**했다 — 카운트는 단일 값이라 부분 교정이 불가능하고 43으로 쓰면 틀린 값이 된다. 선존 오차 사실을 변경이력 행에 명시해 투명성을 유지했다 | PROJECT.md 갱신 |
| 82 | 2026-09-09 20:48 | CLOSE | DECISION | `docs/PROJECT.md` 주요 컴포넌트 표에 wizard를 **추가하지 않았다**. why: 동종 컴포넌트인 `opal-skill-manager`·`opal-skill-creator`가 이 문서에 **아예 미등재**(grep 0건)이며 표가 파이프라인별로만 구성돼 있다. wizard만 넣으면 비대칭이 되고, 3개를 함께 등재하려면 새 절 신설 = 인접 개선([MUST] PRINCIPLES §3)이다 | 자연 스킵. 별건 후보 |
| 83 | 2026-09-09 20:49 | CLOSE | DECISION | DONE.md를 7절 구성으로 작성했다 — 특히 **§3(검증 2원화가 실제로 작동했다)**과 **§7(다음에 이 설계를 건드릴 사람에게)**를 별절로 뺐다. why: 이번 태스크의 최대 재사용 가치는 산출물 목록이 아니라 「작성자≠검증자 분리 지점마다 결함이 나왔고 그중 PM 귀책 3건이 독립 검증자에게 적발됐다」는 관찰이며, §7은 마커 선택·additive 계약 같은 **되돌리면 깨지는 결정**을 후속 작업자에게 전달한다 | DONE.md 생성 |
| 84 | 2026-09-09 20:49 | CLOSE | DECISION | `state-tool`이 자동 생성한 작업 히스토리 행의 핵심결과를 `memory-tool update --kind history`로 즉시 보강했다 — 도구가 "핵심결과 미기재" 리마인더를 반환했고, 미보강 시 히스토리가 제목만 남아 후속 세션에서 맥락을 잃는다. 설계 결정·검증 결과·방법론 산출 2건·사고 3건·이월을 1문단으로 압축 기재 | 히스토리 보강 완료 |
| 85 | 2026-09-09 20:50 | CLOSE | DECISION | brain ingest 디스패치 — `.opal/brain/` 존재 확인 후 concept 5·entity 2·synthesis 1 후보를 PM 판단으로 제시했다. 프롬프트에 [MUST] 4항을 주입: citation-rules §8.2(코드 식별자를 본문 주어로 쓰지 않는다)·§8.8(개발자 부록 분리)·§9(e)(brain 단독 인용 금지, E1~E4 동반)·**본문 사용자 지칭은 역할 일반어**(출처 DONE.md가 「캡틴」을 쓰더라도 지식 본문은 「사용자」로 일반화 — identity.md §영속 산출물 호칭 예외) | ingest 디스패치 |
| 86 | 2026-09-09 20:56 | CLOSE | GATE | brain ingest 완료 검토 — **Pass**. `status: completed`, 신규 8페이지(concept 5·entity 2·synthesis 1) + 갱신 1페이지. PM 독립 검증: (a) 페이지 파일 실존 확인 (b) **호칭 일반화 준수** — 신규 8페이지 전건에 「캡틴」 **0건**(출처 DONE.md는 개인 호칭을 쓰지만 지식 본문은 역할 일반어로 일반화하라는 지시가 지켜졌다) (c) `brain-tool validate` → `valid:true, violations:0` (d) `lint` 20건은 전건 무관 선존 페이지 대상 | Pass |
| 87 | 2026-09-09 20:56 | CLOSE | DECISION | 워커가 `additive-field-extension-over-schema-replacement` 페이지를 **신규 생성이 아니라 갱신**으로 처리한 판단을 승인했다 — 태스크 105가 만든 원 사례에 114의 DEC-5(`resolved_path` additive 필드) 재적용 사례를 덧붙여 **패턴이 반복 유효함을 보강**했다. 같은 개념을 두 페이지로 쪼개면 brain이 중복으로 오염된다 | 승인 |
| 88 | 2026-09-09 20:56 | CLOSE | DECISION | 워커가 `term` 타입 후보 추출을 수행하지 않은 판단을 승인했다 — 이 프로젝트 SCHEMA는 entity/concept/flow/synthesis 4종만 정의하고 `term`을 채택하지 않았음을 워커가 실측 확인했다. 스키마에 없는 타입을 만들면 brain 구조가 깨진다 | 승인 |
| 89 | 2026-09-09 21:10 | CLOSE | ERROR | **채번 오류 검출** — 캡틴의 `git pull` 시도가 divergent branches로 실패한 것을 계기로 실측한 결과, 로컬 `.opal/MEMORY.json`이 `last_task_number: 105`로 stale인 상태에서 `task-number --bump`가 **106**을 발급했으나 **원격은 이미 113**이었고 106·107·108·109가 사용 중이었다. 원격에 `106-260904-opd-코드맵-스킬신설-하네스개정`이 실재한다. 즉 **태스크 번호가 중복된 채 커밋까지 진행**됐다. 근본 원인은 채번 도구가 아니라 **원격 동기화 없이 채번한 작업 순서**다 | 재채번 필요 확정 |
| 90 | 2026-09-09 21:10 | CLOSE | DECISION | 충돌 범위를 merge-base(`69f5ce1`) 기준으로 실측했다 — **핵심 구현 2파일(`skill-registry.js`·`skill-commands.md`)은 원격 변경 0건**이라 충돌 없음을 먼저 확인했고, 나머지 5파일(`opal-skills-registry.json` 2 / `ARCHITECTURE.md` 5 / `PROJECT.md` 4 / `MEMORY.json` 6 / `brain/index.md` 4)만 충돌 대상으로 좁혔다. 이 확인이 「재작업이냐 병합이냐」 판단의 근거였다 | 충돌 범위 확정 |
| 91 | 2026-09-09 21:15 | CLOSE | FIX | 캡틴 승인(권고안)으로 **커밋 해제 → pull → 재채번 → 재커밋** 순서를 실행했다. `backup-106-preRenumber` 브랜치로 안전망을 먼저 만든 뒤 `reset --soft HEAD~2` → `stash --include-untracked` → `pull --ff-only`(fast-forward 성공) → `stash pop`. 충돌 6건 해소 — 도구 관리 파일 4건(`MEMORY.json`·`brain/index.md`·`brain/log.md`·`PROJECT.md`)은 **원격본을 채택한 뒤 도구로 재생성**하고, `ARCHITECTURE.md`는 양쪽 변경이력 행을 모두 보존, `opal-skills-registry.json`은 원격본(v3.15.0, `opal-code-map-builder` 포함)을 취한 뒤 wizard를 재삽입해 v3.16.0으로 올렸다. why: 도구가 생성하는 파일을 손으로 병합하면 도구 재실행 때 다시 어긋난다 | 충돌 전건 해소 |
| 92 | 2026-09-09 21:20 | CLOSE | FIX | 106 → 114 재채번 — 폴더 rename + **문서·코드·brain 23파일 145건 + 잔여 15파일 32건 치환**. 무분별 치환을 피하려고 **출현 형태를 먼저 열거**(`T106/` 67 · `task:106` 29 · `태스크 106` 22 · `(106)` 17 · 경로 6 · `Task 106` 1)하고 줄번호 인용에 106 포함 형태가 없음을 확인한 뒤 패턴별로 치환했다. 이후 `#task-106` 태그(하이픈 형태)를 놓친 것을 index 재생성에서 발견해 `(?<![0-9])106(?![0-9])` 정규식으로 잔여를 마감했다. 테스트 케이스명도 `[T106/L2-NNN]` → `[T114/L2-NNN]`으로 일관 변경 | 재채번 완료. 잔존 0건 |
| 93 | 2026-09-09 21:22 | CLOSE | GATE | 재채번 후 재검증 — **신규 테스트 exit 0 (11 pass)**, **회귀 41 pass**, `match "osw"` → `found:true`. `validate`는 이제 `op-scenario-gate` 오류가 사라졌고(**원격 111이 이미 등재 복구** — 우리 이월 R-5가 해소됐다) 잔여 1건은 `opal-code-map-builder: dangling`으로 **원격 106의 미배포 상태**다. 우리와 동일한 「등재됐으나 미배포」 패턴이며 재배포로 해소된다 — 114 유발 오류 0건 유지 | Pass |
| 94 | 2026-09-09 21:22 | CLOSE | IMPROVE | **교훈: 채번 전 원격 동기화가 선행되어야 한다.** `memory-tool task-number --bump`는 로컬 `.opal/MEMORY.json`만 보므로, 원격이 앞서 있으면 중복 번호를 조용히 발급한다. 도구는 정상 동작했고 결함은 절차에 있다 — TASK 단계 채번 직전에 `git fetch` + 원격 `MEMORY.json`의 `last_task_number` 대조를 넣으면 구조적으로 막힌다. 프레임워크 개선 후보로 이월 | 이월 등록 |
